---
context: default
phase: 110 (next — Human-Gated Adoption + Boundary Re-Verification + Ship/No-Ship REPORT)
milestone: v2.3
task: resume at Phase 110, then milestone close
status: in_progress
last_updated: 2026-06-24
---

# Resume: v2.3 "Task-Success-Driven Skill Optimization" — at Phase 110 (3/4 done)

**107 + 108 + 109 are shipped, verified (status: passed), and committed.** Resume
by running Phase 110, then the milestone-close lifecycle (audit → complete →
cleanup).

## State
- **v2.3 = 3/4 complete.** ROADMAP 107/108/109 ticked; REQUIREMENTS traceability
  AGENT-01/02/03 + ORACLE-01/02 + TUNE-02/03/04 = Complete. Remaining: ADOPT-03,
  ADOPT-04 (Phase 110).
- **Phase 109** (`tools/dspy-tune/`): `grade_swebench.py` (upstream
  `swebench==4.1.0` harness on Podman, parity mirror of `harness.go` via
  `golden/swebench_argv.json` + `argv_parity_test.go`; FAIL_TO_PASS+PASS_TO_PASS
  contract; 0-tests=GradeError; dataset-org allowlist; env allowlist with
  DOCKER_HOST→podman socket). `attribution.py` (ON/OFF delta, val_size>50 +
  positive-delta ship gate, MeteredLLM, `render_report`). 51 py tests green; go
  parity + `make vet` green; zero new Go deps.

## CRITICAL conventions (unchanged)
1. **ALL Python uses `uv`/`uvx` + venv** — `cd tools/dspy-tune && uv run pytest`.
2. **Containers = Podman**. SWE-bench: `podman system service --time=0 &` +
   `DOCKER_HOST=unix://$XDG_RUNTIME_DIR/podman/podman.sock`. NEVER "Docker → blocked".
3. **Anti-vacuity**: every new gate ships a break-the-invariant → assert-RED test,
   mutation-confirmed BEFORE verify.
4. **Inline execution** (uv), branch `docs/readme-langsupport-lineage` — user
   confirmed both at the 108→109 resume.

## Phase 110 plan (ADOPT-03 + ADOPT-04)
- **ADOPT-03**: human-gated adoption ONLY via `helix-refgen --check` — the
  optimizer writes only git-ignored `output/`; never auto-writes SKILL.md /
  reference.md; `## Decision matrix` anchor + SKILL size cap preserved.
- **ADOPT-04 (primary owner)**: re-verify single-binary / no-runtime-Python
  end-to-end: zero new Go deps, no `helix`→Python shell, off `go.mod`/`helix
  setup`/default `go test ./...`/merge path; `make vet` (`toolsquarantine`) green;
  `grep -E 'skills/helix|reference\.md' optimize.py == 0`.
- **Ship/no-ship REPORT**: record the ON/OFF delta, val_size, per-arm cost +
  explicit adopt/no-ship verdict. The `attribution.render_report` machinery (109)
  produces it; the live run no-ships by design until the corpus clears val_size>50
  (TUNE-FUT-01) — surface that honestly, don't fabricate numbers.
- **Anti-vacuity**: hand-edit `reference.md` out of sync with the generator ⇒
  `helix-refgen --check` exits non-zero (break-the-invariant → assert-RED).
- Then milestone lifecycle: `/gsd-audit-milestone` → `/gsd-complete-milestone` →
  `/gsd-cleanup`. Re-acknowledge the 3 stale pre-existing quick-tasks
  (260414-e5n / 260617-j29 / 260617-t7x) as already-done.

## Next action
Resume inline: `/gsd-plan-phase 110` (or build inline like 107/108/109). Grounding
files: `cmd/helix-refgen/` (the `--check` adoption gate), `tools/dspy-tune/
{attribution.py,optimize.py}` (REPORT renderer + optimizer-writes-only-output
boundary), `internal/cli/skills/helix/SKILL.md` (the human-reviewed surface).
