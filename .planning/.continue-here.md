---
context: default
phase: none (v2.3 milestone creation — not yet written)
task: 4
total_tasks: 8
status: paused
last_updated: 2026-06-24T07:43:31.784Z
---

# Resume: v2.3 "Task-Success-Driven Skill Optimization" (TUNE-FUT-02)

Paused at 66% context, at a clean checkpoint — **nothing for v2.3 is written yet**, so there is no partial state to repair. v2.2 is fully shipped and closed.

<current_state>
- **v2.2 = DONE & durable:** all 4 phases shipped, audit PASSED (7/7 reqs), archived to `.planning/milestones/v2.2-*`, tagged `v2.2` (local, **not pushed**), `.planning/phases/` empty, working tree clean.
- **v2.3 = just started:** `/gsd-new-milestone` ran step 1 (init) only — `research_enabled=true`, agents installed, continues phase numbering from 106, no seeds, `current_milestone` still reads v1.0 in init (stale; the milestone-switch SDK write was NOT run yet). No PROJECT.md/STATE.md/REQUIREMENTS.md/ROADMAP.md changes for v2.3 exist.
</current_state>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state (decisions, blockers, next_action).
2. `CLAUDE.md` > Technology Stack — **Podman** is the container engine (committed `3e6168dc`); do NOT say "Docker not installed → blocked".
3. `.planning/milestones/v2.2-REQUIREMENTS.md` — **TUNE-FUT-02** (the v2.3 driver) and TUNE-FUT-01 verbatim.
4. `.planning/milestones/v2.2-phases/106-.../106-REPORT.md` — why v2.2's choice_rate tuning concluded no-ship (corpus too small; proxy gameable) → motivates the task-success pivot.
5. Memory: `helix-container-engine-podman.md`, `helix-antivacuity-mutation-test-guards.md`.

## Critical Anti-Patterns
| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| "Docker not installed → blocked" | I wrongly reported SWE-bench blocked; the env uses **Podman 5.4.2** and `bench/container` auto-detects it | advisory | Check `command -v podman` first; see CLAUDE.md |
| Vacuous break-the-invariant guards | plan-checker passes tautological/wrong guards (v2.2 104 Guard B `x==x`, 106 parity bug); only code-review/verifier mutation-testing catches them | advisory | Fold code-review+fix BEFORE verify; mutation-test every guard |
| choice_rate as the optimization target | gameable first-command proxy ("always say helix"); v2.2 concluded no-ship on it | advisory | v2.3 uses **agent task-success** on real benches instead |

<decisions_made>
- v2.3 = TUNE-FUT-02: optimize SKILL.md/steering for **agent task-success** (Aider polyglot + SWE-bench via Podman), not choice_rate.
- Build a **net-new DeepSeek/OpenAI tool-using agent** that drives the `helix` CLI (NOT Claude Code — that needs ANTHROPIC_API_KEY, which is unset; DEEPSEEK_API_KEY + OPENAI_API_KEY ARE set).
- DeepSeek primary optimizer+agent LM, OpenAI fallback. Human-gated adoption only via refgen + `helix-refgen --check`.
</decisions_made>

<blockers>
- The DeepSeek/OpenAI tool-using agent does not exist yet — it is the foundational v2.3 phase. Existing agent (`bench/runtime/subprocess/claude.go`) is Claude-Code-only.
- SWE-bench needs Podman's docker-compat socket (`podman system service` + `DOCKER_HOST`); Aider polyglot does not (toolchains present: go/python3/java/node/cargo/g++). First cut can be Aider-only.
</blockers>

## Two questions to settle at the START of the fresh run (so research targets them)
1. **Agent transport:** helix **CLI subprocess verbs** (simplest, = product surface) vs internal **MCP `tools/call`** wire.
2. **SWE-bench scope for v2.3 first cut:** Aider-polyglot-only, or also stand up `podman system service` + `DOCKER_HOST` for the upstream swebench harness.

## Infrastructure State
- Branch: `docs/readme-langsupport-lineage`. Tag `v2.3` not created. `v2.2` tag local (unpushed).
- Reusable building blocks present: `bench/datasets/aider-polyglot/` (loader), `bench/runtime/aider_edit_agent.go` + `aider_edit_cell.go` (edit-bench harness), `tools/dspy-tune/` (DSPy GEPA harness + parity-pinned scorer — metric currently choice_rate, to be rewired to task-success), `bench/container` (engine-agnostic, podman-aware).

<next_action>
Start with: `/clear`, then re-run `/gsd-new-milestone v2.3 Task-Success-Driven Skill Optimization — build a net-new DeepSeek/OpenAI tool-using agent driving the helix CLI; GEPA objective = agent task-success on Aider polyglot (+SWE-bench via Podman); human-gated adoption via refgen + helix-refgen --check; DeepSeek primary, OpenAI fallback; containers=Podman.` Answer the 2 questions above, then let it run research → requirements → roadmap → `/gsd-autonomous`.
</next_action>
