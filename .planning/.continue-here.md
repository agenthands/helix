---
context: default
phase: 109 (next — SWE-bench Oracle via Podman + ON/OFF Attribution-Delta)
milestone: v2.3
task: resume at Phase 109
status: paused
last_updated: 2026-06-24
---

# Resume: v2.3 "Task-Success-Driven Skill Optimization" — at Phase 109 (2/4 done)

Paused cleanly at a phase boundary. **107 + 108 are shipped, verified (status: passed), and committed; tree clean.** Resume by running Phases 109 → 110.

## State
- **v2.3 = 2/4 complete.** ROADMAP 107/108 ticked; REQUIREMENTS traceability AGENT-01/02/03 + ORACLE-01 + TUNE-02/03 = Complete. Remaining: ORACLE-02, TUNE-04 (Phase 109), ADOPT-03, ADOPT-04 (Phase 110).
- **Phase 107** (`tools/dspy-tune/agent/`): DeepSeek/OpenAI ReAct agent driving `helix <verb>`; loud-fail on missing key; steering ON/OFF; 7 hermetic tests.
- **Phase 108** (`tools/dspy-tune/`): honest Aider task-success oracle (`grade_aider.py`, parity-pinned to `bench/datasets/aider-polyglot/loader.go` via `golden/aider_native_cmd.json` + `native_cmd_parity_test.go`; 0-tests=hard GradeError; Rust `--include-ignored`), `sandbox.py` (gold-test restore), `taskmetric.py` (task-success reward), `optimize.py` (choice_rate→diagnostic; strict `val_size>50` gate, no-ships today). 25 py tests green.

## CRITICAL conventions (do not repeat the earlier mistake)
1. **ALL Python uses `uv`/`uvx` + venv** — never bare `python`/`pip`. Recorded in CLAUDE.md ("## Python") + memory `helix-python-uv-venv-rule`. Run tests via `cd tools/dspy-tune && uv run pytest`.
2. **Containers = Podman** (memory `helix-container-engine-podman`). For SWE-bench: `podman system service --time=0 &` + `DOCKER_HOST=unix://$XDG_RUNTIME_DIR/podman/podman.sock`. NEVER "Docker → blocked".
3. **Anti-vacuity**: every new gate ships a break-the-invariant → assert-RED test, mutation-confirmed BEFORE verify (memory `helix-antivacuity-mutation-test-guards`).
4. **Subagent roster CHANGED mid-milestone**: the `gsd-executor`/`gsd-planner`/`gsd-verifier`/etc. agents are GONE (replaced by `engineer`/`qa`/`architect` + `anvil-*` skills incl. `anvil-autonomous`). Phases 107/108 were finished INLINE per user choice. For 109/110: continue inline with uv, OR adopt the new anvil framework — confirm with the user.

## Phase 109 plan (ORACLE-02 + TUNE-04)
- `grade_swebench.py`: wrap the upstream `swebench==4.1.0` harness via Podman (`DOCKER_HOST`→podman.sock); enforce FAIL_TO_PASS + PASS_TO_PASS; pin the dataset id to the correct org and assert fetch-resolution + task-count>0 (watch the `princeton-nlp/` vs `SWE-bench/` org drift). Pluggable behind `taskmetric.py`.
- ON/OFF attribution delta: `delta = success(ON) − success(OFF)` on the held-out split + per-arm cost, into a ship/no-ship REPORT.
- Anti-vacuity: hermetic fake-harness test asserts exact argv + env allowlist + dataset pin; live leg gated, skips offline but FAILS loudly on a *requested* real run that produces no result (HELIX_BIN fail-not-skip class).
- Boundary: `swebench`/`openai` stay dev-venv Python pins; zero new Go deps; `make vet` green.

## Phase 110 plan (ADOPT-03 + ADOPT-04)
- Human-gated adoption only via `helix-refgen --check` (optimizer writes only git-ignored `output/`); ship/no-ship REPORT (ON/OFF/Δ/val_size). Re-verify the single-binary/no-runtime-Python boundary end-to-end. Anti-vacuity: hand-edit `reference.md` out of sync → `--check` exits non-zero.
- Then milestone lifecycle: audit → complete → cleanup.

## Next action
Resume inline: continue Phase 109 (create `.planning/phases/109-.../109-CONTEXT.md` skip_discuss, then build grade_swebench.py + attribution + tests with uv/Podman). The grounding files: `bench/evaluators/swebench/harness.go` (DOCKER_HOST allowlist + dataset pin to mirror), `tools/dspy-tune/{taskmetric.py,grade_aider.py}` (the metric the SWE-bench grader plugs into).
