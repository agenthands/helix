---
context: default
phase: none (v2.4 shipped + archived)
status: paused
last_updated: 2026-06-25T08:19:42.442Z
---

# Resume here — post-v2.4 root-cause investigation

> No active phase. **v2.4 is fully shipped, committed, and tagged `v2.4` (local, not pushed).**
> What's paused is the *investigation* into why the v2.4 real run scored so low — which
> defines the next milestone. Read this before starting it.

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Trusting aggregate scores without reading transcripts | v2.4 reported "agent weak (3/51)" from pass/fail alone; inspecting transcripts showed the agent often makes **0 tool calls** and answers in prose. | blocking | Always dump per-step agent transcripts (verb/argv/exit) before believing a success-rate. |
| Hermetic fakes that hide integration gaps | v2.3's fake `run_verb`/fake LLM hid 3 real defects (argv, workspace activation, GEPA thread). | blocking | Add at least one real-CLI smoke per integration surface; fakes are necessary but never sufficient. |
| Calling a measured delta a "verdict" when the agent doesn't use the tool | v2.4 +0.0392 is noise between two non-tool-using configs. | advisory | A delta is only meaningful once the agent demonstrably engages helix on most tasks. |

<current_state>
v2.4 (Corpus Growth & Real Optimization Verdict) shipped + archived (`.planning/milestones/v2.4-*`, tag `v2.4`). The pipeline now runs end-to-end on REAL code (real `helix` CLI on real Aider exercises; real SWE-bench/Podman). But the **root-cause investigation** (this session) found the agent barely uses helix, so the "SHIP-by-rule" verdict is hollow. Working tree clean apart from this handoff.
</current_state>

<completed_work>
- v2.4: 4 phases (111–114), 10/10 reqs, audit PASSED. Real run: ON/OFF delta +0.0392 (3/51 vs 1/51, val_size 51, $0.23); SWE-bench gold 2/2 on Podman.
- Fixed 3 v2.3 integration gaps: verb argv (`--flags`); `helix activate`→`activate_project` (real product bug, E2E test `TestCLI_ActivateEnablesFileVerb`); GEPA candidate→agent thread.
- **Root-cause investigation** across 6 real python tasks (transcripts inspected).
</completed_work>

<root_cause_findings>
The 3/51 is NOT "weak agent" — it's "agent doesn't use the tools + wrong steering + no feedback loop":
1. **~40% of tasks: 0 tool calls** (`reason=done, steps=0`) — agent answers the exercise in **prose** and quits (e.g. beer-song/bottle-song printed lyrics as text, never edited the file).
2. **System prompt / `Solve` signature too weak** — never forces: *edit the named solution file; success = the hidden tests pass; do NOT reply in prose; not done until implemented.*
3. **No feedback loop** — no run-tests / `get-diagnostics` in the ReAct loop, so the agent declares `done` blindly (bowling edited via fuzzy-edit, wrong, said done).
4. **Verbs still error-prone** — book-store burned the tool-error budget (5 failed verb calls).
5. **ON steering is the wrong artifact** — the 6,923-char `SKILL.md` is a grep-vs-helix routing matrix, not solve-by-editing guidance.
6. **GEPA tuning is a no-op** — `AgentProgram.forward` emits no predictor LM trace, so reflective mutation proposes nothing. No real tuning happened.
</root_cause_findings>

<remaining_work>
Next milestone (recommend **TUNE-FUT-06**), NOT a quick patch:
1. Task-solving system prompt: name the solution file, "success = hidden tests pass", forbid prose, "not done until implemented".
2. Feedback loop: wire a run-tests / `get-diagnostics` check into the ReAct loop so the agent knows when it's actually done.
3. Harden verb-arg usage so edits don't burn the error budget.
4. Make GEPA actually evolve: rebuild as a real `dspy` agent module that emits a reflectable trace; tune THAT task-solving instruction (not the routing SKILL.md).
5. Then re-run attribution for a real delta. (TUNE-FUT-05: larger SWE-bench K; add a significance test to `attribution.decide_ship`.)
Optional quick win first: run full `go test ./...` (I only ran `go test ./internal/cli` + `make vet` + hermetic Python this milestone).
</remaining_work>

<decisions_made>
- v2.4 is REPORT-only; do NOT adopt steering (margin is noise AND agent barely uses tools).
- `helix activate` fix kept (real product bug, user-approved).
</decisions_made>

<blockers>
- Agent does not reliably engage helix (prose answers / verb errors) → attribution is noise. Needs the harness rewrite above.
- GEPA optimization is a no-op (no candidate evolution).
</blockers>

## Required Reading (in order)
1. `tools/dspy-tune/REPORT.md` (v2.4 section) — the honest verdict + caveats.
2. `.planning/milestones/v2.4-MILESTONE-AUDIT.md` — what shipped + follow-ons (TUNE-FUT-05/06).
3. Memory `helix-v24-pipeline-gaps-and-activate-bug.md` — the 3 gaps + activate bug + cost reality.
4. `tools/dspy-tune/{agent/react.py, agent/tools.py, optimize.py, runlib.py, run_real.py}` — the harness to rewrite.

## Infrastructure State
- Daemon runtime dir `/tmp/helix-1000/` and `$XDG_RUNTIME_DIR/podman/` must be `mkdir -p`'d before runs (daemon/podman-service don't create them).
- Corpus is git-ignored at `tools/dspy-tune/corpus/` (103 tasks; regen via `uv run python build_corpus.py --tracks go,python,rust --check`).
- DeepSeek key works (len-16 but valid); OpenAI fallback key is INVALID (401) — primary-only.
- Inspect scripts left at `/tmp/inspect_agent.py`, `/tmp/inspect5.py` (full-transcript dumpers — reuse them).

<context>
The plumbing is real and exercised end-to-end (the genuine v2.4 win), but the agent is not actually solving by editing — it short-circuits to prose or fumbles verbs. The next milestone is about making the agent a competent tool-using solver with a feedback loop, THEN tuning. Do not adopt any steering until the agent demonstrably engages helix on most tasks and a re-run shows a robust, significant delta.
</context>

<next_action>
Start fresh (`/clear`), then `/gsd-new-milestone` for TUNE-FUT-06 (agent harness: task-solving prompt + feedback loop + verb-arg hardening + real GEPA module). Optionally run full `go test ./...` first to close the test gap.
</next_action>
