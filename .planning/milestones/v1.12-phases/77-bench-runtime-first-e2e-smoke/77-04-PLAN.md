---
phase: 77-bench-runtime-first-e2e-smoke
plan: 04
type: execute
wave: 4
depends_on: ["77-03"]
files_modified:
  - bench/runtime/matrix.go
  - bench/runtime/matrix_test.go
  - bench/runtime/subprocess/claude.go
  - cmd/helix-bench/main.go
  - cmd/helix-bench/run_cmd_test.go
autonomous: true
requirements: [BENCH-04, BENCH-05]
must_haves:
  truths:
    - "helix-bench run wires --benchmarks/--modes/--tasks/--parallel/--out/--agent/--helix-bin flags and expands (benchmark x mode x task) into cells (criterion #1)"
    - "The matrix expander rejects path-traversal ids and bounds concurrency by --parallel (criterion #1, no port collisions by construction)"
    - "main() is the only os.Exit site; RunE returns an error -> exit 1; exit 0 iff >=1 task succeeded (helix-eval exit semantics)"
    - "The --agent=claude branch is wired (subprocess.claude) and locally runnable but NOT a CI gate (D-01)"
  artifacts:
    - path: "cmd/helix-bench/main.go"
      provides: "Implemented run subcommand replacing notYetImplemented(run) at line 89"
      contains: "RunE"
    - path: "bench/runtime/matrix.go"
      provides: "(benchmark x mode x task) -> cells expander, --parallel-bounded"
      contains: "func "
    - path: "bench/runtime/subprocess/claude.go"
      provides: "Wired-not-gating claude CLI spawn branch (D-01)"
      contains: "agent"
    - path: "cmd/helix-bench/run_cmd_test.go"
      provides: "flag plumbing + exit semantics test (mirror helix-eval)"
      contains: "run"
  key_links:
    - from: "cmd/helix-bench/main.go"
      to: "bench/runtime.RunCell"
      via: "run subcommand drives cells from the expanded matrix"
      pattern: "RunCell"
    - from: "bench/runtime/matrix.go"
      to: "bench/runtime.RunCell"
      via: "per-cell dispatch bounded by --parallel"
      pattern: "parallel"
    - from: "bench/runtime/subprocess/claude.go"
      to: "internal/eval/agent.Agent"
      via: "claude CLI spawn (wired-not-gating)"
      pattern: "agent\\."
---

<objective>
Turn the `notYetImplemented("run")` stub at `cmd/helix-bench/main.go:89` into a working `run`
subcommand: wire the six+1 flags (`--benchmarks`/`--modes`/`--tasks`/`--parallel`/`--out`/`--agent`/
`--helix-bin`), expand `(benchmark x mode x task)` into cells, dispatch each through Plan 03's
`RunCell` bounded by `--parallel`, and apply helix-eval's single-exit semantics (exit 0 iff >=1 task
succeeded). Also wire the `--agent=claude` branch in `bench/runtime/subprocess` (D-01: locally
runnable, NOT a CI gate).

Purpose: This is the operator-facing surface that criterion #1 grades
(`helix-bench run --benchmarks=toolbench-go --modes=your_agent_full --tasks=<one>`). It is CLI +
process orchestration glue, so standard `type: execute` (mirrors `cmd/helix-eval`).

Output: `bench/runtime/matrix.go` (+ test), `bench/runtime/subprocess/claude.go`, the implemented
`cmd/helix-bench/main.go` run subcommand, and `cmd/helix-bench/run_cmd_test.go`.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 04 slice)
- **New file `bench/runtime/matrix.go`** — `ExpandMatrix(benchmarks, modes, tasks []string) ([]Cell,
  error)` (validateTaskID-checked) and a bounded runner (e.g. `RunMatrix(ctx, cells []Cell, parallel
  int, ...) (Summary, error)`) that dispatches `RunCell` under a `parallel`-sized semaphore.
- **New file `bench/runtime/subprocess/claude.go`** — the `--agent=claude` spawn branch delegating to
  `internal/eval/agent.Agent.Run` + `WriteMCPConfig` (wired-not-gating, D-01); surfaces
  `agent.ErrClaudeNotFound` cleanly.
- **Modified `cmd/helix-bench/main.go`** — `newRunCmd()` body implemented (replaces
  `notYetImplemented("run")` at line 89); flags `--benchmarks` (default `toolbench-go`), `--modes`
  (default `[your_agent_full]`), `--tasks`, `--parallel` (default 1), `--out` (default `bench/reports`),
  `--agent` (default `scripted`), `--helix-bin` (default `helix`); default `run-id` =
  `time.Now().UTC().Format("20060102T150405Z")`.
- **New file `cmd/helix-bench/run_cmd_test.go`** — `TestRunSubcommand...` mirroring helix-eval:
  cobra `SetArgs`, tolerate missing `helix`, assert artifacts best-effort.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-bench-runtime-first-e2e-smoke/77-CONTEXT.md
@.planning/phases/77-bench-runtime-first-e2e-smoke/77-RESEARCH.md
@.planning/phases/77-bench-runtime-first-e2e-smoke/77-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Matrix expander (--parallel bounded) + claude branch wiring (criterion #1, D-01)</name>
  <files>bench/runtime/matrix.go, bench/runtime/matrix_test.go, bench/runtime/subprocess/claude.go</files>
  <read_first>
    - `internal/eval/runner/runner.go:304` `RunMatrix` — the per-cell concurrency template: a `sem` channel bounded by `MaxParallel`; mirror this bound for `--parallel` (no TCP ports => no collisions by construction, D-06).
    - `internal/eval/runner/runner.go:65` `validateTaskID` — reject `../`/absolute ids; apply to benchmark/mode/task before joining into cell keys.
    - `bench/runtime/cell.go` (Plan 03) — `RunCell(ctx, cfg)` is the per-cell entry the matrix dispatches.
    - `internal/eval/agent/claude.go:62,76,99` + `internal/eval/agent/mcpconfig.go` — `agent.Agent.Run` argv (`--bare --strict-mcp-config --output-format stream-json`), `cleanEnv` allowlist, `WriteMCPConfig`/`MCPConfig`, `ErrClaudeNotFound` (claude.go:18) — the claude branch reuses these; do NOT re-roll the argv/env.
    - `77-RESEARCH.md` Pattern (claude wired-not-gating) + Environment Availability (claude/API key NOT required for CI).
  </read_first>
  <action>
    Create `bench/runtime/matrix.go`: implement `ExpandMatrix(benchmarks, modes, tasks []string)
    ([]Cell, error)` that produces the cartesian product of (benchmark x mode x task) cells, applying
    a `validateTaskID`-style check (reject `../`, absolute paths) to every id before it becomes a path
    segment (V5/V12). Each `Cell` carries `{Benchmark, Mode, Task}` (run_id is shared across the run).
    Implement a bounded dispatcher (e.g. `RunMatrix(ctx, cells, parallel int, runCfg) (Summary,
    error)`) that runs `RunCell` per cell under a `sem`-channel bounded by `parallel` (mirror
    runner.go:304), aggregating per-cell pass/fail into a `Summary` whose `>=1 success` drives the
    process exit. Write `bench/runtime/matrix_test.go` asserting: expansion count = |benchmarks| x
    |modes| x |tasks|; a `../`-containing id returns an error; the dispatcher respects the `parallel`
    bound (e.g. a counter that never exceeds `parallel` concurrent cells).
    Then create `bench/runtime/subprocess/claude.go`: wire the `--agent=claude` branch by delegating to
    `internal/eval/agent.Agent` (`NewAgent` + `Run` + `WriteMCPConfig` pointing the helix forwarder at
    the per-cell socket). This branch is D-01 wired-not-gating: it must compile and be locally runnable
    via `--agent=claude` but is NEVER the CI default; surface `agent.ErrClaudeNotFound` as a clean
    error (no panic) when `claude` is absent. Do not add it to any default code path that the scripted
    smoke exercises.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run Matrix -count=1 && go build ./bench/runtime/... && go vet ./bench/runtime/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/runtime/ -run Matrix -count=1` passes; `go build ./bench/runtime/...` + `go vet ./bench/runtime/...` succeed.
    - `ExpandMatrix` yields |benchmarks|x|modes|x|tasks| cells and rejects `../`/absolute ids with an error.
    - The dispatcher never exceeds `parallel` concurrent cells (asserted by the test).
    - `subprocess/claude.go` compiles, delegates to `internal/eval/agent.Agent`, and returns `ErrClaudeNotFound` cleanly when claude is absent — it is NOT reached by the scripted path.
    - `gofmt -l bench/runtime/matrix.go bench/runtime/matrix_test.go bench/runtime/subprocess/claude.go` empty.
  </acceptance_criteria>
  <done>The matrix expands + dispatches cells bounded by --parallel with path-traversal-safe ids, and the claude branch is wired-not-gating (D-01).</done>
</task>

<task type="auto">
  <name>Task 2: Implement helix-bench run subcommand + flags + exit semantics + test (BENCH-05 wiring, criterion #1)</name>
  <files>cmd/helix-bench/main.go, cmd/helix-bench/run_cmd_test.go</files>
  <read_first>
    - `cmd/helix-bench/main.go:84-91` — the `newRunCmd()` stub returning `notYetImplemented("run")` (line 89) to REPLACE; `:26-44` `main()` is the only `os.Exit` site; `:30-38` the `verify-tos` special-case dispatch BEFORE `newRootCmd()` — leave it INTACT, swap only `newRunCmd`'s body.
    - `cmd/helix-eval/main.go:58-160` — the run subcommand analog: `:60-93` flag var block + `Flags()`; `:104` default run-id `time.Now().UTC().Format("20060102T150405Z")` (git SHA captured separately in provenance); `:93` the `--helix-bin` flag; `:31-36`+`:158-160` single-exit (RunE returns error -> exit 1).
    - `cmd/helix-eval/run_cmd_test.go:16-79` `TestRunSubcommandWiresThemAll` — build a synthetic fixture in `t.TempDir()`, `root.SetArgs([]string{"run", ...})`, tolerate a missing `helix` binary (`_ = err`), assert the durable out dir + `result.v2.json` appear best-effort (run_cmd_test.go:50-52 tolerance).
    - `bench/runtime/matrix.go` (Task 1) — `ExpandMatrix` + `RunMatrix` the subcommand calls.
    - `77-PATTERNS.md` § "cmd/helix-bench/main.go (MODIFIED)" — exact flag plumbing + divergence (keep verify-tos dispatch).
  </read_first>
  <action>
    Replace the body of `newRunCmd()` in `cmd/helix-bench/main.go` (do NOT touch `main()`'s verify-tos
    dispatch or the other subcommands). Add a flag var block + `cmd.Flags()` mirroring helix-eval:
    `--benchmarks` (StringVar, default `"toolbench-go"`), `--modes` (StringArrayVar, default
    `["your_agent_full"]`), `--tasks` (StringArrayVar, default nil), `--parallel` (IntVar, default 1),
    `--out` (StringVar, default `"bench/reports"`), `--agent` (StringVar, default `"scripted"`),
    `--helix-bin` (StringVar, default `"helix"`). In `RunE`: compute `runID :=
    time.Now().UTC().Format("20060102T150405Z")` (capture git SHA separately into result provenance,
    NOT concatenated into run_id — RESEARCH run_id note); call `runtime.ExpandMatrix(benchmarks, modes,
    tasks)`; if `--tasks` is empty, default to the seed task discovered under
    `bench/datasets/<benchmark>/` (so `--tasks=<one>` is optional for the smoke but supported); call
    `runtime.RunMatrix(ctx, cells, parallel, cfg)` with `agent`, `out`/runID, `helixBin`; return a
    non-nil error iff ZERO tasks succeeded (so `main()` exits 1) — exit 0 iff >=1 task succeeded
    (helix-eval semantics). Keep `SilenceUsage` behavior consistent with the root (errors are gate
    failures, not usage mistakes). Then write `cmd/helix-bench/run_cmd_test.go` mirroring helix-eval's
    `TestRunSubcommandWiresThemAll`: invoke the root with `SetArgs([]string{"run", "--benchmarks",
    "toolbench-go", "--modes", "your_agent_full", "--out", t.TempDir(), "--helix-bin", "helix"})`,
    tolerate a missing `helix` (`_ = err`), and assert the durable out dir + `result.v2.json` appear
    best-effort. Verify all flags are registered (e.g. via `cmd.Flags().Lookup(name) != nil`).
  </action>
  <verify>
    <automated>go test ./cmd/helix-bench/ -count=1 && go build ./cmd/helix-bench && go vet ./cmd/helix-bench</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/helix-bench/ -count=1` passes; `go build ./cmd/helix-bench` succeeds; `go vet ./cmd/helix-bench` clean.
    - `helix-bench run --help` lists `--benchmarks`/`--modes`/`--tasks`/`--parallel`/`--out`/`--agent`/`--helix-bin` (verified by the test's flag-lookup assertions).
    - `notYetImplemented("run")` is gone (grep `notYetImplemented` in main.go matches only the other unimplemented subcommands, NOT run).
    - The `verify-tos` special-case dispatch in `main()` and the other 4 subcommands are unchanged.
    - RunE returns an error (exit 1) when zero tasks succeed; exit 0 when >=1 succeeds.
    - `gofmt -l cmd/helix-bench/` empty.
  </acceptance_criteria>
  <done>helix-bench run is implemented with the full flag set, matrix dispatch, and single-exit semantics; run_cmd_test asserts wiring + best-effort artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags (--benchmarks/--modes/--tasks/--out) -> filesystem cell keys | operator-supplied ids become path segments |
| --agent=claude -> claude CLI subprocess + API key env | the wired-not-gating path spawns an external process with credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-10 | Tampering | matrix expander id -> path join | mitigate | `validateTaskID`-style reject of `../`/absolute benchmark/mode/task ids before any join (V5; runner.go:65) |
| T-77-11 | Info disclosure | dev credentials leaking into the claude subprocess env | mitigate | reuse `agent.cleanEnv` allowlist (claude.go:62) for the claude branch; do not pass the full os env |
| T-77-12 | DoS | unbounded concurrent cells exhausting host | mitigate | `--parallel`-bounded `sem` dispatch (mirror runner.go:304); default 1, criterion target 4 |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new packages this phase (RESEARCH audit: none) |
</threat_model>

<verification>
- `go test ./bench/runtime/ -run Matrix -count=1` and `go test ./cmd/helix-bench/ -count=1` pass.
- `go build ./cmd/helix-bench ./bench/runtime/...` succeeds; `go vet ./...` and `gofmt -w .` clean.
- `helix-bench run --help` shows all seven flags; `notYetImplemented("run")` removed.
</verification>

<success_criteria>
- `helix-bench run` wires all flags, expands the matrix, dispatches `RunCell` bounded by `--parallel`, and exits 0 iff >=1 task succeeded (criterion #1).
- Path-traversal ids are rejected; no TCP ports introduced (no-collision by construction).
- The `--agent=claude` branch is wired and locally runnable but not a CI gate (D-01).
</success_criteria>

<output>
Create `.planning/phases/77-bench-runtime-first-e2e-smoke/77-04-SUMMARY.md` when done.
</output>
