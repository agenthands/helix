---
phase: 79
plan: 02
type: tdd
wave: 2
depends_on: ["79-01"]
files_modified:
  - bench/evaluators/test_runner/test_runner.go
  - bench/evaluators/test_runner/test_runner_test.go
  - bench/evaluators/patch_validator/patch_validator.go
  - bench/evaluators/patch_validator/patch_validator_test.go
  - bench/evaluators/regression_checker/regression_checker.go
  - bench/evaluators/regression_checker/regression_checker_test.go
autonomous: true
requirements: [METRIC-01, METRIC-02, METRIC-04, METRIC-05]
must_haves:
  truths:
    - "test_runner derives task_success/verified_correctness from TestOutcome.Passed (exit==0), never from per-test row counts (Pitfall 4)"
    - "patch_validator computes edit_locality = 1 − modified/total over the git-tracked denominator (`git ls-files`), root-only ≈ 1.0, all-files = 0.0 (D-04/METRIC-04)"
    - "regression_checker double-runs the pre-existing test set pre/post patch and computes regression_rate against the cached pre-patch passing set (D-05/METRIC-05)"
    - "A pre-existing failing test (failing before the patch) does NOT count toward regression_rate (only cached-passing members that now fail)"
    - "Every grader returns a typed (value, *MetricError) so a failure nulls one metric without dropping the row (D-07)"
  artifacts:
    - path: "bench/evaluators/test_runner/test_runner.go"
      provides: "task_success, verified_correctness, compile_errors_before/after from a languages.TestOutcome"
      contains: "TestOutcome"
    - path: "bench/evaluators/patch_validator/patch_validator.go"
      provides: "files_modified, edit_locality, edit_distance_patch via git ls-files / git diff"
      contains: "ls-files"
    - path: "bench/evaluators/regression_checker/regression_checker.go"
      provides: "regression_rate from pre/post RunTests double-run against cached passing set"
      contains: "regression"
  key_links:
    - from: "bench/evaluators/test_runner & regression_checker"
      to: "bench/languages.TestOutcome.Passed"
      via: "exit-code-authoritative gate (never len(Tests))"
      pattern: "\\.Passed"
    - from: "bench/evaluators/patch_validator"
      to: "git ls-files / git diff --name-only (cmd.Dir = repoDir, fixed argv)"
      via: "exec.CommandContext, no shell"
      pattern: "exec\\.CommandContext"
---

<objective>
Implement the three exec/filesystem graders that read the cell's repo working tree and test outcomes: `test_runner` (task_success, verified_correctness, compile_errors_before/after), `patch_validator` (files_modified, edit_locality, edit_distance_patch via git), and `regression_checker` (regression_rate via a pre/post `RunTests` double-run). Each is a thin transform over an already-existing artifact (a `languages.TestOutcome` or the git working tree), and each returns a typed `(value, *evaluators.MetricError)` so D-07 per-metric isolation holds.

Purpose: These graders carry the two genuinely net-new formulas of the phase — `edit_locality` (METRIC-04, git-tracked denominator per D-04) and `regression_rate` (METRIC-05, cached-passing-set denominator per D-05) — plus the exit-code-authoritative success gate (Pitfall 4). TDD locks the formulas at their edge cases before implementation.

Output: three grader packages under `bench/evaluators/{test_runner,patch_validator,regression_checker}/`, each with RED→GREEN tests for the formulas and edge cases named in VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-CONTEXT.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-RESEARCH.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-PATTERNS.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-VALIDATION.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-01-SUMMARY.md
@bench/evaluators/metrics.go
</context>

<artifacts_this_phase_produces>
**New Go packages + exported funcs:**
- `bench/evaluators/test_runner` (package `test_runner`):
  - `Grade(outcome languages.TestOutcome) (success *bool, verified *bool, compileBefore *int, compileAfter *int, errs []evaluators.MetricError)` — exact signature at executor's discretion, but it MUST consume a `languages.TestOutcome` and gate success on `.Passed` (exit==0). `compile_errors_*` derived from the empty-rows-with-nonzero-exit signal (compile failure → non-zero exit, `Tests` empty).
- `bench/evaluators/patch_validator` (package `patch_validator`):
  - `EditLocality(ctx, repoDir) (*float64, *int, *evaluators.MetricError)` — returns (edit_locality, files_modified, err); denominator = `git ls-files`, numerator = `git diff --name-only` ∩ tracked.
  - `EditDistancePatch(ctx, repoDir) (*int, *evaluators.MetricError)` — the chosen deterministic definition (`git diff --numstat` added+deleted lines; documented in METRICS.md by Plan 04).
- `bench/evaluators/regression_checker` (package `regression_checker`):
  - `RegressionRate(prePatch, postPatch languages.TestOutcome) (*float64, *evaluators.MetricError)` — pure transform over two outcomes; denominator = pre-patch passing set keyed on `(Package, Name)`, numerator = cached-passing members now failing.

**New file paths:** the six files in `files_modified`.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: test_runner — exit-authoritative success + compile-error counts (RED→GREEN)</name>
  <files>bench/evaluators/test_runner/test_runner.go, bench/evaluators/test_runner/test_runner_test.go</files>
  <read_first>
    - bench/languages/runner.go (the `TestOutcome` struct: `Passed`, `Tests`, `Raw`, `ExitCode`; `TestResult`: `Name`, `Package`, `Passed` — lines 40-55) and its doc comment "Phase 79 grades structured output / Passed is authoritative"
    - bench/languages/go/runner.go (lines 61-104 — the exit-code-authoritative gate; compile-fail → `Passed=false, Tests=[]`; the ctx-cancel→infra vs exit→outcome split)
    - bench/evaluators/metrics.go (the `MetricError` shape from Plan 01)
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-PATTERNS.md (§"test_runner & regression_checker" — exit-code-authoritative gate, Pitfall 4)
  </read_first>
  <behavior>
    - Test: a `TestOutcome{Passed:true, ExitCode:0, Tests:[...all pass...]}` → `task_success` and `verified_correctness` both `*bool(true)`.
    - Test: a `TestOutcome{Passed:false, ExitCode:1, Tests:[]}` (compile failure — zero rows, non-zero exit) → `task_success` `*bool(false)`, and `compile_errors_after` reflects a compile failure (non-zero, e.g. 1) — assert success is NOT inferred as true from the empty `Tests` slice (Pitfall 4).
    - Test: a `TestOutcome{Passed:false, ExitCode:1, Tests:[one fail, rest pass]}` (compiles, a test fails) → `task_success` `*bool(false)`, `compile_errors_after` == 0 (it compiled).
  </behavior>
  <action>
    RED: write `bench/evaluators/test_runner/test_runner_test.go` (`package test_runner`) with `TestRunnerSuccessGate` covering the three behaviors, constructing `languages.TestOutcome` fixtures directly (no subprocess). Run `go test ./bench/evaluators/test_runner/ -run TestRunnerSuccessGate` — MUST fail. Commit `test(79-02): add failing test_runner success/compile-error gate`.
    GREEN: create `bench/evaluators/test_runner/test_runner.go` (`package test_runner`). Implement the grader that consumes a `languages.TestOutcome` and returns `task_success`/`verified_correctness` gated on `.Passed` (exit==0) — NEVER on `len(Tests)`. Derive `compile_errors_after` from the compile-failure signal (non-zero `ExitCode` with empty `Tests` ⇒ a compile error; a populated `Tests` with a non-zero exit ⇒ test failures, compile_errors_after==0). `compile_errors_before` defaults to a pre-patch snapshot count threaded from the coordinator (Plan 04); in this unit it accepts a pre-patch outcome or 0/null when unavailable. Return any failure as a `*evaluators.MetricError{Grader:"test_runner", ...}`, leaving the affected metric nil (D-07). Commit `feat(79-02): implement test_runner exit-authoritative grader`.
  </action>
  <verify>
    <automated>go test ./bench/evaluators/test_runner/ -run TestRunnerSuccessGate -count=1 -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/evaluators/test_runner/ -count=1` passes.
    - grep confirms the success gate reads `.Passed` / `ExitCode`, not `len(` on `Tests`: `grep -nE 'len\(.*Tests' bench/evaluators/test_runner/test_runner.go` returns no success-gating use (a count-for-compile-errors use is fine, but success must not depend on it).
    - RED commit precedes GREEN commit.
  </acceptance_criteria>
  <done>test_runner derives task_success/verified_correctness from exit-authoritative `Passed` and compile_errors_before/after from the compile-failure signal; failures isolate via MetricError; RED→GREEN commits present.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: patch_validator — edit_locality (git-tracked denominator) + edit_distance_patch (RED→GREEN)</name>
  <files>bench/evaluators/patch_validator/patch_validator.go, bench/evaluators/patch_validator/patch_validator_test.go</files>
  <read_first>
    - bench/runtime/cell.go (lines 444-474 — `runVerify`: the `exec.CommandContext` + `cmd.Dir = repoDir` + fixed-argv, no-shell pattern to mirror for `git`)
    - bench/runtime/validate.go (line 23 — `validatePathSegment`; modified-paths-under-repoDir invariant context)
    - internal/eval/trace/merge.go (lines 122-139 — the path-prefix invariant `PatchPaths` vs `RepoRoot`; resolve modified paths under repoDir before counting)
    - bench/evaluators/metrics.go (MetricError shape)
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-RESEARCH.md (§"Code Examples > git-based edit_locality" lines 286-300; Pitfall 5; Assumption A3 on the root-only=1.0 fixture sizing)
  </read_first>
  <behavior>
    - Test (root-only edit): in a temp `git init` repo with N tracked files (N large, e.g. 20) where exactly one tracked file is modified, `edit_locality = 1 − 1/N` and the test asserts `> 0.9` (per A3: the formula limit approaches 1.0; size the fixture so the assertion holds and document the interpretation in METRICS.md via Plan 04).
    - Test (all-files edit): every tracked file modified → `edit_locality == 0.0` exactly (`1 − N/N`).
    - Test (zero tracked files): an empty `git init` repo → `edit_locality` is `nil` (null) with a `*MetricError` annotation (undefined denominator), and `files_modified` still returns (0 or its count).
    - Test (untracked files excluded): an untracked scratch file in the repo dir does NOT change the denominator (denominator = `git ls-files`, tracked only; D-04).
    - Test (edit_distance_patch): a known diff (e.g. 3 added + 2 deleted lines) yields the documented deterministic value (5 under the `git diff --numstat` added+deleted definition).
  </behavior>
  <action>
    RED: write `bench/evaluators/patch_validator/patch_validator_test.go` (`package patch_validator`) building tiny git repos in `t.TempDir()` via `git init` + `git add` + `git commit` (fixed argv, `cmd.Dir`), then mutating files and running the grader. Cover the five behaviors as `TestEditLocality` (root-only, all-files, zero-tracked, untracked-excluded) and `TestEditDistancePatch`. Run `go test ./bench/evaluators/patch_validator/ -run TestEditLocality` — MUST fail. Commit `test(79-02): add failing patch_validator edit_locality + edit_distance tests`.
    GREEN: create `bench/evaluators/patch_validator/patch_validator.go` (`package patch_validator`). Implement `EditLocality(ctx, repoDir)` — denominator via `git ls-files` (tracked only), numerator via `git diff --name-only` intersected with the tracked set, `edit_locality = 1 − float64(modified)/float64(total)`; zero tracked files ⇒ return nil locality + a `*evaluators.MetricError{Grader:"patch_validator", Metric:"edit_locality", Reason:"zero git-tracked files (undefined denominator)"}`. Implement `EditDistancePatch(ctx, repoDir)` via `git diff --numstat` summing added+deleted lines (the chosen deterministic definition — Plan 04's METRICS.md documents it). All `git` invocations use `exec.CommandContext` with fixed argv and `cmd.Dir = repoDir` (NO shell, no string interpolation of repo content — V5 command-injection mitigation). Resolve modified paths under `repoDir` before counting (path-prefix invariant). A ctx cancellation/timeout is an infra error (return err); a non-zero git exit on a valid repo is handled (empty result, not a panic). Commit `feat(79-02): implement patch_validator git-tracked edit_locality + edit_distance`.
  </action>
  <verify>
    <automated>go test ./bench/evaluators/patch_validator/ -run 'TestEditLocality|TestEditDistancePatch' -count=1 -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/evaluators/patch_validator/ -count=1` passes (all five behaviors).
    - All-files-modified fixture yields `edit_locality == 0.0`; root-only fixture yields `> 0.9`; zero-tracked yields nil + MetricError.
    - grep confirms `git` is invoked via `exec.CommandContext` with `cmd.Dir` set and no `sh -c` / no `fmt.Sprintf` of repo content into argv: `grep -nE 'exec\.CommandContext.*git|"ls-files"|"--numstat"' bench/evaluators/patch_validator/patch_validator.go` matches; `grep -nE 'sh.*-c|bash.*-c' bench/evaluators/patch_validator/patch_validator.go` returns nothing.
    - RED commit precedes GREEN commit.
  </acceptance_criteria>
  <done>edit_locality uses the git-tracked denominator with correct root-only/all-files/zero-tracked edge behavior; edit_distance_patch is a documented deterministic git-diff value; git runs shell-free under repoDir; failures isolate via MetricError; RED→GREEN commits present.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: regression_checker — pre/post double-run regression_rate (RED→GREEN)</name>
  <files>bench/evaluators/regression_checker/regression_checker.go, bench/evaluators/regression_checker/regression_checker_test.go</files>
  <read_first>
    - bench/languages/runner.go (`TestOutcome.Tests`, `TestResult{Name, Package, Passed}` — the per-test rows keyed on `(Package, Name)`)
    - bench/languages/go/runner.go (the ctx-cancel→infra vs exit→outcome split, reused for the double-run)
    - bench/evaluators/metrics.go (MetricError shape)
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-RESEARCH.md (§"Code Examples > regression_checker pre/post double-run" lines 302-313; Pitfall 6 — the pre-patch run must be on the unmodified clone, cache the passing set as denominator)
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-CONTEXT.md (D-05 — full pre-existing test set run twice; cache passing set as denominator)
  </read_first>
  <behavior>
    - Test (clean patch): pre-patch all-passing, post-patch all-passing → `regression_rate == 0.0`.
    - Test (one regression): pre-patch tests A,B,C all pass; post-patch B now fails → `regression_rate == 1.0/3.0` (B is a cached-passing member that now fails; denominator = 3 pre-patch passing).
    - Test (pre-existing failure NOT counted): pre-patch A,B pass and C FAILS (C was already failing); post-patch C still fails, A,B pass → `regression_rate == 0.0` (C was never in the cached passing set, so its post-patch failure must NOT count — Pitfall 6).
    - Test (empty pre-patch passing set): pre-patch zero passing tests → `regression_rate` nil (null) + `*MetricError` (undefined denominator).
  </behavior>
  <action>
    RED: write `bench/evaluators/regression_checker/regression_checker_test.go` (`package regression_checker`) with `TestRegressionRate` covering the four behaviors, constructing pre/post `languages.TestOutcome` fixtures with explicit `Tests` rows keyed on `(Package, Name)`. Run `go test ./bench/evaluators/regression_checker/ -run TestRegressionRate` — MUST fail. Commit `test(79-02): add failing regression_checker rate test`.
    GREEN: create `bench/evaluators/regression_checker/regression_checker.go` (`package regression_checker`). Implement `RegressionRate(prePatch, postPatch languages.TestOutcome) (*float64, *evaluators.MetricError)` — build the cached pre-patch passing set keyed on `(Package, Name)` (denominator `passing_pre-existing_tests_pre_patch`); numerator = count of cached-passing members that are NOT passing in postPatch (`failing_pre-existing_tests_post_patch`); `regression_rate = numerator/denominator`. Empty passing set ⇒ return nil + `*evaluators.MetricError{Grader:"regression_checker", Metric:"regression_rate", Reason:"no pre-patch passing tests (undefined denominator)"}`. This unit is a pure transform over two outcomes — the actual pre-patch `RunTests` invocation is wired into `RunCell` by Plan 04 (this package only consumes the two outcomes). Commit `feat(79-02): implement regression_checker pre/post double-run rate`.
  </action>
  <verify>
    <automated>go test ./bench/evaluators/regression_checker/ -run TestRegressionRate -count=1 -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/evaluators/regression_checker/ -count=1` passes (all four behaviors, including the pre-existing-failure-not-counted case).
    - The one-regression fixture yields exactly `1.0/3.0`; the pre-existing-failure fixture yields `0.0`; the empty-passing-set yields nil + MetricError.
    - RED commit precedes GREEN commit.
  </acceptance_criteria>
  <done>regression_rate computed against the cached pre-patch passing-set denominator, pre-existing failures excluded, empty-denominator nulled with a MetricError; RED→GREEN commits present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → `git`/`go` subprocess | The cell's repo dir (mutated by the agent under test) is the input to `git ls-files`/`git diff` and the test double-run. Repo content (filenames, diff hunks) is untrusted relative to the grader. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | Tampering | `git` argv in patch_validator | mitigate | `exec.CommandContext` with FIXED argv (`git`, `ls-files` / `diff` / `--numstat`); `cmd.Dir = repoDir`; never interpolate repo content into a shell — mirrors `cell.go runVerify` / `go/runner.go RunTests`. Asserted by the no-`sh -c` grep in Task 2 acceptance. |
| T-79-02-02 | Tampering | patch path escaping repoDir | mitigate | Resolve modified paths under `repoDir` before counting (path-prefix invariant, mirrors `merge.go:122-139`); a path outside the subtree is not counted toward files_modified. |
| T-79-02-03 | Denial of Service | runaway `go test` in regression double-run | accept | The Go ToolBench fixtures are tiny single-module repos (D-05 cost note); the double-run cost is negligible. ctx cancellation/timeout from the caller is honored as an infra error (return err), not a hang. Cost tradeoff for external-scale repos is explicitly deferred to Phase 87 (D-05). |
| T-79-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (RESEARCH Package Legitimacy Audit: zero new external packages). |

No `high`-severity threats.
</threat_model>

<verification>
- `go vet ./bench/evaluators/...` clean.
- `go test ./bench/evaluators/test_runner/ ./bench/evaluators/patch_validator/ ./bench/evaluators/regression_checker/ -count=1` green.
- No `sh -c`/`bash -c` in any grader (`grep -rnE 'sh.*-c|bash.*-c' bench/evaluators/{test_runner,patch_validator,regression_checker}/` returns nothing).
</verification>

<success_criteria>
- test_runner gates success on exit-authoritative `TestOutcome.Passed` (Pitfall 4), derives compile_errors_before/after (METRIC-01/02).
- patch_validator computes edit_locality over the git-tracked denominator with correct root-only/all-files/zero-tracked behavior (METRIC-04/D-04) and a documented deterministic edit_distance_patch (METRIC-02).
- regression_checker computes regression_rate against the cached pre-patch passing set, excluding pre-existing failures (METRIC-05/D-05).
- Every grader returns `(value, *MetricError)` for D-07 isolation.
</success_criteria>

<output>
Create `.planning/phases/79-evaluators-result-schema-metrics-layer/79-02-SUMMARY.md` when done. Per TDD task record RED/GREEN/REFACTOR + commits. Note the exact exported grader signatures (so Plan 04's coordinator wires them without re-deriving) and the chosen `edit_distance_patch` definition (Plan 04 documents it in METRICS.md).
</output>
