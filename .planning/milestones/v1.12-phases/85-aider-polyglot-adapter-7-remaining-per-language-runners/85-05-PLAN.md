---
phase: 85
plan: 05
type: tdd
wave: 1
depends_on: []
files_modified:
  - bench/languages/typescript/runner.go
  - bench/languages/typescript/runner_test.go
  - bench/languages/typescript/testdata/vitest-report.json
  - bench/languages/javascript/runner.go
  - bench/languages/javascript/runner_test.go
  - bench/languages/javascript/testdata/jest-report.json
  - bench/datasets/internal-toolbench/typescript/
  - bench/datasets/internal-toolbench/javascript/
  - bench/languages/coverage_tsjs_test.go
autonomous: false
requirements: [TOOLBENCH-04, TOOLBENCH-05]
user_setup:
  - service: npm-vitest
    why: "Verify the vitest package version before pinning it in the TS toolchain image (RESEARCH [SUS] too-new-patch, not a slopsquat)"
    dashboard_config:
      - task: "Confirm vitest (vitest-dev/vitest) and pin a stable 4.1.x on npmjs.com/package/vitest before baking into the TS image"
        location: "https://www.npmjs.com/package/vitest"
must_haves:
  truths:
    - "The TypeScript runner parses committed vitest JSON into pass/fail rows WITHOUT invoking vitest (hermetic golden test; vitest is npx-only here)"
    - "The JavaScript runner parses committed jest JSON into rows WITHOUT invoking jest"
    - "TypeScript ≥8/10 and JavaScript ≥8/10 capability coverage is met by authored capability-tagged fixtures"
    - "Passed is gated on exit code, never on numFailedTests==0"
  artifacts:
    - path: "bench/languages/typescript/runner.go"
      provides: "TSRunner (vitest run --reporter=json)"
      contains: "languages.LanguageRunner"
    - path: "bench/languages/javascript/runner.go"
      provides: "JSRunner (jest --json)"
      contains: "languages.LanguageRunner"
  key_links:
    - from: "bench/languages/typescript/runner.go"
      to: "vitest json parser"
      via: "parse testResults[].assertionResults[].status (passed/failed)"
      pattern: "assertionResults"
    - from: "bench/languages/javascript/runner.go"
      to: "languages registry"
      via: "init() Register(internal-toolbench, javascript)"
      pattern: "Register\\(\"internal-toolbench\", \"javascript\""
---

<objective>
Land the final two LanguageRunners by cloning the Go runner template: **TypeScript** (`vitest run --reporter=json`, TOOLBENCH-04, ≥8/10 + tsserver diagnostics) and **JavaScript** (`jest --json`, TOOLBENCH-05, ≥8/10 + eslint diagnostics). Both reporters emit the Jest-compatible `testResults[].assertionResults[].status` shape (status ∈ passed/failed) with `numPassedTests`/`numFailedTests` summary — one parser strategy, two argv. vitest/jest are npx-only in this env, so the live layer skips; the hermetic golden parser is the authoritative proof.

Purpose: TOOLBENCH-04/05 coverage with hermetic proof that survives the npx-only env; pin a non-flagged vitest version after the legitimacy gate.
Output: 2 runners + 2 hermetic golden tests + per-language capability fixtures meeting ≥8/10 each.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@bench/languages/go/runner.go
@bench/languages/runner.go
@bench/languages/coverage.go
@bench/datasets/internal-toolbench/go/IT-go-rename-safety-1/task.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify vitest package legitimacy before pinning</name>
  <files>(none — verification gate; no files written until approved)</files>
  <action>Pause for a blocking human legitimacy verification of the vitest package name+version BEFORE Task 2 commits it into the TS runner argv or the toolchain image dep list. This gate is never auto-approvable (package-legitimacy checkpoints ignore workflow.auto_advance).</action>
  <verify>Human confirms the package+version on npmjs.com per the steps below.</verify>
  <done>Operator typed "approved" with a chosen stable 4.1.x version (or named an alternative); recorded for Task 2.</done>
  <what-built>RESEARCH flagged `vitest` [SUS] (reason `too-new` — it is the freshest patch of an enormous, legitimate package: vitest-dev/vitest, 70M wk downloads — NOT a slopsquat). The Package Legitimacy Gate requires a blocking human-verify before this name+version is committed/pinned in the TS runner argv + the toolchain image dep list.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/vitest and confirm the package is vitest-dev/vitest with the expected download volume.
    2. Pick a stable 4.1.x patch to pin in the TS toolchain image (avoid the bleeding-edge `too-new` patch).
    3. Confirm `vitest run --reporter=json` emits the `testResults[].assertionResults[].status` shape the runner parses.
  </how-to-verify>
  <resume-signal>Type "approved" with the chosen vitest version, or name the alternative (e.g. jest for TS too) to use instead.</resume-signal>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TypeScript + JavaScript runners + hermetic vitest/jest goldens + capability fixtures (RED then GREEN)</name>
  <files>bench/languages/go/runner.go, bench/languages/runner.go, bench/languages/coverage.go, bench/datasets/internal-toolbench/go/IT-go-rename-safety-1/task.json, bench/languages/typescript/runner.go, bench/languages/javascript/runner.go</files>
  <read_first>
    - bench/languages/go/runner.go (the full template; same anchors as plans 03-04)
    - bench/languages/runner.go (Capability enum 25-36; TestOutcome/TestResult)
    - bench/languages/coverage.go (Coverage gate; benchmark explicit param)
    - RESEARCH §Reporter Formats: vitest/jest both emit `testResults[].assertionResults[].status` (passed/failed) + `.title`/`.fullName`; `numPassedTests`/`numFailedTests`
    - bench/datasets/internal-toolbench/go/IT-go-rename-safety-1/task.json (fixture shape)
  </read_first>
  <behavior>
    - RED: `parseJestStyleJSON(raw)` over a COMMITTED testdata/vitest-report.json AND testdata/jest-report.json (each with ≥1 passed + ≥1 failed assertionResult) returns correct rows via encoding/json, NO subprocess. (Both reporters share the Jest-compatible shape, so the parser is shared and tested against both fixtures.)
    - RED: `TestTSCapabilitiesAtLeast8` and `TestJSCapabilitiesAtLeast8` pass from authored fixtures.
    - RED (live, additive): each runner's `TestRunTestsLive` skips when the runner binary (vitest/jest via npx) is unavailable — never the sole proof.
  </behavior>
  <action>
    Create bench/languages/typescript/runner.go (package typescript; `init(){ languages.Register("internal-toolbench","typescript",&TSRunner{}) }`; conformance assertion; `Detect` checks `package.json` AND `tsconfig.json`; `Setup` returns ctx.Err(); `Capabilities()` declares ≥8 enum constants incl CapLSPDiagnostics for tsserver; `RunTests` runs `exec.CommandContext(ctx,"vitest","run","--reporter=json")` capturing stdout via cmd.Output(), IDENTICAL ctx/ExitError split, returns `TestOutcome{Passed:exitCode==0, Tests:parseJestStyleJSON(raw), Raw:raw, ExitCode:exitCode}`). Create bench/languages/javascript/runner.go (package javascript; Register internal-toolbench/javascript; `Detect` checks `package.json` and the ABSENCE of `tsconfig.json` — or simply `package.json` with the registry resolving TS first; document the Detect precedence so TS and JS do not both claim the same dir; `Capabilities()` ≥8 incl eslint diagnostics; argv `exec.CommandContext(ctx,"jest","--json")`). Both share `parseJestStyleJSON` decoding `testResults[].assertionResults[].status`+`.title`/`.fullName` and the `numPassedTests`/`numFailedTests` summary via encoding/json (only needed fields). Commit testdata/vitest-report.json and testdata/jest-report.json (capture from a live npx vitest/jest run if reachable, else build from the documented Jest-compatible schema and label provenance in the test). Author ≥8 capability-tagged fixtures each under bench/datasets/internal-toolbench/{typescript,javascript}/. Add coverage assertion tests. Write hermetic tests FIRST (RED), then runners (GREEN). Do NOT alter `Passed: exitCode==0` (Pitfall 4).
  </action>
  <verify>
    <automated>go test ./bench/languages/typescript/ ./bench/languages/javascript/ ./bench/languages/ -run 'JestStyle|Vitest|Jest|TSCapabilities|JSCapabilities' -count=1</automated>
  </verify>
  <done>parseJestStyleJSON returns correct rows from both committed fixtures with no subprocess; TS ≥8/10 and JS ≥8/10 coverage asserted; Detect precedence prevents TS/JS double-claim; live layers skip cleanly; go vet clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helix-bench → vitest/jest | fixed argv crosses os/exec; repoDir holds untrusted task code |
| committed reporter fixtures → parser | trusted in-repo; parser must tolerate malformed JSON without inferring pass |
| npm registry → pinned vitest | vitest version is [SUS] (too-new) until the human-verify gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-05-01 | Tampering | os/exec argv injection via task code → test argv | mitigate | fixed argv (never a shell); repoDir set as cmd.Dir, not interpolated (mirror go/runner.go) |
| T-85-05-02 | Tampering | malformed reporter JSON flipping pass/fail | mitigate | Passed gated on exit code ONLY (Pitfall 4); assertion rows advisory; a decode error yields empty rows, never inferred-pass |
| T-85-05-SC | Tampering | pinning a too-new/compromised vitest patch | mitigate | blocking human-verify checkpoint (Task 1) against npmjs.com BEFORE the version is committed; pin a stable 4.1.x; never auto-approvable |
</threat_model>

<verification>
- `go test ./bench/languages/typescript/ ./bench/languages/javascript/ ./bench/languages/ -count=1` green.
- Each runner's hermetic golden parser test passes with NO toolchain (the authoritative proof).
- Coverage gate: typescript ≥8, javascript ≥8.
- `go vet ./bench/...` clean.
- `git log --oneline --grep='^test(85-05)'` and `--grep='^feat(85-05)'` both non-empty.
</verification>

<success_criteria>
- bench/languages/{typescript,javascript}/runner.go each implement LanguageRunner and register for (internal-toolbench, <lang>).
- A shared Jest-compatible parser handles both vitest and jest JSON; each proven by a hermetic golden fixture (the SOLE authoritative proof).
- Live vitest/jest layers skip cleanly (npx-only env).
- ≥8/10 capability coverage met for both; Detect precedence avoids TS/JS double-claim.
</success_criteria>

<output>
Create `.planning/phases/85-aider-polyglot-adapter-7-remaining-per-language-runners/85-05-SUMMARY.md` when done.
</output>
