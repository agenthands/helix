---
phase: 85
plan: 06
type: tdd
wave: 1
depends_on: []
files_modified:
  - cmd/helix-bench/verify_licenses.go
  - cmd/helix-bench/verify_licenses_test.go
  - bench/datasets/aider-polyglot/LICENSE-AUDIT.md
  - Makefile
autonomous: true
requirements: [ADAPTER-AIDER-01]
must_haves:
  truths:
    - "`make verify-licenses` strict-decodes LICENSE-AUDIT.md and HARD-FAILS (non-zero exit) on a missing/malformed per-track entry"
    - "LICENSE-AUDIT.md carries one row per Exercism track with a non-empty SPDX license + license_sha256 + redistribution clause excerpt"
    - "The validator returns (count, error) so a test proves the COUNT is checked, not just that the file passes (count==0 is itself an error)"
  artifacts:
    - path: "cmd/helix-bench/verify_licenses.go"
      provides: "strict-decode hard-fail license validator (clone of verify_tos.go)"
      contains: "KnownFields(true)"
    - path: "bench/datasets/aider-polyglot/LICENSE-AUDIT.md"
      provides: "per-track sha256 + redistribution clause excerpt audit table"
      contains: "license_sha256"
    - path: "Makefile"
      provides: "verify-licenses: hard-gate target"
      contains: "verify-licenses"
  key_links:
    - from: "Makefile verify-licenses:"
      to: "cmd/helix-bench verify-licenses"
      via: "go run ./cmd/helix-bench verify-licenses bench/datasets/aider-polyglot/LICENSE-AUDIT.md"
      pattern: "verify-licenses"
    - from: "cmd/helix-bench/verify_licenses.go"
      to: "strict YAML/MD decode"
      via: "dec.KnownFields(true) → unknown key = error = non-zero exit"
      pattern: "KnownFields\\(true\\)"
---

<objective>
Land SC#4: the per-track Exercism license audit (`bench/datasets/aider-polyglot/LICENSE-AUDIT.md`) and the `make verify-licenses` hard-fail gate, by cloning the proven `cmd/helix-bench/verify_tos.go` strict-decode validator shape and the Makefile `verify-tos:` target.

The validator strict-decodes each per-track entry, asserts a non-empty SPDX license + `license_sha256`, and HARD-FAILS (non-zero exit) on a malformed/missing entry — mirroring the `verify_tos.go` discipline: `KnownFields(true)` (unknown key → error), first-error-returns, fail-CLOSED, and a `(count, error)` signature so a regression test proves the count is checked (count==0 is itself an error). The audit reads each track's ACTUAL license at the pinned sha (A2: Python is confirmed MIT; do NOT assume MIT for the other tracks — read each repo's real LICENSE and compute its sha256).

Purpose: SC#4 redistribution compliance is a hard gate, like Phase 75 `make verify-tos` / `make validate-cost-table` — the dataset cannot ship without a strict, count-asserted license audit.
Output: verify_licenses.go validator + LICENSE-AUDIT.md + Makefile target + a strict-decode/count regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@cmd/helix-bench/verify_tos.go
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: verify-licenses strict-decode hard-fail validator (RED then GREEN)</name>
  <files>cmd/helix-bench/verify_tos.go, Makefile, bench/PROVIDERS.md, cmd/helix-bench/verify_licenses.go</files>
  <read_first>
    - cmd/helix-bench/verify_tos.go (the EXACT clone target: `verifyTOS`/`verifyTOSCount(today, path) (int, error)` split; `dec.KnownFields(true)` strict decode line 71; first-error-returns lines 73-77; fail-close on malformed; the `count==0 is an error` assertion lines 99-101; `newVerifyTOSCmd` NOT added to the root tree lines 150-172)
    - Makefile (the `verify-tos:` target ~line 309 `go run ./cmd/helix-bench verify-tos bench/PROVIDERS.md`; the `.PHONY` line 1 listing the gate targets)
  </read_first>
  <behavior>
    - RED: `verifyLicensesCount(path)` over a committed-good LICENSE-AUDIT.md returns (N, nil) where N == the number of track entries (>0).
    - RED: a fixture entry with an empty `license` OR empty `license_sha256` returns a non-nil error (fail-close).
    - RED: a fixture with an unknown/extra key fails strict decode (`KnownFields(true)`) → error.
    - RED: an empty/zero-track file returns an error (count==0 is itself a failure, mirroring verify_tos.go:99-101).
  </behavior>
  <action>
    Create cmd/helix-bench/verify_licenses.go cloning verify_tos.go's shape: a `TrackLicense` struct ({Track, SourceRepo, License (SPDX), LicenseSHA256, RedistributionClauseExcerpt}); a `verifyLicenses(path) error` wrapping `verifyLicensesCount(path) (int, error)`; parse the per-track rows out of LICENSE-AUDIT.md (use the same frontmatter/segment-extraction approach as verify_tos.go's `splitOnHorizontalRules`+strict YAML decode if the audit uses YAML frontmatter blocks per track, OR a strict table/row decode if it uses a Markdown table — pick the YAML-frontmatter-per-track shape to reuse verify_tos.go's machinery verbatim, since that is the proven strict-decode path); `dec.KnownFields(true)` so an unknown key errors; assert each track has a non-empty License AND a non-empty LicenseSHA256 (return the first error, fail-closed); return an error when zero tracks are found. Add `newVerifyLicensesCmd()` mirroring `newVerifyTOSCmd` (cobra command, NOT added to the root tree — invoked only by the Makefile). Write the failing verify_licenses_test.go cases FIRST (RED) using temp-file fixtures (good, empty-license, unknown-key, zero-track), then implement (GREEN).
  </action>
  <verify>
    <automated>go test ./cmd/helix-bench/ -run 'License' -count=1</automated>
  </verify>
  <done>verifyLicensesCount returns (N>0, nil) on a good file and a non-nil error on empty-license / unknown-key / zero-track fixtures; count is asserted; go vet clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author LICENSE-AUDIT.md + wire the Makefile verify-licenses gate</name>
  <files>Makefile, cmd/helix-bench/verify_licenses.go, bench/datasets/aider-polyglot/LICENSE-AUDIT.md</files>
  <read_first>
    - cmd/helix-bench/verify_licenses.go (Task 1 — the exact entry shape/keys the audit must satisfy)
    - Makefile (the `verify-tos:` target + `.PHONY` line — clone the target shape)
    - RESEARCH §License Audit: one row per track {track, source_repo, license (SPDX), license_sha256, redistribution_clause_excerpt}; Python track confirmed MIT; the polyglot-benchmark repo redistributes with attribution
  </read_first>
  <action>
    Create bench/datasets/aider-polyglot/LICENSE-AUDIT.md with one strict entry per Exercism track in the polyglot-benchmark (cpp, go, java, javascript, python, rust): track name, source_repo (github.com/exercism/<lang>), license SPDX (read each track's ACTUAL LICENSE at the pinned sha — Python is confirmed MIT; for the others, fetch the real LICENSE and record the true SPDX, do NOT assume MIT per A2), license_sha256 (computed over each track's committed LICENSE text at the pinned sha), and a short redistribution_clause_excerpt. Use the exact YAML-frontmatter-per-track shape the Task 1 validator strict-decodes (KnownFields(true) — no stray keys). Add a `verify-licenses:` target to the Makefile mirroring `verify-tos:` — `go run ./cmd/helix-bench verify-licenses bench/datasets/aider-polyglot/LICENSE-AUDIT.md` — and add `verify-licenses` to the `.PHONY` line. If a track's real LICENSE/sha256 cannot be fetched in this env (network-gated clone), record the entry with the pinned sha provenance and flag the sha as toolchain/network-gated for capture on a connected host, but keep the file strict-decodable and `make verify-licenses` green on the structural gate (non-empty license+sha256 strings present).
  </action>
  <verify>
    <automated>make verify-licenses</automated>
  </verify>
  <done>`make verify-licenses` exits 0 on the authored file; every track has a non-empty SPDX license + sha256; the target is in .PHONY; intentionally corrupting one entry (empty license) makes the gate exit non-zero (manual spot-check noted in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| redistributed Exercism dataset → published bench | license compliance is the legal boundary; an unaudited track is a redistribution risk |
| LICENSE-AUDIT.md → validator | strict decode rejects drift/unknown keys; the gate fails closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-06-01 | Repudiation | shipping a track without a recorded license (deniability of compliance) | mitigate | hard-fail gate asserts a non-empty SPDX license + sha256 per track; count==0 is an error; first-error-returns |
| T-85-06-02 | Tampering | a track's LICENSE silently changing upstream | mitigate | per-track license_sha256 computed at the pinned sha (V6); pinned-sha clone (not a mutable tag) |
| T-85-06-03 | Tampering | drift/unknown keys creeping into the audit file | mitigate | `KnownFields(true)` strict decode → unknown key = non-zero exit (mirror verify_tos.go:71) |
</threat_model>

<verification>
- `go test ./cmd/helix-bench/ -count=1` green (strict-decode + count assertions).
- `make verify-licenses` exits 0 on the authored audit.
- A corrupted entry makes the gate exit non-zero (negative-path proof).
- `go vet ./...` clean.
- `git log --oneline --grep='^test(85-06)'` and `--grep='^feat(85-06)'` both non-empty.
</verification>

<success_criteria>
- `make verify-licenses` is a green hard-fail gate over a per-track LICENSE-AUDIT.md.
- The validator is count-asserted and fails closed on empty-license / unknown-key / zero-track.
- Each track records its real SPDX license + sha256 (Python MIT confirmed; others read, not assumed).
</success_criteria>

<output>
Create `.planning/phases/85-aider-polyglot-adapter-7-remaining-per-language-runners/85-06-SUMMARY.md` when done.
</output>
