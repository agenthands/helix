---
phase: 89-reports-ci-policy-contamination-canary
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bench/aggregator/aggregate.go
  - bench/aggregator/report.go
  - bench/aggregator/report_test.go
  - bench/aggregator/per_language_test.go
  - bench/aggregator/ablations_test.go
  - bench/aggregator/testdata/per_language.golden.md
  - bench/aggregator/testdata/ablations.golden.md
  - bench/aggregator/testdata/leaderboard.golden.md
  - bench/aggregator/testdata/cost_quality.golden.md
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-03, REPORT-04]
tags: [bench, aggregator, report, render, determinism]

must_haves:
  truths:
    - "leaderboard.md renders a verified_correctness column and a cost_per_solved column for every (mode × benchmark) row, with em-dash for null totals"
    - "per_language.md lists ALL 8 Tier-1 languages; a language with no benchmark coverage renders n/a (not omitted)"
    - "ablations.md renders all 5 delta tables (full vs no_lsp, no_semantic, no_structured_edit, baseline_plain, baseline_rag) with CI-overlap markers; full vs no_semantic is computed at aggregate-time"
    - "cost_quality.md renders a deterministic ASCII scatter (cost vs verified_correctness) and still cites cost-table valid_until"
    - "all new renderers are byte-stable (double-render diff is empty) and use no RNG"
  artifacts:
    - path: "bench/aggregator/aggregate.go"
      provides: "reduceVerifiedCorrectness (pooledRate over r.Metrics.VerifiedCorrectness); aggregate-time full vs no_semantic delta reduce"
      contains: "reduceVerifiedCorrectness"
    - path: "bench/aggregator/report.go"
      provides: "renderPerLanguage, renderAblations, ASCII scatter in renderCostQuality, verified_correctness + cost_per_solved leaderboard columns"
      contains: "renderPerLanguage"
    - path: "bench/aggregator/testdata/per_language.golden.md"
      provides: "committed golden for REPORT-02 (8 langs, n/a rows)"
    - path: "bench/aggregator/testdata/ablations.golden.md"
      provides: "committed golden for REPORT-03 (5 deltas incl. no_semantic)"
  key_links:
    - from: "bench/aggregator/aggregate.go reduceVerifiedCorrectness"
      to: "bench/aggregator/load.go rowMetrics.VerifiedCorrectness"
      via: "pooledRate over the decoded *bool"
      pattern: "VerifiedCorrectness"
    - from: "bench/aggregator/report.go renderAblations"
      to: "bench/aggregator/report.go ciOverlap"
      via: "STATS-04 overlap predicate reused (not re-implemented)"
      pattern: "ciOverlap"
    - from: "bench/aggregator/report.go renderPerLanguage"
      to: "bench/aggregator/aggregate.go reduceLanguageRows / rep.ByLanguage"
      via: "fixed Tier-1 list iteration over the ByLanguage slice"
      pattern: "ByLanguage"
---

<objective>
Add the missing `verified_correctness` reduce + leaderboard column, surface `cost_per_solved` onto the leaderboard, and build the two new deterministic renderers (`per_language.md`, `ablations.md`) plus the `cost_quality.md` ASCII scatter — all in the exact Phase 82 zero-RNG-in-render style, proven by committed golden `.md` fixtures.

Purpose: REPORT-01/02/03/04 require four report surfaces that consume substrate Phases 82/85/86/87 deliberately built ahead of time (the decoded-but-unrendered `VerifiedCorrectness`, the `ByLanguage` slice, the BCa/`ciOverlap` machinery, the `cost_table_valid_until` footer). This plan renders that substrate. It does NOT wire the CLI or the canary exclusion (those are 89-03 / 89-02).

Output: `reduceVerifiedCorrectness` + two new leaderboard columns in `report.go`; `renderPerLanguage`; `renderAblations` (with aggregate-time `full vs no_semantic`); the ASCII scatter; four committed/regenerated golden fixtures; new golden tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-RESEARCH.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-PATTERNS.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-VALIDATION.md

# Render layer + reduce path this plan extends (read before editing):
@bench/aggregator/report.go
@bench/aggregator/aggregate.go
@bench/aggregator/load.go
@bench/aggregator/aggregate_test.go
</context>

<artifacts_this_phase_produces>
This plan (89-01) produces, of the phase-wide artifact set:
- `reduceVerifiedCorrectness` + the leaderboard `verified_correctness` column (REPORT-01)
- the leaderboard `cost_per_solved` column, single-sourced from the cost row (REPORT-01)
- the `per_language.md` renderer with `n/a` rows for no-coverage Tier-1 languages (REPORT-02)
- the `ablations.md` renderer with 5 deltas incl. aggregate-time `full vs no_semantic` (REPORT-03)
- the `cost_quality.md` ASCII scatter (REPORT-04)
- the regenerated `leaderboard.golden.md` / `cost_quality.golden.md` + new `per_language.golden.md` / `ablations.golden.md`

Deferred to later plans in this phase: the canary exclusion+footnote+InjectPrompt caller (89-02), `helix-bench report --run-id` + `renderAll` factoring + byte-reproducibility test + lockstep guard updates (89-03), the CI workflow + YAML-parse test + cost-budget doc (89-04).
</artifacts_this_phase_produces>

<tier1_language_derivation>
The canonical Tier-1 language set is the 8 directories under `bench/languages/`: `cpp`, `csharp`, `go`, `java`, `javascript`, `python`, `rust`, `typescript` (NO `c` — the research example list was 9 and is wrong; verified against the on-disk registry this session). DERIVE the list as a fixed, sorted, package-level `var tier1Languages = []string{...}` in `report.go` (or beside `renderPerLanguage`) using exactly those 8 IDs. Do NOT hardcode a `c` row. A `checkpoint:human-verify` is NOT needed — the per_language golden test catches any wrong ID.
</tier1_language_derivation>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verified_correctness reduce + verified_correctness/cost_per_solved leaderboard columns (RED→GREEN)</name>
  <files>bench/aggregator/aggregate.go, bench/aggregator/report.go, bench/aggregator/report_test.go, bench/aggregator/testdata/leaderboard.golden.md</files>
  <read_first>
    - bench/aggregator/aggregate.go:416-422 (pooledRate — reuse verbatim), :326-348 (reduceCanaryRate — the pooled-rate analog to mirror), :180-240 (reduceLeaderRow — where the per-mode LeaderRow is built), :428-471 (reduceCostRow / costPerSolvedTask — the single source for CostPerSolved)
    - bench/aggregator/load.go:30-46 (rowMetrics.VerifiedCorrectness *bool, decoded at :32; Row), :64 (Loaded.Rows)
    - bench/aggregator/report.go:46-97 (LeaderRow + CostRow structs — add fields here), :223-256 (renderLeaderboard — header, separator, Fprintf row to extend), :187-192 (fmtCI — every new cell routes through this)
  </read_first>
  <behavior>
    - reduceVerifiedCorrectness(loaded, tasks, mode) iterates Loaded.Rows(task,mode), reads r.Metrics.VerifiedCorrectness (a *bool: nil is skipped, true counts as trueCount, false counts toward total only), and returns pooledRate(trueCount, total). Zero total → ciValue{OK:false} → em-dash. Consumes NO RNG.
    - A LeaderRow gains a VerifiedCorrectness ciValue and a CostPerSolved ciValue; renderLeaderboard emits two new columns (header + separator + row) routed through fmtCI.
    - CostPerSolved on the leaderboard is sourced from the SAME reduceCostRow output (matched by (mode,benchmark) key) — NOT recomputed (Pitfall 3). Leaderboard cost == cost_quality.md cost for the same row.
    - A null verified_correctness total renders em-dash, never a fabricated 0.
  </behavior>
  <action>
    Add reduceVerifiedCorrectness to aggregate.go mirroring reduceCanaryRate's pooled-rate structure (iterate Loaded.Rows, nil-skip the *bool, feed pooledRate). Assign its result onto the LeaderRow in the per-mode build loop AFTER the determinism-locked reduceLeaderRow assignment, exactly where CanaryPassRate/RawScore are assigned today — it consumes no RNG so it cannot perturb the locked BCa contract. Add VerifiedCorrectness and CostPerSolved ciValue fields to the LeaderRow struct in report.go. Single-source CostPerSolved: after both leaderboard and cost rows are reduced, surface the matching CostRow.CostPerSolved (keyed by mode+benchmark from the costPerSolvedTask path at aggregate.go:463) into the LeaderRow — do not run a second cost reduce (Pitfall 3 / IN-02 discipline). Extend renderLeaderboard's header, separator row, and per-row Fprintf with the two new columns, each rendered via fmtCI. Regenerate leaderboard.golden.md with `go test ./bench/aggregator/ -run TestLeaderboard -update` and review the diff: it MUST touch ONLY the two new columns — any reorder or precision drift in an existing column is a determinism bug, stop and fix. Do NOT inline any code in this action; identifiers and call sites are named above. Per REPORT-01.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run 'TestLeaderboard|TestVerifiedCorrectness' -count=1</automated>
  </verify>
  <done>leaderboard.md renders verified_correctness + cost_per_solved columns; reduceVerifiedCorrectness exists and is RNG-free; leaderboard cost == cost_quality cost for the same row; leaderboard.golden.md regenerated with a diff limited to the two new columns; new test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: renderPerLanguage (REPORT-02) + renderAblations with aggregate-time no_semantic (REPORT-03) (RED→GREEN)</name>
  <files>bench/aggregator/report.go, bench/aggregator/aggregate.go, bench/aggregator/per_language_test.go, bench/aggregator/ablations_test.go, bench/aggregator/testdata/per_language.golden.md, bench/aggregator/testdata/ablations.golden.md</files>
  <read_first>
    - bench/aggregator/report.go:114-118 (LanguageRow{Language,PassRate,N}), :302-327 (renderCostQuality — structural template for a new renderer), :347-386 (renderFooter + writeReport — reuse verbatim), :151-156 (ciOverlap — STATS-04 predicate to reuse), :197-217 (sortLeaderRows/sortKey — sort-before-emit pattern)
    - bench/aggregator/aggregate.go:592-605 (bca helper), :255-300 (reduceLanguageRows → rep.ByLanguage)
    - bench/runtime/deltas.go:40-71 (modeFull="your_agent_full", deltaComparisons names 4; no_semantic is a deliberate non-operand — do NOT add it here)
    - .planning/phases/89-reports-ci-policy-contamination-canary/89-PATTERNS.md (Pattern: no_semantic at aggregate-time; Pitfall 2)
  </read_first>
  <behavior>
    - renderPerLanguage(byLang, footer) iterates the fixed tier1Languages list (cpp, csharp, go, java, javascript, python, rust, typescript) in fixed order; a language present in ByLanguage renders pass_rate + n; an absent language renders n/a (NOT omitted — REPORT-02). Appends renderFooter.
    - renderAblations renders 5 delta tables: full vs {no_lsp, no_semantic, no_structured_edit, baseline_plain, baseline_rag}. Each delta is the point difference of the two modes' reduced rates with a CI-overlap marker via ciOverlap. The full vs no_semantic delta is computed at AGGREGATE time from the full and no_semantic BCa CIs in Loaded (both modes present) — NOT from deltas.go ablation_deltas (Pitfall 2). All rows sorted before emit; em-dash for any missing operand.
    - Both renderers are byte-stable (double render identical) and use the same %.4f precision + em-dash discipline as fmtCI. No RNG in the render layer.
  </behavior>
  <action>
    In report.go add renderPerLanguage modeled on renderCostQuality's structure (header, fixed-order rows, footer) — index ByLanguage into a map, then iterate the fixed tier1Languages var; absent → n/a row. Reuse writeReport/renderFooter verbatim; do NOT call os.WriteFile. In aggregate.go add an aggregate-time reduce that produces, for each (full, other) mode pair, the two BCa CIs (reuse the bca helper) needed by the ablations renderer; expose them on the Report (e.g. an AblationRow slice). renderAblations consumes those, computes the delta point + applies ciOverlap for the marker, and renders one table per the 5 comparisons in a fixed order (no_lsp, no_semantic, no_structured_edit, baseline_plain, baseline_rag). For no_semantic, take the full and no_semantic CIs directly from the aggregate-time reduce — never route it through deltas.go's operand set. Sort every row before emit. Add per_language_test.go and ablations_test.go using the goldenFixture/`-update` harness (aggregate_test.go:228-274); generate per_language.golden.md and ablations.golden.md with `-update`. Confirm a no-coverage language shows n/a and the ablations golden shows all 5 deltas. No fenced code in this action; identifiers above. Per REPORT-02, REPORT-03.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run 'TestPerLanguage|TestAblations' -count=1</automated>
  </verify>
  <done>per_language.md shows all 8 Tier-1 languages with n/a for no-coverage; ablations.md shows 5 deltas incl. full vs no_semantic (computed at aggregate-time) with ciOverlap markers; both goldens committed; both new tests green; renderers RNG-free and byte-stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ASCII scatter in cost_quality.md (REPORT-04) (RED→GREEN)</name>
  <files>bench/aggregator/report.go, bench/aggregator/report_test.go, bench/aggregator/testdata/cost_quality.golden.md</files>
  <read_first>
    - bench/aggregator/report.go:302-327 (renderCostQuality — extend, do not rewrite; cost_table_valid_until already cited via renderFooter), :187-192 (fmtCI %.4f discipline), :33 (emDash)
    - bench/aggregator/aggregate.go:624 (costTableValidUntil — already wired into the footer; the citation requirement of REPORT-04 is already met)
    - .planning/phases/89-reports-ci-policy-contamination-canary/89-RESEARCH.md (Pitfall 4 — scatter byte-stability)
  </read_first>
  <behavior>
    - renderCostQuality additionally emits a fenced ASCII scatter of cost (x-axis) vs verified_correctness (y-axis) per (mode × benchmark). Axis bucketing uses fixed %.4f-style rounding into a fixed-size grid; rows are sorted before plotting; the grid is rendered row-by-row deterministically (no map-iteration order).
    - The existing cost_per_solved table + the cost_table_valid_until footer citation are preserved unchanged.
    - The scatter is byte-stable: a double render of cost_quality.md is diff-empty (Pitfall 4).
    - A null verified_correctness or null cost point is plotted as a deterministic em-dash/blank cell, never a fabricated 0.
  </behavior>
  <action>
    Extend renderCostQuality (do not replace its existing table/footer) to append a fenced ASCII scatter block. Bucket cost and verified_correctness into a fixed grid with fixed rounding (mirror fmtCI's %.4f determinism); sort all points before plotting; render the grid deterministically row by row. Keep the existing CostPerSolved table and the renderFooter call (which already cites cost_table_valid_until — do not duplicate the citation). Source verified_correctness for the y-axis from the same reduceVerifiedCorrectness output added in Task 1 (single source). Regenerate cost_quality.golden.md with `-update`; review the diff — it must add only the scatter block (the existing table/footer bytes must be unchanged except where the scatter is appended). Add a double-render byte-stability assertion to report_test.go for cost_quality.md. No fenced code in this action. Per REPORT-04.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run 'TestCostQuality' -count=1</automated>
  </verify>
  <done>cost_quality.md renders a deterministic ASCII scatter (cost vs verified_correctness) and still cites cost-table valid_until; cost_quality.golden.md regenerated with the scatter appended; double-render of cost_quality.md is byte-identical; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| result.v2.json rows → aggregator reduce | Read-only on-disk benchmark rows; a contaminated/malformed metric must never fabricate a headline number |
| reduce/render → committed golden .md | Determinism contract — a golden drift that exceeds the new columns is an integrity signal of a reduce bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-01 | Tampering | reduceVerifiedCorrectness over r.Metrics.VerifiedCorrectness | mitigate | nil *bool is skipped; zero total → ciValue{OK:false} → em-dash via pooledRate (never a fabricated 0 that would inflate a headline) |
| T-89-01-02 | Tampering | golden regeneration (leaderboard/cost_quality) | mitigate | regenerate with `-update`, review the diff line-by-line; a change beyond the new columns/scatter aborts the task (reorder/precision drift = determinism bug) |
| T-89-01-03 | Information disclosure | ASCII scatter float bucketing | mitigate | fixed %.4f rounding + sort-before-plot + deterministic grid emit → byte-stable, no run-to-run flicker (Pitfall 4) |
| T-89-01-SC | Tampering | npm/pip/cargo installs | accept | zero external packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); no go.mod change; the existing leaf-import vet gate guards against a sneaked dep |
</threat_model>

<verification>
- `go test ./bench/aggregator/... -count=1` green (all new + existing aggregator tests).
- `go vet ./bench/...` clean.
- `go list -deps ./bench/aggregator/... | sort -u` shows NO new external dependency (leaf-import discipline).
- Every regenerated golden diff reviewed: leaderboard touches only the 2 new columns; cost_quality adds only the scatter block.
</verification>

<success_criteria>
- REPORT-01: leaderboard.md renders verified_correctness + cost_per_solved (single-sourced) columns with CIs + em-dash null discipline. (Full byte-reproducibility proof lands in 89-03.)
- REPORT-02: per_language.md lists all 8 Tier-1 languages; no-coverage = n/a, not omitted. Golden committed.
- REPORT-03: ablations.md renders 5 deltas incl. full vs no_semantic (aggregate-time) with ciOverlap markers. Golden committed.
- REPORT-04: cost_quality.md renders a deterministic ASCII scatter (cost vs verified_correctness) and cites cost-table valid_until.
- All renderers RNG-free; the two regenerated goldens drifted only by the intended additions.
</success_criteria>

<output>
Create `.planning/phases/89-reports-ci-policy-contamination-canary/89-01-SUMMARY.md` when done. Record: the RED tests added, the GREEN implementations, the golden diffs (confirming they touched only the new columns/scatter), and the verified_correctness/cost_per_solved/per_language/ablations/scatter mappings to REPORT-01..04. Note that the leaderboard/cost_quality goldens are now regenerated and that 89-03 will assert their final byte-reproducibility after the canary footnote (89-02) lands.
</output>
