---
phase: 89-reports-ci-policy-contamination-canary
plan: 02
type: tdd
wave: 2
depends_on: ["89-01"]
files_modified:
  - bench/aggregator/aggregate.go
  - bench/aggregator/report.go
  - bench/aggregator/aggregate_canary_test.go
  - bench/aggregator/testdata/leaderboard.golden.md
  - bench/runtime/prompt_inject.go
  - bench/runtime/prompt_inject_test.go
autonomous: true
requirements: [INFRA-05]
tags: [bench, aggregator, canary, contamination, exclusion]

must_haves:
  truths:
    - "A row whose completion echoes the canary Sentinel is EXCLUDED from the headline (pass@1, verified_correctness, cost_per_solved) reduce"
    - "Excluded contaminated (task, mode) cells are listed in a leaderboard.md footnote"
    - "CanaryPassRate continues to be computed over ALL rows (it MEASURES contamination; the headline EXCLUDES it)"
    - "A deterministic production caller injects the canary Sentinel into select tasks (every Kth task), referencing canary.Sentinel as the single source of truth"
    - "A synthetic contaminated-response row trips the flag, is excluded, and is footnoted"
  artifacts:
    - path: "bench/aggregator/aggregate.go"
      provides: "cleanRows split (clean/contaminated); exclusion at the top of reduceLeaderRow/reduceCostRow; contaminated (task,mode) collected on Report"
      contains: "cleanRows"
    - path: "bench/aggregator/report.go"
      provides: "leaderboard.md contamination footnote section listing excluded (task,mode) cells"
      contains: "footnote"
    - path: "bench/runtime/prompt_inject.go"
      provides: "deterministic InjectPrompt production caller (every Kth select task)"
      contains: "canary.InjectPrompt"
  key_links:
    - from: "bench/aggregator/aggregate.go cleanRows"
      to: "bench/aggregator/aggregate.go rowCanary"
      via: "rowCanary(r) per-row contamination flag drives the clean/contaminated split"
      pattern: "rowCanary"
    - from: "bench/aggregator/aggregate.go reduceLeaderRow"
      to: "bench/aggregator/aggregate.go successCount"
      via: "successCount is fed CLEAN rows so contaminated rows never reach the headline vector"
      pattern: "successCount"
    - from: "bench/runtime/prompt_inject.go"
      to: "bench/canary/canary.go Sentinel / InjectPrompt"
      via: "select-task hook references the single-source Sentinel (never re-derived)"
      pattern: "canary\\.(Sentinel|InjectPrompt)"
---

<objective>
Close INFRA-05: wire the REAL contamination canary. Add a deterministic production caller that injects `canary.Sentinel` into select tasks, EXCLUDE contaminated rows from the leaderboard headline at aggregate-time, and list the excluded `(task, mode)` cells in a `leaderboard.md` footnote — while `CanaryPassRate` keeps measuring contamination over all rows.

Purpose: Phase 86 built the canary probe (`Sentinel`/`InjectPrompt`/`IsContaminated`/`rowCanary`/`CanaryPassRate`) but (a) `InjectPrompt` has NO production caller and (b) the headline still COUNTS contaminated rows. INFRA-05 requires that a model echoing the sentinel be flagged, excluded from headline numbers, and footnoted. This closes Pitfall 1 of the milestone (contaminated data inflating headline numbers — an integrity concern).

Output: `cleanRows` split + exclusion in the reduce path; the contaminated-cell collection on `Report`; the leaderboard footnote; the deterministic `InjectPrompt` caller; a synthetic-contamination golden + unit test. Regenerates `leaderboard.golden.md` (footnote appears).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-RESEARCH.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-PATTERNS.md

# Canary substrate + reduce path this plan extends:
@bench/canary/canary.go
@bench/aggregator/aggregate.go
@bench/aggregator/report.go
@bench/aggregator/aggregate_canary_test.go
@bench/aggregator/load.go

# Depends on 89-01: leaderboard now carries verified_correctness + cost_per_solved columns.
@.planning/phases/89-reports-ci-policy-contamination-canary/89-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (89-02) produces, of the phase-wide artifact set:
- the aggregate-time canary EXCLUSION (`cleanRows`) of contaminated rows from the headline (INFRA-05)
- the `leaderboard.md` contamination footnote listing excluded `(task, mode)` cells (INFRA-05)
- the deterministic production `InjectPrompt` caller for select tasks (INFRA-05 — closes RESEARCH Open Q5)
- a synthetic contaminated-response test + regenerated `leaderboard.golden.md` showing the footnote

Depends on 89-01 (leaderboard columns). Feeds 89-03, which asserts the FINAL byte-reproducibility of all 4 reports (incl. this footnote) and updates the lockstep guards.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggregate-time exclusion (cleanRows) + leaderboard contamination footnote (RED→GREEN)</name>
  <files>bench/aggregator/aggregate.go, bench/aggregator/report.go, bench/aggregator/aggregate_canary_test.go, bench/aggregator/testdata/leaderboard.golden.md</files>
  <read_first>
    - bench/aggregator/aggregate.go:305-315 (rowCanary — returns (contaminated, present) from the completion doc key via canary.IsContaminated; the leaf primitive cleanRows wraps), :326-348 (reduceCanaryRate — stays over ALL rows), :180-240 (reduceLeaderRow — filter at the TOP), :428-471 (reduceCostRow — filter at the TOP), :532-543 (successCount — must receive CLEAN rows)
    - bench/aggregator/report.go:223-256 (renderLeaderboard — append the footnote after the overlap-warning block at :244-251, the structural template for an appended section), :46-97 (Report/LeaderRow — where to carry the contaminated set)
    - bench/aggregator/aggregate_canary_test.go:26-58 (writeCanaryRow — injects the additive completion key; reuse to build a contaminated row), :119-130 (TestAggregateCanaryAbsentIsEmDash — the golden guard whose intent is changing; this plan REGENERATES the golden, 89-03 updates the guard's assertion)
  </read_first>
  <behavior>
    - cleanRows(rows) returns (clean, contaminated []Row): a row where rowCanary(r) reports (contaminated==true, present==true) goes to contaminated; everything else to clean.
    - reduceLeaderRow and reduceCostRow filter their input through cleanRows BEFORE building any headline vector, so successCount and every metric reduce (pass@1, verified_correctness, cost_per_solved) see ONLY clean rows.
    - reduceCanaryRate is UNCHANGED — it still iterates ALL rows (it MEASURES contamination; the headline EXCLUDES it).
    - The contaminated (task, mode) cells are collected onto the Report and rendered as a deterministic, sorted leaderboard.md footnote section.
    - A synthetic row whose completion contains canary.Sentinel is: excluded from the headline (its pass/verified/cost contribution disappears), AND listed in the footnote, AND still counted by CanaryPassRate.
  </behavior>
  <action>
    Add cleanRows(rows []Row) (clean, contaminated []Row) to aggregate.go mirroring rowCanary's flag semantics (research Pattern 3). Filter through cleanRows at the top of reduceLeaderRow and reduceCostRow so successCount (the "solved" gate) and the verified_correctness/cost reduces receive clean rows only. Do NOT touch reduceCanaryRate (it stays over all rows). Collect the contaminated (task,mode) set onto the Report struct. In report.go append a deterministic, sorted contamination footnote section to renderLeaderboard AFTER the overlap-warning block (use the :244-251 appended-section template) listing each excluded (task,mode) cell; empty set → no footnote section (em-dash discipline: never fabricate). Extend aggregate_canary_test.go with a synthetic test: build a contaminated row via writeCanaryRow with completion==canary.Sentinel, assert (a) the headline LeaderRow for that mode EXCLUDES it, (b) the footnote lists it, (c) CanaryPassRate still reflects it. Regenerate leaderboard.golden.md with `-update`; the diff adds only the footnote section. NOTE: do NOT modify TestAggregateCanaryAbsentIsEmDash's assertion in THIS plan beyond what golden regen forces — 89-03 owns the lockstep-guard intent update. No fenced code in this action. Per INFRA-05.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run 'TestCanaryExclusion|TestRowCanary|TestAggregateCanary' -count=1</automated>
  </verify>
  <done>cleanRows excludes contaminated rows from the headline reduce; reduceCanaryRate unchanged (all rows); a contaminated (task,mode) is excluded from pass@1/verified_correctness/cost AND footnoted; leaderboard.golden.md regenerated with the footnote; synthetic test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic production InjectPrompt caller for select tasks (RED→GREEN)</name>
  <files>bench/runtime/prompt_inject.go, bench/runtime/prompt_inject_test.go</files>
  <read_first>
    - bench/canary/canary.go:37 (Sentinel — the single source of truth; reference, never re-derive), :52-63 (InjectPrompt — deterministic, no production caller today per RESEARCH Open Q5), :44 (DocKeyCompletion)
    - bench/runtime/ (the prompt/task path — locate the point where a task prompt is assembled before dispatch; add the select-task hook there or in a new bench/runtime/prompt_inject.go consumed by it)
    - .planning/phases/89-reports-ci-policy-contamination-canary/89-PATTERNS.md ("Task injection point" section — deterministic every-Kth hook, pure + byte-stable)
  </read_first>
  <behavior>
    - A pure function (e.g. InjectCanaryIfSelected(taskIndex int, prompt string) string) injects canary.Sentinel via canary.InjectPrompt into every Kth select task (fixed K, deterministic by task index/identity) and returns the prompt unchanged otherwise.
    - Same input → same output (byte-stable; no RNG, no clock, no map-iteration nondeterminism) — selection is a deterministic function of a stable task key, NOT wall-clock or random.
    - The Sentinel is referenced only via canary.Sentinel / canary.InjectPrompt — never re-derived (anti-pattern).
    - Selecting task index i: injected iff the deterministic selection predicate holds; a non-selected task's prompt is byte-identical to the input.
  </behavior>
  <action>
    Add bench/runtime/prompt_inject.go with a pure deterministic selector that, for a select subset of tasks (every Kth by a stable task key — choose a fixed K and document it), calls canary.InjectPrompt(prompt) and otherwise returns the prompt verbatim. Wire it into the existing prompt/task-assembly path in bench/runtime so a contaminated model would echo the Sentinel into the completion key that rowCanary already reads. Reference canary.Sentinel as the single source of truth — do NOT copy the literal. Keep the selector pure (no clock, no RNG, no os.Environ): selection is a deterministic function of the task key. Add prompt_inject_test.go asserting: a selected task's prompt contains canary.Sentinel; a non-selected task's prompt is byte-identical to the input; the function is idempotent on repeated calls with the same index. No fenced code in this action. Per INFRA-05 (production caller, RESEARCH Open Q5).
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestInjectCanary|TestPromptInject' -count=1</automated>
  </verify>
  <done>A deterministic production caller injects canary.Sentinel into select tasks (every Kth, by stable key); non-selected prompts unchanged; Sentinel referenced via canary package only; pure/byte-stable; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model completion → headline reduce | Untrusted model output; a contaminated completion MUST NOT inflate headline pass@1/verified_correctness/cost |
| canary Sentinel literal → injection/detection | Single source of truth (canary.Sentinel); divergence between injector and detector would silently disable the canary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02-01 | Tampering | contaminated rows inflating the headline | mitigate | cleanRows EXCLUDES contaminated rows from successCount + every metric reduce BEFORE the headline vector is built; fail-safe (a contaminated row is never silently counted) — the security-relevant integrity control of this phase |
| T-89-02-02 | Repudiation | contaminated cells disappearing without a trace | mitigate | excluded (task,mode) cells are listed in a deterministic leaderboard.md footnote (auditable); CanaryPassRate still measures contamination over all rows |
| T-89-02-03 | Spoofing | injector/detector Sentinel divergence | mitigate | both the production caller and rowCanary reference canary.Sentinel (single source, canary.go:37) — never re-derived (anti-pattern enforced by code review of the diff) |
| T-89-02-SC | Tampering | npm/pip/cargo installs | accept | zero external packages installed; no go.mod change (RESEARCH audit N/A) |
</threat_model>

<verification>
- `go test ./bench/aggregator/... ./bench/runtime/... ./bench/canary/... -count=1` green.
- `go vet ./bench/...` clean.
- The leaderboard.golden.md diff adds ONLY the contamination footnote section.
- Manual diff confirmation: reduceCanaryRate still iterates ALL rows (exclusion did not leak into the measurement).
</verification>

<success_criteria>
- INFRA-05: a synthetic contaminated-response row trips the flag, is EXCLUDED from the headline (pass@1/verified_correctness/cost), and is listed in the leaderboard.md footnote.
- A deterministic production caller injects canary.Sentinel into select tasks (closing RESEARCH Open Q5).
- CanaryPassRate continues to measure contamination over all rows.
- leaderboard.golden.md regenerated; the diff is limited to the footnote.
</success_criteria>

<output>
Create `.planning/phases/89-reports-ci-policy-contamination-canary/89-02-SUMMARY.md` when done. Record: the cleanRows exclusion locus, the footnote rendering, the InjectPrompt production caller + its select-K policy, the synthetic-contamination test outcome, and the leaderboard.golden.md diff (footnote only). Flag for 89-03 that the lockstep guard TestAggregateCanaryAbsentIsEmDash's golden has shifted (footnote present) and its intent assertion needs the lockstep update in 89-03.
</output>
