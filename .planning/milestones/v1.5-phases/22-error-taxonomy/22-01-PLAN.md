---
phase: 22-error-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/errors/kinds.go
  - internal/errors/errors.go
  - internal/errors/errors_test.go
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03

must_haves:
  truths:
    - "7 Kind constants (NotFound, InvalidArgs, NoWorkspace, Unsupported, Internal, CircuitOpen, Timeout) are exported from internal/errors"
    - "Error struct carries Kind, Message, Tool, Detail fields and serializes to JSON with all four"
    - "Wrapping a cause error preserves the chain via errors.Is and errors.As"
    - "errors.Is(err, serr.ErrNotFound) works through fmt.Errorf %w wrapping"
    - "Nil *Error returned as error interface does not produce a false positive"
  artifacts:
    - path: "internal/errors/kinds.go"
      provides: "Kind type and 7 constants plus 7 sentinel vars"
      contains: "type Kind string"
    - path: "internal/errors/errors.go"
      provides: "Error struct, New, Wrap, WithTool, WithDetail, Error(), Unwrap(), Is(), MarshalJSON()"
      exports: ["New", "Wrap", "Error"]
    - path: "internal/errors/errors_test.go"
      provides: "Unit tests for all behaviors"
      min_lines: 80
  key_links:
    - from: "internal/errors/errors.go"
      to: "internal/errors/kinds.go"
      via: "same package, Kind type usage"
      pattern: "Kind Kind"
    - from: "internal/errors/errors.go"
      to: "errors (stdlib)"
      via: "Unwrap() method for chain traversal"
      pattern: "func.*Unwrap.*error"
---

<objective>
Create the `internal/errors/` package with a typed error taxonomy: Kind type with 7 constants, Error struct with builder-style constructors, and full errors.Is/As/Unwrap support.

Purpose: Provide a single, consistent error type that all 38+ MCP tools will use (in Phase 23) for structured, programmatic error reporting to agents.
Output: `internal/errors/` package with kinds.go, errors.go, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-error-taxonomy/22-CONTEXT.md
@.planning/phases/22-error-taxonomy/22-RESEARCH.md
@.planning/phases/22-error-taxonomy/22-PATTERNS.md

<interfaces>
<!-- Existing patterns to follow for Is() and MarshalJSON -->

From internal/kernel/lspool/circuit_err.go:
```go
func (e *CircuitOpenError) Is(target error) bool {
    return target == ErrCircuitOpen
}
```

From internal/mcp/errors.go:
```go
func (e ErrorDetail) MarshalJSON() ([]byte, error) {
    type Alias ErrorDetail
    return json.Marshal((*Alias)(&e))
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create internal/errors/ package with Kind type, Error struct, and tests</name>
  <files>internal/errors/kinds.go, internal/errors/errors.go, internal/errors/errors_test.go</files>
  <read_first>
    - internal/mcp/errors.go (existing sentinels and ErrorDetail pattern to follow for MarshalJSON)
    - internal/kernel/lspool/circuit_err.go (Is() pattern to follow for Kind-based matching)
    - .planning/phases/22-error-taxonomy/22-RESEARCH.md (exact code examples and anti-patterns)
    - .planning/phases/22-error-taxonomy/22-PATTERNS.md (naming conventions and structural patterns)
  </read_first>
  <behavior>
    - TestKinds: all 7 Kind constants exist with correct string values (not_found, invalid_args, no_workspace, unsupported, internal, circuit_open, timeout)
    - TestNew: serr.New(serr.NotFound, "msg") returns *Error with Kind=NotFound, Message="msg", Tool="", Detail=""
    - TestWrap: serr.Wrap(serr.Internal, "msg", causeErr) returns *Error where Unwrap() returns causeErr
    - TestWithTool: .WithTool("find_references") sets Tool field and returns same *Error for chaining
    - TestWithDetail: .WithDetail("detail text") sets Detail field and returns same *Error for chaining
    - TestErrorString: Error() returns "not_found: msg" without detail, "not_found: msg (detail)" with detail
    - TestIsKindMatch: errors.Is(serr.New(serr.NotFound, "x"), serr.ErrNotFound) returns true
    - TestIsKindMismatch: errors.Is(serr.New(serr.NotFound, "x"), serr.ErrTimeout) returns false
    - TestIsChainWrapped: errors.Is(fmt.Errorf("outer: %w", serr.New(serr.NotFound, "x")), serr.ErrNotFound) returns true
    - TestIsWrappedCause: errors.Is(serr.Wrap(serr.Internal, "msg", io.EOF), io.EOF) returns true
    - TestAs: errors.As(fmt.Errorf("wrap: %w", serr.New(serr.NotFound, "x").WithTool("t")), &target) sets target.Tool == "t"
    - TestMarshalJSON: json.Marshal produces {"kind":"not_found","message":"msg","tool":"t","detail":"d"} with all fields, omits empty tool/detail
    - TestNilError: a function returning error interface that has no error returns nil (not typed nil *Error)
    - TestSentinels: all 7 ErrXxx sentinel vars exist and have correct Kind
  </behavior>
  <action>
    Create `internal/errors/` directory with three files per D-02.

    **kinds.go** (per D-01):
    - Package declaration: `package errors`
    - `type Kind string`
    - 7 constants: `NotFound Kind = "not_found"`, `InvalidArgs Kind = "invalid_args"`, `NoWorkspace Kind = "no_workspace"`, `Unsupported Kind = "unsupported"`, `Internal Kind = "internal"`, `CircuitOpen Kind = "circuit_open"`, `Timeout Kind = "timeout"`
    - 7 sentinel vars: `ErrNotFound = &Error{Kind: NotFound}`, `ErrInvalidArgs = &Error{Kind: InvalidArgs}`, `ErrNoWorkspace = &Error{Kind: NoWorkspace}`, `ErrUnsupported = &Error{Kind: Unsupported}`, `ErrInternal = &Error{Kind: Internal}`, `ErrCircuitOpen = &Error{Kind: CircuitOpen}`, `ErrTimeout = &Error{Kind: Timeout}`

    **errors.go** (per D-04):
    - Package declaration: `package errors`
    - Imports: `encoding/json`, `fmt` (stdlib only, zero external deps)
    - `type Error struct` with fields: `Kind Kind` (json:"kind"), `Message string` (json:"message"), `Tool string` (json:"tool,omitempty"), `Detail string` (json:"detail,omitempty"), `cause error` (unexported, no json tag)
    - `func New(kind Kind, message string) *Error` -- returns `&Error{Kind: kind, Message: message}`
    - `func Wrap(kind Kind, message string, cause error) *Error` -- returns `&Error{Kind: kind, Message: message, cause: cause}`
    - `func (e *Error) WithTool(tool string) *Error` -- sets e.Tool, returns e
    - `func (e *Error) WithDetail(detail string) *Error` -- sets e.Detail, returns e
    - `func (e *Error) Error() string` -- returns `"kind: message (detail)"` or `"kind: message"` if no detail
    - `func (e *Error) Unwrap() error` -- returns e.cause
    - `func (e *Error) Is(target error) bool` -- if target is *Error, compare Kind fields; otherwise false
    - `func (e *Error) MarshalJSON() ([]byte, error)` -- use type alias anti-recursion pattern from internal/mcp/errors.go

    **CRITICAL anti-pattern (Pitfall 1):** Functions with `error` return type must never assign a `*Error` nil pointer to a variable and return it. Always return `nil` directly. Document this in a code comment.

    **errors_test.go:**
    - Package: `package errors` (internal tests, can access unexported fields)
    - Wait -- use `package errors_test` (external test package) to test via public API. Import as `serr "github.com/postfix/serena/internal/errors"`.
    - Use `github.com/stretchr/testify/assert` and `require` (already in go.mod)
    - Write TDD: create test file first with all behavior tests, run (RED), then implement kinds.go + errors.go (GREEN)
    - Tests must cover: Kind constants, New, Wrap, WithTool, WithDetail, Error(), Is() with kind match/mismatch, Is() through fmt.Errorf wrapping, Unwrap cause chain, errors.As extraction, MarshalJSON output, nil safety, all 7 sentinels
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && go test ./internal/errors/... -v -count=1 && go vet ./internal/errors/... && go build ./cmd/serena</automated>
  </verify>
  <acceptance_criteria>
    - internal/errors/kinds.go contains `type Kind string`
    - internal/errors/kinds.go contains all 7 constants: `NotFound Kind = "not_found"`, `InvalidArgs Kind = "invalid_args"`, `NoWorkspace Kind = "no_workspace"`, `Unsupported Kind = "unsupported"`, `Internal Kind = "internal"`, `CircuitOpen Kind = "circuit_open"`, `Timeout Kind = "timeout"`
    - internal/errors/kinds.go contains 7 sentinel vars: `ErrNotFound`, `ErrInvalidArgs`, `ErrNoWorkspace`, `ErrUnsupported`, `ErrInternal`, `ErrCircuitOpen`, `ErrTimeout`
    - internal/errors/errors.go contains `type Error struct` with fields Kind, Message, Tool, Detail, cause
    - internal/errors/errors.go contains `func New(kind Kind, message string) *Error`
    - internal/errors/errors.go contains `func Wrap(kind Kind, message string, cause error) *Error`
    - internal/errors/errors.go contains `func (e *Error) WithTool(tool string) *Error`
    - internal/errors/errors.go contains `func (e *Error) WithDetail(detail string) *Error`
    - internal/errors/errors.go contains `func (e *Error) Is(target error) bool`
    - internal/errors/errors.go contains `func (e *Error) Unwrap() error`
    - internal/errors/errors.go contains `func (e *Error) MarshalJSON()`
    - internal/errors/errors_test.go contains at least 10 test functions
    - `go test ./internal/errors/...` exits 0
    - `go vet ./internal/errors/...` exits 0
    - `go build ./cmd/serena` exits 0
  </acceptance_criteria>
  <done>All 7 Kind constants exported, Error struct with builder constructors and full errors.Is/As/Unwrap chain support, MarshalJSON serialization, comprehensive test suite passing, go vet clean, binary builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal package boundary | `internal/errors/` is only importable within serena -- no external input parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | Error.Detail field | accept | Detail is human-readable context string, not stack traces. Cause chain is unexported. MCP layer (Phase 23) controls what reaches clients. |
| T-22-02 | Tampering | Kind constants | accept | Kind is a string type; Go type system prevents arbitrary values at compile time in typed contexts. Runtime validation not needed -- invalid kinds are caught by code review, not by agents. |
</threat_model>

<verification>
- `go test ./internal/errors/... -v` -- all tests pass
- `go vet ./internal/errors/...` -- no issues
- `go build ./cmd/serena` -- binary builds successfully
- `go test ./...` -- full suite still passes (no regressions)
</verification>

<success_criteria>
- internal/errors/ package exists with kinds.go, errors.go, errors_test.go
- 7 Kind constants with correct snake_case string values
- 7 sentinel vars for errors.Is matching
- Error struct with Kind, Message, Tool, Detail, cause fields
- New(), Wrap(), WithTool(), WithDetail() constructors
- Error(), Unwrap(), Is(), MarshalJSON() implementations
- Test coverage for all behaviors including chain traversal and nil safety
- go test, go vet, go build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-error-taxonomy/22-01-SUMMARY.md`
</output>
