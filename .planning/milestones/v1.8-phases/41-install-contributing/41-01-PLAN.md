---
phase: 41-install-contributing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - INSTALL.md
autonomous: true
requirements:
  - INST-01
  - INST-02

must_haves:
  truths:
    - "A new user can install Serena with `go install` and configure any of the 6 supported clients using `serena setup <client>`"
    - "A user who prefers manual config can find accurate JSON/CLI configs for all 6 clients in a secondary section"
    - "HTTP mode is documented as a separate use case for shared daemon / multi-agent scenarios"
    - "Verification section references both `get_health` tool and `onboard_project`"
  artifacts:
    - path: "INSTALL.md"
      provides: "Complete installation and client configuration guide"
      contains: "serena setup"
  key_links:
    - from: "INSTALL.md"
      to: "internal/cli/setup_clients.go"
      via: "client list matches registry"
      pattern: "claude-code.*vscode.*jetbrains.*claude-desktop.*gemini-cli.*generic"
---

<objective>
Rewrite INSTALL.md to lead with `serena setup <client>` as the primary installation method, with manual configs as a secondary collapsed section. Remove stale agent entries (Codex, OpenCode, Cursor, Antigravity) and align the client list with the actual `clientRegistry()` in `internal/cli/setup_clients.go`.

Purpose: Users should be able to install and configure Serena in under a minute using the setup CLI, with manual fallback for advanced users.
Output: Rewritten INSTALL.md with accurate, current installation instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-install-contributing/41-RESEARCH.md
@.planning/phases/41-install-contributing/41-PATTERNS.md

<interfaces>
<!-- Client registry from internal/cli/setup_clients.go -->
<!-- 6 registered clients: claude-code, vscode, jetbrains, claude-desktop, gemini-cli, generic -->
<!-- Setup flags: --global, --uninstall, --skip-install, --dry-run, --output (generic only), --no-hooks (Claude Code only) -->

<!-- Config paths verified from setup_clients.go: -->
<!-- claude-code: uses `claude mcp add-json` CLI (no file-based config) -->
<!-- vscode: .vscode/mcp.json with "servers" key (NOT "mcpServers"), requires "type": "stdio" -->
<!-- jetbrains: .junie/mcp/mcp.json with "mcpServers" key -->
<!-- claude-desktop: ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) with "mcpServers" key -->
<!-- gemini-cli: uses `gemini mcp add` CLI (no file-based config) -->
<!-- generic: stdout JSON with "mcpServers" key -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite INSTALL.md with setup CLI as primary path</name>
  <files>INSTALL.md</files>
  <read_first>
    - INSTALL.md (current state -- 144 lines to be restructured)
    - README.md lines 58-135 (Quick Start pattern to follow for consistency)
    - internal/cli/setup_clients.go (authoritative client registry and config paths)
    - internal/cli/setup.go (setup command flags and usage)
    - .planning/phases/41-install-contributing/41-RESEARCH.md (verified source data)
    - .planning/phases/41-install-contributing/41-PATTERNS.md (structural patterns)
  </read_first>
  <action>
Rewrite INSTALL.md with the following section order (per D-05):

**1. Title and opening line:**
```
# Install Guide

Serena is a single Go binary. Install it, point your coding agent at it, and you are ready to go.
```

**2. Prerequisites section:**
- `go install github.com/postfix/serena/cmd/serena@latest` (requires Go 1.25+)
- Build from source alternative: `git clone` + `go build ./cmd/serena`
- PATH verification: `serena --help`
- Note about `$(go env GOPATH)/bin` in PATH

**3. Quick Start section (per D-01 -- setup CLI as primary):**
Heading: `## Quick Start`

Show the setup CLI as the one-command solution:
```bash
serena setup claude-code    # Claude Code
serena setup vscode         # VS Code / Cursor
serena setup jetbrains      # JetBrains IDEs
serena setup gemini-cli     # Gemini CLI
serena setup claude-desktop # Claude Desktop
```

Document flags: `--global` for user-wide registration, `--uninstall` to remove, `--dry-run` to preview, `--no-hooks` for Claude Code. Reference `serena setup --help` for all options.

Mention lazy initialization -- no upfront indexing delay.

**4. Manual Configuration section (per D-02 -- secondary, collapsed):**
Heading: `## Manual Configuration`

Wrap in `<details><summary>Manual configuration (without serena setup)</summary>` per README.md pattern.

Per D-03 and D-04, only include the 6 clients from `clientRegistry()`. Remove Codex, OpenCode, Cursor, Antigravity entirely.

For file-based clients, show JSON configs:
- **VS Code** (`.vscode/mcp.json`): Use `"servers"` key (NOT `"mcpServers"`), include `"type": "stdio"` field. This is a critical pitfall.
  ```json
  {
    "servers": {
      "serena": {
        "type": "stdio",
        "command": "serena",
        "args": ["--mode=stdio"]
      }
    }
  }
  ```
- **JetBrains** (`.junie/mcp/mcp.json`): Use `"mcpServers"` key.
  ```json
  {
    "mcpServers": {
      "serena": {
        "command": "serena",
        "args": ["--mode=stdio", "--profile=ide-assistant"]
      }
    }
  }
  ```
- **Claude Desktop** (platform-specific path -- show macOS `~/Library/Application Support/Claude/claude_desktop_config.json`, Linux `~/.config/Claude/claude_desktop_config.json`): Use `"mcpServers"` key.
  ```json
  {
    "mcpServers": {
      "serena": {
        "command": "serena",
        "args": ["--mode=stdio"]
      }
    }
  }
  ```
- **Generic** (any MCP client): Show `"mcpServers"` format, note `--output` flag for file output.

For CLI-based clients (per Research pitfall 4), show CLI commands instead of JSON:
- **Claude Code**: `claude mcp add-json serena '{"command":"serena","args":["--mode=stdio"]}'` (or just use `serena setup claude-code`)
- **Gemini CLI**: `gemini mcp add serena -- serena --mode=stdio` (or just use `serena setup gemini-cli`)

Close the `</details>` tag.

**5. HTTP Mode section (per D-06 -- separate section):**
Heading: `## HTTP Mode`

```bash
serena --mode=http --http-addr=127.0.0.1:8080
```

Explain: shared daemon, multiple agents, warm cache sharing across client disconnects.

**6. Verify Installation section (per D-07):**
Heading: `## Verify Installation`

Reference both `get_health` and `onboard_project`:
- `get_health` -- quick check that Serena daemon is running and language servers are healthy
- `onboard_project` -- full workspace initialization, prints summary of detected languages and available tools

**7. Next Steps section:**
Links to USAGE.md and README.md using existing `--` dash pattern.

**8. Legacy Python one-liner at bottom (per D-15):**
```
## Legacy Python

The `legacy/` directory contains the original Python Serena for reference only. It is not actively developed.
```

Use `--` (double dash) for inline dashes, not em dash, consistent with all other docs (per PATTERNS.md).
  </action>
  <verify>
    <automated>grep -c "serena setup" INSTALL.md | xargs test 3 -le && grep -q "servers" INSTALL.md && grep -q "get_health" INSTALL.md && grep -q "onboard_project" INSTALL.md && ! grep -qi "codex\|opencode\|cursor\|antigravity" INSTALL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "serena setup claude-code" INSTALL.md returns a match (D-01 primary path)
    - grep "serena setup vscode" INSTALL.md returns a match
    - grep "serena setup jetbrains" INSTALL.md returns a match
    - grep "serena setup gemini-cli" INSTALL.md returns a match
    - grep "serena setup claude-desktop" INSTALL.md returns a match
    - grep -q '"servers"' INSTALL.md returns 0 (VS Code uses "servers" not "mcpServers", per D-03 and Research pitfall 1)
    - grep -qi "codex" INSTALL.md returns 1 (removed per D-04)
    - grep -qi "opencode" INSTALL.md returns 1 (removed per D-04)
    - grep -qi "cursor" INSTALL.md returns 1 (removed per D-04)
    - grep -qi "antigravity" INSTALL.md returns 1 (removed per D-04)
    - grep "get_health" INSTALL.md returns a match (D-07)
    - grep "onboard_project" INSTALL.md returns a match (D-07)
    - grep "HTTP Mode" INSTALL.md returns a match (D-06 separate section)
    - grep "Manual configuration" INSTALL.md returns a match (D-02 secondary section)
    - grep "Legacy Python" INSTALL.md returns a match (D-15)
    - Section order is: Prerequisites, Quick Start, Manual Configuration, HTTP Mode, Verify Installation, Next Steps (D-05)
  </acceptance_criteria>
  <done>INSTALL.md leads with `serena setup` as primary method, manual configs are in a collapsed secondary section, client list matches the 6 clients from clientRegistry(), VS Code uses "servers" key, get_health is referenced in verification, and no stale agent entries remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- documentation-only change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | I (Info Disclosure) | INSTALL.md config examples | accept | No secrets in examples; all configs use generic command paths |
</threat_model>

<verification>
- INSTALL.md has section order matching D-05: Prerequisites -> Quick Start -> Manual Configuration -> HTTP Mode -> Verify Installation -> Next Steps
- All 6 clients from clientRegistry() are represented
- No stale agent configs remain (Codex, OpenCode, Cursor, Antigravity removed)
- VS Code config uses "servers" key with "type": "stdio"
- Claude Code and Gemini CLI show CLI commands, not file-based JSON
</verification>

<success_criteria>
INSTALL.md accurately guides users through installation using `serena setup <client>` as the primary path, with correct manual configs for all 6 supported clients as a secondary option.
</success_criteria>

<output>
After completion, create `.planning/phases/41-install-contributing/41-01-SUMMARY.md`
</output>
