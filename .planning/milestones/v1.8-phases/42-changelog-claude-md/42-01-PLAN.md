---
phase: 42-changelog-claude-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [CLOG-01, CLOG-02]
tags: [documentation, changelog]

must_haves:
  truths:
    - "CHANGELOG.md has a dated entry for every shipped milestone v1.0 through v1.7"
    - "v1.6 entry documents RepoMap, fuzzy editing cascade, and 23-grammar expansion"
    - "v1.7 entry documents setup CLI, get_health, Claude Code hooks, smart errors, progressive descriptions, and lazy init"
    - "Milestone dates in CHANGELOG.md match MILESTONES.md (v1.0=2026-04-08, v1.1=2026-04-09, v1.2=2026-04-10, v1.3=2026-04-11, v1.4=2026-04-14, v1.5=2026-04-15, v1.6=2026-04-20, v1.7=2026-04-22)"
    - "No `## [Unreleased]` placeholder sits above the v1.7 entry without content"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Complete milestone changelog through v1.7"
      contains: "## v1.7 — Developer Experience & Auto-Setup (2026-04-22)"
      min_lines: 180
  key_links:
    - from: "CHANGELOG.md v1.6 section"
      to: "MILESTONES.md v1.6 bullets + README RepoMap/Key Features sections"
      via: "feature parity — every shipped subsystem named in a ### subsection"
      pattern: "### RepoMap|### Fuzzy Editing|### Multi-Language"
    - from: "CHANGELOG.md v1.7 section"
      to: "MILESTONES.md v1.7 bullets + README Key Features table"
      via: "feature parity — every shipped subsystem named in a ### subsection"
      pattern: "### Setup CLI|### Health|### Claude Code Hooks|### Smart Error|### Progressive|### Lazy"
---

<objective>
Audit CHANGELOG.md for completeness and add missing milestone entries for v1.3, v1.4, v1.5, v1.6, and v1.7.

Purpose: CHANGELOG.md is the canonical release history for Serena. Currently it stops at v1.2 (2026-04-10), hiding five shipped milestones from the user-facing record. This plan closes that gap using MILESTONES.md as the source of truth.

Output: Updated `CHANGELOG.md` with complete, accurate entries for all 8 shipped milestones (v1.0 through v1.7), each with correct ship date and the key accomplishments from that milestone's roadmap. No "Unreleased" placeholder and no Python-port framing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONES.md
@.planning/ROADMAP.md
@.planning/milestones/v1.6-ROADMAP.md
@.planning/milestones/v1.7-ROADMAP.md
@CHANGELOG.md
@README.md

<!-- MILESTONES.md has some noise (Rule 1/2/3 bullets from legacy retrospective format). -->
<!-- The clean, canonical feature list per milestone is in the corresponding .planning/milestones/v{N}-ROADMAP.md file. -->
<!-- When in doubt, prefer v{N}-ROADMAP.md phase descriptions over MILESTONES.md bullets. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append v1.3, v1.4, v1.5 entries to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (current state — ends at v1.2)
    - .planning/MILESTONES.md (dates and accomplishments for v1.3-v1.5)
    - .planning/milestones/v1.3-ROADMAP.md, v1.4-ROADMAP.md, v1.5-ROADMAP.md (clean feature lists per phase)
  </read_first>
  <action>
Insert three new sections in CHANGELOG.md immediately before the existing `## v1.2 — Performance & Production Hardening (2026-04-10)` heading (so the file is in reverse-chronological order: v1.5 on top, then v1.4, then v1.3, then v1.2).

The exact content to insert (verbatim markdown, preserving the house style of `## vN.M — Title (YYYY-MM-DD)` + `### Subsection` bullets matching the existing v1.2 entry):

```markdown
## v1.5 — Typed Errors & Hardening (2026-04-15)

### Error Taxonomy
- `internal/errors/` package with 7 error kinds: `NotFound`, `InvalidArgs`, `NoWorkspace`, `Unsupported`, `Internal`, `CircuitOpen`, `Timeout`
- Builder pattern API with `serr.New` / `serr.Wrap` and JSON serialization
- Cause-chain wrapping compatible with `errors.Is` / `errors.As`
- Unified sentinels: `ErrCircuitOpen`, `ErrSessionExpired`, `ErrLSCrashed` migrated into the taxonomy (backward-compatible re-exports, later removed)

### Tool Migration
- All 38+ MCP tools across 10 packages (symbols, edit, fileops, diag, memory, workflow, profile, MCP core) migrated from raw `fmt.Errorf` strings to typed `serr.New` / `serr.Wrap`
- All 24 kernel tool handlers gained inline input validation — empty-string checks on required fields before any workspace or LS work

### Testing
- Three-band error tests upgraded with `extractKind` helper and `expectedKind` struct field for Kind-level assertions
- 4 typed error golden files: `invalid_args`, `no_workspace`, `not_found`, `unsupported`

## v1.4 — Integration Testing v2 (2026-04-14)

### Test Harness
- Importable `test/harness/` package — Runner, golden store, fixture helpers
- Build tag taxonomy: `integration`, `llm`, `llmjudge`

### Oracle Tests
- **Protocol oracle:** MCP handshake, `tools/list` validation, session isolation, reconnect resilience
- **Contract oracle:** JSON Schema Draft 2020-12 validation, 23 golden output files, 6 error category contracts, tool selectability heuristics
- **Scenario oracle:** 14+ full-cycle agent workflow tests across Go, Python, TypeScript, C++, Swift, Zig, JavaScript, PHP, SQL, Markdown, polyglot, unsupported, collision, and degraded fixtures

### LLM Behavioral Tests
- Tool selection across 33 tools, 11 disambiguation pairs, output interpretation
- Multi-provider support (Anthropic + DeepSeek)
- Judge scoring with 5-dimension rubrics, transcript pipeline, aggregate reporting — never blocks merge

## v1.3 — Documentation Catchup (2026-04-11)

### README & Observability Docs
- README **Production & Observability** section documenting metrics, tracing, admin endpoints, and graceful degradation
- Go-native CONTRIBUTING.md replacing the legacy Python contribution guide

### Install & Usage Guides
- USAGE.md accuracy fixes plus a benchmarks subsection
- INSTALL.md with per-agent MCP config sections for Claude Code, Codex, OpenCode, Cursor, Gemini CLI, Antigravity, and HTTP mode

### Changelog
- CHANGELOG.md v1.2 gap fill (items missed during the v1.2 writeup)

```

IMPORTANT:
- Do NOT modify the existing v1.2, v1.1, or v1.0 entries — they are already correct.
- Preserve exact date formatting: `## vN.M — Title (2026-MM-DD)` with an em-dash (`—`), matching the existing v1.2 heading.
- Use `###` for subsections, `-` (hyphen) for bullets, matching the house style of the existing v1.2 entry.
- No "Unreleased" or "TBD" section at the top — nothing above v1.7 after Task 2 runs.
  </action>
  <acceptance_criteria>
    - `grep -c '^## v1.5 — Typed Errors & Hardening (2026-04-15)$' CHANGELOG.md` returns `1`
    - `grep -c '^## v1.4 — Integration Testing v2 (2026-04-14)$' CHANGELOG.md` returns `1`
    - `grep -c '^## v1.3 — Documentation Catchup (2026-04-11)$' CHANGELOG.md` returns `1`
    - `grep -c '^## v1.2 — Performance & Production Hardening (2026-04-10)$' CHANGELOG.md` returns `1` (still present, unchanged)
    - `grep -c '^## v1.1 — Integration Testing (2026-04-09)$' CHANGELOG.md` returns `1` (unchanged)
    - `grep -c '^## v1.0 — MVP (2026-04-08)$' CHANGELOG.md` returns `1` (unchanged)
    - The v1.5 section appears BEFORE the v1.4 section, which appears BEFORE v1.3, which appears BEFORE v1.2 (verify via `grep -n '^## v' CHANGELOG.md` — line numbers strictly ascending with versions strictly descending)
    - `grep -q 'internal/errors/' CHANGELOG.md`
    - `grep -q 'test/harness/' CHANGELOG.md`
    - `grep -q 'USAGE.md' CHANGELOG.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^## v1.5\|^## v1.4\|^## v1.3\|^## v1.2\|^## v1.1\|^## v1.0' CHANGELOG.md | grep -q '^6$' &amp;&amp; grep -q 'internal/errors/' CHANGELOG.md &amp;&amp; grep -q 'test/harness/' CHANGELOG.md</automated>
  </verify>
  <done>Three new entries (v1.5, v1.4, v1.3) inserted above v1.2 in reverse-chronological order; all existing entries unchanged; house style preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Prepend v1.6 and v1.7 entries to CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (current state after Task 1)
    - .planning/MILESTONES.md (v1.7 bullets at lines 3-18, v1.6 summary)
    - .planning/milestones/v1.6-ROADMAP.md (clean Phase 25-33 feature list)
    - .planning/milestones/v1.7-ROADMAP.md (clean Phase 34-38 feature list and success criteria)
    - README.md (cross-check feature names: "Fuzzy Editing", "Smart Errors", "Progressive Descriptions", "Health Monitoring", "Lazy Initialization", "Setup CLI", "RepoMap")
  </read_first>
  <action>
Insert two new top-level sections at the very top of the body of CHANGELOG.md, immediately after the intro line `All notable changes to Serena (Go) are documented here.` and the blank line that follows it. Order: v1.7 first (newest), then v1.6, then the existing v1.5 (from Task 1).

Use exactly this verbatim content:

```markdown
## v1.7 — Developer Experience & Auto-Setup (2026-04-22)

### Setup CLI
- One-command MCP registration: `serena setup <client>` for 6 clients — Claude Code, VS Code, JetBrains, Claude Desktop, Gemini CLI, and generic MCP clients
- Automatic project language detection in the working directory and pre-installation of available language servers
- Uses client CLIs as subprocess (e.g., `claude mcp add-json`) rather than writing config files directly
- `--global` flag for user-wide registration; `--no-hooks` opt-out for Claude Code hook installation

### Health & Status
- `get_health` MCP tool returning per-workspace language server status (running, crashed, indexing), capabilities, and indexing progress
- `serena status` CLI command with human-readable summary, `--json` machine-readable output, and `--verbose` detail mode
- Error-only defaults — surfaces only actionable failures unless verbose is requested

### Claude Code Hooks
- Automatic hook installation during `serena setup claude-code`:
  - **SessionStart** hook activates the workspace when a Claude Code session begins
  - **PreToolUse** hook nudges agents toward symbolic tools (`find_symbol`, `get_symbols_overview`) when they overuse grep/read
  - **Stop** hook cleans up session data on exit
- `--no-hooks` opt-out for users who manage their own hook config

### Smart Error Responses
- MCP middleware that enriches parameter typos and enum value errors with "Did you mean?" suggestions via Levenshtein distance
- Suggestions only correct parameters within the same tool — never redirect to a different tool
- Implemented as middleware wrapping the existing typed error taxonomy — error kinds unchanged

### Progressive Descriptions
- Tiered brief/detailed tool descriptions — listings show brief under 100 tokens each
- `get_tool_help <tool_name>` MCP tool for comprehensive on-demand docs with parameters, types, and usage examples
- Golden-file regression gating — no description ships without passing behavioral tool-selection tests

### Lazy Workspace Initialization
- `sync.Once` per workspace path — first MCP tool call transparently activates the workspace
- Concurrent first calls from multiple agents safely serialized (no duplicate initialization or races)
- No upfront indexing delay after setup

## v1.6 — Context Intelligence & Resilient Editing (2026-04-20)

### Fuzzy Editing
- 4-strategy cascade in `internal/fuzzy/`: exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder
- Common-prefix indentation reflow — preserves indentation when rewriting at different nesting levels
- Ambiguity refusal — ambiguous matches return a structured diff with options rather than silently applying
- Ellipsis placeholder support — `...` in the search block matches across any number of lines
- Strategy reporting in the result envelope so agents can see which strategy matched

### Fuzzy Edit Integration
- Standalone `fuzzy_edit` MCP tool for direct fuzzy match-and-replace
- `replace_in_file` falls back to fuzzy matching when exact matching fails
- `replace_symbol_body` gains a `search_body` parameter for fuzzy body matching before tree-sitter surgery

### RepoMap Context Intelligence
- Tree-sitter tag extraction with per-language `.scm` queries and a shared `GrammarRegistry`
- LSP `documentSymbol` fallback extractor for languages without tree-sitter grammars
- SQLite tag cache with mtime-based invalidation — survives daemon restart
- Scope-aware elision renderer preserves enclosing scope when trimming for token budget
- Cross-file reference graph with hand-rolled PageRank (~60 LOC, no third-party dep, supports personalization)
- Token-budgeted tree renderer using binary search to fit any target budget
- `get_repo_map` MCP tool — ranked structural overview of the repository
- `get_context` MCP tool — given relevant files, returns ranked symbols and definitions across the codebase
- LSP enrichment of tag hover/signature information after PageRank ranking

### Multi-Language Grammar Expansion
- Tree-sitter grammar coverage expanded from 5 to **23 languages** (full aider parity): Java, C, C++, C#, Ruby, PHP, JavaScript, Kotlin, Scala, Bash, Haskell, Julia, OCaml, Lua, Zig, HCL, plus Swift and R via locally vendored bindings
- Both tag and body queries defined per language
- Best-of-breed query merging from aider community collections
- Unit, integration, and Wave 1 LS fixture tests

### Verification & Wiring
- Cache persistence verified end-to-end (daemon restart returns identical output from SQLite cache without re-extraction)
- FallbackExtractor wired through `walkAndExtract` and the daemon post-init path for languages without tree-sitter coverage

```

IMPORTANT:
- `—` is the em-dash character (U+2014), matching the existing v1.2 heading style.
- The v1.7 section MUST appear first (after the intro and blank line), then v1.6, then v1.5 (written by Task 1), and so on.
- Do not touch the existing intro line `All notable changes to Serena (Go) are documented here.` — leave it verbatim.
- Do not add an "Unreleased" section at the top.
- Use README.md's capitalization for feature names where they overlap (e.g., "Fuzzy Editing", "Smart Errors", "Progressive Descriptions", "Lazy Initialization", "Setup CLI"). Do not use "port" or "rewrite" anywhere in the new entries.
  </action>
  <acceptance_criteria>
    - `grep -c '^## v1.7 — Developer Experience &amp; Auto-Setup (2026-04-22)$' CHANGELOG.md` returns `1`
    - `grep -c '^## v1.6 — Context Intelligence &amp; Resilient Editing (2026-04-20)$' CHANGELOG.md` returns `1`
    - `grep -n '^## v' CHANGELOG.md` shows version numbers strictly descending from top to bottom: v1.7 → v1.6 → v1.5 → v1.4 → v1.3 → v1.2 → v1.1 → v1.0
    - `grep -q 'serena setup <client>' CHANGELOG.md` (v1.7 Setup CLI)
    - `grep -q 'get_health' CHANGELOG.md` (v1.7 Health)
    - `grep -q 'SessionStart' CHANGELOG.md` (v1.7 Hooks)
    - `grep -q 'PreToolUse' CHANGELOG.md` (v1.7 Hooks)
    - `grep -q 'Did you mean' CHANGELOG.md` (v1.7 Smart Errors)
    - `grep -q 'get_tool_help' CHANGELOG.md` (v1.7 Progressive)
    - `grep -q 'Lazy' CHANGELOG.md` (v1.7 Lazy Init)
    - `grep -q '4-strategy cascade' CHANGELOG.md` (v1.6 Fuzzy)
    - `grep -q 'fuzzy_edit' CHANGELOG.md` (v1.6 Fuzzy Integration)
    - `grep -q 'PageRank' CHANGELOG.md` (v1.6 RepoMap)
    - `grep -q 'get_repo_map' CHANGELOG.md` (v1.6 RepoMap)
    - `grep -q 'get_context' CHANGELOG.md` (v1.6 RepoMap)
    - `grep -q '23 languages' CHANGELOG.md` (v1.6 Grammar Expansion)
    - `grep -cE '\bport\b|\brewrite\b' CHANGELOG.md | grep -q '^0$'` (no Python-port framing)
    - `wc -l CHANGELOG.md | awk '{print ($1 > 180)}'` returns `1` (file substantially expanded)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^## v1.7 — Developer Experience &amp; Auto-Setup (2026-04-22)$' CHANGELOG.md &amp;&amp; grep -q '^## v1.6 — Context Intelligence &amp; Resilient Editing (2026-04-20)$' CHANGELOG.md &amp;&amp; grep -q 'serena setup <client>' CHANGELOG.md &amp;&amp; grep -q 'PageRank' CHANGELOG.md &amp;&amp; grep -q '23 languages' CHANGELOG.md &amp;&amp; ! grep -qE '\bport\b|\brewrite\b' CHANGELOG.md</automated>
  </verify>
  <done>v1.7 and v1.6 entries prepended in reverse-chronological order; all shipped subsystems from MILESTONES.md represented as `###` subsections; no Python-port framing anywhere in the file.</done>
</task>

</tasks>

<verification>
After both tasks complete, run all acceptance-criteria grep commands. The final CHANGELOG.md should contain, in order from top to bottom:

1. Title + intro
2. `## v1.7 — Developer Experience & Auto-Setup (2026-04-22)` — 6 subsections
3. `## v1.6 — Context Intelligence & Resilient Editing (2026-04-20)` — 5 subsections
4. `## v1.5 — Typed Errors & Hardening (2026-04-15)` — 3 subsections
5. `## v1.4 — Integration Testing v2 (2026-04-14)` — 3 subsections
6. `## v1.3 — Documentation Catchup (2026-04-11)` — 3 subsections
7. `## v1.2 — Performance & Production Hardening (2026-04-10)` (unchanged)
8. `## v1.1 — Integration Testing (2026-04-09)` (unchanged)
9. `## v1.0 — MVP (2026-04-08)` (unchanged)
</verification>

<success_criteria>
- CLOG-01: CHANGELOG.md has accurate entries for all milestones v1.0-v1.7 — verified by 8 `## vN.M` headings in descending version order with correct ISO dates matching MILESTONES.md.
- CLOG-02: v1.6 and v1.7 entries are complete — verified by presence of every shipped subsystem name (setup CLI, get_health, SessionStart/PreToolUse, Did you mean, get_tool_help, Lazy, 4-strategy cascade, fuzzy_edit, PageRank, get_repo_map, get_context, 23 languages).
- No "port" / "rewrite" language anywhere in CHANGELOG.md.
- File length > 180 lines (was 91).
</success_criteria>

<output>
After completion, create `.planning/phases/42-changelog-claude-md/42-01-SUMMARY.md` documenting:
- Milestones added (v1.3, v1.4, v1.5, v1.6, v1.7)
- Line count before/after (91 → ~220+)
- Cross-check: every subsection in v1.6 and v1.7 entries maps back to a MILESTONES.md bullet or v{N}-ROADMAP.md phase
</output>
