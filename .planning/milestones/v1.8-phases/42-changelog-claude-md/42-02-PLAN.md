---
phase: 42-changelog-claude-md
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [CLMD-01, CLMD-02]
tags: [documentation, claude-md, ai-instructions]

must_haves:
  truths:
    - "CLAUDE.md project description presents Serena as a standalone Go-native product matching README.md identity"
    - "CLAUDE.md architecture section lists RepoMap, fuzzy edit engine, setup CLI, and health tools"
    - "CLAUDE.md states a tool count of 41+ (or higher) matching README's 40+/41+ claim"
    - "No 'port' or 'rewrite' language anywhere in CLAUDE.md outside the single explicit disclaimer"
    - "Existing sections preserved: Go Development Commands, Legacy Python Commands, GSD Workflow Enforcement, Developer Profile, Test Markers"
    - "Every package path listed in CLAUDE.md exists on disk"
    - "Every middleware name listed in CLAUDE.md exists in internal/mcp/ source"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Refreshed AI assistant instructions reflecting v1.7 reality"
      contains: "RepoMap"
      min_lines: 130
  key_links:
    - from: "CLAUDE.md Project section"
      to: "README.md tagline and positioning (lines 7-10, 36-45)"
      via: "identity parity — Serena is 'IDE for your coding agent', 40+/41+ tools, 52 languages, Go binary"
      pattern: "IDE for your coding agent|40\\+|41\\+"
    - from: "CLAUDE.md Architecture section"
      to: "actual internal/ and cmd/ directory layout (internal/repomap/, internal/fuzzy/, internal/kernel/health/, internal/kernel/help/, internal/skill/repomap/, internal/cli/setup*.go, internal/cli/status*.go)"
      via: "path accuracy — every listed package path is verified to exist on disk (test -d / test -f)"
      pattern: "internal/repomap|internal/fuzzy|internal/kernel/health|internal/kernel/help|internal/skill/repomap|internal/cli/setup"
    - from: "CLAUDE.md MCP Middleware Stack subsection"
      to: "actual middleware install order in internal/daemon/daemon.go (steps 14, 14b, 14c)"
      via: "LIFO execution order — middlewares installed later run earlier in the chain; brief descriptions are applied inside ProfileFilterMiddleware's tools/list handler (middleware.go:286-294), not a separate middleware"
      pattern: "TelemetryMiddleware|ProfileFilterMiddleware|SuggestionMiddleware|LazyInitMiddleware"
---

<objective>
Refresh CLAUDE.md so AI assistants working on this repo see Serena as the standalone Go-native product it is today, with an architecture map that includes RepoMap, fuzzy editing, setup CLI, health tools, brief descriptions, and lazy init.

Purpose: CLAUDE.md is the instructions file AI assistants (Claude Code, Codex, etc.) read first when entering this repo. The current file describes Serena using v1.0/v1.1-era terms (38+ tools, 4-layer architecture with no RepoMap or fuzzy engine) and is missing every major subsystem shipped in v1.6 and v1.7. This plan updates the Project and Architecture sections to match current reality (41+ tools, RepoMap skill, fuzzy edit engine, setup CLI, health skill, etc.) while preserving the existing Go Development Commands, Legacy Python Commands, GSD Workflow Enforcement, and Developer Profile sections untouched.

Output: Updated `CLAUDE.md` with a refreshed Project description, expanded Architecture section (adds `internal/repomap/`, `internal/fuzzy/`, `internal/cli/` setup + status commands, `internal/kernel/health/`, `internal/kernel/help/`, `internal/skill/repomap/`, the four real MCP middlewares), updated tool count (41+), updated Technology Stack, and updated Key Patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@README.md
@.planning/MILESTONES.md
@.planning/PROJECT.md

<!-- Identity source: README.md lines 7-10, 36-45, 149-168 (Key Features + RepoMap + tool table counts) -->
<!-- Architecture truth: the actual on-disk layout, confirmed during planning:
     - internal/repomap/        (RepoMap engine)
     - internal/fuzzy/          (fuzzy match cascade)
     - internal/kernel/health/  (get_health tool — NOT internal/skill/health/)
     - internal/kernel/help/    (get_tool_help tool — NOT internal/skill/help/)
     - internal/skill/repomap/  (RepoMap skill wrapper — this one IS under internal/skill/)
     - internal/cli/setup*.go   (setup command — NOT cmd/serena/setup/)
     - internal/cli/status*.go  (status command — NOT cmd/serena/status/)
     - cmd/serena/main.go       (the only file under cmd/serena/; no subpackages)
     - internal/mcp/middleware.go      (TelemetryMiddleware + ProfileFilterMiddleware with embedded brief descriptions)
     - internal/mcp/suggest.go         (SuggestionMiddleware)
     - internal/mcp/lazy_init.go       (LazyInitMiddleware)
     - internal/daemon/daemon.go L309-351 (actual install order: TelemetryMiddleware → ProfileFilter → Suggestion → LazyInit; LIFO execution means LazyInit runs first)
-->
<!-- Legacy framing rule: use "Originally inspired by Python Serena" — never "port" / "rewrite" outside the single disclaimer sentence -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh CLAUDE.md Project section and tool count (CLMD-01)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current state — Project section at lines 29-37)
    - README.md (identity source — tagline at lines 7-10, "How Serena Works" at lines 36-45, RepoMap at lines 160-166, Key Features table at lines 149-158)
    - .planning/PROJECT.md (product identity reference)
  </read_first>
  <action>
Replace the Project section of CLAUDE.md (currently lines 29-37, bounded by `## Project` header and the next `## Architecture` header) with the following exact content:

```markdown
## Project

**Serena** — The IDE for your coding agent. A Go-native code intelligence platform for MCP.

Serena provides 41+ MCP tools for semantic code retrieval, editing, and refactoring across 52 languages via LSP. It ships as a **single Go binary** with no Python, Docker, or runtime dependencies, running as a **persistent daemon** that keeps language servers warm between agent sessions.

Targets coding agents (Claude Code, Codex, Gemini CLI, IDE assistants) that need symbol-level operations — go-to-definition, find references, rename across files, replace symbol body, blast-radius analysis — backed by real language servers with warm persistent caching, a ranked RepoMap for structural context, and fuzzy editing that tolerates LLM output drift.

**Core Value:** Rock-solid LSP-backed MCP runtime that survives client disconnects, shares warm caches across sessions, serves ranked structural context on demand, and exposes semantic code operations as agent tools — all from a single binary with one-command client setup.

**Legacy reference:** The `legacy/` directory contains the original Python Serena as a read-only reference. All active development is in Go. Serena is a standalone Go product originally inspired by Python Serena, not a port or rewrite.
```

IMPORTANT:
- Use the em-dash character `—` (U+2014), NOT `--` — this matches README.md's tagline style.
- The phrase `"Originally inspired by Python Serena"` is acceptable paraphrased as `"originally inspired by Python Serena"` in running prose (as above), but the words `port` and `rewrite` must appear ONLY in the explicit `"not a port or rewrite"` disclaimer sentence — nowhere else in the file.
- Keep `## Project` and `## Architecture` as the section boundaries — do not renumber or reorder.
- Do not touch the `## Go Development Commands` or `## Legacy Python Commands` sections above this.
  </action>
  <acceptance_criteria>
    - `grep -q '^## Project$' CLAUDE.md`
    - `grep -q 'The IDE for your coding agent' CLAUDE.md`
    - `grep -q '41+ MCP tools' CLAUDE.md`
    - `grep -q '52 languages' CLAUDE.md`
    - `grep -q 'RepoMap' CLAUDE.md`
    - `grep -q 'fuzzy editing' CLAUDE.md`
    - `grep -q 'single Go binary' CLAUDE.md`
    - `grep -q 'originally inspired by Python Serena' CLAUDE.md`
    - `grep -c 'port or rewrite' CLAUDE.md` returns `1` (the single explicit disclaimer)
    - `grep -cE '\bport\b|\brewrite\b' CLAUDE.md` returns `2` (only the two words in the single disclaimer phrase)
    - `grep -q '38+ MCP tools' CLAUDE.md` returns empty (old tool count removed)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'The IDE for your coding agent' CLAUDE.md &amp;&amp; grep -q '41+ MCP tools' CLAUDE.md &amp;&amp; grep -q 'RepoMap' CLAUDE.md &amp;&amp; grep -q 'fuzzy editing' CLAUDE.md &amp;&amp; grep -q 'originally inspired by Python Serena' CLAUDE.md &amp;&amp; ! grep -q '38+ MCP tools' CLAUDE.md</automated>
  </verify>
  <done>Project section rewritten to match README.md identity with 41+ tool count, RepoMap and fuzzy editing mentioned, legacy framing uses "originally inspired by" (single disclaimer sentence, no other port/rewrite usage).</done>
</task>

<task type="auto">
  <name>Task 2: Expand CLAUDE.md Architecture, Technology Stack, and Key Patterns for v1.6/v1.7 subsystems (CLMD-02)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current state after Task 1 — Architecture section around lines 39-77, Technology Stack 79-89, Key Patterns 91-114)
    - README.md (lines 149-201 for Key Features and Production/Observability, lines 160-166 for RepoMap)
    - .planning/MILESTONES.md (v1.6/v1.7 subsystem inventory)
    - internal/daemon/daemon.go lines 309-351 (authoritative middleware install order)
    - internal/mcp/middleware.go lines 37-46, 286-294 (brief descriptions applied inside ProfileFilterMiddleware, NOT a separate middleware)
    - internal/mcp/lazy_init.go lines 106-112 (LazyInitMiddleware must be installed LAST so it runs FIRST in LIFO)
  </read_first>
  <action>
Make surgical edits inside the existing `## Architecture` section and the `## Technology Stack` / `## Key Patterns` sections. Do NOT touch `## Go Development Commands`, `## Legacy Python Commands`, `## Constraints`, `## GSD Workflow Enforcement`, or `## Developer Profile`.

**Edit 1: Extend Layer 1 (Code Intelligence Kernel)** — after the existing `- \`internal/kernel/jsonrpc/\` -- Custom JSON-RPC 2.0 codec for LS communication` line and BEFORE the `- \`protocol/gen/\` -- Generated LSP 3.17 types ...` line, insert these four bullets (health and help tools live UNDER the kernel, not under skills):

```markdown
- `internal/kernel/health/` -- `get_health` MCP tool returning per-workspace LS status, capabilities, and indexing progress (kernel-level because it introspects kernel state); wrapped as a skill via `skill_adapter.go`
- `internal/kernel/help/` -- `get_tool_help` MCP tool serving on-demand comprehensive tool documentation pulled from the tool registry; wrapped as a skill via `skill_adapter.go`
- `internal/fuzzy/` -- 4-strategy fuzzy match cascade (exact, whitespace-normalized, indent-flexible, ellipsis-placeholder) with ambiguity refusal and indentation reflow; used by `replace_in_file`, `replace_symbol_body`, and standalone `fuzzy_edit` tool
- `internal/repomap/` -- Tag extraction (tree-sitter grammars for 23 languages + LSP documentSymbol fallback), SQLite tag cache with mtime invalidation, scope-aware elision, cross-file PageRank, token-budgeted tree renderer
```

**Edit 2: Extend Layer 2 (Skills & Multi-Language)** — after the existing `- \`internal/skill/workflow/\` -- Onboarding and session handoff tools` line and BEFORE the `- \`internal/memory/\` --` line, insert this single bullet (only `internal/skill/repomap/` is an actual skill package; health and help live under `internal/kernel/` — see Edit 1):

```markdown
- `internal/skill/repomap/` -- `get_repo_map` and `get_context` MCP tools wrapping the RepoMap engine; registers via daemon post-init wiring (`SetEnrichFn`) for LSP enrichment of tags
```

**Edit 3: Update the Layer 3 + Daemon Bootstrap area** — replace the existing block that begins with `### Layer 3: Agent Profiles` and ends at the end of the `### Daemon Bootstrap (\`internal/daemon/daemon.go\`)` block (before `## Technology Stack`) with the following expanded content:

```markdown
### Layer 3: Agent Profiles & Setup
- `internal/profile/` -- 5 agent profiles (claude-code, codex, ide-assistant, ci-bot, full), 4 modes (read/edit/review/admin)
- `internal/config/` -- 4-layer config: CLI > project (.serena/) > user (~/.serena/) > profile defaults
- `internal/cli/setup.go`, `internal/cli/setup_clients.go`, `internal/cli/setup_detect.go`, `internal/cli/setup_hooks.go`, `internal/cli/setup_output.go`, `internal/cli/setup_health.go` -- `serena setup <client>` one-command MCP registration for 6 clients (Claude Code, VS Code, JetBrains, Claude Desktop, Gemini CLI, generic) with language detection, LS pre-installation, and Claude Code hook installer
- `internal/cli/status.go`, `internal/cli/status_output.go` -- `serena status` CLI producing human-readable workspace health summary (`--json`, `--verbose` modes)
- `cmd/serena/main.go` -- single entrypoint; all CLI subcommands (setup, status, activate, deactivate, nudge, root, daemon wiring) live in `internal/cli/` and are mounted via cobra in `internal/cli/root.go`

### MCP Middleware Stack (installed in `internal/daemon/daemon.go` steps 14, 14b, 14c)
Four real middlewares, defined in `internal/mcp/`:
- `TelemetryMiddleware` (middleware.go) -- absorbs the pre-v1.2 logging middleware; emits RED metrics on `tools/call`, injects per-tool deadlines via `BudgetFunc`, classifies outcomes (success / timeout / circuit_open / internal / ...)
- `ProfileFilterMiddleware` (middleware.go) -- filters `tools/list` by active profile; ALSO applies brief descriptions from `ToolRegistry.BriefDescriptions()` (middleware.go:286-294) and then profile-specific description overrides. Brief descriptions are NOT a separate middleware — they piggyback on the profile filter's `tools/list` pass.
- `SuggestionMiddleware` (suggest.go) -- enriches parameter-typo and enum-value errors with "Did you mean?" suggestions via Levenshtein distance on tool schemas; never redirects to a different tool
- `LazyInitMiddleware` (lazy_init.go) -- `sync.Once` per workspace path; transparently activates the workspace on first tool call, serializes concurrent first calls

### Daemon Bootstrap (`internal/daemon/daemon.go`)
- Creates language registry, installer, kernel with pool, GrammarRegistry (23 languages), TagCache (SQLite, persistent)
- Imports all skill packages via blank imports (`imports.go`) for init() registration
- Calls `skill.InitAll()`, registers all tools centrally with MCP SDK
- Post-init wiring: `SetEnrichFn` for RepoMap LSP enrichment, `SetActivateCallback` for workspace activation, FallbackExtractor for languages without tree-sitter coverage
- Installs middleware in three steps (14, 14b, 14c), resolves active profile
- Fail-fast for core subsystems, degraded mode for optional providers
- Kernel-first shutdown ordering
```

**Edit 4: Update Technology Stack** — inside the existing `## Technology Stack` section, replace the bullet `- **MCP:** Official MCP Go SDK` with these two bullets:

```markdown
- **MCP:** Official MCP Go SDK
- **RepoMap:** `github.com/tree-sitter/go-tree-sitter` with 23 language grammars (including locally vendored Swift and R bindings), hand-rolled PageRank (~60 LOC)
```

**Edit 5: Update Key Patterns** — append a new subsection at the END of the `## Key Patterns` section (after the existing `### Worker Pool` block and before `## Constraints`):

```markdown
### Fuzzy Editing & RepoMap
- Edit tools fall back to `fuzzy.Match()` when exact matching fails; strategy used is reported in the result
- Ambiguous fuzzy matches are REFUSED with a diff — never silently applied
- RepoMap extraction is lazy: `walkAndExtract` populates the SQLite cache on demand; `get_repo_map` / `get_context` read from cache and fall back to tree-sitter or LSP `documentSymbol` when cache is cold
- Token budget fitting uses binary search over the elided tree; output scales to any repository size

### Setup & Hooks
- `serena setup <client>` (implemented in `internal/cli/setup*.go`) invokes client CLIs as subprocess (e.g., `claude mcp add-json`) rather than writing config files directly
- Claude Code hooks installed during `serena setup claude-code` (see `internal/cli/setup_hooks.go`): SessionStart (activate workspace), PreToolUse (nudge toward symbolic tools), Stop (cleanup); `--no-hooks` opts out
- Language detection (`internal/cli/setup_detect.go`) scans the project directory for known file extensions and pre-installs LSs via the three-tier installer before registering the MCP server

### Middleware Execution Order (LIFO)
Install order in `internal/daemon/daemon.go` is:
1. `InstallMiddleware` -- adds `TelemetryMiddleware` then `ProfileFilterMiddleware` (step 14)
2. `InstallSuggestionMiddleware` -- adds `SuggestionMiddleware` (step 14b)
3. `InstallLazyInitMiddleware` -- adds `LazyInitMiddleware` LAST (step 14c)

Because `mcp-go-sdk.AddReceivingMiddleware` composes in LIFO order, the execution order on an incoming request is the reverse of the install order:

`LazyInitMiddleware` → `SuggestionMiddleware` → `ProfileFilterMiddleware` (applies brief descriptions on `tools/list`) → `TelemetryMiddleware` → tool handler

LazyInit MUST run first so the workspace is activated before `TelemetryMiddleware` applies its per-tool deadline (see the install-order comment at `internal/mcp/lazy_init.go:106-108`). Any change to this order must preserve that invariant.
```

IMPORTANT:
- Leave the existing `## Constraints` section unchanged (including the `LSP only: No JetBrains or proprietary backends` constraint).
- Leave `## GSD Workflow Enforcement` and `## Developer Profile` sections completely untouched.
- Preserve the `-- ` (two dashes + space) style inside the existing Architecture bullets for consistency, not em-dashes, since that is the established style in the current Architecture block.
- All paths and middleware names in the edits above are the verified on-disk / in-source names. Do NOT substitute any invented names (`internal/skill/health/`, `internal/skill/help/`, `cmd/serena/setup/`, `cmd/serena/status/`, `BriefDescriptionMiddleware` — none of these exist and must NOT appear in the final file).
  </action>
  <acceptance_criteria>
    <!-- Real paths must appear -->
    - `grep -q 'internal/fuzzy/' CLAUDE.md`
    - `grep -q 'internal/repomap/' CLAUDE.md`
    - `grep -q 'internal/skill/repomap/' CLAUDE.md`
    - `grep -q 'internal/kernel/health/' CLAUDE.md`
    - `grep -q 'internal/kernel/help/' CLAUDE.md`
    - `grep -q 'internal/cli/setup' CLAUDE.md`
    - `grep -q 'internal/cli/status' CLAUDE.md`
    - `grep -q 'cmd/serena/main.go' CLAUDE.md`
    <!-- Real middleware names must appear -->
    - `grep -q 'TelemetryMiddleware' CLAUDE.md`
    - `grep -q 'ProfileFilterMiddleware' CLAUDE.md`
    - `grep -q 'SuggestionMiddleware' CLAUDE.md`
    - `grep -q 'LazyInitMiddleware' CLAUDE.md`
    <!-- Tools -->
    - `grep -q 'get_repo_map' CLAUDE.md`
    - `grep -q 'get_context' CLAUDE.md`
    - `grep -q 'get_health' CLAUDE.md`
    - `grep -q 'get_tool_help' CLAUDE.md`
    <!-- Features -->
    - `grep -q '4-strategy fuzzy match cascade' CLAUDE.md`
    - `grep -q 'PageRank' CLAUDE.md`
    - `grep -q '23 languages' CLAUDE.md`
    - `grep -q 'SessionStart' CLAUDE.md`
    - `grep -q 'PreToolUse' CLAUDE.md`
    - `grep -q '### Layer 3: Agent Profiles &amp; Setup' CLAUDE.md`
    - `grep -q '### MCP Middleware Stack' CLAUDE.md`
    - `grep -q '### Fuzzy Editing &amp; RepoMap' CLAUDE.md`
    - `grep -q '### Setup &amp; Hooks' CLAUDE.md`
    - `grep -q '### Middleware Execution Order' CLAUDE.md`
    <!-- Existing sections preserved -->
    - `grep -q '## GSD Workflow Enforcement' CLAUDE.md`
    - `grep -q '## Developer Profile' CLAUDE.md`
    - `grep -q '## Go Development Commands' CLAUDE.md`
    - `grep -q '## Legacy Python Commands' CLAUDE.md`
    <!-- FORBIDDEN invented names must NOT appear -->
    - `! grep -q 'internal/skill/health' CLAUDE.md`
    - `! grep -q 'internal/skill/help' CLAUDE.md`
    - `! grep -q 'cmd/serena/setup' CLAUDE.md`
    - `! grep -q 'cmd/serena/status' CLAUDE.md`
    - `! grep -q 'BriefDescriptionMiddleware' CLAUDE.md`
    <!-- Every listed package path MUST exist on disk -->
    - `test -d internal/repomap`
    - `test -d internal/fuzzy`
    - `test -d internal/kernel/health`
    - `test -d internal/kernel/help`
    - `test -d internal/skill/repomap`
    - `test -f internal/cli/setup.go`
    - `test -f internal/cli/setup_clients.go`
    - `test -f internal/cli/setup_detect.go`
    - `test -f internal/cli/setup_hooks.go`
    - `test -f internal/cli/status.go`
    - `test -f cmd/serena/main.go`
    <!-- Every listed middleware MUST exist in source -->
    - `grep -q 'func TelemetryMiddleware' internal/mcp/middleware.go`
    - `grep -q 'func ProfileFilterMiddleware' internal/mcp/middleware.go`
    - `grep -q 'InstallSuggestionMiddleware' internal/mcp/suggest.go`
    - `grep -q 'InstallLazyInitMiddleware' internal/mcp/lazy_init.go`
    <!-- File size sanity -->
    - `wc -l CLAUDE.md | awk '{print ($1 > 180)}'` returns `1`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'internal/fuzzy/' CLAUDE.md &amp;&amp; grep -q 'internal/repomap/' CLAUDE.md &amp;&amp; grep -q 'internal/kernel/health/' CLAUDE.md &amp;&amp; grep -q 'internal/kernel/help/' CLAUDE.md &amp;&amp; grep -q 'internal/skill/repomap/' CLAUDE.md &amp;&amp; grep -q 'internal/cli/setup' CLAUDE.md &amp;&amp; grep -q 'TelemetryMiddleware' CLAUDE.md &amp;&amp; grep -q 'ProfileFilterMiddleware' CLAUDE.md &amp;&amp; grep -q 'SuggestionMiddleware' CLAUDE.md &amp;&amp; grep -q 'LazyInitMiddleware' CLAUDE.md &amp;&amp; grep -q 'get_repo_map' CLAUDE.md &amp;&amp; ! grep -q 'internal/skill/health' CLAUDE.md &amp;&amp; ! grep -q 'internal/skill/help' CLAUDE.md &amp;&amp; ! grep -q 'cmd/serena/setup' CLAUDE.md &amp;&amp; ! grep -q 'cmd/serena/status' CLAUDE.md &amp;&amp; ! grep -q 'BriefDescriptionMiddleware' CLAUDE.md &amp;&amp; grep -q '## GSD Workflow Enforcement' CLAUDE.md &amp;&amp; grep -q '## Developer Profile' CLAUDE.md &amp;&amp; test -d internal/kernel/health &amp;&amp; test -d internal/kernel/help &amp;&amp; test -f internal/cli/setup.go &amp;&amp; test -f internal/cli/status.go</automated>
  </verify>
  <done>Architecture section lists RepoMap, fuzzy engine, kernel-resident health and help tools, the single real `internal/skill/repomap/` skill, setup/status commands under `internal/cli/`, and the four real MCP middlewares (Telemetry, ProfileFilter, Suggestion, LazyInit) with correct LIFO execution order. Technology Stack mentions RepoMap/tree-sitter stack. Key Patterns covers fuzzy editing rules, setup/hooks behavior, and middleware execution order. Existing preserved sections (Go Commands, Legacy Python Commands, Constraints, GSD Enforcement, Developer Profile) untouched. No invented paths or middleware names appear in the file.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full acceptance-criteria grep set. The final CLAUDE.md should:

1. Open with the unchanged title and `## Go Development Commands` + `## Legacy Python Commands` blocks.
2. Contain a refreshed `## Project` section with "IDE for your coding agent", 41+ tools, RepoMap mention, fuzzy editing mention, and the single `"not a port or rewrite"` disclaimer.
3. Contain an expanded `## Architecture` section with:
   - Layer 1 augmented with `internal/kernel/health/`, `internal/kernel/help/`, `internal/fuzzy/`, `internal/repomap/`
   - Layer 2 augmented with `internal/skill/repomap/` only (health and help are kernel-resident)
   - Layer 3 renamed to "Agent Profiles & Setup" and augmented with `internal/cli/setup*.go`, `internal/cli/status*.go`, `cmd/serena/main.go`
   - New `### MCP Middleware Stack` subsection naming TelemetryMiddleware, ProfileFilterMiddleware (with embedded brief descriptions), SuggestionMiddleware, LazyInitMiddleware
   - Daemon Bootstrap block updated for post-init wiring and three-step middleware install
4. Technology Stack includes the RepoMap/tree-sitter line.
5. Key Patterns includes `### Fuzzy Editing & RepoMap`, `### Setup & Hooks`, `### Middleware Execution Order` subsections with correct LIFO order.
6. `## Constraints`, `## GSD Workflow Enforcement`, `## Developer Profile` sections are byte-for-byte unchanged.
7. File length > 180 lines (was 137).
8. NONE of the invented names appear: `internal/skill/health`, `internal/skill/help`, `cmd/serena/setup`, `cmd/serena/status`, `BriefDescriptionMiddleware`.
9. Every package path listed in CLAUDE.md passes a `test -d` or `test -f` check against the repo.
</verification>

<success_criteria>
- CLMD-01: `## Project` section matches README.md identity — "IDE for your coding agent", 41+ tools, 52 languages, single Go binary, RepoMap and fuzzy editing named, "originally inspired by Python Serena" framing with explicit "not a port or rewrite" disclaimer as the single instance of those words.
- CLMD-02: `## Architecture` section names `internal/repomap/`, `internal/fuzzy/`, `internal/kernel/health/`, `internal/kernel/help/`, `internal/skill/repomap/`, `internal/cli/setup*.go`, `internal/cli/status*.go`, `cmd/serena/main.go`, and the four real middlewares (Telemetry, ProfileFilter, Suggestion, LazyInit) with correct LIFO execution order.
- Preserved sections (Go Commands, Legacy Python Commands, Test Markers, Constraints, GSD Enforcement, Developer Profile) survive intact.
- No Python-port framing outside the single disclaimer sentence.
- No invented paths (`internal/skill/health`, `internal/skill/help`, `cmd/serena/setup`, `cmd/serena/status`) and no invented middleware (`BriefDescriptionMiddleware`) appear anywhere in the file.
</success_criteria>

<output>
After completion, create `.planning/phases/42-changelog-claude-md/42-02-SUMMARY.md` documenting:
- Sections updated (Project, Architecture, Technology Stack, Key Patterns)
- Sections preserved (Go Commands, Legacy Python Commands, Constraints, GSD Enforcement, Developer Profile)
- Line count before/after (137 → ~200+)
- Cross-check: every new `internal/*` and `cmd/*` path listed was verified to exist on disk during execution (run the `test -d` / `test -f` block from Task 2's acceptance criteria and paste the results)
</output>
</content>
</invoke>