---
phase: 43-cross-doc-sync
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - CLMD-01
  - CLMD-02
must_haves:
  truths:
    - "CLAUDE.md fileops line says 7 file operation tools including fuzzy_edit"
    - "CLAUDE.md Layer 3 description says 7 clients (not 6)"
    - "CLAUDE.md fuzzy strategy names match canonical CHANGELOG form (indentation-flexible, not indent-flexible)"
    - "CLAUDE.md fuzzy strategy first token is 'exact match' (not 'exact') to match USAGE and CHANGELOG identically"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Agent-facing project reference"
      contains: "7 file operation tools"
  key_links:
    - from: "CLAUDE.md Layer 1 fileops line"
      to: "internal/kernel/fileops/"
      via: "tool count matches on-disk reality + auto-gen README table"
      pattern: "7 file operation tools"
---

<objective>
Close F-11, F-08 (CLAUDE.md component), and the CLAUDE.md client-count drift inside Layer 3:

1. Fix fileops tool count (6 → 7, add `fuzzy_edit` to enumeration) to match CONTRIBUTING.md:53 and the README auto-generated table.
2. Fix Layer 3 setup-CLI client count inside the file listing (6 → 7, include OpenCode).
3. Align fuzzy strategy enumeration vocabulary with canonical CHANGELOG form. The canonical list across CLAUDE.md, USAGE, and CHANGELOG must be IDENTICAL: `exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder` (first token is "exact match" — two words — not "exact").

Purpose: CLAUDE.md is the AI-facing architecture reference. When its counts drift from code and README, downstream agents (Claude Code sessions) learn wrong facts about their host.

Output: Updated CLAUDE.md Layer 1 fileops line, Layer 1 fuzzy line, and Layer 3 setup-CLI line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.8-INTEGRATION-CHECK.md

<interfaces>
<!-- Source of truth:
     - internal/kernel/fileops/ -- 7 tools (read, write, list, find, search, replace, fuzzy_edit)
     - internal/cli/setup_clients.go:37-46 -- 7 registrars
     - internal/fuzzy/strategies.go -- canonical names: exact, whitespace-normalized, indentation-flexible, ellipsis-placeholder
     - CHANGELOG.md:43 authoritative phrasing: "exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder"
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CLAUDE.md fileops tool count (6 → 7, add fuzzy_edit)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (line 57 — Layer 1 fileops bullet)
    - CONTRIBUTING.md (line 53 — canonical "7 file operation tools" form)
    - README.md (auto-generated tool table — 7 fileops rows)
  </read_first>
  <action>
    In CLAUDE.md line 57, replace the existing bullet:

      `- \`internal/kernel/fileops/\` -- 6 file operation tools (read, write, list, find, search, replace)`

    with:

      `- \`internal/kernel/fileops/\` -- 7 file operation tools (read, write, list, find, search, replace, fuzzy_edit)`

    This is a single in-place string replacement on one line.
  </action>
  <verify>
    <automated>grep -c "7 file operation tools" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "6 file operation tools" CLAUDE.md` returns nothing
    - `grep -c "7 file operation tools" CLAUDE.md` returns ≥1
    - `grep -E "fileops.*fuzzy_edit" CLAUDE.md` returns ≥1
  </acceptance_criteria>
  <done>CLAUDE.md:57 says "7 file operation tools (read, write, list, find, search, replace, fuzzy_edit)".</done>
</task>

<task type="auto">
  <name>Task 2: Fix CLAUDE.md Layer 3 setup-CLI client count (6 → 7, add OpenCode)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (line 77 — Layer 3 setup CLI bullet)
    - internal/cli/setup_clients.go (lines 37-46)
  </read_first>
  <action>
    In CLAUDE.md line 77, the current bullet enumerates 6 clients:

      `... \`serena setup <client>\` one-command MCP registration for 6 clients (Claude Code, VS Code, JetBrains, Claude Desktop, Gemini CLI, generic) with language detection, LS pre-installation, and Claude Code hook installer`

    Replace the count and enumeration with the canonical 7:

      `... \`serena setup <client>\` one-command MCP registration for 7 clients (Claude Code, VS Code, JetBrains, Claude Desktop, Gemini CLI, OpenCode, generic) with language detection, LS pre-installation, and Claude Code hook installer`

    Changes: "6 clients" → "7 clients"; insert "OpenCode," between "Gemini CLI," and "generic" inside the parenthetical list.
  </action>
  <verify>
    <automated>grep -c "for 7 clients" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "for 6 clients" CLAUDE.md` returns nothing
    - `grep -c "for 7 clients" CLAUDE.md` returns ≥1
    - `grep -E "Gemini CLI, OpenCode, generic" CLAUDE.md` returns ≥1
  </acceptance_criteria>
  <done>CLAUDE.md:77 Layer 3 setup-CLI bullet says "7 clients" with OpenCode enumerated.</done>
</task>

<task type="auto">
  <name>Task 3: Align CLAUDE.md fuzzy strategy names on canonical "exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder"</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (line 62 — Layer 1 fuzzy bullet)
    - CHANGELOG.md (line 43 — canonical "exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder")
    - docs/USAGE.md (canonical fuzzy strategy enumeration — must match CLAUDE.md character-for-character)
    - internal/fuzzy/strategies.go (function doc comments)
  </read_first>
  <action>
    In CLAUDE.md line 62, the current bullet uses the abbreviated form `indent-flexible` AND the shortened first token `exact`:

      `- \`internal/fuzzy/\` -- 4-strategy fuzzy match cascade (exact, whitespace-normalized, indent-flexible, ellipsis-placeholder) with ambiguity refusal and indentation reflow; used by \`replace_in_file\`, \`replace_symbol_body\`, and standalone \`fuzzy_edit\` tool`

    Replace the entire parenthetical with the canonical form used by BOTH CHANGELOG.md and docs/USAGE.md, so all three docs contain the identical substring:

      `(exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder)`

    Resulting bullet:

      `- \`internal/fuzzy/\` -- 4-strategy fuzzy match cascade (exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder) with ambiguity refusal and indentation reflow; used by \`replace_in_file\`, \`replace_symbol_body\`, and standalone \`fuzzy_edit\` tool`

    Two substitutions in the same parenthetical: `exact` → `exact match` (first token); `indent-flexible` → `indentation-flexible` (third token). Do NOT rewrite the rest of the bullet.
  </action>
  <verify>
    <automated>grep -c "exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "indent-flexible" CLAUDE.md` returns nothing (the short form is gone)
    - `grep -c "indentation-flexible" CLAUDE.md` returns ≥1
    - `grep -c "ellipsis-placeholder" CLAUDE.md` returns ≥1 (untouched)
    - `grep -c "exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder" CLAUDE.md` returns ≥1 (identical substring shared with USAGE.md and CHANGELOG.md)
    - `grep -E "cascade \(exact, whitespace-normalized" CLAUDE.md` returns nothing (old short-form first token is gone)
  </acceptance_criteria>
  <done>CLAUDE.md:62 fuzzy strategies enumeration matches CHANGELOG.md:43 and docs/USAGE.md vocabulary EXACTLY — the substring `exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder` appears verbatim in all three files.</done>
</task>

</tasks>

<verification>
```bash
grep "6 file operation tools" CLAUDE.md                                                      # empty
grep -c "7 file operation tools" CLAUDE.md                                                   # ≥1
grep "for 6 clients" CLAUDE.md                                                               # empty
grep -c "for 7 clients" CLAUDE.md                                                            # ≥1
grep "indent-flexible" CLAUDE.md                                                             # empty
grep -c "indentation-flexible" CLAUDE.md                                                     # ≥1
grep -c "exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder" CLAUDE.md   # ≥1
```
All seven checks must pass.
</verification>

<success_criteria>
CLAUDE.md Layer 1 fileops count is 7; Layer 3 setup CLI lists 7 clients including OpenCode; fuzzy strategy enumeration uses the canonical identical substring `exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder` also present in docs/USAGE.md and CHANGELOG.md.
</success_criteria>

<output>
After completion, create `.planning/phases/43-cross-doc-sync/43-05-SUMMARY.md`.
</output>
