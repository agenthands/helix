---
phase: 50
plan: 03
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - CONTRIBUTING.md
  - USAGE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
tags:
  - docs
  - cleanup
  - toolchain
must_haves:
  truths:
    - "CONTRIBUTING.md has a gopls compatibility subsection mentioning `>=v0.21` (or `or later`) as a floor — not an exact pin"
    - "CONTRIBUTING.md `Running Benchmarks` section documents the local-only flow via `make bench` / `make bench-baseline`"
    - "CONTRIBUTING.md `Benchmark CI Gate` section is removed (no replacement)"
    - "CONTRIBUTING.md mentions bench-impact PR responsibility once (per D-06 — single mention, not a process gate)"
    - "USAGE.md gopls troubleshooting subsection presents the v0.17.1/Go-1.25 issue as resolved upstream and no longer references capture-baseline.yml"
    - "PROJECT.md tech-debt line 139 no longer mentions the bench (D-10) or GrammarRegistry (D-11) sentences but preserves rust-analyzer + jdtls sentences verbatim"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Gopls compatibility guidance + local-only bench documentation; CI gate section removed"
      contains: "gopls"
    - path: "USAGE.md"
      provides: "Updated gopls troubleshooting subsection (resolved-upstream framing)"
      contains: "gopls"
    - path: ".planning/PROJECT.md"
      provides: "Cleaned tech-debt line preserving only the still-active items"
      contains: "rust-analyzer"
  key_links:
    - from: "CONTRIBUTING.md → Running Benchmarks"
      to: "Makefile → bench / bench-baseline targets"
      via: "documented commands match Plan 02 Make targets"
      pattern: "make bench"
    - from: "USAGE.md → gopls troubleshooting"
      to: "internal/langregistry"
      via: "doc-only — no code change; says PATH-installed gopls is what runs"
      pattern: "internal/langregistry"
---

<objective>
Land all three doc edits required by Phase 50: a CONTRIBUTING.md gopls subsection (D-01), a CONTRIBUTING.md "Running Benchmarks" rewrite + "Benchmark CI Gate" removal (D-05/D-06/D-07), a USAGE.md gopls troubleshooting rewrite (D-01), and a surgical PROJECT.md line 139 edit removing the bench (D-10) and GrammarRegistry (D-11) sentences while preserving the rust-analyzer and jdtls sentences verbatim.

Purpose: Closes TOOL-01 (gopls strategy is doc-only — no code or langregistry version pinning) and TOOL-02's documentation half. Locks in the local-only narrative that matches Plan 02's Make targets and Plan 01's deletions.

Output: 3 modified docs; grep matrix from RESEARCH §Validation Architecture passes; surrounding doc content not in the locked decisions remains untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-toolchain-go1.25-bench-local/50-CONTEXT.md
@.planning/phases/50-toolchain-go1.25-bench-local/50-RESEARCH.md
@.planning/phases/50-toolchain-go1.25-bench-local/50-PATTERNS.md
@./CLAUDE.md
@CONTRIBUTING.md
@USAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CONTRIBUTING.md — replace Running Benchmarks, delete Benchmark CI Gate, add gopls compatibility subsection</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (full file — confirm current line numbers; key sections: Running Benchmarks lines 130-152, Adding Language Support lines 172-176, Benchmark CI Gate lines 178-184)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-PATTERNS.md sections "CONTRIBUTING.md — gopls compatibility subsection (new)", "CONTRIBUTING.md — Running Benchmarks section rewrite", "CONTRIBUTING.md — Benchmark CI Gate delete"
    - .planning/phases/50-toolchain-go1.25-bench-local/50-CONTEXT.md (decisions D-01, D-05, D-06, D-07)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-RESEARCH.md "Code Examples — Suggested CONTRIBUTING.md Running Benchmarks replacement skeleton" and "Common Pitfalls — Pitfall 4: gopls release-version drift" (floor, not pin)
  </read_first>
  <action>
    Three coordinated edits to `CONTRIBUTING.md`. Use the Edit tool for each, reading the surrounding context to preserve unrelated content.

    **Edit A — Replace the Running Benchmarks section (currently lines 130-152) in full.**

    Find the existing section starting `## Running Benchmarks` and ending just before the next `##` heading (`## Adding a New MCP Tool`). Replace it with exactly the following block (write between the BEGIN and END markers; do NOT include the markers themselves):

    BEGIN-CONTRIBUTING-RUNNING-BENCHMARKS
    ## Running Benchmarks

    The benchmark suite lives in `test/bench/`. **Benchmarks run locally only** — the project does not run benchmarks on CI runners because shared GitHub-hosted runners produce noisy, untrustable baselines.

    Run the suite once and print to stdout:

    ```sh
    make bench
    ```

    Capture a local baseline (overwrites `test/bench/baselines/local.txt`, which is gitignored):

    ```sh
    make bench-baseline
    ```

    To compare two captures, install upstream `benchstat` and run it directly:

    ```sh
    go install golang.org/x/perf/cmd/benchstat@latest
    benchstat old.txt new.txt
    ```

    Key details:

    - Benchmarks use `testing.B.Loop` (Go 1.24+) to prevent compiler elision.
    - `test/bench/baselines/` is the conventional capture location; everything you save there is gitignored.
    - If your changes are likely to affect bench numbers, mention the local before/after deltas in your PR description. (Honor system — there is no PR-time gate.)
    END-CONTRIBUTING-RUNNING-BENCHMARKS

    Mandatory mechanics (per CONTEXT D-05/D-06/D-07 and PATTERNS.md analog):
    - Mirrors the structural template of `Running Integration Tests` (CONTRIBUTING.md lines 80-101): `## Heading` then one-paragraph what-and-where then fenced `sh` blocks then "Key details:" bullet list.
    - The bench-impact mention is ONE bullet at the bottom (per D-06: "single mention, not a process gate"). DO NOT introduce a process gate, checkbox, or required PR description field.
    - Documents `make bench` / `make bench-baseline` as the primary commands (not raw `go test`) — driven by D-07 making Make the documented surface.
    - The `benchstat` install is shown inline (matches the README in Plan 02).

    **Edit B — Delete the Benchmark CI Gate section (currently lines 178-184) in full, with no replacement.**

    Find the `## Benchmark CI Gate` heading and remove the entire section through the end of the bullet list (just before `## Legacy Python` or whatever the next `##` is). Per PATTERNS.md "must be removed entirely (no replacement — the local-only flow is documented in Running Benchmarks and the gopls subsection above)". After deletion, the previous section flows directly into the next `##`.

    **Edit C — Add a new gopls compatibility subsection (placement: immediately AFTER the Adding Language Support section, BEFORE the deleted Benchmark CI Gate slot).**

    The placement keeps language/toolchain content together: `Adding Language Support` (lines 172-176) is about per-language config; the gopls subsection is about a specific LS's runtime constraint. Insert exactly the following block (write between the BEGIN and END markers; do NOT include the markers themselves):

    BEGIN-CONTRIBUTING-GOPLS-SUBSECTION
    ## gopls Compatibility

    - `gopls` is installed at runtime by `internal/langregistry`; the project does not pin a gopls version in `go.sum` or `go.mod`.
    - The project tests against `gopls@latest`. Run `go install golang.org/x/tools/gopls@latest` after every Go upgrade.
    - gopls v0.17.1 had a Go 1.25 incompatibility on linux/amd64. The fix shipped upstream in gopls v0.21 and later — use `>=v0.21` as a floor, not a pin.
    END-CONTRIBUTING-GOPLS-SUBSECTION

    Mandatory mechanics (per CONTEXT D-01 and PATTERNS.md "Floor versions, not pins, in docs"):
    - Token requirements: contains `gopls` AND `v0.21`. The exact phrase `>=v0.21` MUST appear (RESEARCH Pitfall 4 — floor language, never an exact-version pin).
    - Mirror the `Adding Language Support` style: `## Heading` followed by short bullets, no preamble paragraph.
    - DO NOT add a `min_version` field, `LSEntry` change, or any code-side enforcement. Doc-only per D-01.
    - DO NOT pin a specific patch version like `v0.21.1` (Pitfall 4 — pinning ages immediately).
  </action>
  <verify>
    <automated>bash -c '! grep -q "Benchmark CI Gate" CONTRIBUTING.md && ! grep -q "bench\.yml" CONTRIBUTING.md && ! grep -q "capture-baseline\.yml" CONTRIBUTING.md && grep -q "gopls" CONTRIBUTING.md && grep -q ">=v0.21" CONTRIBUTING.md && grep -q "make bench" CONTRIBUTING.md && grep -q "make bench-baseline" CONTRIBUTING.md && grep -q "benchstat@latest" CONTRIBUTING.md && grep -q "gitignored" CONTRIBUTING.md && grep -iq "benchmarks run locally only" CONTRIBUTING.md && ! grep -q "tiered thresholds" CONTRIBUTING.md && ! grep -q "PR tier" CONTRIBUTING.md && ! grep -q "release tier" CONTRIBUTING.md'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'Benchmark CI Gate' CONTRIBUTING.md` (Section deleted)
    - `! grep -q 'bench\.yml' CONTRIBUTING.md` (No reference to deleted workflow)
    - `! grep -q 'capture-baseline\.yml' CONTRIBUTING.md` (No reference to deleted workflow)
    - `! grep -q 'tiered thresholds' CONTRIBUTING.md`, `! grep -q 'PR tier' CONTRIBUTING.md`, and `! grep -q 'release tier' CONTRIBUTING.md` (No leftover threshold language)
    - `grep -q 'gopls' CONTRIBUTING.md` (gopls subsection present)
    - `grep -q '>=v0.21' CONTRIBUTING.md` (exact floor wording present, NOT a pin)
    - `grep -q 'make bench' CONTRIBUTING.md` and `grep -q 'make bench-baseline' CONTRIBUTING.md` (new Make commands documented)
    - `grep -q 'benchstat@latest' CONTRIBUTING.md` (canonical install one-liner present)
    - `grep -q 'gitignored' CONTRIBUTING.md` (gitignore framing matches Plan 02)
    - `grep -iq 'benchmarks run locally only' CONTRIBUTING.md` (the policy statement is present)
    - `grep -q '## Adding Language Support' CONTRIBUTING.md` and `grep -q '## Adding a New MCP Tool' CONTRIBUTING.md` (sibling sections preserved)
  </acceptance_criteria>
  <done>CONTRIBUTING.md has the rewritten benchmarks section, the deleted CI gate section, and the new gopls floor-version subsection per D-01/D-05/D-06/D-07; sibling sections untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite USAGE.md gopls troubleshooting subsection (lines 511-529)</name>
  <files>USAGE.md</files>
  <read_first>
    - USAGE.md (lines 500-535 — confirm the gopls section starts at "### gopls version incompatibility with Go 1.25" line 511 and the surrounding sibling sections such as the jdtls bullets above and the rename_symbol Rust block below)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-PATTERNS.md section "USAGE.md — gopls troubleshooting rewrite (lines 511-529)" (verbatim before excerpt and rewrite skeleton)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-CONTEXT.md (decision D-01)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-RESEARCH.md "Code Examples — Suggested USAGE.md line 515 replacement skeleton"
  </read_first>
  <action>
    Replace the existing `### gopls version incompatibility with Go 1.25` subsection (currently 19 lines from `### gopls version` through the closing fence of step 3) in full. The new content must:
    1. Phrase the v0.17.1 / Go 1.25 issue as resolved upstream (NOT as an active bug).
    2. State `>=v0.21` (or `v0.21 and later`) as the floor — never a pin.
    3. NOT reference `capture-baseline.yml` (that workflow is deleted in Plan 01).
    4. NOT reference benchstat-comparison "false regression alerts" (that flow is gone).
    5. Preserve the existing structural triad — `### Heading`, `**Symptom:** ... **Cause:** ... **Fix:**` — to match the surrounding troubleshooting subsections (e.g. the jdtls section just above).

    Replace with exactly the following block (write between the BEGIN and END markers; do NOT include the markers themselves):

    BEGIN-USAGE-GOPLS
    ### gopls version compatibility

    **Symptom:** Build failures or unexpected behavior from the Go integration tests after upgrading Go.

    **Cause:** Older gopls releases (notably v0.17.1) predate Go 1.25 and exhibit incompatibilities on linux/amd64. The fix shipped upstream in gopls v0.21 and later.

    **Fix:** Update gopls after every Go upgrade:

    ```bash
    go install golang.org/x/tools/gopls@latest
    ```

    Serena does not pin a gopls version — `internal/langregistry` invokes whatever `gopls` is on `$PATH`. Use `>=v0.21` as a floor, not as a pin.
    END-USAGE-GOPLS

    Mandatory mechanics (per CONTEXT D-01, PATTERNS.md, and RESEARCH Pitfall 4):
    - Heading rename: `### gopls version incompatibility with Go 1.25` becomes `### gopls version compatibility` (drops the "incompatibility" framing now that it is resolved upstream).
    - Step 3 (the `capture-baseline.yml` re-baseline instruction) is DELETED, not replaced.
    - The Go 1.24 `testing.B.Loop` mention is also dropped — it belongs (and lives) in CONTRIBUTING.md "Running Benchmarks", not in USAGE.md troubleshooting. RESEARCH Test Map asserts `! grep -q 'capture-baseline' USAGE.md` post-edit.
    - Surrounding subsections (the jdtls block above, the `rename_symbol` Rust block below) MUST remain untouched. Do a `git diff USAGE.md` after the edit and confirm the diff is bounded to the gopls subsection.
  </action>
  <verify>
    <automated>bash -c '! grep -q "capture-baseline" USAGE.md && grep -q "gopls v0.21" USAGE.md && grep -q ">=v0.21" USAGE.md && grep -q "gopls version compatibility" USAGE.md && ! grep -q "gopls version incompatibility" USAGE.md && grep -q "rename_symbol" USAGE.md && grep -q "internal/langregistry" USAGE.md'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'capture-baseline' USAGE.md` (no reference to deleted workflow)
    - `grep -q 'gopls v0.21' USAGE.md` (floor version mentioned)
    - `grep -q '>=v0.21' USAGE.md` (exact floor wording present, NOT a pin)
    - `grep -q 'gopls version compatibility' USAGE.md` (new heading present)
    - `! grep -q 'gopls version incompatibility' USAGE.md` (old heading removed)
    - `grep -q 'rename_symbol' USAGE.md` (sibling Rust block preserved)
    - `grep -q 'internal/langregistry' USAGE.md` (PATH-installed framing present)
    - `git diff --stat USAGE.md` shows changes only in the gopls subsection (manual sanity check, not gating)
  </acceptance_criteria>
  <done>USAGE.md gopls troubleshooting section is rewritten as resolved-upstream with floor v0.21 wording per D-01; surrounding sections untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Surgically edit .planning/PROJECT.md line 139 — remove bench (D-10) and GrammarRegistry (D-11) sentences, preserve rust-analyzer and jdtls sentences verbatim</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 135-142 — confirm line 139 is the "Known tech debt" paragraph)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-PATTERNS.md section ".planning/PROJECT.md line 139 — surgical edit" (verbatim before/after content)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-CONTEXT.md (decisions D-10, D-11)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-RESEARCH.md "Common Pitfalls — Pitfall 5: Editing PROJECT.md line 139 incorrectly" (lists exact sentences to remove vs. preserve)
  </read_first>
  <action>
    Surgically edit `.planning/PROJECT.md` line 139 (the single dense `**Known tech debt:**` paragraph). The current verbatim line is:

    `**Known tech debt:** Benchmark baselines captured locally (darwin/arm64) instead of CI ubuntu-latest due to gopls v0.17.1 incompatibility with Go 1.25 on linux/amd64. 3 redundant GrammarRegistry instances (functionally correct). rust-analyzer v1.90 \`textDocument/rename\` returns "No references found at position" in temp workspaces despite hover/references/search working at the same position — upstream LS bug, all other Rust operations work (see USAGE.md Troubleshooting). jdtls cold-start indexing exceeds 2min in temp workspaces — Java integration tests gated behind \`-short=false\`.`

    Replace with EXACTLY this line (verbatim — preserves the rust-analyzer and jdtls sentences; removes both target sentences):

    `**Known tech debt:** rust-analyzer v1.90 \`textDocument/rename\` returns "No references found at position" in temp workspaces despite hover/references/search working at the same position — upstream LS bug, all other Rust operations work (see USAGE.md Troubleshooting). jdtls cold-start indexing exceeds 2min in temp workspaces — Java integration tests gated behind \`-short=false\`.`

    Mandatory mechanics (per CONTEXT D-10/D-11 and RESEARCH Pitfall 5):
    1. **REMOVE (D-10):** the sentence `Benchmark baselines captured locally (darwin/arm64) instead of CI ubuntu-latest due to gopls v0.17.1 incompatibility with Go 1.25 on linux/amd64.` — local-only IS the chosen design, not tech debt.
    2. **REMOVE (D-11):** the sentence `3 redundant GrammarRegistry instances (functionally correct).` — already resolved by Phase 49.
    3. **PRESERVE EXACTLY:** the rust-analyzer sentence (with its backticks, em dashes, parenthetical, and quoted error message).
    4. **PRESERVE EXACTLY:** the jdtls cold-start sentence (with its backticks and em dash).
    5. Use the Edit tool with sufficient surrounding context (a few words on each side of the changed region) to ensure the line replacement is unambiguous. Do NOT touch any other line in PROJECT.md.

    Verification per RESEARCH Test Map:
    - `! grep -q 'GrammarRegistry instances' .planning/PROJECT.md` (D-11 removal verified)
    - `! grep -q 'CI ubuntu-latest' .planning/PROJECT.md` (D-10 removal verified)
    - `grep -q 'rust-analyzer' .planning/PROJECT.md` (preserved)
    - `grep -q 'jdtls cold-start' .planning/PROJECT.md` (preserved)
    - `grep -q 'No references found at position' .planning/PROJECT.md` (rust-analyzer detail preserved)
    - `grep -q '\-short=false' .planning/PROJECT.md` (jdtls detail preserved)
  </action>
  <verify>
    <automated>bash -c '! grep -q "GrammarRegistry instances" .planning/PROJECT.md && ! grep -q "CI ubuntu-latest" .planning/PROJECT.md && ! grep -q "Benchmark baselines captured locally" .planning/PROJECT.md && grep -q "rust-analyzer" .planning/PROJECT.md && grep -q "jdtls cold-start" .planning/PROJECT.md && grep -q "No references found at position" .planning/PROJECT.md && grep -q "short=false" .planning/PROJECT.md'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'GrammarRegistry instances' .planning/PROJECT.md` (D-11 sentence removed)
    - `! grep -q 'CI ubuntu-latest' .planning/PROJECT.md` (D-10 sentence removed)
    - `! grep -q 'Benchmark baselines captured locally' .planning/PROJECT.md` (D-10 sentence head removed)
    - `grep -q 'rust-analyzer' .planning/PROJECT.md` (rust-analyzer sentence preserved)
    - `grep -q 'jdtls cold-start' .planning/PROJECT.md` (jdtls sentence preserved)
    - `grep -q 'No references found at position' .planning/PROJECT.md` (rust-analyzer error string preserved verbatim)
    - `grep -q 'short=false' .planning/PROJECT.md` (jdtls gating detail preserved)
    - `git diff --stat .planning/PROJECT.md` shows changes confined to one line region (sanity check)
  </acceptance_criteria>
  <done>PROJECT.md tech-debt line has the bench and GrammarRegistry sentences removed per D-10/D-11; rust-analyzer and jdtls sentences preserved verbatim per Pitfall 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → contributors reading docs | Doc-only edits; no executable change. The only contributor-facing risk is wording drift (e.g., suggesting an outdated gopls pin), addressed by the floor-not-pin rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-07 | Information Disclosure | Doc edits | accept | Three doc files; no secrets, no PII, no executable change. Public-repo content remains public. |
| T-50-08 | Tampering | Surgical PROJECT.md edit (Pitfall 5) | mitigate | Acceptance criteria explicitly assert that the rust-analyzer and jdtls sentences are preserved verbatim (positive grep) AND the two target sentences are removed (negative grep). Without these guards a too-aggressive edit could lose still-active tech-debt context, leading to a false sense of completeness. |
| T-50-09 | Repudiation | gopls version drift in CONTRIBUTING.md (Pitfall 4) | mitigate | The `>=v0.21` floor wording is required (not optional). Hard-pinning a patch like `v0.21.1` would age out and create silent doc rot — gated by the `grep -q '>=v0.21'` acceptance criterion in Tasks 1 and 2. |

**Net security impact:** No new attack surface. Two mitigated correctness threats (Pitfall 4, Pitfall 5) — both gated by explicit grep acceptance criteria. `block_on: high` — none reach high severity.
</threat_model>

<verification>
After all three tasks land:

```sh
# CONTRIBUTING.md gates
! grep -q 'Benchmark CI Gate' CONTRIBUTING.md
! grep -q 'bench\.yml' CONTRIBUTING.md
! grep -q 'capture-baseline\.yml' CONTRIBUTING.md
grep -q '>=v0.21' CONTRIBUTING.md
grep -q 'make bench' CONTRIBUTING.md
grep -q 'make bench-baseline' CONTRIBUTING.md
grep -q 'benchstat@latest' CONTRIBUTING.md

# USAGE.md gates
! grep -q 'capture-baseline' USAGE.md
grep -q '>=v0.21' USAGE.md
grep -q 'gopls version compatibility' USAGE.md
! grep -q 'gopls version incompatibility' USAGE.md

# PROJECT.md gates
! grep -q 'GrammarRegistry instances' .planning/PROJECT.md
! grep -q 'CI ubuntu-latest' .planning/PROJECT.md
grep -q 'rust-analyzer' .planning/PROJECT.md
grep -q 'jdtls cold-start' .planning/PROJECT.md

# Build still green (sanity)
go vet ./...
```
</verification>

<success_criteria>
- CONTRIBUTING.md `Running Benchmarks` documents the local-only flow with `make bench` / `make bench-baseline` / inline `benchstat`
- CONTRIBUTING.md `Benchmark CI Gate` section is fully removed
- CONTRIBUTING.md gopls compatibility subsection exists with `>=v0.21` floor wording
- USAGE.md gopls subsection is the resolved-upstream rewrite with no `capture-baseline` mention
- PROJECT.md tech-debt line preserves rust-analyzer + jdtls sentences and removes the bench + GrammarRegistry sentences
- All grep acceptance criteria pass
- `go vet ./...` is green
</success_criteria>

<output>
After completion, create `.planning/phases/50-toolchain-go1.25-bench-local/50-03-SUMMARY.md` recording:
- The three doc files modified
- Confirmation that all positive and negative greps from the verification block pass
- The exact final shape of PROJECT.md line 139 (paste the surviving sentence pair)
</output>
