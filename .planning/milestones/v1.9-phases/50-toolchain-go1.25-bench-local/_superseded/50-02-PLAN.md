---
phase: 50
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - .github/workflows/bench.yml
  - CONTRIBUTING.md
  - .planning/PROJECT.md
autonomous: false
requirements:
  - TOOL-01
  - TOOL-02
tags:
  - ci
  - benchmark
  - documentation
  - gopls

must_haves:
  truths:
    - "bench.yml references test/bench/baselines/v1.9-github-hosted.txt in BOTH the benchstat summary step AND the benchgate --baseline flag (no v1.1 references remain in active code paths)"
    - "CONTRIBUTING.md contains a ### Gopls Pin subsection nested under ## Benchmark CI Gate, documenting the v0.21.1 pin, why pinned, where the pin lives (both workflow files), and the bump policy (Pitfall 1 + Pitfall 4 cited)"
    - "PROJECT.md Known tech debt paragraph no longer mentions the gopls v0.17.1 / darwin baselines tech-debt sentence"
    - "A verification PR is opened that bundles the v1.9 baseline (from Plan 01) + bench.yml path swap + CONTRIBUTING.md edit + PROJECT.md edit; the PR's own go-test.yml (SC #1) and bench.yml (SC #2) runs are the success-criteria evidence (D-10)"
  artifacts:
    - path: ".github/workflows/bench.yml"
      provides: "PR bench gate compares against v1.9 ubuntu-latest baseline"
      contains: "v1.9-github-hosted.txt"
    - path: "CONTRIBUTING.md"
      provides: "### Gopls Pin subsection documenting strategy (D-05, D-06, D-14)"
      contains: "### Gopls Pin"
    - path: ".planning/PROJECT.md"
      provides: "Known tech debt paragraph with gopls/baselines sentence removed (D-13)"
      contains: "Known tech debt"
  key_links:
    - from: ".github/workflows/bench.yml (Print benchstat summary step)"
      to: "test/bench/baselines/v1.9-github-hosted.txt"
      via: "benchstat -alpha 0.05 baseline new"
      pattern: "test/bench/baselines/v1\\.9-github-hosted\\.txt"
    - from: ".github/workflows/bench.yml (Run benchgate step)"
      to: "test/bench/baselines/v1.9-github-hosted.txt"
      via: "go run ./test/bench/cmd/benchgate --baseline ... --new ..."
      pattern: "--baseline test/bench/baselines/v1\\.9-github-hosted\\.txt"
    - from: "CONTRIBUTING.md ## Benchmark CI Gate"
      to: "### Gopls Pin subsection"
      via: "nested heading directly after the existing bullet list (line 184), before ## Legacy Python (line 186)"
      pattern: "^### Gopls Pin"
---

<objective>
Wire the v1.9 ubuntu-latest baseline into the PR bench gate, document the gopls
pin strategy in CONTRIBUTING.md, remove the now-stale tech-debt sentence from
PROJECT.md, and open the verification PR whose own CI run satisfies SC #1 and
SC #2 (D-10).

Purpose: Closes TOOL-01 (gopls strategy documented in CONTRIBUTING.md per D-05/D-14;
PR's go-test.yml on Go 1.25/ubuntu-latest is the build/vet/test green evidence)
and TOOL-02 (bench.yml swapped to v1.9 baseline per D-02; PR's bench.yml run on
ubuntu-latest is the gate-enforcement evidence; PROJECT.md tech-debt sentence
removed per D-13).

Output: 3 file edits on the working branch + 1 verification PR open against `main`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-PATTERNS.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-01-SUMMARY.md
@.github/workflows/bench.yml
@.github/workflows/capture-baseline.yml
@CONTRIBUTING.md
@test/bench/baselines/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap bench.yml from v1.1-github-hosted.txt to v1.9-github-hosted.txt (D-02)</name>
  <read_first>
    - .github/workflows/bench.yml (current state — verified line numbers: header comment line 5 mentions "v1.1 baseline"; line 80 benchstat references `test/bench/baselines/v1.1-github-hosted.txt`; line 89 benchgate `--baseline test/bench/baselines/v1.1-github-hosted.txt`)
    - test/bench/baselines/v1.9-github-hosted.txt (must already exist from Plan 01; this task's verify confirms parity)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-PATTERNS.md §"`.github/workflows/bench.yml`" (the exact pattern)
  </read_first>
  <files>.github/workflows/bench.yml</files>
  <action>
    Apply EXACTLY 3 textual replacements in `.github/workflows/bench.yml`:

    1. **Line 5 (header comment):** replace `the committed v1.1 baseline.` with `the committed v1.9 baseline.` Preserve everything else on that line including the trailing reference to `test/bench/baselines/README.md`.

    2. **Line 80 (Print benchstat summary step):** replace `test/bench/baselines/v1.1-github-hosted.txt` with `test/bench/baselines/v1.9-github-hosted.txt`. The line is the first positional argument to `benchstat -alpha 0.05 \`. Preserve the leading whitespace (12 spaces) and the trailing ` \` continuation backslash.

    3. **Line 89 (Run benchgate step):** replace `test/bench/baselines/v1.1-github-hosted.txt` with `test/bench/baselines/v1.9-github-hosted.txt`. The line is the value of the `--baseline` flag. Preserve the leading whitespace, the `--baseline ` flag prefix, and the trailing ` \` continuation backslash.

    DO NOT touch:
    - The `GOPLS_VERSION: v0.21.1` env entry on line 41 (unchanged — already correct).
    - The `GOMAXPROCS: "4"` entry (Pitfall 5 mitigation — unchanged).
    - The `go test ... -short -bench=. -benchmem -count=10 -run=^$ ./test/bench/...` invocation (Pitfall 1 parity — unchanged).
    - Any threshold flags on the benchgate command (D-07: keep 15%/25%, do not touch).
    - The `actions/setup-go@v5` `go-version: '1.25.x'` block.
    - Any other workflow file (D-12: out of scope — only bench.yml in this task).

    Use `gofmt`-equivalent care: byte-exact replacements, no reflow, no whitespace cleanup.
  </action>
  <verify>
    <automated>[ "$(grep -c 'v1.9-github-hosted.txt' .github/workflows/bench.yml)" = "2" ] &amp;&amp; [ "$(grep -c 'v1.1-github-hosted.txt' .github/workflows/bench.yml)" = "0" ] &amp;&amp; grep -q 'GOPLS_VERSION: v0.21.1' .github/workflows/bench.yml &amp;&amp; grep -q 'GOMAXPROCS: "4"' .github/workflows/bench.yml &amp;&amp; grep -q "go-version: '1.25.x'" .github/workflows/bench.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'v1.9-github-hosted.txt' .github/workflows/bench.yml` returns exactly `2` (one in benchstat step, one in benchgate step).
    - `grep -c 'v1.1-github-hosted.txt' .github/workflows/bench.yml` returns exactly `0` (no stale references — header comment also updated).
    - `grep -q 'the committed v1.9 baseline' .github/workflows/bench.yml` succeeds (header comment updated).
    - `grep -q 'GOPLS_VERSION: v0.21.1' .github/workflows/bench.yml` succeeds (untouched).
    - `grep -q 'GOMAXPROCS: "4"' .github/workflows/bench.yml` succeeds (untouched).
    - `grep -q "go test -short -bench=\\. -benchmem -count=10 -run=\\^\\\$ \\./test/bench/" .github/workflows/bench.yml` succeeds (Pitfall 1 parity — bench command untouched).
    - `git diff .github/workflows/bench.yml | grep -c '^[-+]'` returns exactly `6` lines (3 minus, 3 plus — proves no incidental edits).
  </acceptance_criteria>
  <done>
    bench.yml references the v1.9 baseline in both code paths and the header comment; no v1.1 references remain; all parity-critical knobs (GOPLS_VERSION, GOMAXPROCS, bench flags, Go version, thresholds) are byte-identical to pre-edit state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ### Gopls Pin subsection to CONTRIBUTING.md (D-05, D-06, D-14)</name>
  <read_first>
    - CONTRIBUTING.md (current state — verified lines: line 178 = `## Benchmark CI Gate`; line 186 = `## Legacy Python`; the body of `## Benchmark CI Gate` ends at line 184)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-PATTERNS.md §"`CONTRIBUTING.md` (docs)" — exact subsection skeleton and style invariants
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md §"Code Examples" → CONTRIBUTING.md gopls subsection skeleton
    - test/bench/baselines/README.md (referenced by the bump policy text)
  </read_first>
  <files>CONTRIBUTING.md</files>
  <action>
    Insert the following `### Gopls Pin` subsection into `CONTRIBUTING.md` AFTER line 184 (end of the existing `## Benchmark CI Gate` bullet list) and BEFORE line 186 (`## Legacy Python`). The exact insertion point is the blank line currently at line 185.

    Insert this content verbatim (including the leading blank line that separates it from the bullet list above and the trailing blank line that separates it from `## Legacy Python` below):

    ```markdown

    ### Gopls Pin

    The benchmark CI gate pins `gopls` to an explicit version because:

    1. **Compatibility:** `v0.17.1` is incompatible with Go 1.25 on `linux/amd64`. The current pin `v0.21.1` resolves this.
    2. **Bench stability:** Cold-start and warm-reuse metrics (`BenchmarkLSPIndex_Cold`, `BenchmarkLSPIndex_Warm`) are sensitive to gopls indexing changes (Pitfall 4).

    **Where the pin is set (two locations — must stay in sync, Pitfall 1):**
    - `.github/workflows/bench.yml` — `GOPLS_VERSION` env
    - `.github/workflows/capture-baseline.yml` — `GOPLS_VERSION` env

    **Bump policy:** Any change to `GOPLS_VERSION` requires a re-baseline PR per the refresh policy in `test/bench/baselines/README.md`. Do not bump in a PR that does not also re-capture the baseline — the PR will fail bench gate against the old baseline.
    ```

    Style invariants (from PATTERNS.md): backticks around all file paths and env-var names, numbered lists for ordered rationale, bullet lists for unordered enumerations, no emoji, no horizontal rules. Heading is `### Gopls Pin` (third-level, nested under the second-level `## Benchmark CI Gate`).

    Per D-06: the subsection MUST explicitly state the two GOPLS_VERSION values must stay in sync AND reference Pitfall 1 (the bullet list under "Where the pin is set" satisfies both).

    Per D-14: this writeup IS the documented "upgrade" decision (chosen over patch / replacement). Do not add a separate "alternatives considered" section.

    DO NOT:
    - Touch `## Benchmark CI Gate` or its existing bullet list above (lines 178-184 are immutable in this edit).
    - Touch `## Legacy Python` or anything below it.
    - Promote the new subsection to a top-level `##` heading (D-05 + RESEARCH.md Open Question #2: nest under `## Benchmark CI Gate` because the rationale is bench-stability-driven).
    - Add a horizontal rule (`---`) before or after the new subsection.
  </action>
  <verify>
    <automated>grep -q '^### Gopls Pin$' CONTRIBUTING.md &amp;&amp; grep -q 'GOPLS_VERSION' CONTRIBUTING.md &amp;&amp; grep -q 'v0.21.1' CONTRIBUTING.md &amp;&amp; grep -q 'must stay in sync' CONTRIBUTING.md &amp;&amp; grep -q 'Pitfall 1' CONTRIBUTING.md &amp;&amp; grep -q 'Pitfall 4' CONTRIBUTING.md &amp;&amp; grep -q 'test/bench/baselines/README.md' CONTRIBUTING.md &amp;&amp; awk '/^### Gopls Pin$/{found=NR} /^## Legacy Python$/{if(found &amp;&amp; NR&gt;found){print "ok"; exit}}' CONTRIBUTING.md | grep -q ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^### Gopls Pin$' CONTRIBUTING.md` shows exactly one match, with line number > 184 and < line of `## Legacy Python`.
    - `grep -c 'GOPLS_VERSION' CONTRIBUTING.md` returns >= 2 (env-var name appears in both bullet lines under "Where the pin is set").
    - `grep -q 'v0.21.1' CONTRIBUTING.md` succeeds (current pin documented).
    - `grep -q 'v0.17.1' CONTRIBUTING.md` succeeds (broken version called out for context).
    - `grep -q 'must stay in sync' CONTRIBUTING.md` succeeds (D-06 sync requirement).
    - `grep -q 'Pitfall 1' CONTRIBUTING.md` AND `grep -q 'Pitfall 4' CONTRIBUTING.md` both succeed (D-05 / D-06 cited pitfalls).
    - `grep -q 'test/bench/baselines/README.md' CONTRIBUTING.md` succeeds (bump policy reference per D-05).
    - `grep -q 'BenchmarkLSPIndex_Cold' CONTRIBUTING.md` AND `grep -q 'BenchmarkLSPIndex_Warm' CONTRIBUTING.md` both succeed (concrete affected benchmarks named).
    - `grep -c '^## Benchmark CI Gate$' CONTRIBUTING.md` returns `1` (existing section heading untouched).
    - `grep -c '^## Legacy Python$' CONTRIBUTING.md` returns `1` (next section heading untouched).
  </acceptance_criteria>
  <done>
    CONTRIBUTING.md has a new `### Gopls Pin` subsection nested under `## Benchmark CI Gate`, satisfying D-05 (current pin + why + where + bump policy), D-06 (explicit sync requirement, Pitfall 1 reference, no centralization), and D-14 (this is the documented upgrade strategy).
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete benchmarks tech-debt sentence from .planning/PROJECT.md (D-13)</name>
  <read_first>
    - .planning/PROJECT.md line 139 (the `**Known tech debt:**` paragraph — verified to begin with the sentence "Benchmark baselines captured locally (darwin/arm64) instead of CI ubuntu-latest due to gopls v0.17.1 incompatibility with Go 1.25 on linux/amd64.")
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-PATTERNS.md §"`.planning/PROJECT.md` (docs)" — target shape after edit
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    On line 139 of `.planning/PROJECT.md`, delete the leading sentence from the `**Known tech debt:**` paragraph. The sentence to delete is:

    ```
    Benchmark baselines captured locally (darwin/arm64) instead of CI ubuntu-latest due to gopls v0.17.1 incompatibility with Go 1.25 on linux/amd64.
    ```

    (Including the trailing space before the next sentence "3 redundant GrammarRegistry instances...".)

    Keep:
    - The bold lead-in `**Known tech debt:**` at the start of the paragraph.
    - The remaining three sentences in the same paragraph (3 redundant GrammarRegistry instances; rust-analyzer v1.90 textDocument/rename; jdtls cold-start indexing exceeds 2min).

    Target paragraph shape after edit (matches PATTERNS.md exactly):

    ```
    **Known tech debt:** 3 redundant GrammarRegistry instances (functionally correct). rust-analyzer v1.90 `textDocument/rename` returns "No references found at position" in temp workspaces despite hover/references/search working at the same position — upstream LS bug, all other Rust operations work (see USAGE.md Troubleshooting). jdtls cold-start indexing exceeds 2min in temp workspaces — Java integration tests gated behind `-short=false`.
    ```

    Per Claude's Discretion (CONTEXT.md): light flow cleanup of the joining is acceptable but not required. The minimal-change form (just delete the sentence + the space) is preferred.

    DO NOT:
    - Touch any other paragraph in PROJECT.md (D-13 is sentence-scoped).
    - Update tech-debt items owned by other phases (Phase 47 / 48 / 49 / 56 update their own items independently per CONTEXT.md D-13).
    - Remove the bold `**Known tech debt:**` label.
  </action>
  <verify>
    <automated>! grep -q 'Benchmark baselines captured locally' .planning/PROJECT.md &amp;&amp; ! grep -q 'gopls v0.17.1 incompatibility with Go 1.25' .planning/PROJECT.md &amp;&amp; grep -q '\*\*Known tech debt:\*\*' .planning/PROJECT.md &amp;&amp; grep -q 'GrammarRegistry instances' .planning/PROJECT.md &amp;&amp; grep -q 'rust-analyzer v1.90' .planning/PROJECT.md &amp;&amp; grep -q 'jdtls cold-start indexing exceeds 2min' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Benchmark baselines captured locally' .planning/PROJECT.md` FAILS (sentence removed).
    - `grep -q 'gopls v0.17.1' .planning/PROJECT.md` FAILS (incompatibility note removed).
    - `grep -q 'darwin/arm64' .planning/PROJECT.md` FAILS or matches only outside the Known tech debt paragraph (the only mention was in the deleted sentence).
    - `grep -q '\*\*Known tech debt:\*\*' .planning/PROJECT.md` succeeds (bold lead-in preserved).
    - `grep -q '3 redundant GrammarRegistry instances' .planning/PROJECT.md` succeeds (sentence kept — Phase 49's item).
    - `grep -q 'rust-analyzer v1.90' .planning/PROJECT.md` succeeds (sentence kept — Phase 47's item).
    - `grep -q 'jdtls cold-start indexing exceeds 2min' .planning/PROJECT.md` succeeds (sentence kept — Phase 48/56's item).
    - `git diff .planning/PROJECT.md | grep -c '^-'` shows the deleted sentence on a single `-` line (indicating a clean, minimal edit; light cleanup is OK if user discretion exercised).
  </acceptance_criteria>
  <done>
    The Known tech debt paragraph in PROJECT.md no longer contains the gopls/baselines sentence; the three remaining tech-debt items and the bold lead-in are intact (D-13 satisfied).
  </done>
</task>

<task type="auto">
  <name>Task 4: Local pre-flight — confirm parity, run go vet/build/test, then push</name>
  <read_first>
    - .github/workflows/bench.yml (post-edit state)
    - .github/workflows/capture-baseline.yml (env block at lines 29-33 for parity diff)
    - test/bench/baselines/v1.9-github-hosted.txt (must exist from Plan 01)
    - CLAUDE.md §"Go Development Commands" — `go vet ./...` and `go test ./...` MUST run before completing Go tasks (no Go code changed here, but the workspace must remain green so SC #1 evidence is real on the PR)
  </read_first>
  <files>.github/workflows/bench.yml, .github/workflows/capture-baseline.yml, test/bench/baselines/v1.9-github-hosted.txt</files>
  <action>
    Pre-flight checks before opening the verification PR (Phase-50-specific gotcha in RESEARCH.md: avoid self-bootstrap PR failures):

    1. **Confirm baseline parity** between `bench.yml` and `capture-baseline.yml` (Pitfall 1):
       ```sh
       diff <(grep -E 'GOPLS_VERSION|GOMAXPROCS' .github/workflows/bench.yml) \
            <(grep -E 'GOPLS_VERSION|GOMAXPROCS' .github/workflows/capture-baseline.yml)
       ```
       Expect the env values to match (lines may differ in surrounding comments — only the `KEY: value` pair must agree).

       ```sh
       diff <(grep -E 'go test -short -bench' .github/workflows/bench.yml) \
            <(grep -E 'go test -short -bench' .github/workflows/capture-baseline.yml)
       ```
       Expect identical bench command lines.

    2. **Confirm the v1.9 baseline file is referenced correctly** in bench.yml AND exists on disk:
       ```sh
       test -f test/bench/baselines/v1.9-github-hosted.txt
       grep -c 'test/bench/baselines/v1.9-github-hosted.txt' .github/workflows/bench.yml  # expect 2
       grep -c 'test/bench/baselines/v1.1-github-hosted.txt' .github/workflows/bench.yml  # expect 0
       ```

    3. **Run local Go validation** per CLAUDE.md (no Go code changed, but the workspace must be green):
       ```sh
       go vet ./...
       go build ./...
       go test ./... -short -count=1
       ```
       All three must exit 0. If any fails, the failure is unrelated to this phase (Phase 50 touches no Go source) — surface it as a blocker for human triage; do not proceed to PR.

    4. **Push the working branch** to origin:
       ```sh
       git push -u origin "$(git branch --show-current)"
       ```
  </action>
  <verify>
    <automated>test -f test/bench/baselines/v1.9-github-hosted.txt &amp;&amp; [ "$(grep -c 'test/bench/baselines/v1.9-github-hosted.txt' .github/workflows/bench.yml)" = "2" ] &amp;&amp; [ "$(grep -c 'test/bench/baselines/v1.1-github-hosted.txt' .github/workflows/bench.yml)" = "0" ] &amp;&amp; go vet ./... &amp;&amp; go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/bench/baselines/v1.9-github-hosted.txt` exits 0.
    - `grep -c 'test/bench/baselines/v1.9-github-hosted.txt' .github/workflows/bench.yml` returns `2`.
    - `grep -c 'test/bench/baselines/v1.1-github-hosted.txt' .github/workflows/bench.yml` returns `0`.
    - `diff <(grep -E 'GOPLS_VERSION|GOMAXPROCS' .github/workflows/bench.yml) <(grep -E 'GOPLS_VERSION|GOMAXPROCS' .github/workflows/capture-baseline.yml)` shows matching `GOPLS_VERSION: v0.21.1` and `GOMAXPROCS: "4"` on both sides (Pitfall 1 parity).
    - `go vet ./...` exits 0.
    - `go build ./...` exits 0.
    - `go test ./... -short -count=1` exits 0.
    - `git rev-parse --abbrev-ref --symbolic-full-name @{u}` succeeds (working branch tracks an upstream after push).
  </acceptance_criteria>
  <done>
    All parity invariants confirmed locally; full Go suite is green; the working branch is pushed to origin and ready for PR creation in Task 5.
  </done>
</task>

<task type="auto">
  <name>Task 5: Open verification PR against main with SC #1 / SC #2 evidence claim (D-10)</name>
  <read_first>
    - .planning/ROADMAP.md §"Phase 50: toolchain-go1.25-gopls-ci" (Success Criteria 1-4)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md §"Specifics" (PR title/body must make the SC #1/SC #2 evidence claim explicit)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md §"Verification PR Flow (D-10)"
  </read_first>
  <files>.github/workflows/bench.yml, CONTRIBUTING.md, .planning/PROJECT.md, test/bench/baselines/v1.9-github-hosted.txt</files>
  <action>
    Open a verification PR from the working branch to `main` using `gh pr create`. The PR bundles all Plan 01 + Plan 02 changes:
    - `test/bench/baselines/v1.9-github-hosted.txt` (Plan 01 capture)
    - `.github/workflows/bench.yml` (Task 1 path swap)
    - `CONTRIBUTING.md` (Task 2 Gopls Pin subsection)
    - `.planning/PROJECT.md` (Task 3 tech-debt sentence delete)

    PR title (under 70 characters):

    ```
    ci(50): swap bench gate to v1.9 ubuntu-latest baseline + doc gopls pin
    ```

    Concrete command (use a heredoc to preserve PR body formatting):

    ```sh
    gh pr create --base main --title "ci(50): swap bench gate to v1.9 ubuntu-latest baseline + doc gopls pin" --body "$(cat <<'EOF'
    ## Summary

    Phase 50 verification PR — closes TOOL-01 and TOOL-02.

    - Adds `test/bench/baselines/v1.9-github-hosted.txt` (captured from `ubuntu-latest` via `capture-baseline.yml`, Pitfall 1 flag parity verified)
    - Swaps `.github/workflows/bench.yml` to compare PRs against the v1.9 ubuntu-latest baseline (D-02) — both `benchstat` summary and `benchgate --baseline` paths
    - Adds `### Gopls Pin` subsection to `CONTRIBUTING.md` documenting v0.21.1 strategy, two-location sync requirement, and re-baseline bump policy (D-05, D-06, D-14)
    - Removes the now-stale gopls/baseline sentence from PROJECT.md "Known tech debt" paragraph (D-13)

    ## Success-criteria evidence (per D-10)

    - **SC #1** (build/vet/test green on Go 1.25 / ubuntu-latest): this PR's `go-test.yml` run.
    - **SC #2** (bench gate fires on PR-tier thresholds): this PR's `bench.yml` run, comparing PR HEAD against the new v1.9 baseline.
    - **SC #3** (gopls strategy documented): the new `### Gopls Pin` subsection in `CONTRIBUTING.md`.
    - **SC #4** (tech-debt note removed): the diff against `.planning/PROJECT.md`.

    ## Test plan

    - [ ] go-test.yml green on this PR
    - [ ] bench.yml green on this PR (reads v1.9 baseline successfully)
    - [ ] CONTRIBUTING.md ### Gopls Pin subsection rendered under ## Benchmark CI Gate
    - [ ] PROJECT.md Known tech debt paragraph no longer mentions gopls v0.17.1 or darwin baselines
    EOF
    )"
    ```

    Capture the PR URL from `gh pr create` output for the next checkpoint.

    Per D-11: NO `workflow_dispatch` dry-run before PR. Open the PR and let the PR's own CI be the test.
  </action>
  <verify>
    <automated>gh pr view --json number,title,url -q '.url' | grep -q '^https://github.com/'</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view --json state -q '.state'` returns `OPEN`.
    - `gh pr view --json title -q '.title'` matches `ci(50): swap bench gate to v1.9 ubuntu-latest baseline + doc gopls pin`.
    - `gh pr view --json baseRefName -q '.baseRefName'` returns `main`.
    - `gh pr view --json files -q '.files[].path' | sort` includes all four expected files: `.github/workflows/bench.yml`, `.planning/PROJECT.md`, `CONTRIBUTING.md`, `test/bench/baselines/v1.9-github-hosted.txt`.
    - PR body contains the literal strings `SC #1`, `SC #2`, `SC #3`, `SC #4` (evidence claim is explicit per CONTEXT.md Specifics).
  </acceptance_criteria>
  <done>
    Verification PR is open against `main`, contains all four file changes, and explicitly claims the SC #1/SC #2 evidence in the body. CI runs (`go-test.yml`, `bench.yml`) are now triggering on the PR.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human verifies PR-CI green on go-test.yml AND bench.yml (SC #1 + SC #2)</name>
  <read_first>
    - .planning/ROADMAP.md §"Phase 50" Success Criteria 1-4
    - The PR URL produced by Task 5 (open via `gh pr view --web`)
    - The PR's Checks tab — `go-test.yml` and `bench.yml` workflow runs
  </read_first>
  <files>.github/workflows/bench.yml, .github/workflows/go-test.yml, CONTRIBUTING.md, .planning/PROJECT.md, test/bench/baselines/v1.9-github-hosted.txt</files>
  <action>
    PAUSE for human verification. Plans 01 + 02 produced: a fresh ubuntu-latest
    v1.9 baseline file, a bench.yml swap to read it, a CONTRIBUTING.md
    `### Gopls Pin` subsection, a PROJECT.md tech-debt sentence deletion, and a
    verification PR against `main`. Per D-10, the PR's own go-test.yml and
    bench.yml runs are the success-criteria evidence.

    Verification steps for the human:

    1. Open the PR URL produced by Task 5 (e.g. `gh pr view --web`).
    2. Confirm the PR shows the four expected file changes in the diff.
    3. Wait for CI to complete. Run `gh pr checks` or watch the Checks tab.
    4. Confirm the `go-test.yml` workflow run is green on `ubuntu-latest` with Go 1.25.x — this is **SC #1 evidence** (build/vet/test green).
    5. Confirm the `bench.yml` workflow run is green on `ubuntu-latest` — this is **SC #2 evidence** (bench gate enforces 15%/25% thresholds against the v1.9 baseline).
       - Inspect the `Print benchstat summary` step output to confirm it printed a benchstat table comparing against `test/bench/baselines/v1.9-github-hosted.txt` (no "no such file or directory" error — Phase-50-specific gotcha avoided).
       - Inspect the `Run benchgate` step to confirm it exited 0 (no threshold breach on the no-op PR).
    6. Confirm the rendered Markdown of `CONTRIBUTING.md` on the PR's "Files changed" tab shows `### Gopls Pin` correctly nested under `## Benchmark CI Gate`.
    7. Confirm the PROJECT.md diff shows ONLY the gopls/baselines sentence removed (no incidental edits to other tech-debt items).

    If `bench.yml` is red because of a threshold breach (not a path error), this is unexpected for a doc-only PR — re-run the failed job once via the GitHub Actions "Re-run failed jobs" button (D-09 escape hatch). If it fails twice, treat as a real signal and triage (likely env drift, not a Phase 50 issue).

    If `bench.yml` fails with "no such file" pointing at `v1.9-github-hosted.txt`, the baseline was not committed correctly — return to Plan 01 Task 1 and re-trigger `capture-baseline.yml`.

    Resume signal: Type "approved" if both go-test.yml AND bench.yml are green on
    the PR; SC #1, #2, #3, #4 are all satisfied. Or describe what failed and
    which check (job name, step name, error message) so the failure mode can be
    mapped back to a specific D-XX decision.
  </action>
  <verify>
    <manual>Human inspects the PR's GitHub Actions Checks tab to confirm `go-test.yml` and `bench.yml` are both green, then visually inspects the rendered diffs of CONTRIBUTING.md and PROJECT.md on the "Files changed" tab. No automated command — the validation IS the PR-CI run, which is external to this repo's local toolchain.</manual>
  </verify>
  <acceptance_criteria>
    - `gh pr checks` (or the PR's Checks tab) shows `go-test.yml` workflow run with status `completed` and conclusion `success` — SC #1 satisfied.
    - `gh pr checks` shows `bench.yml` workflow run with status `completed` and conclusion `success` — SC #2 satisfied.
    - The `bench.yml` "Print benchstat summary" step output references `test/bench/baselines/v1.9-github-hosted.txt` (no "no such file" error).
    - The `bench.yml` "Run benchgate" step exited 0.
    - On the PR's "Files changed" tab, CONTRIBUTING.md shows the new `### Gopls Pin` subsection rendered under `## Benchmark CI Gate` — SC #3 satisfied.
    - On the PR's "Files changed" tab, .planning/PROJECT.md shows ONLY the gopls/baselines sentence removed from the Known tech debt paragraph — SC #4 satisfied.
    - Human responds "approved" in chat.
  </acceptance_criteria>
  <done>
    Human responds "approved"; all four phase-level Success Criteria (SC #1 build/vet/test, SC #2 bench gate, SC #3 gopls doc, SC #4 tech-debt removed) are confirmed via the verification PR's CI run and visual diff review. TOOL-01 and TOOL-02 are closed; the PR is ready to merge.
  </done>
</task>

</tasks>

<verification>
- `grep -c 'v1.9-github-hosted.txt' .github/workflows/bench.yml` returns 2
- `grep -c 'v1.1-github-hosted.txt' .github/workflows/bench.yml` returns 0
- `grep -q '^### Gopls Pin$' CONTRIBUTING.md` succeeds
- `! grep -q 'Benchmark baselines captured locally' .planning/PROJECT.md` succeeds
- `gh pr view --json state -q '.state'` returns `OPEN`
- `gh pr checks` shows `go-test.yml` and `bench.yml` both green (after CI completes)
- All four success criteria from ROADMAP.md Phase 50 entry are mapped to specific evidence
</verification>

<success_criteria>
The Phase 50 verification PR is open with all four file changes, both `go-test.yml`
and `bench.yml` are green on `ubuntu-latest`, the gopls strategy is documented
in CONTRIBUTING.md, and the gopls/baselines tech-debt sentence is removed from
PROJECT.md. TOOL-01 and TOOL-02 are closed; the bench gate now enforces PR-tier
thresholds against a real ubuntu-latest baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/50-toolchain-go1.25-gopls-ci/50-02-SUMMARY.md`
documenting: PR URL, PR number, list of CI checks and their status, the diff line
counts per file, and which D-XX decisions each task implemented.
</output>
