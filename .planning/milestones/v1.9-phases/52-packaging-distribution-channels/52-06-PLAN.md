---
phase: 52-packaging-distribution-channels
plan: 06
type: execute
wave: 3
depends_on: [52-02, 52-03]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CHANGELOG.md
  - INSTALL.md
  - README.md
  - USAGE.md
  - CONTRIBUTING.md
  - CLAUDE.md
autonomous: true
requirements: [PKG-02, PKG-03, PKG-04]
tags: [packaging, docs, requirements-bookkeeping, changelog, breaking-changes]
must_haves:
  truths:
    - "REQUIREMENTS.md row 'Auto-update mechanism inside the binary | Out of scope' is removed; in-binary upgrade is now documented as in-scope"
    - "REQUIREMENTS.md PKG-02, PKG-03, PKG-04 entries are marked deferred from v1.9 with rationale citing the Phase 52 rescope"
    - "REQUIREMENTS.md Traceability table maps PKG-02/03/04 to 'Deferred' (not Phase 52)"
    - "ROADMAP.md Phase 52 entry's goal and success-criteria match the rescoped phase boundary (rename + self-upgrade + embed audit), not the original brew/scoop/native-Linux scope"
    - "CHANGELOG.md v1.9 entry includes a '### Breaking Changes' subsection enumerating: binary rename (serena → helix), env-var rename (SERENA_* → HELIX_*), config-dir rename (~/.serena/ → ~/.helix/), MCP registration name flip"
    - "CHANGELOG.md v1.9 documents the new helix update / helix upgrade subcommand pair with flag list"
    - "INSTALL.md documents the 'Upgrading' workflow with helix update + helix upgrade + --prerelease + --version + --dry-run + GITHUB_TOKEN rate-limit guidance"
    - "INSTALL.md verification recipe references helix_v archives (not serena_v) and the helix binary"
    - "Every 'serena' reference in README.md / USAGE.md / CONTRIBUTING.md / CLAUDE.md is flipped to 'helix' or rewritten in historical-attribution context"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability + inverted out-of-scope row"
    - path: ".planning/ROADMAP.md"
      provides: "Rewritten Phase 52 goal/success-criteria block"
    - path: "CHANGELOG.md"
      provides: "v1.9 entry with Breaking Changes subsection + self-upgrade subsection"
    - path: "INSTALL.md"
      provides: "Helix-named verification recipe + Upgrading section"
  key_links:
    - from: "CHANGELOG.md v1.9 Breaking Changes"
      to: "INSTALL.md Upgrading section"
      via: "explicit cross-reference (\"see INSTALL.md > Upgrading\")"
      pattern: "INSTALL.md"
    - from: "REQUIREMENTS.md Traceability"
      to: "REQUIREMENTS.md Future Requirements > PKG-DEFER section"
      via: "PKG-02/03/04 deferred-from-v1.9 entries"
      pattern: "PKG-02"
    - from: "ROADMAP.md Phase 52 entry"
      to: "REQUIREMENTS.md PKG-02..04 deferral"
      via: "consistency between rescoped goal and deferred requirements"
      pattern: "Phase 52"
---

<objective>
Land the documentation surface for Phase 52: invert the "Auto-update mechanism inside the binary | Out of scope" row in REQUIREMENTS.md, mark PKG-02/03/04 as deferred from v1.9 with rationale (this plan claims those three REQ-IDs as deferral-bookkeeping per the planner instructions), rewrite ROADMAP.md's Phase 52 goal/success-criteria to match the rescoped phase boundary, add a CHANGELOG.md v1.9 entry with a `### Breaking Changes` subsection enumerating every user-visible break, add an "Upgrading" section to INSTALL.md covering `helix update`/`helix upgrade`/flags/GITHUB_TOKEN, and flip every `serena` reference in the user-facing docs (README, USAGE, CONTRIBUTING, CLAUDE) to `helix`.

Purpose: Plans 02-04 produce a build-green tree where the runtime is `helix` everywhere, but every doc surface still says `serena`. Without this plan, users following INSTALL.md try to fetch `serena_v*` archives that no longer exist, CHANGELOG silently breaks the rename without warning, and PKG-02/03/04 sit in REQUIREMENTS.md mapped to Phase 52 even though Phase 52 is no longer fulfilling them.
Output: REQUIREMENTS.md with PKG-02/03/04 deferred + Out-of-Scope row inverted; ROADMAP.md Phase 52 entry rewritten; CHANGELOG.md v1.9 entry shipped; INSTALL.md "Upgrading" section added; README/USAGE/CONTRIBUTING/CLAUDE.md flipped to helix.

Wave 3 sibling of Plan 04 and Plan 05 with no `files_modified` overlap (those plans touch `internal/` Go code and the phase-local audit; this plan touches doc + planning artifacts).

This plan claims PKG-02/03/04 as the deferral-bookkeeping requirements per the orchestrator instructions: the original phase scope (Homebrew tap, Scoop bucket, native Linux package) was abandoned in favor of self-contained binary + self-upgrade per CONTEXT.md. The bookkeeping is the act of marking those REQ-IDs deferred in REQUIREMENTS.md and updating the Traceability table to reflect that they no longer map to Phase 52.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-packaging-distribution-channels/52-CONTEXT.md
@.planning/phases/52-packaging-distribution-channels/52-RESEARCH.md
@.planning/phases/52-packaging-distribution-channels/52-PATTERNS.md
@.planning/phases/52-packaging-distribution-channels/52-02-SUMMARY.md
@.planning/phases/52-packaging-distribution-channels/52-03-SUMMARY.md
@.planning/phases/51-packaging-goreleaser/51-CONTEXT.md
@INSTALL.md
@README.md
@USAGE.md
@CONTRIBUTING.md
@CHANGELOG.md
@CLAUDE.md

<interfaces>
<!-- REQUIREMENTS.md "Out of Scope" row to invert (REQUIREMENTS.md line 64): -->
| Auto-update mechanism inside the binary | Package managers (brew/scoop/apt) own updates; in-binary updater adds security surface |
<!-- After invert: row removed entirely; replaced by an in-scope acknowledgement in Phase 52's section. -->

<!-- REQUIREMENTS.md Traceability rows for PKG-02/03/04 (currently mapped to Phase 52, "Pending"): -->
| PKG-02 | Phase 52 | Pending |
| PKG-03 | Phase 52 | Pending |
| PKG-04 | Phase 52 | Pending |
<!-- After this plan: Phase = "Deferred" with note pointing to PKG-DEFER-* section. -->

<!-- ROADMAP.md Phase 52 entry currently lines 158-169 with original goal: -->
"Goal: Users can install Serena via Homebrew (`brew install <tap>/serena`), Scoop (`scoop install serena`), and a native Linux package manager..."
<!-- After this plan: Goal rewritten to match CONTEXT.md domain block. -->

<!-- INSTALL.md verification recipe (Phase 51 D-04) — references serena_v archives; flips to helix_v -->
<!-- INSTALL.md "Upgrading" section is NEW per CONTEXT.md domain: -->

<!-- CHANGELOG.md v1.9 entry (NEW) — convention per RESEARCH.md Open Question 4: -->
- Use `## v1.9 — Polish & Infra (<date>)` header
- First subsection MUST be `### Breaking Changes (v1.8 → v1.9)`
- Then narrative subsections covering self-upgrade and embed audit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md (invert out-of-scope row, mark PKG-02/03/04 deferred) and rewrite ROADMAP.md Phase 52 entry</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md (entire file — small enough; line 64 is the row to invert; the Traceability table at line 73 maps PKG-02/03/04; the Future Requirements section at line 36 contains the PKG-DEFER section to extend),
    .planning/ROADMAP.md (Phase 52 entry, lines 158-169 — goal + success criteria to rewrite),
    .planning/phases/52-packaging-distribution-channels/52-CONTEXT.md (Domain block — the rescoped phase boundary; deferred ideas section — explicit PKG-02/03/04 deferral rationale)
  </read_first>
  <action>
    **A. REQUIREMENTS.md edits:**

    1. **Remove the "Auto-update mechanism inside the binary" row from Out of Scope.** Find the row at line 64:
       ```
       | Auto-update mechanism inside the binary | Package managers (brew/scoop/apt) own updates; in-binary updater adds security surface |
       ```
       Delete this row outright. The rationale ("package managers own updates") is invalidated by Phase 52's rescope — package managers are themselves out of v1.9 scope.

    2. **Mark PKG-02/03/04 as deferred from v1.9 in the Packaging & Distribution section.** Update each of:
       ```
       - [ ] **PKG-02**: ...
       - [ ] **PKG-03**: ...
       - [ ] **PKG-04**: ...
       ```
       to:
       ```
       - [ ] **PKG-02** (deferred from v1.9, see Phase 52 rescope): Users can install Serena on macOS and Linux via a Homebrew tap...
       - [ ] **PKG-03** (deferred from v1.9, see Phase 52 rescope): ...
       - [ ] **PKG-04** (deferred from v1.9, see Phase 52 rescope): ...
       ```
       Keep the original requirement text intact so future re-introduction can copy-paste.

    3. **Update the Traceability table** at line 73. Change the three PKG rows:
       ```
       | PKG-02 | Phase 52 | Pending |
       | PKG-03 | Phase 52 | Pending |
       | PKG-04 | Phase 52 | Pending |
       ```
       to:
       ```
       | PKG-02 | Deferred | Rescoped out of v1.9 — see PKG-DEFER-03 |
       | PKG-03 | Deferred | Rescoped out of v1.9 — see PKG-DEFER-04 |
       | PKG-04 | Deferred | Rescoped out of v1.9 — see PKG-DEFER-05 |
       ```

    4. **Add new entries to the Future Requirements > Packaging coverage expansion section** (the existing PKG-DEFER-01 / PKG-DEFER-02 list at line 47):
       ```
       - **PKG-DEFER-03**: Homebrew tap (originally PKG-02; deferred from v1.9 per Phase 52 rescope)
       - **PKG-DEFER-04**: Scoop bucket (originally PKG-03; deferred from v1.9 per Phase 52 rescope)
       - **PKG-DEFER-05**: Native Linux package (deb/rpm/AUR; originally PKG-04; deferred from v1.9 per Phase 52 rescope)
       ```

    5. **Add new in-scope requirements covering the actual Phase 52 work.** After PKG-04 in the Packaging & Distribution section, add:
       ```
       - [ ] **PKG-05**: Binary + product renamed `serena` → `helix` at v1.9; module path renamed to `github.com/agenthands/helix`; env vars `SERENA_*` → `HELIX_*`; config dir `~/.serena/` → `~/.helix/`; MCP server registration name flipped — all per CONTEXT.md D-01..D-05 hard-cut policy
       - [ ] **PKG-06**: In-binary self-upgrade — `helix update` (read-only check) and `helix upgrade` (install with minisign signature verification, atomic swap, hard-refuse downgrade, daemon-aware) per CONTEXT.md D-06..D-12
       - [ ] **PKG-07**: `EMBED-AUDIT.md` manifest classifying every runtime asset; `minisign.pub` embedded via `//go:embed` with build-time-synced copy per CONTEXT.md D-13..D-15
       ```
       And add corresponding traceability rows:
       ```
       | PKG-05 | Phase 52 | Pending |
       | PKG-06 | Phase 52 | Pending |
       | PKG-07 | Phase 52 | Pending |
       ```

    6. **Update the Coverage summary** at line 89-91 to reflect the new totals (was: 14 mapped to phases; now: 14 + 3 new = 17, with 3 deferred → 14 mapped to in-scope phases, 3 deferred). Recompute and write the new lines.

    **B. ROADMAP.md Phase 52 entry rewrite (lines 158-169):**

    Replace the existing entry with the rescoped version:
    ```
    ### Phase 52: packaging-distribution-channels
    **Goal**: Users can install Helix as a single self-contained signed binary (continued from Phase 51) and upgrade it in place via `helix upgrade`. The binary, env vars, config dirs, and MCP server registration name all flip from `serena` to `helix` as a hard-cut breaking change at v1.9. An embed audit manifest documents what ships inside the binary versus what the binary downloads at runtime.
    **Depends on**: Phase 51 (consumes goreleaser archives + minisign signing key + reproducibility CI gate)
    **Requirements**: PKG-05, PKG-06, PKG-07 (PKG-02/03/04 deferred from v1.9 per phase rescope)
    **Sizing**: L
    **Success Criteria** (what must be TRUE):
      1. `cmd/helix/main.go` builds CGO=0 and produces a `helix` binary; `cmd/serena/` does not exist; the Go module path is `github.com/agenthands/helix`.
      2. `goreleaser --snapshot --clean` produces 6 `helix_v*` archives in `dist/` with no leftover `serena_v*` archives.
      3. `helix update` queries the GitHub Releases API and prints `current: vX / latest: vY` plus release-notes body without making any filesystem mutation.
      4. `helix upgrade` end-to-end: daemon-detect → permission probe → API fetch → semver compare (hard-refuse downgrade) → archive download → minisign verify → extract → atomic swap → `os.Exec` re-launch with same args minus `upgrade`.
      5. Tampered or wrong-key signatures are rejected with a single canonical error message; signature verification uses the build-time-synced embedded `minisign.pub`.
      6. `EMBED-AUDIT.md` exists in the phase directory classifying every runtime asset as embedded / external-by-design / gap; gap-flagged items are closed in-phase.
      7. INSTALL.md, CHANGELOG.md, README.md, USAGE.md, CONTRIBUTING.md, CLAUDE.md all use `helix` as the binary/product name; CHANGELOG v1.9 includes a Breaking Changes subsection enumerating the rename, env-var, config-dir, and MCP registration breaks.
      8. `go test ./...`, `go vet ./...`, `make verify-embed-pubkey`, `make release-snapshot` all green.
    **Plans**: 6 plans
    **Rescope rationale**: Original scope (PKG-02 Homebrew, PKG-03 Scoop, PKG-04 native Linux) abandoned during /gsd-discuss-phase 2026-04-29 in favor of self-contained-binary + in-binary self-upgrade. PKG-02/03/04 deferred to a future milestone (PKG-DEFER-03/04/05). Decision recorded in 52-CONTEXT.md.
    ```

    Add the plans listing under the entry:
    ```
    Plans:
    - [ ] 52-01-PLAN.md — Wave 0 test scaffolding + Makefile embed-pubkey + CI gate
    - [ ] 52-02-PLAN.md — Wave 1 module path + cmd dir + goreleaser + version wiring
    - [ ] 52-03-PLAN.md — Wave 2 env vars + config dirs + MCP registration name flip
    - [ ] 52-04-PLAN.md — Wave 3 internal/upgrade/ package + cobra subcommands
    - [ ] 52-05-PLAN.md — Wave 3 EMBED-AUDIT.md manifest
    - [ ] 52-06-PLAN.md — Wave 3 docs + REQUIREMENTS/ROADMAP/CHANGELOG/INSTALL/README/USAGE/CONTRIBUTING/CLAUDE
    ```

    Per CLAUDE.md: this task touches no Go code, but run `go vet ./...` and `go test ./...` anyway as a defensive sanity check (zero cost; catches if a previous plan left the tree dirty).
  </action>
  <verify>
    <automated>! grep -q "Auto-update mechanism inside the binary" .planning/REQUIREMENTS.md &amp;&amp; grep -q "PKG-02.*deferred from v1.9" .planning/REQUIREMENTS.md &amp;&amp; grep -q "PKG-DEFER-03" .planning/REQUIREMENTS.md &amp;&amp; grep -q "PKG-05" .planning/REQUIREMENTS.md &amp;&amp; grep -q "PKG-06" .planning/REQUIREMENTS.md &amp;&amp; grep -q "PKG-07" .planning/REQUIREMENTS.md &amp;&amp; grep -q "PKG-02 | Deferred" .planning/REQUIREMENTS.md &amp;&amp; grep -q "single self-contained signed binary" .planning/ROADMAP.md &amp;&amp; grep -q "PKG-05, PKG-06, PKG-07" .planning/ROADMAP.md &amp;&amp; grep -q "Rescope rationale" .planning/ROADMAP.md &amp;&amp; ! grep -q "brew install <tap>/serena" .planning/ROADMAP.md &amp;&amp; go vet ./... &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <done>
    REQUIREMENTS.md: out-of-scope row removed; PKG-02/03/04 marked deferred with cross-references to PKG-DEFER-03/04/05; new PKG-05/06/07 added in-scope; Traceability table updated. ROADMAP.md: Phase 52 entry rewritten with rescoped goal, new success criteria, and plan list. `go vet` and `go test` green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG.md v1.9 entry, INSTALL.md Upgrading section, and flip every "serena" reference in README/USAGE/CONTRIBUTING/CLAUDE.md</name>
  <files>
    CHANGELOG.md,
    INSTALL.md,
    README.md,
    USAGE.md,
    CONTRIBUTING.md,
    CLAUDE.md
  </files>
  <read_first>
    CHANGELOG.md (existing structure; per RESEARCH.md Open Question 4 the convention is "v1.X — Title (date)" with subsection narratives — Phase 52 v1.9 entry MUST start with `### Breaking Changes`),
    INSTALL.md (existing Phase 51 D-04 verify block + agenthands/helix URLs — needs `serena_v` → `helix_v` flips and a new "Upgrading" section),
    README.md (Phase 51 51-04 left attribution + install path; this task flips remaining `serena` references to `helix` while preserving "originally inspired by Python Serena" attribution),
    USAGE.md (every `serena` invocation in code blocks flips to `helix`),
    CONTRIBUTING.md (build/test/release commands flip),
    CLAUDE.md (build commands example: `go build ./cmd/serena` → `go build ./cmd/helix`; entire CLAUDE.md needs review),
    .planning/phases/52-packaging-distribution-channels/52-CONTEXT.md (D-01..D-05 — exact break list; "Originally inspired by Python Serena" attribution stays per the v1.8 decision in STATE.md)
  </read_first>
  <action>
    **A. CHANGELOG.md v1.9 entry:**

    Add a new entry at the top of the v1.9 section (or create the section if it doesn't exist yet). Structure per RESEARCH.md Open Question 4:

    ```markdown
    ## v1.9 — Polish & Infra (<release-date>)

    ### Breaking Changes (v1.8 → v1.9)

    This release renames the binary and reorganizes user-visible state. Existing users must take one manual step after upgrading.

    - **Binary renamed:** `serena` → `helix`. The archive shipped on GitHub Releases is now `helix_v1.9.0_<os>_<arch>.tar.gz`. Scripts that invoke `serena` will fail with "command not found"; rename the symlink or update the script. There is no backwards-compat symlink (D-04).
    - **Env vars renamed:** every `SERENA_*` becomes `HELIX_*`. Examples: `SERENA_LOG_LEVEL` → `HELIX_LOG_LEVEL`; `SERENA_TEST_*` → `HELIX_TEST_*`. There is no fallback (D-02).
    - **Config dirs renamed:** `~/.serena/` → `~/.helix/`; `<project>/.serena/project.yml` → `<project>/.helix/project.yml`; `<project>/.serena/memories/` → `<project>/.helix/memories/`. There is no migration tool. To free disk after upgrade: `rm -rf ~/.serena`.
    - **MCP server registration name flipped:** existing `serena` registrations in `.mcp.json`, `claude_desktop_config.json`, etc. silently stop working. Re-run `helix setup <client>` (e.g., `helix setup claude-code`) to register the new identity. If you have an old registration: `claude mcp remove serena` first.
    - **Module path renamed:** `github.com/postfix/serena` → `github.com/agenthands/helix`. Affects only contributors who import the module from outside the repo (rare).
    - **Claude Code hooks installed by `serena setup` reference the old binary name** and will silently fail. Re-run `helix setup claude-code` to regenerate hooks against the new binary.

    ### Self-upgrade subcommand pair (NEW)

    Helix can now upgrade itself in place. Two verbs (modeled after `apt update` / `apt upgrade`):

    - `helix update` — read-only check. Queries the GitHub Releases API; prints `current: vX / latest: vY` plus the release-notes body. No mutation.
    - `helix upgrade` — installs the latest release. Permission-probes the install path before any network I/O; downloads the archive + signature; verifies the minisign signature against the embedded public key; atomic-swaps the binary in place; re-launches with the same arguments minus `upgrade`.

    Flags: `--prerelease` (include `v*-rc*`/`v*-beta*` releases), `--version vX.Y.Z` (pin to specific version; overrides `--prerelease`), `--check` (alias for `helix update`), `--dry-run` (download + verify but do not swap).

    Hard refusals (no override flags):
    - **Downgrades:** if the chosen target is `<= current`, `helix upgrade` exits 0 with "already up to date". Use the manual GitHub Releases verification recipe in INSTALL.md to install older versions explicitly.
    - **In-daemon upgrades:** running `helix upgrade` from inside a daemon child prints "restart the daemon manually" and exits 0. Stop the daemon first.
    - **Unwritable install path:** prints the exact `sudo helix upgrade <flags>` re-invocation and exits non-zero. No internal sudo prompt.

    Rate-limit guidance: `helix update`/`helix upgrade` use the GitHub anonymous API (60 req/hr/IP). Set `GITHUB_TOKEN` to lift to 5000/hr in CI environments.

    See `INSTALL.md` > **Upgrading** for full details.

    ### Embed audit manifest (NEW)

    Phase 52 produced `.planning/phases/52-packaging-distribution-channels/EMBED-AUDIT.md`: a living manifest of every runtime asset the binary reads, classified embedded / external-by-design / gap. Future contributors adding new disk-read code consult this manifest. The classification matters because Helix maintains a "single self-contained binary" property except for explicitly-listed exceptions (LS binaries are external-by-design — `internal/langregistry/installer.go` downloads them).

    ### Removed

    - Phase 52 originally targeted Homebrew tap (PKG-02), Scoop bucket (PKG-03), and native Linux packages (PKG-04). The user re-scoped during planning in favor of self-contained-binary + in-binary self-upgrade. PKG-02/03/04 are deferred to a future milestone (see `.planning/REQUIREMENTS.md` PKG-DEFER-03/04/05).

    ### Known Issues

    - On Windows, `helix upgrade` leaves a `helix.exe.old` file next to the new binary (acceptable property of Windows in-place self-replacement; the old file is removable manually). A future v1.10 polish phase may mark it hidden.
    - The minisign keypair shipped at v1.9.0 is the placeholder put in place in Phase 51; the maintainer must rotate to a real keypair before the first user-facing release. CONFIRM via `head -1 minisign.pub` showing the real signing identity, not "PLACEHOLDER".
    ```

    **B. INSTALL.md edits:**

    1. **Flip `serena_v` → `helix_v` in every archive name reference**, especially the Phase 51 D-04 verification recipe block.
    2. **Flip `serena` → `helix` in every binary-name reference**, including the curl-fetch, `chmod +x`, and `mv` commands.
    3. **Add a new `## Upgrading` section** after the existing install instructions:
       ```markdown
       ## Upgrading

       Helix can upgrade itself in place. Two verbs:

       ### Check for a newer release (read-only)

       ```sh
       helix update
       ```

       Prints current and latest versions plus the release notes from GitHub. No filesystem mutation; safe to run anytime. Honors `GITHUB_TOKEN` if set (recommended in CI to avoid the 60 req/hr unauthenticated rate limit).

       ### Install the latest release

       ```sh
       helix upgrade
       ```

       Performs in order: daemon-detect → permission probe → API fetch → semver compare → archive download → minisign verification → extract → atomic swap → re-launch. Exits cleanly with a clear message in any of these cases:

       - Install path not writable: prints the exact `sudo helix upgrade` re-invocation. (No internal sudo prompt.)
       - Already up to date (or remote latest is older than your installed version): exits 0 with "already up to date". There is no `--force-downgrade` flag — go to GitHub Releases manually for an older version.
       - Running as a daemon child: prints "restart the daemon manually" and exits 0. Stop the daemon first.

       ### Flags

       | Flag | Purpose |
       |------|---------|
       | `--prerelease` | Include `v*-rc*` / `v*-beta*` / `v*-alpha*` tags. Defaults to stable-only. |
       | `--version vX.Y.Z` | Pin to a specific version. Overrides `--prerelease`. |
       | `--check` | Alias for `helix update`. Functionally identical. |
       | `--dry-run` | Download + verify but skip swap and re-launch. Useful for CI verification. |

       ### Rate limits

       The GitHub Releases API permits 60 requests/hour per IP for unauthenticated callers. If `helix update` or `helix upgrade` returns "github API rate limited", set `GITHUB_TOKEN` (a fine-grained personal access token with `public_repo` scope is sufficient) and retry. Authenticated calls get 5000/hour.

       ### Manual upgrade (alternative)

       If you prefer not to use the in-binary upgrade, the original Phase 51 verification recipe still works — fetch the archive + signature + checksums from the GitHub Releases page, verify with minisign, and replace your binary by hand.
       ```

    **C. README.md, USAGE.md, CONTRIBUTING.md edits:**

    For each file, run:
    ```bash
    grep -n "serena" <file>
    ```
    For each match, decide:
    - User-facing binary invocation, command name, install path, archive name → flip to `helix`.
    - Historical attribution ("originally inspired by Python Serena", "the legacy Serena Python codebase") → KEEP. The v1.8 decision (STATE.md) preserves this attribution; v1.9 doesn't change it.
    - Code-block invocations (`go build ./cmd/serena`, `make build`) → flip cmd path to `./cmd/helix` (Plan 02 already changed the Makefile so users running `make build` are correct already; CLAUDE.md needs the explicit `./cmd/helix` flip).

    For each match in **CLAUDE.md**, the same rule applies. The Go Development Commands section currently reads:
    ```
    - `go build ./cmd/serena` - Build the serena binary
    ```
    Flips to:
    ```
    - `go build ./cmd/helix` - Build the helix binary
    ```
    Repeat across the file. Preserve "Originally inspired by Python Serena" / "Python Serena as a read-only reference" attribution.

    **D. Final verification:**
    ```
    grep -rn 'serena' README.md USAGE.md CONTRIBUTING.md CLAUDE.md INSTALL.md CHANGELOG.md
    ```
    Every remaining hit MUST be in one of:
    - A historical-attribution context ("inspired by", "legacy Python Serena", "originally" sentences).
    - A v1.9 CHANGELOG breaking-change line documenting the rename ("`serena` → `helix`").
    - The legacy/ subtree (excluded but worth confirming via `grep --exclude-dir=legacy`).

    **Per-file attribution-mention budget** (enforced by the verify gate):
    - `README.md`, `USAGE.md`, `CONTRIBUTING.md`: AT MOST ONE `\bserena\b` mention per file (the "Originally inspired by Python Serena" attribution from CONTEXT.md D-03). Any second mention fails the verify.
    - Each of these three files MUST also be free of `serena` code-pattern invocations: zero `go build \./cmd/serena` lines and zero leading `serena (--version|setup|status|activate)` invocations.
    - `INSTALL.md`, `CLAUDE.md`: zero `serena_v` archive references and zero `go build ./cmd/serena` references (covered by separate explicit grep checks).
    - `CHANGELOG.md`: unrestricted — it documents the historical rename.

    Per CLAUDE.md: run `go vet ./...` and `go test ./...`. They MUST be green.
  </action>
  <verify>
    <automated>grep -q "## v1.9 — Polish &amp; Infra" CHANGELOG.md &amp;&amp; grep -q "### Breaking Changes" CHANGELOG.md &amp;&amp; grep -q "Binary renamed" CHANGELOG.md &amp;&amp; grep -q "helix update" CHANGELOG.md &amp;&amp; grep -q "helix upgrade" CHANGELOG.md &amp;&amp; grep -q "PKG-DEFER-03" CHANGELOG.md &amp;&amp; grep -q "## Upgrading" INSTALL.md &amp;&amp; grep -q "helix update" INSTALL.md &amp;&amp; grep -q "GITHUB_TOKEN" INSTALL.md &amp;&amp; grep -q "helix_v" INSTALL.md &amp;&amp; ! grep -q "serena_v" INSTALL.md &amp;&amp; grep -q "go build ./cmd/helix" CLAUDE.md &amp;&amp; ! grep -q "go build ./cmd/serena" CLAUDE.md &amp;&amp; for f in README.md USAGE.md CONTRIBUTING.md; do [ "$(grep -c '\bserena\b' "$f")" -le 1 ] || { echo "$f has &gt;1 serena mention"; exit 1; }; ! grep -qE 'go build \./cmd/serena|^\s*serena (--version|setup|status|activate)' "$f" || { echo "$f has serena code patterns"; exit 1; }; done &amp;&amp; go vet ./... &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <done>
    CHANGELOG.md has a v1.9 entry with `### Breaking Changes` enumerating the four user-visible breaks (binary rename, env vars, config dirs, MCP registration), the self-upgrade subcommand pair documented with flags, the embed audit referenced, and the rescope rationale captured. INSTALL.md has the new `## Upgrading` section with both verbs, all four flags, GITHUB_TOKEN guidance, and the rate-limit doc. README/USAGE/CONTRIBUTING/CLAUDE.md no longer reference `serena` outside historical-attribution and CHANGELOG-rename contexts. `go vet` and `go test` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project planning artifacts → executor agents | REQUIREMENTS.md and ROADMAP.md are consumed by future planners + executors; an inaccurate row creates a divergent execution path on a future phase. |
| User docs → user environment | INSTALL.md and CHANGELOG.md drive user behavior post-upgrade; a missing breaking-change callout means users hit silent failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06-01 | Spoofing / Repudiation | A future planner reads REQUIREMENTS.md, sees PKG-02/03/04 still mapped to Phase 52, and erroneously assumes Phase 52 is incomplete or schedules a re-execution | mitigate | The Traceability table maps PKG-02/03/04 to "Deferred" with a pointer to PKG-DEFER-03/04/05. The Phase 52 ROADMAP entry has an explicit "Rescope rationale" line. The Out-of-Scope row inversion + the new PKG-05/06/07 entries make the actual scope unambiguous. |
| T-52-06-02 | Tampering | A user upgrades from v1.8 without reading CHANGELOG, hits silent breakage (commands fail, MCP server missing, hooks broken) and cannot diagnose | mitigate | The CHANGELOG `### Breaking Changes` subsection enumerates every break with a one-line "what to do" remediation: rename the script's `serena` invocation, run `helix setup <client>`, optionally `rm -rf ~/.serena`. The INSTALL.md `## Upgrading` section is cross-referenced. |
| T-52-06-03 | Information Disclosure | The CHANGELOG mentions a "minisign placeholder" that hasn't been rotated, hinting that signature verification is a no-op until rotation | accept (with explicit warning) | The Phase 51 phase already established the placeholder posture (DEF-51-03). The Known Issues subsection in CHANGELOG explicitly documents it: signature verification fails-closed under the placeholder, which is the correct security behavior. The mitigation is the maintainer rotating to a real keypair, which is out of Phase 52's authority but documented as required. |
| T-52-06-04 | Denial of Service | A user follows INSTALL.md and runs `helix upgrade` without `GITHUB_TOKEN`; their CI pipeline exhausts the 60 req/hr rate limit and breaks repeatedly | mitigate | The new `### Rate limits` subsection in INSTALL.md tells users explicitly to set `GITHUB_TOKEN` for CI use, what scope to grant, and what rate limit they get with auth. RESEARCH.md Pitfall 6 is the canonical statement of this risk. |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md`: out-of-scope row removed; PKG-02/03/04 marked deferred; PKG-DEFER-03/04/05 entries added; PKG-05/06/07 added in-scope; Traceability table updated to match
- `.planning/ROADMAP.md`: Phase 52 entry rewritten with rescoped goal/success-criteria/plan list and explicit rescope rationale
- `CHANGELOG.md`: v1.9 entry contains `### Breaking Changes`, self-upgrade subcommand documentation with flags, embed audit reference, and rescope rationale
- `INSTALL.md`: contains new `## Upgrading` section; all archive name references flipped to `helix_v`; `helix update` and `helix upgrade` documented with all four flags and GITHUB_TOKEN guidance
- `README.md`, `USAGE.md`, `CONTRIBUTING.md`, `CLAUDE.md`: no `serena` references outside historical-attribution / CHANGELOG-rename contexts
- `go vet ./...` and `go test ./...` green (defensive, non-Go files touched but the project rule applies)
</verification>

<success_criteria>
1. REQUIREMENTS.md: "Auto-update mechanism inside the binary | Out of scope" row removed; PKG-02/03/04 marked deferred from v1.9; PKG-DEFER-03/04/05 added; new in-scope PKG-05/06/07 added; Traceability updated.
2. ROADMAP.md: Phase 52 entry rewritten with rescoped goal, 8 success criteria, plan list, and rescope rationale.
3. CHANGELOG.md: v1.9 entry with `### Breaking Changes` enumerating four breaks + new self-upgrade subcommand pair + embed audit + rescope rationale + Known Issues for placeholder pubkey + Windows .old file.
4. INSTALL.md: `## Upgrading` section added; all `serena_v` archive references flipped to `helix_v`; both verbs + all flags + GITHUB_TOKEN documented.
5. README/USAGE/CONTRIBUTING/CLAUDE.md: zero `serena` references outside historical-attribution and CHANGELOG-rename contexts.
6. `go vet ./...` and `go test ./...` green.
</success_criteria>

<output>
After completion, create `.planning/phases/52-packaging-distribution-channels/52-06-SUMMARY.md` documenting:
- The exact REQUIREMENTS.md edits performed (rows removed, rows added, traceability changes)
- The exact ROADMAP.md Phase 52 rewrite (line numbers before/after; goal text)
- A summary of the CHANGELOG v1.9 entry's structure (Breaking Changes count, new subsection list)
- A list of every doc file flipped (line-counts of `serena` → `helix` flips)
- Confirmation: zero `serena` matches in non-legacy doc files outside attribution/CHANGELOG contexts
</output>
</content>
</invoke>