---
phase: 53-obs-metrics-gaps
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - internal/kernel/lspool/metrics.go
  - internal/kernel/lspool/metrics_test.go
  - internal/kernel/lspool/pool.go
  - internal/daemon/wiring_test.go
autonomous: true
requirements: [OBS-03]
tags: [observability, lspool, metrics, helix]
must_haves:
  truths:
    - "lspool.MetricsSink interface gains LSPoolLookup(language, result string) method (D-14)"
    - "AcquireLease emits result=hit on workerForKeyLocked share-path (pool.go:114-122) and result=miss on the spawn path (pool.go:130-141), regardless of whether spawn succeeds (D-02)"
    - "Dirty acquires count as misses (bypass workerForKeyLocked by design)"
    - "*obs.Metrics satisfies the extended lspool.MetricsSink at compile time (existing wiring_test.go assertion auto-validates)"
  artifacts:
    - path: "internal/kernel/lspool/metrics.go"
      provides: "Extended MetricsSink interface + LookupHit/LookupMiss constants + NoopSink stub method"
      contains: "LSPoolLookup(language, result string)"
    - path: "internal/kernel/lspool/pool.go"
      provides: "AcquireLease branch emission at hit + miss boundaries"
      contains: "p.metrics.LSPoolLookup"
    - path: "internal/kernel/lspool/metrics_test.go"
      provides: "recordingSink lookup tracking + TestPool_AcquireLease_LookupEmission + LookupHit/LookupMiss constants test"
      contains: "TestPool_AcquireLease_LookupEmission"
  key_links:
    - from: "internal/kernel/lspool/pool.go AcquireLease"
      to: "p.metrics.LSPoolLookup"
      via: "branch emission at share/spawn boundaries"
      pattern: "p\\.metrics\\.LSPoolLookup\\(.*Language, Lookup(Hit|Miss)\\)"
    - from: "*obs.Metrics"
      to: "lspool.MetricsSink (extended)"
      via: "compile-time assertion in wiring_test.go"
      pattern: "var _ lspool\\.MetricsSink = \\(\\*obs\\.Metrics\\)"
---

<objective>
Extend `lspool.MetricsSink` with `LSPoolLookup(language, result string)`, instrument `Pool.AcquireLease` at the share-vs-spawn branch (D-02), wire the sink in tests, and confirm `*obs.Metrics` still satisfies the extended interface via the existing daemon compile-time assertion.

Purpose: Closes the lspool half of OBS-03's "cache hit-rate" gap. Branch-emission at the canonical observable boundary (`workerForKeyLocked` returns non-nil = hit; spawn block runs = miss) gives operators a clean PromQL hit-ratio.
Output: lspool-cache hit/miss visibility on `helix_lspool_lookups_total{language, result}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/53-obs-metrics-gaps/53-CONTEXT.md
@.planning/phases/53-obs-metrics-gaps/53-RESEARCH.md
@.planning/phases/53-obs-metrics-gaps/53-PATTERNS.md
@.planning/phases/53-obs-metrics-gaps/53-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: Wave 0 — extend recordingSink + write TestPool_AcquireLease_LookupEmission (RED)</name>
  <files>internal/kernel/lspool/metrics_test.go</files>
  <read_first>
    - internal/kernel/lspool/metrics_test.go (full file — recordingSink shape with mu/append/snapshot, TestMetricsSink_EvictionReasonConstants pattern around lines 94-99)
    - internal/kernel/lspool/metrics.go (full file — interface, EvictXxx constants, NoopSink stubs, var _ MetricsSink assertion at line 63)
    - internal/kernel/lspool/pool.go (lines 108-147 AcquireLease branches; structure of workerForKeyLocked dispatch)
    - .planning/phases/53-obs-metrics-gaps/53-PATTERNS.md (recording-sink event/append/snapshot/constants pattern)
  </read_first>
  <behavior>
    - Test 1: TestPool_AcquireLease_LookupEmission — given a Pool wired with a recordingSink, when AcquireLease is called with dirty=false and a warm worker exists for the workspace key, the recorder observes one lookupEvent{lang, "hit"} and zero lookupEvent{*, "miss"}.
    - Test 2: When AcquireLease is called with dirty=false and NO warm worker exists, the recorder observes one lookupEvent{lang, "miss"} (regardless of whether spawn succeeds, including circuit-open / max-workers refusal).
    - Test 3: When AcquireLease is called with dirty=true, the recorder observes one lookupEvent{lang, "miss"} (D-02: dirty bypasses cache by design).
    - Test 4: TestMetricsSink_LookupResultConstants asserts LookupHit == "hit" and LookupMiss == "miss".
  </behavior>
  <action>
    Modify `internal/kernel/lspool/metrics_test.go`:

    1. Add a new event struct after the existing `evictionEvent` (around lines 23-36):

       type lookupEvent struct {
           lang   string
           result string
       }

    2. Extend `recordingSink` struct to track lookups by adding a `lookups []lookupEvent` field alongside the existing `workers`, `evictions`, `circuit`, `restarts` fields.

    3. Add the new method on `*recordingSink` mirroring the existing `LSPoolEviction` shape — mutex-guarded append to `r.lookups` of `lookupEvent{lang: language, result: result}`. Method signature: `func (r *recordingSink) LSPoolLookup(language, result string)`.

    4. Extend the `snapshot()` helper to also return a deep-copy of `lookups`. The snapshot return tuple becomes `(workers, evictions, circuit, restarts, lookups)` — adjust call sites in existing tests as needed (preserve backward compat by adding `lookups` as the LAST return value).

    5. Add `TestPool_AcquireLease_LookupEmission` after the existing constants test. Use the existing test fixture for Pool construction (look for an existing test like `TestPool_*` in this file — likely `setupPool(t, sink)` helper or similar). Three sub-tests:
       - `t.Run("share_path_emits_hit", ...)` — pre-warm a worker for `wsKey`, call AcquireLease with dirty=false, assert exactly one snap.lookups entry with result=="hit".
       - `t.Run("spawn_path_emits_miss", ...)` — fresh pool, call AcquireLease with dirty=false, assert exactly one snap.lookups entry with result=="miss". Spawn may succeed or fail; both count as miss per D-02.
       - `t.Run("dirty_path_emits_miss", ...)` — pre-warm a worker, call AcquireLease with dirty=true, assert exactly one snap.lookups entry with result=="miss" (cache bypassed).

    6. Add `TestMetricsSink_LookupResultConstants` mirroring the existing `TestMetricsSink_EvictionReasonConstants` (lines 94-99): assert LookupHit == "hit" and LookupMiss == "miss" using stretchr/testify/assert.

    Tests will be RED until Task 2.2 lands the interface extension and pool.go instrumentation. Do NOT use t.Skip(). The compile failure on LSPoolLookup, LookupHit, LookupMiss references signals that Task 2.2 is required next.
  </action>
  <verify>
    <automated>go build ./internal/kernel/lspool/... 2>&amp;1 | grep -q 'undefined.*LookupHit\|undefined.*LookupMiss\|LSPoolLookup' &amp;&amp; echo "PRE-2.2 RED EXPECTED" || echo "build clean — Task 2.2 may have already landed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^func TestPool_AcquireLease_LookupEmission' internal/kernel/lspool/metrics_test.go`
    - `grep -q '^func TestMetricsSink_LookupResultConstants' internal/kernel/lspool/metrics_test.go`
    - `grep -q 'type lookupEvent struct' internal/kernel/lspool/metrics_test.go`
    - `grep -q 'lookups *\[\]lookupEvent' internal/kernel/lspool/metrics_test.go`
    - `grep -q 'func (r \*recordingSink) LSPoolLookup' internal/kernel/lspool/metrics_test.go`
    - `! grep -q 't.Skip' internal/kernel/lspool/metrics_test.go` (no skipped tests)
    - Three sub-tests present: `grep -c '"share_path_emits_hit"\|"spawn_path_emits_miss"\|"dirty_path_emits_miss"' internal/kernel/lspool/metrics_test.go` returns 3
  </acceptance_criteria>
  <done>RED test scaffolding committed; Task 2.2 will turn it green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: Extend MetricsSink interface, NoopSink, constants, and instrument AcquireLease</name>
  <files>internal/kernel/lspool/metrics.go, internal/kernel/lspool/pool.go</files>
  <read_first>
    - internal/kernel/lspool/metrics.go (full file — lines 10-25 interface, lines 30-43 EvictXxx const block, lines 48-60 NoopSink stubs, line 63 compile-time assertion)
    - internal/kernel/lspool/pool.go (lines 108-147 AcquireLease — confirm exact branch lines for hit (~114-122) and miss (~130-141))
    - internal/obs/metrics.go (the LSPoolLookup helper added in Plan 01 — confirm method signature matches)
    - .planning/phases/53-obs-metrics-gaps/53-PATTERNS.md (sink interface extension; AcquireLease branch emission)
    - .planning/phases/53-obs-metrics-gaps/53-RESEARCH.md (Code Examples §1: AcquireLease instrumentation verbatim)
  </read_first>
  <behavior>
    - Test 1: TestPool_AcquireLease_LookupEmission (from Task 2.1) all 3 sub-tests pass.
    - Test 2: TestMetricsSink_LookupResultConstants passes.
    - Test 3: Existing TestObsMetricsIsLSPoolSink in internal/daemon/wiring_test.go still passes (interface satisfaction held).
  </behavior>
  <action>
    **A. internal/kernel/lspool/metrics.go:**

    1. Append `LSPoolLookup(language, result string)` to the MetricsSink interface (after the existing 4 methods). Include a comment line `// Phase 53 D-14`.

    2. Add a new constants block after the existing EvictXxx block (around line 43) with `LookupHit = "hit"` and `LookupMiss = "miss"`. Include a comment `// Lookup result constants — closed enum per Phase 53 D-04`.

    3. Append a NoopSink stub method after the existing 4 NoopSink methods: `func (NoopSink) LSPoolLookup(string, string) {}`.

    The existing `var _ MetricsSink = NoopSink{}` assertion at line 63 stays unchanged — it auto-validates the new method on NoopSink at compile time.

    **B. internal/kernel/lspool/pool.go:**

    Modify AcquireLease (lines 108-147). Insert TWO new emission lines per RESEARCH §Code Examples 1:

    1. After the existing `p.logger.Info("shared lease acquired", ...)` line (within the `if w := p.workerForKeyLocked(wsKey); w != nil {` block), insert: `p.metrics.LSPoolLookup(wsKey.Language, LookupHit)` BEFORE the `return lease, nil` line.

    2. BEFORE the circuit-breaker setup block (BEFORE `cb := p.circuitForLanguage(wsKey.Language)`), insert a comment block and the miss emission:

       // D-02: miss is recorded BEFORE circuit/max-workers checks so refusals
       // count as misses. Dirty acquires also reach this point because they
       // bypass the workerForKeyLocked share branch by design.
       p.metrics.LSPoolLookup(wsKey.Language, LookupMiss)

    Do NOT change any other AcquireLease logic. Do NOT add a `dirty` label dimension (D-02 lock). Do NOT instrument any other call site in pool.go.
  </action>
  <verify>
    <automated>go test ./internal/kernel/lspool/... -run TestPool_AcquireLease_LookupEmission -v &amp;&amp; go test ./internal/kernel/lspool/... -run TestMetricsSink_LookupResultConstants -v &amp;&amp; go test ./internal/daemon/... -run TestObsMetricsIsLSPoolSink -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'LSPoolLookup(language, result string)' internal/kernel/lspool/metrics.go` — interface extended
    - `grep -q 'LookupHit *= *"hit"' internal/kernel/lspool/metrics.go &amp;&amp; grep -q 'LookupMiss *= *"miss"' internal/kernel/lspool/metrics.go` — constants present
    - `grep -q 'func (NoopSink) LSPoolLookup' internal/kernel/lspool/metrics.go` — NoopSink stub present
    - `grep -q 'p.metrics.LSPoolLookup(wsKey.Language, LookupHit)' internal/kernel/lspool/pool.go` — hit emission at share path
    - `grep -q 'p.metrics.LSPoolLookup(wsKey.Language, LookupMiss)' internal/kernel/lspool/pool.go` — miss emission at spawn path
    - `grep -c 'p.metrics.LSPoolLookup' internal/kernel/lspool/pool.go` returns exactly 2 (no double-counting)
    - `go vet ./internal/kernel/lspool/... ./internal/obs/... ./internal/daemon/...` exits 0
    - `go test ./internal/kernel/lspool/... -run 'TestPool_AcquireLease_LookupEmission|TestMetricsSink_LookupResultConstants' -v` exits 0
    - `go test ./internal/daemon/... -run TestObsMetricsIsLSPoolSink -v` exits 0
  </acceptance_criteria>
  <done>lspool sink + pool.go emission green; *obs.Metrics still satisfies extended lspool.MetricsSink.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pool.AcquireLease → metrics sink | language label originates from workspace key (operator-controlled in dev, internally normalized in production) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Denial of Service | helix_lspool_lookups_total cardinality | mitigate | result label is a closed enum (hit/miss) carved out in metrics_labels_test.go (Plan 01); language label is bounded in practice by the langregistry (52 entries); cardinality bound test (Plan 01 TestMetrics_CardinalityBounds_LSPoolLookups) fails the build if combo count exceeds 2*52 |
| T-53-05 | Tampering | recordingSink test fixture | accept | test-only, mutex-guarded, no production exposure |
</threat_model>

<verification>
- `go vet ./internal/kernel/lspool/... ./internal/daemon/...` exits 0
- `go test ./internal/kernel/lspool/...` exits 0 (full lspool package green)
- `go test ./internal/daemon/... -run TestObsMetricsIsLSPoolSink -v` exits 0 (compile-time assertion)
- `grep -c 'p.metrics.LSPoolLookup' internal/kernel/lspool/pool.go` returns exactly 2
</verification>

<success_criteria>
- AcquireLease emits hit/miss at the canonical D-02 boundaries
- *obs.Metrics still satisfies the extended lspool.MetricsSink (Plan 01's helper signature matches D-14)
- Dirty acquires emit miss (D-02 lock)
- Cache circuit-open / max-workers refusals emit miss (D-02 lock)
</success_criteria>

<output>
After completion, create `.planning/phases/53-obs-metrics-gaps/53-02-SUMMARY.md` with:
- Interface extension before/after
- pool.go diff (2 emission lines)
- Test results (3 sub-tests + constants test)
- Confirmation that the existing wiring_test.go compile-time assertion validates the extension automatically
</output>
