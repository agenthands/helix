---
phase: 54-obs-dashboards-runbooks
plan: 02
type: execute
wave: 1
depends_on: [54-01]
files_modified:
  - deploy/grafana/helix-overview.json
  - internal/obs/dashboards_test.go
autonomous: true
requirements: [OBS-01]
tags: [observability, grafana, dashboard, red-metrics, sessions]

must_haves:
  truths:
    - "deploy/grafana/helix-overview.json exists, parses as valid JSON, declares schemaVersion >= 38, uid 'helix-overview'"
    - "Dashboard exposes ${DS_PROMETHEUS} import-template (no hardcoded UIDs) and the $language + $instance template variables per RESEARCH §A.4"
    - "Panels cover RED metrics (tool-call rate, error rate by outcome, p50/p95/p99 latency) AND workspace activity (session start rate by transport, session error rate) AND a text panel restating the Phase 53 D-09 best-effort caveat for HTTP `ended`"
    - "Panel count is in the 6–9 band (D-04)"
    - "Every PromQL targets[].expr references metrics from the 12-family Phase 53 inventory and label names from AllowedLabels ∪ carveOuts (validated by internal/obs/dashboards_test.go)"
    - "Stat panels use reduceOptions.calcs = [\"lastNotNull\"] (Pitfall #2)"
    - "go test ./internal/obs/... -count=1 passes WITHOUT HELIX_DASHBOARDS_TEST_ALLOW_EMPTY (this plan removes the env-var gate from the dashboards branch)"
  artifacts:
    - path: "deploy/grafana/helix-overview.json"
      provides: "Primary operator dashboard — RED + sessions, screenshot target"
      min_lines: 200
      contains: "DS_PROMETHEUS"
    - path: "internal/obs/dashboards_test.go"
      provides: "Validation test with the dashboards-empty env-var gate removed"
  key_links:
    - from: "deploy/grafana/helix-overview.json"
      to: "internal/obs/metrics.go (registered families)"
      via: "PromQL targets[].expr metric names validated by TestDashboardsAndRunbooksReferenceRegisteredMetrics"
      pattern: "helix_(tool_calls_total|tool_duration_seconds|session_lifecycle_total)"
    - from: "deploy/grafana/helix-overview.json"
      to: "templating.list query: label_values(helix_lspool_workers, language)"
      via: "$language template variable"
      pattern: "label_values\\(helix_lspool_workers, language\\)"

threat_model:
  classification: LOW
  threats:
    - "T-1 (LOW): ${DS_PROMETHEUS} substitution at Grafana import time — operator-controlled Grafana side, not Helix; no input crosses a trust boundary into Helix runtime."
    - "T-2 (LOW): JSON parsed only by `internal/obs/dashboards_test.go` from project tree (trusted source)."
  no_high_severity: true
---

<objective>
Author `deploy/grafana/helix-overview.json` — the primary, screenshot-target dashboard covering RED metrics + workspace activity (sessions). 6–9 panels per D-04. Use the `${DS_PROMETHEUS}` import-template + Grafana 10 schemaVersion 38 baseline. After authoring, drop the Wave 0 `HELIX_DASHBOARDS_TEST_ALLOW_EMPTY` gate from the dashboards branch of `internal/obs/dashboards_test.go` so the registry-validation test is fully active for the dashboard surface.

Purpose: deliver the operator's first "is Helix healthy?" view; this is the dashboard the screenshot in Wave 2 will capture.
Output: one Grafana 10+ JSON dashboard, validation test fully gating dashboards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-obs-dashboards-runbooks/54-CONTEXT.md
@.planning/phases/54-obs-dashboards-runbooks/54-RESEARCH.md
@.planning/phases/54-obs-dashboards-runbooks/54-PATTERNS.md
@.planning/phases/54-obs-dashboards-runbooks/54-01-SUMMARY.md
@internal/obs/metrics.go
@internal/obs/dashboards_test.go

<interfaces>
<!-- Frozen metric inventory the dashboard PromQL must reference (from RESEARCH §A.1). -->
<!-- These are the only metric names that will pass validateExpr. -->

helix_tool_calls_total                       counter   labels: tool_name, profile, mode, language, outcome
helix_tool_duration_seconds                  histogram labels: tool_name, profile, mode, language     (use _bucket for histogram_quantile)
helix_session_lifecycle_total                counter   labels: phase ∈ {started,ended,error}, transport ∈ {stdio,http}
helix_lspool_workers                         gauge     labels: language     (used by $language template variable)

Allowed label names on selectors (from internal/obs/metrics.go AllowedLabels + metrics_labels_test.go carveOuts):
  AllowedLabels:        tool_name, profile, mode, language, outcome
  carveOut for helix_session_lifecycle_total: phase, transport
  PromQL-internal exemptions accepted by validateExpr: __name__, le, job, instance
  Runtime/template exemptions: up, go_*, process_*

closed-enum values (do NOT invent others — emission sites enforce):
  outcome (helix_tool_calls_total): success, invalid_args, not_found, circuit_open, ls_crash, timeout, internal
  phase (helix_session_lifecycle_total): started, ended, error
  transport (helix_session_lifecycle_total): stdio, http
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author deploy/grafana/helix-overview.json (6–9 panels — RED + sessions + caveat)</name>
  <files>deploy/grafana/helix-overview.json</files>
  <read_first>
    - .planning/phases/54-obs-dashboards-runbooks/54-RESEARCH.md §A.2 (panel → metric mapping for overview), §A.3 (Grafana 10 schema skeleton), §A.4 (template variable JSON), §C.1 (full skeleton + stat panel + text panel)
    - .planning/phases/54-obs-dashboards-runbooks/54-PATTERNS.md §"`deploy/grafana/helix-overview.json`" (lines 134-175)
    - internal/obs/metrics.go (frozen metric family list — confirm names match)
    - USAGE.md lines 600-706 (existing observability docs; specifically the Phase 53 D-09 best-effort caveat at line 706 — copy verbatim into the text panel)
  </read_first>
  <action>
Author the JSON file with the following structure. Use exact PromQL strings — these are validated by `internal/obs/dashboards_test.go::TestDashboardsAndRunbooksReferenceRegisteredMetrics`.

**Top-level keys (D-05, D-06, D-25 — RESEARCH §C.1 verbatim):**
```json
{
  "__inputs": [
    { "name": "DS_PROMETHEUS", "label": "Prometheus", "description": "", "type": "datasource", "pluginId": "prometheus", "pluginName": "Prometheus" }
  ],
  "__requires": [
    { "type": "grafana", "id": "grafana", "name": "Grafana", "version": "10.0.0" },
    { "type": "datasource", "id": "prometheus", "name": "Prometheus", "version": "1.0.0" },
    { "type": "panel", "id": "timeseries", "name": "Time series", "version": "" },
    { "type": "panel", "id": "stat", "name": "Stat", "version": "" },
    { "type": "panel", "id": "text", "name": "Text", "version": "" }
  ],
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "schemaVersion": 38,
  "tags": ["helix", "observability"],
  "templating": { "list": [ /* see template variables below */ ] },
  "time": { "from": "now-1h", "to": "now" },
  "timezone": "",
  "title": "Helix Overview",
  "uid": "helix-overview",
  "version": 1,
  "panels": [ /* 6–9 panels — see panel inventory below */ ]
}
```

**Template variables (D-08, RESEARCH §A.4 verbatim — both entries):**
- `language`: type=query, datasource=`${DS_PROMETHEUS}`, query=`label_values(helix_lspool_workers, language)`, refresh=2, multi=true, includeAll=true, allValue=`.*`, current=`{ "selected": false, "text": "All", "value": "$__all" }`.
- `instance`: type=query, datasource=`${DS_PROMETHEUS}`, query=`label_values(up{job=~".*helix.*"}, instance)`, refresh=2, multi=true, includeAll=true, allValue=`.*`, current=same as above.

**Panel inventory (target 7 panels — within 6–9 band per D-04):**

Each panel MUST have:
- Unique `id` (1..N).
- `datasource: { "type": "prometheus", "uid": "${DS_PROMETHEUS}" }` at panel level AND on every `targets[]` entry.
- `gridPos` arranged in 24-col grid; suggested layout below.
- `fieldConfig.defaults` and `fieldConfig.overrides: []` set (Grafana requires the keys; empty objects are fine).

| # | id | type | title | gridPos (h,w,x,y) | PromQL `expr` (in `targets[0].expr`) | legendFormat |
|---|----|------|-------|-------------------|---------------------------------------|--------------|
| 1 | 1 | timeseries | Tool call rate (5m) | (8, 12, 0, 0) | `sum by (tool_name) (rate(helix_tool_calls_total{language=~"$language"}[5m]))` | `{{tool_name}}` |
| 2 | 2 | timeseries | Tool error rate by outcome (5m) | (8, 12, 12, 0) | `sum by (outcome) (rate(helix_tool_calls_total{outcome!="success", language=~"$language"}[5m]))` | `{{outcome}}` |
| 3 | 3 | timeseries | Tool duration p50/p95/p99 | (8, 24, 0, 8) | three targets:<br>A: `histogram_quantile(0.50, sum by (le) (rate(helix_tool_duration_seconds_bucket{language=~"$language"}[5m])))` legendFormat `p50`<br>B: `histogram_quantile(0.95, sum by (le) (rate(helix_tool_duration_seconds_bucket{language=~"$language"}[5m])))` legendFormat `p95`<br>C: `histogram_quantile(0.99, sum by (le) (rate(helix_tool_duration_seconds_bucket{language=~"$language"}[5m])))` legendFormat `p99` | (per-target) |
| 4 | 4 | timeseries | Session start rate by transport (5m) | (8, 12, 0, 16) | `sum by (transport) (rate(helix_session_lifecycle_total{phase="started"}[5m]))` | `{{transport}}` |
| 5 | 5 | stat | Session error rate (5m) | (8, 6, 12, 16) | `sum (rate(helix_session_lifecycle_total{phase="error"}[5m]))` | `errors/s` |
| 6 | 6 | timeseries | Session ended rate by transport (5m) | (8, 6, 18, 16) | `sum by (transport) (rate(helix_session_lifecycle_total{phase="ended"}[5m]))` | `{{transport}}` |
| 7 | 99 | text | Note: HTTP session `ended` is best-effort | (4, 24, 0, 24) | (no targets — markdown content) | — |

**Stat panel (#5) MUST include (Pitfall #2):**
```json
"options": {
  "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
  "textMode": "auto",
  "graphMode": "area",
  "colorMode": "background",
  "justifyMode": "auto"
},
"fieldConfig": {
  "defaults": { "unit": "reqps", "decimals": 2 },
  "overrides": []
}
```

**Text panel (#7) content (D-02, RESEARCH §C.1 verbatim — copy `helix_session_lifecycle_total{transport="http",phase="ended"}` caveat from Phase 53 D-09):**
```json
{
  "id": 99,
  "type": "text",
  "title": "Note: HTTP session `ended` is best-effort",
  "datasource": null,
  "gridPos": { "h": 4, "w": 24, "x": 0, "y": 24 },
  "options": {
    "mode": "markdown",
    "content": "`helix_session_lifecycle_total{transport=\"http\", phase=\"ended\"}` only fires on a client-issued `DELETE /mcp`. Sessions that disappear due to server-side timeout do **not** register. This is documented in Phase 53 D-09 and USAGE.md — interpret HTTP `ended` rates as a lower bound."
  }
}
```

**No alerting blocks (D-07).** No panel may include an `alert: { ... }` field.

After authoring:
1. Validate JSON syntax: `python3 -m json.tool deploy/grafana/helix-overview.json > /dev/null`.
2. Run validation test (still in Wave-0 env-var mode for now): `HELIX_DASHBOARDS_TEST_ALLOW_EMPTY=1 go test ./internal/obs/... -count=1` — must pass. (Runbooks dir is still empty at this plan's runtime; the env-var keeps the runbook fatal silent.)
3. Run JUST the dashboard-validation pass (no env-var): `go test ./internal/obs/... -count=1 -run TestDashboardsAndRunbooksReferenceRegisteredMetrics$` — this WILL still fail because runbooks dir is empty (Plan 04's job). That's expected; Plan 04 closes the runbook half. But verify the failure is ONLY about runbooks, not about a dashboard PromQL error.
  </action>
  <verify>
    <automated>python3 -m json.tool deploy/grafana/helix-overview.json &gt; /dev/null &amp;&amp; HELIX_DASHBOARDS_TEST_ALLOW_EMPTY=1 go test ./internal/obs/... -count=1 -run TestDashboardsAndRunbooks &amp;&amp; grep -c '"uid": "helix-overview"' deploy/grafana/helix-overview.json | grep -E "^1$" &amp;&amp; grep -c '"schemaVersion": 38' deploy/grafana/helix-overview.json | grep -E "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - File `deploy/grafana/helix-overview.json` exists and is valid JSON (`python3 -m json.tool` exits 0).
    - `grep -c '"uid": "helix-overview"' deploy/grafana/helix-overview.json` returns 1.
    - `grep -c '"schemaVersion": 38' deploy/grafana/helix-overview.json` returns 1 (or 39 if planner bumped per Open Q 1; document choice in SUMMARY).
    - `grep -c '"DS_PROMETHEUS"' deploy/grafana/helix-overview.json` returns ≥ 8 (one in __inputs, two in templating, one panel-level + one target-level for each of 6 query panels = at least 8 occurrences).
    - Panel count is in [6, 9]: extract via `python3 -c "import json; d=json.load(open('deploy/grafana/helix-overview.json')); print(len(d['panels']))"` returns 6, 7, 8, or 9.
    - `grep -c '"alert"' deploy/grafana/helix-overview.json` returns 0 (D-07).
    - `grep -c '"reduceOptions"' deploy/grafana/helix-overview.json` returns ≥ 1 (the stat panel).
    - `grep -c '"calcs": \\["lastNotNull"\\]' deploy/grafana/helix-overview.json` returns ≥ 1 (Pitfall #2).
    - `grep -c "label_values(helix_lspool_workers, language)" deploy/grafana/helix-overview.json` returns ≥ 1 ($language template var).
    - `grep -c "label_values(up{job=~\\\".\\*helix.\\*\\\"}, instance)" deploy/grafana/helix-overview.json` returns ≥ 1 ($instance template var).
    - The text panel content contains the literal substring `best-effort` and `DELETE /mcp` (Phase 53 D-09 caveat).
    - `HELIX_DASHBOARDS_TEST_ALLOW_EMPTY=1 go test ./internal/obs/... -count=1` exits 0 (validator passes for the dashboard half; runbooks still gated by env-var until Plan 04).
  </acceptance_criteria>
  <done>
    helix-overview.json shipped with 6–9 panels covering RED + sessions + best-effort caveat; every PromQL passes registry validation; stat panels carry the mandatory `reduceOptions.calcs`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Wave 0 env-var gate from dashboards branch of internal/obs/dashboards_test.go</name>
  <files>internal/obs/dashboards_test.go</files>
  <read_first>
    - internal/obs/dashboards_test.go (full — find both `os.Getenv("HELIX_DASHBOARDS_TEST_ALLOW_EMPTY")` clauses)
    - .planning/phases/54-obs-dashboards-runbooks/54-01-SUMMARY.md (Wave 0 SUMMARY — confirms instruction to remove the gate per branch)
  </read_first>
  <action>
1. Find the dashboards branch — the block:
```go
if len(dashes) == 0 && os.Getenv("HELIX_DASHBOARDS_TEST_ALLOW_EMPTY") != "1" {
    t.Fatalf("no dashboards found at %s — Wave 1 must populate deploy/grafana/. " +
        "Set HELIX_DASHBOARDS_TEST_ALLOW_EMPTY=1 only during Phase 54 Wave 0.", dashGlob)
}
```
2. Replace with the unconditional fail-closed:
```go
if len(dashes) == 0 {
    t.Fatalf("no dashboards found at %s — deploy/grafana/ must contain at least one *.json dashboard.", dashGlob)
}
```
3. Leave the runbooks branch UNTOUCHED — Plan 04 (runbooks) drops that gate as the final step. Wave 1 plans 02/03 may run in either order, both before Plan 04; the env-var still needs to gate the runbook fatal until Plan 04 ships content.
4. Update the package-level comment in dashboards_test.go: replace the Wave-1 instruction about the dashboards gate with a note that the runbooks gate is still pending Plan 04.
5. Verify: `HELIX_DASHBOARDS_TEST_ALLOW_EMPTY=1 go test ./internal/obs/... -count=1` — must still pass (runbooks branch still gated).
6. Verify: `go test ./internal/obs/... -count=1 -run TestDashboardsAndRunbooksReferenceRegisteredMetrics$` (no env var) — must FAIL with a "no runbooks found at" message (NOT "no dashboards found"). This proves the dashboards branch is no longer gated and the runbooks branch is the only remaining empty-dir blocker.
  </action>
  <verify>
    <automated>HELIX_DASHBOARDS_TEST_ALLOW_EMPTY=1 go test ./internal/obs/... -count=1 -run TestDashboardsAndRunbooks &amp;&amp; ! go test ./internal/obs/... -count=1 -run "^TestDashboardsAndRunbooksReferenceRegisteredMetrics$" 2&gt;&amp;1 | tee /tmp/wave1-overview-test.log; grep -q "no runbooks found at" /tmp/wave1-overview-test.log &amp;&amp; ! grep -q "no dashboards found at" /tmp/wave1-overview-test.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HELIX_DASHBOARDS_TEST_ALLOW_EMPTY" internal/obs/dashboards_test.go` returns exactly 1 (only the runbooks branch + the package comment may still mention it; aim for exactly 1 occurrence — the runbook gate condition).
    - `grep -c "no dashboards found at" internal/obs/dashboards_test.go` returns 1 (the unconditional fatal still exists; no env-var gate).
    - The replaced block does NOT contain the substring `os.Getenv` adjacent to the dashboards check (verify by reading the file post-edit).
    - With env-var set: tests PASS (dashboards filled, runbooks still env-var-gated).
    - Without env-var: tests FAIL, and the failure mentions "no runbooks found at" but NOT "no dashboards found at".
  </acceptance_criteria>
  <done>
    Dashboards branch of the validator is now unconditionally fail-closed; runbooks branch retains the Wave-0 env-var gate until Plan 04 ships content.
  </done>
</task>

</tasks>

<verification>
- `go vet ./...` exits 0.
- `python3 -m json.tool deploy/grafana/helix-overview.json` exits 0.
- `HELIX_DASHBOARDS_TEST_ALLOW_EMPTY=1 go test ./internal/obs/... -count=1` exits 0.
- Without env-var, the validator fails ONLY on the runbooks branch (proves Task 2 worked).
</verification>

<success_criteria>
- `deploy/grafana/helix-overview.json` ships with 7 panels (within 6–9): tool-call rate, error rate by outcome, p50/p95/p99 latency, session start rate by transport, session error rate (stat), session ended rate by transport, best-effort caveat (text).
- Every PromQL is registry-valid; every label name is in AllowedLabels ∪ carveOuts ∪ {__name__, le, job, instance, up, go_*, process_*}.
- Stat panel carries `reduceOptions.calcs: ["lastNotNull"]`.
- Validation test's dashboards branch is no longer env-var-gated.
</success_criteria>

<output>
Create `.planning/phases/54-obs-dashboards-runbooks/54-02-SUMMARY.md` documenting:
- Final panel count and panel-id-to-title map.
- The exact schemaVersion chosen (38 baseline, or 39 if bumped per Open Q 1).
- Confirmation that the dashboards env-var gate is removed; runbooks gate remains pending Plan 04.
- Any deviations from RESEARCH §A.2 panel mapping (e.g., gridPos tweaks for visual coherence) and rationale.
</output>
