---
milestone: v2.0
audited: 2026-06-22
status: passed
scores:
  requirements: 31/31
  phases: 7/7
  integration: clean
  flows: 4/4
gaps:
  requirements: []   # none — all 31 satisfied (3-source cross-referenced)
  integration: []    # none — integration checker: INTEGRATES CLEANLY (post-Phase-96 re-audit)
  flows: []          # none — all 4 cross-phase E2E flows WIRED
nyquist:
  compliant_phases: [90, 91, 92, 93, 94, 95, 96]
  partial_phases: []
  missing_phases: []
  overall: compliant   # all 7 phases nyquist_compliant:true (93/94/95 closed + 96 closed in Phase 96 TD-05)
tech_debt: []          # all v2.0 audit tech-debt resolved in Phase 96 — see resolved_tech_debt
resolved_tech_debt:
  - phase: 96-address-v2-0-tech-debt
    closes:
      - "TD-01 (was 94): validateAdminAddr hardened to mirror validateGRPCAddr CR-01 (empty-host refusal + !ip.IsUnspecified() guard); empty-ADDR contract preserved in listenAdmin"
      - "TD-02 (was 94): dead mergeJSONConfig + its 4 tests removed; live removeFromJSONConfig retained; build/vet/test green"
      - "TD-03 (was 93): classifyBashTarget no longer counts a grep-family PATTERN as a file operand (grep/rg/ag/egrep/fgrep); fail-open/exit-0 preserved"
      - "TD-04 (was 95): get_tool_help Descriptions reworded 'MCP tool'->'Helix tool' at source; README regenerated via docgen (drift gate green, not hand-edited)"
      - "TD-05 (Nyquist coverage): phases 93/94/95 VALIDATION.md closed to nyquist_compliant:true (retroactive audit, 0 gaps); phase 96 also nyquist_compliant:true"
pre_existing:   # NOT v2.0 — carried from v1.12, environmental; tracked in STATE.md Deferred Items
  - "cmd/helix-bench TestRunSubcommandWires* require helix on PATH + a full bench agent/dataset env; self-skip without HELIX_BIN and fail identically on the pre-v2.0 baseline (the v1.12 'live execution honestly gated-skip' deferred item). Not a v2.0 regression."
---

# v2.0 Milestone Audit — CLI-First — MCP Surface Retirement (Post-Phase-96 Re-Audit)

**Verdict:** Definition of Done MET, status `passed`. All 31 requirements satisfied, all **7** phases (90–96) passed verification, cross-phase integration verified clean (post-Phase-96 re-audit), all 4 E2E flows wired, and **all Nyquist coverage closed (7/7 phases compliant)**. The non-blocking tech debt the first audit (2026-06-22, `tech_debt`) surfaced has been fully resolved by inserted Phase 96. The single remaining item is a pre-existing, environmental v1.12-carried deferred item — not a v2.0 regression.

## What changed since the first audit

The first v2.0 audit returned `status: tech_debt` (DoD met, zero blockers, but accumulated non-blocking items warranted a review before archival). Phase 96 ("Address v2.0 tech debt") was inserted to clear them:

| Item (first audit) | Phase 96 resolution | Evidence |
|--------------------|---------------------|----------|
| P94 `validateAdminAddr` shares the empty-host/wildcard gap CR-01 fixed in `validateGRPCAddr` | **TD-01** — mirrored the CR-01 shape (empty-host refusal + `!ip.IsUnspecified()`), preserved the empty-ADDR no-op in `listenAdmin` | `TestValidateAdminAddr` (+ new wildcard rows) green; reviewer + verifier confirmed no `addr==""` short-circuit regression |
| P94 dead `mergeJSONConfig` after the setup flip | **TD-02** — removed helper + 4 tests; live `removeFromJSONConfig` retained | `grep` zero production refs; `go build`/`go vet`/`go test` green |
| P93 nudge classifier counts a grep *pattern* as a file operand | **TD-03** — skip leading PATTERN for grep family only (grep/rg/ag/egrep/fgrep) | `TestClassifyBashTarget_GrepPatternNotFile` + 5 pre-existing classifier tests green; fail-open preserved |
| P95 generated `get_tool_help` row says "any MCP tool" | **TD-04** — reworded the four Description/BriefDescription literals at source; README regenerated via docgen | README:332 "any Helix tool"; `make verify-docs` green; golden regenerated |
| Nyquist: 93/94/95 `VALIDATION.md` `nyquist_compliant:false` | **TD-05** — retroactive audit closed all three (0 gaps, all COVERED); Phase 96 own flag also closed | 90–96 all `nyquist_compliant:true` |

## Requirements Coverage — 31/31 satisfied

3-source cross-referenced (REQUIREMENTS.md traceability `[x]` + phase VERIFICATION.md `passed` + SUMMARY frontmatter). Phase 96 added no new requirements (its TD-01..TD-04 are audit tech-debt items, not REQUIREMENTS.md IDs).

| Group | IDs | Phase(s) | Status |
|-------|-----|----------|--------|
| CLI | CLI-01..04 | 90 | ✅ satisfied |
| VERB | VERB-01..04 | 91 | ✅ satisfied |
| OUT | OUT-01..07 | 92 | ✅ satisfied |
| SEC | SEC-01..02 | 91 | ✅ satisfied |
| SKILL | SKILL-01..04 | 93 | ✅ satisfied |
| RETIRE | RETIRE-01..04 | 94 | ✅ satisfied |
| DOCS | DOCS-01..03 | 95 | ✅ satisfied |
| TEST | TEST-01..03 | 90/92/93 | ✅ satisfied |

Orphan check: none — every REQ-ID participates in ≥1 verified integration touchpoint.

## Phases — 7/7 passed

| Phase | Verification | Nyquist |
|-------|-------------|---------|
| 90 CLI one-shot dial spine + race-free warm reuse | passed | compliant |
| 91 Code-generated verb surface + tools/call enforcement | passed | compliant |
| 92 Terse output renderer + re-targeted contract oracle | passed | compliant |
| 93 SKILL.md + nudge repurpose + helix setup flip | passed | **compliant** (closed in P96 TD-05) |
| 94 Retire the agent-facing MCP surface (DELETE) | passed | **compliant** (closed in P96 TD-05) |
| 95 Identity & docs rewrite + docgen regen | passed | **compliant** (closed in P96 TD-05) |
| 96 Address v2.0 tech debt | passed | compliant |

## Integration — INTEGRATES CLEANLY (post-Phase-96 re-audit)

`gsd-integration-checker` re-verified all four cross-phase E2E flows WIRED after Phase 96, zero orphaned/missing seams (TD-02 *removed* a genuine orphan, improving the wiring map):
1. spine→verb→profile/mode-enforcement→terse-output over the retained gRPC `StreamMCP` wire (untouched by P96; `verbs_gen.go`/`api/proto/` byte-identical).
2. Frozen verb surface consistent across 3 consumers — `verbs_gen.go` ↔ SKILL.md decision table ↔ docgen README table + CLAUDE.md routing matrix (TD-04 propagated the one Description change through the drift gate, not by hand; README + bench golden regenerated, mutually consistent).
3. CLI is the sole agent surface post-94; loopback-gated gRPC TCP into the identical `mcpServer.SDK()`. TD-01 hardened the *separate* admin/telemetry listener (defense-in-depth), not the `StreamMCP` dispatch.
4. install→steer→use: TD-03 tightened the nudge classifier while preserving fail-open/exit-0; TD-02 left the `helix setup` teardown path intact.

## Tech Debt — RESOLVED

All five items from the first audit are closed (see `resolved_tech_debt` in frontmatter and the change table above). No new tech debt was introduced by Phase 96 (code review: 0 blocker / 0 warning / 2 non-blocking Info within documented fail-open contracts; verifier: 6/6 must-haves).

## Pre-existing / environmental (NOT v2.0 regressions)

- `cmd/helix-bench` `TestRunSubcommandWires*` need `helix` on PATH + a full bench env; they self-skip without `HELIX_BIN` and fail identically on the pre-v2.0 baseline — the v1.12 "live execution honestly gated-skip" deferred item, tracked in STATE.md Deferred Items. Out of scope for v2.0; carried forward on the v1.12 ledger.

## Verdict

v2.0 is **complete and clean** — ready to archive. `go build ./...`, `go vet ./...`, `go test ./...`, and `make verify-docs` are all green; `git diff api/proto/` empty; the frozen 50-verb surface (`verbs_gen.go`) unchanged.
