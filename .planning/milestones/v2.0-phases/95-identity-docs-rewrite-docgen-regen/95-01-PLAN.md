---
phase: 95-identity-docs-rewrite-docgen-regen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/docgen/main.go
  - cmd/docgen/main_test.go
  - Makefile
  - .github/workflows/go-test.yml
  - internal/daemon/imports.go
  - README.md
autonomous: true
requirements: [DOCS-02]
must_haves:
  truths:
    - "The README tool table lists `helix <verb>` names (e.g. `helix go-to-definition`), not raw MCP tool names (`go_to_definition`)."
    - "`go run ./cmd/docgen --check` exits 0 (README table matches the live registry)."
    - "`make verify-docs` runs the docgen drift gate and exits 0."
    - "CI (`.github/workflows/go-test.yml`) contains a `go run ./cmd/docgen --check` step (the v1.12 drift hole is closed)."
    - "`go test ./cmd/docgen/ -count=1` passes with verb-form assertions."
    - "`git diff go.mod` is empty (zero new dependencies)."
  artifacts:
    - path: "cmd/docgen/main.go"
      provides: "Verb-keyed tool-table row emitter (verb = toolName with _→-)"
      contains: "ReplaceAll(tool.Name"
    - path: "Makefile"
      provides: "verify-docs HARD-FAIL drift-gate target"
      contains: "verify-docs"
    - path: ".github/workflows/go-test.yml"
      provides: "CI docgen drift-gate step"
      contains: "cmd/docgen --check"
    - path: "cmd/docgen/main_test.go"
      provides: "Verb-form table assertions"
      contains: "helix go-to-definition"
  key_links:
    - from: "cmd/docgen/main.go"
      to: "README.md"
      via: "replaceSection between <!-- BEGIN/END TOOLS --> markers writes verb-keyed rows"
      pattern: "BEGIN TOOLS"
    - from: ".github/workflows/go-test.yml"
      to: "cmd/docgen/main.go"
      via: "CI step invokes `go run ./cmd/docgen --check` as a hard-fail drift gate"
      pattern: "docgen --check"
---

<objective>
Re-key the auto-generated README tool table from raw MCP tool names (`go_to_definition`) to `helix` CLI verbs (`helix go-to-definition`), then close the v1.12 docgen-drift hole by wiring a `docgen --check` gate into CI and a `make verify-docs` target — mirroring the established `verify-cligen` pattern. Satisfies DOCS-02.

Purpose: DOCS-02's acceptance is "the generated table lists `helix` verbs AND the docgen drift gate is green." The gate does NOT exist in CI today (RESEARCH finding 1 — the only `docgen` token in `go-test.yml` is a comment); a green-locally gate that never runs cannot satisfy the acceptance. This plan ships both the verb re-key and the real enforcement.

Output: a verb-keyed README tool table, a `make verify-docs` target, a CI `docgen --check` step, verb-form docgen tests, and cross-reference comments reconciling docgen ↔ daemon blank imports (tool-set parity via the gate, NOT literal import-list equality — health/help are non-blank in the daemon by design; D-02 forbids importing semantic/extract).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-identity-docs-rewrite-docgen-regen/95-RESEARCH.md
@.planning/phases/95-identity-docs-rewrite-docgen-regen/95-PATTERNS.md

# Interface context (read these before editing):
@cmd/docgen/main.go          # the generator — only the row format at main.go:116 changes
@cmd/docgen/main_test.go     # existing assertions to update (main_test.go:72-88)
@Makefile                    # verify-cligen at Makefile:80 — the analog to mirror
@.github/workflows/go-test.yml  # cligen step at go-test.yml:102-106 — the analog to mirror
@internal/daemon/imports.go  # the daemon blank-import reference set + D-02 invariant
@internal/cli/verb.go        # VerbToolNames() seam at verb.go:81 (frozen-surface enumerator)
</context>

<artifacts_this_phase_produces>
NEW in this plan:
- `make verify-docs` — HARD-FAIL drift-gate target (Makefile)
- CI step "docgen drift gate (DOCS-02)" — `go run ./cmd/docgen --check` (.github/workflows/go-test.yml)
- Verb-keyed README tool table (regenerated, between `<!-- BEGIN/END TOOLS -->`)
- Cross-reference comments in `cmd/docgen/main.go` and `internal/daemon/imports.go` documenting the blank-import parity rule
MODIFIED:
- `cmd/docgen/main.go` row emitter (verb = toolName with `_`→`-`)
- `cmd/docgen/main_test.go` assertions (raw tool names → `helix <verb>` forms)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-key the docgen tool table to `helix <verb>` (RED→GREEN)</name>
  <files>cmd/docgen/main.go, cmd/docgen/main_test.go</files>
  <read_first>
    - cmd/docgen/main.go:99-121 (generateToolTable — the row emitter at line 116 is the ONLY line that changes)
    - cmd/docgen/main.go:43-56, 58-70 (TestGenerateToolTable / TestGenerateLanguageTable row-count filters — must NOT break)
    - cmd/docgen/main_test.go:72-88 (TestToolTableContainsKnownTools — the raw-tool-name asserts to flip)
    - 95-PATTERNS.md:78-108 ("`cmd/docgen/main.go` — re-key tool table to `helix <verb>`") — the exact re-key snippet + the proven `verb == toolName.replace("_","-")` mechanical mapping
    - 95-PATTERNS.md:112-149 ("`cmd/docgen/main_test.go` — verb-form assertions") — the exact knownVerbs list to assert
  </read_first>
  <behavior>
    - RED first: update `cmd/docgen/main_test.go` TestToolTableContainsKnownTools to assert verb forms — replace the seven raw tool names with `helix go-to-definition`, `helix replace-symbol-body`, `helix read-file`, `helix get-diagnostics`, `helix write-memory`, `helix onboard-project`, `helix switch-mode`. Run the test; it MUST fail (table still emits underscored names).
    - GREEN: change the `generateToolTable` row emitter so each row prints the verb instead of the raw tool name, deriving `verb := strings.ReplaceAll(tool.Name, "_", "-")` and formatting as `| \`helix <verb>\` | <category> | <desc> |`.
    - Keep the table HEADER as `| Tool | Category | Description |` (do NOT rename to `Verb`): TestGenerateToolTable's row-count filter (main_test.go:49) skips lines containing the literal `Tool`; renaming the header would make the header counted as a data row and is an unnecessary blast radius. The verb names live in the rows, which is what DOCS-02 requires.
    - Leave intact: TestGenerateToolTable (≥30 rows), TestGenerateLanguageTable (≥40 rows), the three TestReplaceSection* tests — all remain valid after re-keying (they count rows / test marker logic, not tool-name strings).
  </behavior>
  <action>
    Per 95-PATTERNS.md:99-103, replace the row-emitter line in `generateToolTable` (cmd/docgen/main.go:116) so it derives the kebab verb via `strings.ReplaceAll(tool.Name, "_", "-")` and emits `| \`helix <verb>\` | <category> | <desc> |`. This mapping is proven mechanical for all 50 verbs (zero mismatches — RESEARCH §"Deriving the verb name"). Do NOT add an `internal/cli` import to docgen — the inline string transform is the chosen approach (RESEARCH Alternatives table; the seam alternative adds a package dependency for no gain here). First flip the test assertions to verb forms (RED), then make the emitter change (GREEN). Do NOT touch the `--check` mechanism (main.go:69-76) — it is already correct and is what the new CI step invokes. Do NOT rename the table header.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/helix && go test ./cmd/docgen/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/docgen/ -run TestToolTableContainsKnownTools -count=1` passes (verb-form assertions green).
    - `go test ./cmd/docgen/ -count=1` passes (TestGenerateToolTable, TestGenerateLanguageTable, TestReplaceSection* all still green).
    - `grep -n 'ReplaceAll(tool.Name' cmd/docgen/main.go` matches (verb derivation present).
    - `grep -c 'helix go-to-definition' cmd/docgen/main_test.go` ≥ 1.
  </acceptance_criteria>
  <done>The docgen row emitter produces `helix <verb>` table rows; docgen tests assert verb forms and pass; the header and row-count tests are unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: Add the `make verify-docs` target and the CI `docgen --check` drift gate</name>
  <files>Makefile, .github/workflows/go-test.yml</files>
  <read_first>
    - Makefile:1 (.PHONY line — verify-docs must be added to it)
    - Makefile:77-81 (the `docs` regen target + `verify-cligen` HARD-FAIL gate — the exact analog to mirror)
    - .github/workflows/go-test.yml:99-106 (the `go test` step + the `helix-cligen drift gate (VERB-02)` step — insert the docgen sibling adjacent so the two gates read as a pair)
    - 95-PATTERNS.md:28-74 ("Makefile — add verify-docs" and "go-test.yml — add docgen --check step") — the exact target body + CI step YAML to paste
  </read_first>
  <action>
    Per 95-PATTERNS.md:42-45, add a `verify-docs` target to the Makefile as a sibling of `verify-cligen` (Makefile:80): same `$(GO) run ./cmd/<tool> --check` shape, same `## ` help comment style, body `$(GO) run ./cmd/docgen --check`, help text noting it is the DOCS-02 README-table drift gate. Add `verify-docs` to the `.PHONY` declaration (Makefile:1). Reuse the existing `docs` target (Makefile:77) for regeneration — do NOT add a second regen target. Per 95-PATTERNS.md:67-74, add a CI step named "docgen drift gate (DOCS-02)" to `.github/workflows/go-test.yml` immediately after the cligen step (after line 106), mirroring its name/comment/run shape: a comment explaining it HARD-FAILs on a stale README table (closing the v1.12 docgen-drift hole) with the regenerate hint `go run ./cmd/docgen`, and `run: go run ./cmd/docgen --check`. This is the load-bearing DOCS-02 deliverable (RESEARCH CRITICAL FINDING — the gate does not exist in CI today).
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/helix && make verify-docs && grep -q 'cmd/docgen --check' .github/workflows/go-test.yml && grep -q '^verify-docs:' Makefile && echo GATE_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-docs` exits 0 (the new target runs `go run ./cmd/docgen --check` and the README is currently up to date — run AFTER Task 3 regen if the table is stale).
    - `grep -c 'cmd/docgen --check' .github/workflows/go-test.yml` ≥ 1 (CI step present).
    - `grep -c '^verify-docs:' Makefile` == 1 (target present, properly anchored).
    - `grep -c 'verify-docs' Makefile` ≥ 2 (target + .PHONY entry).
  </acceptance_criteria>
  <done>`make verify-docs` is a HARD-FAIL drift gate mirroring `verify-cligen`; CI runs `go run ./cmd/docgen --check` as a sibling of the cligen gate.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the README table and add blank-import cross-ref comments</name>
  <files>README.md, cmd/docgen/main.go, internal/daemon/imports.go</files>
  <read_first>
    - cmd/docgen/main.go:21-36 (docgen blank-import block — add a cross-ref comment here)
    - internal/daemon/imports.go:1-27 (daemon blank-import block + the existing D-02 invariant comment — add the reciprocal cross-ref here)
    - 95-PATTERNS.md:153-171 ("blank-imports ↔ imports.go") — the divergence analysis (health/help non-blank in daemon; guardrails table-neutral; D-02 forbids semantic/extract) and the Option-B cross-ref guidance
    - 95-RESEARCH.md:98-122 (blank-import parity analysis) — why literal equality is the WRONG fix and the gate is the real protection
  </read_first>
  <action>
    Run `make docs` (or `go run ./cmd/docgen`) to regenerate the README tool table between `<!-- BEGIN/END TOOLS -->` with the new verb-keyed rows. NEVER hand-edit the table between the markers (CLAUDE.md "do not hand-edit the tool table"; the new `--check` gate will trip on manual edits). The table will keep its 51 rows for 50 unique tools (`analyze_blast_radius` is registered under two categories — `symbol-retrieval` AND `symbols`); leave that honest dual-row as-is, do NOT dedupe. Then add reciprocal cross-reference comments per 95-PATTERNS.md:167-169: in `cmd/docgen/main.go` near the blank-import block (around line 21) and in `internal/daemon/imports.go` near its blank-import block, each stating the rule "tool-bearing init()-registered providers must appear in both docgen and the daemon; health/help are non-blank in the daemon by design (their RegisterTools also fires init()); the docgen --check CI gate is the real anti-drift protection — literal import-list equality is NOT required; never blank-import internal/semantic/extract/* per D-02." Do NOT add `health`/`help` blank imports to the daemon, do NOT remove them from docgen, and do NOT add any `internal/semantic/extract/*` import to either file (D-02 hard invariant — a second GrammarRegistry would break the singleton).
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/helix && go run ./cmd/docgen --check && grep -q 'helix go-to-definition' README.md && go build ./... && git diff --quiet go.mod && echo REGEN_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `go run ./cmd/docgen --check` prints "README.md is up to date." and exits 0.
    - `grep -c 'helix go-to-definition' README.md` ≥ 1 AND `grep -c '| \`helix ' README.md` ≥ 50 (verb-keyed rows present).
    - `grep -c 'go_to_definition' README.md` == 0 within the tool table region (raw tool names gone from the table). [Note: verify by inspecting the BEGIN/END TOOLS block; underscored tool names may legitimately appear elsewhere in README prose — scope the check to the table.]
    - `go build ./...` succeeds (no unused-import / compile regression from the comment edits).
    - `git diff go.mod` is empty (zero new deps).
    - Both `cmd/docgen/main.go` and `internal/daemon/imports.go` contain the reciprocal cross-ref comment (grep for "docgen --check CI gate" or the agreed marker phrase in each).
  </acceptance_criteria>
  <done>The README table is regenerated to verb forms and `--check` is green; both blank-import files carry reciprocal cross-ref comments documenting the parity-via-gate rule and the D-02 exclusion; the build is clean and go.mod is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This is a docs/tooling phase. `cmd/docgen` reads in-repo Go source only; no untrusted input, no network, no runtime code path, no data/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering | README tool table drift (stale table merged silently — the v1.12 recurrence class) | mitigate | This plan's load-bearing deliverable: the new `go run ./cmd/docgen --check` CI step HARD-FAILs any drift, regardless of blank-import symmetry (Task 2). |
| T-95-SC | Tampering | npm/pip/cargo/go installs | accept | Zero new dependencies — no package installs in this phase. Verified by `git diff go.mod` empty (Task 3 acceptance). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `go test ./cmd/docgen/ ./internal/cli/ -count=1` — docgen + verb-surface tests green.
- `go run ./cmd/docgen --check` — exit 0 (README table matches live registry).
- `make verify-docs` — exit 0 (the new gate runs).
- `grep -q 'cmd/docgen --check' .github/workflows/go-test.yml` — CI step present.
- `go vet ./... && go build ./...` — no regression.
- `git diff go.mod` — empty (zero new deps).
</verification>

<success_criteria>
DOCS-02 satisfied: the generated README tool table lists `helix <verb>` names; the docgen drift gate is green AND wired into CI (`make verify-docs` + the `go run ./cmd/docgen --check` step); docgen ↔ daemon blank-import parity is reconciled via the gate plus reciprocal cross-ref comments (NOT literal import equality), with the D-02 semantic/extract exclusion preserved; zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/95-identity-docs-rewrite-docgen-regen/95-01-SUMMARY.md` when done.
</output>
