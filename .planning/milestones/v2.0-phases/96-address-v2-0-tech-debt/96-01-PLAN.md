---
phase: 96-address-v2-0-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/daemon/telemetry.go
  - internal/daemon/telemetry_test.go
  - internal/cli/setup_clients.go
  - internal/cli/setup_test.go
  - internal/cli/nudge.go
  - internal/cli/nudge_test.go
  - internal/kernel/help/tools.go
  - internal/kernel/help/skill_adapter.go
  - README.md
autonomous: true
requirements: [TD-01, TD-02, TD-03, TD-04]
tags: [tech-debt, go, daemon, cli, docgen, security-hardening]

must_haves:
  truths:
    - "validateAdminAddr refuses empty-host/wildcard binds (:9090, :0, [::]:9090) and still accepts explicit loopback (127.0.0.1, ::1, localhost)"
    - "validateAdminAddr still errors on the empty-addr string (\"\") — the empty-addr no-op stays the caller's job in listenAdmin"
    - "mergeJSONConfig and its four tests are gone; removeFromJSONConfig and the rest of setup_clients.go are untouched"
    - "classifyBashTarget no longer counts a grep-family leading PATTERN (grep foo.go) as a file operand; cat/sed/find behavior is unchanged; the classifier stays fail-open (never non-zero exit)"
    - "the README get-tool-help row no longer says 'any MCP tool' and was regenerated by docgen, not hand-edited"
    - "go build ./..., go vet ./..., go test ./... are green; make verify-docs is green; api/proto/ has zero diff; internal/cli/verbs_gen.go is unchanged"
  artifacts:
    - path: "internal/daemon/telemetry.go"
      provides: "Hardened validateAdminAddr (empty-host refusal + !ip.IsUnspecified() guard)"
      contains: "validateAdminAddr"
    - path: "internal/daemon/telemetry_test.go"
      provides: "Extended TestValidateAdminAddr table covering wildcard/empty-host rejections"
      contains: "wildcard"
    - path: "internal/cli/setup_clients.go"
      provides: "setup_clients with dead mergeJSONConfig removed, live removeFromJSONConfig retained"
      contains: "removeFromJSONConfig"
    - path: "internal/cli/nudge.go"
      provides: "classifyBashTarget with grep-family leading-pattern skip"
      contains: "classifyBashTarget"
    - path: "internal/cli/nudge_test.go"
      provides: "Extended classifier cases (grep foo.go → no file operand)"
      contains: "classifyBashTarget"
    - path: "internal/kernel/help/skill_adapter.go"
      provides: "Reworded get_tool_help Description (CLI-first, no 'MCP tool') that docgen renders into README"
      contains: "get_tool_help"
    - path: "README.md"
      provides: "Regenerated get-tool-help table row without 'MCP tool' phrasing"
      contains: "get-tool-help"
  key_links:
    - from: "internal/daemon/telemetry.go"
      to: "internal/daemon/grpc_tcp.go"
      via: "validateAdminAddr mirrors validateGRPCAddr CR-01 shape (empty-host refusal + !ip.IsUnspecified())"
      pattern: "IsUnspecified"
    - from: "internal/kernel/help/skill_adapter.go"
      to: "README.md"
      via: "docgen renders HelpSkill.Tools()[].Description into the README tool table (get-tool-help row 332)"
      pattern: "get_tool_help"
---

<objective>
Close the four non-blocking tech-debt items the v2.0 milestone audit surfaced so the milestone archives with zero open items. Each item mirrors an existing in-tree precedent — no novel design:

- **TD-01** (security hardening): harden `validateAdminAddr` (`internal/daemon/telemetry.go:108`) to refuse empty-host/wildcard binds, mirroring the CR-01 fix already in `validateGRPCAddr` (`internal/daemon/grpc_tcp.go:85`).
- **TD-02** (dead-code removal): delete the orphaned `mergeJSONConfig` helper (`internal/cli/setup_clients.go:60`) and its four tests; keep the live sibling `removeFromJSONConfig`.
- **TD-03** (classifier edge case): stop `classifyBashTarget` (`internal/cli/nudge.go:337`) from treating a grep-family leading PATTERN as a file operand; preserve fail-open / exit-0.
- **TD-04** (generated-doc correction): reword the off-message "any MCP tool" `get_tool_help` Description at source and regenerate `README.md` through the docgen drift gate.

Purpose: archive v2.0 with zero open audit items, harden one defense-in-depth listener-bind path, and keep all docs CLI-first and drift-gate-green.

Output: hardened `validateAdminAddr` + extended test table; `mergeJSONConfig` removed; tightened `classifyBashTarget` + new regression cases; reworded help Descriptions + regenerated README row. No change to the frozen 50-verb surface (`internal/cli/verbs_gen.go`), `api/proto/`, or the gRPC `StreamMCP` wire.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-address-v2-0-tech-debt/96-CONTEXT.md
@.planning/phases/96-address-v2-0-tech-debt/96-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TD-01 — Harden validateAdminAddr to refuse empty-host/wildcard binds</name>
  <files>internal/daemon/telemetry.go, internal/daemon/telemetry_test.go</files>
  <read_first>
    - internal/daemon/telemetry.go:34-47 (listenAdmin — the caller; note the upstream `if addr == "" { return nil }` no-op at line 42, which is why the validator must NOT add an empty-addr short-circuit)
    - internal/daemon/telemetry.go:105-121 (validateAdminAddr — the function to harden; current accept-switch wrongly includes "" and the ParseIP branch lacks !ip.IsUnspecified())
    - internal/daemon/grpc_tcp.go:85-109 (validateGRPCAddr — the CR-01 reference shape to mirror: explicit "" refusal case in the switch + `net.ParseIP(host); ip != nil && ip.IsLoopback() && !ip.IsUnspecified()`)
    - internal/daemon/telemetry_test.go:18-49 (TestValidateAdminAddr — the existing table to extend; note the `{name: "empty", addr: "", wantErr: true}` row that MUST stay wantErr:true)
  </read_first>
  <behavior>
    Extend TestValidateAdminAddr RED-first with these new rows (mirroring grpc_tcp_test.go cases), then make them pass:
    - "wildcard_empty_host", addr ":9090"  → wantErr:true (empty host binds all interfaces)
    - "wildcard_empty_host_zero", addr ":0" → wantErr:true
    - "wildcard_v6", addr "[::]:9090"       → wantErr:true (unspecified IPv6)
    Preserve every existing row exactly, especially:
    - "empty", addr ""        → wantErr:true (SplitHostPort fails on "" — DO NOT make this pass via an addr=="" short-circuit)
    - "loopback_v4" 127.0.0.1:9090, "localhost" localhost:0, "loopback_v6" [::1]:9090, "auto_port" 127.0.0.1:0 → wantErr:false
    - "zero_bind" 0.0.0.0:9090, "lan_bind" 192.168.1.5:9090, "dns_host" example.com:9090 → wantErr:true, errHas:"v1.3"
    - "malformed" not-a-host → wantErr:true
  </behavior>
  <action>
    Implements TD-01 (per ROADMAP success criterion 1). In validateAdminAddr (internal/daemon/telemetry.go:108), mirror the CR-01 hardening already present in validateGRPCAddr WITHOUT regressing the admin-specific empty-addr contract:
    1. Move the empty-host case `""` OUT of the accept switch (`case "", "localhost", "127.0.0.1", "::1": return nil`). Replace it with an accept switch that lists only `"localhost", "127.0.0.1", "::1"` and an explicit refusal case for `""` that returns an error explaining an empty host binds all interfaces (an empty host on a `:PORT` addr makes net.Listen bind every interface). Keep the existing trailing `v1.3` auth-roadmap reference in the wildcard/non-loopback error message — the admin path cites the v1.3 auth roadmap, NOT REMOTE-01 (that is the grpc path's wording).
    2. On the `net.ParseIP(host)` branch add the `&& !ip.IsUnspecified()` guard for defense-in-depth parity with validateGRPCAddr, so a parsed `0.0.0.0` / `::` is never accepted even if the explicit branch is later reordered.
    DO NOT add an `if addr == "" { return nil }` early-return: the empty-ADDR no-op is handled upstream in listenAdmin (telemetry.go:42), and the validator must keep erroring on `""` (SplitHostPort fails on it) so the existing `{empty → wantErr:true}` test row stays green. Closing the empty-HOST gap (`":9090"`) is a different concern from the empty-ADDR case (`""`); do not conflate them. Then extend TestValidateAdminAddr per the behavior block. Run gofmt on both files.
  </action>
  <verify>
    <automated>go test ./internal/daemon/ -run TestValidateAdminAddr -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestValidateAdminAddr passes with the three new wildcard/empty-host rows AND every pre-existing row (including `empty → wantErr:true`) green.
    - validateAdminAddr refuses `:9090`, `:0`, `[::]:9090`, `0.0.0.0:9090` and accepts `127.0.0.1:9090`, `[::1]:9090`, `localhost:0`, `127.0.0.1:0`.
    - No `if addr == "" { return nil }` short-circuit was added; the admin error message still references `v1.3`.
    - `go vet ./internal/daemon/` clean.
  </acceptance_criteria>
  <done>validateAdminAddr mirrors validateGRPCAddr's empty-host refusal + !ip.IsUnspecified() guard; the extended TestValidateAdminAddr table is green; the empty-addr contract (errors on "") is preserved.</done>
</task>

<task type="execute">
  <name>Task 2: TD-02 — Remove dead mergeJSONConfig helper and its tests</name>
  <files>internal/cli/setup_clients.go, internal/cli/setup_test.go</files>
  <read_first>
    - internal/cli/setup_clients.go:55-91 (mergeJSONConfig — the dead helper to remove; it has zero non-test callers after the Phase-93 setup flip)
    - internal/cli/setup_clients.go:93-123 (removeFromJSONConfig — the LIVE sibling with ~15 production callers; MUST be kept; it uses the same imports encoding/json, os, fmt, path/filepath, so removing mergeJSONConfig will NOT orphan any import)
    - internal/cli/setup_test.go:17-118 (the `// --- mergeJSONConfig tests ---` block: TestMergeJSONConfigNewFile (line 19), TestMergeJSONConfigExistingFile (43), TestMergeJSONConfigVSCodeServersKey (73), TestMergeJSONConfigPreservesNonMCPKeys (93) — all four to remove)
    - internal/cli/setup_test.go:120 (`// --- removeFromJSONConfig tests ---` — the boundary; STOP before this; everything from line 120 onward stays)
  </read_first>
  <action>
    Implements TD-02 (per ROADMAP success criterion 2). Delete the dead production helper and its tests:
    1. In internal/cli/setup_clients.go remove the entire mergeJSONConfig function plus its doc comment (the block at lines 55-91, from the `// mergeJSONConfig reads ...` comment through the closing brace). Do NOT touch removeFromJSONConfig (lines 93-123) or anything else in the file — removeFromJSONConfig is the live teardown path with ~15 callers.
    2. In internal/cli/setup_test.go remove the four mergeJSONConfig test functions and their `// --- mergeJSONConfig tests ---` section header (lines 17-118): TestMergeJSONConfigNewFile, TestMergeJSONConfigExistingFile, TestMergeJSONConfigVSCodeServersKey, TestMergeJSONConfigPreservesNonMCPKeys. STOP at the `// --- removeFromJSONConfig tests ---` marker (line 120) — keep TestRemoveFromJSONConfigExists and everything after it.
    Do NOT prune imports by hand in either file — removeFromJSONConfig and the surviving tests keep the same import set alive; let `go build`/`go vet` confirm. If go vet reports an unused import in setup_test.go after the deletion, remove only that specific import. Run gofmt on both files.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/cli/ && go test ./internal/cli/ -count=1 && [ "$(grep -rc 'func mergeJSONConfig' internal/cli/setup_clients.go)" = "0" ] && grep -q 'func removeFromJSONConfig' internal/cli/setup_clients.go && echo TD-02-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'mergeJSONConfig' internal/cli/` returns zero matches (definition + comment + all four tests gone).
    - `removeFromJSONConfig` is still present in setup_clients.go and still has its ~15 callers (untouched).
    - `go build ./...`, `go vet ./internal/cli/`, and `go test ./internal/cli/` are all green.
    - No production behavior change to `helix setup`.
  </acceptance_criteria>
  <done>mergeJSONConfig and its four tests are removed; removeFromJSONConfig and the rest of setup_clients.go are unchanged; build/vet/test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TD-03 — Stop classifyBashTarget counting a grep-family PATTERN as a file operand</name>
  <files>internal/cli/nudge.go, internal/cli/nudge_test.go</files>
  <read_first>
    - internal/cli/nudge.go:321-406 (classifyBashTarget — the function; note the command switch at line 344-349 listing grep/rg/ag/sed/cat/find/egrep/fgrep, and the operand loop at 355-394 that currently classifies ANY token with a code extension, including the leading pattern, as a code operand)
    - internal/cli/nudge.go:158-175 (steerMessage — the caller; the hook emits nothing on `!ok || !isCode` and exits 0 regardless; the fail-open contract MUST be preserved — never cause a non-zero exit)
    - internal/cli/nudge_test.go:174-249 (the existing classifier tests: TestClassifyBashTarget_CodeTargets, _NonCodeTargets, _FailOpen, _MixedTargetsConservative, _PureDataNoExec — all must stay green; note _CodeTargets uses patterns like `"func Foo"`/`"Bar("` with no code extension, so the pattern-skip will not break them)
  </read_first>
  <behavior>
    Extend the classifier tests RED-first (add a new test func, e.g. TestClassifyBashTarget_GrepPatternNotFile, in the existing assert/testify style), then make them pass. New expectations:
    - `grep foo.go`            → (isCode=false, ok=false)  — pattern only, no file operand → fail open (was wrongly (true,true) before the fix)
    - `grep foo.go bar.go`     → (isCode=true, ok=true)    — foo.go is the skipped pattern, bar.go is the code file
    - `grep -i foo.go util.go` → (isCode=true, ok=true)    — flag, then pattern foo.go, then file util.go
    - `rg pattern.go`          → (isCode=false, ok=false)  — rg pattern only
    - `cat main.go`            → (isCode=true, ok=true)    — control: cat has NO leading pattern; first operand IS the file
    Preserve ALL existing cases, especially TestClassifyBashTarget_CodeTargets (`grep "func Foo" main.go` stays (true,true)), TestClassifyBashTarget_MixedTargetsConservative (`grep x main.go README.md` stays (false,true)), and TestClassifyBashTarget_FailOpen.
  </behavior>
  <action>
    Implements TD-03 (per ROADMAP success criterion 3). In classifyBashTarget (internal/cli/nudge.go:337), skip the leading PATTERN argument for the grep family ONLY:
    1. Define the grep-family set as grep, rg, ag, egrep, fgrep (NOT cat, sed, find — those lead with a file/expr, not a pattern). For those commands, the FIRST non-flag, non-option token after the command is the search pattern and must NOT be classified as a file operand; the remaining non-flag tokens are the real file operands.
    2. Implement minimally: walk fields[1:] as today, but for a grep-family command, once you reach the first non-flag token (after skipping leading `-`-prefixed flags), treat that single token as the pattern (skip it from operand classification) and continue classifying the rest. Keep the existing `-`-prefixed flag skip and the residual-quote stripping. Do NOT attempt to consume `-e`/`-f` flag VALUES — the existing doc comment at nudge.go:357-358 already deems that acceptable; keep it simple: the first non-flag token is the pattern.
    3. Leave cat/sed/find on the current behavior (no leading pattern to skip) — their first operand is a file/expr.
    4. Preserve the fail-open contract exactly: the function still returns (false,false) when no file operand is positively identified; it must never panic, exec, or cause a non-zero exit. Tokenization stays a bounded strings.Fields split — no regex backtracking on attacker input (T-93-05 DATA-only invariant).
    Then add the new test func per the behavior block and confirm all pre-existing classifier tests stay green. Run gofmt on both files.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run TestClassifyBashTarget -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep foo.go` → (false,false); `grep foo.go bar.go` → (true,true); `grep -i foo.go util.go` → (true,true); `rg pattern.go` → (false,false); `cat main.go` → (true,true).
    - All pre-existing TestClassifyBashTarget_* tests still pass (CodeTargets, NonCodeTargets, FailOpen, MixedTargetsConservative, PureDataNoExec).
    - classifyBashTarget never returns a non-zero-exit signal and contains no os/exec or regex backtracking; the pattern-skip is gated to the grep family only (cat/sed/find unchanged).
    - `go vet ./internal/cli/` clean.
  </acceptance_criteria>
  <done>classifyBashTarget skips the leading grep-family pattern; the new and existing classifier tests are green; fail-open / DATA-only contract preserved.</done>
</task>

<task type="execute">
  <name>Task 4: TD-04 — Reword get_tool_help Descriptions at source and regenerate README</name>
  <files>internal/kernel/help/skill_adapter.go, internal/kernel/help/tools.go, README.md</files>
  <read_first>
    - internal/kernel/help/skill_adapter.go:29-33 (HelpSkill.Tools() — line 31 is the README-DRIVING field: docgen renders this `Description` into README:332; the `BriefDescription` here is also off-message)
    - internal/kernel/help/tools.go:25-27 (mcpsdk.Tool.Description — runtime MCP registration, off-message)
    - internal/kernel/help/tools.go:79-82 (registry Register ToolDef.Description — off-message; BriefDescription here is already fine)
    - README.md:332 (the generated get-tool-help row that must change — DO NOT hand-edit; it is regenerated by docgen)
    - cmd/docgen/main.go:108-119 (docgen reads skill.ToolProviders() → tp.Tools()[].Description; the `help` package is already blank-imported at main.go:36, so NO import change is needed)
    - Makefile:77-84 (docs target = `go run ./cmd/docgen`; verify-docs target = `go run ./cmd/docgen --check`, the HARD-FAIL drift gate)
  </read_first>
  <action>
    Implements TD-04 (per ROADMAP success criterion 4). Reword the off-message "any MCP tool" phrasing at SOURCE for CLI-first identity, then regenerate the README — NEVER hand-edit README.md (drift gate). There are four occurrences to reword consistently:
    1. internal/kernel/help/skill_adapter.go:31 `Description` — change "...documentation for any MCP tool including parameters, types, and usage examples" to drop "MCP tool" in favor of CLI-first wording (e.g. "any Helix tool"). THIS is the field docgen renders into README:332 — it MUST change or the README row will not update.
    2. internal/kernel/help/skill_adapter.go:31 `BriefDescription` — change "...usage documentation for an MCP tool" to the matching CLI-first wording (e.g. "a Helix tool").
    3. internal/kernel/help/tools.go:27 `mcpsdk.Tool.Description` — same reword (runtime MCP metadata, kept consistent to avoid future drift).
    4. internal/kernel/help/tools.go:81 registry `ToolDef.Description` — same reword.
    Keep all four wordings identical to avoid future drift confusion. Do NOT touch cmd/docgen/main.go's blank-import list (the `help` package is already imported; touching it risks the v1.12 docgen-drift regression). Do NOT touch internal/cli/verbs_gen.go (the frozen verb `get-tool-help` entry is unrelated to the Description string). After the source edits, regenerate and verify the drift gate:
      `make docs`         (rewrites the README tool table from the live registry)
      `make verify-docs`  (docgen --check; MUST exit 0 with "README.md is up to date.")
    The only README diff should be the single get-tool-help row text. Run gofmt on the two Go files.
  </action>
  <verify>
    <automated>make docs && make verify-docs && [ "$(grep -c 'any MCP tool' README.md)" = "0" ] && [ "$(grep -hn 'any MCP tool' internal/kernel/help/skill_adapter.go internal/kernel/help/tools.go | grep -v '^#' | grep -c 'any MCP tool')" = "0" ] && echo TD-04-OK
  </verify>
  <acceptance_criteria>
    - The four Description/BriefDescription literals in internal/kernel/help/ (skill_adapter.go:31 Description + BriefDescription, tools.go:27, tools.go:81) are reworded CLI-first and identical to each other; `grep -rn 'any MCP tool' internal/kernel/help/*.go` returns zero matches (only the four user/agent-visible metadata literals are reworded — code comments at tools.go:21/94/103, the help-body text at tools.go:85 ("any registered MCP tool"), and the test/docs files are intentionally left untouched and out of TD-04 scope).
    - README.md was regenerated by docgen (not hand-edited); `make verify-docs` exits 0 ("README.md is up to date."); the only README diff is the get-tool-help row text and it no longer says "MCP tool".
    - cmd/docgen/main.go blank-import list and internal/cli/verbs_gen.go are unchanged.
    - `go build ./... && go vet ./internal/kernel/help/` clean.
  </acceptance_criteria>
  <done>get_tool_help Descriptions are reworded CLI-first at all four source sites; README is regenerated through the drift gate (green); no blank-import or frozen-verb change.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

**Modified functions / fields:**
- `validateAdminAddr` (`internal/daemon/telemetry.go`) — empty-host refusal moved out of accept switch + `!ip.IsUnspecified()` guard added (TD-01).
- `classifyBashTarget` (`internal/cli/nudge.go`) — grep-family leading-pattern skip (TD-03).
- `HelpSkill.Tools()[].Description` and `.BriefDescription` (`internal/kernel/help/skill_adapter.go:31`) — CLI-first reword (TD-04, README-driving).
- `mcpsdk.Tool.Description` (`internal/kernel/help/tools.go:27`) and registry `ToolDef.Description` (`tools.go:81`) — CLI-first reword (TD-04).

**New test functions:**
- 3 new rows in `TestValidateAdminAddr` (`internal/daemon/telemetry_test.go`): wildcard_empty_host, wildcard_empty_host_zero, wildcard_v6 (TD-01).
- New classifier test func (e.g. `TestClassifyBashTarget_GrepPatternNotFile`, `internal/cli/nudge_test.go`) covering grep foo.go / grep foo.go bar.go / grep -i foo.go util.go / rg pattern.go / cat main.go (TD-03).

**Removed symbols:**
- `mergeJSONConfig` function (`internal/cli/setup_clients.go`) and its four tests in `setup_test.go`: TestMergeJSONConfigNewFile, TestMergeJSONConfigExistingFile, TestMergeJSONConfigVSCodeServersKey, TestMergeJSONConfigPreservesNonMCPKeys (TD-02).

**Regenerated artifact:**
- `README.md` get-tool-help row (line ~332) regenerated by docgen with the reworded Description string (TD-04).

**Explicitly unchanged (invariants):** `internal/cli/verbs_gen.go` (frozen 50-verb surface), `api/proto/` (zero diff), `removeFromJSONConfig` (live teardown path), `cmd/docgen/main.go` blank-import list, the gRPC `StreamMCP` wire.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → daemon admin listener | The optional admin HTTP listener (`/healthz`, `/readyz`, `/metrics`, opt-in pprof) is reachable by anything that can connect to its bound address; an empty-host bind (`:9090`) exposes it to the LAN (TD-01 boundary). |
| agent Bash command string → PreToolUse advisory hook | `classifyBashTarget` reads an arbitrary, possibly hostile command STRING as DATA to decide whether to emit a nudge — it must never execute or regex-backtrack it (TD-03 boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Information Disclosure | `validateAdminAddr` (admin instrumentation listener) | mitigate | TD-01: refuse empty-host (`:9090`/`:0`/`[::]:9090`) and add `!ip.IsUnspecified()` guard so the admin listener (`/metrics`, opt-in pprof) never binds `0.0.0.0`/`::` by default — mirrors `validateGRPCAddr` CR-01. Severity LOWER than CR-01 (admin exposes instrumentation only, not the tool surface) → below the `high` block threshold; documented, non-blocking. |
| T-96-02 | Tampering / DoS | `classifyBashTarget` advisory hook | accept (already mitigated; must not regress) | TD-03 only NARROWS what counts as a file operand. The command string is tokenized as DATA via a bounded `strings.Fields` split — no `os/exec`, no regex backtracking (T-93-05). The fix preserves the fail-open / exit-0 contract: no new threat introduced. |
| T-96-03 | — | TD-02 dead-code removal | accept (no new threat) | Pure deletion of an unreferenced helper + its tests; removes attack surface rather than adding it. `removeFromJSONConfig` (live) is untouched. |
| T-96-04 | — | TD-04 doc reword + docgen regen | accept (no new threat) | Edits string literals + regenerates a generated README row; no code path, network surface, or input handling changes. |
| T-96-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RESEARCH "Package Legitimacy Audit: Not applicable"). No `[ASSUMED]`/`[SUS]` packages; no install task. |
</threat_model>

<verification>
## Per-task (sub-second targeted)
- TD-01: `go test ./internal/daemon/ -run TestValidateAdminAddr -count=1`
- TD-02: `go build ./... && go vet ./internal/cli/ && go test ./internal/cli/ -count=1`
- TD-03: `go test ./internal/cli/ -run TestClassifyBashTarget -count=1`
- TD-04: `make docs && make verify-docs`

## Wave merge
- `go build ./... && go vet ./... && go test ./internal/cli/... ./internal/daemon/... ./internal/kernel/help/... -count=1`
- `make verify-docs` (drift gate green)

## Phase gate
- `go build ./...` green
- `go vet ./...` green
- `go test ./...` green (pre-existing env-gated `cmd/helix-bench` `TestRunSubcommandWires*` failures are OUT OF SCOPE — carried from v1.12, not a v2.0 regression)
- `make verify-docs` green
- `git diff --stat api/proto/` empty (zero-proto invariant)
- `internal/cli/verbs_gen.go` unchanged (frozen 50-verb surface)
</verification>

<success_criteria>
- TD-01: `validateAdminAddr` refuses `:9090`/`:0`/`[::]:9090` and accepts explicit loopback; extended `TestValidateAdminAddr` green; empty-addr (`""`) still errors (no short-circuit added).
- TD-02: `mergeJSONConfig` + its four tests removed; `removeFromJSONConfig` intact; build/vet/test green.
- TD-03: grep-family leading PATTERN no longer counted as a file operand; new + existing classifier tests green; fail-open / DATA-only contract preserved.
- TD-04: all four "MCP tool" occurrences reworded CLI-first; README regenerated via docgen; `make verify-docs` green.
- Phase gate: `go build ./...`, `go vet ./...`, `go test ./...`, `make verify-docs` all green; `api/proto/` zero diff; `internal/cli/verbs_gen.go` unchanged.
</success_criteria>

<output>
Create `.planning/phases/96-address-v2-0-tech-debt/96-01-SUMMARY.md` when done.
</output>
