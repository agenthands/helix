---
phase: 100-polyglot-edit-benchmark-committed-baseline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bench/datasets/aider-polyglot/loader.go
  - bench/datasets/aider-polyglot/loader_export_test.go
  - bench/runtime/result.go
  - bench/runtime/result_test.go
  - bench/runtime/aider_edit_agent.go
  - bench/runtime/aider_edit_agent_test.go
  - bench/runners/aider_edit/MODE.md
  - bench/runners/mode_resolver_test.go
autonomous: true
requirements:
  - EDITBENCH-01
  - EDITBENCH-02
  - EDITBENCH-03

user_setup: []

must_haves:
  truths:
    - "A deterministic EDIT-verb AgentFn applies the exercism reference solution (files.example) to the solution stub through the helix replace_in_file verb against the warm daemon"
    - "RunExercise is called verbatim with the new AgentFn; the loader is untouched and the WR-01 pristine-test restore still runs"
    - "bench/runners/aider_edit/MODE.md resolves to profile bench-full with zero mode-resolver Go change"
    - "result.v2 carries an additive edit_format_applied *bool open key; a literal false survives marshalling, a nil drops the key, schema_version stays v2"
  artifacts:
    - path: "bench/runtime/aider_edit_agent.go"
      provides: "newDeterministicEditAgent (daemon-dialing AgentFn) + newNativeTestFn (live TestFn) + aiderEditMode const"
      min_lines: 60
    - path: "bench/datasets/aider-polyglot/loader.go"
      provides: "exported LoadExercise + NativeTestCommand thin wrappers (pure-stdlib, leaf boundary preserved)"
      contains: "func LoadExercise"
    - path: "bench/runtime/result.go"
      provides: "EditFormatApplied *bool on ResultInput and resultDoc, wired through BuildResult"
      contains: "edit_format_applied"
    - path: "bench/runners/aider_edit/MODE.md"
      provides: "filesystem-as-table mode entry (mode: aider_edit / profile: bench-full)"
      contains: "aider_edit"
  key_links:
    - from: "bench/runtime/aider_edit_agent.go"
      to: "internal/forwarder"
      via: "forwarder.OpenSession + session.CallTool dialing the daemon's gRPC StreamMCP wire"
      pattern: "forwarder\\.OpenSession"
    - from: "bench/runtime/aider_edit_agent.go"
      to: "bench/datasets/aider-polyglot/loader.go"
      via: "implements aiderpolyglot.AgentFn signature passed to RunExercise"
      pattern: "aiderpolyglot\\.AgentFn"
    - from: "bench/runtime/result.go"
      to: "bench/runtime/result_test.go"
      via: "EditFormatApplied *bool round-trips through BuildResult and validates"
      pattern: "EditFormatApplied"
---

<objective>
Land the EDITBENCH wiring substrate for Phase 100: (1) thin exported loader
accessors (`LoadExercise` / `NativeTestCommand`) so the live runner can load a
fixture exercise and get the native-test argv without breaking the
stdlib-only leaf-import boundary; (2) the deterministic daemon-dialing
EDIT-verb `AgentFn` plus a live `TestFn`, both in `bench/runtime`; (3) the
additive `edit_format_applied *bool` open key on `result.v2` mirroring
`swebench_raw_resolved` EXACTLY (per D-EDITBENCH-03); and (4) the
`bench/runners/aider_edit/MODE.md` filesystem-table mode entry.

Purpose: These are the five reusable pieces the Plan 02 cell branch and
committed baseline depend on. This plan deliberately stops BEFORE the
`runAiderEditCell` cell branch and the committed report — those are Plan 02 —
so each plan stays inside its context budget and the AgentFn/result-key/MODE.md
substrate is proven hermetically first.

Output: `aider_edit_agent.go`, exported loader wrappers, the
`edit_format_applied` field, `MODE.md`, and their hermetic tests. No live
binary, no network — every test here is hermetic (the daemon-dial path is
exercised end-to-end in Plan 02 under HELIX_BIN).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-polyglot-edit-benchmark-committed-baseline/100-RESEARCH.md
@.planning/phases/100-polyglot-edit-benchmark-committed-baseline/100-PATTERNS.md

# Loader leaf — REUSED VERBATIM; only ADD thin exported wrappers (do NOT touch RunExercise/WR-01)
@bench/datasets/aider-polyglot/loader.go

# Daemon-dial precedent for the AgentFn (OpenSession + activate_project + per-step CallTool)
@bench/runtime/drive.go

# The additive open-key byte-template (swebench_raw_resolved *bool)
@bench/runtime/result.go

# Mode-name const + filesystem-table mode precedent
@bench/runtime/rag.go
@bench/runners/mode_resolver.go

# CONFIRMED at plan time — the REAL replace_in_file / fuzzy_edit arg keys (research A1 was WRONG)
@internal/kernel/fileops/tools.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive edit_format_applied *bool open key (EDITBENCH-03)</name>
  <files>bench/runtime/result.go, bench/runtime/result_test.go</files>
  <read_first>
    - bench/runtime/result.go:126-139 (SwebenchRawResolved *bool field + doctrine comment on ResultInput)
    - bench/runtime/result.go:220-229 (resultDoc json tag `swebench_raw_resolved,omitempty`)
    - bench/runtime/result.go:264-287 (BuildResult resultDoc literal where SwebenchRawResolved is wired)
    - PATTERNS.md "bench/runtime/result.go (MODIFY)" — byte-exact template
  </read_first>
  <behavior>
    - Test (round-trip false): BuildResult with EditFormatApplied = ptr(false) marshals an `"edit_format_applied": false` key — a literal false is PRESERVED, not dropped by omitempty.
    - Test (round-trip true): EditFormatApplied = ptr(true) marshals `"edit_format_applied": true`.
    - Test (nil drops key): EditFormatApplied = nil omits the key entirely (no `edit_format_applied` substring in the bytes).
    - Test (schema still valid, no v3): a result with edit_format_applied passes Validate; the marshalled `schema_version` is still "v2" (NOT v3); edit_format_applied is NOT in the schema `required` set.
  </behavior>
  <action>
    Add `EditFormatApplied *bool` to the `ResultInput` struct immediately after the
    `SwebenchRawResolved`/`SwebenchRescoredVerified` fields (per EDITBENCH-03). Add a
    doc comment that names the additive-open-key discipline by concept (pointer type so
    a literal false is preserved; omitempty; schema_version stays v2; additionalProperties
    stays open; not added to required) — mirror the existing SwebenchRawResolved comment
    in shape but describe the edit-format-applied semantics. Add the matching
    `EditFormatApplied *bool` field with json tag `edit_format_applied,omitempty` to the
    `resultDoc` struct after the SwebenchRescoredVerified json-tag field. In the
    `BuildResult` `resultDoc{...}` literal (result.go:264-287), wire
    `EditFormatApplied: in.EditFormatApplied,` alongside the existing SwebenchRawResolved
    wiring. Do NOT bump `resultSchemaVersion`. Do NOT add the key to the schema required
    list. Use a value of `*bool` exactly — a value-type `bool` would drop a load-bearing
    `false` under omitempty.

    In result_test.go add the four behavior cases above. Use a small `func ptrBool(b bool) *bool`
    local helper if one is not already in the package. Marshal the BuildResult bytes and
    assert on the JSON substring presence/absence and on Validate passing.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestEditFormatApplied' -x -count=1</automated>
  </verify>
  <done>EditFormatApplied *bool exists on ResultInput and resultDoc; a literal false marshals present, a nil drops the key; a result carrying it passes Validate with schema_version still "v2"; all four cases green.</done>
  <acceptance_criteria>
    - `grep -n 'edit_format_applied,omitempty' bench/runtime/result.go` returns the resultDoc json tag (exactly mirrors swebench_raw_resolved at result.go:228).
    - `grep -c 'schema_version.*v3\|resultSchemaVersion.*v3' bench/runtime/result.go` is 0 (no v3 bump).
    - `EditFormatApplied: in.EditFormatApplied,` appears in the BuildResult literal (PATTERNS.md result.go MODIFY).
    - `go test ./bench/runtime/ -run 'TestEditFormatApplied' -count=1` passes (RESEARCH Test Map EDITBENCH-03 rows).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Thin exported loader accessors + aider_edit MODE.md (EDITBENCH-02 + loader export)</name>
  <files>bench/datasets/aider-polyglot/loader.go, bench/datasets/aider-polyglot/loader_export_test.go, bench/runners/aider_edit/MODE.md, bench/runners/mode_resolver_test.go</files>
  <read_first>
    - bench/datasets/aider-polyglot/loader.go:96-124 (loadExercise — sets SrcDir, load-bearing for WR-01)
    - bench/datasets/aider-polyglot/loader.go:280-304 (nativeTestCommand — per-language argv table)
    - bench/datasets/aider-polyglot/loader.go:11-15 (leaf import block — stdlib-only; do NOT add a daemon/forwarder import here)
    - bench/runners/mode_resolver.go:35-38,83-119 (ResolveProfileFromRoot strict two-key KnownFields(true) frontmatter)
    - bench/runners/baseline_rag/MODE.md (the closest MODE.md analog)
    - PATTERNS.md "loader.go (MODIFY, thin export wrappers)" + "MODE.md (NEW)"
  </read_first>
  <action>
    In loader.go ADD two thin EXPORTED wrappers that delegate to the existing unexported
    functions — nothing else in the loader changes:
      `func LoadExercise(dir, language string) (*Exercise, error) { return loadExercise(dir, language) }`
      `func NativeTestCommand(language string) ([]string, error) { return nativeTestCommand(language) }`
    These are pure-stdlib accessors; add NO new import. The whole point is to keep the
    daemon-dial logic OUT of this leaf (Plan 02's AgentFn lives in bench/runtime) while
    letting bench/runtime load a fixture exercise and get the native-test argv. `loadExercise`
    sets `SrcDir: dir` (loader.go:120) which is load-bearing — WR-01 restorePristineTests only
    runs when ex.SrcDir != "" (loader.go:243-249), so LoadExercise must return an Exercise with
    SrcDir set (it does, via the delegation).

    Create bench/runners/aider_edit/MODE.md with the strict two-key frontmatter
    `mode: aider_edit` / `profile: bench-full` and a body documenting: this is the polyglot-edit
    bench mode (EDITBENCH-02); it resolves to bench-full (the full helix EDIT-verb surface);
    it is detected BY MODE NAME in RunCell (like baseline_rag) so the cell branches to
    runAiderEditCell in Plan 02; the MODE.md still validates the two-key frontmatter as a side
    effect; mode_resolver.go needs ZERO change. Match the YAML shape of
    bench/runners/baseline_rag/MODE.md exactly (only `mode:` and `profile:` keys — the resolver
    is strict KnownFields(true), so any extra frontmatter key fails the parse).

    In loader_export_test.go (hermetic): assert LoadExercise on the vendored
    `bench/datasets/aider-polyglot/fixtures/go/exercises/practice/wordy` dir (language "go")
    returns an Exercise whose SrcDir is that dir, whose Config.Files.Solution is ["wordy.go"]
    and Config.Files.Example is [".meta/example.go"]; assert NativeTestCommand("go") returns
    {"go","test","./..."} and NativeTestCommand("python") returns {"pytest"}; assert an
    unknown language fail-closes (non-nil error).

    In mode_resolver_test.go ADD a row/subtest TestResolveAiderEdit asserting
    ResolveProfileFromRoot(<runners root>, "aider_edit") resolves to "bench-full" with no error
    (zero Go change to the resolver proves the filesystem-table growth).
  </action>
  <verify>
    <automated>go test ./bench/datasets/aider-polyglot/ -run 'TestLoadExercise|TestNativeTestCommand' -count=1 && go test ./bench/runners/ -run 'TestResolveAiderEdit' -count=1 && go vet ./bench/datasets/aider-polyglot/</automated>
  </verify>
  <done>LoadExercise/NativeTestCommand are exported and delegate; the loader leaf still has no daemon/forwarder import (go vet + vet-ablation-leakage clean); aider_edit/MODE.md resolves to bench-full with zero resolver change; all subtests green.</done>
  <acceptance_criteria>
    - `grep -n 'func LoadExercise\|func NativeTestCommand' bench/datasets/aider-polyglot/loader.go` returns both exported wrappers.
    - `grep -c 'internal/forwarder\|internal/kernel\|bench/runtime' bench/datasets/aider-polyglot/loader.go` is 0 (leaf boundary preserved; PATTERNS.md leaf-boundary rule, STATE.md vet-ablation-leakage constraint).
    - bench/runners/aider_edit/MODE.md frontmatter has exactly the two keys `mode:` and `profile:` (mode_resolver.go:35-38 KnownFields(true)).
    - `go run ./cmd/helix-bench-cligen >/dev/null 2>&1 || true; go test ./bench/runners/ -run TestResolveAiderEdit -count=1` passes (RESEARCH Test Map EDITBENCH-02 row).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic daemon-dialing EDIT-verb AgentFn + live TestFn (EDITBENCH-01)</name>
  <files>bench/runtime/aider_edit_agent.go, bench/runtime/aider_edit_agent_test.go</files>
  <read_first>
    - bench/runtime/drive.go:67-124 (driveScript: OpenSession, activate_project with `repo_path`, per-step CallTool, driveDeadline, the nopWriter/discard slog logger)
    - bench/datasets/aider-polyglot/loader.go:202-204 (AgentFn signature: func(ctx, ex, workDir, prompt) error)
    - bench/datasets/aider-polyglot/loader.go:188-200,230-264 (TestFn/TestResult + the RunExercise 2-attempt loop the AgentFn plugs into)
    - internal/kernel/fileops/tools.go:52-59 (REAL ReplaceInFileArgs: keys are `path`,`pattern`,`replacement`,`is_regex` — NOT relpath/content; research A1 was wrong)
    - internal/kernel/fileops/tools.go:378-471 (replace_in_file requires non-empty `pattern`; replaces ALL occurrences of pattern→replacement)
    - internal/forwarder/session.go:52-96 (OpenSession signature; Session.CallTool / Close)
    - PATTERNS.md "aider_edit_agent.go (NEW)" — the daemon-dial + deterministic-transform contract
    - 100-RESEARCH.md "Pitfall 1" (hermetic golden sibling is the sole authoritative proof) + "Pitfall 4" (WR-01)
  </read_first>
  <behavior>
    - Hermetic test (no HELIX_BIN, no network): RunExercise driven with the deterministic
      AgentFn against a FAKE in-process TestFn (loader_test.go-style scripted tester) and a
      FAKE edit applier (the AgentFn's CallTool seam stubbed) reports Passed and sets
      edit_format_applied=true when the reference body is applied; the loader's 2-attempt
      protocol is exercised end-to-end with NO daemon.
    - Anti-vacuity test (revert-and-fail): when the AgentFn applies a WRONG body (or applies
      nothing), the fake native TestFn reports Passed=false — proving the baseline pass is
      non-vacuous and the test actually grades the applied edit.
    - TestFn test: newNativeTestFn over a tiny throwaway dir builds the per-language argv from
      NativeTestCommand and returns Passed per the command exit (use a trivially-passing and a
      trivially-failing fixture, OR assert argv construction + a fail-closed unknown-language path
      without shelling a real toolchain).
  </behavior>
  <action>
    Create bench/runtime/aider_edit_agent.go in package `runtime` (daemon-dialing is allowed
    here — this is NOT the stdlib leaf). Add:

      `const aiderEditMode = "aider_edit"` — the mode-name const Plan 02's cell branch keys on
      (mirror baselineRagMode in rag.go:37). Keep it here so both the agent file and the Plan 02
      cell file share one const.

      `func newDeterministicEditAgent(sockPath string, applied *bool) aiderpolyglot.AgentFn` —
      returns an AgentFn that: opens ONE session via `forwarder.OpenSession(ctx, sockPath, "", logger, "aider-edit")`
      with a discard slog logger (mirror drive.go:71); `defer sess.Close()`; calls
      `sess.CallTool(ctx, "activate_project", map[string]any{"repo_path": workDir})` to point the
      daemon workspace at the cloned exercise repo (drive.go:87 uses `repo_path`); then for each
      i over `ex.Config.Files.Solution`, reads the reference body from
      `filepath.Join(ex.SrcDir, ex.Config.Files.Example[i])` and applies it to the stub via
      `replace_in_file`. CRITICAL — the REAL replace_in_file does pattern→replacement substitution
      (it has NO whole-file `content` key; confirmed internal/kernel/fileops/tools.go:52-59). To get
      a deterministic whole-stub replacement: read the CURRENT stub body from
      `filepath.Join(workDir, stub)`, pass it verbatim as `pattern` and the reference body as
      `replacement`, with `is_regex:false`, so the single literal occurrence (the entire stub) is
      replaced by the reference body. Args map: `{"path": stub, "pattern": <currentStubBody>,
      "replacement": <referenceBody>, "is_regex": false}`. If a CallTool returns a transport error
      or `res.IsError`, set `*applied = false` and return a wrapped error (fail-closed). On success
      of every stub edit, set `*applied = true` and return nil. Bound each CallTool with a
      `context.WithTimeout(ctx, driveDeadline)` child context like drive.go:107. Validate path
      segments are taken from the validated Config (already V5-safe via the loader) — do NOT join a
      raw untrusted relpath; use ex.Config.Files.* values directly (they passed copyFile's guard).

      `func newNativeTestFn() aiderpolyglot.TestFn` — returns a TestFn that builds argv via
      `aiderpolyglot.NativeTestCommand(ex.Language)` (fail-closed on error → TestResult{Passed:false,Output:err}),
      runs `exec.CommandContext(ctx, argv[0], argv[1:]...)` with `cmd.Dir = workDir`, and returns
      TestResult{Passed: runErr==nil, Output: string(combinedOutput)}. Thin glue per PATTERNS.md.

    Reuse the existing `nopWriter`/discard-logger helper from drive.go if it is package-visible;
    otherwise add a tiny local `io.Discard`-backed slog handler. Do NOT introduce a new logging
    dependency.

    Anti-pattern guards (encode as comments, by concept — do NOT inline a literal that a negative
    grep gate keys on): never move restorePristineTests into the AgentFn; the AgentFn edits ONLY
    files.solution stubs, never files.test; never shell `helix <verb>` per edit (dial in-process).

    In aider_edit_agent_test.go write the three behavior cases above. The hermetic test drives the
    loader's RunExercise with a fake TestFn and an injectable edit applier so NO daemon/HELIX_BIN/
    network is needed — this hermetic sibling is the SOLE authoritative proof per Pitfall 1. To make
    the edit-applier injectable for the hermetic test without a live session, structure
    newDeterministicEditAgent so the per-stub apply step goes through a small unexported function
    value (e.g. an `applyEdit func(ctx, sess, workDir, stub, body) error` seam) that the live path
    fills with the CallTool call and the test fills with an in-process file write; the
    edit_format_applied bookkeeping stays in the AgentFn. Keep the seam minimal.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestAiderEditAgentHermetic|TestAiderEditAgentAntiTamper|TestNativeTestFn' -x -count=1 && go vet ./bench/runtime/</automated>
  </verify>
  <done>newDeterministicEditAgent + newNativeTestFn + aiderEditMode exist in bench/runtime; the hermetic sibling drives RunExercise with a fake tester and a stubbed edit applier with NO HELIX_BIN/network and passes; the anti-vacuity test proves a wrong/absent edit fails the grade; go vet clean.</done>
  <acceptance_criteria>
    - `grep -n 'forwarder.OpenSession\|"activate_project"\|"replace_in_file"' bench/runtime/aider_edit_agent.go` shows the in-process dial + the EDIT verb call (PATTERNS.md shared "Daemon dial" pattern; drive.go:73,87,108).
    - The replace_in_file args use keys `path`/`pattern`/`replacement` (the REAL schema, internal/kernel/fileops/tools.go:52-59) — NOT `relpath`/`content`.
    - `grep -n 'const aiderEditMode' bench/runtime/aider_edit_agent.go` returns the mode-name const (mirrors baselineRagMode rag.go:37).
    - The hermetic test runs with no HELIX_BIN env and no network and is a named non-skipped test (Pitfall 1: hermetic sibling is the sole authoritative proof).
    - `go vet ./bench/runtime/` is clean (the daemon-dial AgentFn lives in bench/runtime, NOT the leaf — vet-ablation-leakage boundary, STATE.md constraint).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture exercise dir → cell scratch repo | Vendored fixture files (solution/test/example) are copied into the per-cell sandbox; untrusted relpaths in config.json cross into filepath.Join |
| AgentFn → warm daemon (gRPC StreamMCP) | The bench AgentFn dials the per-cell daemon and issues replace_in_file/activate_project tools/calls over the unix socket |
| reference solution (files.example) → graded test | The deterministic agent applies the reference body, which then determines pass/fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering | path traversal via exercise/solution/example relpath into filepath.Join | mitigate | Use only validated Config.Files.* values from the loader (already V5-checked by validatePathSegment loader.go:81 + copyFile guard loader.go:129-132); never join a raw untrusted relpath in the AgentFn |
| T-100-02 | Tampering | leaf-import boundary violation (daemon-dial leaking into the stdlib loader leaf) | mitigate | The AgentFn lives in bench/runtime; loader.go gains ONLY pure-stdlib exported wrappers; `go vet` + vet-ablation-leakage assert no internal/forwarder import in the leaf (acceptance criterion, Task 2) |
| T-100-03 | Tampering | load-bearing `false` dropped from result.v2 (edit_format_applied) | mitigate | `*bool` + omitempty (NOT value-type bool) so a literal false survives; round-trip test asserts presence (Task 1) |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | This phase installs NO packages; `git diff go.mod` stays empty (RESEARCH Package Legitimacy Audit — N/A); all work is in-tree Go reuse |
</threat_model>

<verification>
- `go test ./bench/runtime/... ./bench/datasets/aider-polyglot/... ./bench/runners/...` green (hermetic, fast).
- `go vet ./...` clean (CLAUDE.md mandate); the new AgentFn does not leak a daemon import into the loader leaf.
- `git diff go.mod` is empty (zero new deps).
- The hermetic AgentFn test is a named, non-skipped test that runs with no HELIX_BIN and no network (Pitfall 1).
</verification>

<success_criteria>
- EDITBENCH-03: `edit_format_applied *bool` open key round-trips (false preserved, nil dropped), schema stays v2, Validate passes.
- EDITBENCH-02: `bench/runners/aider_edit/MODE.md` resolves to `bench-full` with zero mode-resolver Go change.
- EDITBENCH-01 (substrate): the deterministic daemon-dialing EDIT-verb AgentFn + live TestFn exist in `bench/runtime`, proven hermetically; the loader leaf is untouched except for two pure-stdlib exported wrappers; WR-01 stays intact (RunExercise reused verbatim in Plan 02).
</success_criteria>

<output>
Create `.planning/phases/100-polyglot-edit-benchmark-committed-baseline/100-01-SUMMARY.md` when done
</output>
