---
phase: 102-repomap-quality-fuzzy-robustness-evals-baselines
plan: 03
type: tdd
wave: 2
depends_on:
  - 102-01
  - 102-02
files_modified:
  - bench/aggregator/repomap_eval_baseline.go
  - bench/aggregator/repomap_eval_baseline_test.go
  - bench/aggregator/fuzzy_robust_baseline.go
  - bench/aggregator/fuzzy_robust_baseline_test.go
  - bench/reports/repomap-eval-baseline/result.v2.json
  - bench/reports/repomap-eval-baseline/BENCH-RESULTS.md
  - bench/reports/fuzzy-robust-baseline/result.v2.json
  - bench/reports/fuzzy-robust-baseline/BENCH-RESULTS.md
  - .gitignore
  - Makefile
autonomous: true
requirements:
  - BASELINE-02

must_haves:
  truths:
    - "Two committed baseline trees exist (bench/reports/repomap-eval-baseline/ and fuzzy-robust-baseline/), each carrying a result.v2.json + a deterministically-rendered BENCH-RESULTS.md, force-tracked under the otherwise-ignored bench/reports/* via .gitignore allowlist entries (mirror Phase 100 lines 275-276)."
    - "Each baseline renderer (RenderRepoMapEvalBaseline / RenderFuzzyRobustBaseline) is a pure sort-before-emit function that restates deterministic metrics ONLY (no latency/timestamp/abs-path) and fail-CLOSES on a missing load-bearing key (nDCG@10 for repomap; the ambiguous-refused assertion for fuzzy) — mirror RenderAiderEditBaseline."
    - "Each baseline is byte-reproducible: rendering BENCH-RESULTS.md TWICE from the same committed result.v2.json yields byte-identical bytes AND matches the committed golden (double-render diff-empty)."
    - "Anti-vacuity proven: a result with the load-bearing metric STRIPPED does NOT satisfy the committed-baseline assertion AND the renderer fail-closes (the BASELINE-02 stripped-metric test)."
    - "Makefile regen targets build helix then run the Plan 01/02 //go:build ignore regenerators HELIX_BIN-gated (mirror bench-aider-edit); the committed bytes are the CI contract, regen is local-only."
  artifacts:
    - path: "bench/aggregator/repomap_eval_baseline.go"
      provides: "RenderRepoMapEvalBaseline — deterministic sort-before-emit renderer, fail-closed on missing nDCG@10"
      contains: "func RenderRepoMapEvalBaseline"
      min_lines: 50
    - path: "bench/aggregator/fuzzy_robust_baseline.go"
      provides: "RenderFuzzyRobustBaseline — deterministic renderer, fail-closed on missing ambiguous-refused assertion"
      contains: "func RenderFuzzyRobustBaseline"
      min_lines: 50
    - path: "bench/aggregator/repomap_eval_baseline_test.go"
      provides: "result-valid + byte-reproducible + anti-vacuity (stripped-metric) tests"
      contains: "TestRepoMapEvalBaselineByteReproducible"
    - path: "bench/aggregator/fuzzy_robust_baseline_test.go"
      provides: "result-valid + byte-reproducible + anti-vacuity tests"
      contains: "TestFuzzyRobustBaselineAntiVacuity"
    - path: "bench/reports/repomap-eval-baseline/result.v2.json"
      provides: "committed deterministic-metrics-only RepoMap-eval baseline result"
    - path: ".gitignore"
      provides: "allowlist entries force-tracking the two committed baseline trees"
      contains: "!/bench/reports/repomap-eval-baseline/"
    - path: "Makefile"
      provides: "bench-repomap-eval + bench-fuzzy-robust regen targets (mirror bench-aider-edit)"
      contains: "bench-repomap-eval"
  key_links:
    - from: "bench/aggregator/repomap_eval_baseline.go"
      to: "bench/reports/repomap-eval-baseline/result.v2.json"
      via: "renderer decodes ONLY deterministic fields (headline nDCG@10), fail-closes if absent, emits BENCH-RESULTS.md"
      pattern: "nDCG|ndcg"
    - from: "bench/aggregator/repomap_eval_baseline_test.go"
      to: "bench/reports/repomap-eval-baseline/BENCH-RESULTS.md"
      via: "double-render diff-empty + byte-match against the committed golden"
      pattern: "ByteReproducible"
    - from: "Makefile"
      to: "bench/runtime/repomap_eval_capture_regen.go"
      via: "bench-repomap-eval builds helix then go run the //go:build ignore regenerator HELIX_BIN-gated"
      pattern: "repomap_eval_capture_regen"
---

<objective>
Ship the committed, byte-reproducible RepoMap-eval and fuzzy-robustness baseline artifacts (BASELINE-02), mirroring the Phase 100 trio exactly: a deterministic aggregator renderer (sort-before-emit, deterministic-metrics-only, fail-closed) + a double-render byte-reproducibility `_test.go` + a `.gitignore` allowlist + a `Makefile` regen target, for each of the two evaluators built in Plans 01 and 02.

Purpose: BASELINE-02 — committed RepoMap-eval and fuzzy-robustness baseline artifacts captured `HELIX_BIN`-gated, fail-not-skip, byte-reproducible, deterministic-metrics-only, routed through the existing deterministic `renderAll`/seed/sort-before-emit plumbing. The committed bytes (under the force-tracked `bench/reports/*-baseline/` trees) ARE the CI contract; the hermetic golden sibling (no binary, no network) is the sole authoritative `go test ./bench/...` proof.

Output: two aggregator renderers + their byte-reproducibility/anti-vacuity tests, two committed baseline trees (`result.v2.json` + `BENCH-RESULTS.md`), `.gitignore` allowlist entries, and two `Makefile` regen targets that build helix then invoke the Plan 01/02 `//go:build ignore` regenerators.

This plan is `type: tdd`. The renderers are pure functions with a defined input→output (deterministic restatement), and the byte-reproducibility + stripped-metric anti-vacuity tests are the natural RED anchors. Mirror `aider_edit_baseline.go` + `aider_edit_baseline_test.go` field-by-field.

Depends on Plans 01 and 02: it consumes the committed corpus scores from each evaluator to author the `result.v2.json` shape (headline nDCG@10 for repomap; the ambiguous-refused assertion + per-strategy summary for fuzzy).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-repomap-quality-fuzzy-robustness-evals-baselines/102-CONTEXT.md
@.planning/phases/102-repomap-quality-fuzzy-robustness-evals-baselines/102-RESEARCH.md
@.planning/phases/102-repomap-quality-fuzzy-robustness-evals-baselines/102-PATTERNS.md
@bench/aggregator/aider_edit_baseline.go
@bench/aggregator/aider_edit_baseline_test.go
</context>

<artifacts_this_phase_produces>
- `bench/aggregator/repomap_eval_baseline.go`: `RenderRepoMapEvalBaseline(resultV2 []byte) ([]byte, error)` — decodes ONLY deterministic fields into a `repoMapEvalBaselineRow` (headline `nDCG@10`, plus `recall@10` / `MRR` / `budget_fit_ratio`); sort-before-emit; fail-CLOSED on a missing `nDCG@10` (mirror RenderAiderEditBaseline's `EditFormatApplied == nil` guard).
- `bench/aggregator/fuzzy_robust_baseline.go`: `RenderFuzzyRobustBaseline(resultV2 []byte) ([]byte, error)` — decodes the deterministic per-strategy summary + the ambiguous-refused boolean; fail-CLOSED on a missing ambiguous-refused assertion.
- Tests: `repomap_eval_baseline_test.go` + `fuzzy_robust_baseline_test.go`, each mirroring the three `aider_edit_baseline_test.go` tests: `*BaselineResultValid` (present + schema-ok + load-bearing key), `*BaselineByteReproducible` (double-render diff-empty + golden match), `*BaselineAntiVacuity` (stripped metric fails the assertion + renderer fail-closes).
- Committed baseline trees: `bench/reports/repomap-eval-baseline/{result.v2.json,BENCH-RESULTS.md}` and `bench/reports/fuzzy-robust-baseline/{result.v2.json,BENCH-RESULTS.md}`.
- `.gitignore` allowlist lines (mirror lines 275-276): `!/bench/reports/repomap-eval-baseline/`, `!/bench/reports/repomap-eval-baseline/**`, `!/bench/reports/fuzzy-robust-baseline/`, `!/bench/reports/fuzzy-robust-baseline/**`.
- `Makefile` targets `bench-repomap-eval` and `bench-fuzzy-robust` (mirror `bench-aider-edit` near line 160): build helix, then `HELIX_BIN="$(CURDIR)/$(BINARY)" $(GO) run bench/runtime/repomap_eval_capture_regen.go` (and the fuzzy regen).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RepoMap-eval committed baseline — renderer + byte-reproducibility + anti-vacuity (RED→GREEN)</name>
  <files>bench/aggregator/repomap_eval_baseline.go, bench/aggregator/repomap_eval_baseline_test.go, bench/reports/repomap-eval-baseline/result.v2.json, bench/reports/repomap-eval-baseline/BENCH-RESULTS.md</files>
  <read_first>
    - bench/aggregator/aider_edit_baseline.go (full file) — the `aiderEditBaselineRow` deterministic-only struct, the `EditFormatApplied == nil` fail-closed guard, the `kv` + `sort.Slice` sort-before-emit, and the pure string-builder restating deterministic metrics only. Mirror field-by-field.
    - bench/aggregator/aider_edit_baseline_test.go (full file) — the three tests + `readBaselineResult` fail-closed helper + `const baselineDir` + `cloneMap`. Mirror exactly.
    - bench/evaluators/repomapeval/CORPUS.md + bench/evaluators/repomapeval/testdata/gold + testdata/captured (from Plan 01) — the corpus scores the committed result.v2.json restates (headline nDCG@10 per D-07).
    - .planning/phases/102-repomap-quality-fuzzy-robustness-evals-baselines/102-PATTERNS.md ("repomap_eval_baseline.go" + "*_baseline_test.go" sections).
  </read_first>
  <behavior>
    - Test 1 (TestRepoMapEvalBaselineResultValid): the committed result.v2.json is present + non-empty + carries the headline nDCG@10 metric — a missing file or missing nDCG@10 is a HARD failure (fail-closed).
    - Test 2 (TestRepoMapEvalBaselineByteReproducible): rendering BENCH-RESULTS.md twice from the same committed result.v2.json yields byte-identical bytes AND matches the committed BENCH-RESULTS.md golden.
    - Test 3 (TestRepoMapEvalBaselineAntiVacuity): a result with nDCG@10 STRIPPED does NOT satisfy the present check AND RenderRepoMapEvalBaseline fail-closes on the stripped bytes.
  </behavior>
  <action>
Author the committed `bench/reports/repomap-eval-baseline/result.v2.json` from the Plan 01 corpus scores: a deterministic-metrics-only document carrying the headline `nDCG@10` (D-07) plus `recall@10` / `MRR` / `budget_fit_ratio` (D-08), and identifying fields (schema_version, benchmark, mode, language) — NO latency, NO timestamp, NO absolute path. Use the same `benchruntime.Validate` schema contract the aider baseline uses if the result.v2 schema applies; if the repomap-eval metrics do not fit the existing result.v2 schema, carry them under the additive-open-key convention (do NOT bump the schema — mirror Phase 100's `edit_format_applied` additive `*bool` open key).

Write the RED tests first (mirroring `aider_edit_baseline_test.go`): `readRepoMapEvalBaselineResult` fail-closed helper, `const repoMapEvalBaselineDir = "../reports/repomap-eval-baseline"`, and the three tests. Then implement `repomap_eval_baseline.go`: `RenderRepoMapEvalBaseline` decoding ONLY deterministic fields into `repoMapEvalBaselineRow` (so latency/timestamp can never leak), fail-CLOSED when the headline `nDCG@10` pointer is nil (mirror the `EditFormatApplied == nil` guard), `kv`+`sort.Slice` metric rows sorted by name before emit, pure string-builder. Render the committed `BENCH-RESULTS.md` golden from the renderer output and commit it (so the byte-reproducible test matches its own golden).

Implement to GREEN.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run 'TestRepoMapEvalBaseline' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The committed result.v2.json is present, deterministic-metrics-only (no latency/timestamp/abs-path), and carries the headline nDCG@10.
    - RenderRepoMapEvalBaseline is pure + sort-before-emit + fail-closed on a missing nDCG@10.
    - Double-render is byte-identical AND matches the committed BENCH-RESULTS.md golden.
    - The stripped-nDCG@10 anti-vacuity test fails the present check AND the renderer fail-closes.
  </acceptance_criteria>
  <done>The RepoMap-eval committed baseline (result.v2.json + BENCH-RESULTS.md) is byte-reproducible, deterministic-metrics-only, fail-closed, with passing result-valid/byte-reproducible/anti-vacuity tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fuzzy-robustness committed baseline — renderer + byte-reproducibility + anti-vacuity (RED→GREEN)</name>
  <files>bench/aggregator/fuzzy_robust_baseline.go, bench/aggregator/fuzzy_robust_baseline_test.go, bench/reports/fuzzy-robust-baseline/result.v2.json, bench/reports/fuzzy-robust-baseline/BENCH-RESULTS.md</files>
  <read_first>
    - bench/aggregator/aider_edit_baseline.go + aider_edit_baseline_test.go (full files) — the same renderer + three-test pattern to mirror.
    - bench/evaluators/fuzzyrobust/CORPUS.md + testdata/drift + testdata/captured (from Plan 02) — the per-strategy summary + ambiguous-refused boolean the committed result.v2.json restates.
    - .planning/phases/102-repomap-quality-fuzzy-robustness-evals-baselines/102-PATTERNS.md ("fuzzy_robust_baseline.go" — the fuzzy baseline carries per-case strategy-match + the ambiguous-refused assertion).
  </read_first>
  <behavior>
    - Test 1 (TestFuzzyRobustBaselineResultValid): the committed result.v2.json is present + non-empty + carries the ambiguous-refused assertion (the load-bearing key) — a missing file or missing assertion is a HARD failure.
    - Test 2 (TestFuzzyRobustBaselineByteReproducible): double-render diff-empty + byte-match against the committed BENCH-RESULTS.md golden.
    - Test 3 (TestFuzzyRobustBaselineAntiVacuity): a result with the ambiguous-refused assertion STRIPPED (or flipped to false) does NOT satisfy the assertion AND the renderer fail-closes.
  </behavior>
  <action>
Author the committed `bench/reports/fuzzy-robust-baseline/result.v2.json` from the Plan 02 corpus scores: a deterministic-metrics-only document carrying the per-strategy match summary (counts/ratios per `exact`/`whitespace_normalized`/`indentation_flexible`/ellipsis tier) and the load-bearing `ambiguous_refused` boolean (the must-refuse assertion from D-06), plus identifying fields — NO latency/timestamp/abs-path. Use the additive-open-key convention if these do not fit the existing result.v2 schema (do NOT bump the schema).

Write the RED tests first (mirroring `aider_edit_baseline_test.go`): `readFuzzyRobustBaselineResult` fail-closed helper, `const fuzzyRobustBaselineDir = "../reports/fuzzy-robust-baseline"`, and the three tests; the load-bearing key for the present + fail-closed checks is the `ambiguous_refused` assertion. Then implement `fuzzy_robust_baseline.go`: `RenderFuzzyRobustBaseline` decoding ONLY deterministic fields, fail-CLOSED when the `ambiguous_refused` assertion is absent or false, sort-before-emit per-strategy rows, pure string-builder. Render + commit the `BENCH-RESULTS.md` golden.

Implement to GREEN.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run 'TestFuzzyRobustBaseline' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The committed result.v2.json is present, deterministic-metrics-only, and carries the ambiguous-refused assertion.
    - RenderFuzzyRobustBaseline is pure + sort-before-emit + fail-closed on a missing/false ambiguous-refused assertion.
    - Double-render is byte-identical AND matches the committed BENCH-RESULTS.md golden.
    - The stripped-assertion anti-vacuity test fails the present check AND the renderer fail-closes.
  </acceptance_criteria>
  <done>The fuzzy-robustness committed baseline (result.v2.json + BENCH-RESULTS.md) is byte-reproducible, deterministic-metrics-only, fail-closed, with passing result-valid/byte-reproducible/anti-vacuity tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the .gitignore allowlist + Makefile regen targets (config glue)</name>
  <files>.gitignore, Makefile</files>
  <read_first>
    - .gitignore (lines 268-280) — the `bench/reports/*` ignore + the Phase 100 allowlist (`!/bench/reports/aider-edit-baseline/` + `/**`). Add the two new baseline-tree allowlist pairs mirroring lines 275-276.
    - Makefile (lines 155-172, the `bench-aider-edit` target) — build helix then `HELIX_BIN="$(CURDIR)/$(BINARY)" $(GO) run bench/runtime/aider_edit_baseline_regen.go`; local-only, no CI benchstat gate. Mirror for the two new regenerators.
    - .planning/phases/102-repomap-quality-fuzzy-robustness-evals-baselines/102-PATTERNS.md (".gitignore + Makefile" section).
  </read_first>
  <action>
Add to `.gitignore`, immediately after the Phase 100 aider-edit-baseline allowlist (line 276), four allowlist lines force-tracking the two new committed baseline trees under the otherwise-ignored `bench/reports/*`:
`!/bench/reports/repomap-eval-baseline/`, `!/bench/reports/repomap-eval-baseline/**`, `!/bench/reports/fuzzy-robust-baseline/`, `!/bench/reports/fuzzy-robust-baseline/**`. Add a brief comment naming Phase 102 BASELINE-02 (mirror the Phase 100 comment above line 275). Use Edit (scoped) — do NOT rewrite the whole file.

Add two `Makefile` targets mirroring `bench-aider-edit` (Makefile:160): `bench-repomap-eval` and `bench-fuzzy-robust`, each with a `##` help comment, building helix (`$(GO) build -o $(BINARY) ./cmd/helix`) then running the Plan 01/02 `//go:build ignore` regenerators HELIX_BIN-gated (`HELIX_BIN="$(CURDIR)/$(BINARY)" $(GO) run bench/runtime/repomap_eval_capture_regen.go` and `.../fuzzy_robust_capture_regen.go`). Keep the local-only-benches note (no CI benchstat gate; committed bytes are the CI contract). Use Edit (scoped). Confirm `make -n bench-repomap-eval` and `make -n bench-fuzzy-robust` resolve (dry-run prints the recipe without executing).
  </action>
  <verify>
    <automated>git check-ignore -v bench/reports/repomap-eval-baseline/result.v2.json; test $? -eq 1 && make -n bench-repomap-eval >/dev/null && make -n bench-fuzzy-robust >/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore` reports the two new baseline trees are NOT ignored (force-tracked via the allowlist).
    - `make -n bench-repomap-eval` and `make -n bench-fuzzy-robust` resolve and print the build-helix + HELIX_BIN regen recipe.
    - Changes applied via scoped Edit (whole-file Write NOT used on .gitignore / Makefile).
  </acceptance_criteria>
  <done>The two committed baseline trees are force-tracked via .gitignore allowlist entries, and bench-repomap-eval / bench-fuzzy-robust Makefile targets regenerate them HELIX_BIN-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed result.v2.json → renderer | The renderer decodes committed bytes; only deterministic fields are decoded so non-reproducible fields can never enter the rendered summary |
| Makefile regen → warm daemon (HELIX_BIN) | Local-only regen path; the committed bytes are the CI contract, not the live daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-09 | Tampering | non-determinism leaking into committed baseline bytes | mitigate | decode ONLY deterministic fields into the row struct + sort-before-emit + double-render diff-empty test (no latency/timestamp/abs-path) |
| T-102-10 | Repudiation | baseline assertion never grades (vacuous-pass) | mitigate | mandatory anti-vacuity: a STRIPPED load-bearing metric (nDCG@10 / ambiguous_refused) fails the present check AND the renderer fail-closes |
| T-102-11 | Tampering | committed baseline tree silently ignored by git | mitigate | `.gitignore` allowlist (`!/bench/reports/*-baseline/**`) + `git check-ignore` verification that the trees are force-tracked |
| T-102-SC | Tampering | npm/pip/cargo installs | accept | none — ZERO external packages added (stdlib + in-repo only); no install surface |
</threat_model>

<verification>
- `go test ./bench/aggregator/ -count=1` — both baselines' result-valid + byte-reproducible + anti-vacuity tests pass with NO `HELIX_BIN` and NO network.
- `git check-ignore -v bench/reports/repomap-eval-baseline/result.v2.json` returns non-zero (NOT ignored); same for fuzzy-robust-baseline.
- `make -n bench-repomap-eval` and `make -n bench-fuzzy-robust` resolve.
- `go test ./...` green; `go vet ./...` clean; `gofmt -l bench/aggregator/` reports no diff.
</verification>

<success_criteria>
- BASELINE-02: committed RepoMap-eval and fuzzy-robustness baseline artifacts are captured HELIX_BIN-gated (via the Plan 01/02 regenerators + Makefile targets), fail-not-skip, byte-reproducible (double-render diff-empty), deterministic-metrics-only (sort-before-emit, no latency/timestamp/abs-path), with the stripped-metric anti-vacuity test proving each baseline assertion grades.
- The hermetic golden sibling runs with no binary and no network as the sole authoritative `go test ./bench/...` proof; the committed bytes are the CI contract.
</success_criteria>

<output>
Create `.planning/phases/102-repomap-quality-fuzzy-robustness-evals-baselines/102-03-SUMMARY.md` when done.
</output>
