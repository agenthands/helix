---
phase: 97-generated-per-verb-reference-deterministic-adoption-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/helix-refgen/main.go
  - cmd/helix-refgen/render.go
  - cmd/helix-refgen/main_test.go
  - internal/cli/verb.go
  - internal/cli/skill.go
  - internal/cli/skill_test.go
  - internal/cli/skills/helix/reference.md
  - Makefile
  - .github/workflows/go-test.yml
autonomous: true
requirements: [REF-01, REF-02, REF-03]
must_haves:
  truths:
    - "Running `go run ./cmd/helix-refgen` produces internal/cli/skills/helix/reference.md covering every one of the 50 frozen verbs in verbs_gen.go (synopsis, args, output shape, worked example, use-this-not-that)"
    - "`go run ./cmd/helix-refgen --check` exits 1 on a stale or hand-edited reference.md and exits 0 when in sync"
    - "`make verify-reference` runs the --check drift gate; the CI workflow runs it as a sibling of the cligen/docgen gates"
    - "`helix setup claude-code` installs BOTH SKILL.md and reference.md into <targetDir> atomically, with the withinSkillRoot path-containment guard intact"
    - "EmbeddedSkillBody() still returns SKILL.md bytes only (no reference.md bleed); the SKILL-04 idle-cost ≤1536 cap is still asserted on SKILL.md's description only"
  artifacts:
    - path: "cmd/helix-refgen/main.go"
      provides: "Generator entrypoint with --out/--check flags + daemon-parity blank imports"
      contains: "func main"
    - path: "cmd/helix-refgen/render.go"
      provides: "renderReference() — registry walk + per-verb args from the cli accessor"
      contains: "func renderReference"
    - path: "internal/cli/skills/helix/reference.md"
      provides: "Generated per-verb reference (committed build artifact)"
      min_lines: 100
    - path: "internal/cli/verb.go"
      provides: "Exported read-only verb->flags accessor (VerbSpecsForDocs) mirroring VerbToolNames fresh-copy discipline"
      contains: "func VerbSpecsForDocs"
    - path: "internal/cli/skill.go"
      provides: "embed.FS skill bundle + multi-file atomic installSkill"
      contains: "embed.FS"
  key_links:
    - from: "cmd/helix-refgen/render.go"
      to: "internal/cli/verb.go (VerbSpecsForDocs)"
      via: "refgen reads per-verb args from the exported accessor, NOT tool.InputSchema"
      pattern: "VerbSpecsForDocs"
    - from: "cmd/helix-refgen/main.go"
      to: "internal/skill (ToolProviders)"
      via: "blank-import set identical to internal/daemon/imports.go drives init() registration"
      pattern: "skill.ToolProviders"
    - from: "internal/cli/skill.go (installSkill)"
      to: "internal/cli/skills/helix/* (embed.FS)"
      via: "walks the embedded FS and writes each entry atomically within the skill root"
      pattern: "embeddedSkillFS.ReadDir"
---

<objective>
Build the generation + bundling substrate for Phase 97: a new `cmd/helix-refgen` binary that generates a per-verb `reference.md` from the live tool registry (REF-01), a `--check` drift gate wired into `make` + CI (REF-03), and the single structural code change switching `internal/cli/skill.go` from an embedded `string` to an `embed.FS` so `installSkill` ships `reference.md` alongside `SKILL.md` atomically (REF-02).

Purpose: This is the load-bearing substrate the deterministic adoption contract (Plan 02) tests against. The completeness gate in Plan 02 reads the committed `reference.md` via the embedded FS, so the file and the embed shape must exist first.

Output: `cmd/helix-refgen/{main.go,render.go,main_test.go}`, the committed `internal/cli/skills/helix/reference.md`, the exported `VerbSpecsForDocs()` accessor in `internal/cli/verb.go`, the `embed.FS` switch in `internal/cli/skill.go`, extended `skill_test.go`, a `make verify-reference` target + `reference` regen target, and a CI drift-gate step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-generated-per-verb-reference-deterministic-adoption-contract/97-RESEARCH.md
@.planning/phases/97-generated-per-verb-reference-deterministic-adoption-contract/97-PATTERNS.md

# Load-bearing analogs (read before editing):
@cmd/docgen/main.go
@cmd/helix-cligen/main.go
@internal/cli/skill.go
@internal/cli/verb.go
@internal/daemon/imports.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export a read-only verb->flags accessor (VerbSpecsForDocs) from internal/cli</name>
  <files>internal/cli/verb.go, internal/cli/verb_test.go</files>
  <read_first>
    - internal/cli/verb.go:44-88 — verbFlag, verbSpec (both package-private), and VerbToolNames() (the fresh-copy discipline to mirror exactly)
    - internal/cli/verbs_gen.go:10-40 — the generated verbSpecs map: toolName, short, groupID, []verbFlag{name,toolArg,kind,required,help}
  </read_first>
  <behavior>
    - VerbSpecsForDocs() returns one entry per verb in verbSpecs, sorted by verb name (the kebab key), as a fresh copy — mutating the returned slice or its flag slices does NOT affect verbSpecs (mirror VerbToolNames's fresh-copy note)
    - Each entry exposes the doc-facing view: Verb (kebab key), ToolName, GroupID, Short, and a copied slice of flags each carrying Name, ToolArg, Kind (rendered as a stable string like "string"/"int"/"bool"/"string-slice"/"json"), Required, Help
    - len(VerbSpecsForDocs()) == len(VerbToolNames()) == 50 (same authority, same count)
    - The accessor is read-only: a test mutates a returned flag slice and re-reads to prove verbSpecs is unaffected
  </behavior>
  <action>
    Add an exported doc-facing accessor `VerbSpecsForDocs()` to internal/cli/verb.go, mirroring VerbToolNames()'s fresh-copy discipline (verb.go:81-88). verbFlag/verbSpec are package-private (verb.go:44-74) so an out-of-package generator (cmd/helix-refgen) cannot read verbSpecs directly — export a small public struct (e.g. `VerbDoc{Verb, ToolName, GroupID, Short string; Flags []FlagDoc}` and `FlagDoc{Name, ToolArg, Kind string; Required bool; Help string}`). Iterate verbSpecs, sort by verb key, and deep-copy each flag slice into a new FlagDoc slice so callers cannot mutate verbSpecs. Map the package-private flagKind to a stable lowercase string token (flagString->"string", flagInt->"int", flagBool->"bool", flagStringSlice->"string-slice", flagJSON->"json") via a small switch so the rendered reference is deterministic and gofmt-independent. Add a godoc note matching VerbToolNames's out-of-package-seam wording. Write the fresh-copy + count tests in verb_test.go first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>go test ./internal/cli/... -run 'TestVerbSpecsForDocs' -count=1</automated>
  </verify>
  <done>VerbSpecsForDocs() returns 50 sorted, deep-copied doc entries; mutating a returned flag slice does not affect verbSpecs; `go build ./...` and `go vet ./...` pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cmd/helix-refgen — registry-walk renderer + --check drift gate</name>
  <files>cmd/helix-refgen/main.go, cmd/helix-refgen/render.go, cmd/helix-refgen/main_test.go</files>
  <read_first>
    - cmd/docgen/main.go:21-46 — the blank-import-parity block to copy VERBATIM (includes health+help; NEVER blank-import internal/semantic/extract/* per D-02)
    - cmd/docgen/main.go:109-135 — registry walk: skill.ToolProviders() -> tp.Tools(); tool.Description/.HelpText/.BriefDescription; verb := ReplaceAll(tool.Name,"_","-"); pipe-escape with ReplaceAll(desc,"|","\\|")
    - cmd/helix-cligen/main.go:41-85 — the single-committed-file --out/--check shape (reads existing first, exits 1 on diff)
    - internal/cli/verb.go (VerbSpecsForDocs from Task 1) — the per-verb args source; tool.InputSchema does NOT exist on mcp.ToolDef (97-RESEARCH.md Pitfall 1)
    - .planning/phases/97-generated-per-verb-reference-deterministic-adoption-contract/97-RESEARCH.md "Code Examples" + "Open Questions" — render template guidance (synopsis from Description, args from VerbSpecsForDocs, generic worked example per groupID)
  </read_first>
  <behavior>
    - TestRenderCoversAllVerbs: renderReference() output contains a section header / line for every kebab verb in cli.VerbToolNames() (50/50); zero missing
    - renderReference() is deterministic — two calls produce byte-identical output (sorted iteration, no map-order leakage)
    - Each verb section carries: a synopsis (from tool.Description, pipe-escaped), an args list (from VerbSpecsForDocs flags: name, kind, required, help), an output-shape note, a worked `helix <verb> ...` example, and a "use this not that" line
    - --check mode: when the rendered output equals the on-disk reference.md, exit 0 with "up to date"; when it differs, write the diff message to stderr and exit 1
    - A round-trip test: render to a temp path, then run the --check logic against that temp file and assert it reports in-sync (exit 0 equivalent)
  </behavior>
  <action>
    Create cmd/helix-refgen mirroring cmd/docgen + cmd/helix-cligen. In main.go: copy the docgen blank-import block VERBATIM (cmd/docgen/main.go:21-46 — this is the helix-tool-docs-drift defense; the set must equal internal/daemon/imports.go's providers). Declare `--out` (default `internal/cli/skills/helix/reference.md`) and `--check` flags (flag pkg, exactly docgen/cligen shape). In render.go: implement `renderReference()` that walks `skill.ToolProviders() -> tp.Tools()` for synopsis/description prose (pipe-escape Descriptions with ReplaceAll(desc,"|","\\|") per cmd/docgen/main.go:125, the Security-V12 markup-injection mitigation), and reads `cli.VerbSpecsForDocs()` for the per-verb args section — do NOT call help.ExtractParamDocs(tool.InputSchema) and do NOT construct a SerenaMCPServer (RESEARCH Pitfall 1; tool.InputSchema is not a field on mcp.ToolDef). Key the verb sections off the kebab name (ReplaceAll(tool.Name,"_","-")). Template a generic worked example + "use this not that" line per groupID (navigation/edit/fileops/diagnostics/memory/repomap) so output stays 100% generated (no hand-authoring — that would make --check meaningless). For the --check/--out triad clone cmd/helix-cligen/main.go:68-84, but on a first non-check run the file may not exist: handle a missing-file os.ReadFile gracefully on write (only --check against a missing file is a hard fail). Write main_test.go (render-covers-all-verbs + determinism + check round-trip) FIRST as RED, then implement GREEN.
    Do NOT inline a fenced reference.md sample in this action — the render template lives in render.go; the committed artifact is produced in Task 3.
  </action>
  <verify>
    <automated>go test ./cmd/helix-refgen/... -count=1</automated>
  </verify>
  <done>renderReference() covers all 50 verbs deterministically; --check exits 1 on diff and 0 in-sync; `go vet ./...` passes. (reference.md itself is generated in Task 3.)</done>
</task>

<task type="auto">
  <name>Task 3: Generate + commit reference.md; switch skill.go to embed.FS with multi-file atomic install</name>
  <files>internal/cli/skills/helix/reference.md, internal/cli/skill.go, internal/cli/skill_test.go</files>
  <read_first>
    - internal/cli/skill.go:21-29 — `//go:embed skills/helix/SKILL.md` + `embeddedSkillMD string` + EmbeddedSkillBody()
    - internal/cli/skill.go:42-109 — skillDescription() and the frontmatter parsers operate on the SKILL.md string (must keep reading SKILL.md only)
    - internal/cli/skill.go:131-205 — installSkill (temp+rename single file) + withinSkillRoot/lexicalSkillRoot/containedIn path-traversal guard (UNCHANGED)
    - internal/cli/skill.go:213-227 — uninstallSkill (currently removes SKILL.md only)
    - internal/cli/skill_test.go:105-169 — TestSkillDescriptionCap / TestSkillIdleCostBound / TestSkillTokenNoteFilled reference embeddedSkillMD + the test-local frontmatterBlock/frontmatterValue helpers
    - .planning/phases/97-generated-per-verb-reference-deterministic-adoption-contract/97-PATTERNS.md "internal/cli/skill.go (MODIFY)" — the verified installSkill walk loop + ripple list
  </read_first>
  <action>
    First generate the committed artifact: run `go run ./cmd/helix-refgen` to produce internal/cli/skills/helix/reference.md, then commit it (it is a generated build artifact like verbs_gen.go — DO NOT hand-edit it). Then make the structural REF-02 change in skill.go: replace `//go:embed skills/helix/SKILL.md` + `var embeddedSkillMD string` with `//go:embed skills/helix/*` + `var embeddedSkillFS embed.FS` (add the non-underscore `"embed"` import; the current file uses `_ "embed"`). Provide a SKILL.md-only accessor (e.g. `embeddedSkillBytes() string` that reads `skills/helix/SKILL.md` from the FS) and mechanically repoint every `embeddedSkillMD` consumer to it: EmbeddedSkillBody() must STILL return SKILL.md bytes only (consumed by test/oracle/llm/prompt.go:142 + skill_trigger_test.go:91 — no reference.md bleed), and skillDescription()/skillFrontmatter()/skillFrontmatterValue() must keep reading the SKILL.md entry, never the bundle (RESEARCH Pitfall 4). Extend installSkill (skill.go:131) per 97-PATTERNS.md: keep the withinSkillRoot guard + MkdirAll UNCHANGED, then ReadDir("skills/helix") and for each entry ReadFile + single-trailing-newline policy + per-file temp+rename atomic write to filepath.Join(targetDir, e.Name()). Entry names come ONLY from the embedded FS (never user input) so the T-93-01 path-traversal posture holds. Update uninstallSkill to remove every embedded entry name (SKILL.md + reference.md) so uninstall stays clean. In skill_test.go, mechanically repoint the ~13 embeddedSkillMD references (TestSkillDescriptionCap/IdleCostBound/TokenNoteFilled etc. at lines ~106,156,162,166,185,209,217,245 and any siblings) plus setup_test.go:590 to the SKILL.md-only accessor — verify with `go build ./...`. Keep the SKILL-04 idle-cost assertions (TestSkillIdleCostBound, TestSkillDescriptionCap) reading SKILL.md's description ONLY; reference.md is EXEMPT from the 1536-char cap (it is the on-demand tier).
  </action>
  <verify>
    <automated>go run ./cmd/helix-refgen --check && go test ./internal/cli/... -run 'TestSkill|TestEmbeddedSkillBody|TestInstallSkill' -count=1</automated>
  </verify>
  <done>reference.md exists and is in-sync (`--check` clean); skill.go embeds skills/helix/* as embed.FS; EmbeddedSkillBody() returns SKILL.md only; TestSkillIdleCostBound passes on SKILL.md; `go build ./...` + `go vet ./...` pass.</done>
</task>

<task type="auto">
  <name>Task 4: Wire the --check drift gate into make + CI; assert install ships both files</name>
  <files>Makefile, .github/workflows/go-test.yml, internal/cli/skill_test.go</files>
  <read_first>
    - Makefile:77-85 — `docs:` regen target + `verify-cligen` / `verify-docs` HARD-FAIL drift gates (the exact shape to mirror)
    - .github/workflows/go-test.yml:102-113 — the helix-cligen + docgen drift-gate CI steps (sibling step to add)
    - internal/cli/skill_test.go:234-313 (existing install test region) — extend to assert BOTH SKILL.md AND reference.md land in targetDir
    - internal/cli/setup_clients.go:158-163 — the installSkill call site (likely no change; installSkill walks the FS internally now)
  </read_first>
  <action>
    Add a `verify-reference` HARD-FAIL drift-gate target to Makefile mirroring `verify-cligen`/`verify-docs` (Makefile:80-85): `$(GO) run ./cmd/helix-refgen --check` with a `## HARD-FAIL drift gate: internal/cli/skills/helix/reference.md must match the live registry (REF-03)` comment, plus a `reference:` regen target mirroring `docs:` (Makefile:77-78) running `$(GO) run ./cmd/helix-refgen`. Add a sibling CI step to .github/workflows/go-test.yml after the docgen gate (:108-112): `- name: helix-refgen drift gate (REF-03)` running `go run ./cmd/helix-refgen --check`, with a comment matching the docgen step's discipline. In skill_test.go, add/extend a `TestInstallSkill`-family test asserting installSkill writes BOTH `<targetDir>/SKILL.md` AND `<targetDir>/reference.md` (both present, non-empty, single-trailing-newline), and a `TestEmbeddedSkillBody` proving EmbeddedSkillBody() returns SKILL.md content only (does NOT contain a reference.md-only marker line). Leave the setup_clients.go call site unchanged unless the Success/DryRun copy needs the cosmetic "skill bundle (SKILL.md + reference.md)" wording (optional, not load-bearing).
  </action>
  <verify>
    <automated>make verify-reference && go test ./internal/cli/... -run 'TestInstallSkill|TestEmbeddedSkillBody' -count=1</automated>
  </verify>
  <done>`make verify-reference` passes (gate green when reference is in sync); CI has the helix-refgen drift step; the install test proves both files land; `go vet ./...` + `go test ./...` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time generator → committed file | `helix-refgen` reads only in-tree registry data (no untrusted input) and writes one in-repo path |
| `helix setup` → filesystem (skill install) | the multi-file `installSkill` loop writes into a per-client `skills/helix` target dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | `installSkill` multi-file write loop (skill.go) | mitigate | Preserve `withinSkillRoot`/`lexicalSkillRoot`/`containedIn` UNCHANGED (skill.go:172-205); entry names come ONLY from the embedded FS (never user-controlled), so the new loop writes only `<targetDir>/<embedded-entry-name>` — the T-93-01 path-traversal posture is unchanged |
| T-97-02 | Tampering | partial/corrupt skill file on concurrent `helix setup` | mitigate | Keep the per-file atomic temp+rename (skill.go:147-153 pattern) for EACH bundle entry, not just SKILL.md |
| T-97-03 | Tampering | markup injection from a tool Description into generated reference.md | mitigate | Escape pipes when rendering Descriptions into markdown (`ReplaceAll(desc,"|","\\|")`, mirroring cmd/docgen/main.go:125) |
| T-97-04 | Tampering | `helix-refgen` blank-import drift from the daemon (silent tool drop — the helix-tool-docs-drift defect) | mitigate | Copy the docgen blank-import block VERBATIM; the `--check` gate + Plan 02's `VerbToolNames()`-authoritative completeness gate catch any divergence (a 49-vs-50 mismatch turns Plan 02 RED) |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | Phase 97 installs ZERO external packages (RESEARCH "Package Legitimacy Audit"); go.mod must stay unchanged (milestone zero-dep invariant). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `go vet ./...` clean
- `go test ./...` green (untagged default suite)
- `go run ./cmd/helix-refgen --check` clean (reference.md in sync)
- `make verify-reference` green
- `git diff go.mod` empty (zero new dependencies — milestone invariant)
- `git diff api/proto/` empty (no wire change)
</verification>

<success_criteria>
- REF-01: `cmd/helix-refgen` generates reference.md covering all 50 frozen verbs with synopsis/args/output-shape/worked-example/use-this-not-that, args sourced from VerbSpecsForDocs (not InputSchema)
- REF-02: skill.go switched to embed.FS; installSkill writes BOTH SKILL.md + reference.md atomically with path-containment intact; EmbeddedSkillBody() returns SKILL.md only; SKILL-04 idle-cost still on SKILL.md
- REF-03: `helix-refgen --check` fails on stale/hand-edited reference; wired into `make verify-reference` + CI; inherits the docgen blank-import-parity rule
</success_criteria>

<output>
Create `.planning/phases/97-generated-per-verb-reference-deterministic-adoption-contract/97-01-SUMMARY.md` when done
</output>
