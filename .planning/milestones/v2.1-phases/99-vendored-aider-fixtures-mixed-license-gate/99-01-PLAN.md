---
phase: 99-vendored-aider-fixtures-mixed-license-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/datasets/aider-polyglot/fixtures/python/exercises/practice/*
  - bench/datasets/aider-polyglot/fixtures/go/exercises/practice/*
  - bench/datasets/aider-polyglot/fixtures/rust/exercises/practice/*
  - bench/datasets/aider-polyglot/fixtures/python/NOTICE
  - bench/datasets/aider-polyglot/fixtures/go/NOTICE
  - bench/datasets/aider-polyglot/fixtures/rust/NOTICE
  - bench/datasets/aider-polyglot/fixtures/_aider-edit-format/*
  - bench/datasets/aider-polyglot/VENDOR-MANIFEST.md
  - bench/datasets/aider-polyglot/LICENSE-AUDIT.md
autonomous: true
requirements: [VENDOR-01, VENDOR-02]
must_haves:
  truths:
    - "The 9-exercise MIT polyglot subset is committed under fixtures/{python,go,rust}/exercises/practice/<ex>/, byte-identical to polyglot-benchmark@7e0611e"
    - "The Apache-2.0 aider edit-format subset is committed under fixtures/_aider-edit-format/ (small languages/* files + a trimmed SEARCH/REPLACE excerpt), byte-identical to aider@5dc9490 for verbatim files"
    - "Every vendored file has a VENDOR-MANIFEST.md row with a REAL crypto/sha256 digest (no 40-zero placeholders) and a MIT|Apache-2.0 disposition"
    - "Per-track NOTICE sidecars cite exercism/<lang>@<sha> (MIT); the _aider-edit-format/ subtree carries a NOTICE + an Apache-2.0 LICENSE text copy"
    - "LICENSE-AUDIT.md carries the existing 6 MIT track blocks UNCHANGED plus new Apache-2.0 fixture block(s) keyed by a top-level fixture: key"
    - "The pre-existing hermetic stubs fixtures/python/.../wordy and fixtures/rust/.../leap are untouched (loader_test.go still passes)"
  artifacts:
    - path: "bench/datasets/aider-polyglot/VENDOR-MANIFEST.md"
      provides: "Reproducibility record: pinned SHAs + per-file selection + real sha256 + disposition"
      contains: "7e0611e77b54e2dea774cdc0aa00cf9f7ed6144f"
    - path: "bench/datasets/aider-polyglot/LICENSE-AUDIT.md"
      provides: "Dual-disposition license record (MIT track blocks + Apache-2.0 fixture block)"
      contains: "fixture:"
    - path: "bench/datasets/aider-polyglot/fixtures/_aider-edit-format/LICENSE"
      provides: "Apache-2.0 license text copy for the aider edit-format subtree"
      contains: "Apache License"
    - path: "bench/datasets/aider-polyglot/fixtures/python/NOTICE"
      provides: "MIT attribution sidecar citing exercism/python@<sha>"
      contains: "exercism/python"
  key_links:
    - from: "bench/datasets/aider-polyglot/VENDOR-MANIFEST.md"
      to: "bench/datasets/aider-polyglot/fixtures/"
      via: "every manifest row's sha256 matches the committed on-disk bytes (Plan 02 gate verifies this bidirectionally)"
      pattern: "sha256"
    - from: "bench/datasets/aider-polyglot/LICENSE-AUDIT.md"
      to: "bench/datasets/aider-polyglot/fixtures/_aider-edit-format/"
      via: "Apache-2.0 fixture block's notice_path / license_path point at the committed NOTICE + LICENSE sidecars"
      pattern: "notice_path|license_path"
---

<objective>
Vendor a deterministic, offline, mixed-license fixture tree into the Helix tree: the MIT Exercism polyglot subset (VENDOR-01) and the Apache-2.0 aider edit-format subset (VENDOR-02), each with attribution sidecars, recorded in a reproducible VENDOR-MANIFEST.md (real sha256) and a dual-disposition LICENSE-AUDIT.md.

Purpose: This committed, byte-identical fixture tree is the offline substrate the Phase 100 polyglot edit benchmark + the Phase 102 corpora consume from disk. It must be REAL upstream bytes (cloned at the pinned SHAs), never fabricated, and byte-preserving (NO inline SPDX in executed fixtures — disposition lives in the manifest/audit/NOTICE sidecars).

Output: fixtures/{python,go,rust}/exercises/practice/<ex>/... (MIT), fixtures/_aider-edit-format/... (Apache-2.0), per-track NOTICE sidecars, an Apache-2.0 NOTICE+LICENSE copy, VENDOR-MANIFEST.md, and an extended LICENSE-AUDIT.md. (The Plan 02 gate in Wave 2 verifies all of this.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-vendored-aider-fixtures-mixed-license-gate/99-RESEARCH.md
@.planning/phases/99-vendored-aider-fixtures-mixed-license-gate/99-PATTERNS.md

# Existing in-tree analogs (read these before vendoring):
@bench/datasets/aider-polyglot/pin.go
@bench/datasets/aider-polyglot/LICENSE-AUDIT.md
@bench/datasets/aider-polyglot/loader.go
</context>

<artifacts_this_phase_produces>
This plan (Plan 01) produces the vendored DATA + disposition records. The downstream Plan 02 gate consumes them.

| Artifact | Disposition | Provenance | Notes |
|----------|-------------|-----------|-------|
| `fixtures/{python,go,rust}/exercises/practice/<ex>/...` | MIT | `polyglot-benchmark@7e0611e` (Exercism redistribution) | 9 exercises × 3 langs; byte-identical to upstream; excludes `.docs/`, `.approaches/` |
| `fixtures/{python,go,rust}/NOTICE` | MIT (attribution sidecar) | cites `exercism/<lang>@<sha>` from LICENSE-AUDIT.md | non-executed; inline SPDX allowed here |
| `fixtures/_aider-edit-format/languages/{python,go,rust,java,javascript}/test.<ext>` | Apache-2.0 | `aider@5dc9490` `tests/fixtures/languages/*` | small; byte-identical |
| `fixtures/_aider-edit-format/search-replace-sample.txt` | Apache-2.0 | trimmed excerpt of `aider@5dc9490` `tests/fixtures/chat-history-search-replace-gold.txt` (≤~150 lines) | non-executed; trimmed |
| `fixtures/_aider-edit-format/NOTICE` + `LICENSE` | Apache-2.0 (attribution + license text) | `aider@5dc9490` `LICENSE.txt` | non-executed; inline SPDX allowed here |
| `VENDOR-MANIFEST.md` | n/a (record) | pinned SHAs + per-file selection + REAL sha256 + MIT\|Apache-2.0 | the reproducibility contract |
| `LICENSE-AUDIT.md` (MODIFY) | n/a (record) | existing 6 MIT track blocks + NEW Apache-2.0 `fixture:` block(s) | gate strict-decodes both |

Pre-existing, PRESERVED (do NOT overwrite): `fixtures/python/.../wordy`, `fixtures/rust/.../leap` — repo-authored hermetic stubs, recorded in the manifest as "internal — repo license", NOT Exercism MIT.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Clone at pinned SHAs and vendor the MIT polyglot subset + per-track NOTICE sidecars</name>
  <files>bench/datasets/aider-polyglot/fixtures/python/exercises/practice/*, bench/datasets/aider-polyglot/fixtures/go/exercises/practice/*, bench/datasets/aider-polyglot/fixtures/rust/exercises/practice/*, bench/datasets/aider-polyglot/fixtures/python/NOTICE, bench/datasets/aider-polyglot/fixtures/go/NOTICE, bench/datasets/aider-polyglot/fixtures/rust/NOTICE</files>
  <read_first>
    - 99-RESEARCH.md §"Upstream Layout — Polyglot Benchmark" (track layout `<lang>/exercises/practice/<ex>/`; per-exercise shape; the NO-LICENSE-for-5/6-tracks finding; MIT provenance from exercism/<lang>@<sha>)
    - 99-RESEARCH.md §"Recommended Subset" → MIT polyglot subset: the 9 exercises `book-store bowling forth pig-latin poker react two-bucket variable-length-quantity wordy` × {python, go, rust}
    - 99-PATTERNS.md §"bench/datasets/aider-polyglot/fixtures/<lang>/..." (the shape loader.go requires; DO NOT overwrite wordy/leap)
    - bench/datasets/aider-polyglot/pin.go (RepoURL + PinnedSHA `7e0611e77b54e2dea774cdc0aa00cf9f7ed6144f` + isHexSHA1 discipline)
    - bench/datasets/aider-polyglot/loader.go:96-124 (loadExercise reads `<dir>/.meta/config.json` with files.solution/test/example — the shape the vendored dirs must match) and loader.go:81-89 (validatePathSegment)
    - bench/datasets/aider-polyglot/LICENSE-AUDIT.md:31-66 (the 6 MIT track blocks — copy each track's exercism/<lang>@<sha> into the matching NOTICE)
  </read_first>
  <action>
    FIRST verify network reachability for both pinned SHAs (`git ls-remote https://github.com/Aider-AI/polyglot-benchmark` returns 7e0611e77b54e2dea774cdc0aa00cf9f7ed6144f; this was confirmed reachable at research time). If unreachable, STOP and surface a BLOCKER per 99-RESEARCH.md §Environment Availability — do NOT fabricate fixture bytes.

    Clone polyglot-benchmark at the pinned SHA into a scratch dir (outside the repo tree): shallow init + fetch the exact 40-hex SHA + checkout, mirroring pin.go discipline (validate the SHA via the same 40-lowercase-hex rule isHexSHA1 enforces; NEVER clone a mutable ref/branch/fork). Use the SHA `7e0611e77b54e2dea774cdc0aa00cf9f7ed6144f` (matches pin.go PinnedSHA per D-pin).

    Copy the 9-exercise × {python, go, rust} subset from the clone into `bench/datasets/aider-polyglot/fixtures/<lang>/exercises/practice/<ex>/`, BYTE-IDENTICAL (no reformatting, no header injection). The 9 exercises: book-store, bowling, forth, pig-latin, poker, react, two-bucket, variable-length-quantity, wordy. Per exercise copy ONLY: `.meta/config.json`, `.meta/example.<ext>`, the stub (`<ex>.<ext>` for python/go, `src/lib.rs` for rust), the test file(s) (`<ex>_test.py` / `tests/<ex>.rs` / `<ex>_test.go`+`cases_test.go`), and toolchain files (`Cargo.toml` for rust, `go.mod` for go). EXCLUDE `.docs/` and `.approaches/` (instructional markdown not needed by the bench — keeps the tree lean per Pitfall 4).

    PRESERVE the pre-existing hermetic stubs: do NOT touch `fixtures/python/.../wordy` (already committed, repo-authored) and `fixtures/rust/.../leap`. The upstream `wordy` for python is one of the 9 — it MUST be vendored under a path that does not collide with the existing hermetic `python/.../wordy`. Per 99-RESEARCH.md Recommendation (a) + Open Question 2: KEEP the hermetic `python/wordy` at its pinned loader_test.go path; vendor the REAL upstream `wordy` for go and rust only (python's hermetic stub already occupies that slot), OR if a real upstream python/wordy is also vendored, place it so the existing hermetic `python/wordy` dir (with `authors:["fixture"]` config) is byte-unchanged — the loader_test.go assertion `Files.Solution==[wordy.py]` at `fixtures/python/exercises/practice/wordy` MUST keep passing. Record the conflict resolution in the manifest (Task 3).

    Write per-track NOTICE sidecars at `fixtures/python/NOTICE`, `fixtures/go/NOTICE`, `fixtures/rust/NOTICE`. Each NOTICE: states the track content is Exercism exercise data redistributed under MIT, cites the exact `exercism/<lang>@<sha>` from the matching LICENSE-AUDIT.md track block (python@d8886cad..., go@68c309ce..., rust@557695eb...), reproduces the MIT copyright + permission notice, and MAY carry an inline `SPDX-License-Identifier: MIT` line (NOTICE is non-executed — inline SPDX is safe ONLY here, never in the fixture source/test/config files). Per 99-RESEARCH.md §"SPDX Disposition", inline SPDX in executed `.py`/`.go`/`.rs`/`.json` fixtures is FORBIDDEN (mutates bench inputs, invalid in JSON, breaks byte-reproducibility).
  </action>
  <verify>
    <automated>go test ./bench/datasets/aider-polyglot/... -run Load</automated>
  </verify>
  <acceptance_criteria>
    - The 9-exercise × {python,go,rust} subset is committed under `fixtures/<lang>/exercises/practice/<ex>/`, byte-identical to polyglot-benchmark@7e0611e (no `.docs/`/`.approaches/`).
    - `loadExercise` succeeds on at least one newly-vendored exercise dir (config.json shape matches loader.go's Config struct).
    - The pre-existing hermetic `python/.../wordy` and `rust/.../leap` dirs are byte-unchanged; `go test ./bench/datasets/aider-polyglot/... -run Load` still passes (loader_test.go's pinned `fixtures/python/.../wordy` + `Files.Solution==[wordy.py]` assertions hold).
    - `fixtures/{python,go,rust}/NOTICE` exist, each citing the matching `exercism/<lang>@<sha>` and reproducing the MIT permission notice.
    - NO `.py`/`.go`/`.rs`/`.json` fixture file under `<lang>/exercises/practice/` contains an injected `SPDX-License-Identifier` line.
  </acceptance_criteria>
  <done>The MIT polyglot subset + per-track NOTICE sidecars are committed byte-identical to upstream @ 7e0611e; the loader test still passes; no executed fixture carries inline SPDX.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor the Apache-2.0 aider edit-format subset + NOTICE + LICENSE under fixtures/_aider-edit-format/</name>
  <files>bench/datasets/aider-polyglot/fixtures/_aider-edit-format/languages/python/test.py, bench/datasets/aider-polyglot/fixtures/_aider-edit-format/languages/go/test.go, bench/datasets/aider-polyglot/fixtures/_aider-edit-format/languages/rust/test.rs, bench/datasets/aider-polyglot/fixtures/_aider-edit-format/languages/java/test.java, bench/datasets/aider-polyglot/fixtures/_aider-edit-format/languages/javascript/test.js, bench/datasets/aider-polyglot/fixtures/_aider-edit-format/search-replace-sample.txt, bench/datasets/aider-polyglot/fixtures/_aider-edit-format/NOTICE, bench/datasets/aider-polyglot/fixtures/_aider-edit-format/LICENSE</files>
  <read_first>
    - 99-RESEARCH.md §"Upstream Layout — Aider Tool Repo" (LICENSE.txt = Apache-2.0, NO root NOTICE; edit-format fixtures at `tests/fixtures/`; the size table — `chat-history-search-replace-gold.txt` ~27,810 lines, `chat-history.md` ~99,961 lines DO NOT vendor whole; `tests/fixtures/languages/<lang>/test.<ext>` small)
    - 99-RESEARCH.md §"Recommended Subset" → Apache-2.0 aider subset (`tests/fixtures/languages/{python,go,rust,java,javascript}/test.<ext>` + a trimmed ≤~150-line SEARCH/REPLACE gold excerpt)
    - 99-PATTERNS.md Open-Question-1-RESOLVED note (loader has NO discovery walk; `_aider-edit-format/` underscore sibling is SAFE — the loader never enumerates `fixtures/`)
    - 99-RESEARCH.md §"NOTICE/Attribution" + Pitfall 2 (Apache-2.0 ONLY for the `_aider-edit-format/` subtree; never stamp Apache-2.0 on the MIT `<lang>/exercises/practice/` files)
  </read_first>
  <action>
    Clone aider at the pinned SHA `5dc9490bb35f9729ef2c95d00a19ccd30c26339c` into a scratch dir (same shallow fetch-by-SHA + checkout discipline as Task 1; validate the 40-hex SHA; never a mutable ref). Confirmed reachable at research time; if unreachable, STOP and surface a BLOCKER (do not fabricate).

    Copy BYTE-IDENTICAL the 5 small `tests/fixtures/languages/{python,go,rust,java,javascript}/test.<ext>` files into `fixtures/_aider-edit-format/languages/<lang>/test.<ext>`. These are the small Apache-2.0 edit-format targets the Phase 100 EDIT bench applies edits against.

    Produce a TRIMMED excerpt of `tests/fixtures/chat-history-search-replace-gold.txt` (≤~150 lines; 2–4 representative `@@@ SEARCH: <file> @@@ … @@@@@@` blocks) and save it as `fixtures/_aider-edit-format/search-replace-sample.txt`. Do NOT vendor the ~27,810-line full gold or the ~99,961-line `chat-history.md` (Pitfall 4 — oversized/unauditable). The excerpt is a non-executed `.txt`, so an inline `SPDX-License-Identifier: Apache-2.0` comment line at the top is permitted (and recommended) here.

    Write `fixtures/_aider-edit-format/LICENSE` = a byte copy of aider's `LICENSE.txt` (the Apache License 2.0 text) at the pinned SHA. Write `fixtures/_aider-edit-format/NOTICE` stating: these edit-format fixtures are derived from `Aider-AI/aider@5dc9490` under Apache-2.0; aider ships NO upstream NOTICE (so §4(d) propagation does not apply); the obligation satisfied is attribution + the bundled Apache-2.0 LICENSE text + the recorded disposition. The NOTICE MAY carry an inline `SPDX-License-Identifier: Apache-2.0` line (non-executed sidecar). Place EVERYTHING under the underscore-prefixed `_aider-edit-format/` sibling so the (walk-free) loader never picks it up — verified safe in 99-PATTERNS.md.

    DISPOSITION DISCIPLINE: do NOT inject SPDX into the executed `test.<ext>` source files (they may be run/compiled by the Phase 100 bench). Their Apache-2.0 disposition lives in the manifest (Task 3) + the LICENSE-AUDIT.md fixture block (Task 3) + this subtree's NOTICE/LICENSE — never inline in the `.py`/`.go`/`.rs`/`.java`/`.js` bytes.
  </action>
  <verify>
    <automated>test -f bench/datasets/aider-polyglot/fixtures/_aider-edit-format/LICENSE && grep -q "Apache License" bench/datasets/aider-polyglot/fixtures/_aider-edit-format/LICENSE && test -f bench/datasets/aider-polyglot/fixtures/_aider-edit-format/NOTICE</automated>
  </verify>
  <acceptance_criteria>
    - The 5 `languages/<lang>/test.<ext>` files are committed byte-identical to aider@5dc9490 `tests/fixtures/languages/*`.
    - `search-replace-sample.txt` is a trimmed ≤~150-line excerpt (NOT the full ~28k-line gold; `chat-history.md` is absent from the tree).
    - `_aider-edit-format/LICENSE` contains the Apache License 2.0 text; `_aider-edit-format/NOTICE` cites `Aider-AI/aider@5dc9490` + Apache-2.0.
    - The whole subtree is under the underscore-prefixed `_aider-edit-format/` (loader-walk-safe).
    - `go test ./bench/datasets/aider-polyglot/...` still passes (the loader does not pick up the Apache subtree).
    - NO executed `test.<ext>` source file carries an injected inline SPDX line; Apache-2.0 disposition is recorded in the manifest/audit/NOTICE only.
  </acceptance_criteria>
  <done>The Apache-2.0 aider edit-format subset (5 small language files + a trimmed SEARCH/REPLACE excerpt + NOTICE + LICENSE) is committed under `_aider-edit-format/`, byte-identical for verbatim files, loader-walk-safe, with no inline SPDX in executed bytes.</done>
</task>

<task type="auto">
  <name>Task 3: Write VENDOR-MANIFEST.md (real sha256) and extend LICENSE-AUDIT.md with the Apache-2.0 fixture block</name>
  <files>bench/datasets/aider-polyglot/VENDOR-MANIFEST.md, bench/datasets/aider-polyglot/LICENSE-AUDIT.md</files>
  <read_first>
    - 99-RESEARCH.md §"SPDX Disposition" item 1 (VENDOR-MANIFEST.md = upstream URL + pinned SHA + exact selection + per-file sha256; mirrors pin.go + license_sha256 discipline)
    - 99-PATTERNS.md §"VENDOR-MANIFEST.md (NEW)" (mirror pin.go SHA discipline + bench/LICENSES.md pipe-table shape; REAL crypto/sha256 digests, NOT 40-zero placeholders — the gate verifies them)
    - 99-PATTERNS.md §"LICENSE-AUDIT.md (MODIFY)" (existing MIT block shape at LICENSE-AUDIT.md:31-66; ADD parallel Apache-2.0 blocks keyed by top-level `fixture:`; the gate's new hasTopLevelFixtureKey + FixtureLicense struct pre-filter/decode them)
    - 99-RESEARCH.md §"Code Examples" → FixtureLicense struct fields (`fixture`, `source_repo`, `license`, `notice_path`, `license_path`) — the manifest/audit keys MUST match the struct the Plan 02 gate strict-decodes (KnownFields(true) errors on any unknown key)
    - bench/datasets/aider-polyglot/LICENSE-AUDIT.md (the existing 6 MIT track blocks — leave them UNCHANGED; the gate's hasTopLevelTrackKey pre-filter + TrackLicense struct still decode them)
    - bench/LICENSES.md:12-16 (the pipe-delimited audit-table shape; note its 40-zero placeholder digests — the manifest must use REAL digests)
  </read_first>
  <action>
    Compute the REAL `crypto/sha256` hex digest of EVERY vendored file committed in Task 1 + Task 2 (all `<lang>/exercises/practice/<ex>/...` files, all `_aider-edit-format/...` files including NOTICE/LICENSE, and the per-track NOTICE sidecars). Use `sha256sum` or `go run` — the digests MUST be real (the Plan 02 gate recomputes them and fails on mismatch; 40-zero placeholders à la bench/LICENSES.md would FAIL the gate, per 99-PATTERNS.md).

    Write `bench/datasets/aider-polyglot/VENDOR-MANIFEST.md` as the reproducibility record:
    - A header naming the two upstream repos + pinned SHAs: `polyglot-benchmark@7e0611e77b54e2dea774cdc0aa00cf9f7ed6144f` (MIT, matches pin.go) and `aider@5dc9490bb35f9729ef2c95d00a19ccd30c26339c` (Apache-2.0).
    - A pipe-delimited table (mirror bench/LICENSES.md shape) with columns: `path` (repo-relative under fixtures/), `sha256` (REAL digest), `license` (MIT | Apache-2.0 | internal — repo license), `upstream_provenance` (`exercism/<lang>@<sha>` for MIT exercises / `Aider-AI/aider@5dc9490` for Apache fixtures / `(repo-authored hermetic stub)` for the 2 pre-existing stubs).
    - One row per vendored file. The exact (language, exercise) selection matrix is recorded here so the subset is reproducible (the Phase 100 baseline depends on a stable subset).
    - Mark the 2 pre-existing hermetic stubs (`python/.../wordy`, `rust/.../leap` files) DISTINCTLY as `internal — repo license` / `(repo-authored hermetic stub)` — they are NOT Exercism MIT (their config.json carries `authors:["fixture"]`).
    - Decide the manifest's covered-tree scope to match what the Plan 02 gate walks: the manifest must cover EVERY on-disk file under the gate's `--tree` root (default `fixtures/`). State the covered root explicitly in the header so Plan 02 wires the same root.

    Extend `bench/datasets/aider-polyglot/LICENSE-AUDIT.md`: leave the existing 6 MIT `track:` blocks byte-unchanged. APPEND Apache-2.0 fixture block(s) keyed by a top-level `fixture:` key, with EXACTLY these keys (matching the Plan 02 FixtureLicense struct — any extra key fails strict decode): `fixture` (e.g. `_aider-edit-format`), `source_repo` (`Aider-AI/aider@5dc9490bb35f9729ef2c95d00a19ccd30c26339c`), `license` (`Apache-2.0`), `notice_path` (`fixtures/_aider-edit-format/NOTICE`), `license_path` (`fixtures/_aider-edit-format/LICENSE`). Update the audit's header prose to describe the now-DUAL disposition (MIT track blocks + Apache-2.0 fixture block) and that the gate also runs a manifest-vs-disk sha256 walk. Do NOT cite MIT provenance from polyglot-benchmark (it ships no LICENSE for 5/6 tracks) — keep the exercism/<lang>@<sha> provenance the existing blocks use.

    NOTE for the executor: the Plan 02 gate is what enforces this — but the keys/paths written here MUST exactly match the FixtureLicense struct + the notice_path/license_path files committed in Task 2, or the Wave-2 gate will (correctly) go RED.
  </action>
  <verify>
    <automated>grep -q "7e0611e77b54e2dea774cdc0aa00cf9f7ed6144f" bench/datasets/aider-polyglot/VENDOR-MANIFEST.md && grep -q "5dc9490bb35f9729ef2c95d00a19ccd30c26339c" bench/datasets/aider-polyglot/VENDOR-MANIFEST.md && grep -q "^fixture:" bench/datasets/aider-polyglot/LICENSE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `VENDOR-MANIFEST.md` exists, names both pinned SHAs (`7e0611e...`, `5dc9490...`), and has one row per vendored file with a REAL (non-zero-placeholder) sha256 + MIT|Apache-2.0|internal disposition + upstream provenance.
    - The 2 pre-existing hermetic stubs are recorded distinctly as `internal — repo license` (not Exercism MIT).
    - `LICENSE-AUDIT.md` retains its 6 MIT `track:` blocks byte-unchanged AND carries ≥1 Apache-2.0 block keyed by a top-level `fixture:` key whose ONLY keys are `fixture`/`source_repo`/`license`/`notice_path`/`license_path`.
    - `notice_path` / `license_path` in the fixture block resolve to the files committed in Task 2 (`fixtures/_aider-edit-format/NOTICE`, `fixtures/_aider-edit-format/LICENSE`).
    - The manifest header states the covered-tree root (default `fixtures/`) so Plan 02 wires the same `--tree`.
  </acceptance_criteria>
  <done>VENDOR-MANIFEST.md records every vendored file with a real sha256 + disposition + provenance; LICENSE-AUDIT.md carries the dual MIT-track + Apache-2.0-fixture disposition with keys matching the Plan 02 gate's FixtureLicense struct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub → vendored tree | untrusted upstream bytes cross into the committed tree at vendor-time (one-time clone) |
| vendored fixture files → Phase 100 toolchain | the bench executes vendored solution/test bytes against real toolchains (downstream) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | upstream clone (mutable-ref swap) | mitigate | Clone ONLY the 40-hex pinned SHAs (`7e0611e...`, `5dc9490...`); validate the SHA via the same lowercase-40-hex rule isHexSHA1 enforces (pin.go); fetch-by-SHA + checkout, never a branch/tag/fork |
| T-99-02 | Tampering | copying upstream files into the tree | mitigate | Reject any path segment that `filepath.Clean` rewrites / contains a separator / leads with `.` (validatePathSegment precedent, loader.go:81-89); no `..`/symlink escape out of the fixtures root |
| T-99-03 | Tampering | silently swapped fixture bytes | mitigate | Record a REAL per-file `crypto/sha256` in VENDOR-MANIFEST.md (the Plan 02 gate recomputes + fails closed on mismatch) |
| T-99-04 | Repudiation | wrong-license redistribution (Apache-2.0 stamped on MIT fixtures) | mitigate | MIT disposition + NOTICE→`exercism/<lang>@<sha>` for `<lang>/exercises/practice/`; Apache-2.0 disposition + NOTICE + LICENSE copy ONLY under `_aider-edit-format/`; dual-disposition audit |
| T-99-05 | Tampering | byte-mutation of executed fixtures via inline SPDX | mitigate | NO inline SPDX in any `.py`/`.go`/`.rs`/`.java`/`.js`/`.json` fixture the bench executes; disposition lives in manifest/audit/NOTICE only |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages — it vendors DATA (test fixtures), not executable dependencies (per 99-RESEARCH.md §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go vet ./...` clean.
- `go test ./bench/datasets/aider-polyglot/...` passes (loader_test.go's pinned `wordy` path + the Apache subtree being loader-walk-safe).
- The 2 pre-existing hermetic stubs are byte-unchanged (no diff under `fixtures/python/.../wordy`, `fixtures/rust/.../leap`).
- No executed fixture under `<lang>/exercises/practice/` or `_aider-edit-format/languages/` carries an injected inline `SPDX-License-Identifier` line: `! grep -rl "SPDX-License-Identifier" bench/datasets/aider-polyglot/fixtures/*/exercises bench/datasets/aider-polyglot/fixtures/_aider-edit-format/languages`.
- VENDOR-MANIFEST.md + LICENSE-AUDIT.md exist with the dual disposition (the Plan 02 gate is the authoritative `make verify-licenses` check in Wave 2).
</verification>

<success_criteria>
- VENDOR-01: the 9-exercise MIT polyglot subset (× python/go/rust) is committed under `fixtures/<lang>/exercises/practice/`, byte-identical to polyglot-benchmark@7e0611e, with per-track NOTICE sidecars and a VENDOR-MANIFEST.md recording the exact selection.
- VENDOR-02: the Apache-2.0 aider edit-format subset (small `languages/*` + trimmed SEARCH/REPLACE excerpt + NOTICE + Apache-2.0 LICENSE copy) is committed under `_aider-edit-format/`, producing a documented mixed-license tree.
- Byte-reproducible + offline: no inline SPDX in executed fixtures; the tree is self-sufficient (no run-time network).
- Pre-existing hermetic stubs preserved (loader_test.go green).
</success_criteria>

<output>
Create `.planning/phases/99-vendored-aider-fixtures-mixed-license-gate/99-01-SUMMARY.md` when done.
</output>