---
author: architect
responsible: architect
milestone: v2.10
status: passed
audited: 2026-06-30
audit_method: goal-backward, grounded in re-reading the code system (not the SUMMARYs)
parent_artifacts:
  - .planning/milestones/v2.10-REQUIREMENTS.md
  - .planning/milestones/v2.9-MILESTONE-AUDIT.md
---

# MILESTONE-AUDIT — v2.10 trace_data_flow verb (DATA_FLOWS read surface)

## Scope audited

v2.10 as roadmapped: a dedicated `trace_data_flow` verb consuming v2.9's
DATA_FLOWS substrate via a new read-only `DataFlowReachabilityAccessor`
(3 phases: 127 primitive, 128 handler, 129 atomic frozen-51 surface bump).

## Verdict: PASSED

The built system matches the milestone's stated intent. Confirmed by re-reading
the code + running the gates, not the phase summaries.

## Goal-backward grounding (code reality, verified at HEAD)

| Intent | Code reality | Verdict |
|---|---|---|
| A verb answers "from this seed param, what's reachable via DATA_FLOWS?" | `handleTraceDataFlow` (tools_trace_data_flow.go) → `ReachableFrom` BFS → `[]ReachableSymbol{symbol_id, hops}` | ✅ |
| ReachableFrom over the v2.9 substrate | `semP1DataFlowReachabilityAdapter.ReachableFrom` bulk-loads `QueryAllDataFlowEdges` (one query) → in-memory BFS (hop-cap, visited, deterministic) | ✅ |
| Seed = a PARAMETER (function seed can't reach param-anchored edges) | `TestDataFlowReachability_FunctionSeedEmpty` proves a function seed reaches only itself; `MultiHop` proves src.param → sink.param @ hop 2 | ✅ |
| Honest empty ≠ fallback | `TestTraceDataFlow_HonestEmptyNoFallback`: empty reachable set has NO fallback_reason; `NilAccessorFallback` returns `data_flow_lookup_unavailable` | ✅ |
| 51st frozen verb, all count-sensitive gates atomic | VerbToolNames()==51; anchor bumped 50→51; render-class + querySet + graphReaderVerbs; verbs_gen + reference regen; README (docgen) regen | ✅ |
| D-09 read-only on graph state | tools_trace_data_flow.go in `gatedHandlerFiles` + INVARIANT header; handler only reads via the accessor seam | ✅ |

## Red-team fold (`agent://RedTeamV210`, PROCEED-WITH-FIXES)

- **B1 (anchor absent) → REFUTED by direct read.** The `require.Equal(t,50,…)`
  anchor IS real (`reference_contract_test.go:58`); the red-team's grep regex
  failed. Bumped to 51.
- **B2 (4 count-sensitive gates) → CONFIRMED + folded:** render-class map,
  graphReaderVerbs, querySet, D-09 gatedHandlerFiles. (Profile goldens: verified
  NOT needed — semantic-graph verbs aren't golden-tracked.)
- **B3 (function-seed empty) → finding confirmed, recommended fix REFUTED.** No
  tree-sitter CONTAINS function→param at HEAD → param-only seed is the only
  viable design (kept).
- **M1 (bulk query) → adopted** (`QueryAllDataFlowEdges`); **M2 (3-phase split)
  → adopted**; **M3 (accessor rationale) → corrected.**

## Quality gates

- `go build ./...` clean; cli/daemon/skill-semantic/store **4 pkgs ok**;
  **`make vet` (go vet + 8 vettools) clean**; helix-cligen/refgen/docgen `--check`
  green at 51.
- Invariants: zero new Go deps (`go.mod`/`go.sum` byte-unchanged); no schema
  migration (the bulk query is a new SELECT).
- Anti-vacuity: honest-empty-is-not-fallback + broken-hop unreachable, both green.

## Honest scope

`trace_data_flow` answers syntactic param→param reachability over DATA_FLOWS
(v2.9's case-1 substrate). It is NOT taint analysis (no in-body origins / field
flow / sanitization). Seed is a PARAMETER; function-seed support deferred (needs
`OwnerSymbolID` population / a persisted func→params index — future milestone).

## Ship state

Working tree carries v2.10's 20 files (16 modified + 4 new); **not yet committed**
(user's call). All gates green at 51.
