---
author: codebase-archaeologist
responsible: architect
milestone: v2.13
status: refreshed
refreshed: 2026-07-01
refresh_method: grounded against real code (helix daemon emission + dataflow engine), not the SUMMARYs; self-audit via `anvil-cc self-audit`
parent_artifacts:
  - .planning/milestones/v2.13-ROADMAP.md
  - .planning/milestones/v2.13-REQUIREMENTS.md
  - docs/edge-types.md
  - .planning/codebase/STRUCTURE.md
---

# DOCS-REFRESH — v2.13 True intraprocedural DATA_FLOWS (in-body origins)

Durable-documentation refresh for the v2.13 milestone close. Read-only w.r.t.
source (no `.go` edits, no commit); docs + planning artifacts only.

## What v2.13 shipped (the documented facts, all code-grounded)

- **`internal/semantic/dataflow/`** — the case-1 intraprocedural flow engine now
  models **in-body origins**: the return of an in-body call
  (`y := producer(); sink(y)`) is a flow origin, not just params.
  Types (`summary.go`): `Origin{Param,Callee}`, `InBodyFlow{Producer,Consumer,ArgPos}`,
  `Summary.InBodyFlows`. Leaf pkg (stdlib + tree-sitter); invoked from the shared
  `extract.FingerprintBody` seam (`fingerprint.go:54`) → `FlowSummary` rides on
  `ExtractedSymbol` (`fact.go:127`), so all 11 providers inherit it edit-free.
- **`internal/daemon/semantic_similarity_edges.go`** emits two new `DATA_FLOWS`
  edge kinds beyond v2.9's `def_use`:
  - `inBodyDataFlowEdges` → Source `def_use_inbody`, `producer.function →
    consumer.param`, conf 0.50 (line 595-605).
  - `returnBridgeEdges` → Source `def_use_return`, `param → enclosing-function`,
    conf 0.55 (line 653-663) — the multi-hop return-bridge connector.
  Both wired in `factsFromExtracted` (`semantic_wiring.go:2553-2554`) **strictly
  after** `dataFlowEdges` (`:2546`) — M1 dense-EdgeID ordering constraint.
- **`langFromExt`** (`semantic_wiring.go:1838`) now maps `.rs/.kt/.kts/.php/.rb →
  rust/kotlin/php/ruby` (Path B): all 11 languages index + emit `DATA_FLOWS`
  through the real daemon; their type resolvers remain nil-stubs (no
  `has_type`/`uses_type`).
- Proven 11/11 E2E through the shipped `helix` binary; function-seeded multi-hop
  via `trace-data-flow`. Zero new Go deps; no schema migration.

## 1. `docs/edge-types.md` — VERIFIED (no gap)

Verified line-by-line against the real emission code; **complete and accurate for
v2.13, no fix needed**:

- **3 Source markers, exact confidences match code:**
  - `def_use` (v2.9, conf ~0.55) — caller.param→callee.param — matches
    `dataFlowEdges` conf `0.55` (`semantic_similarity_edges.go:526`). ✅
  - `def_use_inbody` (v2.13, conf ~0.50) — producer.function→consumer.param —
    matches `inBodyDataFlowEdges` conf `0.50` (`:602`). ✅
  - `def_use_return` (v2.13, conf ~0.55) — param→enclosing-function — matches
    `returnBridgeEdges` conf `0.55` (`:660`). ✅
- **In-body-origins-modeled note** present (§`data_flows`, "the return value of an
  in-body call … is an origin"). ✅
- **All-11 coverage ledger** present and correct (Go, TS, Java, C#, Python, C, C++,
  Rust, Kotlin, PHP, Ruby proven E2E; nil-stub resolver caveat stated). Matches
  `langFromExt` (`semantic_wiring.go:1838-1866`). ✅
- **Function-seed note** present ("a function seed now mechanically reaches in-body
  targets over `def_use_inbody`; full documented-contract support is a
  fast-follow"). Honest about the deferred verb-contract. ✅
- **Anti-mis-bind** (`nameCount==1`) documented for all variants; matches the
  guards in all three emitters. ✅
- **Family table** (line 21) correctly attributes `data_flows` to
  `dataFlowEdges`/`inBodyDataFlowEdges`/`returnBridgeEdges`. ✅

Conclusion: **edge-types.md is verified accurate for v2.13** — the engineer's
FLOW-06e update stands; nothing edited.

## 2. `.planning/codebase/STRUCTURE.md` — v2.13 subsystem entry ADDED

Added a scope-limited **"v2.13 Addendum — Intraprocedural Data-Flow Subsystem
(current Go tree)"** documenting `internal/semantic/dataflow/` (types, `AnalyzeFlow`
entry point, leaf-pkg boundary, `FingerprintBody` seam) and the daemon emission
(`dataFlowEdges`/`inBodyDataFlowEdges`/`returnBridgeEdges` with sources,
confidences, anti-mis-bind, strict-after ordering, and the `langFromExt` widening).

**Also added a staleness banner** at the top of STRUCTURE.md (see §4) because the
addendum is the only current-tree section in an otherwise wholly stale file — a
reader must not mistake the Python layout below it for the shipped codebase.

## 3. `.planning/codebase/ARCHITECTURE.md` — intentionally NOT edited

ARCHITECTURE.md has **no semantic-graph / edge-intelligence section** — it is
entirely Python-Serena layers (MCP factory, `SerenaAgent`, `SolidLanguageServer`,
Jinja prompt templating). Per the refresh scope, a v2.13 line was **not forced**
into a document with no home for it. Its staleness is captured in the finding below.

## 4. `anvil-cc self-audit` — CLEAN (4/4 probes)

Run against the deployed substrate (`~/.claude/anvil`, target_kind `deployed`);
`anvil-cc self-audit --json`:

| Probe layer | Status | Covers |
|---|---|---|
| `prompt-integrity` | ok — all 20 agent defs compose the SMTC discipline | uncomposed agent defs |
| `spec-snapshot` | ok — RECORDS/foundations/envelope deployed | stale spec snapshot |
| `record-type-integrity` | ok — RECORDS.md declares 9 kinds, documents 9 | record-type drift |
| `degraded-substrate-honesty` | ok — an agent def carries the cold-LSP tier-cap honesty instruction | cold-LSP overclaim |

**4 probes run, 0 findings — clean** (`"ok": true`; by_severity all 0). No harness
health issues. (Running the audit from the helix repo CWD reports "neither deployed
nor source substrate — nothing to probe"; the substrate is the anvil harness at
`~/.claude/anvil`, so the audit is correctly run there, not against the Go tree.)

## 5. FINDING — the `.planning/codebase/` map is grossly stale (worse than assumed)

**Severity: HIGH (documentation-integrity). Recommendation: dedicated re-mapping
pass, NOT folded into a milestone close.**

The assignment scoped this as "~13 milestones stale (Jun 13, pre-v2.8)." The actual
state is **worse**: the map does not describe an early-Go Helix — it describes the
**pre-Go Python `serena` codebase** that was *removed from the tree*.

- **Content:** all 7 files (`ARCHITECTURE.md`, `STRUCTURE.md`, `CONCERNS.md`,
  `CONVENTIONS.md`, `INTEGRATIONS.md`, `STACK.md`, `TESTING.md`) carry
  **"Analysis Date: 2026-04-07"** and document `src/serena/`, `src/solidlsp/`,
  `SerenaAgent`, `SolidLanguageServer`, Python `*_tools.py`, Jinja2 prompt
  templating, `.serena/` — none of which exist in the current tree.
- **Provenance:** the sole `git log` entry touching `.planning/codebase/` is
  `33998609 docs: map existing codebase` dated **2026-04-07**. The "Jun 13" is only
  a filesystem mtime (a bulk touch/copy); the content and its authored analysis
  date are April 7. No commit has updated the map since.
- **Drift span:** the repo renamed to `helix` at v1.9, removed the Python legacy
  tree before v1.12, and is now at **v2.13** — a Go-native single binary
  (`cmd/helix`, `internal/**`). The map missed the **entire v1.9→v2.13 Go rewrite**
  (roughly the whole current architecture, including the v2.8→v2.13 semantic-graph
  edge line: SIMILAR_TO/STRUCTURAL_TWIN/SEMANTICALLY_RELATED, DATA_FLOWS,
  RESOLVES_TO/type resolvers).

**Why NOT silently rebuilt here:** a faithful re-map of a ~13-milestone,
Go-native, 4-layer daemon architecture is a multi-file archaeology effort in its
own right (STRUCTURE + ARCHITECTURE + STACK + CONVENTIONS + INTEGRATIONS +
CONCERNS + TESTING, all Go). Attempting it inside a milestone-close docs refresh
would either be shallow-and-wrong or would silently balloon scope. The honest move
is to (a) land the accurate v2.13 dataflow entry, (b) flag every reader with a
banner, and (c) route the rebuild to a dedicated pass.

**Actions taken to contain the risk now:**
1. Added a **staleness banner** to `STRUCTURE.md` (top) stating the body describes
   the removed Python tree as of 2026-04-07 and pointing here.
2. Landed the v2.13 dataflow subsystem as a clearly-scoped current-tree addendum.

**Recommended follow-up (route to a dedicated mapping milestone/phase):**
- Spawn a codebase-mapper pass to regenerate all 7 `.planning/codebase/` files
  against the current Go tree (Layer 0–3 architecture per `CLAUDE.md`, the
  `internal/semantic/` graph subsystem, the `internal/kernel/` LSP kernel, the
  52-language registry, the frozen `helix` verb surface).
- Until then, treat `.planning/codebase/*` (except the v2.13 addendum + banner in
  STRUCTURE.md) as **historical Python-Serena reference, not current fact**.

## Summary

- `docs/edge-types.md`: **verified accurate** for v2.13 (3 Source markers, in-body
  note, all-11 ledger, function-seed note all match real emission code) — no edit.
- `.planning/codebase/STRUCTURE.md`: **v2.13 dataflow subsystem + emission entry
  added**, plus a staleness banner.
- `.planning/codebase/ARCHITECTURE.md`: not edited (no semantic-graph section to
  host the line); staleness captured in finding.
- `anvil-cc self-audit`: **4 probes, 0 findings, clean.**
- **Map-staleness finding:** the codebase map describes the *removed Python
  codebase* (2026-04-07), missing the entire Go rewrite → dedicated re-mapping pass
  recommended; not silently rebuilt.
