---
author: qa
responsible: qa
milestone: v2.13
status: complete
verdict: passed
created: 2026-07-01
parent_artifacts:
  - .planning/milestones/v2.13-MILESTONE-AUDIT.md
  - .planning/phases/138-origin-space-widening-unions/138-VERIFICATION.md
  - .planning/phases/139-inbody-returnbridge-emission-go-proof/139-VERIFICATION.md
  - .planning/phases/140-all11-realbinary-e2e-multihop-docs/140-VERIFICATION.md
---

# v2.13 VERIFICATION — independent qa gate (milestone-close)

## Verdict: passed

Independent qa verification of the v2.13 milestone-close, grounded in **my own**
fresh build/test/E2E runs + direct reads of the load-bearing code — NOT the phase
SUMMARYs and NOT the architect's MILESTONE-AUDIT prose. This is the standing-qa
independence pass the autonomous run's ephemeral per-phase verifiers did not
constitute (the lead spawned those; the lead also authored the audit — this closes
that judge≠doer gap before commit).

## Why this pass exists

The autonomous run's per-phase verifications were performed by task-subagents the
architect spawned, and the MILESTONE-AUDIT was self-authored by the architect (the
lead). A milestone about to be committed deserves one verdict from the independent
qa seat. It also fixes a concrete ledger gap: the run produced prose VERIFICATION.md
files with **no typed `gate_result` records**, so `anvil-cc measure refusal-rate`
read `0 gates`. Those records are qa's to author — emitted below.

## Acceptance handshake — PASSED

- **Inputs complete:** all 3 phases carry CONTEXT + PLAN + SUMMARY + VERIFICATION;
  close carries MILESTONE-AUDIT + SECURITY-REVIEW + DOCS-REFRESH. Confirmed on disk.
- **PLAN↔SUMMARY↔code consistency:** the two new emission functions and the M3 test
  match their contracts against real code (read directly — see below), not just the
  SUMMARY's claims.
- **Diff hygiene:** `git diff` is coherent — only v2.13 files; no stray edits leaked.
- Inputs verifiable and consistent → no refusal.

## Independent verification method + evidence (my own runs)

All runs on a freshly-built binary/tree at verification time (`-count=1`, uncached):

| Check | Command | Result |
|---|---|---|
| Build | `go build ./cmd/helix` | clean (exit 0) |
| Vet | `go vet ./internal/semantic/dataflow/... ./internal/daemon/... ./internal/skill/semantic/...` | clean (exit 0) |
| Unit + emission | `go test -count=1 ./internal/semantic/dataflow/... ./internal/daemon/... ./internal/skill/semantic/...` | 3 pkgs ok |
| 138 all-11 matrix | `go test -count=1 -v -run TestAnalyzeFlow_AllElevenMatrix ./internal/semantic/dataflow/` | 11/11 subtests PASS, **0 skip** |
| 139 emission/multi-hop | `go test -count=1 -v -run 'InBody|ReturnBridge|Distinctness|DefUseSetNonRegression|MultiHop|FunctionSeed' ./internal/daemon/` | all PASS incl. `MultiHop_BrokenHop` + `FunctionSeedReachesInBody` |
| 140 fresh-binary E2E | `HELIX_BIN=/tmp/helix-qa-v213 go test -count=1 -v -run TestCLI_E2E_InBody ./internal/cli/` | 11/11 subtests PASS + `MultiHop` PASS, **0 skip** |
| Deps | `git diff --stat go.mod go.sum` | empty (zero new deps) |
| Schema | `git diff --stat internal/semantic/store/` | empty (no migration) |

## Code-reality checks (read the code, not the SUMMARY)

- **`inBodyDataFlowEdges`** (semantic_similarity_edges.go:547-609): emits
  function→parameter, `Source:"def_use_inbody"`, Conf 0.50, Weight 0.5; anti-mis-bind
  on BOTH producer + consumer (`nameCount!=1` → skip); endpoint-0 gate; self-guard
  (`dstParam==prodNode`); directed dedup on `(prodNode,dstParam)`; iterates sorted
  `withFlow` + `InBodyFlows` slice, maps LOOKUP-only. **Matches contract exactly.**
- **`returnBridgeEdges`** (semantic_similarity_edges.go:621-667): emits
  parameter→function, `Source:"def_use_return"`, Conf 0.55, Weight 0.5; `!pf.Returns`
  gate; self-guard; directed dedup; `nodeToParams` LOOKUP-only with an explicit
  "never range the map" comment (M4 determinism). **Matches contract + M4 exactly.**
- **M1 append order** (semantic_wiring.go:2541-2542): both new passes append STRICTLY
  after `dataFlowEdges` (:2534) and before `resolveTypeEdges` (:2551) — EdgeIDs
  unshifted. Confirmed on disk.
- **M3 test repurpose** (dataflow_reachability_test.go:138-182):
  `TestDataFlowReachability_FunctionSeedReachesInBody` asserts the **corrected** truth
  (a function seed reaches its in-body target at hop 1 over a `def_use_inbody` edge;
  `t.Fatalf` if not) — it does NOT re-encode the old false emptiness invariant. The
  now-false invariant is corrected, not papered over.
- **`langFromExt`** (semantic_wiring.go:1856-1863): `.rs→rust`, `.kt/.kts→kotlin`,
  `.php→php`, `.rb→ruby` (Path B); doc-comment corrected (no "stay '' until v2.13").

## Anti-vacuity confirmed non-theater

The per-phase verifiers each ran an independent revert-and-fail with a DIFFERENT
mutation than the engineer's (138: m2 whitelist→naive findFirst → selector/multi-call
guards RED; 139: return-bridge gate inverted → `_ReturnBridge` + `_MultiHop` RED, sha256
restore; 140: `.rb` arm removed + binary rebuilt → ruby subtest RED). I re-ran and
confirmed the current (restored) state is green, and read the guard bodies to confirm
they assert real behavior (`MultiHop_BrokenHop` requires unreachability;
`Distinctness` requires zero cross-contamination of the def_use vs def_use_inbody sets).

## Confidence tier

**HIGH.** Grounded in fresh test *execution* + direct reads of the load-bearing
emission code and M3 test — not grep, not SUMMARY-trust. SMTC is not available in
this harness; the evidence tier rests on real `go test` runs and code reads, which is
legitimate HIGH for a Go project without the semantic daemon. No claim here rests on
grep alone. Corroboration is multiply-independent (3 per-phase passes + architect
audit + this qa pass), so the dual context-blind spawn was not spent.

## Gaps

None blocking. Two non-blocking follow-ons already routed to the architect and
recorded in the audit: (1) the `.planning/codebase/` map still describes the removed
pre-Go Python serena tree (HIGH doc-integrity; contained via banner + addendum,
warrants a dedicated re-mapping milestone); (2) full function-seed support in the
`trace_data_flow` verb contract (fast-follow). Neither affects the v2.13 deliverable.

## Records

```yaml
gate_result:
  record_author: qa
  accountable_owner: qa
  id: GV213-138
  kind: phase_verification
  gates_subject: phase-138-origin-space-widening-unions
  evaluator: qa
  verdict: pass
  status: accepted
  method:
    - ran go test -count=1 -run TestAnalyzeFlow_AllElevenMatrix (11/11, 0 skip)
    - ran the m2 RED guards + anti-vacuity + determinism tests fresh
    - read summary.go Origin/InBodyFlow/unwrapToCall against the contract
  evidence_refs:
    - EV213-138
```

```yaml
gate_result:
  record_author: qa
  accountable_owner: qa
  id: GV213-139
  kind: phase_verification
  gates_subject: phase-139-inbody-returnbridge-emission-go-proof
  evaluator: qa
  verdict: pass
  status: accepted
  method:
    - ran the emission/distinctness/non-regression/multi-hop suite fresh (all pass)
    - read inBodyDataFlowEdges + returnBridgeEdges against the edge contracts
    - confirmed M1 append order + M4 map-free output on disk
    - confirmed the M3 test asserts the corrected truth (not a falsehood)
  evidence_refs:
    - EV213-139
```

```yaml
gate_result:
  record_author: qa
  accountable_owner: qa
  id: GV213-140
  kind: phase_verification
  gates_subject: phase-140-all11-realbinary-e2e-multihop-docs
  evaluator: qa
  verdict: pass
  status: accepted
  method:
    - built a fresh binary, ran TestCLI_E2E_InBody (11/11 + multi-hop, 0 skip)
    - confirmed langFromExt Path B wiring + corrected doc on disk
  evidence_refs:
    - EV213-140
```

```yaml
gate_result:
  record_author: qa
  accountable_owner: qa
  id: GV213-CLOSE
  kind: release_gate
  gates_subject: milestone-v2.13
  evaluator: qa
  verdict: pass
  status: accepted
  method:
    - independent build + vet + affected-pkg test + fresh-binary E2E, all green
    - deps (go.mod/go.sum) + schema (internal/semantic/store/) empty-diff
    - acceptance handshake passed; code-reality checks match contracts
  evidence_refs:
    - EV213-138
    - EV213-139
    - EV213-140
```

```yaml
evidence:
  record_author: qa
  accountable_owner: qa
  evidence_producer: qa
  commissioned_by: qa
  issuance:
    kind: peer_authored
  id: EV213-138
  subject_ref: GV213-138
  kind: test
  warrant:
    kind: test_pass
    scope:
      - internal/semantic/dataflow/dataflow_matrix_test.go
      - internal/semantic/dataflow/dataflow_test.go
      - internal/semantic/dataflow/summary.go
    rationale: >-
      go test -count=1 TestAnalyzeFlow_AllElevenMatrix → 11/11 subtests PASS, 0 skip;
      m2 RED guards + anti-vacuity + determinism green on a fresh run; Origin/InBodyFlow/
      unwrapToCall read against the contract.
  freshness:
    status: fresh
    checked_against: HEAD
  supports:
    - GV213-138
```

```yaml
evidence:
  record_author: qa
  accountable_owner: qa
  evidence_producer: qa
  commissioned_by: qa
  issuance:
    kind: peer_authored
  id: EV213-139
  subject_ref: GV213-139
  kind: test
  warrant:
    kind: test_pass
    scope:
      - internal/daemon/dataflow_edges_test.go
      - internal/daemon/dataflow_reachability_test.go
      - internal/daemon/semantic_similarity_edges.go
      - internal/daemon/semantic_wiring.go
    rationale: >-
      go test -count=1 on the daemon emission suite → InBodyEmission, ReturnBridge,
      Distinctness, DefUseSetNonRegression, MultiHop (+ BrokenHop guard),
      FunctionSeedReachesInBody all PASS; emission funcs + M1 append order + M4 map-free
      + M3 test correction confirmed by reading.
  freshness:
    status: fresh
    checked_against: HEAD
  supports:
    - GV213-139
```

```yaml
evidence:
  record_author: qa
  accountable_owner: qa
  evidence_producer: qa
  commissioned_by: qa
  issuance:
    kind: peer_authored
  id: EV213-140
  subject_ref: GV213-140
  kind: test
  warrant:
    kind: test_pass
    scope:
      - internal/cli/cli_dataflow_inbody_e2e_test.go
      - internal/daemon/semantic_wiring.go
    rationale: >-
      Fresh-binary HELIX_BIN=/tmp/helix-qa-v213 go test -count=1 TestCLI_E2E_InBody →
      11/11 language subtests PASS + TestCLI_E2E_InBodyMultiHop PASS, 0 skip; langFromExt
      Path B wiring + corrected doc confirmed on disk.
  freshness:
    status: fresh
    checked_against: HEAD
  supports:
    - GV213-140
```

## Conclusion

v2.13 delivers its stated intent — in-body-origin DATA_FLOWS modeled, emitted at
existing symbol nodes with no schema change, proven E2E through the real `helix`
binary for all 11 languages with a function-seeded multi-hop — and I independently
confirm it against code reality and fresh test execution. **passed.** No blocking
gaps. Safe to commit at the architect/co-driver's discretion.
