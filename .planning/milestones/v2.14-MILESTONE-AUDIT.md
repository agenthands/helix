---
author: architect
responsible: architect
milestone: v2.14
status: complete
verdict: PASSED
created: 2026-07-01
parent_artifacts:
  - .planning/milestones/v2.14-REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/TESTING.md
---

# v2.14 MILESTONE AUDIT — Codebase-Map Re-mapping (Go tree)

## Verdict: PASSED

The re-mapped codebase docs satisfy the milestone's stated intent. Grounded in an
**independent re-read of the SYSTEM** (real Go source at HEAD + `go.mod` + Makefile +
`.github/workflows/`) and a residue/date/banner sweep at audit time — not the phase-time
writer reports.

## Refinement check — does the delivered map match the abstract intent?

**Milestone intent (locked):** re-map all 7 `.planning/codebase/*.md` from the removed
pre-Go Python `serena` tree to the **current Go tree** at HEAD; every claim grounded in
a citable Go path; Python-serena residue gone; staleness banners removed.

**Delivered — confirmed against real code at audit time:**

- **All 7 files describe the Go tree.** Before: 131/70/52/26/36/19/89 Python-tree hits
  across STRUCTURE/ARCHITECTURE/CONCERNS/CONVENTIONS/INTEGRATIONS/STACK/TESTING (all
  dated 2026-04-07). After: every file `**Analysis Date:** 2026-07-01`, structured for
  `cmd/`+`internal/`+`api/proto`+`protocol/`+`bench/`+`tools/`.
- **Residue clean.** `grep -iE '(solidlsp|SerenaAgent|_tools\.py|pytest|ruff|conftest|
  xfail|pyproject|FastMCP|Pydantic|Flask|dataclass|JetBrains|src/serena)'` across
  `.planning/codebase/` → **zero hits**. The only `serena`-spelled surfaces remaining are
  the two deliberately-retained lineage artifacts — `SerenaMCPServer` (Go identifier,
  `internal/mcp/server.go:45`, Phase 52-03) and `api/proto/serena/v1/` (wire-format
  proto dir, Phase 52-03) — each explicitly labeled "retained lineage, NOT residue" in
  the prose. This is correct: renaming either is a churn/wire-break, not a cleanup.
- **Staleness banners removed.** STRUCTURE.md's v2.13 banner + addendum framing is gone;
  the still-true dataflow content was folded into the normal structure. Zero banner hits.
- **`internal/semantic/` documented first-class.** The 30.8k-LOC largest subsystem (18
  subpackages) — nearly absent from CLAUDE.md — is surveyed fresh in ARCHITECTURE.md as
  an extract→store→enrich→emit pipeline with real subpackage cites and the edge-kind
  ledger (cross-referencing `docs/edge-types.md`), and enumerated in STRUCTURE.md.

## Independent grounding at audit time (fresh, not phase-time)

Spot-checked a sample of claims spanning all 7 files against real source; **every one
confirmed**:

| Claim (file) | Audit check | Result |
|---|---|---|
| 16 `cmd/` binaries (STRUCTURE) | `ls -d cmd/*/ \| wc -l` | 16 ✓ |
| 6-deep MCP middleware chain (ARCHITECTURE) | `internal/mcp/{lazy_init,profile_enforce,guardrail_middleware,suggest,middleware}.go` all present | 6 ✓ |
| 8 `helix setup` registrars (ARCH/INTEG) | `setup_clients.go` → claude-code/-desktop/codex/gemini-cli/generic/jetbrains/opencode/vscode | 8 ✓ |
| testify v1.11.1 (TESTING) | `go.mod` | ✓ |
| table-test example `internal/fuzzy/strategies_test.go` (TESTING) | file exists | ✓ |
| golden helper `internal/semantic/extract/testutil/golden.go` (TESTING) | file exists | ✓ |
| CLI real-binary E2E `internal/cli/cli_e2e_test.go` (TESTING) | file exists | ✓ |
| duckdb `//go:build !(windows && arm64)` + winarm64 stub (CONCERNS) | `internal/semantic/store/duckdb.go` head + `duckdb_winarm64.go` sibling | ✓ |
| deferred IDs DEF-59-NOTARIZE / -WIN-ARM64-RESTORE / -59.1-LINUX-ZIG (CONCERNS) | `.planning/deferred-items.md` | ✓ |
| `serr` errors alias (CONVENTIONS) | `internal/errors/errors.go:65` | ✓ |
| 7 vettools in `make vet` (CONVENTIONS/CONCERNS/TESTING) | `Makefile:55` | ✓ (verify-no-docker-sdk + 6 vet-* + core VETTOOL) |
| dep versions (STACK) | read verbatim from `go.mod` | ✓ |

## The writers corrected THREE stale facts in the source-of-record (honesty floor working)

The one-pass writers, grounding in real code rather than parroting the brief/CLAUDE.md,
**refuted three inputs** — the map is now MORE accurate than CLAUDE.md:
1. **`cmd/` = 16, not 17** (my REQUIREMENTS/ROADMAP/PROJECT/STATE brief miscounted;
   corrected in all four planning docs at audit time).
2. **MCP middleware chain = 6-deep, not 4** — CLAUDE.md's "four real middlewares" omits
   `ProfileEnforceMiddleware` + `GuardrailMiddleware` (both present in `internal/mcp/`).
3. **`helix setup` = 8 clients, not 7** — CLAUDE.md says 7; `setup_clients.go` registers 8
   (adds `codex`).
4. **cobra v1.10.2, not v1.9.1** — CLAUDE.md's stack line is stale; go.mod is authoritative.

## Requirement coverage

| Requirement | Deliverable | Status |
|---|---|---|
| MAP-01 (STRUCTURE) / MAP-03 (STACK) | Phase 141 | ✅ Go layout + go.mod stack, grounded |
| MAP-02 (ARCHITECTURE) / MAP-04 (INTEGRATIONS) / MAP-05 (CONVENTIONS) | Phase 142 | ✅ 4 layers + semantic fresh + integrations + Go conventions |
| MAP-06 (CONCERNS) / MAP-07 (TESTING) / MAP-08 (integrity) | Phase 143 | ✅ grounded debt/posture + Go test arch + residue/banner/date gate |

**8/8 requirements delivered + independently spot-verified. Success criteria SC1–SC6 met.**

- SC1 (Go-only, residue only retained-lineage) ✅ · SC2 (claims path-grounded) ✅ ·
  SC3 (`internal/semantic/` first-class) ✅ · SC4 (banners gone, dates refreshed) ✅ ·
  SC5 (stale ROADMAP `## Phases` regenerated; `roadmap analyze` now reports 141/142/143,
  no phantom 121-123) ✅ · SC6 (no source change; build/vet/test untouched — nothing in
  `.go`/go.mod was edited) ✅.

## Scope integrity

Documentation-only, as scoped. `git status` shows the changed files are the 7
`.planning/codebase/*.md` + the milestone spine (REQUIREMENTS/ROADMAP/PROJECT/STATE/
MILESTONES/AUDIT) — **zero `.go`/`go.mod`/`go.sum` changes**, so `go build`/`go vet`/
`go test` are provably unaffected (nothing that feeds them was touched). No formatters
or tests were run by the writers, by contract.

## Deferred / follow-ons (non-blocking)

- **CLAUDE.md drift (LOW, documentation-integrity):** CLAUDE.md carries the same three
  stale facts the writers corrected (4 middlewares → 6; 7 setup clients → 8; cobra
  v1.9.1 → v1.10.2). CLAUDE.md was **out of scope** for v2.14 (the milestone target was
  `.planning/codebase/`), and part of it is `generate-claude-profile`-managed. Recommend a
  cheap follow-on to reconcile CLAUDE.md's hand-maintained Architecture/Stack sections to
  the now-accurate map. NOT a v2.14 blocker.
- **`docs/` contributor docs:** current (v2.13 refresh); untouched, as scoped.
- **v2.13 remains uncommitted** (co-driver's call, pre-existing) — v2.14's commit is
  separate and additive.

## Conclusion

v2.14 refines the milestone intent faithfully: all 7 codebase-map files now describe the
current Go tree, every sampled claim is source-grounded at HEAD, the Python-serena residue
is gone (only labeled retained-lineage remains), the staleness banners are removed, and the
`internal/semantic/` subsystem is documented first-class. The planning-integrity fix
(regenerated ROADMAP `## Phases`) resolved the tooling misfire that surfaced this milestone.
The writers' three code-grounded corrections demonstrate the grounding discipline held.
**PASSED.**
