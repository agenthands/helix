---
phase: 103-bundle-integrity-non-vacuous-reference-contract
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/cli/skill.go
  - internal/cli/skill_bundle_test.go
  - internal/cli/reference_contract_test.go
  - internal/cli/skills/helix/SKILL-ISSUE.md
  - .planning/SKILL-ISSUE.md
autonomous: true
requirements:
  - BUNDLE-01
  - BUNDLE-02

must_haves:
  truths:
    - "After helix setup, the installed skill dir contains EXACTLY {SKILL.md, reference.md} and no stray file (D-BUNDLE-01, SC-1)"
    - "SKILL-ISSUE.md no longer compiles into the helix binary or installs to a user's disk (D-BUNDLE-01, SC-2)"
    - "installSkill and uninstallSkill drive a single closed-set bundleFiles allowlist filtered once up front; 2-pass atomicity and withinSkillRoot containment are byte-for-byte unchanged (D-BUNDLE-01, SC-2)"
    - "uninstall is filtered identically — an upgrade that stops shipping a file removes it from disk, never orphans it (D-BUNDLE-01, SC-2)"
    - "The reference-completeness contract is non-vacuous: exact-count == len(VerbToolNames())==50 AND RED on a fabricated known-absent verb (D-BUNDLE-02, SC-3)"
    - "The hardened bundle allowlist + non-vacuous contract land in Phase 103, BEFORE the Phase 104/105 reference/skill format churn (D-BUNDLE-02, SC-4)"
  artifacts:
    - path: "internal/cli/skill.go"
      provides: "package-level bundleFiles allowlist + filter-once-up-front applied to installSkill and uninstallSkill"
      contains: "var bundleFiles"
    - path: "internal/cli/skill_bundle_test.go"
      provides: "closed-set TestInstallSkillInstallsExactlyBundle (RED on any stray installed file) + uninstall-drop symmetry test"
      contains: "TestInstallSkillInstallsExactlyBundle"
    - path: "internal/cli/reference_contract_test.go"
      provides: "fabricated-absent-verb discriminator sealing the gate against vacuous pass through 104/105"
      exports: []
    - path: ".planning/SKILL-ISSUE.md"
      provides: "the maintainer analysis, moved OUT of the embed dir so it is neither embeddable nor installable"
  key_links:
    - from: "internal/cli/skill.go installSkill"
      to: "internal/cli/skill.go bundleFiles"
      via: "filter ReadDir entries against bundleFiles ONCE before the 2-pass stage→rename"
      pattern: "bundleFiles"
    - from: "internal/cli/skill_bundle_test.go"
      to: "internal/cli/skill.go installSkill"
      via: "installs into t.TempDir() then asserts os.ReadDir(dir) == exactly {SKILL.md, reference.md}"
      pattern: "TestInstallSkillInstallsExactlyBundle"
    - from: "internal/cli/reference_contract_test.go"
      to: "internal/cli/verb.go VerbToolNames"
      via: "fabricated-absent verb appended to the authority is reported missing"
      pattern: "referenceMissingVerbs"
---

<objective>
Stop a LIVE supply-chain leak and seal the reference-completeness gate against vacuity — the smallest-blast-radius safety patch of v2.2, landed FIRST so Phases 104 (generator fixes) and 105 (SKILL.md rewrite) churn a clean embed dir behind a non-vacuous contract.

Three deliverables, all verified against the live tree (103-RESEARCH.md):
1. **BUNDLE-01 — stop the embed leak.** `internal/cli/skill.go` uses `//go:embed skills/helix/*`; both `installSkill` (skill.go:189-256) and `uninstallSkill` (skill.go:320-332) iterate EVERY `ReadDir("skills/helix")` entry, so the 18 KB git-untracked `SKILL-ISSUE.md` currently compiles into the binary AND is written by `helix setup`. Fix: a package-level `bundleFiles = {SKILL.md, reference.md}` allowlist, the entry list filtered ONCE up front and the SAME filtered slice fed to both the install passes and the uninstall loop (2-pass atomicity + `withinSkillRoot` containment unchanged); MOVE `SKILL-ISSUE.md` to `.planning/`; add a CLOSED-SET install test that goes RED on any stray file.
2. **BUNDLE-02 — seal the contract.** `internal/cli/reference_contract_test.go` already asserts exact-count==50 (line 58) and a real-verb-drop revert proof (lines 82-104). Confirm-and-seal: add an explicit fabricated-absent-verb discriminator (RED-first) proving the membership test keys on the real frozen `VerbToolNames()` set and cannot pass `∅ ⊇ ∅` through the 104/105 churn.

Purpose: A stray asset is currently embedded into the binary and shipped to every user's `.claude/skills/helix/`; a positive-only bundle test cannot catch it (the repo's recurring Phase 87/89 CR-01 vacuous-gate class). Closing both before the format rewrite is the locked build-order constraint.
Output: Modified `skill.go` (allowlist), new `skill_bundle_test.go` (closed-set + uninstall-drop), extended `reference_contract_test.go` (fabricated-absent discriminator), `SKILL-ISSUE.md` moved to `.planning/`, all invariants green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/103-bundle-integrity-non-vacuous-reference-contract/103-RESEARCH.md
@.planning/phases/103-bundle-integrity-non-vacuous-reference-contract/103-CONTEXT.md
@.planning/research/PITFALLS.md

# Code under change (read these BEFORE editing — they carry the verbatim invariants to preserve):
@internal/cli/skill.go
@internal/cli/skill_test.go
@internal/cli/reference_contract_test.go
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — closed-set bundle test + fabricated-absent contract discriminator</name>
  <files>internal/cli/skill_bundle_test.go, internal/cli/reference_contract_test.go</files>
  <read_first>
    - internal/cli/skill_test.go:317-340 (TestInstallSkillWritesBundle — the existing positive-only bundle test; do NOT delete it. Reuse its skillTargetDir(t.TempDir()) + installSkill pattern.)
    - internal/cli/skill_test.go:291-315 (TestInstallSkillNoTempLeftover + TestInstallSkillContainment — atomicity/containment proofs that MUST stay green; your new test must not regress them.)
    - internal/cli/reference_contract_test.go:37-104 (referenceMissingVerbs helper + TestReferenceCoversEveryVerb exact-count==50 + TestReferenceCompletenessRevertFails real-verb-drop proof — the existing non-vacuity anchors. Reuse referenceMissingVerbs + VerbToolNames() as the authority.)
  </read_first>
  <behavior>
    - Closed-set install (BUNDLE-01): installSkill into a fresh skillTargetDir(t.TempDir()), then os.ReadDir the target and collect non-dir entry names; sorted, the set MUST equal exactly {"SKILL.md", "reference.md"}. RED today: with SKILL-ISSUE.md still embedded, the installed set is {SKILL-ISSUE.md, SKILL.md, reference.md} and the assertion fails.
    - Uninstall-drop symmetry (BUNDLE-01): install into a temp dir, manually drop an extra non-allowlisted file alongside the bundle to simulate a prior leaked install, run uninstallSkill, and assert the allowlisted bundle files are removed; the test documents that uninstall is driven by the SAME closed-set allowlist as install. (Simplest form: after uninstallSkill on a clean install, the bundle files are gone and the dir is pruned — extend the existing idempotent/uninstall coverage so install and uninstall stay symmetric.)
    - Fabricated-absent discriminator (BUNDLE-02): call referenceMissingVerbs(realReference, append(VerbToolNames(), "totally-not-a-verb")) and assert the result CONTAINS "totally-not-a-verb" — proving the membership check keys on the real frozen set and reports a verb that is genuinely absent. This is the explicit anti-vacuity anchor that, alongside the existing exact-count==50 and real-verb-drop proofs, seals the gate before the 104/105 churn.
  </behavior>
  <action>
    Create internal/cli/skill_bundle_test.go in package cli. Add TestInstallSkillInstallsExactlyBundle: install via installSkill(skillTargetDir(t.TempDir())), os.ReadDir the dir, gather non-dir entry names into a slice, sort.Strings both got and the want {"SKILL.md","reference.md"}, and assert.Equal(want, got) with a message that a stray installed file is a leak. Add a sibling uninstall-symmetry test asserting uninstallSkill leaves no allowlisted bundle file behind (and, per the existing uninstallSkill contract, prunes the now-empty dir). Use the same testify assert/require style and the existing skillTargetDir helper — do NOT re-derive the embed glob from a ReadDir of the SOURCE dir (that is the set-compared-to-itself tautology PITFALLS.md and 97-RESEARCH Pitfall 2 forbid); assert against the literal {SKILL.md, reference.md} and the ACTUAL installed-disk listing.

    In internal/cli/reference_contract_test.go, ADD a new test (e.g. TestReferenceContractDiscriminatesAbsentVerb) that builds authorityPlus := append(append([]string{}, VerbToolNames()...), "totally-not-a-verb"), then require.Contains(referenceMissingVerbs(readEmbeddedReference(t), authorityPlus), "totally-not-a-verb"). Keep TestReferenceCoversEveryVerb and TestReferenceCompletenessRevertFails byte-for-byte intact — do NOT weaken or duplicate the existing exact-count==50 or real-verb-drop coverage; this is an ADDITIVE discriminator. Use a fabricated verb token that is NOT a real frozen verb and contains no markdown that could accidentally match a section header.

    This task is RED-FIRST: TestInstallSkillInstallsExactlyBundle MUST fail now (SKILL-ISSUE.md is still embedded → 3 installed files). The contract discriminator is expected to PASS immediately (the real reference never has a "totally-not-a-verb" section), which is correct — it is a confirm-and-seal anchor, not a behavior change; its value is that it would go RED if a future refactor made referenceMissingVerbs stop reporting genuinely-absent names. Do NOT add the allowlist or move SKILL-ISSUE.md in this task. Commit on RED: `test(103-01): add closed-set bundle test (RED) + fabricated-absent contract discriminator`.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run 'TestInstallSkillInstallsExactlyBundle' -count=1 2>&1 | grep -q -- '--- FAIL' && echo "RED-CONFIRMED-bundle" || echo "EXPECTED-RED-MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - TestInstallSkillInstallsExactlyBundle exists and FAILS now (installed set includes SKILL-ISSUE.md → not equal to {SKILL.md, reference.md}).
    - TestReferenceContractDiscriminatesAbsentVerb exists and passes (referenceMissingVerbs reports the fabricated absent verb).
    - The existing TestInstallSkillWritesBundle, TestInstallSkillNoTempLeftover, TestInstallSkillContainment, TestReferenceCoversEveryVerb, TestReferenceCompletenessRevertFails are unmodified and still present.
    - `go vet ./internal/cli/` exits 0; the new files compile.
  </acceptance_criteria>
  <done>The closed-set bundle test is RED on the current 3-file install; the fabricated-absent contract discriminator is added and green; no existing test was weakened or deleted.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — bundleFiles allowlist (filter once, both loops) + move SKILL-ISSUE.md out of embed dir</name>
  <files>internal/cli/skill.go, internal/cli/skills/helix/SKILL-ISSUE.md, .planning/SKILL-ISSUE.md</files>
  <read_first>
    - internal/cli/skill.go:21-22 (the //go:embed skills/helix/* glob + embeddedSkillFS var — UNCHANGED; the move makes the glob no longer match SKILL-ISSUE.md, the allowlist is additive defense-in-depth)
    - internal/cli/skill.go:189-256 (installSkill: ReadDir → 2-pass stage→rename. The filter goes ONCE up front, right after the ReadDir at line 189, producing a filtered entries slice fed UNCHANGED to both passes. Do NOT sprinkle continue guards through Pass 1/Pass 2 — PITFALLS.md Pitfall 3 mitigation: filter the entry list once, feed the SAME filtered slice to the existing staging/rename/cleanup passes.)
    - internal/cli/skill.go:308-339 (uninstallSkill: the SAME ReadDir loop — must be filtered IDENTICALLY or it orphans a now-un-allowlisted file on upgrade; PITFALLS.md asymmetric-allowlist pitfall)
    - internal/cli/skill.go:259-306 (withinSkillRoot / lexicalSkillRoot / containedIn — the containment guard; the first statement of each func is UNCHANGED)
  </read_first>
  <action>
    1. Add a package-level allowlist to skill.go (single source of truth both loops consult), e.g. `var bundleFiles = map[string]bool{"SKILL.md": true, "reference.md": true}` with a doc comment naming it the EXACT set shipped to disk and noting any other embed entry (e.g. SKILL-ISSUE.md maintainer notes) is filtered at install/uninstall time. Per 103-RESEARCH "Code Examples" note, do NOT echo the literal string SKILL-ISSUE.md into a comment that another gate might negative-grep — refer to it by concept ("non-bundle maintainer notes").

    2. In installSkill, immediately after `entries, err := embeddedSkillFS.ReadDir("skills/helix")` succeeds (skill.go:189-192), filter ONCE into a new slice — iterate entries, skip e.IsDir(), keep only those whose e.Name() is in bundleFiles — and use THAT filtered slice as the input to Pass 1 (the staging loop at skill.go:212). The 2-pass stage→rename body (the `pending []staged`, cleanupTemps, WriteFile-to-.tmp, Rename, and the revert paths at skill.go:200-255) is byte-for-byte UNCHANGED. The filter runs BEFORE any staging so all-or-nothing is preserved over the allowlisted set; no `.tmp` sibling is ever created for a filtered-out file.

    3. In uninstallSkill, apply the IDENTICAL filter to its ReadDir loop (skill.go:324-332) — the removal loop only touches allowlisted bundle files. Keep the os.IsNotExist tolerance and the empty-dir prune (skill.go:329,337) unchanged.

    4. Move the maintainer analysis OUT of the embed dir: `mv internal/cli/skills/helix/SKILL-ISSUE.md .planning/SKILL-ISSUE.md`. It is git-UNTRACKED (verified `??`) — use a PLAIN filesystem move, NOT `git mv`; then `git add .planning/SKILL-ISSUE.md` at the destination so the moved doc is tracked. After the move, `ls internal/cli/skills/helix/` shows exactly SKILL.md + reference.md.

    5. ROADMAP repath check: grep the ROADMAP for any LITERAL `internal/cli/skills/helix/SKILL-ISSUE.md` path string. The current ROADMAP carries only bare-filename prose mentions of `SKILL-ISSUE.md` (which stay accurate — they reference the document's findings, not its path), so there is likely NOTHING to rewrite. Update ONLY a literal old-path string if one exists; do NOT rewrite bare-filename prose. (`grep -rn "internal/cli/skills/helix/SKILL-ISSUE" .planning/ROADMAP.md` → if empty, this step is a no-op, record that in the SUMMARY.)

    This is the GREEN step that makes Task 1's RED pass: with the allowlist filtering both loops AND SKILL-ISSUE.md no longer matched by the embed glob, installSkill writes exactly {SKILL.md, reference.md}. Commit: `fix(103-01): bundleFiles closed-set allowlist on install+uninstall; move SKILL-ISSUE.md out of embed dir (BUNDLE-01)`.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run 'InstallSkill|Bundle|Uninstall|Containment|Reference' -count=1 && go vet ./internal/cli/ && go build ./cmd/helix</automated>
  </verify>
  <acceptance_criteria>
    - `var bundleFiles` (or equivalently-named package-level allowlist) exists in skill.go and is consulted by BOTH installSkill and uninstallSkill via a single filter-once-up-front pass.
    - The installSkill 2-pass stage→rename body and withinSkillRoot/lexicalSkillRoot/containedIn are unchanged (no edits inside the staging/rename/revert blocks or the containment guards).
    - `ls internal/cli/skills/helix/` lists exactly SKILL.md and reference.md; `.planning/SKILL-ISSUE.md` exists and is git-tracked.
    - TestInstallSkillInstallsExactlyBundle (from Task 1) now PASSES; TestInstallSkillWritesBundle, TestInstallSkillNoTempLeftover, TestInstallSkillContainment, TestInstallSkillIdempotent stay green.
    - `go build ./cmd/helix` succeeds; `go vet ./internal/cli/` exits 0; `git diff go.mod go.sum` is empty (ZERO new deps).
  </acceptance_criteria>
  <done>installSkill/uninstallSkill ship exactly the two bundle files via a single closed-set allowlist; SKILL-ISSUE.md is moved to .planning/ and no longer embeds; the closed-set test is GREEN and all atomicity/containment tests remain green.</done>
</task>

<task type="execute">
  <name>Task 3: REFACTOR/VERIFY — full suite, byte-reproducibility, adopt anchor, binary-clean proof</name>
  <files>internal/cli/skill.go, internal/cli/skill_bundle_test.go, internal/cli/reference_contract_test.go</files>
  <read_first>
    - .planning/phases/103-bundle-integrity-non-vacuous-reference-contract/103-VALIDATION.md (the Per-Task Verification Map + Phase-gate sampling: full suite + helix-refgen -check + adopt scorecard)
    - test/oracle/adopt/scorecard.go:101-111 (StripDecisionMatrix anchor — the "## Decision matrix" heading in SKILL.md must be untouched this phase; SKILL.md is NOT edited here)
  </read_first>
  <action>
    Final phase-gate verification — no behavior change, only proof the patch preserved every invariant:

    1. Full Go suite: `go test ./... && go vet ./...` — all green (catches any cross-package regression from the move/allowlist).
    2. Byte-reproducibility: `go run ./cmd/helix-refgen -check` MUST print "reference.md is up to date." and exit 0 (proves reference.md was NOT incidentally touched — the move + allowlist do not regenerate it).
    3. Adopt scorecard / StripDecisionMatrix anchor: `go test ./test/oracle/adopt/... -count=1` green (proves the "## Decision matrix" anchor in SKILL.md is intact — SKILL.md was not edited).
    4. Binary-clean proof: confirm the moved doc is no longer compiled in — `go build ./cmd/helix` then `strings ./helix | grep -c "maintainer analysis"` (or a phrase unique to SKILL-ISSUE.md) returns 0; equivalently the closed-set install test already proves SKILL-ISSUE.md is not installed. Record the chosen proof in the SUMMARY.
    5. gofmt: `gofmt -l internal/cli/skill.go internal/cli/skill_bundle_test.go internal/cli/reference_contract_test.go` returns empty (run `gofmt -w .` if not).

    If any of the four invariant gates regress, STOP and fix before completing (do not paper over a refgen-check or adopt-anchor failure). Commit: `test(103-01): verify full suite + refgen -check + adopt anchor green; binary clean of moved doc`.
  </action>
  <verify>
    <automated>go test ./... && go vet ./... && go run ./cmd/helix-refgen -check && go test ./test/oracle/adopt/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./...` and `go vet ./...` both exit 0.
    - `go run ./cmd/helix-refgen -check` prints "reference.md is up to date." and exits 0.
    - `go test ./test/oracle/adopt/... -count=1` is green (StripDecisionMatrix anchor intact).
    - The built helix binary contains no SKILL-ISSUE.md content (closed-set install test + strings-grep proof).
    - `gofmt -l` on the three touched files is empty; `git diff go.mod go.sum` is empty.
  </acceptance_criteria>
  <done>Full suite, byte-reproducibility, adopt-anchor, and binary-clean gates all green; the patch is proven to have preserved every pre-existing invariant byte-for-byte.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

| Artifact | Kind | Location | Provides |
|----------|------|----------|----------|
| `bundleFiles` | package-level var (allowlist set) | `internal/cli/skill.go` | The EXACT closed set `{SKILL.md, reference.md}` consulted by installSkill + uninstallSkill |
| `TestInstallSkillInstallsExactlyBundle` | new test func | `internal/cli/skill_bundle_test.go` | Closed-set assertion: installed dir == exactly the two bundle files; RED on any stray |
| uninstall-symmetry test | new test func | `internal/cli/skill_bundle_test.go` | Proves uninstall is driven by the SAME allowlist (no orphaned file on upgrade) |
| `TestReferenceContractDiscriminatesAbsentVerb` | new test func | `internal/cli/reference_contract_test.go` | Fabricated-absent-verb discriminator sealing the contract against vacuity before 104/105 |
| `.planning/SKILL-ISSUE.md` | moved file | `.planning/` (was `internal/cli/skills/helix/`) | Maintainer analysis, OUT of the embed glob → neither embeddable nor installable |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time embed → shipped binary | `//go:embed skills/helix/*` compiles whatever is in the dir INTO the binary — an over-broad glob ships unintended assets |
| `helix setup` → user's `.claude/skills/helix/` | installSkill writes embedded files to a user-controlled-path directory; the only inputs are embed-FS entry names (never user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Information Disclosure | `//go:embed skills/helix/*` + installSkill ReadDir loop | mitigate | LIVE leak: 18 KB maintainer analysis ships in the binary + to every user. Move it OUT of the embed dir AND add a closed-set `bundleFiles` allowlist filtered once on both install + uninstall (Task 2). Closed-set test (Task 1) is the regression guard. |
| T-103-02 | Tampering | installSkill 2-pass stage→rename atomicity | accept (unchanged) | The filter runs BEFORE staging, over the allowlisted set; the all-or-nothing rename + revert path is byte-for-byte unchanged — no new torn-bundle/`.tmp`-orphan surface introduced. TestInstallSkillNoTempLeftover stays green. |
| T-103-03 | Tampering | install target path traversal (`withinSkillRoot`) | accept (unchanged) | `withinSkillRoot`/`lexicalSkillRoot`/`containedIn` filepath.Rel ".."-escape guard is unchanged; TestInstallSkillContainment stays green. Entry names still come ONLY from the embed FS. |
| T-103-04 | Tampering | uninstall asymmetry (orphaned file) | mitigate | uninstallSkill filtered with the IDENTICAL allowlist so an upgrade that stops shipping a file removes it from disk rather than orphaning it (Task 2 + uninstall-symmetry test). |
| T-103-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all edits are inside already-vendored `internal/cli` and its tests; `git diff go.mod go.sum` is empty. No `[ASSUMED]`/`[SUS]` packages, so no legitimacy checkpoint is needed. |
</threat_model>

<verification>
## Phase-level checks (run before /gsd-verify-work)

- `go test ./...` and `go vet ./...` exit 0.
- `go test ./internal/cli/ -run 'InstallSkill|Bundle|Uninstall|Containment|Reference' -count=1` green — including the new closed-set + fabricated-absent tests.
- `go run ./cmd/helix-refgen -check` prints "reference.md is up to date." (exit 0) — byte-reproducibility preserved.
- `go test ./test/oracle/adopt/... -count=1` green — StripDecisionMatrix anchor / SKILL.md untouched.
- `ls internal/cli/skills/helix/` == exactly `SKILL.md` + `reference.md`; `.planning/SKILL-ISSUE.md` exists and is git-tracked.
- `git diff go.mod go.sum` empty (ZERO new Go deps).
- Anti-vacuity proof: the closed-set bundle test demonstrably ran RED before Task 2 (the SUMMARY records the RED→GREEN transition).
</verification>

<success_criteria>
Phase 103 is complete when (mapping the 4 ROADMAP success criteria):
1. After `helix setup`, the installed skill dir contains exactly `{SKILL.md, reference.md}` — `TestInstallSkillInstallsExactlyBundle` asserts the closed set and went RED on the prior 3-file install. (SC-1)
2. `SKILL-ISSUE.md` no longer compiles into the binary or installs — moved to `.planning/`; `installSkill`/`uninstallSkill` drive a single closed-set `bundleFiles` allowlist (filter once up front; atomicity + containment unchanged; uninstall filtered identically). (SC-2)
3. The reference-completeness contract is non-vacuous: exact-count `== len(VerbToolNames())==50` (existing) + a fabricated-absent-verb discriminator (new, RED-first) + the existing real-verb-drop revert proof. (SC-3)
4. Both the hardened allowlist and the non-vacuous contract land in Phase 103, BEFORE the Phase 104/105 reference/skill format churn. (SC-4)
- All invariants green: full suite, `helix-refgen -check` exit 0, adopt scorecard, `go vet`, `gofmt -l` empty, `go.mod`/`go.sum` unchanged.
</success_criteria>

<output>
Create `.planning/phases/103-bundle-integrity-non-vacuous-reference-contract/103-01-SUMMARY.md` when done. Record: the RED→GREEN transition for the closed-set bundle test (the anti-vacuity proof); the exact `bundleFiles` var name + where the filter-once pass lives; the SKILL-ISSUE.md move (plain mv, not git mv, + git add at destination); whether the ROADMAP repath was a no-op (no literal old-path string) or a real edit; and confirmation that refgen -check + adopt anchor stayed green (SKILL.md/reference.md untouched).
</output>
