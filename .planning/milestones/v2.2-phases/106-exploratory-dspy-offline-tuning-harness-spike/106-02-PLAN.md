---
phase: 106-exploratory-dspy-offline-tuning-harness-spike
plan: 02
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - tools/dspy-tune/scorer.py
  - tools/dspy-tune/test_parity.py
  - tools/dspy-tune/test_split.py
  - tools/dspy-tune/test_degenerate.py
  - tools/dspy-tune/optimize.py
  - tools/dspy-tune/data/train.jsonl
  - tools/dspy-tune/data/test.jsonl
  - tools/dspy-tune/requirements.txt
  - tools/dspy-tune/README.md
  - tools/dspy-tune/REPORT.md
  - .gitignore
autonomous: true
requirements: [TUNE-01]

must_haves:
  truths:
    - "The Python scorer (scorer.py) re-implements the Go FirstCommand + ClassifyChoice EXACTLY (first-command prefix matching, the 6 trailing-space fallback prefixes, lowercase-once-on-extracted-command) — NOT a `\"helix\" in response` substring check."
    - "test_parity.py asserts scorer.classify_choice / scorer.first_command agree with EVERY case in the same committed tools/dspy-tune/golden/parity_cases.json the Go test reads (no duplicate corpus)."
    - "test_parity.py::test_broken_classifier_diverges proves a deliberately-wrong substring classifier produces a DIFFERENT result on the substring-trap case (planted-divergence anti-vacuity — a broken classifier MUST fail parity)."
    - "test_split.py asserts the held-out TEST split (data/test.jsonl) is disjoint from train∪val — TEST ⊄ trainset∪valset — and the split is the FIRST harness step the optimizer can never see."
    - "test_degenerate.py flags a degenerate always-`helix` steering text (metric-gaming guard)."
    - "optimize.py runs DSPy GEPA over TRAIN/VAL only (TEST excluded), reads OPENAI_API_KEY from the dev env only, and saves to a git-ignored output/optimized.json — never auto-adopting into SKILL.md/reference.md."
    - "REPORT.md documents the spike's no-ship/ship conclusion (the spike outcome is recorded), and the harness re-entry path is human-review-only through SKILL.md/refgen passing helix-refgen --check."
  artifacts:
    - path: "tools/dspy-tune/scorer.py"
      provides: "Python re-impl of the adopt classifier (first_command, classify_choice, score_choice_rate) — exact mirror of test/oracle/adopt/scorecard.go"
      contains: "def first_command"
    - path: "tools/dspy-tune/test_parity.py"
      provides: "pytest parity over the shared golden corpus + the planted-divergence anti-vacuity test"
      contains: "test_broken_classifier_diverges"
    - path: "tools/dspy-tune/test_split.py"
      provides: "overfit guard: held-out TEST disjoint from train∪val"
      contains: "test_"
    - path: "tools/dspy-tune/test_degenerate.py"
      provides: "metric-gaming guard: degenerate always-helix steering flagged"
      contains: "test_"
    - path: "tools/dspy-tune/optimize.py"
      provides: "DSPy GEPA harness loop over TRAIN/VAL (TEST excluded); output git-ignored"
      contains: "GEPA"
    - path: "tools/dspy-tune/requirements.txt"
      provides: "pinned dev-time deps (dspy, pytest) — never in go.mod or the binary"
      contains: "dspy=="
    - path: "tools/dspy-tune/README.md"
      provides: "how to run the harness; dev-env-only OPENAI_API_KEY; no-ship is a legitimate outcome; human-review re-entry gate"
      contains: "no-ship"
    - path: "tools/dspy-tune/REPORT.md"
      provides: "the documented spike outcome (no-ship/ship conclusion + the MinTasks=5 too-small finding)"
      contains: "no-ship"
    - path: ".gitignore"
      provides: "ignore tools/dspy-tune/output/ (the raw optimizer dump must never be committed)"
      contains: "tools/dspy-tune/output"
  key_links:
    - from: "tools/dspy-tune/test_parity.py"
      to: "tools/dspy-tune/golden/parity_cases.json"
      via: "json.load of the SAME committed corpus the Go parity_test.go reads; asserts scorer.classify_choice/first_command per case"
      pattern: "parity_cases\\.json"
    - from: "tools/dspy-tune/scorer.py"
      to: "test/oracle/adopt/scorecard.go"
      via: "exact port of FirstCommand + ClassifyChoice (first-command prefix, 6 trailing-space fallback prefixes, lowercase-once)"
      pattern: "FALLBACK_PREFIXES"
    - from: "tools/dspy-tune/optimize.py"
      to: "tools/dspy-tune/data/test.jsonl"
      via: "GEPA trainset/valset drawn ONLY from train.jsonl; test.jsonl sequestered (optimizer never sees it)"
      pattern: "trainset|valset"

prohibitions:
  - "scorer.py MUST NOT use `\"helix\" in response` or any substring/Contains-over-whole-response check — it keys on first_command prefix only (the substring trap)."
  - "No .go file and no go.mod is created under tools/ — tools/ stays invisible to `go test ./...` (only .py, .jsonl, .json, .txt, .md)."
  - "optimize.py MUST NOT write to or auto-adopt internal/cli/skills/helix/SKILL.md or reference.md — adopted text re-enters ONLY via a human-reviewed commit passing helix-refgen --check."
  - "output/optimized.json is git-ignored and never committed raw; no OPENAI_API_KEY is committed."
  - "go.mod is unchanged: no dspy/python edge added by this plan."
---

<objective>
Build the dev-time-only Python `tools/dspy-tune/` tree: the parity-pinned scorer, the four pytest gates (parity + planted-divergence, overfit held-out split, metric-gaming degenerate-steering), the GEPA optimizer harness, the pinned requirements, the README, and the spike REPORT documenting the no-ship/ship conclusion — plus the `.gitignore` edit for the optimizer output. This is the optimizer half of the spike; it is quarantined off `go.mod`, the binary, and `go test ./...` (proven by Plan 01's analyzer + the no-.go-under-tools/ invariant).

Purpose: Mirror the Go `adopt` classifier into Python at golden parity, gate it against the named CR-01 vacuity class (a broken classifier must fail), enforce the held-out TEST split + degenerate-steering guard the spike's honesty depends on, and record the documented spike outcome — without ever auto-adopting optimizer output into the shipped surface.
Output: the `tools/dspy-tune/` tree (scorer + 3 pytest guard files + optimize.py + data split + requirements.txt + README.md + REPORT.md) and a `.gitignore` entry for `tools/dspy-tune/output/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The single source of truth the Python scorer must mirror EXACTLY
@test/oracle/adopt/scorecard.go

# The shared golden corpus + the Go-side parity test created by Plan 01 (read, do not modify)
@tools/dspy-tune/golden/parity_cases.json
@test/oracle/adopt/parity_test.go

# The existing .gitignore (Python patterns already present; add only output/)
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Held-out TEST split (data/train.jsonl + data/test.jsonl) + scorer.py + test_split.py — the FIRST harness step</name>
  <files>tools/dspy-tune/data/train.jsonl, tools/dspy-tune/data/test.jsonl, tools/dspy-tune/scorer.py, tools/dspy-tune/test_split.py</files>
  <read_first>
    - test/oracle/adopt/scorecard.go (FirstCommand lines 59-72; fallbackPrefixes line 77 = {"grep ","sed ","cat ","find ","rg ","ls "} trailing-space load-bearing; ClassifyChoice lines 85-95 — lowercase ONCE on the extracted command, NOT the whole response)
    - 106-RESEARCH.md §"Code Examples → Python scorer" (lines 345-366): the exact Python port to use verbatim (first_command, classify_choice, FALLBACK_PREFIXES)
    - 106-RESEARCH.md §"The Corpus / Held-Out TEST Split" (lines 154-162): the held-out TEST split is the FIRST harness task; GEPA trainset/valset draw ONLY from train.jsonl; TEST sequestered before any optimization
    - 106-PATTERNS.md §"tools/dspy-tune/scorer.py" (lines 209-217) and §"test_split.py" (lines 220-227, plus the No-Analog table line 277)
  </read_first>
  <action>
    FIRST (spike discipline — the held-out split is the FIRST harness step): create tools/dspy-tune/data/train.jsonl and tools/dspy-tune/data/test.jsonl. Each line is a JSON object with at least a `task` (a code-intel prompt the agent might face) and an `expected_tool_family` hint; keep TEST disjoint from TRAIN by construction (distinct task strings). Seed TRAIN with a modest set (e.g. 6-10 tasks) and TEST with 2-3 sequestered tasks — document in README that the corpus is honestly too small (MinTasks=5) for a trustworthy delta. Do NOT draw any TEST task from TRAIN.

    Create tools/dspy-tune/scorer.py: port FirstCommand → first_command(response: str) -> str and ClassifyChoice → classify_choice(response: str) -> tuple[bool,bool] EXACTLY from scorecard.go using the verbatim Python from 106-RESEARCH.md lines 346-366. FALLBACK_PREFIXES = ("grep ", "sed ", "cat ", "find ", "rg ", "ls ") (trailing space load-bearing — "lsp" is NOT a fallback). In first_command: split on "\n"; per line strip(); skip if "" or startswith("```"); then strip("`").strip(); if startswith("$ ") drop 2 chars elif startswith("> ") drop 2 chars; return stripped. In classify_choice: cmd = first_command(response).lower() (lowercase ONCE on the extracted command); chose = cmd.startswith("helix "); fell_back = any(cmd.startswith(t) for t in FALLBACK_PREFIXES). Add score_choice_rate(response) -> tuple[bool,bool] returning classify_choice(response) for optimize.py to wrap. MUST NOT use `"helix" in response` (the substring trap — 106-RESEARCH Pitfall 1).

    Create tools/dspy-tune/test_split.py: a pytest that loads data/train.jsonl and data/test.jsonl, builds the set of TRAIN task strings (the trainset∪valset universe — val is carved from train) and the set of TEST task strings, and asserts TEST is disjoint from TRAIN (test_task_set & train_task_set == empty), i.e. TEST ⊄ trainset∪valset. Assert both files are non-empty and the TEST set is non-empty (a sequestered split must actually hold data). This is the overfit guard: if a future edit leaks a TEST task into TRAIN, the test turns RED.
  </action>
  <verify>
    <automated>command -v pytest >/dev/null 2>&1 && pytest tools/dspy-tune/test_split.py -x || (python3 -c "import json; tr={json.loads(l)['task'] for l in open('tools/dspy-tune/data/train.jsonl')}; te={json.loads(l)['task'] for l in open('tools/dspy-tune/data/test.jsonl')}; assert tr and te and not (tr & te), 'TEST must be disjoint from TRAIN'; print('split-disjoint OK')")</automated>
  </verify>
  <acceptance_criteria>
    - If pytest available: `pytest tools/dspy-tune/test_split.py -x` passes (TEST disjoint from TRAIN, both non-empty). If pytest NOT available: the python3 fallback one-liner prints "split-disjoint OK" and exits 0 (records pytest as dev-time-deferred).
    - `python3 -c "import sys; sys.path.insert(0,'tools/dspy-tune'); from scorer import classify_choice, first_command; assert classify_choice('The helix tool would help.\ngrep x')==(False,False); assert classify_choice('helix go-to-definition x')==(True,False); assert classify_choice('lsp restart')==(False,False); assert classify_choice('ls -la')==(False,True); print('scorer OK')"` prints "scorer OK" (proves first-command prefix matching, the substring trap, and the lsp/ls trailing-space discipline).
    - `find tools -name '*.go' -o -name 'go.mod' | wc -l` == 0 (no Go files under tools/).
  </acceptance_criteria>
  <done>The held-out TEST split exists and is provably disjoint from TRAIN (test_split.py or fallback green); scorer.py mirrors the Go classifier exactly and passes the substring-trap + lsp-lookalike spot checks.</done>
</task>

<task type="auto">
  <name>Task 2: test_parity.py (parity + planted-divergence anti-vacuity) + test_degenerate.py (metric-gaming guard)</name>
  <files>tools/dspy-tune/test_parity.py, tools/dspy-tune/test_degenerate.py</files>
  <read_first>
    - tools/dspy-tune/golden/parity_cases.json (the shared corpus created by Plan 01 — read its keys response/first_command/chose/fell_back; this Python side asserts against the SAME file the Go test reads)
    - test/oracle/adopt/scorecard_test.go (TestFirstCommandNotSubstring lines 46-62 — the substring-trap discriminator this Python parity must mirror)
    - 106-RESEARCH.md §"The Parity Contract Design → Anti-vacuity guard" (lines 144-152): test_broken_classifier_diverges must prove a `"helix" in response` substring classifier produces a DIFFERENT result on the substring-trap case
    - 106-RESEARCH.md §"Pitfall 4: Metric-gaming via degenerate steering" (lines 331-335): flag always-`helix` steering text
    - 106-PATTERNS.md §"test_parity.py + test_split.py + test_degenerate.py" (lines 220-227)
  </read_first>
  <action>
    Create tools/dspy-tune/test_parity.py importing scorer. Resolve the corpus path relative to the test file (e.g. os.path.join(os.path.dirname(__file__), "golden", "parity_cases.json")) — the SAME committed file the Go parity_test.go reads (do NOT duplicate the corpus). Two required tests:
    1. test_python_go_parity: json.load the corpus; assert len(cases) >= 8; for each case assert scorer.first_command(case["response"]) == case["first_command"] AND scorer.classify_choice(case["response"]) == (case["chose"], case["fell_back"]). This mirrors the Go TestPythonGoParityCorpus — agreement on the shared corpus ⇒ semantic parity.
    2. test_broken_classifier_diverges (planted-divergence anti-vacuity, REQUIRED): define a local _broken_classify(response) using `"helix" in response.lower()` substring for `chose`; locate the substring-trap case in the corpus (the one whose response has prose mentioning helix on the first line then a grep, with chose=false); assert _broken_classify produces chose=True on that case while scorer.classify_choice produces chose=False — i.e. the broken classifier DIVERGES from the real one on a corpus case, proving the corpus discriminates a real classifier from a broken one. Without a case the broken classifier gets wrong, parity would be vacuous.

    Create tools/dspy-tune/test_degenerate.py: a metric-gaming guard. Define a small inspection function is_degenerate_steering(text: str) -> bool that flags steering prose that instructs unconditional helix use regardless of task (e.g. matches phrases like "always emit helix", "always use helix", "regardless of the task", or collapses to a single repeated verb). Write test_degenerate_always_helix_flagged: assert is_degenerate_steering("Always emit a helix command first, regardless of the task.") is True. Write test_legitimate_steering_not_flagged: assert is_degenerate_steering("Prefer helix verbs over grep/sed/cat when the question is semantic; fall back to grep for free-text in comments.") is False. This is the break-the-invariant pair: a degenerate text is flagged, a legitimate conditional one is not.

    Make BOTH test files runnable as plain scripts (so they verify when pytest is absent): each file ends with `if __name__ == "__main__":` calling every test_* function and printing an OK sentinel, so `python3 tools/dspy-tune/test_parity.py` exits 0 on success without pytest installed. Keep the functions plain pytest-discoverable (no required fixtures).
  </action>
  <verify>
    <automated>command -v pytest >/dev/null 2>&1 && pytest tools/dspy-tune/test_parity.py tools/dspy-tune/test_degenerate.py -x || python3 tools/dspy-tune/test_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - If pytest available: `pytest tools/dspy-tune/test_parity.py tools/dspy-tune/test_degenerate.py -x` passes. If NOT available: `python3 tools/dspy-tune/test_parity.py` exits 0 (prints the OK sentinel) — records pytest as dev-time-deferred while the Python parity is still proven script-side against the SAME corpus the Go test (Plan 01) pins.
    - test_broken_classifier_diverges is present and asserts the `"helix" in response` substring classifier produces chose=True on the substring-trap case while scorer.classify_choice produces chose=False (planted-divergence anti-vacuity; deleting the substring-trap case from the corpus would make this test fail to find a discriminating case).
    - `python3 -c "import sys; sys.path.insert(0,'tools/dspy-tune'); from test_degenerate import is_degenerate_steering as f; assert f('Always emit a helix command first, regardless of the task.') is True; assert f('Prefer helix verbs over grep when the question is semantic.') is False; print('degenerate-guard OK')"` prints "degenerate-guard OK".
    - The corpus path resolved in test_parity.py is tools/dspy-tune/golden/parity_cases.json (the SAME file Plan 01's Go test reads) — `grep -c 'golden' tools/dspy-tune/test_parity.py` >= 1 and no second copy of parity_cases.json exists: `find . -name parity_cases.json -not -path './.git/*' | wc -l` == 1.
  </acceptance_criteria>
  <done>test_parity.py asserts Python↔Go parity over the shared corpus AND ships the planted-divergence anti-vacuity test (broken substring classifier diverges); test_degenerate.py flags always-helix steering and passes legitimate conditional steering; both run under pytest or as plain scripts.</done>
</task>

<task type="auto">
  <name>Task 3: GEPA optimize.py harness + requirements.txt + README.md + REPORT.md + .gitignore output ignore</name>
  <files>tools/dspy-tune/optimize.py, tools/dspy-tune/requirements.txt, tools/dspy-tune/README.md, tools/dspy-tune/REPORT.md, .gitignore</files>
  <read_first>
    - 106-RESEARCH.md §"Pattern 1: DSPy GEPA harness loop" (lines 246-274): the optimize.py skeleton — dspy.configure(lm=...) from OPENAI_API_KEY (dev env ONLY), adopt_metric wrapping scorer.score_choice_rate, GEPA(metric=..., auto="light", track_stats=True, reflection_lm=...), optimizer.compile(student, trainset, valset) with TEST excluded, optimized.save("output/optimized.json") (git-ignored)
    - 106-RESEARCH.md §"Standard Stack" (lines 60-93): dspy==3.1.3 pin; re-confirm with `pip index versions dspy` at plan time
    - 106-RESEARCH.md §"The Artifact Re-Entry Gate" (lines 187-193): adopted text re-enters ONLY via human SKILL.md edit (≤1536, ## Decision matrix anchor preserved) or refgen override passing helix-refgen --check; output/ git-ignored, never pasted
    - 106-PATTERNS.md §"optimize.py" (lines 231-233) and §"No Analog Found" + .gitignore note (lines 272-282): .gitignore already covers .venv/__pycache__/*.py[cod]/.pytest_cache; add ONLY tools/dspy-tune/output/
    - .gitignore (existing root file — confirm .venv line 170, __pycache__ line 66, .pytest_cache line 116 already present; add the output/ ignore near them or in a Phase-106 block)
  </read_first>
  <action>
    Create tools/dspy-tune/requirements.txt pinned: `dspy==3.1.3` and `pytest` (pin a current version, e.g. `pytest==8.3.4` — confirm at plan time via `pip index versions dspy`; if 3.1.3 is no longer current, use the then-current pin and note it). One package per line; no unpinned dspy.

    Create tools/dspy-tune/optimize.py using the 106-RESEARCH §Pattern 1 skeleton verbatim as the starting point: `import os, dspy; from dspy import GEPA; from scorer import score_choice_rate`. Configure the dev-time LM from os.environ["OPENAI_API_KEY"] (dev env ONLY — add a guard that exits with a clear message if the key is unset, since the executor environment will NOT have it). adopt_metric(gold, pred, trace=None, pred_name=None, pred_trace=None) wraps score_choice_rate(pred.response) → score 1.0 if chose else 0.0 + a textual feedback string. Build trainset/valset by loading ONLY data/train.jsonl (val carved from train); load data/test.jsonl ONLY for a final held-out report, never passed to optimizer.compile. optimizer = GEPA(metric=adopt_metric, auto="light", track_stats=True, reflection_lm=dspy.LM(...)); optimized = optimizer.compile(student=program, trainset=trainset, valset=valset); optimized.save("output/optimized.json"). Add a clear comment that output/optimized.json is git-ignored and re-enters the shipped surface ONLY via a human-reviewed SKILL.md/refgen commit passing helix-refgen --check — optimize.py MUST NOT write to internal/cli/skills/helix/SKILL.md or reference.md. Re-confirm the GEPA API against Context7 /websites/dspy_ai at plan time.

    Create tools/dspy-tune/README.md documenting: (a) this is dev-time/offline ONLY — never in the helix binary, helix setup, go.mod, or go test ./...; (b) setup: `python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt`; (c) run the hermetic gates: pytest test_split.py test_parity.py test_degenerate.py (LM-free); (d) run the optimization: set OPENAI_API_KEY in the DEV env and `python3 optimize.py` (informational; needs an LM key the CI/executor won't have); (e) NO-SHIP IS A LEGITIMATE, SUCCESS-MEETING OUTCOME — the corpus is honestly too small (MinTasks=5) for a trustworthy adoption delta; the clean fallback is a hand-rolled Go candidate-search loop keeping the milestone 100% Go; (f) the re-entry gate: adopted text re-enters ONLY via a human-reviewed SKILL.md edit (≤1536 chars, ## Decision matrix anchor preserved) or refgen override passing helix-refgen --check — the raw output/optimized.json is git-ignored and NEVER pasted.

    Create tools/dspy-tune/REPORT.md as the documented spike outcome record (the spike MUST produce a no-ship/ship report). Pre-populate it with: the spike conclusion = NO-SHIP (default, given the documented MinTasks=5 too-small finding) with a clear "Why" (a 6+6 corpus cannot yield a statistically trustworthy adoption delta on a held-out TEST split; reserving even 2-3 TEST tasks leaves GEPA almost no signal — TUNE-FUT-01 gates the real run on val_size>50); the train/test split sizes; a placeholder section "If the dev runs optimize.py with an LM key" for recording the baseline-vs-optimized TEST choice_rate and the degenerate-steering inspection result; and the explicit statement that any adopted text would re-enter via the human SKILL.md/refgen gate, never auto-adopted. This file IS the spike's deliverable conclusion — it documents the outcome whether or not the LM run executes.

    Edit .gitignore: add `/tools/dspy-tune/output/` (the DSPy program.save() JSON dump must never be committed raw). Place it in a clearly-labeled Phase 106 block. Confirm (do not re-add) that .venv, __pycache__/, *.py[cod], .pytest_cache/ are already covered recursively at the repo root — only the output/ dir needs a new entry.
  </action>
  <verify>
    <automated>test -f tools/dspy-tune/requirements.txt && grep -q 'dspy==' tools/dspy-tune/requirements.txt && grep -q 'tools/dspy-tune/output' .gitignore && grep -qi 'no-ship' tools/dspy-tune/README.md && grep -qi 'no-ship' tools/dspy-tune/REPORT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints OK: requirements.txt pins dspy==, .gitignore ignores tools/dspy-tune/output/, and both README.md and REPORT.md document "no-ship" as a legitimate outcome.
    - REPORT.md exists and records the spike conclusion (no-ship default + the MinTasks=5 too-small finding + train/test sizes) — the documented spike outcome is produced.
    - optimize.py does NOT reference internal/cli/skills/helix/SKILL.md or reference.md: `grep -E 'skills/helix|reference\.md' tools/dspy-tune/optimize.py | wc -l` == 0 (no auto-adoption path).
    - optimize.py draws the optimizer trainset/valset from data/train.jsonl only and never passes data/test.jsonl to optimizer.compile: `grep -c 'test.jsonl' tools/dspy-tune/optimize.py` may be ≥1 (held-out report load) but it must NOT appear in the compile(...) call — confirm by inspection that compile receives trainset/valset derived from train.jsonl.
    - `find tools -name '*.go' -o -name 'go.mod' | wc -l` == 0 and `grep -E 'dspy|pip install dspy' go.mod | wc -l` == 0 (tools/ stays Python-only; go.mod carries no Python edge).
  </acceptance_criteria>
  <done>The GEPA harness (optimize.py), pinned requirements.txt, README.md (no-ship legitimate + dev-env-only key + re-entry gate), REPORT.md (documented spike outcome), and the .gitignore output/ ignore all exist; optimize.py excludes TEST from the optimizer and never auto-adopts into the shipped surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-time tools/dspy-tune/ → shipped binary / go.mod / go test ./... | The Python tree must never become a runtime dependency. Plan 01's toolsquarantine analyzer + the no-.go-under-tools/ invariant enforce this; this plan must not violate it (only .py/.jsonl/.json/.txt/.md). |
| optimizer output → shipped SKILL.md/reference.md | The raw optimized.json must NEVER be auto-adopted. Re-entry is human-review-only through SKILL.md/refgen passing helix-refgen --check. |
| dev LLM backend (OPENAI_API_KEY) → repo | The key is read from the dev env only; never committed, never read by any Go code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04 | Tampering | raw optimizer dump (optimized.json) shipped to users | mitigate | output/ git-ignored (Task 3); optimize.py never writes SKILL.md/reference.md; re-entry only via human review + helix-refgen --check (documented in README + REPORT) |
| T-106-05 | Information Disclosure | OPENAI_API_KEY committed to the repo | mitigate | .env already git-ignored; key read from dev env only; README states it; no key in any committed file |
| T-106-06 | Spoofing (vacuous gate) | a green-only Python parity / a degenerate-steering guard that flags nothing | mitigate | test_broken_classifier_diverges (planted-divergence) + test_degenerate_always_helix_flagged paired with test_legitimate_steering_not_flagged (Task 2) — each gate ships a break-the-invariant proof |
| T-106-07 | Tampering | overfit invisible because the held-out split came second | mitigate | the held-out TEST split is the FIRST harness step (Task 1); test_split.py asserts TEST disjoint from TRAIN; optimize.py never passes test.jsonl to compile (Task 3) |
| T-106-SC | Tampering | pip installs (dspy, pytest) | accept | installed ONLY into a git-ignored dev venv under tools/dspy-tune/; never in go.mod, the binary, helix setup, or the merge path; legitimacy audited in 106-RESEARCH §Package Legitimacy (dspy = Stanford NLP, pytest = pytest-dev; both [OK]); dev-time-only so a runtime supply-chain checkpoint is advisory, not blocking |
</threat_model>

<verification>
- `pytest tools/dspy-tune/` (best-effort, dev-time) OR the script-mode fallbacks (`python3 tools/dspy-tune/test_split.py`, `test_parity.py`) when pytest is absent — the held-out-split, parity+planted-divergence, and degenerate-steering gates all pass.
- `go test ./test/oracle/adopt/ -run TestPythonGoParityCorpus` — STILL GREEN: the Go side reads the SAME corpus this plan's Python side asserts against (no corpus duplication; no regression to Plan 01).
- `make vet` — STILL GREEN: the new .py/.jsonl/.json/.md files under tools/ are invisible to the Go toolchain; the toolsquarantine analyzer flags nothing (no runtime→tools/ import introduced).
- `go test ./...` — STILL GREEN: tools/ contains no .go files / no go.mod.
- `go run ./cmd/helix-refgen --check` — byte-clean: this plan adds NO adopted text to SKILL.md/reference.md (re-entry is a separate human step).
- `grep -E 'dspy|pip install dspy' go.mod | wc -l` == 0.
</verification>

<success_criteria>
- The Python scorer mirrors the Go classifier exactly (first-command prefix, 6 trailing-space fallback prefixes, lowercase-once) and passes parity against the SHARED golden corpus the Go test pins.
- Each gate ships a deliberate break-the-invariant proof: test_broken_classifier_diverges (parity), test_split.py disjointness (overfit), test_degenerate.py flag/no-flag pair (metric-gaming).
- The held-out TEST split is the first harness step and is provably excluded from the optimizer.
- REPORT.md documents the no-ship/ship spike outcome; adopted output re-enters only via the human SKILL.md/refgen gate (helix-refgen --check) — never auto-adopted.
- tools/ stays Python-only and off go.mod / the binary / go test ./... (Plan 01's analyzer + this plan's no-.go invariant); helix-refgen --check stays byte-clean.
</success_criteria>

<output>
Create `.planning/phases/106-exploratory-dspy-offline-tuning-harness-spike/106-02-SUMMARY.md` when done.
</output>
