---
status: passed
milestone: v2.4
audited: 2026-06-24
requirements_total: 10
requirements_satisfied: 10
phases_total: 4
phases_verified: 4
---

# v2.4 Milestone Audit — Corpus Growth & Real Optimization Verdict (TUNE-FUT-01)

## Verdict: PASSED

All 10 requirements satisfied, all 4 phases verified, the pipeline runs end-to-end and produced a real, gate-cleared ship/no-ship verdict. The single-binary / no-runtime-Python boundary holds. Documented limitations are recorded as follow-ons, none blocking the milestone's deliverable (a real, honest verdict — the explicit goal that turns v2.3's corpus-size no-ship into a measured one).

## Requirements (10/10)

| REQ | Phase | Status | Evidence |
|-----|-------|--------|----------|
| CORPUS-01 | 111 | ✓ | `build_corpus.py --check` → 103 descriptors (go 39 / python 34 / rust 30 ≥ 101) |
| CORPUS-02 | 111 | ✓ | `split_corpus` → train 26 / val 26 / heldout 51 (>50), pairwise disjoint; planted-leak RED test |
| SCALE-01 | 112 | ✓ | optimizer program+reflection LM pinned `deepseek-v4-flash`, DeepSeek-primary/OpenAI-fallback |
| SCALE-02 | 112 | ✓ | rollout cap + bounded num_threads + 429 backoff + AGENT_MAX_TURNS, all tested |
| SCALE-03 | 112 | ✓ | `grade_swebench` refuses harness 0-tests/`resolved` footgun (break-the-invariant test) |
| RUN-01 | 113 | ✓ | real `optimize.py` → `optimized.json`, `val_size=51 (>50)` (GEPA non-evolving — TUNE-FUT-06) |
| RUN-02 | 113 | ✓ | ON/OFF attribution: delta **+0.0392** (3/51 vs 1/51), val_size 51, cost $0.234 |
| RUN-03 | 113 | ✓ | SWE-bench Verified gold **2/2 resolved** on Podman; fail-not-skip proven |
| REPORT-01 | 114 | ✓ | `REPORT.md` v2.4 verdict (SHIP-by-rule, do-not-adopt) — REPORT-only |
| ADOPT-05 | 114 | ✓ | go.mod untouched since v2.0; `make vet` green; adoption gate desync → `--check` exit 1 |

## Phases (4/4 verified)
- 111 PASSED (5/5) · 112 PASSED (4/4) · 113 PASSED (4/4) · 114 PASSED (2/2). Each VERIFICATION.md `status: passed`.

## Integration / E2E
- The full pipeline ran end-to-end for real: corpus → 3-way sequestered split → GEPA (RUN-01) + ON/OFF attribution (RUN-02) → SWE-bench confirm (RUN-03) → REPORT (114). The verdict artifacts (`output/{optimized,attribution,heldout_test,swebench_confirm}.json`, `REPORT-RUN.md`) were produced by real billed DeepSeek + real Podman containers. Total LLM cost ≈ **$0.23**.
- **The real story of v2.4:** the v2.3 pipeline was never functional end-to-end (its hermetic fakes hid three integration gaps); v2.4 found and fixed all three (agent verb argv; `helix activate`→`activate_project` real product bug; GEPA candidate→agent threading) and made the pipeline actually produce a verdict.

## Build / test evidence
- `go build ./...` OK; `make vet` (8 analyzers incl. `toolsquarantine`) green; `go test ./internal/cli` green (12.7s); hermetic Python suite **68 passed** (bare `pytest`, after the testdata-collection fix the audit caught).

## Boundary (ADOPT-04)
- Zero new Go deps (`go.mod`/`go.sum` last touched `7f20a874`, v2.0). One Go change all milestone: the **user-approved** `helix activate`→`activate_project` product-bug fix (existing `forwarder.CallTool`, no deps). No `helix`→Python shell; optimizer never writes the shipped surface; run output git-ignored. New deps are dev-venv Python only.

## Tech debt / follow-ons (none blocking)
- **TUNE-FUT-06** (new): rebuild the GEPA program as a proper agent module that emits a predictor trace, so reflective mutation actually evolves the steering (RUN-01 ran but did not evolve this milestone).
- **TUNE-FUT-05**: larger stratified SWE-bench K (oracle + fail-not-skip proven at K=2).
- **decide_ship significance test**: the +0.0392 delta (2 tasks, n=51) is within binomial noise; adoption is REPORT-only and explicitly NOT recommended on this margin.
- **TUNE-FUT-03**: actual SKILL.md adoption (deferred by design; human `helix-refgen --check`-gated).
- **Pre-existing (carried forward, not from v2.4):** 3 stale-but-completed quick-tasks (`260414-e5n`, `260617-j29`, `260617-t7x`) + the `cmd/helix-bench/TestRunSubcommandWiresDeltaPass` network/HELIX_BIN failure.

## Notable
- A Go **product bug** was discovered and fixed: `helix activate` (the shipped SessionStart-hook flow) did not enable the CLI file/edit verbs (`no_workspace`) because only the `activate_project` tool sets the file-tool workspace and it had no CLI verb. Now fixed + E2E regression-tested — a real product improvement beyond the milestone's optimization scope.
