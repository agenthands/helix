# Phase 113 — Plan 01: The Real Cost-Aware Run

**Requirements:** RUN-01, RUN-02, RUN-03
**Approach:** real billed run on DeepSeek-primary, cost-bounded. Dev-time Python only.

## Tasks

### Task 0 — End-to-end smoke (de-risk before bulk spend)
- Build `run_real.py` with a `smoke` mode: load 1 python descriptor, run `ReActAgent` (steering OFF) for real in a sandbox, grade via `grade_aider.grade_task`. Confirm the full chain works (helix-editing + native test grading + DeepSeek calls). On failure → fix-as-needed (in scope) or surface a blocker. NOT a green-vacuous skip.

### Task 1 — RUN-01: real GEPA optimization (cost-bounded)
- `AIDER_TASKS_DIR=corpus GEPA_MAX_METRIC_CALLS=<small> AGENT_MAX_TURNS=<small> uv run python optimize.py`.
- Assert: writes git-ignored `output/optimized.json`, prints `train/val/heldout` with `heldout > 50` (gate cleared, not the no-ship short-circuit), and persists `output/heldout_test.json`.

### Task 2 — RUN-02: honest ON/OFF attribution + per-arm cost
- `run_real.py attribution`: load the sequestered `output/heldout_test.json`; run the agent ON (optimized steering text) and OFF (control) over the SAME held-out split via `attribution.compute_attribution`, each arm wrapped in `MeteredLLM` for per-arm cost; write `output/attribution.json` (ArmResult ON/OFF, delta, val_size, per-arm cost) + the `decide_ship` verdict.
- Honest: a low/zero/negative delta is a valid result (likely no-ship); never fabricate a positive delta.

### Task 3 — RUN-03: SWE-bench Verified confirming slice (Podman)
- Start `podman system service --time=0 &`; `DOCKER_HOST=unix://$XDG_RUNTIME_DIR/podman/podman.sock`.
- K≈2 gold-patch smoke (`predictions_path=gold`) via `grade_swebench` to confirm the harness runs on Podman; then a stratified K=25–50 slice. Write `output/swebench_confirm.json`.
- **fail-not-skip**: a *requested* real run that yields no result FAILS loudly. If Podman/images are genuinely unavailable, surface a blocker (do not silently pass).

## Success Criteria (phase-level)
1. RUN-01: real `optimized.json` written; `val_size>50` proven (gate cleared).
2. RUN-02: real ON/OFF delta + per-arm cost in `output/attribution.json` (honest, not fabricated).
3. RUN-03: SWE-bench Verified confirming agreement (K=25–50) recorded, fail-not-skip; gold smoke first.
4. Boundary: `git diff go.mod` empty; subprocess-only; `make vet` green.

## Verification
- Inspect `output/optimized.json`, `output/attribution.json`, `output/swebench_confirm.json`.
- `git diff --quiet go.mod go.sum`; `make vet`.
