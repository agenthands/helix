---
author: architect
responsible: architect
milestone: v2.8
status: passed
audited: 2026-06-30
audit_method: goal-backward, grounded in re-reading the code system (not the SUMMARYs)
parent_artifacts:
  - .planning/milestones/v2.8-ROADMAP.md
  - .planning/REQUIREMENTS.md
---

# MILESTONE-AUDIT — v2.8 Graph Intelligence Depth (cbm-mcp parity)

## Scope audited

v2.8 as roadmapped: **Workstream A** (SEMANTICALLY_RELATED via Random Indexing,
Phases 121–123) + the **B0-a** decision consequence (Phase 124: reclaim the
DATA_FLOWS name). **Workstream B's feature build** (true interprocedural
DATA_FLOWS) was explicitly gated/deferred from the start — it is NOT in scope
here and its absence is not a gap against this milestone.

## Verdict: PASSED

The built system matches the milestone's stated intent. Confirmed by re-reading
the code, not the phase summaries.

## Goal-backward grounding (code reality)

| Milestone intent | Code reality (verified at HEAD) | Verdict |
|---|---|---|
| The unwired SEMANTICALLY_RELATED edge gets a producer (0 → ≥1) | `semanticallyRelatedEdges` emits `EdgeKind:"SEMANTICALLY_RELATED"` Source `random_index` (`semantic_similarity_edges.go:435`), wired at `semantic_wiring.go:2401` | ✅ |
| Via Random Indexing (cbm-mcp's mechanism), single-binary-safe | `internal/semantic/relatedidx/` pure-Go leaf pkg; `go.mod`/`go.sum` byte-unchanged across the milestone | ✅ |
| Distinct from SIMILAR_TO (not a duplicate edge) | Measured on real Go: same-struct/disjoint-vocab Jaccard 1.0/cosine 0.0; diff-struct/shared-vocab Jaccard 0.0/cosine 0.80 (`TestRelatedness_DistinctAndSelective`) | ✅ |
| Selective (not boilerplate noise) | Stop-list (`stopwords.go`); selectivity guard mutation-confirmed RED without it (unrelated cosine 0.69 > 0.55) | ✅ |
| Deterministic graph output | int32 accumulation; 50-shuffle byte-identical (`TestDeterminism_NShuffles`) | ✅ |
| Readable from a `helix` verb | `explain-symbol-deep` surfaces it unfiltered (`TestShapeEdges_SurfacesSemanticallyRelated`); B1 correction folded | ✅ |
| B0-a: reclaim DATA_FLOWS for real flow | structural edge renamed `STRUCTURAL_TWIN` (`:297`); `DATA_FLOWS`/`data_flows` reserved, producerless, still validatable | ✅ |

## Refinement check (does the concrete system satisfy the abstract intent?)

The milestone's abstract goal — "close the cbm-mcp *depth* gap for the one
unwired edge kind, honestly" — is refined by the concrete system: a new edge
that is a genuinely new signal (orthogonal to SIMILAR_TO by construction,
measured), not theater. The independent red-team (agent://RedTeamV28,
PROCEED-WITH-FIXES) caught that the original read-surface acceptance was
unsatisfiable (B1) and that the anti-vacuity guard tested the wrong property
(M1); both were corrected before close. That the audit's headline edge is
provably distinct AND selective AND readable — each with an executable test — is
what distinguishes this from a plausible-looking-but-vacuous edge.

## Quality gates

- Build/test: `go build ./...` clean; **45 pkg ok**; `make vet` (8 vettools) clean; gofmt clean.
- Invariants held: zero new Go deps; no schema migration; generated-file `--check` gates (helix-cligen, helix-refgen) pass.
- `debt query: raw_debt=0` — no open coordination debt.
- `ledger validate: 0 errors` (1031 warns are all the pre-v1.0 `no_records_section` convention, project-wide, not a v2.8 artifact).

## Findings

1. **[process, medium] Typed gate records not emitted.** v2.8's verify gates live
   in prose `VERIFICATION.md` files, not §5 `gate_result`/`finding` typed records,
   so `measure refusal-rate` reads `0 gates` and `quality report` reads
   `candidate_pairs=0`. The verification itself is real (executable tests,
   mutation-confirmed), but it is not machine-legible to the measurability
   sidecar. Typed records are an affordance (not forced), but a future milestone
   that wants the refusal-rate/quality signals to mean anything should emit them.
   Not a correctness gap; an observability gap. Recorded honestly rather than
   claiming a measured streak that wasn't produced.
2. **[substrate, informational] Degraded run.** No native standing-team / SMTC
   MCP this session; the lead loop ran inline and delegated the one
   independence-critical step (red-team) to an `oracle` subagent. Code claims are
   Structural-tier + direct test execution, never SMTC-Decision. Stated on day one.
3. **[disk vs state, low] git not committed.** The working tree carries v2.8's
   edits plus pre-existing uncommitted work from prior sessions; nothing is
   committed. A commit is the user's call (the pre-existing changes aren't mine to
   bundle).

## Not done (by design, not omission)

- **Workstream B feature build** (true interprocedural DATA_FLOWS: def-use →
  arg→param → propagation/taint substrate). High-effort 3–4 phases; needs its own
  roadmap + red-team cycle and carries open decisions (storage: `Reason` vs new
  metadata column; node granularity: symbol- vs variable-level). B0-a (the name
  reclaim) is done; the feature is a future milestone.
- cbm-mcp's other differentiators (Cypher, vector/neural semantic search, 3D viz,
  team packaging, 158-language coverage) remain out of scope — see RESUME.md.
