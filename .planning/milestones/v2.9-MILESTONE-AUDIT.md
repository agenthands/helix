---
author: architect
responsible: architect
milestone: v2.9
status: passed
audited: 2026-06-30
audit_method: goal-backward, grounded in re-reading the code system (not the SUMMARYs)
parent_artifacts:
  - .planning/milestones/v2.9-REQUIREMENTS.md
  - .planning/phases/125-intraprocedural-flow-summary-engine/125-01-SUMMARY.md
  - .planning/phases/126-arg-to-param-dataflows-emission/126-01-SUMMARY.md
---

# MILESTONE-AUDIT — v2.9 Interprocedural DATA_FLOWS (param-flow reachability substrate)

## Scope audited

v2.9 as roadmapped: a real producer for the reserved `DATA_FLOWS` edge kind —
case-1 param→param interprocedural flow (2 phases: 125 flow-summary engine +
126 emission/read-surface/reachability). Full taint analysis was explicitly
out of scope (deferred variable-level nodes).

## Verdict: PASSED

The built system matches the milestone's stated intent. Confirmed by re-reading
the code, not the phase summaries.

## Goal-backward grounding (code reality, verified at HEAD)

| Milestone intent | Code reality | Verdict |
|---|---|---|
| `DATA_FLOWS` gets a producer (0 → ≥1) | `dataFlowEdges` emits `EdgeKind:"DATA_FLOWS"` Source `def_use` (`semantic_similarity_edges.go`); wired at `semantic_wiring.go` batch emission | ✅ |
| Arg→param flow through a resolved in-repo call | `fingerprintedNode.flow` (Phase 125 `Summary`) → caller.param → callee.param; callee resolved via batch name index | ✅ |
| NOT the structural-shape edge (distinct) | edges are param-anchored (`SrcKind/DstKind:"parameter"`), distinct from `STRUCTURAL_TWIN`/`SIMILAR_TO` (function-anchored); e2e asserts | ✅ |
| Callee params NOT via the flat DEFINES heuristic (B1) | `nodeToParams` built by **emit-order adjacency** in `factsFromExtracted` (consecutive `KindParameter` run); NOT DEFINES | ✅ |
| Anti-mis-bind (D5) | `nameCount`; callee resolves only when count == 1; unit-tested | ✅ |
| No fabricated edges for external/unresolved/ambiguous | `dataFlowEdges` skips zero/self/unresolved/multi-candidate; unit-tested | ✅ |
| Reachable from a `helix` verb | `MapInternalKind("DATA_FLOWS")→EdgeKindDataFlows` (already wired v2.8) → `explain-symbol-deep` surfaces it | ✅ |
| Reachability payoff (folded Phase 127) | `src→mid→sink` reachable end-to-end by walking edges (e2e); broken-hop revert-and-fail passes | ✅ |
| Single-binary / zero-dep invariant | `dataflow` imports only tree-sitter + stdlib; `go.mod`/`go.sum` byte-unchanged | ✅ |
| No schema migration | `EdgeFact` unchanged; binding = node identity | ✅ |

## Refinement check

The abstract goal — "give DATA_FLOWS an honest producer, a genuine flow signal" —
is refined by a concrete edge that is provably **distinct** (param-anchored, not
similarity), **selective** (dead params / external callees emit nothing), and
**reachable** (each with an executable test). The independent red-team
(`agent://RedTeamV29`, PROCEED-WITH-FIXES) caught two blocking design errors
pre-build (DEFINES-is-flat; reference-node namespace) and six majors — all
folded before emission. That the headline edge is param-precise AND the
reachability proof holds end-to-end on REAL extraction is what distinguishes
this from a plausible-but-vacuous edge.

## Honest scope (stated plainly)

This is **syntactic param→param pass-through reachability** — the classical
function-summary taint-composition primitive. It is NOT taint analysis: no
in-body origins, no field/heap flow, no source/sink modeling, no sanitization.
Framed honestly in the REQUIREMENTS + both SUMMARYs.

## Quality gates

- `go build ./...` clean; **15 pkgs ok** (dataflow/extract/daemon/skill-semantic);
  **`make vet` (go vet + 8 vettools) clean**; generated-file `--check` gates
  (helix-cligen, helix-refgen) pass.
- Invariants: zero new Go deps; no schema migration.
- Anti-vacuity: dead-param (Phase 125) + broken-hop (Phase 126) revert-and-fail
  guards, both green.

## Security review (inline, lightweight)

Pure local graph analysis over already-extracted tree-sitter facts; no network,
no auth, no untrusted-input surface, no new external code path. Low security
risk. No findings.

## Findings

1. **[process, medium] Typed gate records still not emitted.** Like v2.8, the
   verify gates are executable tests + this audit, not §5 `gate_result` records,
   so `measure refusal-rate` reads 0 gates. Observability gap, not correctness.
2. **[docs, low] RESOLVED this session.** There is no `docs/` edge-reference
   page (only runbooks), so the canonical contributor-facing edge doc is
   `internal/skill/semantic/edge_kind_surface.go` (its DATA_FLOWS comment is now
   current — "produced", not "reserved") plus the README "Semantic graph"
   section, refreshed to state the true 11-extractor set + the full edge model
   (DATA_FLOWS included). `docgen --check` / `helix-refgen --check` stay green.
3. **[substrate, informational] Inline run.** No native standing-team; the lead
   loop ran inline, delegating the one independence-critical step (red-team) to
   an `oracle` subagent. SMTC was up this session but not used for Decision-tier
   grounding (Structural-tier + direct test execution).

## Not done (by design, not omission)

- Dedicated `trace-data-flow` verb / `get-change-impact-graph` multi-projection
  rework (milestone-sized).
- Variable-level nodes / in-body origins / field-flow / sanitization (full taint
  analysis) — future milestone.
- Queryable arg/param-position `semantic_edges` column (deferred to the trace verb).
- cbm-mcp's other differentiators (Cypher, vector/neural search, 3D viz, team
  packaging, 158-lang) — see RESUME.md.

## Ship state

Working tree carries v2.9's edits; **not yet committed** (the user's call, as at
v2.8 close). All gates green.
