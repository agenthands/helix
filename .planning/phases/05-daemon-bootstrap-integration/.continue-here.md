---
context: phase
phase: 05-daemon-bootstrap-integration
task: 0
total_tasks: 0
status: not_started
last_updated: 2026-04-08T13:35:38.642Z
---

<current_state>
Phase 5 (Daemon Bootstrap Integration) was just created as a gap closure phase after the v1.0 milestone audit. No plans have been created yet. The phase needs discuss/plan before execution.

The milestone audit found that all 68 v1.0 requirements have working, tested code, but 47/68 are unreachable from the production binary because daemon.go was never updated to wire in components built in Phases 2-4.
</current_state>

<completed_work>

Completed Tasks:
- Milestone audit: v1.0-MILESTONE-AUDIT.md created with full gap analysis
- Gap closure planning: Phase 5 added to ROADMAP.md with 3 planned plans
- REQUIREMENTS.md: DGN-01..03 checkboxes fixed (were marked Pending, code is verified complete)
- STATE.md: Updated to reflect Phase 5 as current
</completed_work>

<remaining_work>

- Plan Phase 5 (discuss-phase and/or plan-phase)
- Execute 05-01: Fix component contracts (RegisterFn, profile skill names, pool-installer)
- Execute 05-02: Daemon bootstrap wiring (kernel, skills, tools, middleware, config, shutdown)
- Execute 05-03: Integration smoke tests for all 6 E2E flows
- Re-audit milestone after Phase 5 completes
- Complete milestone if audit passes
</remaining_work>

<decisions_made>

- Single gap closure phase with 3 plans (all gaps share one root cause)
- Plan order: fix contracts first (prerequisites), then daemon wiring, then integration tests
- No new features — purely orchestration code connecting existing tested components
</decisions_made>

<blockers>
None
</blockers>

## Required Reading (in order)
1. `.planning/v1.0-MILESTONE-AUDIT.md` — full gap analysis with 7 integration gaps and 5 broken E2E flows
2. `.planning/ROADMAP.md` — Phase 5 definition with success criteria
3. `internal/daemon/daemon.go` — the file that needs the most changes (bootstrap wiring)
4. `internal/mcp/server.go` — where tools get registered with MCP SDK

## Infrastructure State
- go.mod has a minor unstaged change (unrelated to Phase 5)
- All existing tests pass (`go test ./...`, `go vet ./...`)
- No running services or background processes

<context>
The project is a Go-native code intelligence platform (Serena 2.0). Phases 1-4 built all components in isolation — each passes unit tests and verification. But the daemon startup code was never updated to instantiate the kernel, register tools, import skill packages, or install profile middleware. The fix is focused: wire existing components into daemon.go and fix a few contract issues (skill RegisterFn no-ops, profile YAML skill name mismatches, pool not using installer).
</context>

<next_action>
Start with: `/gsd-plan-phase 5` (or `/gsd-discuss-phase 5` first if context gathering preferred)
</next_action>
