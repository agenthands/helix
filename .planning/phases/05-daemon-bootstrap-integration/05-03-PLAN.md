---
phase: 05-daemon-bootstrap-integration
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - internal/daemon/daemon_integration_test.go
  - internal/daemon/bootstrap_test.go
autonomous: true
requirements:
  - SYM-01
  - SYM-02
  - SYM-03
  - SYM-04
  - SYM-05
  - SYM-06
  - SYM-07
  - SYM-08
  - SYM-09
  - EDT-01
  - EDT-02
  - EDT-03
  - EDT-04
  - EDT-05
  - EDT-06
  - FIL-01
  - FIL-02
  - FIL-03
  - FIL-04
  - FIL-05
  - FIL-06
  - DGN-01
  - DGN-02
  - DGN-03
  - DMN-07
  - DMN-08
  - DMN-09
  - DMN-10
  - DMN-11
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04
  - MEM-05
  - WFL-01
  - WFL-02
  - WFL-03
  - PRF-01
  - PRF-02
  - PRF-03
  - PRF-04
  - PRF-05
  - LNG-01
  - LNG-02
  - LNG-03
  - WRK-02
  - WRK-03

must_haves:
  truths:
    - "Daemon bootstrap creates kernel, registers 30+ tools, and all are listed via MCP tools/list"
    - "Skill-provided tools (memory, workflow, profile) are registered and callable"
    - "Profile filtering reduces tool list when a restrictive profile is active"
    - "Kernel tools are registered and can be invoked (at least to the point of parameter validation)"
    - "Daemon shuts down cleanly without panics or leaked goroutines"
  artifacts:
    - path: "internal/daemon/bootstrap_test.go"
      provides: "Bootstrap integration tests"
      contains: "TestBootstrapRegistersAllTools"
    - path: "internal/daemon/daemon_integration_test.go"
      provides: "E2E flow smoke tests"
      contains: "TestE2E"
  key_links:
    - from: "internal/daemon/bootstrap_test.go"
      to: "internal/daemon/daemon.go"
      via: "daemon.New() creating full bootstrap"
      pattern: "daemon\\.New"
    - from: "internal/daemon/daemon_integration_test.go"
      to: "internal/mcp/server.go"
      via: "MCP tool invocation"
      pattern: "tools/list|CallTool"
---

<objective>
Write integration smoke tests proving all 6 E2E flows work: tool registration completeness, skill tool invocation, profile filtering, kernel tool reachability, mode switching, and clean shutdown.

Purpose: These tests are the final gate confirming that the daemon bootstrap wiring from Plan 02 actually works end-to-end. Without them, we have compilation success but no runtime proof.

Output: Integration test files that verify the 6 E2E flows identified in the milestone audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-daemon-bootstrap-integration/05-CONTEXT.md
@.planning/phases/05-daemon-bootstrap-integration/05-01-SUMMARY.md
@.planning/phases/05-daemon-bootstrap-integration/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap integration tests for tool registration and profile filtering</name>
  <read_first>
    - internal/daemon/daemon.go (fully wired daemon after Plan 02)
    - internal/daemon/imports.go (blank imports)
    - internal/mcp/server.go (SerenaMCPServer, tool registry)
    - internal/mcp/registry.go (ToolRegistry, ToolDef)
    - internal/skill/registry.go (All, ToolProviders, InitAll)
    - internal/config/loader.go (ResolveProfile)
    - internal/config/config.go (SerenaConfig)
    - internal/profile/loader.go (ProfileStore, Profile)
  </read_first>
  <files>
    internal/daemon/bootstrap_test.go
  </files>
  <action>
    Create `internal/daemon/bootstrap_test.go` with tests that verify the daemon bootstrap wiring without starting network listeners.

    **Test 1: TestBootstrapRegistersAllTools**
    - Create a minimal SerenaConfig with defaults
    - Call `daemon.New(cfg, logger)` (this triggers the full bootstrap)
    - Access the MCP server's tool registry via the daemon
    - Assert that ALL expected tools are registered. The complete list (30+ tools):
      Symbol (9): `go_to_definition`, `find_references`, `get_symbol_overview`, `search_symbols`, `get_hover_info`, `find_implementations`, `get_call_hierarchy`, `get_type_hierarchy`, `analyze_blast_radius`
      Edit (6): `replace_symbol_body`, `insert_before_symbol`, `insert_after_symbol`, `rename_symbol`, `safe_delete_symbol`, `verify_edit`
      File ops (6): `read_file`, `create_file`, `list_directory`, `find_files`, `search_in_files`, `replace_in_file`
      Diag (3): `get_diagnostics`, `get_code_actions`, `format_code`
      Memory (7): `write_memory`, `read_memory`, `list_memories`, `search_memories`, `rename_memory`, `edit_memory`, `delete_memory`
      Workflow (2): `onboard_project`, `prepare_for_new_conversation`
      Profile (2): `switch_mode`, `get_token_budget`
      Built-in (3): `ping`, `echo`, `activate_project`
    - Total expected: 38 tools. Assert `len(registry.All()) >= 38` and check specific names exist.

    **Test 2: TestBootstrapSkillsInitialized**
    - After `daemon.New()`, call `skill.All()` and verify at least 7 skills registered: `memory`, `workflow`, `profile`, `symbol-retrieval`, `symbol-editing`, `file-ops`, `diagnostics`
    - Verify `skill.ToolProviders()` returns at least 7 providers

    **Test 3: TestBootstrapProfileResolved**
    - Create config with `Profile: "claude-code"`
    - Call `daemon.New()` and verify the active profile is "claude-code"
    - Verify the profile's skill filter excludes tools like `read_file` (which claude-code excludes)

    **Test 4: TestBootstrapDefaultProfileIsFull**
    - Create config with empty Profile (defaults to "full")
    - Verify all tools are available (no filtering)

    NOTE: If `daemon.New()` does not expose the needed internal state for assertions, add minimal accessor methods to the Daemon struct:
    - `func (d *Daemon) MCPServer() *mcp.SerenaMCPServer` (if not already accessible)
    - `func (d *Daemon) ActiveProfile() *profile.Profile`
    The daemon struct can also be tested by checking the MCP server's tool registry.

    Use `t.Cleanup(func() { skill.Reset() })` to reset global skill registry between tests to avoid cross-test contamination.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && go test ./internal/daemon/ -run TestBootstrap -count=1 -timeout 60s -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/daemon/bootstrap_test.go contains `func TestBootstrapRegistersAllTools`
    - internal/daemon/bootstrap_test.go contains `func TestBootstrapSkillsInitialized`
    - internal/daemon/bootstrap_test.go contains `func TestBootstrapProfileResolved`
    - internal/daemon/bootstrap_test.go contains `func TestBootstrapDefaultProfileIsFull`
    - TestBootstrapRegistersAllTools asserts at least 38 tools registered
    - TestBootstrapSkillsInitialized asserts at least 7 skills
    - `go test ./internal/daemon/ -run TestBootstrap -count=1 -timeout 60s` exits 0
  </acceptance_criteria>
  <done>Bootstrap tests prove that daemon.New() creates all subsystems, registers all 38 tools, initializes all 7 skills, and resolves the configured profile.</done>
</task>

<task type="auto">
  <name>Task 2: E2E flow smoke tests for tool invocation, mode switching, and shutdown</name>
  <read_first>
    - internal/daemon/daemon.go (Run method, errgroup structure)
    - internal/daemon/shutdown.go (shutdown sequence)
    - internal/daemon/bootstrap_test.go (from Task 1, for patterns)
    - internal/skill/memory/skill.go (ExecuteTool for memory)
    - internal/skill/workflow/skill.go (ExecuteTool for workflow)
    - internal/profile/skill.go (ExecuteSwitchMode, ExecuteGetTokenBudget)
    - internal/mcp/session.go (SessionInfo)
  </read_first>
  <files>
    internal/daemon/daemon_integration_test.go
  </files>
  <action>
    Create `internal/daemon/daemon_integration_test.go` with smoke tests for the 6 E2E flows.

    **Test 5: TestE2EMemoryToolInvocation**
    - Create daemon with default config
    - Get the memory skill via `skill.Get("memory")`
    - Type-assert to interface with `ExecuteTool(name string, args map[string]interface{}) (string, error)`
    - Call `ExecuteTool("write_memory", map[string]interface{}{"name": "test-memory", "content": "hello"})` using a temp dir for ProjectDir
    - Assert result contains "written successfully"
    - Call `ExecuteTool("read_memory", map[string]interface{}{"name": "test-memory"})`
    - Assert result contains "hello"
    - Call `ExecuteTool("list_memories", map[string]interface{}{})` and verify non-empty
    - Call `ExecuteTool("delete_memory", map[string]interface{}{"name": "test-memory"})` and verify success

    **Test 6: TestE2EModeSwitching**
    - Create daemon with config `Profile: "full"`
    - Get profile skill via `profile.GetProfileSkill()`
    - Wire a mock SessionProvider with session Mode="edit", Profile="full"
    - Call `ExecuteSwitchMode("read")` and verify PreviousMode="edit", CurrentMode="read"
    - Call `ExecuteSwitchMode("review")` and verify success
    - Verify mode history has 2 transitions

    **Test 7: TestE2ETokenBudget**
    - Get profile skill
    - Call `ExecuteGetTokenBudget("full", "edit", "detailed")`
    - Assert TotalTokens > 0 and ToolCount > 0
    - Assert PerTool is non-empty (since format is "detailed")

    **Test 8: TestE2ECleanShutdown**
    - Create daemon with default config
    - Start `daemon.Run()` in a goroutine with a cancellable context
    - Cancel context after 100ms
    - Verify Run() returns without panic
    - Verify no goroutine leaks (use `runtime.NumGoroutine()` before/after with reasonable tolerance)

    **Test 9: TestE2EProfileConfigLayering**
    - Create config with `Profile: "ci-bot"`
    - Call `config.ResolveProfile(cfg, globalDir)`
    - Verify returned profile name is "ci-bot"
    - Verify the profile excludes editing tools (ci-bot excludes replace_symbol_body etc.)
    - Verify `skill.ResolveTools` with ci-bot's skills/exclude_tools produces a tool list without editing tools

    **Test 10: TestE2EWorkflowOnboarding**
    - Get workflow skill, call `ExecuteTool("onboard_project", map[string]interface{}{})`
    - Verify result contains "Project Root" or "languages" (onboarding output)

    Use `t.TempDir()` for all file paths. Use `t.Cleanup(func() { skill.Reset() })` for registry cleanup.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && go test ./internal/daemon/ -run TestE2E -count=1 -timeout 120s -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/daemon/daemon_integration_test.go contains `func TestE2EMemoryToolInvocation`
    - internal/daemon/daemon_integration_test.go contains `func TestE2EModeSwitching`
    - internal/daemon/daemon_integration_test.go contains `func TestE2ETokenBudget`
    - internal/daemon/daemon_integration_test.go contains `func TestE2ECleanShutdown`
    - internal/daemon/daemon_integration_test.go contains `func TestE2EProfileConfigLayering`
    - internal/daemon/daemon_integration_test.go contains `func TestE2EWorkflowOnboarding`
    - `go test ./internal/daemon/ -run TestE2E -count=1 -timeout 120s` exits 0
  </acceptance_criteria>
  <done>6 E2E smoke tests pass: memory CRUD, mode switching, token budget, clean shutdown, profile config layering, and workflow onboarding. All flows from the milestone audit are covered.</done>
</task>

</tasks>

<verification>
```bash
# All daemon tests pass
go test ./internal/daemon/ -count=1 -timeout 120s -v
# Full project test suite still passes
go test ./internal/... -count=1 -short -timeout 180s
# Binary still compiles
go build ./cmd/serena/
```
</verification>

<success_criteria>
- 10 integration tests pass covering all 6 E2E flows from milestone audit
- Memory CRUD works end-to-end via skill ExecuteTool
- Mode switching works with validation
- Profile config layering produces correct tool filtering
- Daemon shuts down cleanly
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-daemon-bootstrap-integration/05-03-SUMMARY.md`
</output>
