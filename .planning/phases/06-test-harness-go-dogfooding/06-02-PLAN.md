---
phase: 06-test-harness-go-dogfooding
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - test/integration/symbols_test.go
  - test/integration/fileops_test.go
  - test/integration/diag_test.go
autonomous: true
requirements: [DOG-01, DOG-02, DOG-03]

must_haves:
  truths:
    - "All 9 symbol retrieval tools return correct results against Go fixture"
    - "All 6 file operation tools work correctly against fixture and codebase"
    - "All 3 diagnostic tools return valid results against Go code"
    - "Fixture tests use structural assertions (exact symbol names/types)"
    - "Codebase smoke tests use behavioral assertions (non-empty, contains known name)"
  artifacts:
    - path: "test/integration/symbols_test.go"
      provides: "Tests for go_to_definition, find_references, get_hover_info, search_symbols, get_symbol_overview, get_implementations, get_call_hierarchy, get_type_hierarchy, get_blast_radius"
      contains: "func TestSymbols_"
    - path: "test/integration/fileops_test.go"
      provides: "Tests for read_file, write_file, list_directory, find_file, search_for_pattern, replace_in_file"
      contains: "func TestFileOps_"
    - path: "test/integration/diag_test.go"
      provides: "Tests for get_diagnostics, get_code_actions, format_code"
      contains: "func TestDiag_"
  key_links:
    - from: "test/integration/symbols_test.go"
      to: "test/integration/harness.go"
      via: "StartTestDaemon + callTool"
      pattern: "StartTestDaemon|callTool"
    - from: "test/integration/symbols_test.go"
      to: "testdata/fixtures/go/"
      via: "PrepareFixture"
      pattern: "PrepareFixture\\(t, \"go\"\\)"
---

<objective>
Exercise all 9 symbol retrieval tools, 6 file operation tools, and 3 diagnostic tools against Serena's Go fixture and own codebase via integration tests.

Purpose: Prove that the core code intelligence tools (symbols, files, diagnostics) work end-to-end through the full MCP protocol path with a real gopls language server.
Output: Three test files covering DOG-01, DOG-02, DOG-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-test-harness-go-dogfooding/06-CONTEXT.md
@.planning/phases/06-test-harness-go-dogfooding/06-RESEARCH.md
@.planning/phases/06-test-harness-go-dogfooding/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 harness (must exist before this plan runs) -->

From test/integration/harness.go:
```go
type Options struct {
    WorkspaceDir string
    SkipLS       bool
    LSTimeout    time.Duration
}
type TestDaemon struct {
    Session *mcp.ClientSession
    // ...
}
func StartTestDaemon(t *testing.T, opts Options) *TestDaemon
func WaitForLS(t *testing.T, session *mcp.ClientSession, timeout time.Duration)
func requireGopls(t *testing.T)
func PrepareFixture(t *testing.T, lang string) string
```

From test/integration/helpers.go:
```go
func textContent(r *mcp.CallToolResult) string
func callTool(t *testing.T, session *mcp.ClientSession, name string, args map[string]any) *mcp.CallToolResult
func callToolExpectError(t *testing.T, session *mcp.ClientSession, name string, args map[string]any) *mcp.CallToolResult
```

Known fixture symbols (testdata/fixtures/go/):
- main.go: main(), Helper(), DemoStruct{Field int}, Value(), UsingHelper()
- pkg/greeter.go: Greeter (interface), SimpleGreeter{}, Greet(name string), NewGreeter()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Symbol retrieval integration tests (9 tools)</name>
  <files>test/integration/symbols_test.go</files>
  <read_first>
    - test/integration/harness.go (harness API -- StartTestDaemon, Options, PrepareFixture, requireGopls, WaitForLS)
    - test/integration/helpers.go (callTool, textContent signatures)
    - testdata/fixtures/go/main.go (exact symbol names and positions)
    - testdata/fixtures/go/pkg/greeter.go (interface and implementation symbols)
    - internal/kernel/symbols/retrieval.go (tool names and argument schemas)
  </read_first>
  <action>
Create `test/integration/symbols_test.go` with `//go:build integration` tag, package `integration_test`.

The file tests all 9 symbol retrieval tools per DOG-01. Use a shared TestMain or test helper pattern to start ONE daemon with the Go fixture workspace activated (per Pitfall 5: one daemon per package). Each tool gets a subtest.

**Structure:**
```go
func TestSymbols_GoFixture(t *testing.T) {
    requireGopls(t)
    fixture := PrepareFixture(t, "go")
    td := StartTestDaemon(t, Options{WorkspaceDir: fixture})
    // subtests below share td
}
```

**9 subtests (all within TestSymbols_GoFixture):**

1. `t.Run("go_to_definition", ...)` -- Call with `file_path: "main.go"`, `symbol_name: "Helper"`. Assert result contains "Helper" and "main.go" (or line info showing function definition).

2. `t.Run("find_references", ...)` -- Call with `file_path: "main.go"`, `symbol_name: "Helper"`. Assert result mentions at least 2 locations (definition + UsingHelper call + main call).

3. `t.Run("get_hover_info", ...)` -- Call with `file_path: "main.go"`, `symbol_name: "Helper"`. Assert result contains "func Helper()" (the signature).

4. `t.Run("search_symbols", ...)` -- Call with `query: "Demo"`, `scope: "workspace"`. Assert result contains "DemoStruct".

5. `t.Run("get_symbol_overview", ...)` -- Call with `file_path: "main.go"`. Assert result contains "Helper", "DemoStruct", "Value", "UsingHelper", "main".

6. `t.Run("get_implementations", ...)` -- Call with `file_path: "pkg/greeter.go"`, `symbol_name: "Greeter"`. Assert result contains "SimpleGreeter".

7. `t.Run("get_call_hierarchy", ...)` -- Call with `file_path: "main.go"`, `symbol_name: "Helper"`. Assert result mentions callers (main or UsingHelper).

8. `t.Run("get_type_hierarchy", ...)` -- Call with `file_path: "pkg/greeter.go"`, `symbol_name: "SimpleGreeter"`. Assert result is not empty (type hierarchy may show Greeter interface relationship). Use behavioral assertion since gopls type hierarchy support varies.

9. `t.Run("get_blast_radius", ...)` -- Call with `file_path: "main.go"`, `symbol_name: "Helper"`. Assert result contains references to Helper (similar to find_references but shows impact).

**Codebase smoke test (separate top-level test, per D-05/D-06):**
```go
func TestSymbols_CodebaseSmoke(t *testing.T) {
    requireGopls(t)
    td := StartTestDaemon(t, Options{WorkspaceDir: "."})  // Serena's own codebase
    // Behavioral assertions only
}
```
- Call `search_symbols` with `query: "Daemon"`, `scope: "workspace"`. Assert result is non-empty and contains "Daemon".
- Call `get_symbol_overview` with `file_path: "internal/daemon/daemon.go"`. Assert result contains "Daemon" and "New".

Note: CodebaseSmoke test may need longer LS timeout (60s) since Serena codebase is larger than fixture. Pass `LSTimeout: 60 * time.Second` in Options.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && go vet -tags integration ./test/integration/... && go test -tags integration -count=1 -run "TestSymbols_" ./test/integration/... -timeout 120s -v</automated>
  </verify>
  <acceptance_criteria>
    - test/integration/symbols_test.go starts with `//go:build integration`
    - test/integration/symbols_test.go contains `func TestSymbols_GoFixture(t *testing.T)` with 9 subtests
    - test/integration/symbols_test.go contains `func TestSymbols_CodebaseSmoke(t *testing.T)`
    - Each subtest calls one of: go_to_definition, find_references, get_hover_info, search_symbols, get_symbol_overview, get_implementations, get_call_hierarchy, get_type_hierarchy, get_blast_radius
    - `go test -tags integration -count=1 -run "TestSymbols_GoFixture" ./test/integration/... -timeout 120s` exits 0
    - Fixture subtests assert specific symbol names (structural per D-06): "Helper", "DemoStruct", "SimpleGreeter"
    - Codebase smoke subtests use behavioral assertions: `assert.NotEmpty` and `assert.Contains`
  </acceptance_criteria>
  <done>All 9 symbol retrieval tools tested against Go fixture with structural assertions and against own codebase with behavioral assertions.</done>
</task>

<task type="auto">
  <name>Task 2: File operations and diagnostics integration tests (6 + 3 tools)</name>
  <files>test/integration/fileops_test.go, test/integration/diag_test.go</files>
  <read_first>
    - test/integration/harness.go (harness API)
    - test/integration/helpers.go (callTool, textContent)
    - internal/kernel/fileops/fileops.go (tool names and argument schemas for read_file, write_file, list_directory, find_file, search_for_pattern, replace_in_file)
    - internal/kernel/diag/tools.go (tool names and argument schemas for get_diagnostics, get_code_actions, format_code)
    - testdata/fixtures/go/main.go (fixture content)
  </read_first>
  <action>
**File 1: `test/integration/fileops_test.go`** with `//go:build integration` tag.

File operation tools do NOT need an LS -- they work against the filesystem. Start daemon with `SkipLS: true` for speed.

```go
func TestFileOps_GoFixture(t *testing.T) {
    fixture := PrepareFixture(t, "go")
    td := StartTestDaemon(t, Options{WorkspaceDir: fixture, SkipLS: true})
    // subtests
}
```

**6 subtests:**

1. `t.Run("read_file", ...)` -- Call with `file_path: "main.go"`. Assert result contains "func Helper()" and "package main".

2. `t.Run("write_file", ...)` -- Call with `file_path: "newfile.go"`, `content: "package main\n\nfunc NewFunc() {}\n"`. Assert no error. Then call `read_file` on "newfile.go" and assert it contains "func NewFunc()".

3. `t.Run("list_directory", ...)` -- Call with `path: "."`. Assert result contains "main.go" and "pkg".

4. `t.Run("find_file", ...)` -- Call with `pattern: "greeter"`. Assert result contains "pkg/greeter.go".

5. `t.Run("search_for_pattern", ...)` -- Call with `pattern: "func Helper"`, `scope: "workspace"` (or `path: "."`). Assert result contains "main.go" and "Helper".

6. `t.Run("replace_in_file", ...)` -- Call with `file_path: "main.go"`, `old_text: "Helper function called"`, `new_text: "Helper was invoked"`. Assert no error. Then call `read_file` on "main.go" and assert it contains "Helper was invoked" and does NOT contain "Helper function called".

**File 2: `test/integration/diag_test.go`** with `//go:build integration` tag.

Diagnostic tools need LS active.

```go
func TestDiag_GoFixture(t *testing.T) {
    requireGopls(t)
    fixture := PrepareFixture(t, "go")
    td := StartTestDaemon(t, Options{WorkspaceDir: fixture})
    // subtests
}
```

**3 subtests:**

1. `t.Run("get_diagnostics", ...)` -- Call with `file_path: "main.go"`. Assert result is not nil. For a clean file, diagnostics may be empty or contain informational items -- assert the call succeeds without error (behavioral assertion since diagnostic content varies by gopls version).

2. `t.Run("get_code_actions", ...)` -- Call with `file_path: "main.go"`. Assert result is not nil and the call succeeds. gopls typically offers "organize imports" or other code actions even on clean files.

3. `t.Run("format_code", ...)` -- Call with `file_path: "main.go"`. Assert no error. Then call `read_file` on "main.go" and assert it still contains "func Helper()" (format should preserve correct Go code). The fixture is already gofmt-formatted, so content should be unchanged.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && go vet -tags integration ./test/integration/... && go test -tags integration -count=1 -run "TestFileOps_|TestDiag_" ./test/integration/... -timeout 120s -v</automated>
  </verify>
  <acceptance_criteria>
    - test/integration/fileops_test.go starts with `//go:build integration`
    - test/integration/fileops_test.go contains `func TestFileOps_GoFixture(t *testing.T)` with 6 subtests
    - Each file ops subtest calls one of: read_file, write_file, list_directory, find_file, search_for_pattern, replace_in_file
    - test/integration/diag_test.go starts with `//go:build integration`
    - test/integration/diag_test.go contains `func TestDiag_GoFixture(t *testing.T)` with 3 subtests
    - Each diag subtest calls one of: get_diagnostics, get_code_actions, format_code
    - `go test -tags integration -count=1 -run "TestFileOps_" ./test/integration/... -timeout 60s` exits 0
    - `go test -tags integration -count=1 -run "TestDiag_" ./test/integration/... -timeout 120s` exits 0
  </acceptance_criteria>
  <done>All 6 file operation tools and all 3 diagnostic tools tested against Go fixture with appropriate assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> fixture files | Tests write to copied fixture in t.TempDir(), not source |
| test -> gopls | Local process, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | T (Tampering) | replace_in_file test | mitigate | Test operates on copy in t.TempDir(), original fixture untouched |
| T-06-05 | D (Denial of Service) | Codebase smoke timeout | mitigate | LSTimeout: 60s for codebase tests, t.Fatalf on deadline |
</threat_model>

<verification>
1. `go test -tags integration -count=1 -run "TestSymbols_GoFixture" ./test/integration/... -timeout 120s` passes with all 9 subtests
2. `go test -tags integration -count=1 -run "TestFileOps_" ./test/integration/... -timeout 60s` passes with all 6 subtests
3. `go test -tags integration -count=1 -run "TestDiag_" ./test/integration/... -timeout 120s` passes with all 3 subtests
4. `go vet -tags integration ./test/integration/...` exits 0
</verification>

<success_criteria>
- 9 symbol retrieval tools all return correct results against Go fixture and codebase
- 6 file operation tools all work correctly (including write and replace round-trips)
- 3 diagnostic tools return valid results against Go code
- Fixture tests use structural assertions, codebase tests use behavioral assertions
</success_criteria>

<output>
After completion, create `.planning/phases/06-test-harness-go-dogfooding/06-02-SUMMARY.md`
</output>
