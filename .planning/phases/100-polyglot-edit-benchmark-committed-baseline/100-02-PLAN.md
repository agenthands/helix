---
phase: 100-polyglot-edit-benchmark-committed-baseline
plan: 02
type: tdd
wave: 2
depends_on:
  - 100-01-polyglot-edit-benchmark-committed-baseline
files_modified:
  - bench/runtime/cell.go
  - bench/runtime/aider_edit_cell.go
  - bench/runtime/aider_edit_cell_test.go
  - bench/reports/aider-edit-baseline/result.v2.json
  - bench/reports/aider-edit-baseline/BENCH-RESULTS.md
  - bench/aggregator/aider_edit_baseline_test.go
  - .gitignore
  - Makefile
autonomous: true
requirements:
  - EDITBENCH-01
  - BASELINE-01

user_setup: []

must_haves:
  truths:
    - "RunCell branches to runAiderEditCell by mode name (aider_edit), mirroring the baseline_rag branch, with the loader's RunExercise called verbatim and WR-01 preserved"
    - "runAiderEditCell spawns the warm daemon, loads the vendored go/wordy exercise, drives the deterministic EDIT-verb AgentFn, runs the native go test, and writes a schema-valid result.v2.json carrying edit_format_applied"
    - "A committed, byte-reproducible polyglot-edit baseline exists at bench/reports/aider-edit-baseline/ (result.v2.json + BENCH-RESULTS.md) with deterministic metrics only — no live latency or tokens"
    - "The HELIX_BIN live leg fails-closed (never silently SKIPs) when HELIX_BIN is set but no result.v2.json / empty bucket / missing edit_format_applied is produced; a hermetic golden sibling is the sole authoritative proof"
  artifacts:
    - path: "bench/runtime/aider_edit_cell.go"
      provides: "runAiderEditCell — the mode-name cell branch (sibling of runRAGCell) driving RunExercise via the deterministic AgentFn"
      min_lines: 80
    - path: "bench/reports/aider-edit-baseline/result.v2.json"
      provides: "the committed byte-reproducible baseline result row (deterministic metrics only)"
      contains: "edit_format_applied"
    - path: "bench/reports/aider-edit-baseline/BENCH-RESULTS.md"
      provides: "the committed deterministic baseline summary rendered via the zero-RNG path"
      contains: "aider_edit"
    - path: "bench/aggregator/aider_edit_baseline_test.go"
      provides: "double-render byte-reproducibility golden + committed-result.v2 assertion"
      contains: "byte"
  key_links:
    - from: "bench/runtime/cell.go"
      to: "bench/runtime/aider_edit_cell.go"
      via: "if cfg.Mode == aiderEditMode { return runAiderEditCell(...) } near the baselineRagMode branch"
      pattern: "aiderEditMode"
    - from: "bench/runtime/aider_edit_cell.go"
      to: "bench/datasets/aider-polyglot/loader.go"
      via: "aiderpolyglot.RunExercise(ctx, ex, workDir, testFn, agentFn) called verbatim"
      pattern: "RunExercise"
    - from: "bench/reports/aider-edit-baseline/"
      to: ".gitignore"
      via: "an allowlist exception force-tracks the committed baseline under the otherwise-ignored bench/reports/*"
      pattern: "aider-edit-baseline"
---

<objective>
Land the live cell branch and the committed, byte-reproducible polyglot-edit
baseline. Add `runAiderEditCell` (a mode-name branch off `RunCell`, mirroring
`runRAGCell`) that spawns the warm daemon, loads the vendored `go/wordy`
exercise, drives the Plan 01 deterministic EDIT-verb `AgentFn` + live `TestFn`
through `RunExercise` VERBATIM, stamps `edit_format_applied`, and writes a
schema-valid `result.v2.json`. Capture ONE exercise × ONE mode (`aider_edit`)
× one hermetic language (`go`) as the committed baseline at the FIXED run-id
dir `bench/reports/aider-edit-baseline/`, force-tracked via a `.gitignore`
exception. Prove byte-reproducibility with a double-render golden and prove
fail-closed HELIX_BIN behavior with a "did it RUN" sentinel.

Purpose: This closes BASELINE-01 and the live half of EDITBENCH-01. The
committed baseline is driven by a DETERMINISTIC scripted agent (apply the
exercism reference `.meta/example.go` via `replace_in_file` → guaranteed PASS),
NOT a live LLM — so the artifact is byte-reproducible. The real-LLM
`--agent=claude` arm is wired-but-not-the-baseline.

Output: `runAiderEditCell`, the `cell.go` branch, the committed
`bench/reports/aider-edit-baseline/{result.v2.json,BENCH-RESULTS.md}`, the
byte-reproducibility golden, the HELIX_BIN sentinel, and an optional
`make bench-aider-edit` target.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-polyglot-edit-benchmark-committed-baseline/100-RESEARCH.md
@.planning/phases/100-polyglot-edit-benchmark-committed-baseline/100-PATTERNS.md
@.planning/phases/100-polyglot-edit-benchmark-committed-baseline/100-01-SUMMARY.md

# The cell branch template (baseline_rag) + the daemon spine to reuse
@bench/runtime/rag.go
@bench/runtime/cell.go

# The loader reused VERBATIM (RunExercise + WR-01) — do NOT touch it
@bench/datasets/aider-polyglot/loader.go

# Plan 01 substrate this plan consumes (AgentFn factory, native TestFn, aiderEditMode const)
@bench/runtime/aider_edit_agent.go

# Byte-reproducibility proof pattern (double-render diff-empty + committed golden)
@bench/aggregator/byte_reproducible_test.go
@bench/aggregator/aggregate.go

# .gitignore — bench/reports/* is ignored except an allowlisted entry
@.gitignore
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runAiderEditCell + RunCell mode-name branch (EDITBENCH-01 live half)</name>
  <files>bench/runtime/aider_edit_cell.go, bench/runtime/cell.go, bench/runtime/aider_edit_cell_test.go</files>
  <read_first>
    - bench/runtime/rag.go:44-263 (runRAGCell — the full mode-name cell template: sandbox/New/Prepare, preserve-on-failure, own path resolution, spawn, drive, kill, SynthCCTap, BuildResult/Validate/writeDurable)
    - bench/runtime/cell.go:434-441 (the baselineRagMode branch — byte-template for the dispatch)
    - bench/runtime/cell.go:485-517 (StartDaemon spawn + PID guard + SocketFor)
    - bench/runtime/cell.go:585-808 (graceful Stop+Kill, SynthCCTap, trace.Merge, coordinator.Grade, BuildResult(ResultInput{...}), Validate, writeDurable)
    - bench/runtime/aider_edit_agent.go (Plan 01: newDeterministicEditAgent, newNativeTestFn, aiderEditMode)
    - bench/datasets/aider-polyglot/loader.go:230-264 (RunExercise — called VERBATIM) + loader.go:241-249 (WR-01 restore, runs only when SrcDir != "")
    - internal/eval/sandbox/sandbox.go:94-99,110,126 (RepoFor / SocketFor / Prepare / CloneRepo walk the full fixture tree)
    - PATTERNS.md "aider_edit_cell.go (NEW)" + "cell.go (MODIFY)"
  </read_first>
  <behavior>
    - Hermetic golden sibling (no HELIX_BIN, no network, the SOLE authoritative proof):
      drive runAiderEditCell's PURE assembly logic against a fake/in-process daemon seam OR
      drive RunExercise + the deterministic transform against the vendored go/wordy fixture with
      a fake TestFn so the path (load → AgentFn applies reference → grade) is exercised end-to-end
      WITHOUT a live binary; assert the result carries edit_format_applied=true and outcome solved.
    - HELIX_BIN "did it RUN" sentinel (gated): when HELIX_BIN is set, run the REAL runAiderEditCell
      over go/wordy and assert it produced a result.v2.json that EXISTS, is schema-valid, and carries
      edit_format_applied=true with a solved outcome. A missing file / empty bucket / missing
      edit_format_applied is a HARD failure (fail-CLOSED), never a pass. When HELIX_BIN is unset the
      sentinel may t.Skip ONLY because the hermetic sibling covers the logic.
    - Anti-vacuity (revert-and-fail): feeding a WRONG reference body (so the applied edit does not
      satisfy the tests) yields a non-solved outcome — the baseline pass is non-vacuous.
  </behavior>
  <action>
    Create bench/runtime/aider_edit_cell.go in package `runtime` with
    `func runAiderEditCell(ctx context.Context, cfg CellConfig, res CellResult) (CellResult, error)`.
    Mirror runRAGCell's structure (rag.go:57-263): benchsandbox.New → res.ScratchDir → the
    cleanup/preserve closures verbatim → sb.Prepare(cfg.Task, cfg.Mode).

    DIVERGENCE (RESEARCH Open Q3 recommendation (a)): the aider-edit branch OWNS its own fixture
    path resolution — the standard cellSeedDir/scripted_agent.yaml seed-dir assumptions do NOT apply.
    Resolve the fixture exercise dir as
    `bench/datasets/aider-polyglot/fixtures/<cfg.Language>/exercises/practice/<cfg.Task>` (Task is the
    exercise name, e.g. "wordy"; Language is "go"). Reuse the loader's path-segment validators by
    only joining values that are validated upstream — Task and Language are already validated against
    traversal by CellConfig's contract (cell.go validates them before dispatch); additionally rely on
    aiderpolyglot.LoadExercise re-validating the exercise-name and language segments (loader.go:81,96).
    Then `sb.CloneRepo(<fixtureDir>, cfg.Task, cfg.Mode)` to copy the WHOLE exercise tree (stub, test,
    cases, go.mod, .meta/) into the per-cell repo working copy (CloneRepo is a full recursive walk).

    Spawn the warm daemon exactly like the daemon spine: writeCellConfig (store-off default) →
    `subprocess.StartDaemon(ctx, sb, cfg.Task, cfg.Mode, profileName, cfgPath)` → PID guard. Resolve
    profileName via the resolver (the aider_edit MODE.md resolves to bench-full, validated as a side
    effect). NOTE the divergence: the AgentFn loads the exercise from SrcDir = the FIXTURE dir (so
    WR-01 restore reads pristine files from the fixture, loader.go:243-249) but applies edits into the
    CLONED repo working copy (workDir = sb.RepoFor(cfg.Task, cfg.Mode)). Set ex via
    `aiderpolyglot.LoadExercise(<fixtureDir>, cfg.Language)` so ex.SrcDir is the fixture dir (pristine
    source) and pass workDir = the cloned repo to RunExercise. The native go test runs in workDir.

    Build the deterministic agent + live test:
      `applied := false` (a *bool the result will carry)
      `agentFn := newDeterministicEditAgent(sb.SocketFor(cfg.Task, cfg.Mode), &applied)`
      `testFn := newNativeTestFn()`
    Then call `aiderpolyglot.RunExercise(ctx, ex, workDir, testFn, agentFn)` VERBATIM. Map the returned
    AttemptResult.Passed to verifyExit (0 on pass, 1 on fail) like runRAGCell:165-169.

    Reap the daemon (graceful Stop + Kill fallback, cell.go:603-...). Record the EDIT-verb calls as
    StepResults so SynthCCTap builds a real CC leg with zero Usage (no model → tokens legitimately 0).
    If threading per-edit StepResults from the AgentFn is heavier than the budget allows, synthesize a
    single faithful StepResult for the replace_in_file call (mirror runRAGCell's single-probe CC leg) —
    do NOT fabricate token usage. trace.Merge (CC-only leg tolerated, runRAGCell:185) → coordinator.Grade
    with UsagePresent:false → BuildResult(ResultInput{ TaskID, Mode, Benchmark, RunIndex, Outcome:
    merged.Outcome, TraceRef: res.MergedTracePath, Language: cfg.Language, Fairness:
    runners.DefaultContract, AblationStatus:"", EditFormatApplied:&applied, Metrics, MetricErrors })
    → Validate → writeDurable(res.ResultPath / res.MergedTracePath). cleanup() on success.

    In cell.go ADD the dispatch branch immediately AFTER the baselineRagMode branch (cell.go:434-441):
      `if cfg.Mode == aiderEditMode { _ = profileName; return runAiderEditCell(ctx, cfg, res) }`
    Detection is BY MODE NAME (the resolver is strict two-key; the profile is validated as a side
    effect). The unconditional fairness gate + path layout (ResultPath/MergedTracePath) already ran
    above.

    Write the three behavior cases in aider_edit_cell_test.go. The hermetic sibling is the SOLE
    authoritative proof (Pitfall 1) — it must run with NO HELIX_BIN and NO network and be a named,
    non-skipped test. The HELIX_BIN sentinel asserts fail-CLOSED on a missing/empty/metric-less
    result (Pitfall 2): read the produced result.v2.json with a `present` signal and hard-fail when
    absent — NEVER read absence as a zero/pass.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestAiderEditCellHermetic|TestAiderEditCellAntiTamper' -x -count=1 && go vet ./bench/runtime/</automated>
  </verify>
  <done>runAiderEditCell exists and is dispatched by mode name from RunCell; RunExercise is called verbatim (WR-01 intact); the hermetic sibling passes with no HELIX_BIN/network; the anti-vacuity test proves a wrong reference fails the grade; go vet clean.</done>
  <acceptance_criteria>
    - `grep -n 'if cfg.Mode == aiderEditMode' bench/runtime/cell.go` returns the dispatch branch placed right after the baselineRagMode branch (cell.go:434, PATTERNS.md cell.go MODIFY).
    - `grep -n 'aiderpolyglot.RunExercise' bench/runtime/aider_edit_cell.go` shows RunExercise called verbatim (the loader is NOT modified — `git diff bench/datasets/aider-polyglot/loader.go` shows no change vs Plan 01's exported wrappers).
    - `grep -n 'EditFormatApplied: *&applied\|EditFormatApplied:&applied' bench/runtime/aider_edit_cell.go` shows the stamp wired into BuildResult.
    - The hermetic sibling is a named non-skipped test that runs with no HELIX_BIN and no network (Pitfall 1: sole authoritative proof; project memory helix-bench-smoke-false-green).
    - `go vet ./bench/runtime/` clean (RESEARCH Validation Architecture; CLAUDE.md mandate).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Capture + commit the byte-reproducible baseline + .gitignore exception (BASELINE-01)</name>
  <files>bench/reports/aider-edit-baseline/result.v2.json, bench/reports/aider-edit-baseline/BENCH-RESULTS.md, .gitignore, Makefile</files>
  <read_first>
    - .gitignore:271-272 (the `/bench/reports/*` ignore + `!/bench/reports/.gitkeep` allowlist pattern to mirror)
    - bench/aggregator/aggregate.go:67,193 (Aggregate / renderAll — the zero-RNG, sort-before-emit shared render path)
    - bench/runtime/result.go:244-289 (BuildResult — the exact JSON shape the baseline result.v2.json must match)
    - Makefile:149-158 (bench-quick — the HELIX_BIN-gated build-then-run pattern to mirror)
    - RESEARCH.md "Pitfall 3" (commit deterministic metrics ONLY — exclude latency/tokens) + "Pattern 5"
  </read_first>
  <action>
    Capture the baseline at the FIXED run-id dir `bench/reports/aider-edit-baseline/` (a STABLE
    string, NOT a timestamp — a timestamped dir would never be byte-reproducible or committable).
    Generate the artifact by running the deterministic arm locally (HELIX_BIN set), then HAND-CURATE
    the committed bytes to deterministic-metrics-only:

      result.v2.json — the per-cell row BuildResult produced for go/wordy aider_edit. STRIP any
      machine-specific / non-reproducible fields before committing: zero out or null any wall-clock
      latency / duration_ms, any absolute scratch path, any timestamp, and any live token counts
      (the scripted agent has no provider usage → token metrics are already explicit null; keep them
      null, never a fabricated 0). KEEP: schema_version "v2", task_id "wordy", mode "aider_edit",
      benchmark "aider-polyglot", language "go", outcome (solved), edit_format_applied true, exit_code,
      the fairness block (overrides sorted), and the deterministic quality metrics (pass/fail,
      file/edit counts). The committed result.v2.json MUST pass Validate.

      BENCH-RESULTS.md — a deterministic baseline summary. Render it through the existing zero-RNG
      path: either reuse renderAll over the single-cell run dir (it emits the standard reports) and
      additionally emit a thin BENCH-RESULTS.md that restates the deterministic row, OR build
      BENCH-RESULTS.md as a fixed-format summary table keyed on the committed result.v2.json
      (task, mode, language, outcome, edit_format_applied). It MUST contain NO latency, NO timestamp,
      NO absolute path, NO map-iteration-ordered field — sort any list before emit (Pitfall 3). The
      file must regenerate byte-identically (proven by Task 3's double-render test).

    Add a .gitignore allowlist exception immediately after `!/bench/reports/.gitkeep` so the baseline
    dir is force-tracked under the otherwise-ignored bench/reports/*:
      `!/bench/reports/aider-edit-baseline/`
      `!/bench/reports/aider-edit-baseline/**`
    Confirm with `git check-ignore -v bench/reports/aider-edit-baseline/result.v2.json` returns the
    NEGATED (allowlisted) rule, and `git add bench/reports/aider-edit-baseline/` actually stages both
    files.

    Add a `bench-aider-edit` Makefile target mirroring bench-quick (Makefile:149-158): `go build -o
    $(BINARY) ./cmd/helix` then run the deterministic aider_edit arm
    (`--benchmarks=aider-polyglot --languages=go --modes=aider_edit --tasks=wordy --agent=scripted
    --helix-bin=$(CURDIR)/$(BINARY) --out bench/reports`). Add the target to the `.PHONY` line. This is
    the documented local regenerate path — it stays LOCAL-ONLY (no CI benchstat gate; STATE.md
    local-only-benches constraint). If `cmd/helix-bench run` cannot yet route the aider-polyglot
    benchmark to runAiderEditCell through the matrix, document the manual regenerate command in the
    target comment and have it invoke the smallest entrypoint that drives runAiderEditCell — do NOT
    add a new schema or matrix subsystem (RESEARCH Open Q3: the branch owns its own path resolution).
  </action>
  <verify>
    <automated>test -f bench/reports/aider-edit-baseline/result.v2.json && test -f bench/reports/aider-edit-baseline/BENCH-RESULTS.md && git check-ignore -v bench/reports/aider-edit-baseline/result.v2.json | grep -q '!' && go run ./cmd/helix-bench validate-result bench/reports/aider-edit-baseline/result.v2.json 2>/dev/null || go test ./bench/runtime/ -run 'TestAiderEditBaselineResultValid' -count=1</automated>
  </verify>
  <done>The committed baseline at bench/reports/aider-edit-baseline/ exists (result.v2.json + BENCH-RESULTS.md), is force-tracked via a .gitignore exception, carries deterministic metrics only (no latency/timestamp/absolute-path/live-tokens), and the result.v2.json validates; the bench-aider-edit Makefile target regenerates it locally.</done>
  <acceptance_criteria>
    - `git check-ignore -v bench/reports/aider-edit-baseline/result.v2.json` prints a NEGATED rule (the `!/bench/reports/aider-edit-baseline/**` allowlist), proving the artifact is tracked (mirrors `!/bench/reports/.gitkeep` .gitignore:272).
    - `grep -c 'duration_ms\|latency\|"started_at"\|/tmp/\|/home/' bench/reports/aider-edit-baseline/result.v2.json` is 0 (deterministic metrics only; Pitfall 3).
    - `grep -c 'edit_format_applied' bench/reports/aider-edit-baseline/result.v2.json` is at least 1, and the value is `true` (BASELINE-01 + EDITBENCH-03).
    - `grep -n 'bench-aider-edit' Makefile` returns the target and its `.PHONY` entry (mirrors bench-quick Makefile:149).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Byte-reproducibility golden + committed-baseline assertion (BASELINE-01 anti-vacuity)</name>
  <files>bench/aggregator/aider_edit_baseline_test.go</files>
  <read_first>
    - bench/aggregator/byte_reproducible_test.go:25-77 (double-render diff-empty + committed-golden compare — the proof pattern to mirror)
    - bench/aggregator/aggregate.go:67,193 (Aggregate / renderAll shared path)
    - bench/reports/aider-edit-baseline/{result.v2.json,BENCH-RESULTS.md} (the committed artifact under test, from Task 2)
    - RESEARCH.md Test Map BASELINE-01 rows (TestAiderEditBaselineGolden, TestAiderEditBaselineByteReproducible)
  </read_first>
  <behavior>
    - Committed-result assertion (fail-CLOSED): the committed bench/reports/aider-edit-baseline/result.v2.json
      EXISTS, is schema-valid (Validate passes), and carries outcome=solved + edit_format_applied=true.
      A missing file or a missing edit_format_applied is a HARD failure, never a pass (Pitfall 2).
    - Byte-reproducibility (double-render diff-empty): rendering BENCH-RESULTS.md twice from the SAME
      committed result.v2.json yields byte-identical bytes AND matches the committed BENCH-RESULTS.md
      exactly — a render that drifts on re-render (RNG leak, map-order, unsorted emit, embedded
      timestamp/latency) fails HERE.
    - Anti-vacuity: a result.v2.json with edit_format_applied STRIPPED (or outcome flipped) does NOT
      satisfy the committed-baseline assertion — proving the assertion actually grades the metric.
  </behavior>
  <action>
    Create bench/aggregator/aider_edit_baseline_test.go mirroring byte_reproducible_test.go:25-77.
    The committed bench/reports/aider-edit-baseline/ tree is the hermetic fixture (no daemon, no
    HELIX_BIN, no network — the byte-reproducibility is proven by the golden test alone, exactly like
    TestReportByteReproducible). If the aggregator package cannot import bench/runtime's Validate due
    to a cycle, assert schema validity by re-unmarshalling and checking the required keys + the
    edit_format_applied value directly, OR place the test in bench/runtime where BuildResult/Validate
    live and read the committed file via a relative path to the repo root — choose whichever avoids an
    import cycle; the test package location is at executor discretion as long as all three behaviors are
    covered.

    Implement: TestAiderEditBaselineResultValid (committed-result assertion, fail-closed on a missing
    file / missing edit_format_applied), TestAiderEditBaselineByteReproducible (render the summary twice
    and assert byte-identical + matches the committed BENCH-RESULTS.md), and TestAiderEditBaselineAntiVacuity
    (a stripped/flipped result is rejected by the assertion). Use a `present` boolean signal when reading
    the committed file so an os.ReadFile error is a HARD failure, NEVER read as a zero/pass.
  </action>
  <verify>
    <automated>go test ./bench/... -run 'TestAiderEditBaseline' -x -count=1</automated>
  </verify>
  <done>The committed baseline is asserted present + schema-valid + carrying edit_format_applied=true (fail-closed on absence); BENCH-RESULTS.md regenerates byte-identically (double-render diff-empty + golden match); the anti-vacuity test proves the assertion grades the metric; all three green with no HELIX_BIN/network.</done>
  <acceptance_criteria>
    - The byte-reproducibility test asserts `assert.Equal(first, second)` (double-render diff-empty) AND `assert.Equal(committedGolden, first)` — mirroring byte_reproducible_test.go:46,52.
    - The committed-result assertion uses a `present` fail-closed read (a missing result.v2.json hard-fails) — mirrors scrapeSemanticReadsTotal's present signal (cell.go:99-141; PATTERNS.md "Fail-CLOSED on missing/empty result").
    - `go test ./bench/... -run 'TestAiderEditBaseline' -count=1` passes with no HELIX_BIN env and no network (Pitfall 1).
    - The anti-vacuity case (stripped edit_format_applied / flipped outcome) is rejected by the assertion (RESEARCH Wave 0 anti-vacuity requirement).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture exercise dir → per-cell scratch repo | The vendored go/wordy tree is cloned into the sandbox; CloneRepo walks untrusted relpaths into filepath.Join |
| deterministic AgentFn → warm daemon | The cell's AgentFn dials the per-cell daemon over the unix socket and issues replace_in_file/activate_project |
| committed baseline bytes → repo | The committed result.v2.json + BENCH-RESULTS.md are force-tracked and become a frozen byte contract |
| test-tampering agent → graded test | WR-01 restores the pristine test file after the edit, before grading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-04 | Tampering | test-tampering agent forces a spurious green | mitigate | RunExercise called VERBATIM — restorePristineTests runs after the edit, before each grade, only when ex.SrcDir != "" (loader.go:241-249); the AgentFn edits only files.solution, never files.test (Task 1 + Plan 01) |
| T-100-05 | Tampering | path traversal via exercise/language/relpath into the fixture-dir join | mitigate | Task/Language are CellConfig-validated before dispatch; aiderpolyglot.LoadExercise re-validates the exercise-name + language segments (loader.go:81,96) before any join |
| T-100-06 | Tampering | vacuous baseline pass (missing/empty result read as a pass) | mitigate | Fail-CLOSED: the HELIX_BIN sentinel + the committed-baseline assertion use a `present` signal; a missing result.v2.json / missing edit_format_applied is a HARD error (Tasks 1 + 3; Pitfall 2) |
| T-100-07 | Tampering | non-reproducible committed baseline (latency/timestamp/absolute path/map-order) | mitigate | Commit deterministic metrics ONLY; render via the zero-RNG sort-before-emit path; double-render diff-empty test (Tasks 2 + 3; Pitfall 3) |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | This phase installs NO packages; `git diff go.mod` stays empty (RESEARCH Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `go test ./bench/runtime/... ./bench/aggregator/...` green (hermetic, fast); the hermetic siblings run with NO HELIX_BIN and NO network.
- `HELIX_BIN="$(pwd)/helix" go test ./bench/runtime/ -run 'TestAiderEditCellLiveRan' -count=1` — the live leg RUNS (not skipped) and produces a schema-valid result.v2.json carrying edit_format_applied (build helix first: `go build -o helix ./cmd/helix`).
- `go vet ./...` clean (CLAUDE.md mandate).
- `git diff go.mod` empty (zero new deps).
- `git check-ignore -v bench/reports/aider-edit-baseline/result.v2.json` shows the negated allowlist rule (artifact tracked).
- `make bench-aider-edit` regenerates the baseline byte-identically to the committed bytes.
</verification>

<success_criteria>
- EDITBENCH-01 (live half): RunCell branches to runAiderEditCell by mode name; the deterministic EDIT-verb AgentFn drives RunExercise VERBATIM against the warm daemon; WR-01 preserved.
- BASELINE-01: a committed, byte-reproducible polyglot-edit baseline (bench/reports/aider-edit-baseline/result.v2.json + BENCH-RESULTS.md) carries deterministic metrics only (no live latency/tokens), is force-tracked via a .gitignore exception, and regenerates byte-identically.
- HELIX_BIN fail-not-skip: the hermetic golden sibling is the sole authoritative proof; the live "did it RUN" sentinel fails-CLOSED on a missing/empty/metric-less result when HELIX_BIN is set.
</success_criteria>

<output>
Create `.planning/phases/100-polyglot-edit-benchmark-committed-baseline/100-02-SUMMARY.md` when done
</output>
