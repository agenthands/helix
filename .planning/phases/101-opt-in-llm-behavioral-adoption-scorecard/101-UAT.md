---
status: complete
phase: 101-opt-in-llm-behavioral-adoption-scorecard
source: [101-01-SUMMARY.md, 101-02-SUMMARY.md]
started: 2026-06-23T14:32:48Z
updated: 2026-06-23T14:32:48Z
---

## Current Test

[testing complete]

## Tests

### 1. Hermetic adoption scorer runs in the DEFAULT test suite
expected: `go test ./test/oracle/adopt/...` passes with NO API key and NO build tags — the anti-vacuity proof for ADOPT-02 runs as part of plain `go test ./...`.
result: pass
evidence: "ok github.com/agenthands/helix/test/oracle/adopt 0.017s (no key, no -tags)"

### 2. Adopt package is visible to the default `go list`
expected: `go list ./test/oracle/adopt/...` (no tags) resolves the package, proving default-suite inclusion.
result: pass
evidence: "github.com/agenthands/helix/test/oracle/adopt"

### 3. Tagged legs are EXCLUDED from the default suite
expected: `go list ./test/oracle/llm/...` and `./test/oracle/judge/...` (no tags) both report "matched no packages" — the live/judge legs never run in `go test ./...`.
result: pass
evidence: "both → 'matched no packages'"

### 4. Fixture transcripts are committed (not gitignored)
expected: `git check-ignore test/oracle/adopt/testdata/transcripts/intact-01.json` exits 1 — the 12 hermetic fixtures are committed as the spec.
result: pass
evidence: "check-ignore exit 1 (not ignored)"

### 5. Judge adoption negative exemplar yields a failing verdict (hermetic)
expected: `go test -tags llmjudge ./test/oracle/judge/... -run TestAdoptionNegativeExemplarVerdict` passes with NO key — a grep-first response scores adoption=0.0 → soft_fail/fail, proving the rubric can demonstrably FAIL (SC#2).
result: pass
evidence: "ok github.com/agenthands/helix/test/oracle/judge 0.013s (no key)"

### 6. Live llm capture leg skips cleanly without an API key
expected: with ANTHROPIC_API_KEY/DEEPSEEK_API_KEY unset, `go test -tags llm ./test/oracle/llm/... -run TestAdoptionScorecardLive` SKIPS (compiles, no failure/panic).
result: pass
evidence: "--- SKIP: TestAdoptionScorecardLive (0.00s); PASS; ok"

### 7. Full tagged judge suite stays green with the 6th dimension
expected: `go test -tags llmjudge ./test/oracle/judge/...` passes — the new adoption dimension causes no regression in the existing 5.
result: pass
evidence: "ok github.com/agenthands/helix/test/oracle/judge 0.013s"

### 8. The scorecard never blocks merge
expected: `grep -rn 'tags.*llm\|llmjudge' Makefile .github/workflows/` finds NOTHING — no CI/Makefile target gates on the llm/llmjudge tags.
result: pass
evidence: "grep returns no matches"

### 9. Default vet clean and zero new dependencies
expected: `go vet ./test/oracle/adopt/...` is clean and `git diff go.mod go.sum` is empty.
result: pass
evidence: "vet clean; go.mod/go.sum diff empty"

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
