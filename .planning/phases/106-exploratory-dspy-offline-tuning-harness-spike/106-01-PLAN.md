---
phase: 106-exploratory-dspy-offline-tuning-harness-spike
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/oracle/adopt/parity_test.go
  - tools/dspy-tune/golden/parity_cases.json
  - internal/lint/toolsquarantine/analyzer.go
  - internal/lint/toolsquarantine/analyzer_test.go
  - internal/lint/toolsquarantine/testdata/src/github.com/agenthands/helix/internal/leakyruntime/imports.go
  - internal/lint/toolsquarantine/testdata/src/github.com/agenthands/helix/internal/goodruntime/imports.go
  - internal/lint/toolsquarantine/testdata/src/github.com/agenthands/helix/internal/toolsupport/imports.go
  - internal/lint/toolsquarantine/testdata/src/github.com/agenthands/helix/tools/dspytune/dspytune.go
  - cmd/vet-tools-quarantine/main.go
  - Makefile
autonomous: true
requirements: [TUNE-01]

must_haves:
  truths:
    - "The shared golden corpus pins the Go classifier's branch behavior: ClassifyChoice + FirstCommand agree with each committed case (helix-choice, all 6 fallback prefixes, unclassified prose, fence-wrapped, $/> prompt, substring-trap, lsp-lookalike)."
    - "A runtime/cmd package importing the dev-time tools/ tree is FLAGGED by `make vet` (planted-leak RED fixture trips the analyzer)."
    - "A clean runtime package and a slash-boundary lookalike sibling (internal/toolsupport) are NOT flagged (GREEN fixtures stay silent)."
    - "`make vet` runs the new quarantine analyzer on the REAL tree with no false-positive on the pip/pipx LS installer (internal/langregistry/installer.go)."
    - "`go.mod` carries no Python/DSPy module edge and the real source tree has zero runtime→tools/ import."
  artifacts:
    - path: "test/oracle/adopt/parity_test.go"
      provides: "Go side of the Python↔Go parity cross-check; pins golden/parity_cases.json to the Go truth via ClassifyChoice + FirstCommand"
      contains: "TestPythonGoParityCorpus"
      min_lines: 25
    - path: "tools/dspy-tune/golden/parity_cases.json"
      provides: "The single shared golden corpus read by BOTH the Go parity test and the Python pytest (never duplicated)"
      contains: "first_command"
    - path: "internal/lint/toolsquarantine/analyzer.go"
      provides: "go/analysis import-boundary analyzer: no package outside tools/ may import github.com/agenthands/helix/tools/..."
      contains: "toolsquarantine"
      min_lines: 30
    - path: "internal/lint/toolsquarantine/analyzer_test.go"
      provides: "analysistest harness: RejectsRuntimeImportingTools (RED), AllowsCleanRuntime + AllowsSlashBoundaryLookalike (GREEN)"
      contains: "RejectsRuntimeImportingTools"
    - path: "cmd/vet-tools-quarantine/main.go"
      provides: "singlechecker wrapper installed by make vet"
      contains: "singlechecker.Main"
    - path: "Makefile"
      provides: "VETTOOL_TOOLS_QUARANTINE var + vet: prereq + go vet -vettool line + install rule"
      contains: "VETTOOL_TOOLS_QUARANTINE"
  key_links:
    - from: "test/oracle/adopt/parity_test.go"
      to: "tools/dspy-tune/golden/parity_cases.json"
      via: "os.ReadFile of ../../../tools/dspy-tune/golden/parity_cases.json, asserts ClassifyChoice/FirstCommand per case"
      pattern: "parity_cases\\.json"
    - from: "internal/lint/toolsquarantine/analyzer.go"
      to: "github.com/agenthands/helix/tools"
      via: "import-path prefix match (exact OR slash-boundary) against toolsPrefix const"
      pattern: "agenthands/helix/tools"
    - from: "Makefile"
      to: "cmd/vet-tools-quarantine"
      via: "go install ./cmd/vet-tools-quarantine + go vet -vettool=$(VETTOOL_TOOLS_QUARANTINE) ./..."
      pattern: "vet-tools-quarantine"

prohibitions:
  - "No runtime package (internal/..., cmd/... except the analyzer's own cmd) imports github.com/agenthands/helix/tools/... — proven by the analyzer running clean under `make vet`."
  - "go.mod is unchanged: no dspy/python module path added (Go cannot depend on PyPI; assert no dspy edge)."
  - "No .go file and no go.mod is created under tools/ (only golden/parity_cases.json, a JSON data file, lives there from this plan) — tools/ stays invisible to `go test ./...`."
  - "The leakage analyzer adds NO blanket pip/exec.Command ban — it is import-boundary-only — so internal/langregistry/installer.go (legit pip/pipx LS install) is never flagged."
  - "The golden corpus is a SINGLE committed file; it is not duplicated under test/oracle/adopt/testdata."
---

<objective>
Build the Go-side authoritative gates for the DSPy quarantine spike (TUNE-01): (1) the shared golden parity corpus + the Go test that pins it to the `test/oracle/adopt` classifier truth, and (2) the `toolsquarantine` go/analysis leakage analyzer + its RED/GREEN/lookalike fixtures + cmd wrapper + `make vet` wiring. These are the merge-path-enforceable halves of the spike — every gate here is provable with `go test` / `make vet` and ships a deliberate break-the-invariant → RED fixture (the named CR-01 anti-vacuity discipline).

Purpose: The Go classifier (`test/oracle/adopt/scorecard.go`) is the single source of truth; this plan creates the corpus that BOTH languages assert against and the static boundary that keeps the dev-time Python tree (Plan 02) out of the shipped binary, `go.mod`, and `go test ./...`.
Output: `tools/dspy-tune/golden/parity_cases.json`, `test/oracle/adopt/parity_test.go`, the `internal/lint/toolsquarantine` package + testdata fixtures, `cmd/vet-tools-quarantine/main.go`, and Makefile `vet:` wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The single source of truth being mirrored + the parity-test sibling analog
@test/oracle/adopt/scorecard.go
@test/oracle/adopt/scorecard_test.go

# The leakage-analyzer template (import-boundary leg + slash-boundary discipline)
@internal/lint/ablationleakage/analyzer.go
@internal/lint/ablationleakage/analyzer_test.go
@internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/bench/runners/badrunner/imports.go
@internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/bench/runners/goodrunner/imports.go
@internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/bench/runners/siblingrunner/imports.go

# The singlechecker cmd wrapper to copy verbatim, and the Makefile vet: target to extend
@cmd/vet-ablation-leakage/main.go
@Makefile
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Shared golden parity corpus + Go parity test (pins corpus to the Go classifier truth)</name>
  <files>tools/dspy-tune/golden/parity_cases.json, test/oracle/adopt/parity_test.go</files>
  <read_first>
    - test/oracle/adopt/scorecard.go (FirstCommand lines 59-72; fallbackPrefixes line 77 = {"grep ","sed ","cat ","find ","rg ","ls "}; ClassifyChoice lines 85-95 — the EXACT branch behavior the corpus must encode)
    - test/oracle/adopt/scorecard_test.go (sibling-file idiom: package adopt; encoding/json + os + path/filepath + testify/require imports lines 1-12; loadFixtureBucket MinTasks floor lines 24-32; TestFirstCommandNotSubstring substring-trap discriminator lines 46-62)
    - 106-PATTERNS.md §"test/oracle/adopt/parity_test.go" (lines 164-205): the exact relative path ../../../tools/dspy-tune/golden/parity_cases.json and the per-case assert form
    - 106-RESEARCH.md §"The Parity Contract Design" (lines 128-152): the corpus must include ≥1 case per branch
  </read_first>
  <behavior>
    - Test asserts, for EVERY case in parity_cases.json: FirstCommand(response) == case.first_command AND ClassifyChoice(response) == (case.chose, case.fell_back).
    - Test asserts len(cases) >= 8 (corpus-shrink turns RED — mirror the loadFixtureBucket MinTasks floor).
    - Branch coverage cases that MUST be present and MUST pass: (a) a helix-choice e.g. "helix go-to-definition --symbol NewClient" → first_command lowercased "helix go-to-definition --symbol newclient", chose=true, fell_back=false; (b) ONE case per each of the 6 fallback prefixes "grep ","sed ","cat ","find ","rg ","ls " → chose=false, fell_back=true; (c) an unclassified prose-only case → chose=false, fell_back=false; (d) the SUBSTRING TRAP: "The helix tool would help, but let me grep.\ngrep -rn 'x' ." whose first_command is the prose line → chose=false, fell_back=false (NOT a choice — this is the load-bearing discriminator mirroring TestFirstCommandNotSubstring); (e) a fence-wrapped case "```\n$ helix find-references --symbol X\n```" → fences skipped, "$ " stripped, first_command "helix find-references --symbol x", chose=true; (f) a "lsp restart" lookalike → first_command "lsp restart", chose=false, fell_back=false (NOT an "ls " fallback — trailing-space discipline).
  </behavior>
  <action>
    Create tools/dspy-tune/golden/parity_cases.json as a JSON array of objects with keys `response`, `first_command`, `chose`, `fell_back`. Hand-author each expected field by mentally executing FirstCommand then ClassifyChoice from scorecard.go (lowercase happens ONCE on the extracted command via ToLower, so first_command is stored lowercased to match `FirstCommand`-lowercased classification expectations — NOTE: FirstCommand itself does NOT lowercase; ClassifyChoice lowercases. Store `first_command` as the RAW FirstCommand output, NOT lowercased, since the Go test asserts FirstCommand(response) == first_command directly). Include ≥10 cases covering every branch listed in <behavior>: helix-choice, all 6 fallback prefixes (grep/sed/cat/find/rg/ls), unclassified prose, the substring trap, a fence+prompt case, and the "lsp" lookalike. The substring-trap case and the lsp-lookalike are MANDATORY discriminators per 106-RESEARCH lines 150-152.

    Create test/oracle/adopt/parity_test.go in package adopt (sibling of scorecard_test.go). Add func TestPythonGoParityCorpus(t *testing.T): os.ReadFile(filepath.Join("..","..","..","tools","dspy-tune","golden","parity_cases.json")); json.Unmarshal into []struct{ Response string `json:"response"`; FirstCommand string `json:"first_command"`; Chose bool `json:"chose"`; FellBack bool `json:"fell_back"` }; require.GreaterOrEqual(len, 8, "corpus must cover every classifier branch"); loop asserting require.Equalf(c.FirstCommand, FirstCommand(c.Response), ...) and chose,fell := ClassifyChoice(c.Response); require.Equalf(c.Chose, chose, ...); require.Equalf(c.FellBack, fell, ...). Use the exact relative path from 106-PATTERNS.md line 185 (test/oracle/adopt is 3 dirs deep → ../../../tools/dspy-tune/...). This is RED-then-GREEN: writing the corpus to match the Go truth makes it pass; a deliberately wrong expected field in any case would fail, proving the test is non-vacuous (the corpus IS the assertion).
  </action>
  <verify>
    <automated>go test ./test/oracle/adopt/ -run TestPythonGoParityCorpus -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./test/oracle/adopt/ -run TestPythonGoParityCorpus -x` passes.
    - `python3 -c "import json,sys; d=json.load(open('tools/dspy-tune/golden/parity_cases.json')); print(len(d))"` prints >= 10 (best-effort; or `go test` GreaterOrEqual(len,8) is the authoritative floor).
    - The corpus contains the substring-trap case (a response whose first line is prose mentioning helix, second line greps) classified chose=false,fell_back=false, AND the "lsp"-lookalike classified chose=false,fell_back=false — grep the JSON to confirm both are present.
    - No .go file and no go.mod is created under tools/ (only the JSON): `find tools -name '*.go' -o -name 'go.mod' | wc -l` == 0.
  </acceptance_criteria>
  <done>TestPythonGoParityCorpus passes against a ≥10-case branch-covering corpus that includes the substring-trap and lsp-lookalike discriminators; the corpus is the single committed file under tools/dspy-tune/golden/.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: toolsquarantine leakage analyzer + RED/GREEN/lookalike fixtures (analysistest)</name>
  <files>internal/lint/toolsquarantine/analyzer.go, internal/lint/toolsquarantine/analyzer_test.go, internal/lint/toolsquarantine/testdata/src/github.com/agenthands/helix/internal/leakyruntime/imports.go, internal/lint/toolsquarantine/testdata/src/github.com/agenthands/helix/internal/goodruntime/imports.go, internal/lint/toolsquarantine/testdata/src/github.com/agenthands/helix/internal/toolsupport/imports.go, internal/lint/toolsquarantine/testdata/src/github.com/agenthands/helix/tools/dspytune/dspytune.go</files>
  <read_first>
    - internal/lint/ablationleakage/analyzer.go (package-doc block lines 1-27; imports lines 29-34; the slash-boundary discipline form `path == forbidden || strings.HasPrefix(path, forbidden+"/")` — quarantine uses ONLY this import-boundary leg, dropping the entire call-site/ChooseSource machinery)
    - internal/lint/ablationleakage/analyzer_test.go (whole file: package _test; analysistest.Run idiom; three named tests)
    - internal/lint/ablationleakage/testdata/src/.../badrunner/imports.go (RED `// want` directive form), goodrunner/imports.go (GREEN no-directive), siblingrunner/imports.go (lookalike no-directive)
    - 106-PATTERNS.md §"internal/lint/toolsquarantine/analyzer.go" (lines 30-74) and §"Leakage testdata fixtures" (lines 113-141) — the exact analyzer body, test names, and fixture placement
  </read_first>
  <behavior>
    - RED: a package whose import path does NOT start with the tools/ prefix (here internal/leakyruntime) that imports github.com/agenthands/helix/tools/dspytune MUST be reported (testdata `// want` directive matches the report message).
    - GREEN: a clean runtime package (internal/goodruntime importing only os/exec) MUST produce NO diagnostic (no `// want`).
    - GREEN (slash-boundary lookalike): internal/toolsupport — a package whose path contains the bare substring "tools" without the "tools/" slash boundary — MUST produce NO diagnostic, proving the analyzer uses exact-OR-slash matching not bare HasPrefix.
    - Self-import exemption: a package WHOSE OWN path is rooted under github.com/agenthands/helix/tools is exempt (the analyzer returns early; tools/ may self-import).
  </behavior>
  <action>
    Create internal/lint/toolsquarantine/analyzer.go, package toolsquarantine. Package doc mirrors ablationleakage's block: state the forbidden edge (no package outside tools/ imports github.com/agenthands/helix/tools/...), the slash-boundary discipline, and "the deliberate violation lives only under testdata/ so make vet on the real tree stays green". Imports: "strings" and "golang.org/x/tools/go/analysis" (NO go/ast — the import edge is read off file.Imports). const toolsPrefix = "github.com/agenthands/helix/tools". var Analyzer = &analysis.Analyzer{ Name:"toolsquarantine", Doc:"fails if a runtime/cmd package imports the dev-time tools/ tree", Run: ... }. In Run: if strings.HasPrefix(pass.Pkg.Path(), toolsPrefix) { return nil, nil } (self-import exemption); then for each file in pass.Files, for each imp in file.Imports: path := strings.Trim(imp.Path.Value, backquote-double-quote); if path == toolsPrefix || strings.HasPrefix(path, toolsPrefix+"/") { pass.Reportf(imp.Pos(), "runtime package %s must not import dev-time %s (got %s)", pass.Pkg.Path(), toolsPrefix, path) }. Use the EXACT analyzer body from 106-PATTERNS.md lines 50-69. Do NOT add any exec.Command/pip scan (import-boundary only — avoids the installer.go false-positive trap, 106-RESEARCH Pitfall 2).

    Create internal/lint/toolsquarantine/analyzer_test.go, package toolsquarantine_test. Import testing, golang.org/x/tools/go/analysis/analysistest, and the toolsquarantine package. Three funcs (names load-bearing — the per-wave gate greps -run RejectsRuntimeImportingTools and -run Allows): TestAnalyzer_RejectsRuntimeImportingTools → analysistest.Run on "github.com/agenthands/helix/internal/leakyruntime"; TestAnalyzer_AllowsCleanRuntime → "github.com/agenthands/helix/internal/goodruntime"; TestAnalyzer_AllowsSlashBoundaryLookalike → "github.com/agenthands/helix/internal/toolsupport".

    Create the four testdata fixtures under internal/lint/toolsquarantine/testdata/src/github.com/agenthands/helix/:
    - internal/leakyruntime/imports.go (package leakyruntime): a doc comment noting the deliberate violation lives only under testdata/; `import _ "github.com/agenthands/helix/tools/dspytune"` with a trailing `// want` directive whose regexp matches the report, e.g. `// want \`runtime package .* must not import .*\``.
    - internal/goodruntime/imports.go (package goodruntime): `import _ "os/exec"`, no // want.
    - internal/toolsupport/imports.go (package toolsupport): a lookalike whose path shares the bare "tools" prefix without the slash boundary; import only a permitted package (e.g. `import _ "os"`), no // want. NOTE: the package PATH internal/toolsupport does not start with toolsPrefix (which is .../tools, not .../internal/toolsupport), so it is analyzed; it imports nothing under tools/, so it must stay silent — this proves the import-side slash boundary AND that a bare-substring "tools" in a path is never matched.
    - tools/dspytune/dspytune.go (package dspytune): a stub package so the leakyruntime import resolves under the analysistest GOPATH; just `package dspytune` with an optional exported symbol. This fixture is UNDER the tools/ prefix so the analyzer's self-import exemption applies to it.
  </action>
  <verify>
    <automated>go test ./internal/lint/toolsquarantine/... -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/lint/toolsquarantine/ -run RejectsRuntimeImportingTools -x` passes (the planted leak trips the analyzer; the `// want` directive matches).
    - `go test ./internal/lint/toolsquarantine/ -run Allows -x` passes (both GREEN fixtures stay silent; analysistest fails on any spurious diagnostic).
    - `go build ./internal/lint/toolsquarantine/ ./cmd/vet-tools-quarantine/` succeeds (after Task 3).
    - Mutation check (proves non-vacuity): temporarily changing the analyzer's match to bare `strings.HasPrefix(path, toolsPrefix)` (without the slash) would NOT break the lookalike test (the import path under tools IS matched either way) — instead the load-bearing non-vacuity proof is the RED fixture: deleting the `// want` directive from leakyruntime/imports.go makes TestAnalyzer_RejectsRuntimeImportingTools FAIL. (Do not commit either mutation; this is the documented break-the-invariant proof.)
  </acceptance_criteria>
  <done>The analyzer flags a planted runtime→tools/ import (RED fixture with `// want`) and stays silent on a clean package and a slash-boundary lookalike (GREEN fixtures); all three analysistest functions pass.</done>
</task>

<task type="auto">
  <name>Task 3: cmd/vet-tools-quarantine singlechecker + Makefile vet: wiring (no LS-installer false-positive)</name>
  <files>cmd/vet-tools-quarantine/main.go, Makefile</files>
  <read_first>
    - cmd/vet-ablation-leakage/main.go (the 4-line singlechecker body to copy verbatim, swapping the analyzer import)
    - Makefile lines 20-25 (VETTOOL var defs), 47-54 (vet: prereq list + go vet -vettool lines), 56-72 (per-tool install rules) — the three edit sites
    - 106-PATTERNS.md §"Makefile vet: wiring (3 edit sites)" (lines 261-266): the exact VETTOOL_TOOLS_QUARANTINE entries mirroring the ABLATION_LEAKAGE pattern
    - internal/langregistry/installer.go lines 119,129 (the legit pip/pipx LS shell-out the analyzer MUST NOT flag — confirm `make vet` stays green on the real tree)
  </read_first>
  <action>
    Create cmd/vet-tools-quarantine/main.go: copy cmd/vet-ablation-leakage/main.go verbatim. Change the package doc to describe the runtime → dev-time tools/ import boundary. Swap the import to "github.com/agenthands/helix/internal/lint/toolsquarantine" and the body to func main() { singlechecker.Main(toolsquarantine.Analyzer) }. Keep "golang.org/x/tools/go/analysis/singlechecker".

    Edit Makefile in three places, mirroring the VETTOOL_ABLATION_LEAKAGE entry exactly:
    1. After line 25 (after VETTOOL_BENCH_RAG_LEAKAGE), add: VETTOOL_TOOLS_QUARANTINE=$(shell go env GOPATH)/bin/vet-tools-quarantine
    2. Add $(VETTOOL_TOOLS_QUARANTINE) to the vet: prereq list (line 47) and add a recipe line after line 54: $(GO) vet -vettool=$(VETTOOL_TOOLS_QUARANTINE) ./...
    3. After the $(VETTOOL_BENCH_RAG_LEAKAGE) install rule (line 71-72), add the install rule: $(VETTOOL_TOOLS_QUARANTINE): cmd/vet-tools-quarantine/main.go internal/lint/toolsquarantine/*.go \n\t$(GO) install ./cmd/vet-tools-quarantine
    Add a short comment in the vet: comment block (lines 27-46 region) explaining Phase 106 TUNE-01: vet-tools-quarantine enforces the runtime → dev-time tools/ import boundary so the DSPy harness never leaks into the shipped binary or go.mod.

    Do NOT add any pip/exec.Command ban to the Makefile or analyzer — the import-boundary analyzer is the sole mechanism, so the existing pip/pipx LS install in internal/langregistry/installer.go is never flagged.
  </action>
  <verify>
    <automated>make vet</automated>
  </verify>
  <acceptance_criteria>
    - `make vet` exits 0 on the real tree (installs vet-tools-quarantine, runs it via go vet -vettool, no diagnostic — proving zero runtime→tools/ import AND no false-positive on internal/langregistry/installer.go's pip/pipx).
    - `grep -c 'VETTOOL_TOOLS_QUARANTINE' Makefile` == 3 (var def + prereq/recipe usage + install rule target — count may be ≥3; confirm the var def line, a `-vettool=$(VETTOOL_TOOLS_QUARANTINE)` recipe line, and a `$(VETTOOL_TOOLS_QUARANTINE):` install target all exist).
    - `go run ./cmd/vet-tools-quarantine ./...` runs without flagging internal/langregistry/installer.go (the pip/pipx LS installer).
    - `grep -E 'dspy|pythonpath|pip install dspy' go.mod | wc -l` == 0 (go.mod carries no Python/DSPy edge).
  </acceptance_criteria>
  <done>`make vet` passes on the real tree with the new quarantine analyzer installed and wired into the vet: target; the analyzer flags zero packages (no runtime→tools/ import exists) and does NOT false-positive on the pip/pipx LS installer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-time tools/ tree → shipped Go binary / go.mod / go test ./... | The DSPy harness (Plan 02) must never become a runtime dependency. The static import boundary is the belt; this plan makes it load-bearing. |
| committed golden corpus → both language classifiers | The corpus is trusted, in-tree JSON read by the Go test (this plan) and the Python tests (Plan 02). No untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering / Elevation | dev-time Python dep leaks into the shipped binary | mitigate | cmd/vet-tools-quarantine import-boundary analyzer wired into make vet + planted-leak RED fixture (Task 2); tools/ holds no .go/go.mod so it is invisible to go test ./... |
| T-106-02 | Tampering | analyzer false-positive breaks make vet on the legit pip/pipx LS installer | mitigate | analyzer is import-boundary-ONLY (no exec.Command/pip scan), verified by `make vet` green on the real tree (Task 3) — installer.go:119,129 untouched |
| T-106-03 | Spoofing (vacuous gate) | a green-only parity test / green-only analyzer (the named CR-01 class) | mitigate | substring-trap + lsp-lookalike discriminators in the corpus (Task 1); RED `// want` fixture for the analyzer (Task 2) — each gate ships a deliberate break-the-invariant proof |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | this Go-side plan installs NO packages (only `go install ./cmd/vet-tools-quarantine` of an in-tree cmd); the Python pip installs live in Plan 02's git-ignored dev venv, off go.mod/binary/merge path |
</threat_model>

<verification>
- `go test ./test/oracle/adopt/... ./internal/lint/toolsquarantine/...` — both new gates pass.
- `make vet` — the quarantine analyzer installs, runs on the real tree, flags nothing (no runtime→tools/ import), and does NOT false-positive on the pip/pipx LS installer.
- `go test ./...` — full suite stays green (the new analyzer package + the new parity test are the only additions to the Go build; tools/ contains only JSON and is invisible to go test).
- `go run ./cmd/helix-refgen --check` — byte-clean (this plan touches no reference.md / SKILL.md content; the gate must remain green).
- `grep -E 'dspy|pip install dspy' go.mod | wc -l` == 0 — go.mod carries no Python/DSPy edge.
</verification>

<success_criteria>
- The shared golden corpus exists as ONE committed file under tools/dspy-tune/golden/ and TestPythonGoParityCorpus pins it to the Go classifier (ClassifyChoice + FirstCommand) over ≥8 branch-covering cases including the substring-trap and lsp-lookalike.
- The toolsquarantine analyzer flags a planted runtime→tools/ import (RED fixture) and stays silent on a clean package + a slash-boundary lookalike (GREEN fixtures).
- `make vet` runs the new analyzer on the real tree with no false-positive on the pip/pipx LS installer; go.mod carries no Python edge.
- `go test ./...` and `go run ./cmd/helix-refgen --check` remain green.
</success_criteria>

<output>
Create `.planning/phases/106-exploratory-dspy-offline-tuning-harness-spike/106-01-SUMMARY.md` when done.
</output>
