---
phase: 107-react-tool-using-agent-deepseek-openai-client-on-off-steerin
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/dspy-tune/test_agent.py
  - tools/dspy-tune/requirements.txt
  - tools/dspy-tune/agent/__init__.py
  - tools/dspy-tune/agent/llm.py
  - tools/dspy-tune/agent/tools.py
  - tools/dspy-tune/agent/react.py
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03]

must_haves:
  truths:
    - "A bounded ReAct loop emits `helix <verb>` argv via subprocess, observes each verb's stdout/exit, and returns a transcript with a termination reason (done | max_turns | no_progress | tool_error_budget) (AGENT-01)"
    - "A repeating/no-progress agent deterministically terminates with reason `no_progress` (not an infinite loop) (AGENT-01)"
    - "The LM is config-driven via one openai client: DeepSeek primary (base_url=https://api.deepseek.com), OpenAI fallback; model id read from AGENT_MODEL defaulting to `deepseek-v4-flash` (AGENT-02)"
    - "A real run with the selected provider's API key absent RAISES loudly (not a silent skip / empty transcript) (AGENT-02)"
    - "A DeepSeek failure falls back to the OpenAI client when OPENAI_API_KEY is present (AGENT-02)"
    - "`build_system_prompt(steering_text, 'on')` injects the steering text under a sentinel; `build_system_prompt(steering_text, 'off')` returns an independent control prompt that provably omits the sentinel (AGENT-03)"
    - "A hermetic test (fake LLM + fake helix, no network, keys unset) proves a degenerate always-grep agent scores 0 / emits no helix verb, and the OFF prompt provably omits the steering sentinel (anti-vacuity, break-the-invariant → assert-RED)"
  artifacts:
    - path: "tools/dspy-tune/agent/__init__.py"
      provides: "package barrel re-exporting ReActAgent, run(), build_system_prompt, STEERING_SENTINEL for in-process import by the Phase-108 metric"
      contains: "ReActAgent"
    - path: "tools/dspy-tune/agent/llm.py"
      provides: "provider-agnostic LLM.chat(): DeepSeek primary + OpenAI fallback, AGENT_MODEL config var, loud-fail-on-missing-key"
      min_lines: 30
      contains: "deepseek-v4-flash"
    - path: "tools/dspy-tune/agent/tools.py"
      provides: "fixed-argv `helix <verb>` subprocess shim (run_verb) + curated OpenAI TOOL_SCHEMAS for the routing-table verbs"
      min_lines: 30
      contains: "subprocess.run"
    - path: "tools/dspy-tune/agent/react.py"
      provides: "bounded think→act→observe loop, no-progress cap, transcript, steering ON/OFF system-prompt builder"
      min_lines: 40
      contains: "STEERING_SENTINEL"
    - path: "tools/dspy-tune/test_agent.py"
      provides: "hermetic fake-LLM + fake-helix test: AGENT-01/02/03 + the anti-vacuity break-the-invariant pair"
      min_lines: 60
      contains: "def test_degenerate_always_grep_scores_zero"
    - path: "tools/dspy-tune/requirements.txt"
      provides: "dev-venv-only openai pin"
      contains: "openai==2.43.0"
  key_links:
    - from: "tools/dspy-tune/agent/react.py"
      to: "tools/dspy-tune/agent/tools.py"
      via: "react loop calls run_verb(call, cwd) which subprocess-runs `helix <verb>`"
      pattern: "run_verb"
    - from: "tools/dspy-tune/agent/react.py"
      to: "tools/dspy-tune/agent/llm.py"
      via: "react loop calls self.llm.chat(messages, tools=TOOL_SCHEMAS)"
      pattern: "\\.chat\\("
    - from: "tools/dspy-tune/test_agent.py"
      to: "tools/dspy-tune/agent/react.py"
      via: "test imports ReActAgent / build_system_prompt / STEERING_SENTINEL and injects a duck-typed fake LLM + fake helix"
      pattern: "from agent"
  prohibitions:
    - "No `shell=True` anywhere in tools/dspy-tune/agent/ (fixed-argv subprocess.run([...]) only)"
    - "No new Go module dependency: `git diff --exit-code go.mod` MUST pass (empty)"
    - "No new `.go` file under tools/ and no `helix` subcommand that shells to Python (the agent is dev-time Python only)"
    - "The OFF control prompt MUST NOT contain STEERING_SENTINEL (and MUST NOT be built by redacting the steering text at runtime — it is an independent constant)"
    - "The agent's real run()/chat() path MUST NOT copy optimize.py's quiet `return 0` on a missing key — it raises; quiet-skip-exit-0 may live ONLY in an optional `__main__` wrapper"
    - "No API key value is ever written to the transcript, output/, logs, or git (keys read from env only)"
---

<objective>
Build a standalone, importable, dev-time Python tool-using ReAct agent under `tools/dspy-tune/agent/` (sibling of `scorer.py`/`optimize.py`) that drives the real `helix <verb>` CLI product surface in a bounded loop, with config-driven DeepSeek-primary/OpenAI-fallback provider selection and a first-class steering ON/OFF switch — the foundation every later v2.3 grader and the metric rewire depend on.

Purpose: Phase 107 is the first link in the v2.3 forced dependency chain (agent → Aider oracle + metric rewire → SWE-bench oracle → human-gated adoption). It ships a plain Python package the future GEPA metric can `import` and call in-process, exactly as `optimize.py` imports `score_choice_rate` today (per D — Claude's Discretion, locked constraints in 107-CONTEXT.md).

Output: `tools/dspy-tune/agent/{__init__.py,llm.py,tools.py,react.py}`, a hermetic `tools/dspy-tune/test_agent.py`, and one additive `openai==2.43.0` pin in `tools/dspy-tune/requirements.txt`. Zero Go code; zero `go.mod` change; `make vet` (`vet-tools-quarantine`) stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase artifacts — the implementation spec
@.planning/phases/107-react-tool-using-agent-deepseek-openai-client-on-off-steerin/107-RESEARCH.md
@.planning/phases/107-react-tool-using-agent-deepseek-openai-client-on-off-steerin/107-PATTERNS.md
@.planning/phases/107-react-tool-using-agent-deepseek-openai-client-on-off-steerin/107-CONTEXT.md

# In-tree analogs (read before writing — these define the conventions to mirror)
@tools/dspy-tune/optimize.py
@tools/dspy-tune/scorer.py
@tools/dspy-tune/test_parity.py
@tools/dspy-tune/test_degenerate.py
@tools/dspy-tune/requirements.txt
</context>

## Artifacts this phase produces

| Artifact | Kind | Key symbols / contents |
|----------|------|------------------------|
| `tools/dspy-tune/agent/__init__.py` | NEW package barrel | re-exports `ReActAgent`, `run`, `build_system_prompt`, `STEERING_SENTINEL`, `OFF_CONTROL_PROMPT` |
| `tools/dspy-tune/agent/llm.py` | NEW service | `LLM` class; `chat(messages, tools)`; config vars `AGENT_MODEL` (default `deepseek-v4-flash`), `AGENT_FALLBACK_MODEL` (default `gpt-4.1-mini`); loud-fail-on-missing-key; DeepSeek→OpenAI fallback |
| `tools/dspy-tune/agent/tools.py` | NEW utility | `TOOL_SCHEMAS` (curated routing-table verbs), `run_verb(call, cwd, timeout)`, `VerbResult`; fixed-argv `subprocess.run([...])` |
| `tools/dspy-tune/agent/react.py` | NEW service | `ReActAgent`, `run(task, cwd, max_turns)`, `Transcript`, `Step`, `build_system_prompt`, `STEERING_SENTINEL`, `OFF_CONTROL_PROMPT`; bounded loop + no-progress cap |
| `tools/dspy-tune/test_agent.py` | NEW hermetic test | `test_loop_emits_and_observes`, `test_no_progress_terminates`, `test_missing_key_raises`, `test_model_config_var`, `test_provider_fallback`, `test_steering_on_off_omission`, `test_degenerate_always_grep_scores_zero` |
| `tools/dspy-tune/requirements.txt` | MODIFIED | add `openai==2.43.0` (dev venv only) |
| config var `AGENT_MODEL` | NEW env var | default `deepseek-v4-flash` (alias-safe; `deepseek-chat` retires 2026/07/24 15:59 UTC) |
| config var `AGENT_FALLBACK_MODEL` | NEW env var | default `gpt-4.1-mini` (OpenAI fallback model id) |

> Test-location reconciliation (PATTERNS.md flag): the orchestrator file list and VALIDATION.md mention `agent/test_agent.py` + `conftest.py` + a pytest config. The existing `tools/dspy-tune/` convention is **flat, fixture-free, script-runnable pytest** (`test_parity.py`, `test_degenerate.py`, `test_split.py` are all flat siblings of `scorer.py`, fixture-free, pytest-discoverable from the directory with no config file). **Decision: the test lives FLAT at `tools/dspy-tune/test_agent.py`, uses inline fakes (no `conftest.py`), and no new pytest config file is added** — the directory is already pytest-discoverable. This matches the existing convention exactly; do not introduce `conftest.py` or a nested test unless a fixture is genuinely shared (it is not — there is one test module).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — hermetic test_agent.py contract + openai pin</name>
  <files>tools/dspy-tune/test_agent.py, tools/dspy-tune/requirements.txt</files>
  <read_first>
    @tools/dspy-tune/test_degenerate.py
    @tools/dspy-tune/test_parity.py
    @tools/dspy-tune/requirements.txt
    @.planning/phases/107-react-tool-using-agent-deepseek-openai-client-on-off-steerin/107-RESEARCH.md
  </read_first>
  <behavior>
    Author the FULL hermetic test contract FIRST (RED), mirroring the flat, fixture-free, script-runnable convention of test_parity.py / test_degenerate.py (pytest-discoverable AND runnable as `python3 test_agent.py` via an `if __name__ == "__main__":` block that calls every test and prints "agent OK"). Inline fakes, NO conftest.py, NO new pytest config. The tests import from the `agent` package (`from agent.react import ReActAgent, build_system_prompt, STEERING_SENTINEL`, `from agent.llm import LLM`, `from agent.tools import run_verb`) — they will fail at import (RED) until Tasks 2-3 land. Construct NO openai client and reach NO socket; assert the suite passes with DEEPSEEK_API_KEY/OPENAI_API_KEY UNSET (monkeypatch.delenv).

    Tests (the Req→Test map from 107-RESEARCH Validation Architecture):
    - test_loop_emits_and_observes (AGENT-01): inject a duck-typed fake LLM whose chat() returns a tool-call to a curated helix verb on turn 0 then a final answer on turn 1; monkeypatch run_verb (or put a stub `helix` script on PATH/cwd) to return canned stdout+exit 0; assert the returned Transcript has a step recording the verb + argv + exit + stdout, final answer set, reason == "done".
    - test_no_progress_terminates (AGENT-01): fake LLM emits the SAME tool call every turn (byte-identical argv) with identical canned output; assert the loop terminates with reason == "no_progress" within the cap (test must not hang — bound max_turns small).
    - test_missing_key_raises (AGENT-02): with DEEPSEEK_API_KEY unset (monkeypatch.delenv), constructing/using the REAL LLM (provider="deepseek") for a real run RAISES RuntimeError (not a silent empty result). Assert with pytest.raises.
    - test_model_config_var (AGENT-02): the default model id is "deepseek-v4-flash" when AGENT_MODEL is unset; setting AGENT_MODEL overrides it. (Read the default via the module-level constant or LLM().model with a key monkeypatched in — but never construct a real network client; assert the model string only.)
    - test_provider_fallback (AGENT-02): inject a fake primary client whose create() raises and a fake fallback path; with OPENAI_API_KEY present (monkeypatched to a dummy) and a fake OpenAI client, assert chat() returns the fallback client's message; with OPENAI_API_KEY absent after a primary failure, assert it raises.
    - test_steering_on_off_omission (AGENT-03): assert STEERING_SENTINEL in build_system_prompt(S, "on") AND STEERING_SENTINEL not in build_system_prompt(S, "off") — the provably-omitted control.
    - test_degenerate_always_grep_scores_zero (anti-vacuity, break-the-invariant): a fake LLM that ALWAYS emits a non-helix `grep` action (or never emits any helix verb) drives the loop; assert the transcript records NO helix verb step / the run does not coerce a "solved" success. Document inline that breaking the invariant (making the loop coerce a default success, or building OFF by leaking the sentinel) turns this RED.

    requirements.txt: add exactly one additive pin `openai==2.43.0` with the existing dev-time-only comment discipline; keep `dspy==3.2.1` and `pytest==8.3.5`.
  </behavior>
  <action>
    Create tools/dspy-tune/test_agent.py as a flat sibling of scorer.py (NOT under agent/, NOT with a conftest.py — honor the PATTERNS.md reconciliation flag: the existing suite is flat + fixture-free + script-runnable). Author every test in the behavior block above using inline duck-typed fakes (a FakeLLM with a scripted chat() returning objects exposing `.tool_calls` and `.content`, and FakeToolCall objects exposing `.id`, `.function.name`, `.function.arguments`) and either monkeypatching `agent.tools.run_verb` or placing a stub `helix` shim — pick whichever keeps the test hermetic with no network and no installed openai. The fake message objects must duck-type what react.py consumes (so coordinate the surface with Tasks 2-3; this is the contract). Include the `if __name__ == "__main__":` runner calling all tests then `print("agent OK")`. Tests MUST pass with both API keys unset. This is the RED step — it will fail at `from agent...` import until Tasks 2-3 create the package; commit it as the failing contract. Then edit tools/dspy-tune/requirements.txt to add `openai==2.43.0` (dev-venv-only comment), keeping the two existing pins. Do NOT inline any fenced code or the literal `shell=True` token anywhere (the negative grep in <verify> below greps the agent sources for it; keeping the test free of that literal too avoids any cross-file confusion).
  </action>
  <verify>
    <automated>cd tools/dspy-tune && python3 -c "import ast,sys; t=ast.parse(open('test_agent.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; req={'test_loop_emits_and_observes','test_no_progress_terminates','test_missing_key_raises','test_model_config_var','test_provider_fallback','test_steering_on_off_omission','test_degenerate_always_grep_scores_zero'}; missing=req-names; sys.exit('MISSING TESTS: '+','.join(sorted(missing)) if missing else 0)"</automated>
    <automated>grep -q 'openai==2.43.0' tools/dspy-tune/requirements.txt && grep -q 'dspy==3.2.1' tools/dspy-tune/requirements.txt && grep -q 'pytest==8.3.5' tools/dspy-tune/requirements.txt</automated>
    <automated>git diff --exit-code go.mod</automated>
  </verify>
  <acceptance_criteria>
    - tools/dspy-tune/test_agent.py exists flat (no agent/test_agent.py, no conftest.py, no new pytest config) and defines all 7 named tests.
    - The module is script-runnable (`if __name__ == "__main__":` calls every test).
    - Tests import from the `agent` package (will RED until Tasks 2-3) and require NO API key / NO network.
    - requirements.txt has exactly the three pins (dspy==3.2.1, pytest==8.3.5, openai==2.43.0); git diff go.mod is empty.
  </acceptance_criteria>
  <done>The hermetic test contract is authored and committed as the RED baseline; `openai==2.43.0` is pinned dev-venv-only; go.mod unchanged.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — llm.py (provider-agnostic chat, loud-fail, fallback) + tools.py (fixed-argv subprocess shim + schemas)</name>
  <files>tools/dspy-tune/agent/__init__.py, tools/dspy-tune/agent/llm.py, tools/dspy-tune/agent/tools.py</files>
  <read_first>
    @tools/dspy-tune/optimize.py
    @tools/dspy-tune/scorer.py
    @.planning/phases/107-react-tool-using-agent-deepseek-openai-client-on-off-steerin/107-RESEARCH.md
    @.planning/phases/107-react-tool-using-agent-deepseek-openai-client-on-off-steerin/107-PATTERNS.md
  </read_first>
  <behavior>
    - llm.py: an `LLM` class with `chat(messages, tools)` returning the first choice's message. Module-top `from openai import OpenAI`. Config vars via `os.environ.get(VAR, default)`: `AGENT_MODEL` default "deepseek-v4-flash" (alias-safe), `AGENT_FALLBACK_MODEL` default "gpt-4.1-mini". DeepSeek primary client `OpenAI(api_key=DEEPSEEK_API_KEY, base_url="https://api.deepseek.com")`. LOUD-FAIL: if the selected provider's key is absent on a real run, RAISE RuntimeError (INVERT optimize.py's quiet `return 0` — that quiet-skip belongs only to an optional `__main__` wrapper, never inside chat()/__init__ used by run()). Fallback: try primary `chat.completions.create(...)`; on any exception, construct the OpenAI fallback client lazily from OPENAI_API_KEY (raise if absent) and retry with AGENT_FALLBACK_MODEL. Keys read from env ONLY; never logged or returned.
    - tools.py: `TOOL_SCHEMAS` — a curated subset of the routing-table verbs (go-to-definition, find-references, search-symbols, get-symbol-overview, read-file, replace-in-file, fuzzy-edit, insert-before-symbol, insert-after-symbol, get-diagnostics — verbatim kebab names from internal/cli/verbs_gen.go), each an OpenAI function-tool schema. `run_verb(call, cwd, timeout=60)`: decode `json.loads(call.function.arguments or "{}")`, build a FIXED argv list `["helix", verb, *mapped_args]`, `subprocess.run(argv, cwd=cwd, capture_output=True, text=True, timeout=timeout)`, return a `VerbResult(argv, exit, stdout)`. NEVER `shell=True`; NEVER string-build the command. The verb is constrained to the schema name set.
    - __init__.py: thin barrel re-exporting `LLM` from llm and `run_verb`, `TOOL_SCHEMAS`, `VerbResult` from tools (react.py exports land in Task 3). Mirror scorer.py's thin-public-API shape.
    - All three modules carry the optimize.py-style quarantine docstring header ("lives under tools/ — NEVER linked into the helix binary / helix setup / go.mod / `go test ./...`").
  </behavior>
  <action>
    Create the `tools/dspy-tune/agent/` package. Implement llm.py per the behavior block: config-var model selection defaulting to deepseek-v4-flash, the loud-fail-on-missing-key RAISE (the explicit INVERSION of optimize.py:48-62 quiet-skip — write a docstring note citing this inversion), and the DeepSeek→OpenAI try/except fallback (RESEARCH Example 2). Implement tools.py per the behavior block: the curated TOOL_SCHEMAS list (use only verb names confirmed present in internal/cli/verbs_gen.go) and the fixed-argv run_verb shim (RESEARCH Example 3 / PATTERNS Pattern 2) — argv is always a Python list passed to subprocess.run; do not pass a string and do not enable a shell. Add the quarantine docstring header to each module. Create __init__.py re-exporting the llm + tools symbols. Run the relevant hermetic tests for these modules to confirm the loud-fail, model-config-var, and provider-fallback tests go GREEN (the loop/transcript tests still RED until Task 3 — that is expected). Do NOT write any fenced code block or the literal three-token `shell` directive into module comments.
  </action>
  <verify>
    <automated>cd tools/dspy-tune && python3 -m pytest test_agent.py -k "missing_key or model_config or provider_fallback or steering" -q 2>/dev/null || python3 -c "import ast; [ast.parse(open(f).read()) for f in ('agent/llm.py','agent/tools.py','agent/__init__.py')]"</automated>
    <automated>grep -RnL --include='*.py' . tools/dspy-tune/agent >/dev/null 2>&1; grep -rn 'shell=True' tools/dspy-tune/agent && exit 1 || exit 0</automated>
    <automated>grep -q 'deepseek-v4-flash' tools/dspy-tune/agent/llm.py && grep -q 'subprocess.run' tools/dspy-tune/agent/tools.py && grep -q 'base_url' tools/dspy-tune/agent/llm.py</automated>
    <automated>git diff --exit-code go.mod</automated>
  </verify>
  <acceptance_criteria>
    - agent/llm.py defines LLM.chat() with deepseek-v4-flash default, base_url=https://api.deepseek.com primary, loud-fail RAISE on missing key, and OpenAI fallback.
    - agent/tools.py defines TOOL_SCHEMAS (curated verbs, names matching verbs_gen.go) and run_verb() using a fixed-argv subprocess.run list; no `shell=True` anywhere in agent/.
    - agent/__init__.py re-exports LLM, run_verb, TOOL_SCHEMAS, VerbResult.
    - The missing-key / model-config-var / provider-fallback / steering-omission hermetic tests pass (loop tests may still be RED pending Task 3).
    - git diff go.mod empty.
  </acceptance_criteria>
  <done>llm.py and tools.py exist with the loud-fail, config-var, fallback, and fixed-argv subprocess contracts; the LM/tool hermetic tests are GREEN; the package barrel exports the symbols; no shell injection surface; go.mod unchanged.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: GREEN — react.py (bounded loop, no-progress cap, steering ON/OFF, transcript) + full suite + boundary gate</name>
  <files>tools/dspy-tune/agent/react.py, tools/dspy-tune/agent/__init__.py</files>
  <read_first>
    @tools/dspy-tune/agent/llm.py
    @tools/dspy-tune/agent/tools.py
    @.planning/phases/107-react-tool-using-agent-deepseek-openai-client-on-off-steerin/107-RESEARCH.md
    @.planning/phases/107-react-tool-using-agent-deepseek-openai-client-on-off-steerin/107-PATTERNS.md
  </read_first>
  <behavior>
    - react.py: `ReActAgent(system_prompt, llm, ...)` with `run(task, cwd, max_turns=12) -> Transcript`. The bounded loop (RESEARCH Example 1): messages = [{system}, {user:task}]; each turn `msg = self.llm.chat(messages, tools=TOOL_SCHEMAS)`; if no tool_calls → return Transcript(steps, final=msg.content, reason="done"); else append the assistant message FIRST (Pattern 1 critical detail), then for each call `run_verb(call, cwd)`, record a Step(verb, argv, exit, stdout-tail), append `{"role":"tool","tool_call_id":call.id,"content":stdout[:STDOUT_CAP]}`. Two-bound termination: hard `max_turns` cap (reason="max_turns") AND a no-progress heuristic — terminate with reason="no_progress" when the last N tool calls are byte-identical argv+output (or a tool-error budget is exceeded → reason="tool_error_budget"). Record the termination reason in the Transcript.
    - Steering builder: `STEERING_SENTINEL` = a distinctive token only ever in the steered prompt; `OFF_CONTROL_PROMPT` = an INDEPENDENT constant (not derived from the steering text); `build_system_prompt(steering_text, steering)` returns `f"{STEERING_SENTINEL}\n{steering_text}"` for "on" and `OFF_CONTROL_PROMPT` for "off". The OFF prompt provably omits the sentinel (the test asserts disjointness). Do NOT build OFF by redacting the ON prompt at runtime.
    - Transcript/Step are simple dataclasses (or dicts) serializable to the AGENT-01 JSON trace shape (task, steering, model, steps[], final, reason). Keys are never written into the trace.
    - __init__.py: add re-exports of ReActAgent, run (if a module-level convenience exists), build_system_prompt, STEERING_SENTINEL, OFF_CONTROL_PROMPT so the Phase-108 metric can `from agent import ReActAgent` in-process.
    - The full hermetic suite (all 7 tests) MUST pass with keys unset and no network.
  </behavior>
  <action>
    Implement react.py per the behavior block: the bounded think→act→observe loop with the assistant-message-before-tool-results ordering (Pattern 1), the two-bound termination (max_turns hard cap + no-progress heuristic recording a reason), the Transcript/Step trace, and the steering ON/OFF builder with STEERING_SENTINEL + an independent OFF_CONTROL_PROMPT constant (RESEARCH Example 5 / PATTERNS Pattern 4 — build OFF as a separate constant, assert omission; never redact). Add the optimize.py-style quarantine docstring header. Extend agent/__init__.py to re-export the react symbols. Run the FULL hermetic suite and confirm all 7 tests GREEN with both keys unset. Then run the boundary gate: `make vet` (must stay green — proves no Go regression / `vet-tools-quarantine` does not fire, since all new edges are Python under the exempt tools/ prefix) and `git diff --exit-code go.mod` (empty). Run `go vet ./...` / `go build ./cmd/helix` only to confirm the Go side is byte-unchanged. Fold a code-review pass on the no-progress heuristic and the OFF-omission BEFORE declaring done (the v2.2 vacuous-guard lesson): verify the degenerate-agent test genuinely RED-flips if the loop is mutated to coerce a default success, and the OFF-omission test RED-flips if the sentinel leaks into OFF.
  </action>
  <verify>
    <automated>cd tools/dspy-tune && python3 -m pytest test_agent.py -q</automated>
    <automated>cd tools/dspy-tune && python3 test_agent.py</automated>
    <automated>grep -q 'STEERING_SENTINEL' tools/dspy-tune/agent/react.py && grep -q 'no_progress' tools/dspy-tune/agent/react.py && grep -q 'max_turns' tools/dspy-tune/agent/react.py</automated>
    <automated>grep -rn 'shell=True' tools/dspy-tune/agent && exit 1 || exit 0</automated>
    <automated>make vet</automated>
    <automated>git diff --exit-code go.mod</automated>
  </verify>
  <acceptance_criteria>
    - agent/react.py implements the bounded loop (assistant msg appended before tool results), the no-progress + max_turns + tool_error_budget termination reasons, the Transcript/Step trace, and build_system_prompt with STEERING_SENTINEL + an independent OFF_CONTROL_PROMPT.
    - All 7 hermetic tests in test_agent.py pass via pytest AND via `python3 test_agent.py`, with DEEPSEEK_API_KEY/OPENAI_API_KEY unset and no network.
    - The degenerate-always-grep test scores 0 / emits no helix verb; the OFF prompt provably omits the sentinel (anti-vacuity pair green AND review-confirmed to RED-flip on a mutated invariant).
    - `make vet` green; `git diff go.mod` empty; no `shell=True` and no new `.go` file under tools/.
  </acceptance_criteria>
  <done>The full hermetic agent suite is GREEN (no network, keys unset), the bounded loop + steering ON/OFF + transcript are complete, the anti-vacuity pair is review-confirmed non-vacuous, and the ADOPT-04 boundary (empty go.mod diff, green make vet, no Go edge) is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM (DeepSeek/OpenAI) → agent | The model's tool-call arguments (JSON strings) are untrusted input crossing into `run_verb`; they select a verb name and supply args. |
| agent → `helix` subprocess | The agent spawns the shipped `helix` binary; the argv is the only thing crossing. |
| dev environment → agent | `DEEPSEEK_API_KEY` / `OPENAI_API_KEY` are read from env; they must not leak into the transcript, output/, logs, or git. |

> Scope note: this is an offline, dev-time Python tool under `tools/` with no production/network-service surface, no user auth, and no untrusted network input beyond the model's own tool-call JSON. The threat register is kept proportionate (ASVS L1, light V5/V7 only).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Tampering / EoP | `tools.py` run_verb (model-supplied tool-call args) | mitigate | Fixed-argv `subprocess.run([...])` — NEVER `shell=True`, never string-concatenate into a shell; the verb is constrained to the curated TOOL_SCHEMAS name set (the model cannot invoke an arbitrary command); args mapped through a fixed argv map. Negative grep `shell=True == 0` in agent/ is a hard verify in Tasks 2-3. |
| T-107-02 | Information disclosure | `llm.py` / `react.py` (API keys) | mitigate | Keys read from `os.environ` ONLY; the transcript records argv + stdout, never env; keys are never logged or returned. Mirrors the existing harness env-only-key discipline. |
| T-107-03 | DoS (runaway loop) | `react.py` loop | mitigate | Hard `max_turns` cap + no-progress heuristic (byte-identical argv+output) + per-subprocess `timeout=`; the no_progress termination test deterministically proves the bound (test must not hang). |
| T-107-04 | Spoofing / Repudiation | provider selection | accept | Single dev-time tool, single operator; no multi-tenant or audit requirement. Low value target, offline. |
| T-107-SC | Tampering | pip install (`openai==2.43.0`) | mitigate | One net-new pin; `openai` is the first-party OpenAI SDK (RESEARCH Package Legitimacy Audit: OK/Approved, Context7 High + official repo). No `[ASSUMED]`/`[SUS]` packages → no blocking-human legitimacy checkpoint required. Installed into a git-ignored dev venv only; never touches go.mod / the binary / `helix setup`. |
</threat_model>

<verification>
## Phase-level checks (run before /gsd-verify-work)

- `cd tools/dspy-tune && python3 -m pytest test_agent.py -q` — all 7 hermetic tests green (no network, keys unset).
- `cd tools/dspy-tune && python3 test_agent.py` — script-runnable path prints "agent OK".
- `cd tools/dspy-tune && python3 -m pytest -q` — full existing suite (split/parity/degenerate) + the new agent test all green (no regression).
- `make vet` — green (includes `vet-tools-quarantine`; proves no Go import edge into tools/ and no Go regression).
- `git diff --exit-code go.mod` — empty (zero new Go module deps).
- `grep -rn 'shell=True' tools/dspy-tune/agent` — no match (no shell injection surface).
- No new `.go` file under `tools/`; no `helix` subcommand shells to Python (the agent is dev-time Python only).

## Anti-vacuity review (fold BEFORE verify — v2.2 vacuous-guard lesson)
- Mutate the loop to coerce a default "solved" → `test_degenerate_always_grep_scores_zero` must go RED.
- Leak STEERING_SENTINEL into OFF_CONTROL_PROMPT → `test_steering_on_off_omission` must go RED.
- Remove the no-progress cap → `test_no_progress_terminates` must hang/RED (bound max_turns small so it fails fast, not infinitely).
</verification>

<success_criteria>
1. The agent under `tools/dspy-tune/agent/` drives `helix <verb>` via a fixed-argv subprocess in a bounded ReAct loop, observes stdout/exit, and returns a transcript with a termination reason; a no-progress agent deterministically terminates (AGENT-01).
2. The LM is config-driven via one openai client — DeepSeek primary (base_url=https://api.deepseek.com, model AGENT_MODEL default deepseek-v4-flash), OpenAI fallback — and a real run with the selected provider's key absent RAISES loudly (AGENT-02).
3. `--steering on|off` is plumbed via build_system_prompt: ON injects the steering text under STEERING_SENTINEL, OFF returns an independent control prompt that provably omits it (AGENT-03).
4. The hermetic `test_agent.py` (fake LLM + fake helix, no network, keys unset) ships the break-the-invariant pair: a degenerate always-grep agent scores 0, and OFF provably omits the steering text — both review-confirmed to RED-flip on a mutated invariant.
5. ADOPT-04 boundary preserved: empty `git diff go.mod`, green `make vet`, no `helix`→Python shell, no new `.go` under tools/.
</success_criteria>

<output>
Create `.planning/phases/107-react-tool-using-agent-deepseek-openai-client-on-off-steerin/107-01-SUMMARY.md` when done.
</output>
