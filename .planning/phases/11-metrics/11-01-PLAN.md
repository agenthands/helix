---
phase: 11-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/obs/obs.go
  - internal/obs/metrics.go
  - internal/obs/metrics_labels_test.go
  - internal/obs/metrics_test.go
  - internal/daemon/telemetry.go
  - internal/daemon/telemetry_metrics_test.go
  - internal/daemon/daemon.go
autonomous: true
requirements: [METRIC-01, METRIC-04, METRIC-05]
must_haves:
  truths:
    - "An operator can scrape http://127.0.0.1:{admin}/metrics and receive a 200 with Prometheus exposition format"
    - "The response contains go_goroutines, go_gc_*, process_resident_memory_bytes (runtime collectors)"
    - "Every registered *Vec uses label names from the 5-element allowlist (tool_name, profile, mode, language, outcome)"
    - "obs.Noop(...) still returns a Provider whose Metrics() accessor returns a non-nil sink"
  artifacts:
    - path: "internal/obs/metrics.go"
      provides: "Metrics struct with pre-registered tool + lspool vectors and Registry"
      contains: "prometheus.NewRegistry"
    - path: "internal/obs/metrics_labels_test.go"
      provides: "CI allowlist enforcement for METRIC-05"
      contains: "AllowedLabels"
    - path: "internal/daemon/telemetry.go"
      provides: "/metrics handler mounted on admin mux via promhttp.HandlerFor"
      contains: "promhttp.HandlerFor"
  key_links:
    - from: "internal/daemon/telemetry.go"
      to: "internal/obs/metrics.go"
      via: "d.obs.Metrics().Registry() passed to promhttp.HandlerFor"
      pattern: "promhttp\\.HandlerFor"
    - from: "internal/obs/metrics.go"
      to: "prometheus.NewRegistry"
      via: "owned non-global registry (PITFALLS #2)"
      pattern: "prometheus\\.NewRegistry"
---

<objective>
Stand up the `internal/obs/` metrics scaffold with pre-registered Prometheus vectors, enforce the bounded-label allowlist via CI test, and mount `/metrics` on the existing Phase 10 admin listener. Noop path remains zero-dep for tests that do not want metrics.

Purpose: METRIC-01, METRIC-04, METRIC-05. Provides the registry + exposition surface that plans 02 and 03 will populate.
Output: obs.Metrics type, label lint test, /metrics endpoint wired on admin mux, Go + Process runtime collectors registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-metrics/11-CONTEXT.md
@.planning/phases/11-metrics/11-RESEARCH.md
@internal/obs/obs.go
@internal/obs/handler.go
@internal/daemon/telemetry.go
@internal/daemon/daemon.go

<interfaces>
<!-- Phase 10 obs.Provider surface -->
```go
// internal/obs/obs.go
type Provider struct {
    slogHandler slog.Handler
}
func Noop(inner slog.Handler) *Provider
func (p *Provider) SlogHandler() slog.Handler
```

<!-- Phase 10 admin mux — /metrics must be added alongside /healthz -->
```go
// internal/daemon/telemetry.go
mux := http.NewServeMux()
mux.HandleFunc("/healthz", d.handleHealthz)
mux.HandleFunc("/readyz", d.handleReadyz)
if d.config.Observability.EnablePprof {
    registerPprof(mux)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add prometheus/client_golang and scaffold obs.Metrics with registry + vectors</name>
  <read_first>
    - .planning/phases/11-metrics/11-RESEARCH.md (Pattern 1, lines 147-222)
    - internal/obs/obs.go (Provider struct — extend without breaking)
    - go.mod (verify prometheus not yet present)
  </read_first>
  <files>
    go.mod
    go.sum
    internal/obs/metrics.go
    internal/obs/obs.go
    internal/obs/metrics_test.go
  </files>
  <behavior>
    - Test: newMetrics() returns a non-nil *Metrics whose registry is non-nil and separate from prometheus.DefaultRegisterer
    - Test: the registry gathers at least these metric families: serena_tool_calls_total, serena_tool_duration_seconds, serena_lspool_workers, serena_lspool_evictions_total, serena_lspool_circuit_state, serena_lspool_restarts_total, go_goroutines, process_resident_memory_bytes
    - Test: Noop(inner).Metrics() returns a non-nil sink whose ToolCalls/ToolDuration are usable (no nil deref) — use a real noop registry; do NOT branch on nil at call sites
    - Test: calling newMetrics() twice in the same test succeeds (fresh registry each time — no double-registration panic)
    - Test: AllowedLabels is the exact 5-element array {tool_name, profile, mode, language, outcome}
  </behavior>
  <action>
    1. Run `go get github.com/prometheus/client_golang@latest` then `go mod tidy`. Record the exact resolved version (expect v1.23.x) in the task commit message.
    2. Create `internal/obs/metrics.go`:
       - Exported `AllowedLabels = [...]string{"tool_name", "profile", "mode", "language", "outcome"}` (array, not slice — immutable).
       - Exported `Metrics` struct with fields: `registry *prometheus.Registry`, `ToolCalls *prometheus.CounterVec`, `ToolDuration *prometheus.HistogramVec`, `LSPoolWorkers *prometheus.GaugeVec`, `LSPoolEvictions *prometheus.CounterVec`, `LSPoolCircuitState *prometheus.GaugeVec`, `LSPoolRestarts *prometheus.CounterVec`.
       - Constructor `newMetrics() *Metrics` that creates a fresh `prometheus.NewRegistry()` and registers ALL vectors PLUS `collectors.NewGoCollector()` and `collectors.NewProcessCollector(collectors.ProcessCollectorOpts{})` via `reg.MustRegister(...)`. Metric names + labels per D-09..D-15 (see RESEARCH.md Pattern 1 for exact OPTS). `ToolDuration` uses `prometheus.DefBuckets` per D-01. NEVER touch `prometheus.DefaultRegisterer` (PITFALLS.md meta-rule).
       - Exported method `Registry() *prometheus.Registry` returning the internal registry for promhttp.
       - Implement lspool sink adapter methods on `*Metrics`: `LSPoolWorkersSet(language string, delta float64)` that calls `.WithLabelValues(language).Add(delta)`, `LSPoolEviction(language, reason string)`, `LSPoolCircuitStateSet(language string, state float64)`, `LSPoolRestart(language string)`. These satisfy the `lspool.MetricsSink` interface defined in plan 03; compile-time assertion happens in plan 03's wiring.
    3. Extend `internal/obs/obs.go`:
       - Add field `metrics *Metrics` to `Provider`.
       - Change `Noop(inner slog.Handler) *Provider` to also construct `newMetrics()` so noop callers get a working (but unscraped) metrics sink. This is cheap — Noop is only called at startup.
       - Add exported method `func (p *Provider) Metrics() *Metrics { return p.metrics }` (never returns nil).
    4. Create `internal/obs/metrics_test.go` with the behavior tests above. Use `reg.Gather()` to enumerate registered metric families and assert names. Use `prometheus.CollectAndCount(vec)` only if needed.
    5. Run `gofmt -w internal/obs/ && go vet ./internal/obs/... && go test ./internal/obs/...`.

    DO NOT import `prometheus/client_golang` anywhere outside `internal/obs/` in this task — keeps the blast radius tight. Plans 02 and 03 consume via `*obs.Metrics` only.
  </action>
  <verify>
    <automated>go test ./internal/obs/... -run TestMetrics -x && go vet ./internal/obs/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'prometheus/client_golang' go.mod` succeeds
    - `grep -q 'prometheus.NewRegistry' internal/obs/metrics.go` succeeds
    - `grep -q 'AllowedLabels' internal/obs/metrics.go` succeeds
    - `grep -q 'DefaultRegisterer' internal/obs/metrics.go` returns NO matches (globals banned)
    - `grep -q 'func (p \*Provider) Metrics()' internal/obs/obs.go` succeeds
    - `go test ./internal/obs/... -run TestMetrics` passes
    - **LSPool sink contract (FIXED signatures consumed by Plan 03 — verbatim match required):**
      - `grep -c 'func (m \*Metrics) LSPoolWorkersSet(language string, delta float64)' internal/obs/metrics.go` equals 1
      - `grep -c 'func (m \*Metrics) LSPoolEviction(language, reason string)' internal/obs/metrics.go` equals 1
      - `grep -c 'func (m \*Metrics) LSPoolCircuitStateSet(language string, state float64)' internal/obs/metrics.go` equals 1
      - `grep -c 'func (m \*Metrics) LSPoolRestart(language string)' internal/obs/metrics.go` equals 1
      - These signatures are the source of truth for `lspool.MetricsSink` (Plan 03). If Plan 03 execution reveals a mismatch, Plan 03 MUST adapt its interface — Plan 01's obs.Metrics methods are frozen after this plan lands.
  </acceptance_criteria>
  <done>obs.Metrics registers 6 serena_* vectors + Go + Process collectors on an owned registry; Provider.Metrics() never returns nil; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Label allowlist CI lint test (METRIC-05)</name>
  <read_first>
    - internal/obs/metrics.go (from Task 1 — inspect *Vec fields)
    - .planning/phases/11-metrics/11-RESEARCH.md (D-03..D-05)
  </read_first>
  <files>
    internal/obs/metrics_labels_test.go
  </files>
  <behavior>
    - Test TestMetricsLabelsAllowlist: enumerate every *Vec field on *Metrics (via reflection or explicit list), gather metric families from the registry, and assert each label name on every collected metric is a member of AllowedLabels
    - Test TestMetricsLabelsAllowlist fails loudly if a future plan adds a vector with label "user_id" or similar
    - Test TestLSPoolEvictionsReasonLabel: the evictions vector uses "language" and "reason" — "reason" is NOT in AllowedLabels, so the test must carve an exemption OR we must add "reason" to AllowedLabels. DECISION: "reason" is a closed enum (idle/pressure/crash/shutdown) NOT in D-04 allowlist. Resolution: the evictions vector counter's "reason" label is an internal dimensional slice, not an RED label. Extend the test to allow {"language", "reason"} exclusively for serena_lspool_evictions_total. Document the carve-out in a comment referencing CONTEXT.md D-13.
  </behavior>
  <action>
    1. Create `internal/obs/metrics_labels_test.go`:
       - Build an `allowed` set from `AllowedLabels`.
       - Define a `carveOuts` map: `{"serena_lspool_evictions_total": {"reason": true}}` with a block comment explaining D-13 (reason is a closed 4-value enum enforced at emission sites, not at the CI label lint).
       - Fetch `m := newMetrics()`, gather `mfs, _ := m.Registry().Gather()`.
       - For each MetricFamily: skip go_* and process_* (Go/Process collectors — not Serena-owned, not subject to D-04).
       - For each metric in the family, for each label pair, assert `allowed[labelName] || carveOuts[familyName][labelName]`.
       - Failure message: `"metric %q uses forbidden label %q — add to AllowedLabels or remove"`.
    2. Confirm the test passes with the Task 1 vector set.
    3. Add a negative unit test `TestMetricsLabelsAllowlist_catchesDrift` that constructs a temporary registry with a fake vector carrying label `"user_id"` and asserts the same lint function rejects it — prove the lint actually works.
    4. Run `go vet ./internal/obs/... && go test ./internal/obs/...`.
  </action>
  <verify>
    <automated>go test ./internal/obs/... -run TestMetricsLabels -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'TestMetricsLabelsAllowlist' internal/obs/metrics_labels_test.go` succeeds
    - `grep -q 'carveOuts' internal/obs/metrics_labels_test.go` succeeds
    - `grep -q 'user_id' internal/obs/metrics_labels_test.go` succeeds (negative test)
    - `go test ./internal/obs/... -run TestMetricsLabels` passes
  </acceptance_criteria>
  <done>CI-time label lint enforces the allowlist; drift-detection negative test proves the lint catches a bad vector; D-13 carve-out documented inline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount /metrics on admin listener via promhttp.HandlerFor</name>
  <read_first>
    - internal/daemon/telemetry.go (admin mux — add handler alongside /healthz, /readyz)
    - internal/daemon/daemon.go (how d.obs is constructed; extend if needed)
    - .planning/phases/11-metrics/11-RESEARCH.md (Open Question #2 — HandlerFor over Handler())
  </read_first>
  <files>
    internal/daemon/telemetry.go
    internal/daemon/daemon.go
    internal/daemon/telemetry_metrics_test.go
  </files>
  <behavior>
    - Test: with AdminAddr set to 127.0.0.1:0 and metrics enabled, GET /metrics returns 200 and body contains "serena_tool_calls_total", "serena_lspool_workers", "go_goroutines", "process_resident_memory_bytes"
    - Test: Content-Type on /metrics is "text/plain; version=0.0.4; charset=utf-8" (promhttp default) OR the OpenMetrics variant — assert it contains "text/plain"
    - Test: /healthz and /readyz still work alongside /metrics (regression guard for Phase 10)
    - Test: when d.obs.Metrics() registry has no serena_tool_calls_total observations yet, the metric family is still listed (counters pre-initialized with zero labels? No — CounterVec with no labelled series yields empty family. The "listed" assertion only applies after some Inc. For this test, increment a counter before scrape.)
  </behavior>
  <action>
    1. Modify `internal/daemon/telemetry.go`:
       - Add import `"github.com/prometheus/client_golang/prometheus/promhttp"`.
       - Inside `listenAdmin`, after pprof block, add:
         ```go
         mux.Handle("/metrics", promhttp.HandlerFor(
             d.obs.Metrics().Registry(),
             promhttp.HandlerOpts{
                 ErrorLog:      nil,
                 ErrorHandling: promhttp.ContinueOnError,
             },
         ))
         d.logger.Info("metrics endpoint enabled", "addr", bound, "path", "/metrics")
         ```
       - Place `/metrics` registration regardless of EnablePprof — metrics is independent of pprof.
    2. Verify `daemon.Daemon` already holds `d.obs *obs.Provider`. If the field is named differently (e.g. `d.observability`), use that. Task 1 already ensured `Noop(...).Metrics()` is non-nil so this is safe when metrics aren't explicitly enabled.
    3. Create `internal/daemon/telemetry_metrics_test.go` mirroring the existing Phase 10 admin listener test pattern (likely in `telemetry_test.go`):
       - Construct a `Daemon` with `Observability.AdminAddr = "127.0.0.1:0"`.
       - Run `listenAdmin` in a goroutine with a cancelable ctx.
       - Wait on `adminListenerAddr.Load()` to be non-nil (existing test hook).
       - Before scraping: call `d.obs.Metrics().ToolCalls.WithLabelValues("t", "p", "m", "go", "success").Inc()` so the family is non-empty.
       - GET `http://<bound>/metrics`, assert 200 + body contains the expected metric name substrings.
       - GET `/healthz` and `/readyz` in the same test — regression guard.
       - Cancel ctx, `<-done`, assert listener shut down cleanly.
    4. Run `gofmt -w . && go vet ./internal/daemon/... && go test ./internal/daemon/...`.
  </action>
  <verify>
    <automated>go test ./internal/daemon/... -run TestAdmin_Metrics -x && go vet ./internal/daemon/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'promhttp.HandlerFor' internal/daemon/telemetry.go` succeeds
    - `grep -q '/metrics' internal/daemon/telemetry.go` succeeds
    - `grep -q 'serena_tool_calls_total' internal/daemon/telemetry_metrics_test.go` succeeds
    - `grep -q 'go_goroutines' internal/daemon/telemetry_metrics_test.go` succeeds
    - `grep -q 'process_resident_memory_bytes' internal/daemon/telemetry_metrics_test.go` succeeds
    - `go test ./internal/daemon/... -run TestAdmin_Metrics` passes
    - `go test ./internal/daemon/... -run TestAdminListener` (existing Phase 10) still passes
  </acceptance_criteria>
  <done>/metrics scrapes 200 on the admin listener with serena_* + go_* + process_* metrics; Phase 10 /healthz and /readyz regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraper → admin listener | untrusted HTTP clients crossing loopback; loopback bind enforced by Phase 10 validateAdminAddr |
| metrics registry → exposition | internal state read by promhttp during Gather |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | /metrics label values (profile, mode) | accept | Loopback-only bind; Phase 10 validateAdminAddr refuses non-loopback; v1.3 adds auth |
| T-11-02 | Denial of Service | cardinality explosion via user-controlled labels | mitigate | Bounded allowlist + TestMetricsLabelsAllowlist CI lint (D-03..D-05) |
| T-11-03 | Denial of Service | scrape-time stop-the-world from runtime.ReadMemStats | accept | Sub-ms on Go 1.25; document 5s min scrape interval in Phase 14 USAGE.md |
| T-11-04 | Information Disclosure | outcome label leaks raw error strings | mitigate | classifyOutcome is a closed 8-value enum (plan 02); no fmt.Sprintf on error paths |
| T-11-05 | Tampering | registration on prometheus.DefaultRegisterer allows cross-package pollution | mitigate | Owned prometheus.NewRegistry() in newMetrics(); DefaultRegisterer never touched |
</threat_model>

<verification>
- `go vet ./... && go test ./internal/obs/... ./internal/daemon/...` green
- Manual: `curl -s http://127.0.0.1:<bound>/metrics | grep -E '^(serena_|go_|process_)'` returns non-empty when running the test daemon
- TestMetricsLabelsAllowlist enforces D-04 at CI time (METRIC-05)
</verification>

<success_criteria>
- METRIC-01: /metrics endpoint returns 200 + Prom exposition on the admin listener
- METRIC-04: Go runtime + process collectors visible in the exposition
- METRIC-05: TestMetricsLabelsAllowlist green; negative drift test proves the lint works
- Phase 10 regression: /healthz, /readyz, pprof gating untouched
</success_criteria>

<output>
After completion, create `.planning/phases/11-metrics/11-01-SUMMARY.md` listing exact resolved prometheus/client_golang version, vector names registered, and the allowlist carve-out for serena_lspool_evictions_total.reason.
</output>
