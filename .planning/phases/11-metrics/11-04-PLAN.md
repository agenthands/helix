---
phase: 11-metrics
plan: 04
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03]
files_modified:
  - test/bench/metrics_bench_test.go
  - test/bench/baselines/v1.2-phase11-github-hosted.txt
autonomous: true
requirements: [METRIC-02]
must_haves:
  truths:
    - "BenchmarkTelemetryMiddleware runs via testing.B.Loop (Go 1.25) and produces stable ns/op + allocs/op numbers"
    - "The new benchmark's allocs/op is ≤ Phase 10 baseline + 3 (D-09 budget; A4 resolution)"
    - "A Phase 11 baseline file is committed under test/bench/baselines/ so Phase 12 can delta against it"
    - "benchgate confirms no >25% allocs regression on existing BenchmarkTools vs the Phase 10 baseline"
  artifacts:
    - path: "test/bench/metrics_bench_test.go"
      provides: "BenchmarkTelemetryMiddleware + optional Phase-10-parity baseline bench"
      contains: "b.Loop"
    - path: "test/bench/baselines/v1.2-phase11-github-hosted.txt"
      provides: "Phase 11 baseline for the next phase's delta gate"
  key_links:
    - from: "test/bench/metrics_bench_test.go"
      to: "internal/mcp/middleware.go"
      via: "constructs TelemetryMiddleware + noop MethodHandler + drives tools/call"
      pattern: "TelemetryMiddleware"
    - from: "test/bench/baselines/v1.2-phase11-github-hosted.txt"
      to: "test/bench/baselines/v1.2-phase10-github-hosted.txt"
      via: "benchstat comparison — alloc delta ≤ +3/op"
      pattern: "benchstat"
---

<objective>
Prove the Phase 11 hot-path budget holds: `TelemetryMiddleware` adds ≤ +3 allocs/op vs the Phase 10 baseline on a synthetic tools/call. Capture a Phase 11 baseline file for Phase 12 to delta against. Run the existing BenchmarkTools suite through benchgate to ensure no >25% allocs regression on the macro path.

Purpose: verifies the performance contract implicit in METRIC-02 (RESEARCH.md Claude's Discretion: "target ≤ +3 allocs/op vs Phase 10 baseline"). Resolves RESEARCH.md Assumption A4.
Output: BenchmarkTelemetryMiddleware + a new v1.2-phase11 baseline file + passing benchgate.

**Wave 4 note:** Runs after wave 3 (Plan 03) commits. Depends on Plan 02 (TelemetryMiddleware exists) and Plan 03 (lspool wiring complete so full-suite benchgate sees the production shape). Plan 01 is listed in depends_on because the benchmark constructs a real *obs.Provider via obs.Noop which requires Plan 01's Metrics() wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-metrics/11-RESEARCH.md
@test/bench/baselines/v1.2-phase10-github-hosted.txt
@test/bench/cmd/benchgate/main.go
@internal/mcp/middleware.go
@internal/obs/metrics.go

<interfaces>
<!-- From Plan 02: TelemetryMiddleware signature -->
```go
func TelemetryMiddleware(
    provider *obs.Provider,
    getSession func(ctx context.Context) *SessionInfo,
    logger *slog.Logger,
) mcpsdk.Middleware
```

<!-- benchgate CLI from Phase 9 -->
```
go run ./test/bench/cmd/benchgate \
    --baseline test/bench/baselines/v1.2-phase10-github-hosted.txt \
    --new new.txt \
    --release-tier=false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (A4 resolution): Add BenchmarkTelemetryMiddleware + capture Phase 10 parity bench if missing</name>
  <read_first>
    - test/bench/baselines/v1.2-phase10-github-hosted.txt (determine whether Phase 10 benchmarked a middleware analog)
    - test/bench/bench_helpers.go (if exists — reuse harness setup)
    - .planning/phases/11-metrics/11-RESEARCH.md (A4, Pattern 2 hot-path notes lines 270-280)
    - internal/mcp/middleware.go (from Plan 02)
  </read_first>
  <files>
    test/bench/metrics_bench_test.go
  </files>
  <action>
    1. Inspect `test/bench/baselines/v1.2-phase10-github-hosted.txt` for a middleware-analog benchmark name (e.g., `BenchmarkLoggingMiddleware`, `BenchmarkNoopMiddleware`, `BenchmarkSlogHotPath`). Record what you find:
       - **Case A:** Phase 10 baseline contains a comparable middleware benchmark → use that line as the reference and add ONLY `BenchmarkTelemetryMiddleware` in this task.
       - **Case B:** No comparable benchmark exists → A4 triggers. Add BOTH `BenchmarkBaselineMiddleware` (a minimal no-op mcpsdk.Middleware wrapper — matches the Phase 10 shape that existed before Phase 11) AND `BenchmarkTelemetryMiddleware`. The delta is computed BenchmarkTelemetryMiddleware minus BenchmarkBaselineMiddleware in the SAME run, eliminating environmental drift.
    2. Create `test/bench/metrics_bench_test.go`:
       - Package `bench_test` (matches existing bench package — verify from bench_helpers.go).
       - Construct a real `*obs.Provider` via `obs.Noop(slog.NewTextHandler(io.Discard, nil))` so Metrics() is non-nil.
       - Construct a fake `mcpsdk.MethodHandler` that returns `&mcpsdk.CallToolResult{}, nil` — the innermost no-op.
       - Construct a pre-built `*mcpsdk.CallToolRequest` with Params.Name="test_tool" — do this OUTSIDE `b.Loop` so request construction isn't measured.
       - Construct a fake `getSession` closure returning a pre-built `*mcp.SessionInfo` with Profile="claude-code", Mode="edit", Language="go".
       - Wrap the noop handler via `TelemetryMiddleware(provider, getSession, logger)`.
       - Hot loop uses `testing.B.Loop` per BENCH-01 / Go 1.25:
         ```go
         func BenchmarkTelemetryMiddleware(b *testing.B) {
             // setup ...
             b.ReportAllocs()
             for b.Loop() {
                 _, _ = wrapped(ctx, "tools/call", req)
             }
         }
         ```
       - Add `BenchmarkBaselineMiddleware` alongside it (Case B). Same structure, but wrap with a minimal pass-through middleware that only calls `next(ctx, method, req)` and nothing else. This establishes the "delta origin" in the same run.
       - Add `BenchmarkTelemetryMiddleware_ToolsList` variant: same setup but method="tools/list" — asserts the early-return fast path is near-zero (expected: identical to baseline).
    3. Run locally:
       ```bash
       go test -bench='BenchmarkTelemetryMiddleware|BenchmarkBaselineMiddleware' \
           -benchmem -count=10 -run='^$' ./test/bench/... | tee /tmp/phase11-new.txt
       ```
       Verify output shows stable allocs/op (std-dev < 10% across the 10 runs).
    4. Compute the allocs delta by hand or via `benchstat /tmp/phase11-baseline.txt /tmp/phase11-new.txt` if Case A, or directly from the single-file output comparing the two benchmark names if Case B. The delta MUST be ≤ +3 allocs/op.
    5. If the delta exceeds +3: DO NOT silently raise the budget. Profile (`go test -bench=BenchmarkTelemetryMiddleware -memprofile=mem.out ./test/bench/...` then `go tool pprof -alloc_objects mem.out`). Typical culprits per RESEARCH.md Pitfall 1: hidden fmt.Sprintf, per-call string interpolation, session snapshot copy larger than expected. Fix and re-run. If the budget cannot be met after profiling, escalate via a checkpoint — do NOT widen the budget without user sign-off.
    6. Run `gofmt -w . && go vet ./test/bench/...`.
  </action>
  <verify>
    <automated>go test -bench='BenchmarkTelemetryMiddleware|BenchmarkBaselineMiddleware' -benchmem -count=3 -run='^$' ./test/bench/... && go vet ./test/bench/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func BenchmarkTelemetryMiddleware' test/bench/metrics_bench_test.go` succeeds
    - `grep -q 'b.Loop()' test/bench/metrics_bench_test.go` succeeds (Go 1.25 idiom per BENCH-01)
    - `grep -q 'b.ReportAllocs' test/bench/metrics_bench_test.go` succeeds
    - `grep -q 'BenchmarkBaselineMiddleware\|v1.2-phase10-github-hosted' test/bench/metrics_bench_test.go` succeeds (A4 resolution — either Case A reference OR Case B companion bench)
    - Measured allocs/op delta ≤ +3 (documented in the task commit message)
    - `go test -bench=BenchmarkTelemetryMiddleware -count=3 -run='^$' -benchmem ./test/bench/...` runs clean
  </acceptance_criteria>
  <done>Hot-path alloc budget verified empirically; benchmark is reproducible; delta documented with exact numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Capture Phase 11 baseline and run benchgate against Phase 10</name>
  <read_first>
    - test/bench/cmd/benchgate/main.go (flags: --baseline, --new, --release-tier)
    - test/bench/baselines/v1.2-phase10-github-hosted.txt (existing reference)
    - .planning/phases/11-metrics/11-RESEARCH.md (Validation Architecture / Phase gate)
  </read_first>
  <files>
    test/bench/baselines/v1.2-phase11-github-hosted.txt
  </files>
  <action>
    1. Run the full benchmark suite (same set Phase 10 captured) with `-count=10` to generate a Phase 11 baseline:
       ```bash
       go test -bench=. -benchmem -count=10 -run='^$' -timeout=60m ./test/bench/... \
           | tee test/bench/baselines/v1.2-phase11-github-hosted.txt
       ```
       If this exceeds GitHub-hosted runner budget, use `-count=5` and document that in the file header comment.
    2. Prepend the new baseline file with a header comment noting:
       - Date (2026-04-08 context; use `date` at run time)
       - Git commit SHA (`git rev-parse HEAD`)
       - Go version (`go version`)
       - Runner type (GitHub-hosted)
       - Delta vs Phase 10: paste the benchstat output summary
    3. Run benchgate PR-tier (the looser gate: >15% time / >25% allocs per BENCH-05):
       ```bash
       go run ./test/bench/cmd/benchgate \
           --baseline test/bench/baselines/v1.2-phase10-github-hosted.txt \
           --new test/bench/baselines/v1.2-phase11-github-hosted.txt \
           --release-tier=false
       ```
       MUST exit 0. If it exits non-zero:
       - Inspect the offending benchmarks.
       - If the regression is on the target metrics hot path → fix in plans 01/02/03 before completing Phase 11.
       - If the regression is unrelated (environmental noise on GitHub runners), re-run with a higher `-count` for statistical confidence. Do NOT relax the gate.
    4. Commit `test/bench/baselines/v1.2-phase11-github-hosted.txt` so Phase 12 has a reference point.
    5. Document in the task commit message:
       - BenchmarkTelemetryMiddleware: ns/op, allocs/op, B/op
       - Delta vs baseline path (Case A or B from Task 1)
       - benchgate exit code
  </action>
  <verify>
    <automated>go run ./test/bench/cmd/benchgate --baseline test/bench/baselines/v1.2-phase10-github-hosted.txt --new test/bench/baselines/v1.2-phase11-github-hosted.txt --release-tier=false</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/bench/baselines/v1.2-phase11-github-hosted.txt` succeeds
    - `grep -q 'BenchmarkTelemetryMiddleware' test/bench/baselines/v1.2-phase11-github-hosted.txt` succeeds
    - `grep -qE '^# (date|commit|go version)' test/bench/baselines/v1.2-phase11-github-hosted.txt` succeeds (header metadata)
    - benchgate PR-tier returns exit 0 against Phase 10 baseline
    - Commit message documents exact allocs/op delta
  </acceptance_criteria>
  <done>Phase 11 baseline committed; benchgate green vs Phase 10; Phase 12 has a reference point for its own delta gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark harness → production code | in-process; benchmarks run against real middleware, not mocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Denial of Service | Phase 11 instrumentation silently adds hot-path allocs | mitigate | BenchmarkTelemetryMiddleware + benchgate enforce ≤ +3 allocs/op budget at phase gate |
| T-11-14 | Tampering | baseline file drift masks regressions | mitigate | Header metadata (commit SHA + Go version + runner type) makes provenance auditable |
</threat_model>

<verification>
- benchgate PR-tier exit 0 against Phase 10 baseline
- BenchmarkTelemetryMiddleware allocs/op delta ≤ +3 vs the Task 1 reference (Case A or Case B companion bench)
- Phase 11 baseline committed with metadata header
</verification>

<success_criteria>
- METRIC-02 performance contract satisfied: TelemetryMiddleware ≤ +3 allocs/op vs Phase 10
- Phase 12 has a concrete baseline to delta against
- A4 resolved: whether Phase 10 baseline needed a companion Case-B bench is documented in the summary
</success_criteria>

<output>
After completion, create `.planning/phases/11-metrics/11-04-SUMMARY.md` with:
- Exact BenchmarkTelemetryMiddleware numbers (ns/op, B/op, allocs/op)
- Delta vs reference (Case A or B)
- Any profiling findings if budget was tight
- benchgate exit code + command invocation used
</output>
