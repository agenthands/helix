# Phase 112 — Plan 01: Optimizer Model Pin + Cost Caps + SWE-bench Footgun Re-verify

**Requirements:** SCALE-01, SCALE-02, SCALE-03
**Approach:** TDD (RED → GREEN), dev-time Python only. Zero new Go deps.

## Tasks

### Task 1 — SCALE-01: optimizer DeepSeek-primary LM pin
- **RED** (`test_scale.py`): `resolve_lm` not defined →
  - `test_default_pin_is_deepseek_v4_flash`: `DEFAULT_LM_MODEL == "deepseek-v4-flash"` and NOT a deprecation-bound alias (`deepseek-chat`/`deepseek-reasoner`).
  - `test_resolve_lm_deepseek_primary`: env `{DEEPSEEK_API_KEY}` → model `deepseek/deepseek-v4-flash`, api_base `https://api.deepseek.com`, key = the deepseek key.
  - `test_resolve_lm_openai_fallback`: env `{OPENAI_API_KEY}` only (no deepseek key) → falls back to an `openai/…` model with the openai key.
  - `test_resolve_lm_none_without_key`: no keys → `None` (drives the quiet hermetic-gate exit-0).
- **GREEN**: add `DEFAULT_LM_MODEL="deepseek-v4-flash"`, `DEFAULT_FALLBACK_MODEL="gpt-4.1-mini"`, `DEEPSEEK_BASE_URL`, and `resolve_lm(environ=None) -> LMConfig|None` (DeepSeek-primary/OpenAI-fallback, `num_retries=4`). Rewire `main()`: `lm = resolve_lm(); if lm is None: <existing quiet msg>; return 0`; build `dspy.LM(model=lm.model, api_key=lm.api_key, api_base=lm.api_base, num_retries=lm.num_retries)` for BOTH program LM (`dspy.configure`) and `reflection_lm`.

### Task 2 — SCALE-02: cost caps (rollout / concurrency / 429 / turns)
- **RED** (`test_scale.py`):
  - `test_gepa_kwargs_default_auto_light`: `build_gepa_kwargs({})` → `auto=="light"`, `num_threads<=8`, `track_stats` True, deterministic `seed` set, NO `max_metric_calls`.
  - `test_gepa_kwargs_explicit_max_metric_calls`: env `GEPA_MAX_METRIC_CALLS=200` → `max_metric_calls==200` and NO `auto` (explicit hard cap wins).
  - `test_lm_num_retries_for_429_backoff`: `resolve_lm({DEEPSEEK_API_KEY}).num_retries >= 3`.
  - `test_agent_turn_cap_env_override`: `ReActAgent(..., )` honors `AGENT_MAX_TURNS` (set 2 → loop terminates ≤2 turns under a tool-call-forever fake LLM) and defaults to 12.
- **GREEN**: add `build_gepa_kwargs(environ=None)` to `optimize.py`; wire `GEPA(metric=..., reflection_lm=..., **build_gepa_kwargs())`. In `agent/react.py`, read `AGENT_MAX_TURNS` (int, default `_DEFAULT_MAX_TURNS`) in `ReActAgent.__init__` (keep explicit-arg precedence).

### Task 3 — SCALE-03: SWE-bench footgun re-verify (strengthen)
- **RED** (`test_swebench.py`): `test_ignores_harness_resolved_flag_on_zero_tests` — a report `{"resolved": True, "tests_status": {empty buckets}}` must STILL raise `GradeError` (we never trust the harness's vacuous `resolved`).
- **GREEN**: already satisfied by `grade_report` (recompute-from-status + 0-tests refusal); the test documents+locks the research footgun. Add a 1-line comment in `grade_swebench.py` citing the harness `compute_fail_to_pass`→1.0-on-`total==0` behavior.

## Success Criteria (phase-level)
1. SCALE-01: optimizer program LM + reflection_lm default to `deepseek-v4-flash` (DeepSeek-primary/OpenAI-fallback), config-var, no deprecated alias.
2. SCALE-02: agent turn cap + GEPA num_threads + LM num_retries + explicit `max_metric_calls` option all wired and asserted by tests (not silently unbounded).
3. SCALE-03: harness 0-tests/`resolved` footgun refused with a break-the-invariant → assert-RED test.
4. Boundary: `git diff go.mod` empty; `make vet` green; hermetic suite key-free/network-free.

## Verification
- `uv run pytest test_scale.py test_swebench.py test_corpus.py test_split.py -q` (key-free).
- `git diff --quiet go.mod go.sum`; `make vet`.
