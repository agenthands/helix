# Phase 114 — Plan 01: Ship/No-Ship REPORT + Boundary Re-Verification

**Requirements:** REPORT-01, ADOPT-05
**Approach:** REPORT + re-verification only. No new production code.

## Tasks
### Task 1 — REPORT-01: fill the real ship/no-ship REPORT
- Prepend a v2.4 section to `tools/dspy-tune/REPORT.md`: ON/OFF/Δ, val_size, per-arm cost, SWE-bench agreement; the three integration gaps fixed; honest caveats (thin margin, non-evolving GEPA, ON=existing SKILL.md, K=2); recommendation = do NOT adopt on this margin; REPORT-only.

### Task 2 — ADOPT-05: re-verify the boundary + adoption gate
- `git log -1 -- go.mod go.sum` == the v2.0 commit (zero new Go deps); working tree clean.
- no `helix` subcommand shells to Python; `grep -cE 'skills/helix|reference\.md' optimize.py == 0`.
- `make vet` (incl. `toolsquarantine`) green; `go test ./internal/cli` green.
- Adoption gate live (break-the-invariant): desync `reference.md` → `helix-refgen --check` exit 1; restore → exit 0, reference.md clean.

## Success Criteria
1. REPORT-01: REPORT.md records the real verdict + caveats; no SKILL.md adoption committed.
2. ADOPT-05: boundary re-verified (go.mod untouched, no runtime-Python edge, make vet green) + adoption gate proven live (desync → exit 1).

## Verification
- Inspect REPORT.md v2.4 section; the boundary commands above (all green); the desync proof.
