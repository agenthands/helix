---
context: phase
phase: 115-task-solving-prompt-feedback-loop
task: 1
total_tasks: 7
status: not_started
last_updated: 2026-06-26T08:38:01.724Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Anti-vacuity tests MUST ship with break-the-invariant verification — every test that adds a constraint must prove the constraint is enforced (HARNESS-01d, HARNESS-02d)

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Prose-only answer acceptance | Agent returns text without tool calls and declares done — v2.4 had ~40% of tasks fail this way | blocking | Anti-vacuity test (test_prose_only_answer_fails) MUST assert FAIL when agent accepts prose without tools |
| Done-on-broken code | Agent declares done without running tests — v2.4 showed agent quit after editing without verification | blocking | Anti-vacuity test (test_done_on_broken_without_tests_fails) MUST assert FAIL when agent declares done on untested code |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 115 (Task-Solving Prompt + Feedback Loop) is about to begin implementation. CONTEXT.md has all locked decisions from /gsd-discuss-phase. Plan 115-01 has 3 tasks (HARNESS-01 requirements). Plan 115-02 has 4 tasks (HARNESS-02 requirements). Session paused before any implementation started — working tree is clean except for STATE.md (milestone start marker).
</current_state>

<completed_work>

Completed Tasks:
- Milestone v2.5 created (4 phases: 115-118)
- Phase 115 CONTEXT.md locked with 5 key decisions
- Phase 115 PLAN.md created with 2 plans, 7 tasks total
- All requirements mapped: HARNESS-01a/b/c/d (Plan 01), HARNESS-02a/b/c/d (Plan 02)
</completed_work>

<remaining_work>

Plan 115-01 (Task-Solving Prompt):
- Task 1: Anti-vacuity test for prose refusal (HARNESS-01d) — NOT STARTED
- Task 2: Task-solving system prompt (HARNESS-01a/b/c) — NOT STARTED
- Task 3: Prose retry-nudge mechanism (D-04) — NOT STARTED

Plan 115-02 (Feedback Loop):
- Task 4: Add run-tests verb to agent toolkit (HARNESS-02a) — NOT STARTED
- Task 5: Wire get-diagnostics into ReAct loop (HARNESS-02b) — NOT STARTED
- Task 6: Agent uses test results to iterate (HARNESS-02c) — NOT STARTED
- Task 7: Anti-vacuity test for done-on-broken (HARNESS-02d) — NOT STARTED
</remaining_work>

<decisions_made>

From CONTEXT.md (all locked):

1. **D-01: Solution File Conveyance** — Both paths: explicit naming when available, extraction fallback for implied files
2. **D-02: Success Criterion Declaration** — Prompt declares "hidden tests pass", agent discovers via get-diagnostics
3. **D-03: Feedback Loop Integration** — Hybrid: agent-driven by default, prompt instructs, anti-vacuity test enforces
4. **D-04: Prose Enforcement** — Retry nudge first, then fail task if agent persists
5. **D-05: Test Discovery** — Both verbs: get-diagnostics to find, run-tests to execute
</decisions_made>

<blockers>
None. Ready to begin implementation.
</blockers>

## Required Reading (in order)
1. `.planning/phases/115-task-solving-prompt-feedback-loop/115-CONTEXT.md` — locked decisions from discuss-phase
2. `.planning/phases/115-task-solving-prompt-feedback-loop/115-PLAN.md` — execution plan with TDD tasks
3. `tools/dspy-tune/agent/react.py` — current ReAct loop implementation
4. `tools/dspy-tune/agent/tools.py` — verb interface and TOOL_SCHEMAS
5. `tools/dspy-tune/test_agent.py` — existing test patterns to follow
6. `.planning/METHODOLOGY.md` — if it exists — project analytical lenses

## Critical Anti-Patterns (do NOT repeat these)
- **ANTI-PATTERN:** Accepting prose answers without tool calls → implement test_prose_only_answer_fails first
- **ANTI-PATTERN:** Declaring done without running tests → implement test_done_on_broken_without_tests_fails
- **ANTI-PATTERN:** Implementing without TDD → every task has `type: tdd`; write test first

## Infrastructure State
- Python environment: `uv` required (not bare python/pip) — CLAUDE.md constraint
- Test command: `cd tools/dspy-tune && uv run pytest test_agent.py -v`
- Container engine: Podman (NOT Docker) — irrelevant for this phase (no containers needed)
- Git status: clean except .planning/STATE.md

## Pre-Execution Critique Required
<!-- Fill in ONLY if pausing between design and execution (e.g. spike design done, not yet run) -->
N/A — design is complete (PLAN.md exists), ready for execution.

<context>
Phase 115 fixes the core harness so the agent uses tools instead of answering in prose, runs tests to verify work, and iterates based on results. This is the foundation for all future tuning. The v2.4 root cause investigation showed ~40% of tasks had 0 tool calls — the agent answered in prose and quit. Plan 115-01 adds the task-solving prompt that forces editing. Plan 115-02 wires the feedback loop (run-tests, get-diagnostics). Both plans have anti-vacuity tests to prove the mechanisms work.

All decisions locked in CONTEXT.md. Plan 115-01 ready to execute. TDD mode enabled — write test first, then implement.
</context>

<next_action>
Start with:
1. Read `tools/dspy-tune/agent/react.py` (current ReAct loop)
2. Read `tools/dspy-tune/test_agent.py` (existing test patterns)
3. Implement Task 1: `test_prose_only_answer_fails` in test_agent.py
4. Run test (should FAIL initially — TDD)
5. Then implement the prose detection + retry-nudge mechanism in react.py
6. Re-run test (should PASS)
</next_action>