---
phase: 14-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [DOC-10]
must_haves:
  truths:
    - "CHANGELOG.md documents v1.0, v1.1, and v1.2 releases with dates"
    - "Each version entry summarizes what was shipped using milestone/roadmap data"
    - "No legacy Python changelog entries remain"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Go-only changelog with v1.0, v1.1, v1.2 entries"
      contains: "## v1.0"
  key_links: []
---

<objective>
Replace the legacy Python CHANGELOG.md with a fresh Go-only changelog covering v1.0, v1.1, and v1.2.

Purpose: Give users a clean release history for the Go rewrite (DOC-10). Per D-05, start fresh -- no Python history. Per D-06, use v1.0/v1.1/v1.2 format matching milestone naming.

Output: `CHANGELOG.md` at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-documentation/14-CONTEXT.md
@.planning/phases/14-documentation/14-RESEARCH.md

@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
@.planning/milestones/v1.0-ROADMAP.md
@.planning/milestones/v1.1-ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CHANGELOG.md with v1.0, v1.1, v1.2 entries</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (current legacy content to replace)
    - .planning/ROADMAP.md (phase goals for all milestones, completion dates)
    - .planning/milestones/v1.0-ROADMAP.md (v1.0 phase details)
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md (v1.0 summary and gap info)
    - .planning/milestones/v1.1-ROADMAP.md (v1.1 phase details)
    - .planning/milestones/v1.1-MILESTONE-AUDIT.md (v1.1 summary)
    - .planning/REQUIREMENTS.md (full requirement list with completion status)
  </read_first>
  <action>
    Replace the entire CHANGELOG.md with a fresh Go-only changelog. Per D-05, no Python history. Per D-06, use `v1.0` / `v1.1` / `v1.2` format.

    **Structure:**

    ```markdown
    # Changelog

    All notable changes to Serena (Go) are documented here.

    ## v1.2 — Performance & Production Hardening (2026-04-10)

    ### Benchmark Harness
    - Benchmark suite in `test/bench/` using `testing.B.Loop` for all MCP tools
    - Benchstat CI regression gate with tiered thresholds (PR: >15% time / >25% allocs; release: >10% / >20%)
    - Committed v1.1 baselines for p50/p95/p99 tool latency, LSP indexing, and memory profiles

    ### Observability
    - `internal/obs/` package with noop-default provider (zero-cost when disabled)
    - Trace-aware slog handler: log records carry `trace_id` and `span_id` from context
    - Dedicated loopback admin listener with `/healthz`, `/readyz`, and gated `/debug/pprof/*`
    - Prometheus `/metrics` endpoint with RED histograms per tool and lspool gauges
    - Bounded-label contract enforced by CI lint (allowlist: tool_name, profile, mode, language, outcome)

    ### Tracing
    - End-to-end trace propagation: forwarder -> daemon -> kernel -> LS via `otelgrpc`
    - Telemetry middleware with per-tool sub-spans
    - Optional OTLP/gRPC exporter behind config flag
    - Default sampler `ParentBased(TraceIDRatioBased(0.0))` — off by default

    ### Graceful Degradation
    - Per-class timeout budgets (read 5s / search 15s / edit 10s / index 120s / diagnostics 20s)
    - Deadline propagation from forwarder through daemon and kernel to language server
    - Typed `lspool.ErrCircuitOpen` with structured error envelope
    - Circuit breaker tuning: decorrelated jitter backoff, single-probe half-open
    - LS crash recovery with configurable restart budget
    - `runtime/debug.SetMemoryLimit` wired from config
    - Graceful shutdown: SIGTERM drains in-flight calls, flushes telemetry within 5s

    ### Documentation
    - Auto-generated tool and language tables in README.md via `cmd/docgen`
    - USAGE.md: profiles, modes, tutorials, troubleshooting, observability quickstart, performance tuning
    - This changelog

    ## v1.1 — Integration Testing (2026-04-09)

    ### Test Harness & Dogfooding
    - MCP round-trip integration harness (`test/integration/`)
    - All 38 tools exercised via stdio transport against live Go fixture
    - Language field gap closure for multi-language routing

    ### Symbol Editing
    - Edit round-trip integration tests (replace_symbol_body, insert_before/after, rename, safe_delete)
    - Python, TypeScript, Java, and Rust multi-language fixtures

    ### Advanced Testing
    - Profile/mode golden contract tests (5 profiles x 4 modes)
    - Three-tier concurrency coverage: t.Parallel(), errgroup fan-out, testing/synctest
    - Three-band error classification tests

    ## v1.0 — MVP (2026-04-08)

    Initial release of Serena as a Go-native code intelligence platform for MCP.

    ### Core
    - Single Go binary with persistent daemon, gRPC forwarder, and stdio/HTTP transports
    - MCP server with official Go SDK, tool registry, and profile filtering middleware
    - Signal-first lifecycle with errgroup orchestration

    ### Code Intelligence
    - LSP 3.17 protocol support with generated types (324 structs, 216 union types)
    - Worker pool with share-until-dirty semantics, adaptive TTL, circuit breaking, and memory pressure eviction
    - 9 symbol retrieval tools (definition, references, hover, implementations, call/type hierarchy, blast radius)
    - 6 symbol editing tools with tree-sitter body surgery and post-edit diagnostic verification
    - 6 file operation tools (read, write, list, find, search, replace)
    - 3 diagnostic tools (diagnostics, code actions, formatting)

    ### Skills & Multi-Language
    - 52-language embedded registry with YAML override and three-tier LS installer
    - 7 memory tools with markdown storage and SQLite FTS5 search
    - Workflow tools (onboarding, session handoff)
    - Caddy-style skill plugin system with init() registration

    ### Agent Profiles
    - 5 pre-built profiles: claude-code, codex, ide-assistant, ci-bot, full
    - 4 operational modes: read, edit, review, admin
    - 4-layer config precedence: CLI > project > user > profile defaults
    - Token budget awareness for context optimization
    ```

    **Important:** Read the milestone audit files and roadmap to get accurate details. The above is a template -- the executor must cross-reference the actual milestone data to ensure accuracy. Use dates from the ROADMAP.md Progress table (v1.0: 2026-04-08, v1.1: 2026-04-09, v1.2: use the completion date of Phase 14 which is the current date).

    The v1.2 section should be written as if Phase 14 (Documentation) is the final phase, noting the documentation additions. The benchmark gate hardening (Phase 15) is not part of v1.2's changelog since it's a gap closure phase.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && grep -q '## v1.0' CHANGELOG.md && grep -q '## v1.1' CHANGELOG.md && grep -q '## v1.2' CHANGELOG.md && ! grep -q '0.1.3\|0.1.4\|pypi\|asyncio\|multilspy\|JetBrains' CHANGELOG.md && echo "CHANGELOG.md valid"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md exists at repo root
    - CHANGELOG.md contains `## v1.0`
    - CHANGELOG.md contains `## v1.1`
    - CHANGELOG.md contains `## v1.2`
    - CHANGELOG.md contains `2026-04-08` (v1.0 date)
    - CHANGELOG.md contains `2026-04-09` (v1.1 date)
    - CHANGELOG.md does NOT contain `0.1.3` (no legacy Python versions)
    - CHANGELOG.md does NOT contain `0.1.4` (no legacy Python versions)
    - CHANGELOG.md does NOT contain `asyncio` (no Python implementation details)
    - CHANGELOG.md does NOT contain `JetBrains` (no legacy backend references)
    - CHANGELOG.md contains `38` or `tool` (documents tool count)
    - CHANGELOG.md contains `52` or `language` (documents language count)
  </acceptance_criteria>
  <done>CHANGELOG.md has clean Go-only entries for v1.0, v1.1, and v1.2 with dates and accurate summaries derived from milestone data</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure documentation file. No executable code, no user input processing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | T (Tampering) | CHANGELOG.md replaces existing file | accept | File is under git version control. Legacy content preserved in git history per D-05. |
</threat_model>

<verification>
- CHANGELOG.md contains v1.0, v1.1, v1.2 sections
- No legacy Python history remains
- Dates match milestone completion dates from ROADMAP.md
</verification>

<success_criteria>
- CHANGELOG.md satisfies DOC-10 with three version entries
- Version format matches D-06 (v1.0 / v1.1 / v1.2)
- Legacy Python history is gone per D-05
</success_criteria>

<output>
After completion, create `.planning/phases/14-documentation/14-03-SUMMARY.md`
</output>
