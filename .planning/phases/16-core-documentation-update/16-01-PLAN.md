---
phase: 16-core-documentation-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - README-01
  - README-02
  - README-03

must_haves:
  truths:
    - "README contains a 'Production & Observability' section describing metrics, tracing, admin listener, and graceful degradation"
    - "README reflects 38+ tools, 4-layer architecture, and all v1.2 capabilities"
    - "README install instructions show go install, build from source, and client config examples that match current CLI flags"
    - "Tool and language tables are regenerated from code and match current registry"
  artifacts:
    - path: "README.md"
      provides: "Updated project README with v1.2 capabilities"
      contains: "Production & Observability"
  key_links:
    - from: "README.md"
      to: "cmd/docgen"
      via: "make docs regenerates tool/language tables"
      pattern: "BEGIN TOOLS|BEGIN LANGUAGES"
---

<objective>
Update README.md to accurately reflect Serena's current capabilities after v1.2, including a new Production & Observability section and regenerated auto-generated tables.

Purpose: README is the project's front door. It currently lacks v1.2 production features (observability, metrics, tracing, graceful degradation) and needs table regeneration to stay accurate.
Output: Updated README.md with new section, verified install instructions, and fresh auto-generated tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-core-documentation-update/16-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Production & Observability section and update README content</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — 240 lines, need to see existing structure)
    - .planning/phases/16-core-documentation-update/16-CONTEXT.md (decisions D-01, D-02, D-03)
    - .planning/codebase/ARCHITECTURE.md (4-layer architecture details for accuracy)
    - CHANGELOG.md (v1.2 feature list for cross-referencing capabilities)
  </read_first>
  <action>
Edit README.md to add a new section and update existing content:

1. **Add "Production & Observability" section** (per D-01, D-02) — insert it AFTER the "Key Advantages Over File-Based Approaches" table and BEFORE "Programming Language Support". This section should contain:

   **Subsection: Metrics & Monitoring**
   - Prometheus `/metrics` endpoint with RED histograms (rate, errors, duration) per tool
   - lspool health gauges (active workers, evictions, restarts, circuit state)
   - Bounded-label contract (allowlist: tool_name, profile, mode, language, outcome)

   **Subsection: Distributed Tracing**
   - End-to-end trace propagation: forwarder -> daemon -> kernel -> language server via `otelgrpc`
   - Per-tool sub-spans in telemetry middleware
   - Optional OTLP/gRPC exporter — off by default (`ParentBased(TraceIDRatioBased(0.0))`)

   **Subsection: Admin Endpoints** (per D-03 — brief feature highlights, detailed config in USAGE.md)
   - `/healthz` — daemon liveness
   - `/readyz` — readiness (language servers initialized)
   - `/metrics` — Prometheus scrape target
   - `/debug/pprof/*` — gated behind admin profile scope
   - Mention: "Dedicated loopback admin listener, isolated from MCP traffic"

   **Subsection: Graceful Degradation**
   - Per-class timeout budgets (read 5s, search 15s, edit 10s, index 120s, diagnostics 20s)
   - Deadline propagation from forwarder through daemon/kernel to language server
   - Circuit breaker with decorrelated jitter backoff, single-probe half-open
   - LS crash recovery with configurable restart budget
   - `GOMEMLIMIT` support for memory-constrained environments
   - Clean shutdown: SIGTERM drains in-flight calls, flushes telemetry within 5s

2. **Update the Architecture section** to mention observability as part of Layer 0:
   Add after the existing architecture ASCII diagram: "The **admin listener** exposes health checks (`/healthz`, `/readyz`), Prometheus metrics, and gated pprof on a dedicated loopback port."

3. **Verify the "How Serena Works" intro** still says "38+ MCP tools" — confirm this is accurate (it is, per tool table). No change needed if already correct.

4. **Do NOT add a v1.3 stub or changelog reference** (per D-07).
  </action>
  <verify>
    <automated>grep -c "Production & Observability" README.md && grep -c "/healthz" README.md && grep -c "GOMEMLIMIT" README.md && grep -c "OTLP" README.md && grep -c "Graceful Degradation" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains heading "## Production & Observability" or "## Production &amp; Observability"
    - README.md contains "/healthz" and "/readyz" and "/metrics" and "/debug/pprof"
    - README.md contains "OTLP" in the tracing subsection
    - README.md contains "GOMEMLIMIT" in the degradation subsection
    - README.md contains "Graceful Degradation" as a subsection heading
    - README.md contains "per-class timeout budgets" or equivalent phrasing describing the 5 timeout classes
    - The "Production & Observability" section appears AFTER "Key Advantages" and BEFORE "Programming Language Support"
    - README.md still contains "38+ MCP tools"
  </acceptance_criteria>
  <done>README has a complete Production & Observability section covering metrics, tracing, admin endpoints, and graceful degradation. Architecture section mentions admin listener. All v1.2 capabilities represented.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate auto-generated tables and verify install instructions</name>
  <files>README.md</files>
  <read_first>
    - README.md (after Task 1 modifications — verify tool/language table markers exist)
    - cmd/docgen/main.go (understand what the docgen tool does)
    - Makefile (confirm `make docs` target runs `go run ./cmd/docgen`)
  </read_first>
  <action>
1. **Regenerate tool and language tables** (per D-08):
   Run `make docs` (which executes `go run ./cmd/docgen`) to regenerate the `<!-- BEGIN TOOLS -->...<!-- END TOOLS -->` and `<!-- BEGIN LANGUAGES -->...<!-- END LANGUAGES -->` sections from the current registry.

2. **Verify install instructions are current and correct** (README-03):
   - Confirm `go install github.com/postfix/serena/cmd/serena@latest` is the correct import path by checking `go.mod` module name
   - Confirm the build-from-source commands (`git clone`, `cd serena`, `go build ./cmd/serena`) work by running `go build ./cmd/serena`
   - Confirm the Claude Code config example uses `"command": "serena"` and `"args": ["--mode=stdio"]` — verify these flags exist by running `./serena --help` or checking cmd/serena
   - Confirm the Codex config example uses `--profile=codex` — verify this profile exists
   - Confirm the HTTP mode example uses `--serve --http-addr=:9091` flags

3. **Run `go vet ./...`** to ensure nothing is broken.

4. **Verify the final README** by checking tool count in the generated table matches "38+ MCP tools" claim.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && make docs && go build ./cmd/serena && go vet ./... && grep -c "BEGIN TOOLS" README.md && grep -c "BEGIN LANGUAGES" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `make docs` exits 0 (tables regenerated successfully)
    - `go build ./cmd/serena` exits 0 (install instructions verified)
    - `go vet ./...` exits 0
    - README.md contains `<!-- BEGIN TOOLS -->` and `<!-- END TOOLS -->` markers
    - README.md contains `<!-- BEGIN LANGUAGES -->` and `<!-- END LANGUAGES -->` markers
    - The tool table contains at least 38 rows (matching "38+ MCP tools" claim)
  </acceptance_criteria>
  <done>Auto-generated tables are fresh from current code. Install instructions verified to work. Tool count matches README claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan modifies documentation files only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | I (Info Disclosure) | README.md | accept | README is public documentation; no secrets or internal-only info included |
</threat_model>

<verification>
- README.md contains "Production & Observability" section with metrics, tracing, admin endpoints, graceful degradation
- Auto-generated tool and language tables are current (regenerated via `make docs`)
- Install instructions verified by successful `go build ./cmd/serena`
- `go vet ./...` passes
</verification>

<success_criteria>
README accurately describes Serena's 4-layer architecture, 38+ tools, 52 languages, and all v1.2 production features (observability, metrics, tracing, graceful degradation). Install instructions work end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/16-core-documentation-update/16-01-SUMMARY.md`
</output>
