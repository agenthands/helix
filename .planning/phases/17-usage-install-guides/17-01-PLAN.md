---
phase: 17-usage-install-guides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - USAGE.md
autonomous: true
requirements:
  - USAGE-01
  - USAGE-02
  - USAGE-03

must_haves:
  truths:
    - "USAGE.md Observability Settings table includes all config keys from internal/config/config.go ObservabilityConfig struct"
    - "USAGE.md Prometheus Metrics table lists all 6 metrics from internal/obs/metrics.go"
    - "USAGE.md contains a benchmark subsection under Observability Quickstart with local benchmark commands"
    - "USAGE.md degradation settings match internal/config/config.go DegradationConfig struct"
  artifacts:
    - path: "USAGE.md"
      provides: "Complete observability, degradation, and benchmark documentation"
      contains: "### Benchmarks"
  key_links:
    - from: "USAGE.md observability config table"
      to: "internal/config/config.go ObservabilityConfig"
      via: "config key names"
      pattern: "observability\\.service_name"
    - from: "USAGE.md metrics table"
      to: "internal/obs/metrics.go"
      via: "metric names"
      pattern: "serena_tool_calls_total"
---

<objective>
Update USAGE.md for accuracy against v1.2 code and add benchmark workflow subsection.

Purpose: USAGE.md has verified gaps (missing config key, missing metrics, missing label details) and lacks benchmark documentation. This plan fixes all accuracy issues and adds the benchmark subsection under Observability Quickstart per D-07.

Output: Updated USAGE.md with accurate config keys, complete metrics table, and new benchmark subsection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-usage-install-guides/17-CONTEXT.md
@.planning/phases/17-usage-install-guides/17-RESEARCH.md
@USAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix USAGE.md accuracy gaps in observability and metrics sections</name>
  <files>USAGE.md</files>
  <read_first>
    USAGE.md
    internal/config/config.go
    internal/obs/metrics.go
    .planning/phases/17-usage-install-guides/17-RESEARCH.md
  </read_first>
  <action>
Fix the 4 verified accuracy gaps in USAGE.md. Read the source files first to verify each gap still exists and get exact values.

**Gap 1 -- Add missing `service_name` config key (per D-08, D-09):**
In the "Observability Settings" table (around line 263-270), add a new row:
| `observability.service_name` | string | `serena` | Service name used in tracing span attributes |

Also add `service_name: "my-service"` to the example YAML config block under the observability section (around line 284+) where `admin_addr`, `enable_pprof`, `tracing_endpoint`, and `tracing_sample_ratio` are shown.

**Gap 2 -- Add 2 missing metrics to Prometheus table (per D-08):**
In the "Prometheus Metrics" table (around line 511-516), add these 2 rows after the existing 4 metrics:
| `serena_tool_calls_total` | counter | Total MCP tool calls by outcome (labels: `tool_name`, `profile`, `mode`, `language`, `outcome`) |
| `serena_lspool_restarts_total` | counter | Language server worker restarts per language (labels: `language`) |

**Gap 3 -- Add metric label documentation (per D-08):**
After the Prometheus Metrics table (after the scrape config example, before "### Enable Tracing"), add a brief paragraph:

```
**Metric labels:** The `serena_tool_duration_seconds` and `serena_tool_calls_total` metrics share five label dimensions: `tool_name`, `profile`, `mode`, `language`, and `outcome`. Use these for targeted queries:

\`\`\`promql
# Error rate per tool (last 5 minutes)
rate(serena_tool_calls_total{outcome="error"}[5m])

# p95 tool latency
histogram_quantile(0.95, rate(serena_tool_duration_seconds_bucket[5m]))

# Active workers by language
serena_lspool_workers

# Eviction rate by reason
rate(serena_lspool_evictions_total[5m])
\`\`\`
```

**Gap 4 -- Add `reason` label to evictions metric (per D-08):**
In the Prometheus Metrics table, update the description for `serena_lspool_evictions_total` from:
"Total worker evictions due to memory pressure or TTL"
to:
"Total worker evictions (labels: `language`, `reason`). Reason values: `idle`, `pressure`, `crash`, `shutdown`"

**Verification step:** After making changes, read `internal/config/config.go` and `internal/obs/metrics.go` to confirm every documented config key and metric name matches the actual code. Do NOT add any config keys or metrics that do not exist in the source code.
  </action>
  <verify>
    <automated>grep -c "service_name" USAGE.md | grep -q "[1-9]" && grep -c "serena_tool_calls_total" USAGE.md | grep -q "[1-9]" && grep -c "serena_lspool_restarts_total" USAGE.md | grep -q "[1-9]" && grep -c "idle.*pressure.*crash.*shutdown" USAGE.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - USAGE.md Observability Settings table contains row with `observability.service_name`
    - USAGE.md Prometheus Metrics table contains 6 metrics (was 4): `serena_tool_duration_seconds`, `serena_lspool_workers`, `serena_lspool_evictions_total`, `serena_lspool_circuit_state`, `serena_tool_calls_total`, `serena_lspool_restarts_total`
    - USAGE.md contains `reason` label documentation with enum values `idle`, `pressure`, `crash`, `shutdown` for evictions metric
    - USAGE.md contains PromQL query examples after the metrics table
    - USAGE.md example YAML config includes `service_name` key
    - `go vet ./...` passes (no code changes, but validates no doc references break anything)
  </acceptance_criteria>
  <done>All 4 accuracy gaps fixed: service_name config key added, 2 missing metrics added, label dimensions documented, eviction reason label documented. Every value verified against source code.</done>
</task>

<task type="auto">
  <name>Task 2: Add benchmark subsection under Observability Quickstart</name>
  <files>USAGE.md</files>
  <read_first>
    USAGE.md
    test/bench/baselines/README.md
    CONTRIBUTING.md
    .planning/phases/17-usage-install-guides/17-RESEARCH.md
  </read_first>
  <action>
Add a new "### Benchmarks" subsection at the end of the "## Observability Quickstart" section, after the "### Enable pprof" subsection (after line ~563) and before "## Performance Tuning" (line ~565). Per D-07, this groups benchmark content with monitoring-adjacent observability content.

The subsection content (per D-05 and D-06 -- local benchmarks and interpretation only, NO CI gate details):

```markdown
### Benchmarks

Serena ships a benchmark suite in `test/bench/` for measuring tool response times, LSP indexing throughput, observability overhead, and memory usage. Benchmarks require `gopls` installed and use `testing.B.Loop` (Go 1.24+).

**Run all benchmarks:**

\`\`\`bash
go test -bench=. ./test/bench/ -timeout 300s
\`\`\`

**Run a specific suite:**

\`\`\`bash
go test -bench=BenchmarkTools ./test/bench/ -timeout 300s
\`\`\`

**With memory allocation stats:**

\`\`\`bash
go test -bench=. -benchmem ./test/bench/ -timeout 300s
\`\`\`

**Comparing runs with benchstat:**

Run benchmarks multiple times for statistical significance, then compare:

\`\`\`bash
# Install benchstat
go install golang.org/x/perf/cmd/benchstat@latest

# Capture before and after (5+ runs recommended)
go test -bench=. -benchmem -count=5 ./test/bench/ -timeout 300s > bench-before.txt
# ... make changes ...
go test -bench=. -benchmem -count=5 ./test/bench/ -timeout 300s > bench-after.txt

# Compare
benchstat bench-before.txt bench-after.txt
\`\`\`

benchstat reports per-benchmark deltas with confidence intervals. A `~` result means no statistically significant change; `+` or `-` indicates a measurable regression or improvement.

**Tips:**

- Use `-short` to skip `BenchmarkFullRepoSmoke` (the longest-running suite)
- Use `GOMAXPROCS=4` to reduce noise on machines with many cores
- For CI benchmark gate details, see [CONTRIBUTING.md](CONTRIBUTING.md)
```

Do NOT include any of the following (per D-05): benchgate tool details, CI thresholds, baseline capture procedures, PR gate mechanics. These belong in CONTRIBUTING.md.

Per D-10, do not restructure any other sections of USAGE.md.
  </action>
  <verify>
    <automated>grep -n "### Benchmarks" USAGE.md | grep -q "." && grep -c "benchstat" USAGE.md | grep -q "[1-9]" && grep -c "BenchmarkTools" USAGE.md | grep -q "[1-9]" && ! grep -q "benchgate" USAGE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - USAGE.md contains a `### Benchmarks` heading within the Observability Quickstart section (between Enable pprof and Performance Tuning)
    - USAGE.md contains `go test -bench=. ./test/bench/ -timeout 300s` command
    - USAGE.md contains `go test -bench=BenchmarkTools` command
    - USAGE.md contains `benchstat bench-before.txt bench-after.txt` command
    - USAGE.md contains `go install golang.org/x/perf/cmd/benchstat@latest`
    - USAGE.md contains `-count=5` for statistical significance
    - USAGE.md does NOT contain `benchgate` (CI detail, per D-05)
    - USAGE.md contains link to CONTRIBUTING.md for CI details
    - `### Benchmarks` appears after `### Enable pprof` and before `## Performance Tuning`
  </acceptance_criteria>
  <done>Benchmark subsection added under Observability Quickstart with local run commands, benchstat workflow, interpretation guidance, and tips. No CI gate content. Links to CONTRIBUTING.md for CI details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- documentation-only phase. No code changes, no new endpoints, no input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | I (Information Disclosure) | USAGE.md config examples | accept | Config examples use localhost addresses and default values only; no secrets documented |
</threat_model>

<verification>
- All config keys in USAGE.md verified against `internal/config/config.go` koanf struct tags
- All metric names in USAGE.md verified against `internal/obs/metrics.go` newMetrics()
- Benchmark commands verified against `test/bench/baselines/README.md`
- No CI gate content in benchmark section (D-05 compliance)
- `go vet ./...` and `go test -short ./...` pass (no code changes should break anything)
</verification>

<success_criteria>
- USAGE.md Observability Settings table has 5 config keys (was 4, added service_name)
- USAGE.md Prometheus Metrics table has 6 metrics (was 4, added tool_calls_total and restarts_total)
- USAGE.md has metric label documentation with PromQL examples
- USAGE.md has Benchmarks subsection under Observability Quickstart
- Benchmark subsection covers local runs and benchstat only (no CI gate content)
</success_criteria>

<output>
After completion, create `.planning/phases/17-usage-install-guides/17-01-SUMMARY.md`
</output>
