---
phase: 17-usage-install-guides
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - INSTALL.md
  - llms-install.md
autonomous: true
requirements:
  - INST-01
  - INST-02

must_haves:
  truths:
    - "INSTALL.md exists and llms-install.md is deleted"
    - "INSTALL.md has shared prerequisites section with go install command"
    - "INSTALL.md has per-agent setup sections for all 6 agents: Claude Code, Codex, OpenCode, Cursor, Gemini CLI, Antigravity"
    - "INSTALL.md OpenCode config uses 'mcp' key with array command format, not 'mcpServers'"
    - "INSTALL.md has verification steps to confirm a working installation"
  artifacts:
    - path: "INSTALL.md"
      provides: "Multi-agent install guide for Go binary"
      contains: "go install github.com/postfix/serena"
  key_links:
    - from: "INSTALL.md"
      to: "USAGE.md"
      via: "cross-reference link"
      pattern: "USAGE.md"
    - from: "INSTALL.md"
      to: "README.md"
      via: "cross-reference link"
      pattern: "README.md"
---

<objective>
Replace stale Python-based llms-install.md with new INSTALL.md covering Go binary setup for six coding agents.

Purpose: The current llms-install.md is 29 lines of Python/uv instructions that are entirely stale for the Go binary. Per D-01 through D-04, this is a complete rewrite to INSTALL.md with shared prerequisites and per-agent MCP config sections.

Output: New INSTALL.md file, llms-install.md deleted, all cross-references updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-usage-install-guides/17-CONTEXT.md
@.planning/phases/17-usage-install-guides/17-RESEARCH.md
@llms-install.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create INSTALL.md and delete llms-install.md</name>
  <files>INSTALL.md, llms-install.md</files>
  <read_first>
    llms-install.md
    README.md
    USAGE.md
    .planning/phases/17-usage-install-guides/17-RESEARCH.md
  </read_first>
  <action>
**Step 1: Delete llms-install.md** using `git rm llms-install.md`.

**Step 2: Create INSTALL.md** with the following content structure (per D-01, D-02, D-03, D-04). This is a human-first document -- write in natural prose, not robotic numbered steps.

The file must contain these exact sections and content:

```markdown
# Install Guide

Serena is a single Go binary. Install it, point your coding agent at it, and you are ready to go.

## Prerequisites

**Install the binary:**

\`\`\`bash
go install github.com/postfix/serena/cmd/serena@latest
\`\`\`

Requires Go 1.25 or later. After installation, verify the binary is in your PATH:

\`\`\`bash
serena --help
\`\`\`

If `serena` is not found, ensure `$(go env GOPATH)/bin` is in your PATH.

## Agent Setup

Each coding agent has its own MCP configuration format. Pick the section for your agent below.

### Claude Code

Add to `.claude/settings.json` (project-level) or `~/.claude/settings.json` (global):

\`\`\`json
{
  "mcpServers": {
    "serena": {
      "command": "serena",
      "args": ["--mode=stdio"]
    }
  }
}
\`\`\`

### Codex

Add to `.codex/config.json`:

\`\`\`json
{
  "mcpServers": {
    "serena": {
      "command": "serena",
      "args": ["--mode=stdio", "--profile=codex"]
    }
  }
}
\`\`\`

The `--profile=codex` flag loads a tool set tuned for Codex's capabilities.

### OpenCode

Add to `opencode.json` (project root) or `~/.config/opencode/opencode.json` (global):

\`\`\`json
{
  "mcp": {
    "serena": {
      "type": "local",
      "command": ["serena", "--mode=stdio"],
      "enabled": true
    }
  }
}
\`\`\`

Note: OpenCode uses the `"mcp"` key (not `"mcpServers"`), and `"command"` is an array rather than a string.

### Cursor

Add to `.cursor/mcp.json` (project-level) or `~/.cursor/mcp.json` (global):

\`\`\`json
{
  "mcpServers": {
    "serena": {
      "command": "serena",
      "args": ["--mode=stdio", "--profile=ide-assistant"]
    }
  }
}
\`\`\`

Use absolute paths if `serena` is not in your PATH (e.g., `"/home/user/go/bin/serena"`).

### Gemini CLI

Add to `~/.gemini/settings.json` (global) or `.gemini/settings.json` (project-level):

\`\`\`json
{
  "mcpServers": {
    "serena": {
      "command": "serena",
      "args": ["--mode=stdio"]
    }
  }
}
\`\`\`

### Antigravity

Open the Agent Panel, click "..." > MCP Servers > Manage > Edit configuration, and add:

\`\`\`json
{
  "mcpServers": {
    "serena": {
      "command": "serena",
      "args": ["--mode=stdio"]
    }
  }
}
\`\`\`

Use absolute paths for the command. The config file location varies by platform.

### HTTP Mode

For agents that support HTTP-based MCP servers, or for connecting multiple agents to a shared daemon:

\`\`\`bash
serena --mode=http --http-addr=127.0.0.1:8080
\`\`\`

Point your agent's MCP client at `http://127.0.0.1:8080`. The daemon persists across client disconnects and shares warm language server caches between sessions.

## Verify Installation

After configuring your agent, run the `onboard_project` tool from your agent to confirm everything works. This initializes Serena for your workspace and prints a summary of available capabilities.

Expected behavior: Serena starts a background daemon, launches a language server for your project's primary language, and returns a project summary with detected languages and available tools.

## Next Steps

- [USAGE.md](USAGE.md) -- Configuration, profiles, modes, observability, and performance tuning
- [README.md](README.md) -- Feature overview, architecture, and full tool list
```

**Step 3: Check for stale references.** Run `grep -rn "llms-install" . --include="*.md" --include="*.go" --include="*.json" --include="*.yml"` (excluding `.planning/` directory). If any non-planning files reference `llms-install.md`, update them to reference `INSTALL.md` instead.

**Important per D-03:** The tone must be natural developer documentation. Write for a human reading a markdown file, not for a machine parsing instructions.
  </action>
  <verify>
    <automated>test -f INSTALL.md && ! test -f llms-install.md && grep -q "go install github.com/postfix/serena" INSTALL.md && grep -c "^### " INSTALL.md | grep -q "[7-9]" && grep -q '"mcp":' INSTALL.md && grep -q "USAGE.md" INSTALL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `INSTALL.md` exists at repo root
    - `llms-install.md` does NOT exist at repo root
    - INSTALL.md contains `go install github.com/postfix/serena/cmd/serena@latest`
    - INSTALL.md contains `serena --help` verification command
    - INSTALL.md contains exactly 7 agent subsections (h3): Claude Code, Codex, OpenCode, Cursor, Gemini CLI, Antigravity, HTTP Mode
    - INSTALL.md OpenCode section uses `"mcp"` key (not `"mcpServers"`)
    - INSTALL.md OpenCode section uses `"command": ["serena", "--mode=stdio"]` (array format)
    - INSTALL.md Codex section includes `--profile=codex` flag
    - INSTALL.md Cursor section includes `--profile=ide-assistant` flag
    - INSTALL.md contains `## Verify Installation` section with `onboard_project` mention
    - INSTALL.md contains `## Next Steps` with links to both USAGE.md and README.md
    - No files outside `.planning/` reference `llms-install.md` (grep returns empty)
    - `go vet ./...` passes
  </acceptance_criteria>
  <done>INSTALL.md created with shared prerequisites and 7 agent setup sections (6 named agents + HTTP mode). llms-install.md deleted. All cross-references updated. Human-first tone throughout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable -- documentation-only phase. No code changes, no new endpoints, no input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | I (Information Disclosure) | INSTALL.md config examples | accept | Config examples contain only command paths and flags, no secrets or credentials |
</threat_model>

<verification>
- INSTALL.md exists, llms-install.md deleted
- All 6 agents from D-02 have dedicated sections
- OpenCode config uses correct format (different from other agents, per Research Pitfall 2)
- No stale `llms-install` references in non-planning files
- `go vet ./...` and `go test -short ./...` pass
</verification>

<success_criteria>
- INSTALL.md is a complete, human-readable install guide for the Go binary
- All 6 coding agents have per-agent MCP config sections with correct JSON formats
- llms-install.md is removed from the repository
- Cross-references point to INSTALL.md
</success_criteria>

<output>
After completion, create `.planning/phases/17-usage-install-guides/17-02-SUMMARY.md`
</output>
