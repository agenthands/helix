---
phase: 20-scenarios-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/fixtures/polyglot/go.mod
  - testdata/fixtures/polyglot/main.go
  - testdata/fixtures/polyglot/pkg/greeter.go
  - testdata/fixtures/polyglot/pyproject.toml
  - testdata/fixtures/polyglot/main.py
  - testdata/fixtures/polyglot/utils.py
  - testdata/fixtures/polyglot/tsconfig.json
  - testdata/fixtures/polyglot/main.ts
  - testdata/fixtures/polyglot/greeter.ts
  - testdata/fixtures/unsupported/main.xyz
  - testdata/fixtures/unsupported/utils.xyz
  - testdata/fixtures/collision/go.mod
  - testdata/fixtures/collision/pyproject.toml
  - testdata/fixtures/collision/tsconfig.json
  - testdata/fixtures/collision/backend/config/main.go
  - testdata/fixtures/collision/worker/config/main.py
  - testdata/fixtures/collision/web/config/main.ts
  - test/oracle/runtime/doc.go
autonomous: true
requirements:
  - SCEN-01

must_haves:
  truths:
    - "Polyglot fixture detected as Go+Python+TypeScript by DetectLanguages"
    - "Unsupported fixture uses .xyz extension not in language registry"
    - "Collision fixture has Config/Handler/Parse symbols in Go, Python, and TypeScript"
    - "Runtime oracle package exists with correct build tag"
  artifacts:
    - path: "testdata/fixtures/polyglot/go.mod"
      provides: "Go language detection marker"
      contains: "module polyglot-fixture"
    - path: "testdata/fixtures/polyglot/pyproject.toml"
      provides: "Python language detection marker"
      contains: "polyglot-fixture"
    - path: "testdata/fixtures/polyglot/tsconfig.json"
      provides: "TypeScript language detection marker"
      contains: "compilerOptions"
    - path: "testdata/fixtures/unsupported/main.xyz"
      provides: "Unsupported language file"
    - path: "testdata/fixtures/collision/backend/config/main.go"
      provides: "Go collision symbols"
      contains: "Config"
    - path: "testdata/fixtures/collision/worker/config/main.py"
      provides: "Python collision symbols"
      contains: "Config"
    - path: "testdata/fixtures/collision/web/config/main.ts"
      provides: "TypeScript collision symbols"
      contains: "Config"
    - path: "test/oracle/runtime/doc.go"
      provides: "Runtime oracle package declaration"
      contains: "package runtime_test"
  key_links:
    - from: "testdata/fixtures/polyglot/"
      to: "internal/kernel/workspace.go"
      via: "DetectLanguages scans root marker files"
      pattern: "go\\.mod|pyproject\\.toml|tsconfig\\.json"
---

<objective>
Create three new test fixtures (polyglot monorepo, unsupported language, name collision) and the runtime oracle package stub.

Purpose: Fixtures provide the diverse repository shapes required by SCEN-01 scenario tests in Plans 02-03. The runtime doc.go enables Plan 04 runtime tests.
Output: Three new fixture directories under testdata/fixtures/ and test/oracle/runtime/doc.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-scenarios-runtime/20-CONTEXT.md
@.planning/phases/20-scenarios-runtime/20-RESEARCH.md

<interfaces>
<!-- From test/harness/fixture.go -->
```go
func PrepareFixture(tb testing.TB, lang string) string  // copies testdata/fixtures/{lang}/ to temp dir
func RequireGopls(tb testing.TB)
func RequireLS(tb testing.TB, binary string)
```

<!-- From internal/kernel/workspace.go — DetectLanguages scans root-level marker files -->
<!-- Marker files: go.mod, pyproject.toml, tsconfig.json, Cargo.toml, pom.xml, etc. -->
<!-- CRITICAL: Markers must be at fixture ROOT, not nested in subdirs -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create polyglot, unsupported, and collision fixtures</name>
  <files>
    testdata/fixtures/polyglot/go.mod
    testdata/fixtures/polyglot/main.go
    testdata/fixtures/polyglot/pkg/greeter.go
    testdata/fixtures/polyglot/pyproject.toml
    testdata/fixtures/polyglot/main.py
    testdata/fixtures/polyglot/utils.py
    testdata/fixtures/polyglot/tsconfig.json
    testdata/fixtures/polyglot/main.ts
    testdata/fixtures/polyglot/greeter.ts
    testdata/fixtures/unsupported/main.xyz
    testdata/fixtures/unsupported/utils.xyz
    testdata/fixtures/collision/go.mod
    testdata/fixtures/collision/pyproject.toml
    testdata/fixtures/collision/tsconfig.json
    testdata/fixtures/collision/backend/config/main.go
    testdata/fixtures/collision/worker/config/main.py
    testdata/fixtures/collision/web/config/main.ts
  </files>
  <read_first>
    testdata/fixtures/go/main.go
    testdata/fixtures/go/go.mod
    testdata/fixtures/python/main.py
    testdata/fixtures/python/utils.py
    testdata/fixtures/typescript/main.ts
    testdata/fixtures/typescript/greeter.ts
    testdata/fixtures/typescript/tsconfig.json
    internal/kernel/workspace.go (read DetectLanguages to confirm marker files)
    internal/langregistry/ (grep for .xyz to confirm absence)
  </read_first>
  <action>
    Create three fixture directories per D-01, D-02, D-03:

    **Polyglot monorepo** (`testdata/fixtures/polyglot/`) — per D-01, composite of existing fixtures:
    - `go.mod`: `module polyglot-fixture` with `go 1.21`
    - `main.go`: package main with `func main()` calling `Helper()` and `fmt.Println("Hello, Go!")`, plus `func Helper()`, `type DemoStruct struct { Field int }`, `func (d *DemoStruct) Value() int` — reuse patterns from existing go/main.go
    - `pkg/greeter.go`: package pkg with `type Greeter struct{}` and `func (g *Greeter) Greet(name string) string`
    - `pyproject.toml`: `[project]` with `name = "polyglot-fixture"`, `version = "0.1.0"`
    - `main.py`: `from utils import greet`, `def helper() -> str:`, `class DemoClass:` — reuse patterns from existing python/main.py
    - `utils.py`: `def greet(name: str) -> str:`, `class Greeter:` — reuse patterns from existing python/utils.py
    - `tsconfig.json`: `{"compilerOptions": {"target": "es2020", "module": "commonjs", "strict": true}}` — match existing typescript/tsconfig.json
    - `main.ts`: TypeScript main with imports, Greeter usage — reuse patterns from existing typescript/main.ts
    - `greeter.ts`: `export class Greeter { greet(name: string): string }` — reuse patterns from existing typescript/greeter.ts
    
    CRITICAL: All marker files (go.mod, pyproject.toml, tsconfig.json) MUST be at the fixture ROOT, not nested. DetectLanguages only scans root-level markers.

    **Unsupported language** (`testdata/fixtures/unsupported/`) — per D-02:
    - `main.xyz`: arbitrary content like `// This is an unsupported language file\nfunction main() { print("hello") }`
    - `utils.xyz`: arbitrary content like `// Utility file\nfunction helper() { return 42 }`
    - NO marker files (no go.mod, pyproject.toml, etc.) — DetectLanguages should return empty
    - Verify `.xyz` is NOT in `internal/langregistry/` before creating

    **Name collision** (`testdata/fixtures/collision/`) — per D-03:
    - Root marker files: `go.mod` (module collision-fixture), `pyproject.toml` ([project] name="collision-fixture"), `tsconfig.json`
    - `backend/config/main.go`: package config with `type Config struct { Name string }`, `func Handler(c Config) string { return c.Name }`, `func Parse(raw string) Config { return Config{Name: raw} }`
    - `worker/config/main.py`: `class Config:` with `__init__(self, name: str)`, `def Handler(c: Config) -> str:`, `def Parse(raw: str) -> Config:`
    - `web/config/main.ts`: `export class Config { constructor(public name: string) {} }`, `export function Handler(c: Config): string`, `export function Parse(raw: string): Config`
  </action>
  <verify>
    <automated>ls -R testdata/fixtures/polyglot/ testdata/fixtures/unsupported/ testdata/fixtures/collision/ && grep -r "Config" testdata/fixtures/collision/ && grep -c "module polyglot-fixture" testdata/fixtures/polyglot/go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `testdata/fixtures/polyglot/go.mod` contains "module polyglot-fixture"
    - `testdata/fixtures/polyglot/pyproject.toml` contains 'name = "polyglot-fixture"'
    - `testdata/fixtures/polyglot/tsconfig.json` contains "compilerOptions"
    - `testdata/fixtures/unsupported/main.xyz` exists (any content)
    - `testdata/fixtures/unsupported/` has NO go.mod, pyproject.toml, or tsconfig.json
    - `testdata/fixtures/collision/backend/config/main.go` contains "type Config struct"
    - `testdata/fixtures/collision/worker/config/main.py` contains "class Config"
    - `testdata/fixtures/collision/web/config/main.ts` contains "export class Config"
    - `testdata/fixtures/collision/go.mod` exists at ROOT level
    - All three marker files (go.mod, pyproject.toml, tsconfig.json) are at collision/ ROOT, not nested
  </acceptance_criteria>
  <done>Three fixture directories created with correct structure and language marker files at root level</done>
</task>

<task type="auto">
  <name>Task 2: Create runtime oracle package stub</name>
  <files>test/oracle/runtime/doc.go</files>
  <read_first>
    test/oracle/scenario/doc.go
    test/oracle/contract/doc.go
  </read_first>
  <action>
    Create `test/oracle/runtime/doc.go` following the same pattern as `test/oracle/scenario/doc.go`:
    ```go
    //go:build integration || llm || llmjudge

    // Package runtime_test tests worker pool stress, clean shutdown, and degraded subsystem behavior.
    package runtime_test
    ```
    
    Use `runtime_test` as package name (external test package pattern, consistent with oracle convention where test files use `_test` suffix and external package).

    NOTE: Check `test/oracle/scenario/doc.go` — if it uses `package scenario` (not `scenario_test`), match that pattern instead. The doc.go establishes the package; test files use `_test.go` suffix which Go treats as test-only.
  </action>
  <verify>
    <automated>head -5 test/oracle/runtime/doc.go && go vet ./test/oracle/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - `test/oracle/runtime/doc.go` first line is `//go:build integration || llm || llmjudge`
    - Package declaration matches the pattern from test/oracle/scenario/doc.go (likely `package runtime_test`)
    - `go vet ./test/oracle/runtime/` passes without error
  </acceptance_criteria>
  <done>Runtime oracle package exists with correct build tag, passes go vet</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture files → test runtime | Test fixtures are trusted static files; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | testdata/fixtures/ | accept | Fixtures are test-only static files committed to git; no runtime exposure |
| T-20-02 | Information Disclosure | .xyz fixture content | accept | Arbitrary content, no secrets, test-only |
</threat_model>

<verification>
- `ls testdata/fixtures/polyglot/ testdata/fixtures/unsupported/ testdata/fixtures/collision/` shows all three directories
- `go vet ./test/oracle/runtime/` passes
- `go test ./...` still passes (no tags, no new test code yet)
</verification>

<success_criteria>
- Three new fixture directories exist with correct structure
- Polyglot fixture has all three language markers at root level
- Unsupported fixture has .xyz files and no language markers
- Collision fixture has Config/Handler/Parse in Go, Python, TypeScript
- Runtime oracle package compiles with correct build tag
</success_criteria>

<output>
After completion, create `.planning/phases/20-scenarios-runtime/20-01-SUMMARY.md`
</output>
