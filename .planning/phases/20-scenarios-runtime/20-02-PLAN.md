---
phase: 20-scenarios-runtime
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - test/oracle/scenario/go_test.go
  - test/oracle/scenario/python_test.go
  - test/oracle/scenario/typescript_test.go
  - test/oracle/scenario/polyglot_test.go
  - test/oracle/scenario/unsupported_test.go
  - test/oracle/scenario/collision_test.go
  - test/oracle/scenario/degraded_test.go
autonomous: true
requirements:
  - SCEN-01
  - SCEN-02
  - SCEN-03

must_haves:
  truths:
    - "Full-cycle agent workflow (activate->search->read->edit->verify) passes against Go fixture"
    - "Full-cycle agent workflow passes against Python fixture"
    - "Full-cycle agent workflow passes against TypeScript fixture"
    - "Polyglot monorepo detects all three languages and symbols are language-isolated"
    - "Unsupported language fixture: LS-dependent tools fail cleanly, file tools work"
    - "Name collision fixture: same symbol names across languages do not cross-contaminate"
    - "Degraded capability scenario: file tools work when LS is unavailable"
  artifacts:
    - path: "test/oracle/scenario/go_test.go"
      provides: "Go full-cycle scenario"
      contains: "TestScenario_Go_FullCycle"
    - path: "test/oracle/scenario/python_test.go"
      provides: "Python full-cycle scenario"
      contains: "TestScenario_Python_FullCycle"
    - path: "test/oracle/scenario/typescript_test.go"
      provides: "TypeScript full-cycle scenario"
      contains: "TestScenario_TypeScript_FullCycle"
    - path: "test/oracle/scenario/polyglot_test.go"
      provides: "Polyglot honesty scenarios"
      contains: "TestScenario_Polyglot"
    - path: "test/oracle/scenario/unsupported_test.go"
      provides: "Unsupported language scenarios"
      contains: "TestScenario_Unsupported"
    - path: "test/oracle/scenario/collision_test.go"
      provides: "Name collision scenarios"
      contains: "TestScenario_Collision"
    - path: "test/oracle/scenario/degraded_test.go"
      provides: "Degraded capability scenario"
      contains: "TestScenario_Degraded"
  key_links:
    - from: "test/oracle/scenario/*_test.go"
      to: "test/harness/"
      via: "StartRunner, PrepareFixture, CallTool, CallToolExpectError"
      pattern: "harness\\.(StartRunner|PrepareFixture|CallTool)"
    - from: "test/oracle/scenario/polyglot_test.go"
      to: "testdata/fixtures/polyglot/"
      via: "PrepareFixture(t, \"polyglot\")"
      pattern: "PrepareFixture.*polyglot"
---

<objective>
Implement multi-step agent workflow scenario tests across all seven fixture types: Go, Python, TypeScript, polyglot monorepo, unsupported language, name collision, and degraded capability.

Purpose: Proves realistic agent workflows (activate -> search -> read -> edit -> verify) work across diverse repository shapes and that polyglot honesty rules are enforced.
Output: Seven test files in test/oracle/scenario/ covering SCEN-01, SCEN-02, SCEN-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-scenarios-runtime/20-CONTEXT.md
@.planning/phases/20-scenarios-runtime/20-RESEARCH.md
@.planning/phases/20-scenarios-runtime/20-01-SUMMARY.md

<interfaces>
<!-- From test/harness/runner.go -->
```go
type RunnerOptions struct {
    WorkspaceDir string
    SkipLS       bool
    LSTimeout    time.Duration
    Profile      string
    Mode         string
    MaxWorkers   int
}

type Runner struct {
    Daemon  *daemon.Daemon
    Session *mcp.ClientSession
}

func StartRunner(tb testing.TB, opts RunnerOptions) *Runner
func (r *Runner) Stop()
func (r *Runner) NewHTTPSession(t *testing.T) *mcp.ClientSession
func DefaultTestConfig(tb testing.TB) *config.SerenaConfig
func WaitForLS(tb testing.TB, session *mcp.ClientSession, timeout time.Duration)
func ProjectRoot() string
```

<!-- From test/harness/tools.go -->
```go
func CallTool(tb testing.TB, session *mcp.ClientSession, name string, args map[string]any) *mcp.CallToolResult
func TextContent(r *mcp.CallToolResult) string
func CallToolExpectError(tb testing.TB, session *mcp.ClientSession, name string, args map[string]any) *mcp.CallToolResult
func ListSessionTools(tb testing.TB, session *mcp.ClientSession) []string
```

<!-- From test/harness/fixture.go -->
```go
func PrepareFixture(tb testing.TB, lang string) string  // copies testdata/fixtures/{lang}/ to temp dir
func RequireGopls(tb testing.TB)
func RequireLS(tb testing.TB, binary string)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Go, Python, and TypeScript full-cycle scenario tests</name>
  <files>
    test/oracle/scenario/go_test.go
    test/oracle/scenario/python_test.go
    test/oracle/scenario/typescript_test.go
  </files>
  <read_first>
    test/oracle/scenario/doc.go
    test/harness/runner.go
    test/harness/tools.go
    test/harness/fixture.go
    testdata/fixtures/go/main.go
    testdata/fixtures/go/pkg/greeter.go (if exists, ls first)
    testdata/fixtures/python/main.py
    testdata/fixtures/python/utils.py
    testdata/fixtures/typescript/main.ts
    testdata/fixtures/typescript/greeter.ts
    test/integration/concurrency_test.go (for pattern reference)
  </read_first>
  <action>
    Create three test files implementing full-cycle agent workflows per D-05 and D-06.

    **go_test.go** — `TestScenario_Go_FullCycle`:
    1. `harness.RequireGopls(t)`
    2. `fixtureDir := harness.PrepareFixture(t, "go")`
    3. `runner := harness.StartRunner(t, harness.RunnerOptions{WorkspaceDir: fixtureDir})`
    4. Step 1 (search): `CallTool(t, s, "search_symbols", {"query": "Helper"})` — assert TextContent contains "Helper"
    5. Step 2 (read): `CallTool(t, s, "read_file", {"path": "main.go"})` — assert contains "Helper"
    6. Step 3 (edit): `CallTool(t, s, "replace_in_file", {"path": "main.go", "old": "Helper function called", "new": "Helper function invoked"})` — assert success
    7. Step 4 (verify read-back): `CallTool(t, s, "read_file", {"path": "main.go"})` — assert contains "invoked", assert does NOT contain "called"
    8. Step 5 (verify search after edit): `CallTool(t, s, "search_symbols", {"query": "Helper"})` — assert still finds Helper

    **python_test.go** — `TestScenario_Python_FullCycle`:
    1. `harness.RequireLS(t, "pyright-langserver")`
    2. `fixtureDir := harness.PrepareFixture(t, "python")`
    3. `runner := harness.StartRunner(t, harness.RunnerOptions{WorkspaceDir: fixtureDir})`
    4. Step 1 (search): search_symbols for "helper" — assert found
    5. Step 2 (read): read_file "main.py" — assert contains "helper"
    6. Step 3 (edit): replace_in_file in main.py, change `return "hello"` to `return "hi"`
    7. Step 4 (verify): read_file "main.py" — assert contains `"hi"`, not `"hello"`

    **typescript_test.go** — `TestScenario_TypeScript_FullCycle`:
    1. `harness.RequireLS(t, "typescript-language-server")`
    2. `fixtureDir := harness.PrepareFixture(t, "typescript")`
    3. `runner := harness.StartRunner(t, harness.RunnerOptions{WorkspaceDir: fixtureDir})`
    4. Step 1 (search): search_symbols for "Greeter" — assert found
    5. Step 2 (read): read_file "greeter.ts" — assert contains "Greeter"
    6. Step 3 (edit): replace_in_file in greeter.ts, change "Hello" to "Hey"
    7. Step 4 (verify): read_file "greeter.ts" — assert contains "Hey"

    All files MUST start with `//go:build integration || llm || llmjudge` build tag.
    All files use `package scenario_test` (external test package).
    Import `"github.com/postfix/serena/test/harness"` and `"github.com/stretchr/testify/require"`.
    Use `require.Contains` / `require.NotContains` for assertions.
  </action>
  <verify>
    <automated>go vet ./test/oracle/scenario/ && go test -tags integration -run "TestScenario_Go_FullCycle" -count=1 -timeout 2m ./test/oracle/scenario/</automated>
  </verify>
  <acceptance_criteria>
    - `test/oracle/scenario/go_test.go` contains `func TestScenario_Go_FullCycle(t *testing.T)`
    - `test/oracle/scenario/python_test.go` contains `func TestScenario_Python_FullCycle(t *testing.T)`
    - `test/oracle/scenario/typescript_test.go` contains `func TestScenario_TypeScript_FullCycle(t *testing.T)`
    - All three files start with `//go:build integration || llm || llmjudge`
    - All three files import `"github.com/postfix/serena/test/harness"`
    - Go test calls `harness.RequireGopls(t)` as first line
    - Python test calls `harness.RequireLS(t, "pyright-langserver")` as first line
    - TypeScript test calls `harness.RequireLS(t, "typescript-language-server")` as first line
    - Each test exercises activate->search->read->edit->verify sequence
    - `go vet ./test/oracle/scenario/` passes
    - `go test -tags integration -run TestScenario_Go_FullCycle -count=1 ./test/oracle/scenario/` passes (requires gopls)
  </acceptance_criteria>
  <done>Three single-language full-cycle scenario tests created and Go test passes</done>
</task>

<task type="auto">
  <name>Task 2: Polyglot, unsupported, collision, and degraded scenario tests</name>
  <files>
    test/oracle/scenario/polyglot_test.go
    test/oracle/scenario/unsupported_test.go
    test/oracle/scenario/collision_test.go
    test/oracle/scenario/degraded_test.go
  </files>
  <read_first>
    test/oracle/scenario/go_test.go (pattern from Task 1)
    testdata/fixtures/polyglot/ (ls to confirm structure from Plan 01)
    testdata/fixtures/unsupported/ (ls to confirm structure)
    testdata/fixtures/collision/ (ls to confirm structure)
    testdata/fixtures/collision/backend/config/main.go
    testdata/fixtures/collision/worker/config/main.py
    testdata/fixtures/collision/web/config/main.ts
    internal/kernel/workspace.go (DetectLanguages behavior)
  </read_first>
  <action>
    Create four test files for multi-fixture scenarios per D-01, D-02, D-03, D-04.

    **polyglot_test.go** — per D-01, SCEN-03:
    - `TestScenario_Polyglot_FullCycle`:
      1. `harness.RequireGopls(t)` (Go is the primary LS tested)
      2. `fixtureDir := harness.PrepareFixture(t, "polyglot")`
      3. Activate workspace, search for Go symbol "Helper" — assert found
      4. Search for "Greeter" — assert found (exists in Go and TypeScript)
      5. Read `main.go` — assert Go content
      6. Read `main.py` — assert Python content
      7. Edit Go file (replace_in_file), verify edit took effect
    - `TestScenario_Polyglot_NoCrossLanguageContamination` (SCEN-03):
      1. Activate polyglot workspace
      2. Use `get_definition` on Go "Helper" — assert result path contains ".go", NOT ".py" or ".ts"
      3. Use `find_references` on Go "Helper" — assert all results are .go files
      4. Verify no Python or TypeScript file paths appear in Go symbol results
    - `TestScenario_Polyglot_FileOpsAcrossLanguages`:
      1. `list_directory` at root — assert sees .go, .py, .ts files
      2. `search_in_files` for "main" — assert results span multiple file types

    **unsupported_test.go** — per D-02, SCEN-03:
    - `TestScenario_Unsupported_FileOpsWork`:
      1. `fixtureDir := harness.PrepareFixture(t, "unsupported")`
      2. Start runner with `SkipLS: true` (no LS expected)
      3. `read_file` "main.xyz" — assert success, returns file content
      4. `list_directory` "" — assert sees main.xyz and utils.xyz
      5. `search_in_files` for "function" — assert finds matches in .xyz files
    - `TestScenario_Unsupported_LSToolsFailCleanly`:
      1. Same fixture, `SkipLS: true`
      2. `CallToolExpectError` for `search_symbols` with query "main" — assert error response
      3. Assert error text contains meaningful message (not empty, not a stack trace)
      4. Assert error `IsError` is true

    **collision_test.go** — per D-03, SCEN-03:
    - `TestScenario_Collision_FullCycle`:
      1. `harness.RequireGopls(t)`
      2. `fixtureDir := harness.PrepareFixture(t, "collision")`
      3. Activate workspace, search for "Config" — assert found
      4. Read `backend/config/main.go` — assert Go Config struct
      5. Read `worker/config/main.py` — assert Python Config class
      6. Read `web/config/main.ts` — assert TypeScript Config class
      7. Edit Go Config (replace_in_file), verify only Go file changed
      8. Read Python and TypeScript files again — assert unchanged
    - `TestScenario_Collision_NoCrossContamination`:
      1. `get_definition` on Go "Config" — assert result is in backend/config/main.go, NOT worker/ or web/
      2. `find_references` for Go "Config" — assert all results in .go files

    **degraded_test.go** — per D-04:
    - `TestScenario_Degraded_FileOpsWithoutLS`:
      1. `fixtureDir := harness.PrepareFixture(t, "go")`
      2. Start runner with `SkipLS: true` (simulates LS unavailable — per D-04, simplest approach that tests same path)
      3. `read_file` "main.go" — assert success
      4. `list_directory` "" — assert success
      5. `search_in_files` for "Helper" — assert finds matches
      6. `replace_in_file` in main.go — assert success
      7. `read_file` again to verify edit
    - `TestScenario_Degraded_LSToolsReportHonestly`:
      1. Same SkipLS setup
      2. `CallToolExpectError` for `search_symbols` — assert clean error about LS not ready/available
      3. `CallToolExpectError` for `get_definition` — assert clean error

    All files MUST start with `//go:build integration || llm || llmjudge`.
    Package: `package scenario_test`.
  </action>
  <verify>
    <automated>go vet ./test/oracle/scenario/ && go test -tags integration -run "TestScenario_Unsupported|TestScenario_Degraded_FileOps" -count=1 -timeout 2m ./test/oracle/scenario/</automated>
  </verify>
  <acceptance_criteria>
    - `test/oracle/scenario/polyglot_test.go` contains `TestScenario_Polyglot_FullCycle` and `TestScenario_Polyglot_NoCrossLanguageContamination`
    - `test/oracle/scenario/unsupported_test.go` contains `TestScenario_Unsupported_FileOpsWork` and `TestScenario_Unsupported_LSToolsFailCleanly`
    - `test/oracle/scenario/collision_test.go` contains `TestScenario_Collision_FullCycle` and `TestScenario_Collision_NoCrossContamination`
    - `test/oracle/scenario/degraded_test.go` contains `TestScenario_Degraded_FileOpsWithoutLS` and `TestScenario_Degraded_LSToolsReportHonestly`
    - All four files start with `//go:build integration || llm || llmjudge`
    - Polyglot tests assert no cross-language contamination (Go results don't include .py/.ts paths)
    - Unsupported tests use `SkipLS: true` and test both file ops (success) and LS tools (clean error)
    - Collision tests verify edits to one language don't affect others
    - `go vet ./test/oracle/scenario/` passes
  </acceptance_criteria>
  <done>Polyglot honesty, unsupported language, name collision, and degraded capability scenario tests created; unsupported and degraded file-ops tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture content -> tool responses | Fixtures are trusted; tool responses validated for honesty |
| Cross-language symbol isolation | Go symbol queries must not leak Python/TS results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Information Disclosure | Cross-language symbol leakage | mitigate | TestScenario_Polyglot_NoCrossLanguageContamination and TestScenario_Collision_NoCrossContamination assert no cross-language results in symbol queries |
| T-20-04 | Spoofing | Unsupported language fake identification | mitigate | TestScenario_Unsupported_LSToolsFailCleanly asserts clean error, no fake language detection |
| T-20-05 | Denial of Service | Degraded LS causing tool hangs | mitigate | TestScenario_Degraded tests verify tools respond promptly (SkipLS path), no infinite waits |
</threat_model>

<verification>
- `go vet ./test/oracle/scenario/` passes
- `go test -tags integration -run TestScenario_Go_FullCycle -count=1 ./test/oracle/scenario/` passes
- `go test -tags integration -run "TestScenario_Unsupported|TestScenario_Degraded_FileOps" -count=1 ./test/oracle/scenario/` passes
- All test files have correct build tags
</verification>

<success_criteria>
- Seven fixture types covered: Go, Python, TypeScript, polyglot, unsupported, collision, degraded
- Full-cycle activate->search->read->edit->verify workflow exercised for each applicable fixture
- Polyglot honesty: no cross-language symbol contamination
- Unsupported: LS tools fail cleanly, file tools work
- Degraded: file tools work without LS
</success_criteria>

<output>
After completion, create `.planning/phases/20-scenarios-runtime/20-02-SUMMARY.md`
</output>
