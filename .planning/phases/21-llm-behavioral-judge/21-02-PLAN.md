---
phase: 21-llm-behavioral-judge
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - test/oracle/llm/prompt.go
  - test/oracle/llm/interpretation_test.go
  - test/oracle/judge/rubric.go
  - test/oracle/judge/scorer.go
  - test/oracle/judge/aggregate.go
  - test/oracle/judge/judge_test.go
autonomous: true
requirements: [LLM-03, LLM-04]
must_haves:
  truths:
    - "An LLM can correctly interpret tool outputs — distinguishes success from failure, does not hallucinate unsupported conclusions"
    - "Judge scores every transcript via 5-dimension rubric with 3-level scale"
    - "Verdict thresholds correctly classify pass/soft_fail/fail"
    - "Judge runs from stored transcripts (offline) or inline, never blocks merge"
    - "Self-judged runs are explicitly labeled in output"
    - "Aggregate summary shows per-dimension averages, pass/fail counts, worst performers"
  artifacts:
    - path: "test/oracle/llm/interpretation_test.go"
      provides: "Output interpretation tests using golden file content"
      contains: "TestInterpretation"
    - path: "test/oracle/judge/rubric.go"
      provides: "Rubric definitions with anchor text per dimension"
      exports: ["Score", "ComputeVerdict", "RubricPrompt"]
    - path: "test/oracle/judge/scorer.go"
      provides: "Judge scoring: read transcript, call judge model, parse score JSON"
      exports: ["ScoreTranscript", "ParseScoreJSON"]
    - path: "test/oracle/judge/aggregate.go"
      provides: "Aggregate report with averages, counts, worst performers"
      exports: ["AggregateReport", "Aggregate"]
    - path: "test/oracle/judge/judge_test.go"
      provides: "Judge test that reads transcripts and produces scores"
      contains: "TestJudge"
  key_links:
    - from: "test/oracle/judge/judge_test.go"
      to: "test/oracle/llm/testdata/transcripts/"
      via: "Reads transcript JSON files produced by Plan 01"
      pattern: "ReadTranscript|transcripts"
    - from: "test/oracle/judge/scorer.go"
      to: "test/oracle/llm/client.go"
      via: "Uses NewClient, JudgeModel, AskSingleTurn for judge API calls"
      pattern: "llm\\.NewClient|llm\\.JudgeModel|llm\\.AskSingleTurn"
    - from: "test/oracle/judge/rubric.go"
      to: "test/oracle/judge/scorer.go"
      via: "RubricPrompt used in scorer to construct judge system prompt"
      pattern: "RubricPrompt"
---

<objective>
Implement output interpretation behavioral tests (LLM-03) and the LLM judge scoring infrastructure (LLM-04).

Purpose: Complete the LLM behavioral test suite with interpretation tests that verify Claude can correctly understand tool outputs, then build the judge that scores all transcripts via structured rubrics for quality regression tracking.

Output: `interpretation_test.go` feeding golden file content to Claude for interpretation, plus complete `test/oracle/judge/` package with rubric definitions, scorer, aggregation, and judge test. Score JSON files written to `test/oracle/judge/testdata/scores/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-llm-behavioral-judge/21-CONTEXT.md
@.planning/phases/21-llm-behavioral-judge/21-RESEARCH.md
@.planning/phases/21-llm-behavioral-judge/21-01-SUMMARY.md

@test/oracle/llm/doc.go
@test/oracle/llm/client.go
@test/oracle/llm/prompt.go
@test/oracle/llm/transcript.go
@test/oracle/judge/doc.go

<interfaces>
<!-- From Plan 01 outputs -->

From test/oracle/llm/client.go:
```go
func SkipWithoutAPIKey(t *testing.T)
func NewClient() *anthropic.Client
func SubjectModel() string
func JudgeModel() (model string, selfJudged bool)
func AskSingleTurn(ctx context.Context, client *anthropic.Client, model, system, userPrompt string) (responseText string, stopReason string, err error)
func InterCallDelay()
```

From test/oracle/llm/transcript.go:
```go
type Transcript struct {
    ScenarioID string `json:"scenario_id"`
    Category   string `json:"category"`
    Model      string `json:"model"`
    System     string `json:"system"`
    UserPrompt string `json:"user_prompt"`
    Response   string `json:"response"`
    StopReason string `json:"stop_reason"`
    SelfJudged bool   `json:"self_judged,omitempty"`
}
func WriteTranscript(t *testing.T, tr *Transcript)
func ReadTranscript(path string) (*Transcript, error)
func TranscriptDir() string
```

From test/oracle/llm/prompt.go:
```go
func FormatToolList(tools []*mcp.Tool) string
```

Golden files exist at: test/oracle/contract/testdata/golden/{tool_name}/*.golden
Available tool golden dirs: activate_project, errors, find_files, find_implementations,
find_references, format_code, get_call_hierarchy, get_code_actions, get_diagnostics,
get_hover_info, get_symbol_overview, get_type_hierarchy, go_to_definition, list_directory,
list_memories, onboard_project, prepare_for_new_conversation, read_file, read_memory,
search_in_files, search_memories, search_symbols, switch_mode, write_memory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interpretation prompt templates and implement output interpretation tests</name>
  <files>test/oracle/llm/prompt.go, test/oracle/llm/interpretation_test.go</files>
  <read_first>
    - test/oracle/llm/prompt.go (existing prompt templates from Plan 01)
    - test/oracle/llm/client.go (shared helpers)
    - test/oracle/llm/transcript.go (transcript I/O)
    - test/oracle/contract/testdata/golden/ (directory listing to identify available golden files)
    - test/harness/golden.go (golden file patterns)
    - test/harness/runner.go (ProjectRoot function)
  </read_first>
  <action>
**Step 1: Add interpretation prompts to `test/oracle/llm/prompt.go`.**

Add these exported functions (append to existing file):

- `InterpretationSystemPrompt() string` — returns:
  ```
  You are an expert at interpreting MCP tool results. You will be shown the output of an MCP tool call.

  Answer the following questions about the output:
  1. STATUS: Did the tool call succeed, fail, or is the outcome unclear? Answer exactly one of: SUCCESS, FAILURE, UNCLEAR.
  2. SUMMARY: In one sentence, what did the tool find or do?
  3. LIMITATIONS: What can you NOT conclude from this output? List specific things the output does NOT tell you.
  4. NEXT_ACTION: What would be a logical next step after seeing this output?

  Respond in exactly this format:
  STATUS: <SUCCESS|FAILURE|UNCLEAR>
  SUMMARY: <one sentence>
  LIMITATIONS: <comma-separated list>
  NEXT_ACTION: <one sentence>
  ```

- `InterpretationUserPrompt(toolName, goldenContent string) string` — returns:
  ```
  Tool: {toolName}
  Output:
  {goldenContent}
  ```

- `LoadGoldenFiles(t *testing.T) []GoldenCase` — walks `test/oracle/contract/testdata/golden/` using `harness.ProjectRoot()`, reads each `.golden` file, returns slice of `GoldenCase{ToolName string, Scenario string, Content string}`. Skip the `errors/` subdirectory (handle separately below). Export the `GoldenCase` type.

- `LoadErrorGoldenFiles(t *testing.T) []GoldenCase` — reads from `test/oracle/contract/testdata/golden/errors/` subdirectory. These represent failure cases.

**Step 2: Create `test/oracle/llm/interpretation_test.go`** with build tag `//go:build llm`:

Implement `TestInterpretation(t *testing.T)` per D-04:
1. Call `SkipWithoutAPIKey(t)`.
2. Load golden files: `successCases := LoadGoldenFiles(t)` and `errorCases := LoadErrorGoldenFiles(t)`.
3. Create client, get model.
4. Build system prompt: `InterpretationSystemPrompt()`.

5. **Success cases:** Loop over `successCases` sequentially. For each case:
   a. `InterCallDelay()` (skip first).
   b. `t.Run("success/"+case.ToolName+"/"+case.Scenario, func(t *testing.T) { ... })`.
   c. Call `AskSingleTurn` with system and `InterpretationUserPrompt(case.ToolName, case.Content)`.
   d. Assert response contains `"STATUS: SUCCESS"` (case-insensitive check for "success" after "status").
   e. Assert response does NOT contain hallucination markers: should not claim the tool can do things the output doesn't show. Check that response contains "LIMITATIONS:" section (the LLM should acknowledge what it cannot conclude).
   f. Write transcript with ScenarioID `"interpret-"+case.ToolName+"-"+case.Scenario+"-success"`, Category `"interpretation"`.

6. **Error cases:** Loop over `errorCases` sequentially. For each case:
   a. Same pattern but assert response contains `"STATUS: FAILURE"` or `"STATUS: UNCLEAR"` (not SUCCESS).
   b. Write transcript with ScenarioID `"interpret-"+case.ToolName+"-"+case.Scenario+"-error"`.

7. Limit total API calls: if `successCases` has more than 30 entries, sample every Nth case to keep total under ~40 calls plus error cases. Use deterministic sampling (sort by name, take every Nth). Log how many skipped.

Important: Do NOT use `t.Parallel()` anywhere. Per D-19.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && go vet -tags="llm" ./test/oracle/llm/...</automated>
  </verify>
  <acceptance_criteria>
    - test/oracle/llm/prompt.go contains `func InterpretationSystemPrompt() string`
    - test/oracle/llm/prompt.go contains `func InterpretationUserPrompt(`
    - test/oracle/llm/prompt.go contains `func LoadGoldenFiles(`
    - test/oracle/llm/prompt.go contains `type GoldenCase struct`
    - test/oracle/llm/interpretation_test.go contains `func TestInterpretation(t *testing.T)`
    - test/oracle/llm/interpretation_test.go contains `SkipWithoutAPIKey(t)`
    - test/oracle/llm/interpretation_test.go contains `STATUS: SUCCESS` or similar assertion string
    - test/oracle/llm/interpretation_test.go contains `WriteTranscript`
    - test/oracle/llm/interpretation_test.go contains `InterCallDelay`
    - test/oracle/llm/interpretation_test.go does NOT contain `t.Parallel()`
    - `go vet -tags="llm" ./test/oracle/llm/...` exits 0
  </acceptance_criteria>
  <done>TestInterpretation feeds real golden file content to Claude and verifies: (1) success outputs produce STATUS: SUCCESS, (2) error outputs produce STATUS: FAILURE or UNCLEAR, (3) responses include LIMITATIONS section acknowledging what cannot be concluded, (4) transcripts written for judge consumption. Sequential with delays per D-19.</done>
</task>

<task type="auto">
  <name>Task 2: Build LLM judge scoring infrastructure with rubrics, scorer, aggregation, and tests</name>
  <files>test/oracle/judge/rubric.go, test/oracle/judge/scorer.go, test/oracle/judge/aggregate.go, test/oracle/judge/judge_test.go</files>
  <read_first>
    - test/oracle/judge/doc.go (existing build tag stub)
    - test/oracle/llm/client.go (shared Anthropic client for judge calls)
    - test/oracle/llm/transcript.go (Transcript type, ReadTranscript, TranscriptDir)
    - test/oracle/llm/prompt.go (FormatToolList for context)
    - .planning/phases/21-llm-behavioral-judge/21-CONTEXT.md (D-05 through D-09, D-16)
  </read_first>
  <action>
**Step 1: Create `test/oracle/judge/rubric.go`** with build tag `//go:build llmjudge`:

Define the `Score` struct per D-05/D-07:
```go
type Score struct {
    ToolChoice           float64  `json:"tool_choice"`
    DescriptionUse       float64  `json:"description_use"`
    OutputInterpretation float64  `json:"output_interpretation"`
    UncertaintyHandling  float64  `json:"uncertainty_handling"`
    PolyglotReasoning    float64  `json:"polyglot_reasoning"`
    Total                float64  `json:"total"`
    Verdict              string   `json:"verdict"`
    Failures             []string `json:"failures"`
    SelfJudged           bool     `json:"self_judged,omitempty"`
    ScenarioID           string   `json:"scenario_id,omitempty"`
}
```

Implement `ComputeVerdict(s *Score)` per D-06:
- Count zeros among the 5 dimension scores.
- Compute `s.Total` = sum of all 5 dimensions.
- Build `s.Failures` listing any dimension with 0.0 score.
- If zeros >= 2 OR total < 2.5 → `s.Verdict = "fail"`.
- If zeros == 1 OR (total >= 2.5 AND total < 4.0) → `s.Verdict = "soft_fail"`.
- Else (no zeros AND total >= 4.0) → `s.Verdict = "pass"`.

Implement `ValidateScoreValues(s *Score) error` — checks each dimension is in {0.0, 0.5, 1.0}. Returns error listing invalid dimensions.

Implement `RubricPrompt() string` per D-08 — returns the full judge system prompt with anchor definitions:
```
You are an expert judge evaluating an LLM's interaction with MCP (Model Context Protocol) tools.

Score the following transcript on 5 dimensions using this scale:
- 0.0 = fail
- 0.5 = partial
- 1.0 = pass

## Dimensions and Anchors

### tool_choice
- 0.0: Wrong tool selected, or clearly wasteful/irrelevant tool sequence
- 0.5: Acceptable tool family but suboptimal choice (e.g., search_in_files when search_symbols would be better)
- 1.0: Correct and efficient tool choice for the task

### description_use
- 0.0: Ignored tool descriptions, selected tool by name guessing or hallucination
- 0.5: Partially referenced descriptions but missed key distinguishing details
- 1.0: Clearly used description content to make informed selection

### output_interpretation
- 0.0: Misinterpreted output (called success a failure or vice versa), hallucinated data not in output
- 0.5: Correct high-level interpretation but missed nuances or made minor unsupported claims
- 1.0: Accurate interpretation, correctly identified what output does and does not show

### uncertainty_handling
- 0.0: Made confident claims about things the output does not support, no acknowledgment of limitations
- 0.5: Acknowledged some uncertainty but still made minor unsupported claims
- 1.0: Appropriately qualified conclusions, identified limitations, no hallucinated capabilities

### polyglot_reasoning
- 0.0: Made language-specific assumptions that are wrong, confused cross-language semantics
- 0.5: Mostly correct but missed language-specific nuances
- 1.0: Correctly handled language context, or task was language-agnostic (score 1.0 for N/A)

Respond with ONLY a JSON object in this exact format, no other text:
{"tool_choice": <0.0|0.5|1.0>, "description_use": <0.0|0.5|1.0>, "output_interpretation": <0.0|0.5|1.0>, "uncertainty_handling": <0.0|0.5|1.0>, "polyglot_reasoning": <0.0|0.5|1.0>}
```

**Step 2: Create `test/oracle/judge/scorer.go`** with build tag `//go:build llmjudge`:

Import the `llm` package as `llm "github.com/postfix/serena/test/oracle/llm"`.

Implement `ScoreTranscript(ctx context.Context, client *anthropic.Client, model string, tr *llm.Transcript) (*Score, error)`:
1. Build system prompt: `RubricPrompt()`.
2. Build user prompt showing the transcript:
   ```
   ## Transcript to Judge

   Scenario: {tr.ScenarioID}
   Category: {tr.Category}

   System prompt given to subject:
   {tr.System}

   User prompt given to subject:
   {tr.UserPrompt}

   Subject's response:
   {tr.Response}
   ```
3. Call `llm.AskSingleTurn(ctx, client, model, system, userPrompt)`.
4. Parse response JSON via `ParseScoreJSON(responseText)`.
5. Set `score.ScenarioID = tr.ScenarioID` and `score.SelfJudged = tr.SelfJudged || selfJudged` (where selfJudged comes from `llm.JudgeModel()`).
6. Call `ComputeVerdict(&score)`.
7. Validate with `ValidateScoreValues(&score)`. If invalid, retry once with a more explicit prompt appending: "\nYour previous response had invalid values. Each dimension MUST be exactly 0.0, 0.5, or 1.0."
8. Return the score.

Implement `ParseScoreJSON(text string) (*Score, error)`:
1. Find first `{` and last `}` in text.
2. If not found, return error "no JSON object found in response".
3. Unmarshal the substring into Score struct.
4. Return the score (without Total/Verdict — those are computed by ComputeVerdict).

Implement `WriteScore(t *testing.T, score *Score)`:
- Marshal to indented JSON.
- Write to `test/oracle/judge/testdata/scores/{score.ScenarioID}.json`.
- Create directory if needed. Use `harness.ProjectRoot()` for base path. Per D-14.

**Step 3: Create `test/oracle/judge/aggregate.go`** with build tag `//go:build llmjudge`:

Define `AggregateReport` struct per D-09:
```go
type AggregateReport struct {
    TotalTranscripts int                `json:"total_transcripts"`
    PassCount        int                `json:"pass_count"`
    SoftFailCount    int                `json:"soft_fail_count"`
    FailCount        int                `json:"fail_count"`
    DimensionAvgs    map[string]float64 `json:"dimension_averages"`
    WorstPerformers  []string           `json:"worst_performers"`  // scenario IDs with verdict=fail
    WorstDimensions  []string           `json:"worst_dimensions"`  // dimensions with lowest avg
    SelfJudged       bool               `json:"self_judged"`
}
```

Implement `Aggregate(scores []*Score) *AggregateReport`:
1. Count pass/soft_fail/fail verdicts.
2. Compute per-dimension averages across all scores.
3. Collect WorstPerformers (scenario IDs where verdict == "fail").
4. Find WorstDimensions (dimensions with average below 0.7, sorted ascending).
5. Set SelfJudged = true if ANY score has SelfJudged=true.
6. Return the report.

Implement `WriteAggregate(t *testing.T, report *AggregateReport)`:
- Marshal to indented JSON.
- Write to `test/oracle/judge/testdata/aggregate.json`.
- Also log summary to `t.Log()` with formatted output.

**Step 4: Create `test/oracle/judge/judge_test.go`** with build tag `//go:build llmjudge`:

Implement `TestJudge(t *testing.T)` per D-04/D-16/D-17:
1. Call `llm.SkipWithoutAPIKey(t)`.
2. Get judge model: `model, selfJudged := llm.JudgeModel()`.
3. Create client: `client := llm.NewClient()`.
4. Read all transcript files from `llm.TranscriptDir()`:
   - `os.ReadDir(llm.TranscriptDir())` — if directory doesn't exist or is empty, `t.Skip("no transcripts found — run llm tests first")`.
   - For each `.json` file, call `llm.ReadTranscript(filepath.Join(dir, entry.Name()))`.
5. Score each transcript sequentially:
   - `llm.InterCallDelay()` between calls.
   - `t.Run("judge/"+tr.ScenarioID, func(t *testing.T) { ... })`.
   - Call `ScoreTranscript(ctx, client, model, tr)`.
   - If selfJudged, mark score: `score.SelfJudged = true`.
   - Call `WriteScore(t, score)`.
   - Log score: `t.Logf("Score for %s: verdict=%s total=%.1f", tr.ScenarioID, score.Verdict, score.Total)`.
   - Collect scores into a slice.
6. After all transcripts scored, compute aggregate: `report := Aggregate(scores)`.
7. Write aggregate: `WriteAggregate(t, report)`.
8. Log aggregate summary.
9. Do NOT assert pass/fail on judge scores — judge is informational, never blocks merge per D-04 and phase boundary. Use `t.Log` not `t.Error`.

Implement `TestJudgeInline(t *testing.T)` for inline mode per D-16:
1. Skip unless `os.Getenv("SERENA_INLINE_JUDGE") == "1"`.
2. Also skip without API key.
3. This test is a placeholder that notes inline mode would run behavioral tests and judge in one pass. Log: `t.Log("Inline judge mode: would run behavioral + judge in single pass. Use -tags=llm first, then -tags=llmjudge for canonical offline flow.")`.
4. The canonical path is offline (run llm tests first, then llmjudge). Inline is convenience for local iteration.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && go vet -tags="llmjudge" ./test/oracle/judge/... && go vet -tags="llm" ./test/oracle/llm/...</automated>
  </verify>
  <acceptance_criteria>
    - test/oracle/judge/rubric.go contains `type Score struct`
    - test/oracle/judge/rubric.go contains `func ComputeVerdict(`
    - test/oracle/judge/rubric.go contains `func ValidateScoreValues(`
    - test/oracle/judge/rubric.go contains `func RubricPrompt() string`
    - test/oracle/judge/rubric.go contains `tool_choice` and `description_use` and `output_interpretation` and `uncertainty_handling` and `polyglot_reasoning` in anchor text
    - test/oracle/judge/scorer.go contains `func ScoreTranscript(`
    - test/oracle/judge/scorer.go contains `func ParseScoreJSON(`
    - test/oracle/judge/scorer.go contains `func WriteScore(`
    - test/oracle/judge/aggregate.go contains `type AggregateReport struct`
    - test/oracle/judge/aggregate.go contains `func Aggregate(`
    - test/oracle/judge/aggregate.go contains `func WriteAggregate(`
    - test/oracle/judge/judge_test.go contains `func TestJudge(t *testing.T)`
    - test/oracle/judge/judge_test.go contains `func TestJudgeInline(t *testing.T)`
    - test/oracle/judge/judge_test.go contains `SkipWithoutAPIKey`
    - test/oracle/judge/judge_test.go contains `InterCallDelay`
    - test/oracle/judge/judge_test.go contains `WriteScore`
    - test/oracle/judge/judge_test.go contains `Aggregate(`
    - test/oracle/judge/judge_test.go does NOT contain `t.Error` or `t.Fatal` on score verdicts (judge is informational)
    - `go vet -tags="llmjudge" ./test/oracle/judge/...` exits 0
    - `go vet -tags="llm" ./test/oracle/llm/...` exits 0
  </acceptance_criteria>
  <done>Judge infrastructure complete: rubric.go defines 5-dimension scoring with anchor definitions per D-08, verdict thresholds per D-06, structured JSON output per D-07. Scorer calls judge model, parses JSON, validates score values. Aggregate produces per-dimension averages, pass/fail counts, worst performers per D-09. TestJudge reads transcripts from Plan 01, scores them, writes scores and aggregate. Self-judged flag propagated per D-10/D-12. Judge is informational only — never blocks merge per D-04. Build tag `llmjudge` controls compilation per D-17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Judge model → score parsing | Non-deterministic LLM output parsed as JSON for score values |
| Transcript files → judge input | Files on disk read and sent to external API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Information Disclosure | scorer.go | mitigate | Never log API key; transcript content is test data only (tool descriptions + golden file outputs), no secrets |
| T-21-05 | Tampering | scorer.go ParseScoreJSON | mitigate | Validate all score values are in {0.0, 0.5, 1.0} via ValidateScoreValues; reject and retry on invalid values |
| T-21-06 | Denial of Service | judge_test.go | mitigate | Sequential execution with InterCallDelay; SDK retries handle transient 429s; judge tests only run on manual trigger (llmjudge tag) |
| T-21-07 | Spoofing | aggregate.go | accept | Self-judged flag explicitly labels lower-confidence results; no security impact since judge scores are informational only |
</threat_model>

<verification>
1. `go vet -tags="llmjudge" ./test/oracle/judge/...` passes
2. `go vet -tags="llm" ./test/oracle/llm/...` passes (interpretation test)
3. `go test ./...` (no tags) does NOT compile or run judge tests
4. `go test -tags="llmjudge" ./test/oracle/judge/ -count=1 -short` skips (no key) or passes
5. Score JSON files appear in `test/oracle/judge/testdata/scores/` after a live run
6. `aggregate.json` appears in `test/oracle/judge/testdata/` after a live run
</verification>

<success_criteria>
- InterpretationSystemPrompt returns structured format prompt (LLM-03)
- TestInterpretation verifies success/failure interpretation against golden files
- Score struct has all 5 dimensions + total + verdict + failures per D-05/D-07
- ComputeVerdict implements thresholds per D-06
- RubricPrompt contains anchor definitions for all 5 dimensions per D-08
- ScoreTranscript calls judge model, parses JSON, validates, retries on failure
- Aggregate computes per-dimension averages, pass/fail counts, worst performers per D-09
- TestJudge reads transcripts, scores them, writes results — informational only
- Self-judged flag propagated through pipeline per D-10/D-12
- Offline mode is canonical; inline mode documented per D-16
- Build tag `llmjudge` controls judge compilation per D-17
</success_criteria>

<output>
After completion, create `.planning/phases/21-llm-behavioral-judge/21-02-SUMMARY.md`
</output>
