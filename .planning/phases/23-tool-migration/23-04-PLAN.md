---
phase: 23-tool-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/kernel/diag/actions.go
  - internal/kernel/diag/format.go
  - internal/kernel/diag/skill_adapter.go
autonomous: true
requirements: [MIG-04]

must_haves:
  truths:
    - "All 3 diagnostic tools return typed serr errors instead of raw fmt.Errorf strings"
    - "LSP request failures use serr.Internal, missing workspace edits use serr.Unsupported"
  artifacts:
    - path: "internal/kernel/diag/actions.go"
      provides: "Typed errors for code action operations"
      contains: "serr.Wrap(serr.Internal"
    - path: "internal/kernel/diag/format.go"
      provides: "Typed errors for formatting operations"
      contains: "serr.Wrap(serr.Internal"
  key_links:
    - from: "internal/kernel/diag/actions.go"
      to: "internal/errors"
      via: "import serr"
      pattern: 'serr "github.com/postfix/serena/internal/errors"'
---

<objective>
Migrate all 3 diagnostic tools (get_diagnostics, apply_code_action, format_file) from raw `fmt.Errorf` to typed `serr` errors.

Purpose: Diagnostic tools have 8 error sites covering LSP communication failures and code action semantics. Agents need to know when a code action lacks a workspace edit (Unsupported) vs when the LSP request itself failed (Internal).
Output: All 8 error sites in the diag package use `serr` constructors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-error-taxonomy/22-01-SUMMARY.md

<interfaces>
From internal/errors/errors.go:
```go
func New(kind Kind, message string) *Error
func Wrap(kind Kind, message string, cause error) *Error
func (e *Error) WithTool(tool string) *Error
func (e *Error) WithDetail(detail string) *Error
```

Kind mapping for diag:
- "codeAction request: %w" -> Internal (LSP failure, wrap cause)
- "code action has no workspace edit" -> Unsupported (action not applicable)
- "applying workspace edit: %w" -> Internal (wrap cause)
- "workspace edit was not applied" -> Internal (server-side failure)
- "formatting request: %w" -> Internal (LSP failure, wrap cause)
- "reading file: %w" -> Internal (I/O, wrap cause)
- "writing temp file: %w" -> Internal (I/O, wrap cause)
- "renaming temp file: %w" -> Internal (I/O, wrap cause)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate diag implementation and handler files</name>
  <files>internal/kernel/diag/actions.go, internal/kernel/diag/format.go, internal/kernel/diag/skill_adapter.go</files>
  <read_first>
    - internal/kernel/diag/actions.go
    - internal/kernel/diag/format.go
    - internal/kernel/diag/skill_adapter.go
    - internal/errors/kinds.go
  </read_first>
  <action>
Add `serr "github.com/postfix/serena/internal/errors"` import to actions.go and format.go. Also read skill_adapter.go to check for any handler-layer errors that need migration.

**actions.go** (4 sites):
- L37: `fmt.Errorf("codeAction request: %w", err)` -> `serr.Wrap(serr.Internal, "code action request", err)`
- L62: `fmt.Errorf("code action %q has no workspace edit (may require a command)", action.Title)` -> `serr.New(serr.Unsupported, "code action has no workspace edit").WithDetail(action.Title)`
- L80: `fmt.Errorf("applying workspace edit: %w", err)` -> `serr.Wrap(serr.Internal, "applying workspace edit", err)`
- L83: `fmt.Errorf("workspace edit was not applied by the server")` -> `serr.New(serr.Internal, "workspace edit was not applied by the server")`

**format.go** (4 sites):
- L46: `fmt.Errorf("formatting request: %w", err)` -> `serr.Wrap(serr.Internal, "formatting request", err)`
- L68: `fmt.Errorf("reading file: %w", err)` -> `serr.Wrap(serr.Internal, "reading file", err)`
- L92: `fmt.Errorf("writing temp file: %w", err)` -> `serr.Wrap(serr.Internal, "writing temp file", err)`
- L96: `fmt.Errorf("renaming temp file: %w", err)` -> `serr.Wrap(serr.Internal, "renaming temp file", err)`

**skill_adapter.go**: Read to check for handler-layer fmt.Errorf patterns. If present, migrate similarly (workspace not activated -> NoWorkspace, acquire session -> Internal, operation errors -> pass through since they're now typed).
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && go vet ./internal/kernel/diag/ && go test ./internal/kernel/diag/ -count=1 && go build ./cmd/serena</automated>
  </verify>
  <acceptance_criteria>
    - actions.go contains `serr.Wrap(serr.Internal, "code action request"` (grep confirms)
    - actions.go contains `serr.New(serr.Unsupported, "code action has no workspace edit")`
    - format.go contains `serr.Wrap(serr.Internal, "formatting request"` (grep confirms)
    - `grep -rn 'fmt.Errorf' internal/kernel/diag/` returns zero matches
    - `go test ./internal/kernel/diag/ -count=1` passes
    - `go build ./cmd/serena` succeeds
  </acceptance_criteria>
  <done>All 8 error sites in diag package use serr constructors, tests pass, build succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries -- internal refactoring.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | I (Info Disclosure) | actions.go Detail field | accept | Code action title already visible in tool responses |
</threat_model>

<verification>
- `go vet ./internal/kernel/diag/` passes
- `go test ./internal/kernel/diag/ -count=1` passes
- `go build ./cmd/serena` succeeds
- `grep -rn 'fmt.Errorf' internal/kernel/diag/` returns zero matches
</verification>

<success_criteria>
All 8 error sites in internal/kernel/diag/ use serr constructors. Tests pass. Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/23-tool-migration/23-04-SUMMARY.md`
</output>
