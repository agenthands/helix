---
phase: 23-tool-migration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/skill/workflow/skill.go
  - internal/skill/workflow/skill_test.go
autonomous: true
requirements: [MIG-06]

must_haves:
  truths:
    - "All 2 workflow tools return typed serr errors instead of raw fmt.Errorf strings"
  artifacts:
    - path: "internal/skill/workflow/skill.go"
      provides: "Typed error for unknown workflow tool"
      contains: "serr.New(serr.InvalidArgs"
  key_links:
    - from: "internal/skill/workflow/skill.go"
      to: "internal/errors"
      via: "import serr"
      pattern: 'serr "github.com/postfix/serena/internal/errors"'
---

<objective>
Migrate the 2 workflow tools (onboard_project, session_handoff) from raw `fmt.Errorf` to typed `serr` errors.

Purpose: Workflow tools have only 1 error site ("unknown workflow tool") but consistent migration ensures no tool returns raw strings.
Output: The single error site in workflow/skill.go uses `serr` constructor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-error-taxonomy/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate workflow/skill.go to typed errors</name>
  <files>internal/skill/workflow/skill.go, internal/skill/workflow/skill_test.go</files>
  <read_first>
    - internal/skill/workflow/skill.go
    - internal/skill/workflow/skill_test.go
    - internal/errors/kinds.go
  </read_first>
  <action>
Add `serr "github.com/postfix/serena/internal/errors"` import to skill.go.

**Unknown tool** (1 site):
- L94: `fmt.Errorf("unknown workflow tool: %s", name)` -> `serr.New(serr.InvalidArgs, "unknown workflow tool").WithTool(name)`

Remove `"fmt"` import if no longer used (check for remaining fmt.Sprintf usage). If the file uses fmt elsewhere, keep the import.

**Test updates** (skill_test.go): Read the test file and update any string-matching assertion for the "unknown workflow tool" error. Use `errors.Is(err, serr.ErrInvalidArgs)` if applicable.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena && go vet ./internal/skill/workflow/ && go test ./internal/skill/workflow/ -count=1 && go build ./cmd/serena</automated>
  </verify>
  <acceptance_criteria>
    - skill.go contains `serr.New(serr.InvalidArgs, "unknown workflow tool")` (grep confirms)
    - skill.go does NOT contain `fmt.Errorf` (grep confirms zero matches)
    - skill.go imports `serr "github.com/postfix/serena/internal/errors"`
    - `go test ./internal/skill/workflow/ -count=1` passes
  </acceptance_criteria>
  <done>The 1 error site in workflow/skill.go uses serr constructor, tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | — | workflow skill | accept | Single error site, no security implications |
</threat_model>

<verification>
- `go vet ./internal/skill/workflow/` passes
- `go test ./internal/skill/workflow/ -count=1` passes
- `go build ./cmd/serena` succeeds
</verification>

<success_criteria>
The error site in internal/skill/workflow/skill.go uses serr constructor. Tests pass. Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/23-tool-migration/23-06-SUMMARY.md`
</output>
