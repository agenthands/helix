---
phase: 29-phase25-formal-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-fuzzy-edit-engine/25-VERIFICATION.md
autonomous: true
requirements:
  - FUZZ-01
  - FUZZ-02
  - FUZZ-03
  - FUZZ-07
  - FUZZ-08

must_haves:
  truths:
    - "VERIFICATION.md exists for Phase 25 with pass/fail status for all 5 FUZZ requirements"
    - "Each requirement has at least one test name cited as evidence"
    - "All cited tests actually pass when executed"
  artifacts:
    - path: ".planning/phases/25-fuzzy-edit-engine/25-VERIFICATION.md"
      provides: "Formal verification evidence for FUZZ-01, FUZZ-02, FUZZ-03, FUZZ-07, FUZZ-08"
      contains: "FUZZ-01"
  key_links: []
---

<objective>
Create formal VERIFICATION.md for Phase 25 (Fuzzy Edit Engine) by running existing tests
and inspecting code to produce evidence that all 5 FUZZ requirements are satisfied.

Purpose: Close 5 unsatisfied FUZZ requirements (FUZZ-01/02/03/07/08) that have working
code and passing tests but lack formal verification documentation.

Output: `.planning/phases/25-fuzzy-edit-engine/25-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run tests and create 25-VERIFICATION.md with evidence</name>
  <files>.planning/phases/25-fuzzy-edit-engine/25-VERIFICATION.md</files>
  <read_first>
    internal/fuzzy/types.go
    internal/fuzzy/match.go
    internal/fuzzy/strategies.go
    internal/fuzzy/ellipsis.go
    internal/fuzzy/indent.go
    internal/fuzzy/diff.go
    internal/fuzzy/fuzzy_test.go
    internal/fuzzy/strategies_test.go
    internal/fuzzy/ellipsis_test.go
    internal/fuzzy/indent_test.go
    internal/fuzzy/diff_test.go
    internal/kernel/edit/replace.go
    internal/kernel/edit/replace_fuzzy_test.go
    internal/kernel/edit/tools.go
  </read_first>
  <action>
Run the fuzzy package tests and edit package fuzzy tests with verbose output to capture evidence:

```bash
go test ./internal/fuzzy/... -count=1 -v 2>&1
go test ./internal/kernel/edit/... -count=1 -run "Fuzzy|fuzzy" -v 2>&1
```

Then create `.planning/phases/25-fuzzy-edit-engine/25-VERIFICATION.md` with this structure:

## Header
- Phase: 25 (Fuzzy Edit Engine)
- Verified by: Phase 29
- Date: current date

## For each requirement (FUZZ-01, FUZZ-02, FUZZ-03, FUZZ-07, FUZZ-08), document:

### FUZZ-01: 4-strategy cascade
- **Status:** PASS or FAIL
- **Code evidence:** `internal/fuzzy/match.go` function `matchSingle` implements cascade: sweepExact -> sweepWhitespace -> sweepIndentFlex -> failureError. Cite exact function names and line ranges.
- **Test evidence:** List the specific passing test names that exercise each cascade tier:
  - `TestMatch_ExactStrategy` (exact tier)
  - `TestMatch_WhitespaceStrategy` (whitespace tier)
  - `TestMatch_IndentationStrategy` (indent-flex tier)
  - `TestMatch_FailWithDiff` (fail tier)
  - `TestSweepExact/*` subtests
  - `TestSweepWhitespace/*` subtests
  - `TestSweepIndentFlex/*` subtests

### FUZZ-02: Strategy reporting (match_strategy, similarity_score)
- **Status:** PASS or FAIL
- **Code evidence:** `internal/fuzzy/types.go` Result struct has `Strategy` and `Score` fields. `internal/kernel/edit/tools.go` formats `match_strategy: %s\nsimilarity_score: %.2f` in tool response.
- **Test evidence:**
  - `TestMatch_StrategyReporting/exact` (verifies Strategy=exact)
  - `TestMatch_StrategyReporting/whitespace` (verifies Strategy=whitespace_normalized)
  - `TestMatch_StrategyReporting/indent-diff-hits-whitespace`
  - `TestMatch_ScoreTiers` (verifies score values 1.0/0.95/0.85)
  - `TestReplaceBodyFuzzy_ExactMatchWithinBody` (verifies FuzzyMatchInfo.Strategy)

### FUZZ-03: Indentation preservation via reflow
- **Status:** PASS or FAIL
- **Code evidence:** `internal/fuzzy/indent.go` implements `reflow` function (commonLeadingPrefix + dedent + reapplyPrefix). Called from `buildResult` in match.go.
- **Test evidence:**
  - `TestReflow_AppliesSourcePrefix`
  - `TestReflow_TabsAndSpaces`
  - `TestReflow_PreservesEmptyLines`
  - `TestMatch_ReplacementText_Reflow`
  - `TestReplaceBodyFuzzy_WhitespaceNormalized` (end-to-end reflow through edit layer)

### FUZZ-07: Ambiguity refusal
- **Status:** PASS or FAIL
- **Code evidence:** `internal/fuzzy/match.go` `matchSingle` checks `len(hits) > 1` at each cascade tier and returns `ambiguityError` immediately without cascading. `ambiguityError` returns `serr.InvalidArgs`.
- **Test evidence:**
  - `TestCascade_StopsOnAmbiguity` (confirms no cascade past ambiguous tier)
  - `TestAmbiguity_Exact` (exact tier ambiguity)
  - `TestAmbiguity_LineNumberCap`
  - `TestAmbiguity_OverflowSuffix`
  - `TestAmbiguity_DoesNotCascade`
  - `TestSweepExact/ambiguous_2_hits`

### FUZZ-08: Ellipsis/placeholder support
- **Status:** PASS or FAIL
- **Code evidence:** `internal/fuzzy/ellipsis.go` implements `segmentSearch` detecting "..." on own line. `internal/fuzzy/match.go` `Match` dispatches to `matchSegmented` when `AllowEllipsis=true` and segments found.
- **Test evidence:**
  - `TestEllipsis_TwoSegments`
  - `TestEllipsis_MultipleSegments`
  - `TestEllipsis_LeadingWhitespace`
  - `TestEllipsis_InlineLiteral` (inline "..." NOT treated as marker)
  - `TestEllipsis_EmptySegmentRejected/*`
  - `TestEllipsis_ReplacementSegmentMismatch`
  - `TestEllipsis_NoMarkerReturnsNil`
  - `TestEllipsis_InOrderMatching`
  - `TestEllipsis_MixedTierAggregationReportsWeakest`
  - `TestEllipsis_ByteOffsetsWithTrailingNewline`

## Summary table
A table with columns: Requirement | Status | Tests Cited | Code Location

Include the raw `go test -v` output (or a summary) as an appendix showing all tests pass.
  </action>
  <verify>
    <automated>grep -c "FUZZ-0" .planning/phases/25-fuzzy-edit-engine/25-VERIFICATION.md | grep -q "^5$" && grep "PASS" .planning/phases/25-fuzzy-edit-engine/25-VERIFICATION.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/25-fuzzy-edit-engine/25-VERIFICATION.md` exists
    - Contains string "FUZZ-01" with a "PASS" status on same line or next line
    - Contains string "FUZZ-02" with a "PASS" status
    - Contains string "FUZZ-03" with a "PASS" status
    - Contains string "FUZZ-07" with a "PASS" status
    - Contains string "FUZZ-08" with a "PASS" status
    - Contains at least 5 test function names (e.g., `TestMatch_ExactStrategy`)
    - Contains code file paths: `internal/fuzzy/match.go`, `internal/fuzzy/types.go`, `internal/fuzzy/indent.go`, `internal/fuzzy/ellipsis.go`
    - `go test ./internal/fuzzy/... -count=1` exits 0
    - `go test ./internal/kernel/edit/... -count=1 -run "Fuzzy"` exits 0
  </acceptance_criteria>
  <done>
    25-VERIFICATION.md exists with PASS evidence for all 5 FUZZ requirements (FUZZ-01, FUZZ-02, FUZZ-03, FUZZ-07, FUZZ-08), each citing specific test names and code locations. All cited tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- this plan creates documentation only, no production code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | I (Information Disclosure) | VERIFICATION.md | accept | Planning artifact only, not shipped in binary; no secrets involved |
</threat_model>

<verification>
- `go test ./internal/fuzzy/... -count=1` passes (all fuzzy package tests)
- `go test ./internal/kernel/edit/... -count=1 -run "Fuzzy"` passes (edit layer fuzzy tests)
- `.planning/phases/25-fuzzy-edit-engine/25-VERIFICATION.md` contains evidence for FUZZ-01, FUZZ-02, FUZZ-03, FUZZ-07, FUZZ-08
</verification>

<success_criteria>
VERIFICATION.md exists for Phase 25 with documented PASS evidence for all 5 FUZZ requirements, each backed by specific test names and code inspection.
</success_criteria>

<output>
After completion, create `.planning/phases/29-phase25-formal-verification/29-01-SUMMARY.md`
</output>
