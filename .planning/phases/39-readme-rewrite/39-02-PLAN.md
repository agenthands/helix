---
phase: 39-readme-rewrite
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - README.md
autonomous: true
requirements:
  - README-01
  - README-03
  - README-04
  - LEGC-01

must_haves:
  truths:
    - "A first-time reader understands Serena as a universal code intelligence platform, not a Python port"
    - "The hero tagline 'Serena is the IDE for your coding agent' is preserved"
    - "A subtitle communicates the scope: MCP tools across 52 languages"
    - "Table of contents appears near the top for navigation"
    - "Quick start appears after advantages table, not buried at line 186"
    - "Quick start leads with serena setup claude-code, not manual JSON"
    - "RepoMap has a dedicated subsection explaining get_repo_map and get_context"
    - "v1.7 features (fuzzy editing, smart errors, progressive descriptions, health, lazy init) are highlighted"
    - "Python legacy is a single footer one-liner using 'Originally inspired by'"
    - "No occurrences of 'port' or 'rewrite' in product context"
    - "Architecture section is brief: ASCII diagram + 2 paragraphs"
    - "Auto-gen markers for tools and languages are preserved intact"
  artifacts:
    - path: "README.md"
      provides: "Complete product identity document"
      contains: "Originally inspired by"
  key_links:
    - from: "README.md"
      to: "USAGE.md"
      via: "markdown link"
      pattern: "\\[USAGE.md\\]"
    - from: "README.md"
      to: "INSTALL.md"
      via: "markdown link"
      pattern: "\\[INSTALL.md\\]"
---

<objective>
Rewrite README.md to present Serena as a universal code intelligence platform for coding agents -- leading with what it does (40+ MCP tools across 52 languages), not what it's written in. Restructure sections per D-06, add TOC, move quick start up, add RepoMap and v1.7 highlights sections, clean up legacy framing.

Purpose: README is the primary product identity document. It must accurately reflect all capabilities through v1.7 and position Serena as a standalone product. The user objects to "Go-native" as primary framing -- lead with user value, mention single-binary Go architecture as a technical advantage.

Output: Fully rewritten README.md with new section order, new content sections, and accurate product positioning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-readme-rewrite/39-RESEARCH.md
@.planning/phases/39-readme-rewrite/39-CONTEXT.md
@.planning/phases/39-readme-rewrite/39-PATTERNS.md
@.planning/phases/39-readme-rewrite/39-01-SUMMARY.md

<interfaces>
From internal/cli/setup.go (valid clients and flags for quick start):
Valid clients: claude-code, vscode, jetbrains, claude-desktop, gemini-cli, generic
Key flags: --global, --uninstall, --skip-install, --dry-run, --output, --no-hooks

From internal/skill/repomap/skill.go (RepoMap tools):
- get_repo_map: Structural overview via tree-sitter tag extraction + PageRank ranking; token-budget-aware
- get_context: Task-focused context retrieval; given files, returns ranked symbols and definitions

From internal/fuzzy/ (fuzzy editing):
- fuzzy_edit: 4-strategy cascade: exact, whitespace-normalized, indentation-flexible matching

From internal/mcp/suggest.go (smart errors):
- "Did you mean?" suggestions via Levenshtein distance on parameter names and enum values

From internal/mcp/registry.go (progressive descriptions):
- Tools expose short descriptions for listing, full descriptions on demand via get_tool_help

From internal/mcp/lazy_init.go (lazy init):
- Workspace initialization deferred until first tool call; no upfront indexing delay

From internal/kernel/health/ (health tool):
- get_health: Runtime health status of language servers and worker pool
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure README.md with new section order and content</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content -- Plan 01 will have regenerated tables, read the updated version)
    - USAGE.md (style reference and cross-link target)
    - INSTALL.md (cross-link target for manual configs)
    - .planning/phases/39-readme-rewrite/39-CONTEXT.md (locked decisions D-01 through D-13)
    - .planning/phases/39-readme-rewrite/39-RESEARCH.md (verified tool inventory, feature details)
    - .planning/phases/39-readme-rewrite/39-PATTERNS.md (exact patterns to follow)
  </read_first>
  <action>
Rewrite README.md following the locked section order from D-06. The full target structure is:

**1. Hero section** (per D-01, D-02):
- Keep the existing logo and tagline HTML exactly as-is:
```html
<p align="center" style="text-align:center;">
  <img src="resources/serena-logo.svg#gh-light-mode-only" style="width:500px">
  <img src="resources/serena-logo-dark-mode.svg#gh-dark-mode-only" style="width:500px">
</p>

<h3 align="center">
    Serena is the IDE for your coding agent.
</h3>
```
- Add a subtitle line after the closing `</h3>` (per D-02, Claude's discretion on wording). Use something like:
```html
<p align="center">Code intelligence platform for MCP &mdash; 40+ tools across 52 languages.</p>
```
- Keep the 3 bullet points but update "35+ MCP tools" to "40+ MCP tools" in the "How Serena Works" section. Rephrase the third bullet to say "Ships as a **single Go binary**" (keeping Go as a technical advantage, not the hero framing). The user objects to "Go-native" as primary positioning -- lead with what Serena does.

**2. Table of Contents** (per D-05):
Add a markdown TOC with anchor links to all major sections:
```markdown
## Table of Contents

- [How Serena Works](#how-serena-works)
- [Key Advantages](#key-advantages-over-file-based-approaches)
- [Quick Start](#quick-start)
- [Key Features](#key-features)
- [RepoMap](#repomap)
- [Production & Observability](#production--observability)
- [Languages](#programming-language-support)
- [Tools](#features)
- [Architecture](#architecture)
- [Acknowledgements](#acknowledgements)
```

**3. How Serena Works** (existing, update tool count):
- Change "35+ MCP tools" to "40+ MCP tools"
- Rest stays the same

**4. Key Advantages table** (existing, no changes)

**5. Quick Start** (per D-07, D-11):
Move this section up to right after the advantages table. Restructure to lead with `serena setup`:
```markdown
## Quick Start

### Install

```bash
go install github.com/postfix/serena/cmd/serena@latest
```

Or build from source:

```bash
git clone https://github.com/postfix/serena.git
cd serena
go build ./cmd/serena
```

### Configure Your Client

```bash
serena setup claude-code    # Claude Code
serena setup vscode         # VS Code / Cursor
serena setup jetbrains      # JetBrains IDEs
serena setup gemini-cli     # Gemini CLI
serena setup claude-desktop # Claude Desktop
```

Add `--global` for user-wide registration. Run `serena setup --help` for all options.

Serena uses **lazy initialization** -- workspaces are configured on first tool call, so there is no upfront indexing delay.
```

Then add a collapsed `<details>` block for manual JSON configs:
```html
<details>
<summary>Manual configuration (without serena setup)</summary>

[Keep the existing Claude Code, Codex, IDE Assistant JSON blocks and HTTP mode section]

</details>
```

Keep the "Select a Profile" subsection after the configs.

**6. Key Features** (per D-13, NEW section):
```markdown
## Key Features

| Feature | Description |
|---------|-------------|
| **Fuzzy Editing** | 4-strategy cascade (exact, whitespace-normalized, indentation-flexible) handles imprecise LLM-generated edits |
| **Smart Errors** | "Did you mean?" suggestions via Levenshtein distance on misspelled parameter names and enum values |
| **Progressive Descriptions** | Tools expose short descriptions for listing; full documentation available on demand via `get_tool_help` |
| **Health Monitoring** | `get_health` reports runtime status of language servers and worker pool |
| **Lazy Initialization** | Workspaces initialize on first tool call -- no upfront indexing delay |
| **Setup CLI** | One-command client registration: `serena setup claude-code` with language detection and health check |
```

**7. RepoMap** (per D-12, NEW section):
```markdown
## RepoMap

Serena includes a structural code intelligence engine for understanding repository layout and finding task-relevant context:

- **`get_repo_map`** -- Generates a structural overview of the repository using tree-sitter tag extraction and PageRank ranking. Token-budget-aware output scales to any repository size.
- **`get_context`** -- Given a set of files relevant to your task, returns ranked symbols and definitions across the codebase that are most relevant, using dependency graph analysis.
```

**8. Production & Observability** (per D-08, existing, no changes):
Keep the entire Production & Observability section as-is.

**9. Programming Language Support** (per D-09, existing with auto-gen markers):
Keep as-is. The auto-gen markers `<!-- BEGIN LANGUAGES -->` / `<!-- END LANGUAGES -->` MUST be preserved exactly on their own lines. Do not modify content between markers.

**10. Features / Tools table** (per D-09, existing with auto-gen markers):
Keep as-is. The auto-gen markers `<!-- BEGIN TOOLS -->` / `<!-- END TOOLS -->` MUST be preserved exactly on their own lines. Do not modify content between markers.

**11. Architecture** (per D-10, existing, brief):
Keep as-is -- the 4-layer ASCII diagram + 2 paragraphs about daemon/pool + admin listener paragraph. No expansion.

**12. Acknowledgements** (reworded per LEGC-01):
Keep the existing acknowledgement text but ensure it credits the community without implying Python dependency. The current wording is acceptable:
```markdown
## Acknowledgements

A significant part of Serena, especially support for various languages, was contributed by the open source community.
We are very grateful for the many contributors who made this possible and who played an important role in making Serena
what it is today.
```

**13. Legacy footer** (per D-04, LEGC-01):
Add a single one-liner at the very end of the file:
```markdown
---

<sub>Originally inspired by [Python Serena](https://github.com/lks-ai/serena).</sub>
```

**CRITICAL RULES:**
- Do NOT use "port", "rewrite", "based on", or "derived from" anywhere in the README
- Do NOT use "Go-native" as hero/primary framing -- lead with what Serena does for users
- Preserve all auto-gen markers exactly (`<!-- BEGIN TOOLS -->`, `<!-- END TOOLS -->`, `<!-- BEGIN LANGUAGES -->`, `<!-- END LANGUAGES -->`)
- Do NOT hand-edit content between auto-gen markers
- After writing, verify with `go run ./cmd/docgen --check` that markers are intact
  </action>
  <verify>
    <automated>go run ./cmd/docgen --check && grep -c 'serena setup' README.md && ! grep -iE '\b(port|rewrite)\b' README.md | grep -v 'Originally inspired' | grep -v 'BEGIN\|END' || true</automated>
  </verify>
  <acceptance_criteria>
    - README.md line 7 contains "Serena is the IDE for your coding agent"
    - README.md contains a `<p align="center">` subtitle line with "40+" and "52 languages"
    - README.md contains "## Table of Contents"
    - README.md contains "## Quick Start" appearing BEFORE "## Key Features" and "## Programming Language Support"
    - README.md contains `serena setup claude-code` in the Quick Start section
    - README.md contains "Lazy Initialization" or "lazy initialization"
    - README.md contains "## Key Features" with rows for Fuzzy Editing, Smart Errors, Progressive Descriptions, Health Monitoring, Lazy Initialization, Setup CLI
    - README.md contains "## RepoMap" with mentions of `get_repo_map` and `get_context`
    - README.md contains "## Production & Observability"
    - README.md contains `<!-- BEGIN TOOLS -->` and `<!-- END TOOLS -->` on their own lines
    - README.md contains `<!-- BEGIN LANGUAGES -->` and `<!-- END LANGUAGES -->` on their own lines
    - README.md contains "Originally inspired by" as a footer one-liner
    - README.md does NOT contain the words "port" or "rewrite" in product context (grep -iw check)
    - README.md contains "40+ MCP tools" (not "35+")
    - `go run ./cmd/docgen --check` exits 0
  </acceptance_criteria>
  <done>README.md fully restructured per D-06 section order, all 13 locked decisions implemented, no "port"/"rewrite" language, auto-gen markers intact, docgen --check passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- documentation only, no runtime code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02 | T (Tampering) | README.md auto-gen markers | mitigate | docgen --check verifies markers intact after every edit |
</threat_model>

<verification>
- `go run ./cmd/docgen --check` passes (auto-gen markers intact, tables unchanged)
- `grep -c 'serena setup' README.md` returns >= 5 (setup commands in quick start)
- `grep -c 'Originally inspired' README.md` returns exactly 1
- `grep -iwE 'port|rewrite' README.md` returns 0 matches in product context
- `grep -c '## Table of Contents' README.md` returns 1
- `grep -c '## RepoMap' README.md` returns 1
- `grep -c '## Key Features' README.md` returns 1
- Quick Start section appears before Language table (by line number)
</verification>

<success_criteria>
- README presents Serena as a universal code intelligence platform, not a port or Go-specific product
- All 13 locked decisions (D-01 through D-13) implemented
- Section order matches D-06 specification exactly
- Quick start leads with `serena setup <client>` per D-07/D-11
- RepoMap subsection present per D-12
- v1.7 highlights section present per D-13
- Legacy one-liner footer per D-04/LEGC-01
- Auto-gen markers preserved, docgen --check passes
</success_criteria>

<output>
After completion, create `.planning/phases/39-readme-rewrite/39-02-SUMMARY.md`
</output>
