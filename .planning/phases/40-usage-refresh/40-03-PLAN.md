---
phase: 40-usage-refresh
plan: 03
type: execute
wave: 1
depends_on: ["40-01", "40-02"]
files_modified: ["USAGE.md"]
autonomous: true
gap_closure: true
requirements: ["USAGE-01", "USAGE-03"]

must_haves:
  truths:
    - "RepoMap documentation uses token_budget parameter name matching source code"
    - "fuzzy_edit code example uses named parameters (path, search, replacement)"
    - "Troubleshooting section contains rust-analyzer rename entry with Symptom/Cause/Workaround"
  artifacts:
    - path: "USAGE.md"
      provides: "Corrected RepoMap parameter names and fuzzy_edit example"
      contains: "token_budget"
    - path: "USAGE.md"
      provides: "rust-analyzer rename troubleshooting entry"
      contains: "rust-analyzer rename fails"
  key_links: []
---

<objective>
Fix three documentation accuracy gaps found during phase 40 verification: wrong parameter name in RepoMap docs (max_tokens vs token_budget), invalid fuzzy_edit code example (positional args vs named params), and missing rust-analyzer rename troubleshooting entry.

Purpose: Agents following USAGE.md will produce correct tool calls and find workarounds for known rust-analyzer limitations.
Output: Corrected USAGE.md with accurate parameter names, valid code examples, and complete troubleshooting coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-usage-refresh/40-01-SUMMARY.md
@.planning/phases/40-usage-refresh/40-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RepoMap parameter name and fuzzy_edit code example</name>
  <files>USAGE.md</files>
  <read_first>
    USAGE.md
    internal/skill/repomap/skill.go
    internal/kernel/fileops/tools.go
  </read_first>
  <action>
Fix two factual inaccuracies in the Feature Guide section of USAGE.md:

**Gap 1 — RepoMap parameter name (lines 153 and 159):**

On line 153, change:
```
Accepts a `max_tokens` parameter (default 4096, max 32768)
```
to:
```
Accepts a `token_budget` parameter (default 4096, max 32768)
```

On line 159, change:
```
get_repo_map(max_tokens=4096)
```
to:
```
get_repo_map(token_budget=4096)
```

The actual parameter name in `internal/skill/repomap/skill.go` is `token_budget` (see `extractTokenBudget` function and tool description). Using `max_tokens` causes silent fallback to default or a smart-error suggestion.

**Gap 2 — fuzzy_edit code example (line 146):**

Replace the current positional-args example:
```
fuzzy_edit("src/handler.go", "func HandleRequest(", "...", "func HandleRequest(ctx context.Context,", "...")
```
with a correct named-parameter example showing ellipsis inside the search/replacement strings:
```
fuzzy_edit(path="src/handler.go", search="func HandleRequest(\n...\n)", replacement="func HandleRequest(ctx context.Context,\n...\n)")
```

The actual tool (defined in `internal/kernel/fileops/tools.go` as `FuzzyEditArgs`) takes exactly 3 named parameters: `path`, `search`, `replacement`. Ellipsis segments (`...` on its own line) are embedded within the search and replacement strings, not passed as separate positional arguments.
  </action>
  <verify>
    <automated>grep -n "token_budget" USAGE.md | head -5 && grep -n "max_tokens" USAGE.md | head -5 && grep -n "fuzzy_edit" USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - USAGE.md contains `token_budget` on the line describing get_repo_map parameter (formerly line 153)
    - USAGE.md contains `get_repo_map(token_budget=4096)` in the code example (formerly line 159)
    - USAGE.md does NOT contain `max_tokens` anywhere in the RepoMap subsection
    - USAGE.md contains `fuzzy_edit(path=` in the code example (formerly line 146)
    - USAGE.md fuzzy_edit example uses exactly 3 named parameters: path, search, replacement
    - USAGE.md fuzzy_edit example does NOT use positional arguments
  </acceptance_criteria>
  <done>RepoMap docs use correct `token_budget` parameter name; fuzzy_edit example uses correct named-parameter form with path, search, replacement</done>
</task>

<task type="auto">
  <name>Task 2: Add rust-analyzer rename troubleshooting entry</name>
  <files>USAGE.md</files>
  <read_first>
    USAGE.md
    test/integration/rust_test.go
    .planning/phases/40-usage-refresh/40-PATTERNS.md
  </read_first>
  <action>
Add a new troubleshooting entry for the rust-analyzer rename limitation. Insert it in the Troubleshooting section, after the "gopls version incompatibility with Go 1.25" entry (after line 529) and before any subsequent section.

The entry must follow the existing Symptom/Cause/Workaround pattern established by the jdtls and gopls entries. Use this exact content:

```markdown
### rust-analyzer rename fails in fresh workspaces

**Symptom:** `rename_symbol` returns `"internal: rename (No references found at position)"` when targeting a Rust symbol, even though `get_hover_info`, `find_references`, and `search_symbols` all work correctly at the same position.

**Cause:** rust-analyzer (tested with v1.90) has a known limitation where `textDocument/rename` and `textDocument/prepareRename` return "No references found at position" in freshly-opened workspaces. Other LSP operations (`textDocument/hover`, `textDocument/references`, `workspace/symbol`) work correctly at the same position, indicating the issue is specific to the rename protocol handler's workspace readiness check.

**Workaround:** Use `replace_symbol_body` (tree-sitter-based) instead of `rename_symbol` (LSP-based) for Rust symbol renaming. The `replace_symbol_body` tool uses tree-sitter parsing rather than LSP rename, bypassing the rust-analyzer limitation entirely.
```

This documents the known issue referenced in `test/integration/rust_test.go` (skipped test with message: "rust-analyzer 1.90 returns 'No references found at position' for textDocument/rename in temp workspaces") and satisfies roadmap SC-3.
  </action>
  <verify>
    <automated>grep -c "rust-analyzer rename" USAGE.md && grep -c "No references found at position" USAGE.md && grep -c "replace_symbol_body" USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - USAGE.md contains heading "### rust-analyzer rename fails in fresh workspaces"
    - USAGE.md contains "No references found at position" in the Symptom text
    - USAGE.md contains "replace_symbol_body" as the workaround
    - The entry appears after the gopls troubleshooting entry
    - The entry uses bold **Symptom:**/**Cause:**/**Workaround:** labels
  </acceptance_criteria>
  <done>rust-analyzer rename troubleshooting entry exists in USAGE.md with Symptom/Cause/Workaround pattern documenting the known limitation and tree-sitter-based workaround</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- documentation-only changes with no code execution paths.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | I (Info Disclosure) | USAGE.md | accept | Documentation is public; no secrets or internal-only information exposed |
</threat_model>

<verification>
After both tasks complete:
1. `grep "max_tokens" USAGE.md` returns no matches in the RepoMap section
2. `grep "token_budget" USAGE.md` returns matches on the parameter description and code example lines
3. `grep "fuzzy_edit(path=" USAGE.md` returns match for the corrected named-parameter example
4. `grep "rust-analyzer rename" USAGE.md` returns match for the new troubleshooting heading
5. `go vet ./...` passes (no code changes, but confirms repo health)
</verification>

<success_criteria>
All three verification gaps from 40-VERIFICATION.md are closed:
- RepoMap parameter name matches source code (`token_budget` not `max_tokens`)
- fuzzy_edit example uses valid named parameters (`path`, `search`, `replacement`)
- rust-analyzer rename troubleshooting entry exists with Symptom/Cause/Workaround pattern
</success_criteria>

<output>
After completion, create `.planning/phases/40-usage-refresh/40-03-SUMMARY.md`
</output>
