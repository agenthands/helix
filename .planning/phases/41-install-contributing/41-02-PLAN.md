---
phase: 41-install-contributing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
autonomous: true
requirements:
  - CONT-01
  - CONT-02

must_haves:
  truths:
    - "A new contributor can find the complete project structure with all current internal/ packages"
    - "The test section documents the oracle test hierarchy (6 layers) and the test harness package"
    - "Tool counts are accurate (fileops=7, not 6)"
    - "Integration test, benchmark, and CI gate sections are current"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Complete contributor guide with current project structure and test harness"
      contains: "test/oracle/"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "test/oracle/"
      via: "oracle test hierarchy documentation"
      pattern: "protocol.*contract.*runtime.*scenario.*llm.*judge"
---

<objective>
Update CONTRIBUTING.md to reflect the current Go codebase structure through v1.7: add missing packages to the project structure listing, correct tool counts, and document the full oracle test hierarchy alongside the existing integration test and benchmark sections.

Purpose: New contributors should understand the complete project layout, know how to run all test layers, and follow accurate guides for adding tools and languages.
Output: Updated CONTRIBUTING.md with current project structure and test documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-install-contributing/41-RESEARCH.md
@.planning/phases/41-install-contributing/41-PATTERNS.md

<interfaces>
<!-- Verified package listing from RESEARCH.md: -->
<!-- Missing from current CONTRIBUTING.md: internal/cli/, internal/errors/, internal/fuzzy/, -->
<!-- internal/kernel/health/, internal/kernel/help/, internal/repomap/, internal/skill/repomap/, -->
<!-- internal/treesitter/, internal/workspace/ -->

<!-- Corrected tool counts: -->
<!-- kernel/symbols: 9, kernel/edit: 6, kernel/fileops: 7 (NOT 6), kernel/diag: 3 -->
<!-- kernel/health: 1 (get_health), kernel/help: 1 (get_tool_help) -->
<!-- skill/memory: 7, skill/workflow: 2, skill/repomap: 2 -->
<!-- Total: ~38 tools -->

<!-- Oracle test hierarchy (test/oracle/): -->
<!-- protocol: MCP handshake, reconnect, session isolation (5 test files) -->
<!-- contract: schema validation, golden output, error contracts (4 test files) -->
<!-- runtime: degraded start, pool stress, shutdown, deferred errors (4 test files) -->
<!-- scenario: 20 multi-language end-to-end scenario tests -->
<!-- llm: LLM-driven disambiguation, interpretation (3 test files) -->
<!-- judge: automated scoring with rubrics (1 test file) -->

<!-- Test harness (test/harness/): -->
<!-- runner.go, tools.go, golden.go, fixture.go, doc.go, runner_test.go -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CONTRIBUTING.md project structure and test documentation</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (current state -- 147 lines, identify exact sections to update)
    - CLAUDE.md (authoritative architecture description for cross-reference)
    - .planning/phases/41-install-contributing/41-RESEARCH.md (verified package listing, tool counts, oracle hierarchy)
    - .planning/phases/41-install-contributing/41-PATTERNS.md (structural patterns to follow)
  </read_first>
  <action>
Update CONTRIBUTING.md preserving the existing structure (opening, scope, prerequisites, dev commands, project structure, tests, adding tool/language guides, benchmark CI gate, legacy). Make these specific changes:

**1. Project Structure section -- add missing packages (per D-11, D-12, D-13):**

Replace the current project structure listing (lines 42-63) with the complete verified listing. Organize by layer with all packages:

```
## Project Structure

Serena uses a 4-layer architecture shipping as a single Go binary:

- `cmd/serena/` -- CLI entry point
- `internal/mcp/` -- MCP runtime, smart error suggestions, lazy workspace init (Layer 0)
- `internal/daemon/` -- Persistent supervisor daemon (Layer 0)
- `internal/forwarder/` -- Stdio-to-gRPC proxy (Layer 0)
- `internal/cli/` -- CLI commands: setup, status, activate, deactivate, nudge
- `internal/kernel/` -- Code intelligence kernel (Layer 1)
- `internal/kernel/lspool/` -- LS worker pool (share-until-dirty, adaptive TTL, circuit breaking)
- `internal/kernel/symbols/` -- 9 symbol retrieval tools
- `internal/kernel/edit/` -- 6 symbol editing tools (tree-sitter body surgery)
- `internal/kernel/fileops/` -- 7 file operation tools (includes fuzzy_edit)
- `internal/kernel/diag/` -- 3 diagnostic tools
- `internal/kernel/health/` -- get_health tool
- `internal/kernel/help/` -- get_tool_help tool
- `internal/kernel/jsonrpc/` -- Custom JSON-RPC 2.0 codec for LS communication
- `internal/fuzzy/` -- Fuzzy editing strategies (whitespace-normalized, indentation-flexible)
- `internal/repomap/` -- RepoMap subsystem (PageRank-based context selection)
- `internal/treesitter/` -- Tree-sitter grammar integration
- `internal/skill/` -- Skill plugin system, Caddy-style init() registration (Layer 2)
- `internal/skill/memory/` -- 7 memory tools
- `internal/skill/repomap/` -- 2 RepoMap tools (get_repo_map, get_context)
- `internal/skill/workflow/` -- 2 workflow tools (onboard_project, prepare_for_new_conversation)
- `internal/langregistry/` -- 52-language registry with YAML override (Layer 2)
- `internal/memory/` -- Memory store, FTS5 index, fsnotify watcher
- `internal/profile/` -- 5 agent profiles (Layer 3)
- `internal/config/` -- 4-layer configuration (Layer 3)
- `internal/errors/` -- Structured error types
- `internal/obs/` -- Observability (metrics, tracing, admin listener)
- `internal/degrade/` -- Graceful degradation
- `internal/workspace/` -- Workspace key and state
- `api/proto/serena/v1/` -- gRPC IPC definitions
- `protocol/gen/` -- Generated LSP 3.17 types
- `test/harness/` -- Test harness: Runner, tool helpers, golden file comparison, fixtures
- `test/oracle/` -- Oracle test suite (6 layers: protocol, contract, runtime, scenario, llm, judge)
- `test/integration/` -- MCP round-trip integration tests
- `test/bench/` -- Benchmark suite with baselines
```

Key changes from current:
- `internal/kernel/fileops/` changes from "6 file operation tools" to "7 file operation tools (includes fuzzy_edit)" (per D-12)
- Add 9 missing packages: `internal/cli/`, `internal/errors/`, `internal/fuzzy/`, `internal/kernel/health/`, `internal/kernel/help/`, `internal/kernel/jsonrpc/`, `internal/repomap/`, `internal/skill/repomap/`, `internal/treesitter/`, `internal/workspace/`
- Add `test/harness/` and `test/oracle/` (per D-09 and D-13)
- Update `internal/mcp/` description to mention smart error suggestions and lazy init

**2. Add Oracle Test Suite section (per D-08, D-09) -- insert AFTER the "Running Integration Tests" section:**

```
## Oracle Test Suite

The oracle test suite in `test/oracle/` provides structured verification across 6 layers:

| Layer | Package | Purpose |
|-------|---------|---------|
| protocol | `test/oracle/protocol/` | MCP handshake, reconnect, session isolation, smoke tests, tool listing |
| contract | `test/oracle/contract/` | Schema validation, golden output comparison, error contracts, selectability |
| runtime | `test/oracle/runtime/` | Degraded start, pool stress, shutdown ordering, deferred errors |
| scenario | `test/oracle/scenario/` | Multi-language end-to-end scenarios (Go, Python, TS, Java, Rust, PHP, C++, JS, Swift, and more) |
| llm | `test/oracle/llm/` | LLM-driven disambiguation, interpretation, selection |
| judge | `test/oracle/judge/` | Automated scoring with rubrics and aggregation |

Run all oracle tests:

```sh
go test ./test/oracle/... -v -timeout 300s
```

Run a specific layer:

```sh
go test ./test/oracle/scenario/ -v -timeout 120s
```

The test harness (`test/harness/`) provides shared infrastructure: `Runner` (starts daemon + exercises MCP round-trips), tool call helpers, golden file comparison, and fixture management.
```

**3. Update "Adding a New MCP Tool" section:**

Update the "Choose the right layer" step to include the new kernel subcategories:
```
1. **Choose the right layer:**
   - Kernel tools go in `internal/kernel/{category}/` (symbols, edit, fileops, diag, health, help)
   - Skill tools go in `internal/skill/{name}/`
```

Add step mentioning oracle tests:
```
5. **Add test coverage:** Add integration tests in `test/integration/` and oracle scenario tests in `test/oracle/scenario/` for end-to-end verification.
```

**4. Keep existing sections intact:**
- Opening, Scope of Contributions, Prerequisites, Development Commands -- no changes needed
- Running Integration Tests -- keep as-is (still accurate)
- Running Benchmarks -- keep as-is (still accurate)
- Benchmark CI Gate -- keep as-is (still accurate)
- Adding Language Support -- keep as-is (still accurate)

**5. Legacy Python at bottom (per D-15):**
Keep existing: `The legacy/ directory contains the original Python Serena for reference only. It is not actively developed.`

Use `--` (double dash) for all inline dashes consistently.
  </action>
  <verify>
    <automated>grep -q "7 file operation tools" CONTRIBUTING.md && grep -q "test/oracle/" CONTRIBUTING.md && grep -q "internal/fuzzy/" CONTRIBUTING.md && grep -q "internal/cli/" CONTRIBUTING.md && grep -q "internal/repomap/" CONTRIBUTING.md && grep -q "internal/kernel/health/" CONTRIBUTING.md && grep -q "test/harness/" CONTRIBUTING.md && grep -q "protocol.*contract.*runtime.*scenario.*llm.*judge" CONTRIBUTING.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "7 file operation tools" CONTRIBUTING.md returns a match (D-12 corrected count)
    - grep "6 file operation tools" CONTRIBUTING.md returns NO match (old count removed)
    - grep "internal/cli/" CONTRIBUTING.md returns a match (D-11 missing package added)
    - grep "internal/fuzzy/" CONTRIBUTING.md returns a match (D-11)
    - grep "internal/kernel/health/" CONTRIBUTING.md returns a match (D-11)
    - grep "internal/kernel/help/" CONTRIBUTING.md returns a match (D-11)
    - grep "internal/repomap/" CONTRIBUTING.md returns a match (D-11)
    - grep "internal/skill/repomap/" CONTRIBUTING.md returns a match (D-11)
    - grep "internal/treesitter/" CONTRIBUTING.md returns a match (D-11)
    - grep "internal/workspace/" CONTRIBUTING.md returns a match (D-11)
    - grep "internal/errors/" CONTRIBUTING.md returns a match (D-11)
    - grep "test/oracle/" CONTRIBUTING.md returns a match (D-13)
    - grep "test/harness/" CONTRIBUTING.md returns a match (D-09)
    - grep "Oracle Test Suite" CONTRIBUTING.md returns a match (D-08 oracle hierarchy documented)
    - grep "protocol" CONTRIBUTING.md returns a match in the oracle table context
    - grep "scenario" CONTRIBUTING.md returns a match in the oracle table context
    - grep "Legacy Python" CONTRIBUTING.md returns a match (D-15)
    - The document does NOT reference "progressive.go" (non-existent file per Research pitfall 3)
  </acceptance_criteria>
  <done>CONTRIBUTING.md has complete project structure listing with all current internal/ packages, corrected tool counts (fileops=7), oracle test hierarchy documented with 6 layers, test harness referenced, and no stale/incorrect information.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- documentation-only change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02 | I (Info Disclosure) | CONTRIBUTING.md | accept | No secrets; only public project structure and test commands |
</threat_model>

<verification>
- All 9 missing packages from RESEARCH.md are present in the project structure listing
- Tool count for fileops is 7, not 6
- Oracle test suite section documents all 6 layers with table format
- Test harness package is referenced
- No reference to non-existent progressive.go
- Existing accurate sections (integration tests, benchmarks, CI gate) are preserved
</verification>

<success_criteria>
CONTRIBUTING.md accurately reflects the current Go codebase structure including all internal/ packages through v1.7, with correct tool counts and comprehensive test documentation covering oracle tests, integration tests, and benchmarks.
</success_criteria>

<output>
After completion, create `.planning/phases/41-install-contributing/41-02-SUMMARY.md`
</output>
