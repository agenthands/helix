---
phase: 43-cross-doc-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - README-04
  - CLMD-01
  - CLMD-02
must_haves:
  truths:
    - "README Quick Start enumerates all 7 setup-CLI clients including opencode"
    - "README hero/intro tool count matches CLAUDE.md (41+)"
    - "README Architecture Layer 3 label matches CLAUDE.md exactly (Agent Profiles & Setup)"
    - "README Fuzzy Editing feature description uses canonical strategy names (exact, whitespace-normalized, indentation-flexible, ellipsis-placeholder)"
  artifacts:
    - path: "README.md"
      provides: "Public product identity, Quick Start, Key Features table, Architecture layer block"
      contains: "serena setup opencode"
  key_links:
    - from: "README.md Quick Start"
      to: "internal/cli/setup_clients.go"
      via: "client enumeration"
      pattern: "serena setup (claude-code|vscode|jetbrains|gemini-cli|claude-desktop|opencode|generic)"
---

<objective>
Align README.md with source-of-truth code and CLAUDE.md on:
1. Setup-CLI client count and enumeration (7 clients, include opencode, drop "/ Cursor" parenthetical)
2. Tool count hero/intro figure (40+ → 41+ to match CLAUDE.md)
3. Architecture Layer 3 label ("Agent Profiles" → "Agent Profiles & Setup")
4. Fuzzy Editing strategy vocabulary (unify on canonical names from CHANGELOG / strategies.go)

Closes: F-01 (critical), F-03, F-08, F-10 for README.md.

Purpose: README is the primary product-identity doc; drift here undermines every other doc's trust. Canonical client list and tool count must match the code.

Output: Updated README.md with 7-client Quick Start, 41+ tool count, and Architecture block matching CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.8-INTEGRATION-CHECK.md
@CLAUDE.md

<interfaces>
<!-- Source of truth: internal/cli/setup_clients.go:37-46 -->
Canonical 7 clients (in registration order):
  claude-code, vscode, jetbrains, claude-desktop, gemini-cli, opencode, generic

Canonical fuzzy strategy names (from internal/fuzzy/strategies.go + CHANGELOG.md:43):
  exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder

Canonical tool count (from CLAUDE.md:33,117 + auto-gen table): 41+
Canonical Layer 3 label (from CLAUDE.md:74): "Agent Profiles & Setup"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix README Quick Start client list (add opencode, generic; drop Cursor parenthetical)</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 70-90, 145-160, 315-327 — Quick Start, Key Features, Architecture)
    - internal/cli/setup_clients.go (lines 37-46 — source of truth for 7 registrars)
  </read_first>
  <action>
    In README.md Quick Start code block at lines 76-82, replace the existing 5-client block with the canonical 7-client block. Target string replacements:

    1. Replace the line `serena setup vscode         # VS Code / Cursor` with `serena setup vscode         # VS Code` (drop "/ Cursor" — no cursor registrar exists in setup_clients.go).
    2. After the `serena setup claude-desktop # Claude Desktop` line, insert two new lines:
       `serena setup opencode       # OpenCode`
       `serena setup generic        # Generic MCP client`

    Resulting block (exact whitespace):
    ```
    serena setup claude-code    # Claude Code
    serena setup vscode         # VS Code
    serena setup jetbrains      # JetBrains IDEs
    serena setup gemini-cli     # Gemini CLI
    serena setup claude-desktop # Claude Desktop
    serena setup opencode       # OpenCode
    serena setup generic        # Generic MCP client
    ```
  </action>
  <verify>
    <automated>grep -c "serena setup opencode" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "serena setup opencode" README.md` returns ≥1
    - `grep -c "serena setup generic" README.md` returns ≥1
    - `grep "VS Code / Cursor" README.md` returns nothing (exit 1)
    - `grep -E "serena setup (claude-code|vscode|jetbrains|claude-desktop|gemini-cli|opencode|generic)" README.md | wc -l` returns ≥7
  </acceptance_criteria>
  <done>Quick Start block enumerates all 7 canonical clients in registration order; Cursor parenthetical removed.</done>
</task>

<task type="auto">
  <name>Task 2: Align README tool count on "41+" and Layer 3 label on "Agent Profiles & Setup"</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 10, 38, 319-327 — hero, intro, Architecture block)
    - CLAUDE.md (lines 33, 74, 117 — canonical "41+" and "Layer 3: Agent Profiles & Setup")
  </read_first>
  <rationale>
    ROADMAP success criterion 4 reads "CLAUDE.md Layer 3 label matches README". The chosen canonical direction is `Agent Profiles & Setup` (CLAUDE.md's form), and README is the doc being updated to match it — NOT the other way around. Reason: the setup CLI shipped in v1.7 and is a genuine Layer 3 component (see `internal/cli/setup*.go`), so "Agent Profiles & Setup" is the factually accurate label for what Layer 3 contains today. Treating CLAUDE.md as truth here is consistent with the rest of this phase (plans 02–05 all canonicalize on CLAUDE.md-aligned vocabulary).
  </rationale>
  <action>
    Note on direction: CLAUDE.md is the truth source for the Layer 3 label in this phase. The setup CLI shipped in v1.7 is a real Layer 3 component, so "Agent Profiles & Setup" accurately describes the layer; README is updated to match CLAUDE.md, not the reverse. (See <rationale> block above.)

    Three literal string replacements in README.md:

    1. Line 10 (hero subtitle): Replace `40+ tools across 52 languages` with `41+ tools across 52 languages`.
    2. Line 38 (intro paragraph): Replace `Serena provides 40+ MCP tools for coding workflows` with `Serena provides 41+ MCP tools for coding workflows`.
    3. Line 326 (Architecture 4-layer block, Layer 3 line): Replace `Agent Profiles (5 profiles, 4 modes, token budget, layered config)` with `Agent Profiles & Setup (5 profiles, 4 modes, token budget, layered config, setup CLI)`.

    Do NOT touch CHANGELOG-historical references like "38+" (those are milestone-historical counts and remain correct).
  </action>
  <verify>
    <automated>grep -c "41+ tools across 52 languages\|41+ MCP tools" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "40+ tools across 52 languages" README.md` returns nothing
    - `grep "40+ MCP tools for coding workflows" README.md` returns nothing
    - `grep -c "41+ MCP tools\|41+ tools across" README.md` returns ≥2
    - `grep "Agent Profiles & Setup" README.md` returns ≥1
    - `grep -E "^Agent Profiles \(5 profiles" README.md` returns nothing (old line gone)
  </acceptance_criteria>
  <done>Hero + intro say "41+"; Architecture Layer 3 line reads "Agent Profiles & Setup (5 profiles, 4 modes, token budget, layered config, setup CLI)".</done>
</task>

<task type="auto">
  <name>Task 3: Unify README fuzzy-editing feature description on canonical strategy names</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 149-153 — Key Features table Fuzzy Editing row)
    - CHANGELOG.md (line 43 — canonical naming "exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder")
    - internal/fuzzy/strategies.go (lines 1-100 — 4 sweep functions: sweepExact, sweepWhitespace, sweepIndentFlex, plus ellipsis handling in match.go)
  </read_first>
  <action>
    In README.md line 153 (Key Features table Fuzzy Editing row), replace the current parenthetical:
      `4-strategy cascade (exact, whitespace-normalized, indentation-flexible)`
    with the canonical 4-strategy enumeration:
      `4-strategy cascade (exact, whitespace-normalized, indentation-flexible, ellipsis-placeholder)`

    Exact target string to change (current line 153):
      `| **Fuzzy Editing** | 4-strategy cascade (exact, whitespace-normalized, indentation-flexible) handles imprecise LLM-generated edits |`
    New line:
      `| **Fuzzy Editing** | 4-strategy cascade (exact, whitespace-normalized, indentation-flexible, ellipsis-placeholder) handles imprecise LLM-generated edits |`
  </action>
  <verify>
    <automated>grep -c "exact, whitespace-normalized, indentation-flexible, ellipsis-placeholder" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ellipsis-placeholder" README.md` returns ≥1
    - `grep "exact, whitespace-normalized, indentation-flexible)" README.md` returns nothing (old 3-name form gone)
    - `grep "IndentFlex\|Failed" README.md` returns nothing (USAGE vocabulary not in README)
  </acceptance_criteria>
  <done>README Key Features row lists all 4 canonical strategy names matching CHANGELOG and CLAUDE.md canonical form.</done>
</task>

</tasks>

<verification>
After all tasks complete, run:
```bash
grep -c "serena setup opencode" README.md                                  # ≥1
grep -c "serena setup generic" README.md                                   # ≥1
grep "VS Code / Cursor" README.md                                          # empty
grep "40+ MCP tools\|40+ tools across" README.md                           # empty
grep -c "41+" README.md                                                    # ≥2
grep "Agent Profiles & Setup" README.md                                    # ≥1
grep -c "ellipsis-placeholder" README.md                                   # ≥1
```
All 7 checks must pass.
</verification>

<success_criteria>
README.md enumerates 7 setup clients including opencode; hero/intro use "41+"; Architecture Layer 3 label matches CLAUDE.md; Fuzzy Editing row lists all 4 canonical strategy names.
</success_criteria>

<output>
After completion, create `.planning/phases/43-cross-doc-sync/43-01-SUMMARY.md`.
</output>
