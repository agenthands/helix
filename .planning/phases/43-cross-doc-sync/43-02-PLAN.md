---
phase: 43-cross-doc-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - INSTALL.md
autonomous: true
requirements:
  - INST-01
  - INST-02
must_haves:
  truths:
    - "INSTALL.md Quick Start enumerates all 7 setup-CLI clients including opencode and generic"
    - "INSTALL.md 'Supported clients' enumeration sentence lists all 7 registrars"
  artifacts:
    - path: "INSTALL.md"
      provides: "Install guide Quick Start and manual-config reference"
      contains: "serena setup opencode"
  key_links:
    - from: "INSTALL.md Quick Start"
      to: "internal/cli/setup_clients.go"
      via: "canonical client enumeration"
      pattern: "serena setup opencode"
---

<objective>
Align INSTALL.md Quick Start and client enumeration with the 7-registrar source of truth in `internal/cli/setup_clients.go`. Close F-01 for INSTALL.md.

Purpose: INSTALL.md is the canonical install reference; Quick Start drift under-advertises OpenCode/generic support even though manual-config sections for them already exist in the same file.

Output: Updated INSTALL.md Quick Start block with 7 clients, matching README Quick Start exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.8-INTEGRATION-CHECK.md

<interfaces>
<!-- Source of truth: internal/cli/setup_clients.go:37-46 -->
Canonical 7 clients (registration order):
  claude-code, vscode, jetbrains, claude-desktop, gemini-cli, opencode, generic
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add opencode and generic to INSTALL Quick Start block</name>
  <files>INSTALL.md</files>
  <read_first>
    - INSTALL.md (lines 25-50 — Quick Start section)
    - internal/cli/setup_clients.go (lines 37-46 — source of truth)
    - README.md (lines 76-82 — aligned reference from Plan 01)
  </read_first>
  <action>
    In INSTALL.md lines 35-41 (Quick Start code block), the current block is:
    ```
    serena setup claude-code    # Claude Code
    serena setup vscode         # VS Code
    serena setup jetbrains      # JetBrains IDEs
    serena setup gemini-cli     # Gemini CLI
    serena setup claude-desktop # Claude Desktop
    ```

    Append two new lines after the `claude-desktop` line so it becomes:
    ```
    serena setup claude-code    # Claude Code
    serena setup vscode         # VS Code
    serena setup jetbrains      # JetBrains IDEs
    serena setup gemini-cli     # Gemini CLI
    serena setup claude-desktop # Claude Desktop
    serena setup opencode       # OpenCode
    serena setup generic        # Generic MCP client
    ```

    Column alignment: preserve the existing two-space gap before `#`.
  </action>
  <verify>
    <automated>grep -c "serena setup opencode" INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "serena setup opencode" INSTALL.md` returns ≥2 (new Quick Start line + existing mentions at line 149)
    - `grep -c "serena setup generic" INSTALL.md` returns ≥2 (new Quick Start line + existing mention at line 214)
    - `grep -E "^serena setup (claude-code|vscode|jetbrains|claude-desktop|gemini-cli|opencode|generic)" INSTALL.md | wc -l` returns ≥7
  </acceptance_criteria>
  <done>Quick Start block contains all 7 canonical `serena setup <client>` lines.</done>
</task>

<task type="auto">
  <name>Task 2: Update INSTALL "Supported clients" enumeration sentence to include opencode</name>
  <files>INSTALL.md</files>
  <read_first>
    - INSTALL.md (grep for "Supported clients:" — find each enumeration sentence)
    - internal/cli/setup_clients.go (lines 37-46)
  </read_first>
  <action>
    Search INSTALL.md for any "Supported clients:" sentence or similar prose enumeration that lists setup-CLI targets. If a sentence exists enumerating `claude-code, vscode, jetbrains, claude-desktop, gemini-cli, generic` (6 clients, missing opencode), replace with the canonical 7-client list.

    If the file contains a USAGE-style sentence like:
      `Supported clients: \`claude-code\`, \`vscode\`, \`jetbrains\`, \`claude-desktop\`, \`gemini-cli\`, \`generic\`.`
    replace with:
      `Supported clients: \`claude-code\`, \`vscode\`, \`jetbrains\`, \`claude-desktop\`, \`gemini-cli\`, \`opencode\`, \`generic\`.`

    Use `grep -n "Supported clients\|supported clients" INSTALL.md` to locate candidates. If zero matches, this task is a no-op — mark done after documenting the grep evidence in SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'if grep -qi "supported clients:" INSTALL.md; then grep -i "supported clients:" INSTALL.md | grep -q "opencode"; else true; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Either: no "Supported clients:" prose sentence exists in INSTALL.md (command exits 0, no-op acknowledged in SUMMARY)
    - OR: every "Supported clients:" sentence includes `opencode`
    - `grep -i "supported clients:" INSTALL.md | grep -v opencode` returns nothing when any such sentence exists
    - **No-op branch explicit assertion:** `bash -c 'grep -qi "supported clients:" INSTALL.md || echo NO_OP_CONFIRMED'` prints `NO_OP_CONFIRMED` when no such sentence exists, OR (if any sentence exists) `grep -ci "supported clients:" INSTALL.md` equals the count reported by `grep -ci "supported clients:.*opencode" INSTALL.md` — i.e. every match contains `opencode`. Exactly one of these two states MUST hold; SUMMARY must record which branch was taken.
  </acceptance_criteria>
  <done>Every client-enumeration prose sentence in INSTALL.md includes opencode, or documented absence thereof (no-op branch confirmed via the explicit assertion above).</done>
</task>

</tasks>

<verification>
```bash
grep -c "serena setup opencode" INSTALL.md    # ≥2
grep -c "serena setup generic" INSTALL.md     # ≥2
grep -E "^serena setup " INSTALL.md | wc -l   # ≥7
```
</verification>

<success_criteria>
INSTALL.md Quick Start enumerates all 7 canonical clients and every prose enumeration sentence includes opencode.
</success_criteria>

<output>
After completion, create `.planning/phases/43-cross-doc-sync/43-02-SUMMARY.md`.
</output>
