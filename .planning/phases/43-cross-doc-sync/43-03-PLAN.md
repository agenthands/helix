---
phase: 43-cross-doc-sync
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - USAGE.md
autonomous: true
requirements:
  - USAGE-02
must_haves:
  truths:
    - "USAGE.md Setup CLI section enumerates all 7 canonical clients including opencode"
    - "USAGE.md Fuzzy Editing section uses canonical 4-strategy names (exact, whitespace-normalized, indentation-flexible, ellipsis-placeholder)"
  artifacts:
    - path: "USAGE.md"
      provides: "Feature guide and tutorial reference"
      contains: "opencode"
  key_links:
    - from: "USAGE.md Setup CLI section"
      to: "internal/cli/setup_clients.go"
      via: "canonical client enumeration"
      pattern: "opencode"
---

<objective>
Align USAGE.md with canonical client list (7 including opencode) and canonical fuzzy-strategy naming. Closes F-01 and F-08 for USAGE.md.

Purpose: USAGE.md documents user-facing features; its client list and fuzzy-strategy names must match code and other docs so agents reading USAGE can't learn wrong vocabulary.

Output: Updated USAGE.md with opencode in Supported clients, and canonical strategy names replacing the "Exact / Whitespace / IndentFlex / Failed" vocabulary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.8-INTEGRATION-CHECK.md

<interfaces>
<!-- Canonical 7 clients: claude-code, vscode, jetbrains, claude-desktop, gemini-cli, opencode, generic -->
<!-- Canonical fuzzy strategy names (from CHANGELOG.md:43 + strategies.go):
     exact match, whitespace-normalized, indentation-flexible, ellipsis-placeholder
     Note: "Failed" is NOT a strategy — it's a fallback behavior when all strategies miss. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add opencode to USAGE "Supported clients" list (line 173 and line 25 context)</name>
  <files>USAGE.md</files>
  <read_first>
    - USAGE.md (lines 20-30 and 165-180 — Tutorial 1 setup description and Setup CLI feature section)
    - internal/cli/setup_clients.go (lines 37-46)
  </read_first>
  <action>
    Two string replacements in USAGE.md:

    1. Line 25 (Tutorial 1 — "Supported clients" sentence). Current text:
       `Supported clients: \`claude-code\`, \`vscode\`, \`jetbrains\`, \`claude-desktop\`, \`gemini-cli\`, \`generic\`.`
       Replace with:
       `Supported clients: \`claude-code\`, \`vscode\`, \`jetbrains\`, \`claude-desktop\`, \`gemini-cli\`, \`opencode\`, \`generic\`.`

    2. Line 173 (Feature Guide Setup CLI section — second "Supported clients" sentence). Same replacement as above.

    Preserve existing markdown inline-code backticks around each client name.
  </action>
  <verify>
    <automated>grep -c "opencode" USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "opencode" USAGE.md` returns ≥2
    - `grep -E "Supported clients:.*opencode" USAGE.md | wc -l` returns ≥2
    - `grep "Supported clients:" USAGE.md | grep -v opencode` returns nothing (every Supported-clients sentence has opencode)
  </acceptance_criteria>
  <done>Both Supported-clients sentences in USAGE.md include opencode in canonical position (between gemini-cli and generic).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite USAGE fuzzy-strategy paragraph on canonical names</name>
  <files>USAGE.md</files>
  <read_first>
    - USAGE.md (lines 137-148 — Fuzzy Editing section)
    - CHANGELOG.md (line 43 — canonical phrasing)
    - internal/fuzzy/strategies.go (sweepExact, sweepWhitespace, sweepIndentFlex function docs)
  </read_first>
  <action>
    Rewrite USAGE.md line 141 (the fuzzy-strategy enumeration sentence). Current text:

      `Serena's \`fuzzy_edit\` tool matches search blocks against file content using a 4-strategy cascade that tolerates whitespace and indentation differences. The strategies, in order: **Exact** (byte-for-byte match), **Whitespace** (ignores leading/trailing whitespace per line), **IndentFlex** (tabs and spaces interchangeable), and **Failed** (returns a unified-diff showing the nearest match). If multiple matches are found at any strategy level, the tool returns an error asking you to add more context to disambiguate.`

    Replace with the canonical-named version that lists the real 4 strategies (ellipsis-placeholder IS a strategy; "Failed" is NOT — it is the fallback response when all strategies miss):

      `Serena's \`fuzzy_edit\` tool matches search blocks against file content using a 4-strategy cascade that tolerates whitespace and indentation differences. The strategies, in order: **exact match** (byte-for-byte), **whitespace-normalized** (ignores leading/trailing whitespace per line), **indentation-flexible** (tabs and spaces interchangeable at line start), and **ellipsis-placeholder** (allows \`...\` in the search block to skip intermediate content). If all four strategies miss, the tool returns a unified-diff envelope showing the nearest candidate match. If multiple matches are found at any strategy level, the tool returns an error asking you to add more context to disambiguate.`

    Do NOT change line 143 (ellipsis mechanics paragraph) or line 146 (fuzzy_edit code example) — those are already correct.
  </action>
  <verify>
    <automated>grep -c "indentation-flexible" USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "indentation-flexible" USAGE.md` returns ≥1
    - `grep -c "ellipsis-placeholder" USAGE.md` returns ≥1
    - `grep "IndentFlex" USAGE.md` returns nothing (old vocab gone)
    - `grep "\*\*Failed\*\*" USAGE.md` returns nothing (Failed was not a real strategy)
    - `grep "whitespace-normalized" USAGE.md` returns ≥1
  </acceptance_criteria>
  <done>USAGE Fuzzy Editing paragraph uses canonical strategy names matching CHANGELOG.md:43 and CLAUDE.md:62 (after Plan 05 runs).</done>
</task>

</tasks>

<verification>
```bash
grep -c "opencode" USAGE.md                   # ≥2
grep "IndentFlex" USAGE.md                    # empty
grep -c "indentation-flexible" USAGE.md       # ≥1
grep -c "ellipsis-placeholder" USAGE.md       # ≥1
```
</verification>

<success_criteria>
USAGE.md Supported-clients sentences include opencode; Fuzzy Editing paragraph uses canonical 4-strategy names; "IndentFlex"/"Failed" strategy-name vocabulary fully removed.
</success_criteria>

<output>
After completion, create `.planning/phases/43-cross-doc-sync/43-03-SUMMARY.md`.
</output>
