---
phase: 43-cross-doc-sync
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - CLOG-01
  - CLOG-02
must_haves:
  truths:
    - "CHANGELOG v1.7 Setup CLI entry says 7 clients and names opencode explicitly"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release history record"
      contains: "7 clients"
  key_links:
    - from: "CHANGELOG.md v1.7 Setup CLI bullet"
      to: "internal/cli/setup_clients.go"
      via: "client count and enumeration"
      pattern: "7 clients.*OpenCode"
---

<objective>
Correct CHANGELOG v1.7 Setup CLI entry to list 7 clients (adding OpenCode) to match the shipped registry. Closes F-07 and the CHANGELOG component of F-01.

Purpose: CHANGELOG is the canonical release record; claiming "6 clients" when 7 shipped understates the feature and contradicts the code.

Output: Updated CHANGELOG.md v1.7 entry with "7 clients" and OpenCode in the enumeration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.8-INTEGRATION-CHECK.md

<interfaces>
<!-- Source of truth: internal/cli/setup_clients.go:37-46 — 7 registrars since v1.7 ship -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CHANGELOG v1.7 Setup CLI bullet with 7-client enumeration</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-15 — v1.7 header and Setup CLI subsection)
    - internal/cli/setup_clients.go (lines 37-46)
  </read_first>
  <action>
    In CHANGELOG.md line 8, the current bullet reads:

      `- One-command MCP registration: \`serena setup <client>\` for 6 clients — Claude Code, VS Code, JetBrains, Claude Desktop, Gemini CLI, and generic MCP clients`

    Replace with:

      `- One-command MCP registration: \`serena setup <client>\` for 7 clients — Claude Code, VS Code, JetBrains, Claude Desktop, Gemini CLI, OpenCode, and generic MCP clients`

    Only this one line changes. Do NOT touch line 115 (v1.3 INSTALL entry mentioning "OpenCode, Cursor, Gemini CLI, Antigravity" — that is historical manual-config content and remains accurate). Do NOT touch line 193 ("6 file operation tools" under v1.0) — that is historically correct for v1.0 before fuzzy_edit landed in v1.6.
  </action>
  <verify>
    <automated>grep -c "for 7 clients" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "for 7 clients" CHANGELOG.md` returns ≥1
    - `grep "for 6 clients" CHANGELOG.md` returns nothing
    - `grep -E "Gemini CLI, OpenCode, and generic" CHANGELOG.md` returns ≥1
    - `grep "6 file operation tools" CHANGELOG.md` returns ≥1 (historical v1.0 line preserved unchanged)
  </acceptance_criteria>
  <done>CHANGELOG v1.7 Setup CLI bullet says "7 clients" and enumerates OpenCode between Gemini CLI and generic; historical v1.0 file-ops-count line untouched.</done>
</task>

</tasks>

<verification>
```bash
grep "for 6 clients" CHANGELOG.md                               # empty
grep -c "for 7 clients" CHANGELOG.md                            # ≥1
grep -c "Gemini CLI, OpenCode, and generic" CHANGELOG.md        # ≥1
grep -c "6 file operation tools" CHANGELOG.md                   # ≥1 (historical preserved)
```
</verification>

<success_criteria>
CHANGELOG v1.7 Setup CLI entry reflects the shipped 7-client registry without altering historical v1.0/v1.3 entries.
</success_criteria>

<output>
After completion, create `.planning/phases/43-cross-doc-sync/43-04-SUMMARY.md`.
</output>
