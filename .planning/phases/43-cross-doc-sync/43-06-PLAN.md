---
phase: 43-cross-doc-sync
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/setup_clients.go
autonomous: true
requirements:
  - INST-02
must_haves:
  truths:
    - "internal/cli/setup_clients.go:36 comment says 7 registrars, not 6"
  artifacts:
    - path: "internal/cli/setup_clients.go"
      provides: "Source of truth for setup-CLI client registry"
      contains: "all 7 registrars"
  key_links:
    - from: "internal/cli/setup_clients.go:36 comment"
      to: "clientRegistry map at lines 37-46"
      via: "count must match map cardinality"
      pattern: "all 7 registrars"
---

<objective>
Fix the stale code comment at `internal/cli/setup_clients.go:36` that says "all 6 registrars" while the map below it contains 7 entries. Closes the source-side tail of F-01/F-07.

Purpose: Code comments drift first in docs overhauls. This single-line comment is the only documentation inside the source of truth itself — it must match its own map.

Output: One-line comment correction. Build and existing tests must still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v1.8-INTEGRATION-CHECK.md
@CLAUDE.md

<interfaces>
<!-- File: internal/cli/setup_clients.go
     Line 36 (comment): "// clientRegistry returns all 6 registrars keyed by client name."
     Lines 37-46 (map):  claude-code, vscode, jetbrains, claude-desktop, gemini-cli, opencode, generic (7 entries)
     The comment undercounts by one. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update setup_clients.go:36 comment from "6 registrars" to "7 registrars"</name>
  <files>internal/cli/setup_clients.go</files>
  <read_first>
    - internal/cli/setup_clients.go (lines 30-50 — see current comment and the map it describes)
  </read_first>
  <action>
    In `internal/cli/setup_clients.go` line 36, the comment currently reads:

      `// clientRegistry returns all 6 registrars keyed by client name.`

    Replace with:

      `// clientRegistry returns all 7 registrars keyed by client name.`

    This is the ONLY change in this file. Do NOT modify the map at lines 37-46, any other comments, or any other file. The change is a comment-only update — it MUST NOT affect generated code, compile output, or tests.
  </action>
  <verify>
    <automated>go build ./... && grep -c "all 7 registrars keyed by client name" internal/cli/setup_clients.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep "all 6 registrars" internal/cli/setup_clients.go` returns nothing
    - `grep -c "all 7 registrars keyed by client name" internal/cli/setup_clients.go` returns 1
    - `go build ./cmd/serena` exits 0
    - `go vet ./internal/cli/...` exits 0
  </acceptance_criteria>
  <done>Comment at `internal/cli/setup_clients.go:36` says "all 7 registrars keyed by client name." and the package still builds + vets clean.</done>
</task>

</tasks>

<verification>
```bash
grep "all 6 registrars" internal/cli/setup_clients.go     # empty
grep "all 7 registrars" internal/cli/setup_clients.go     # ≥1
go build ./cmd/serena                                     # exit 0
go vet ./internal/cli/...                                 # exit 0
```
</verification>

<success_criteria>
`internal/cli/setup_clients.go:36` comment agrees with its own map (7 registrars); build and vet pass.
</success_criteria>

<output>
After completion, create `.planning/phases/43-cross-doc-sync/43-06-SUMMARY.md`.
</output>
