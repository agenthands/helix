---
phase: 45-polish-crosslinks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - USAGE.md
autonomous: true
requirements:
  - README-03
  - USAGE-03
  - INST-02
tags:
  - docs
  - cross-links
  - readme
  - usage
  - v1.8

must_haves:
  truths:
    - "README manual-config block explicitly names Cursor, Antigravity, VS Code, JetBrains, Claude Desktop, Gemini CLI, and OpenCode and points to INSTALL.md#manual-configuration."
    - "README contains a link to CHANGELOG.md in its footer area (Release Notes)."
    - "USAGE.md contains an INSTALL.md link in its Prerequisites/Setup intro area (first 20 lines)."
    - "USAGE.md lines 531–537 rust-analyzer troubleshooting block is unchanged and still contains v1.90 plus the Symptom/Cause/Workaround triple and replace_symbol_body reference."
  artifacts:
    - path: "README.md"
      provides: "Explicit 7-client manual-config pointer + CHANGELOG footer link"
      contains: "INSTALL.md#manual-configuration"
    - path: "USAGE.md"
      provides: "Early INSTALL.md cross-link + preserved rust-analyzer troubleshooting entry"
      contains: "INSTALL.md"
    - path: ".planning/phases/45-polish-crosslinks/45-VERIFICATION.md"
      provides: "Phase verification record including F-06 read-only confirmation + hash"
      contains: "v1.90"
  key_links:
    - from: "README.md manual-config block"
      to: "INSTALL.md#manual-configuration"
      via: "explicit sentence naming the 7 clients not shown inline"
      pattern: "Cursor.*Antigravity.*VS Code.*JetBrains.*Claude Desktop.*Gemini CLI.*OpenCode"
    - from: "README.md footer"
      to: "CHANGELOG.md"
      via: "Release Notes link"
      pattern: "\\[CHANGELOG\\.md\\]\\(CHANGELOG\\.md\\)"
    - from: "USAGE.md intro"
      to: "INSTALL.md"
      via: "Prerequisites pointer"
      pattern: "\\[INSTALL\\.md\\]\\(INSTALL\\.md\\)"
---

<objective>
Close the three non-blocking v1.8 integration-check findings deferred to Phase 45:

- **F-02** — Replace the vague README manual-config pointer with an explicit 7-client sentence naming Cursor, Antigravity, VS Code, JetBrains, Claude Desktop, Gemini CLI, and OpenCode (per D-02, D-03; D-01 keeps existing JSON blocks unchanged).
- **F-12** — Add README → CHANGELOG.md (Release Notes) and USAGE → INSTALL.md cross-links (per D-04, D-05).
- **F-06** — Read-only confirmation that USAGE.md:531–537 rust-analyzer troubleshooting is still current; no edit expected (per D-07, D-08).

Purpose: Satisfy Phase 45 success criteria (README manual-config pointer upgraded, README→CHANGELOG + USAGE→INSTALL symmetry restored, USAGE rust-analyzer metadata confirmed current) and close REQ IDs README-03, USAGE-03, INST-02.

Output: Two edited markdown files (README.md, USAGE.md) plus a 45-VERIFICATION.md recording the F-06 read-only check and all grep acceptance results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.8-INTEGRATION-CHECK.md
@.planning/phases/45-polish-crosslinks/45-CONTEXT.md
@.planning/phases/45-polish-crosslinks/45-RESEARCH.md

<!-- Decision IDs from 45-CONTEXT.md that this plan implements: -->
<!-- D-01: keep existing 3 JSON examples unchanged -->
<!-- D-02: replace vague pointer with explicit 7-client sentence -->
<!-- D-03: one sentence; no bullets/table -->
<!-- D-04: add README→CHANGELOG "Release Notes" link in footer -->
<!-- D-05: add USAGE→INSTALL link in Prerequisites/Setup intro area -->
<!-- D-06: do NOT add CONTRIBUTING back-links (out of scope) -->
<!-- D-07/D-08: F-06 text review only; no edit expected -->
<!-- D-09: targeted grep verification; no v1.8 integration re-run -->

<interfaces>
<!-- Verified strings/anchors from 45-RESEARCH.md. Use these directly. -->

README.md (current state, 343 lines):
- Line 135 contains the vague pointer to replace:
  `For detailed configuration options, see [INSTALL.md](INSTALL.md).`
- Line 341 is a `---` horizontal rule
- Line 343 is the closing `<sub>` Python-Serena attribution
- No existing CHANGELOG/release-notes reference (grep returns 0)

USAGE.md (current state, 817 lines):
- Line 5: `For installation and feature overview, see [README.md](README.md).`
- Lines 531–537: rust-analyzer block, header at 531, body through 537, must remain unchanged
- No existing INSTALL.md reference (grep returns 0)

INSTALL.md anchor (verified):
- `## Manual Configuration` at INSTALL.md:55 → GFM slug `#manual-configuration`
- Full URL: `INSTALL.md#manual-configuration` (valid, no slug collision)

Exact sentences to write (from CONTEXT.md §Specific Ideas, locked wording):
- D-02 replacement: `For Cursor, Antigravity, VS Code, JetBrains, Claude Desktop, Gemini CLI, and OpenCode — see [INSTALL.md#manual-configuration](INSTALL.md#manual-configuration) for full examples.`
- D-04 new line: `See [CHANGELOG.md](CHANGELOG.md) for release notes.`
- D-05 new line: `If you haven't installed Serena yet, see [INSTALL.md](INSTALL.md) first.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: F-02 — Replace README manual-config pointer with explicit 7-client sentence</name>
  <files>README.md</files>
  <read_first>
    - README.md (full, or at minimum lines 85–145 to locate the current pointer and confirm it still matches `For detailed configuration options, see [INSTALL.md](INSTALL.md).`)
    - .planning/phases/45-polish-crosslinks/45-CONTEXT.md (decisions D-01, D-02, D-03)
    - .planning/phases/45-polish-crosslinks/45-RESEARCH.md §"Exact Edit Targets" and §"Established Patterns"
  </read_first>
  <action>
    Per D-02 (locked wording), replace the single line in README.md whose content is exactly:

    `For detailed configuration options, see [INSTALL.md](INSTALL.md).`

    (Currently at line 135; match by string content, NOT by line number — research noted +1 drift potential.)

    Replace with EXACTLY this sentence (one line, one sentence, no bullet list, no table — per D-03):

    `For Cursor, Antigravity, VS Code, JetBrains, Claude Desktop, Gemini CLI, and OpenCode — see [INSTALL.md#manual-configuration](INSTALL.md#manual-configuration) for full examples.`

    Notes:
    - Preserve surrounding whitespace/blank lines exactly as they are.
    - Do NOT touch the existing JSON examples inside the `<details>` block (D-01 keeps them unchanged).
    - Do NOT add additional JSON snippets, bullet lists, or tables (D-03).
    - The em-dash is `—` (U+2014), matching README's existing usage style (per RESEARCH §"Established Patterns").
    - The replacement still sits inside the `<details>` block (`</details>` is at line 137); this is correct and does not change GFM rendering.
  </action>
  <verify>
    <automated>grep -E "Cursor.*Antigravity.*VS Code.*JetBrains.*Claude Desktop.*Gemini CLI.*OpenCode" README.md | grep -q "INSTALL.md#manual-configuration" && ! grep -qE "^For detailed configuration options, see \[INSTALL\.md\]\(INSTALL\.md\)\.$" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "Cursor.*Antigravity.*VS Code.*JetBrains.*Claude Desktop.*Gemini CLI.*OpenCode" README.md | grep -q "INSTALL.md#manual-configuration"` exits 0 (sentence present with anchor link).
    - `! grep -qE "^For detailed configuration options, see \[INSTALL\.md\]\(INSTALL\.md\)\.$" README.md` exits 0 (old vague line removed).
    - `grep -c "For Cursor, Antigravity, VS Code, JetBrains, Claude Desktop, Gemini CLI, and OpenCode" README.md` returns exactly `1` (no duplicate insertion).
    - `grep -qE "^## Manual Configuration$" INSTALL.md` exits 0 (anchor target still exists in INSTALL.md).
  </acceptance_criteria>
  <done>
    README.md line formerly containing the vague pointer now contains the explicit 7-client sentence with `[INSTALL.md#manual-configuration](INSTALL.md#manual-configuration)` link; no other content in README.md changed by this task (verified by `git diff README.md` showing only this one-line replacement).
  </done>
</task>

<task type="auto">
  <name>Task 2: F-12 — Add README→CHANGELOG Release Notes link in footer area</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 330–343 (footer area: `## Acknowledgements`, horizontal rule at 341, `<sub>` attribution at 343)
    - CHANGELOG.md line 1 (confirm `# Changelog` heading exists — link target is the file root per RESEARCH §D-04)
    - .planning/phases/45-polish-crosslinks/45-CONTEXT.md (decision D-04)
    - .planning/phases/45-polish-crosslinks/45-RESEARCH.md §"Open Questions" #1 (placement recommendation: ABOVE line 341 rule)
  </read_first>
  <action>
    Per D-04 (locked: add README→CHANGELOG link at footer/docs-index area, label "Release Notes"), insert exactly this one line in README.md:

    `See [CHANGELOG.md](CHANGELOG.md) for release notes.`

    Placement (per RESEARCH Open Question #1 recommendation):
    - Insert as a new paragraph AFTER the `## Acknowledgements` section content ends but BEFORE the horizontal rule `---` at line 341.
    - Surround with blank lines so it reads as a standalone paragraph (one blank line before, one blank line after).
    - Leave the `---` rule and the final `<sub>` Python-Serena attribution (line 343) UNTOUCHED — they remain the literal last two non-empty lines of the file.

    Do NOT:
    - Link to a specific release heading (CHANGELOG root is stable; pinning would require maintenance on every release — RESEARCH D-04 target).
    - Add any other footer content or restructure Acknowledgements.
    - Add CONTRIBUTING back-links (D-06 deferred).

    Match README's inline link style `[Text](path.md)` (no reference-style links).
  </action>
  <verify>
    <automated>grep -qF "See [CHANGELOG.md](CHANGELOG.md) for release notes." README.md && grep -c "CHANGELOG.md" README.md | awk '$1>=1{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "See [CHANGELOG.md](CHANGELOG.md) for release notes." README.md` exits 0.
    - `grep -c "CHANGELOG.md" README.md` returns a count ≥ 1 (previously 0 per RESEARCH).
    - The `<sub>` attribution remains the final non-empty line of README.md (no insertion AFTER the attribution): `tail -5 README.md` shows the horizontal rule still precedes the `<sub>` line, unchanged.
    - `grep -cF "See [CHANGELOG.md](CHANGELOG.md) for release notes." README.md` returns exactly `1` (no duplicate).
  </acceptance_criteria>
  <done>
    README.md contains one new "Release Notes" line pointing to CHANGELOG.md, placed above the `---` rule at line 341; the `<sub>` attribution remains the final line; `git diff README.md` for this task shows only the added line (plus any surrounding blank-line adjustments needed for paragraph separation).
  </done>
</task>

<task type="auto">
  <name>Task 3: F-12 — Add USAGE→INSTALL link in Prerequisites/Setup intro area</name>
  <files>USAGE.md</files>
  <read_first>
    - USAGE.md lines 1–20 (intro area: title, README backlink at line 5, `## Quick Tutorials` at line 7)
    - .planning/phases/45-polish-crosslinks/45-CONTEXT.md (decision D-05)
    - .planning/phases/45-polish-crosslinks/45-RESEARCH.md §"USAGE.md" target table and §"Open Questions" #2 (placement recommendation: new sibling sentence immediately after existing line 5 README backlink)
  </read_first>
  <action>
    Per D-05 (locked: add USAGE→INSTALL link in early Prerequisites/Setup area), insert exactly this new line immediately after USAGE.md's existing README backlink (current line 5: `For installation and feature overview, see [README.md](README.md).`):

    `If you haven't installed Serena yet, see [INSTALL.md](INSTALL.md) first.`

    Placement:
    - Insert as a new line directly after line 5, as a sibling sentence in the same intro paragraph cluster.
    - Keep existing line 5 UNCHANGED — do not replace or merge it; add the new sentence as a distinct line.
    - Ensure the sentence appears in the first 20 lines of USAGE.md (acceptance criterion uses `head -20`).

    Do NOT:
    - Touch `## Quick Tutorials` at line 7 or Tutorial 1 content.
    - Edit the rust-analyzer troubleshooting block (lines 531–537) — that is Task 4's read-only scope.
    - Add more links (e.g., to CONTRIBUTING) beyond this one sentence (D-06 deferred).

    Match USAGE's existing inline link style `[Text](path.md)`.
  </action>
  <verify>
    <automated>head -20 USAGE.md | grep -qF "[INSTALL.md](INSTALL.md)" && grep -qF "If you haven't installed Serena yet, see [INSTALL.md](INSTALL.md) first." USAGE.md && grep -qF "For installation and feature overview, see [README.md](README.md)." USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `head -20 USAGE.md | grep -qF "[INSTALL.md](INSTALL.md)"` exits 0 (INSTALL link in intro area).
    - `grep -qF "If you haven't installed Serena yet, see [INSTALL.md](INSTALL.md) first." USAGE.md` exits 0 (exact sentence present).
    - `grep -qF "For installation and feature overview, see [README.md](README.md)." USAGE.md` exits 0 (existing README backlink preserved).
    - `grep -cF "[INSTALL.md](INSTALL.md)" USAGE.md` returns exactly `1` (no duplicate insertion).
  </acceptance_criteria>
  <done>
    USAGE.md contains exactly one new sentence linking to INSTALL.md, inserted after the existing README backlink; the sentence lives in the first 20 lines; `git diff USAGE.md` shows only this addition.
  </done>
</task>

<task type="auto">
  <name>Task 4: F-06 verification + phase VERIFICATION.md write</name>
  <files>.planning/phases/45-polish-crosslinks/45-VERIFICATION.md</files>
  <read_first>
    - USAGE.md lines 525–545 (full rust-analyzer troubleshooting block context)
    - .planning/phases/45-polish-crosslinks/45-CONTEXT.md (decisions D-07, D-08, D-09)
    - .planning/phases/45-polish-crosslinks/45-RESEARCH.md §"Grep Verification Recipes" (copy recipes verbatim into VERIFICATION.md)
    - .planning/ROADMAP.md §"Phase 45" (3 success criteria)
  </read_first>
  <action>
    Per D-07/D-08 (locked: text review only; if accurate, no edit), perform a READ-ONLY confirmation that USAGE.md lines 531–537 still contain:
    - The header `### rust-analyzer rename fails in fresh workspaces` at line 531
    - A `v1.90` reference (line 535 per RESEARCH)
    - The `Symptom:` / `Cause:` / `Workaround:` triple
    - A reference to `replace_symbol_body`

    If ALL confirmations pass → NO EDIT to USAGE.md rust-analyzer block. If any fails → STOP and report the mismatch; do not attempt to fix in this plan (per CONTEXT §Deferred Ideas: live rust-analyzer re-test is out of scope).

    Then create `.planning/phases/45-polish-crosslinks/45-VERIFICATION.md` recording:

    1. **Phase metadata:** phase=45-polish-crosslinks, plan=01, verified_date=<today>, requirements=[README-03, USAGE-03, INST-02], status=passed|failed.

    2. **F-02 verification (Task 1):** Paste the `grep -E "Cursor.*Antigravity..."` output showing the new line is present with the anchor link; note that the old vague line is absent (negative grep).

    3. **F-12 verification (Tasks 2 and 3):** Paste grep results for CHANGELOG.md presence in README.md and INSTALL.md presence in USAGE.md head -20.

    4. **F-06 verification (Task 4):** Record:
       - Confirmation that `sed -n '531,537p' USAGE.md` contains `v1.90`, `Symptom:`, `Cause:`, `Workaround:`, and `replace_symbol_body` (paste each matching line or mark PASS/FAIL per item).
       - The SHA-256 hash of the block (for future drift detection): capture output of `sed -n '531,537p' USAGE.md | shasum -a 256` and record it verbatim.
       - Decision: "No edit required — text matches D-07/D-08 criteria" OR "Mismatch found; flagged for manual revision" (only the first is expected per RESEARCH).

    5. **Link-integrity check:** Paste result of `grep -qE "^## Manual Configuration$" INSTALL.md` (anchor target still exists).

    6. **ROADMAP success-criteria mapping:**
       - Criterion 1 (README manual-config either covers 7 clients or points to INSTALL) → SATISFIED by Task 1.
       - Criterion 2 (README→CHANGELOG; USAGE→INSTALL) → SATISFIED by Tasks 2 and 3.
       - Criterion 3 (USAGE rust-analyzer metadata current) → SATISFIED by Task 4 read-only confirmation.

    7. **REQ ID mapping:** README-03 (Task 1), USAGE-03 (Task 4), INST-02 (Task 1 via explicit 7-client pointer landing on INSTALL.md#manual-configuration).

    Use the grep recipes from 45-RESEARCH.md §"Grep Verification Recipes" verbatim.
  </action>
  <verify>
    <automated>test -f .planning/phases/45-polish-crosslinks/45-VERIFICATION.md && sed -n '531,537p' USAGE.md | grep -q "v1.90" && sed -n '531,537p' USAGE.md | grep -q "Symptom:" && sed -n '531,537p' USAGE.md | grep -q "Cause:" && sed -n '531,537p' USAGE.md | grep -q "Workaround:" && sed -n '531,537p' USAGE.md | grep -q "replace_symbol_body" && grep -q "v1.90" .planning/phases/45-polish-crosslinks/45-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/45-polish-crosslinks/45-VERIFICATION.md` exists.
    - `sed -n '531,537p' USAGE.md | grep -q "v1.90"` exits 0.
    - `sed -n '531,537p' USAGE.md | grep -q "Symptom:"` exits 0.
    - `sed -n '531,537p' USAGE.md | grep -q "Cause:"` exits 0.
    - `sed -n '531,537p' USAGE.md | grep -q "Workaround:"` exits 0.
    - `sed -n '531,537p' USAGE.md | grep -q "replace_symbol_body"` exits 0.
    - VERIFICATION.md contains the recorded SHA-256 hash string of the rust-analyzer block (grep for a 64-char hex sequence).
    - VERIFICATION.md references all three REQ IDs: `grep -qE "README-03" 45-VERIFICATION.md && grep -qE "USAGE-03" 45-VERIFICATION.md && grep -qE "INST-02" 45-VERIFICATION.md` all exit 0.
    - VERIFICATION.md status line reports `passed` (all 3 success criteria satisfied).
  </acceptance_criteria>
  <done>
    45-VERIFICATION.md committed with F-02/F-12/F-06 checks, block hash recorded, all three ROADMAP success criteria mapped to SATISFIED, and all three REQ IDs (README-03, USAGE-03, INST-02) explicitly covered. USAGE.md:531–537 remains byte-unchanged.
  </done>
</task>

</tasks>

<verification>
Phase-level verification (run at end of plan, all from repo root):

```bash
# F-02 — explicit 7-client sentence replaces vague pointer
grep -E "Cursor.*Antigravity.*VS Code.*JetBrains.*Claude Desktop.*Gemini CLI.*OpenCode" README.md | grep -q "INSTALL.md#manual-configuration"
! grep -qE "^For detailed configuration options, see \[INSTALL\.md\]\(INSTALL\.md\)\.$" README.md

# F-12 — README → CHANGELOG
grep -qF "See [CHANGELOG.md](CHANGELOG.md) for release notes." README.md

# F-12 — USAGE → INSTALL (in intro area)
head -20 USAGE.md | grep -qF "[INSTALL.md](INSTALL.md)"
grep -qF "If you haven't installed Serena yet, see [INSTALL.md](INSTALL.md) first." USAGE.md

# F-06 — rust-analyzer block unchanged content-wise
sed -n '531,537p' USAGE.md | grep -q "v1.90"
sed -n '531,537p' USAGE.md | grep -q "Symptom:"
sed -n '531,537p' USAGE.md | grep -q "Cause:"
sed -n '531,537p' USAGE.md | grep -q "Workaround:"
sed -n '531,537p' USAGE.md | grep -q "replace_symbol_body"

# Link integrity — INSTALL.md anchor target still exists
grep -qE "^## Manual Configuration$" INSTALL.md

# Phase verification record exists
test -f .planning/phases/45-polish-crosslinks/45-VERIFICATION.md
```

All commands above must exit 0.

No full v1.8 integration-check re-run is scheduled for this phase (per D-09).
</verification>

<success_criteria>
Phase 45 is complete when all of the following are true:

1. README manual-config block explicitly points to INSTALL.md#manual-configuration and names all 7 clients (Cursor, Antigravity, VS Code, JetBrains, Claude Desktop, Gemini CLI, OpenCode) in one sentence — **ROADMAP criterion 1** (REQ README-03, INST-02).
2. README.md contains a Release Notes link to CHANGELOG.md in its footer area AND USAGE.md contains an INSTALL.md link in its Prerequisites/Setup intro area — **ROADMAP criterion 2** (REQ README-03, USAGE-03).
3. USAGE.md:531–537 rust-analyzer troubleshooting block is confirmed current (contains v1.90 + Symptom/Cause/Workaround + replace_symbol_body) and unchanged — **ROADMAP criterion 3** (REQ USAGE-03).
4. 45-VERIFICATION.md records all four task outcomes, the rust-analyzer block hash, and maps each ROADMAP criterion + REQ ID to SATISFIED.
5. All verification greps above exit 0.

No Go source code is modified in this phase, so `go vet` / `go test` are not applicable (per CLAUDE.md: "Always run go vet and go test before completing any Go task" — N/A here).
</success_criteria>

<output>
After completion, create `.planning/phases/45-polish-crosslinks/45-01-SUMMARY.md` following the standard summary template, including:
- The exact diffs applied to README.md and USAGE.md (one-line replacement in README, one-line insertion in README footer, one-line insertion in USAGE intro).
- The rust-analyzer block SHA-256 hash (from 45-VERIFICATION.md).
- Confirmation that 45-VERIFICATION.md was written.
- REQ IDs closed: README-03, USAGE-03, INST-02.
- Findings closed: F-02, F-06, F-12.
</output>
