---
phase: 47
plan: 03
type: execute
wave: 3
depends_on: [47-01, 47-02]
files_modified:
  - test/integration/rust_test.go
  - USAGE.md
  - internal/kernel/lspool/quirks.go
  - internal/kernel/edit/rename_override.go
autonomous: true
requirements: [BUG-02]
requirements_addressed: [BUG-02]
tags: [rust, lsp, rust-analyzer, rename, integration-test, docs, usage]

must_haves:
  truths:
    - "The rename subtest at test/integration/rust_test.go:119-141 no longer contains t.Skip and runs under default 'go test ./...' on a machine with rust-analyzer installed"
    - "The subtest asserts result text matches regex /strategy: (lsp-native|rust-client-side)/ (D-08)"
    - "TestSymbols_RustFixture is still green (hover/references/find_implementations unchanged — D-09 regression gate)"
    - "USAGE.md Troubleshooting documents the hybrid fix, the strategy tag, and semantic limits of rust-client-side with a pointer to BUG-DEFER-02"
    - "RustAnalyzerAdapter struct doc comment (or RustAnalyzerRenameOverride doc comment) documents the semantic-accuracy limits and references BUG-DEFER-02 (D-05)"
  artifacts:
    - path: "test/integration/rust_test.go"
      provides: "Green integration test for rust rename + strategy assertion"
      contains: "assert.Regexp(t, `strategy: (lsp-native|rust-client-side)`, text"
    - path: "USAGE.md"
      provides: "Troubleshooting entry updated in-place for Phase 47 hybrid fix"
      contains: "rust-client-side, BUG-DEFER-02"
    - path: "internal/kernel/lspool/quirks.go"
      provides: "RustAnalyzerAdapter doc comment with D-05 limits"
      contains: "cross-crate trait-impl, macro"
  key_links:
    - from: "test/integration/rust_test.go:119-141 rename subtest"
      to: "edit.RenameSymbol dispatcher (lsp-native or rust-client-side)"
      via: "callTool(..., 'rename_symbol', ...)"
      pattern: "rename_symbol"
    - from: "USAGE.md Troubleshooting section"
      to: "BUG-DEFER-02 in REQUIREMENTS.md"
      via: "markdown anchor + explicit BUG-DEFER-02 reference"
      pattern: "BUG-DEFER-02"
---

<objective>
Close out Phase 47 by unskipping the rust rename integration test, asserting the strategy tag, verifying regression coverage is green, and documenting the hybrid fix in USAGE.md and the adapter doc comment. Satisfies Phase 47 Success Criteria #1, #2, and #3.

Purpose: Without unskipping the test, the phase has shipped no user-observable guarantee. Without USAGE.md + doc-comment updates, Success Criterion #2 (documented workaround) is unmet and D-05 (documented semantic limits) is unmet.

Output:
- Unskipped `TestEdit_RustFixture/rename` with strategy assertion.
- Updated USAGE.md Troubleshooting entry (in-place rewrite at lines 531-537 per RESEARCH §Sources).
- Adapter doc comment updated with D-05 limits and BUG-DEFER-02 reference (if not already done in Plan 01 Task 2).
- Green `go test ./...` + `go vet ./...` + (on dev machine with rust-analyzer) green `go test -tags integration ./test/integration/...`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-bug-rust-analyzer-rename/47-CONTEXT.md
@.planning/phases/47-bug-rust-analyzer-rename/47-RESEARCH.md
@.planning/phases/47-bug-rust-analyzer-rename/47-PATTERNS.md
@.planning/phases/47-bug-rust-analyzer-rename/47-VALIDATION.md
@.planning/phases/47-bug-rust-analyzer-rename/47-RCA.md
@.planning/phases/47-bug-rust-analyzer-rename/47-01-SUMMARY.md
@.planning/phases/47-bug-rust-analyzer-rename/47-02-SUMMARY.md
@CLAUDE.md
@test/integration/rust_test.go
@USAGE.md
@internal/kernel/lspool/quirks.go
@internal/kernel/edit/rename_override.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unskip rust rename integration test and add strategy assertion</name>
  <files>test/integration/rust_test.go</files>
  <read_first>
    - test/integration/rust_test.go (full file; the rename subtest starts at line 119 with `t.Run("rename", ...)` and the `t.Skip(...)` is at line 120)
    - test/integration/rust_test.go lines 98-117 (`t.Run("replace_body", ...)`) — shape template for the unskipped subtest per PATTERNS.md lines 242-259
    - testdata/fixtures/rust/src/main.rs (confirm `fn helper()` at line 4 col 4 1-indexed; the test already uses these coordinates)
    - .planning/phases/47-bug-rust-analyzer-rename/47-02-SUMMARY.md (confirm strategy constants final string values are "lsp-native" and "rust-client-side")
  </read_first>
  <action>
Edit `test/integration/rust_test.go`:

1. Remove the `t.Skip(...)` call at line 120 verbatim. The exact existing line (per CONTEXT.md reproduction coordinates) will be similar to:
```go
t.Skip("rust-analyzer 1.90 rename fails in temp workspaces -- tracked in BUG-02 / Phase 47")
```
Delete ONLY this `t.Skip(...)` call. Keep the enclosing `t.Run("rename", func(t *testing.T) { ... })` block intact.

2. Locate the existing `callTool(t, td.Session, "rename_symbol", ...)` invocation and its `text := textContent(result)` line (mirror the shape at lines 98-117 for `replace_body`). Keep all existing fixture setup + readback verification lines.

3. Add the strategy assertion IMMEDIATELY after the `text := textContent(result)` line and any existing `t.Logf("rename: %s", text)`:

```go
assert.Regexp(t, `strategy: (lsp-native|rust-client-side)`, text,
    "rename_symbol must advertise strategy tag (D-07)")
```

If `assert` is not already imported in the file, add `"github.com/stretchr/testify/assert"` to the import block (Task 1 notes: the file already imports testify per PATTERNS.md analog — no new import typically needed).

4. Do NOT add any additional semantic-coverage cases (multi-file rename, trait methods, macros) — per D-08 the scope is strictly: unskip + one regex assertion. Extra cases create flake risk.

5. Run locally (developer machine with rust-analyzer installed per D-10):
```
go test -tags integration -run 'TestEdit_RustFixture/rename' ./test/integration/... -count=1 -v
go test -tags integration -run 'TestSymbols_RustFixture' ./test/integration/... -count=1 -v
```

Both MUST pass. If the rename test fails with "No references found at position" despite Plan 01 + 02 work, do NOT re-add `t.Skip`; instead halt and escalate to the user — this indicates the hybrid fix is insufficient and the phase needs a Plan 04. If the rename test fails because the strategy tag is absent from the response text, the defect is in Plan 02 Task 2 Step C (tool-layer rendering) — fix there, not by weakening the assertion.

6. Also run the full default suite:
```
go vet ./...
go test ./... -count=1
```

Both MUST be green.

Commit: `test(47): unskip rust rename integration test and assert strategy tag test/integration/rust_test.go`.
  </action>
  <verify>
    <automated>! grep -nE 't\.Skip\(' test/integration/rust_test.go | grep -E ':120:|:121:|:122:|rename' &amp;&amp; grep -qE 'strategy: \(lsp-native\|rust-client-side\)' test/integration/rust_test.go &amp;&amp; go vet ./... &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 't.Skip' test/integration/rust_test.go` shows NO match within the `t.Run("rename", ...)` block (a Skip may exist in other unrelated subtests — that is fine).
    - `grep -qE 'assert\.Regexp\(t, ` + "`" + `strategy: \(lsp-native\|rust-client-side\)` + "`" + `' test/integration/rust_test.go` returns 0.
    - `go vet ./...` exits 0.
    - `go test ./... -count=1` exits 0 (non-integration build-tag suite).
    - On a machine with rust-analyzer: `go test -tags integration -run 'TestEdit_RustFixture/rename' ./test/integration/... -count=1` exits 0.
    - On a machine with rust-analyzer: `go test -tags integration -run 'TestSymbols_RustFixture' ./test/integration/... -count=1` exits 0 (D-09 regression gate).
    - No new test helpers, fixtures, or harness changes introduced — diff limited to the one subtest.
  </acceptance_criteria>
  <done>The rename integration test runs and passes on developer machines with rust-analyzer; strategy tag is asserted; regression suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Update USAGE.md Troubleshooting and adapter doc comments (D-05)</name>
  <files>USAGE.md, internal/kernel/lspool/quirks.go, internal/kernel/edit/rename_override.go</files>
  <read_first>
    - USAGE.md (full §Troubleshooting; the existing rust-analyzer rename entry lives around line 531-537 per RESEARCH §Sources / §State of the Art — verify exact line range before editing)
    - internal/kernel/lspool/quirks.go (RustAnalyzerAdapter doc comment at lines 87-92 post-Plan-01 — confirm whether D-05 language was already added by Plan 01 Task 2; if yes, extend rather than duplicate)
    - internal/kernel/edit/rename_override.go (RustAnalyzerRenameOverride doc comment — add D-05 language here if not already present)
    - .planning/REQUIREMENTS.md (BUG-DEFER-02 entry — lift wording if needed)
  </read_first>
  <action>
**Step A — USAGE.md**. Locate the existing Troubleshooting entry about rust-analyzer rename (`grep -n 'rust-analyzer' USAGE.md | grep -i 'rename\|Troubleshoot'`). Replace the existing 5-7 line entry (circa lines 531-537) with the following markdown block, preserving the surrounding section heading structure:

```markdown
### `rename_symbol` on Rust symbols

`rename_symbol` works against Rust symbols via rust-analyzer using a hybrid
strategy. The tool response includes a `strategy:` line indicating which path
produced the rename:

- `strategy: lsp-native` — rust-analyzer's `textDocument/rename` succeeded.
  Fully semantically accurate (cross-crate trait-impls, macro expansion,
  re-exports).
- `strategy: rust-client-side` — a references-driven fallback was used
  because rust-analyzer's native rename returned
  `"No references found at position"` for the temp workspace. The fallback is
  deterministic for the common case (single identifier, direct references),
  but does NOT match native rust-analyzer rename on:
  - cross-crate trait-impl method renames,
  - macro-expansion call sites,
  - re-exports that rename at the re-export site.

  If you need cross-crate or macro-accurate rename, open the project in a
  persistent workspace (so rust-analyzer's crate graph is fully loaded) or
  track upstream-fix work under `BUG-DEFER-02`.

If both paths fail, `rename_symbol` returns an `unsupported` error pointing
you at `fuzzy_edit`, `replace_symbol_body`, or `search_in_files` for a manual
rename.
```

If the existing entry is not at lines 531-537 (file drifted), use grep to find it and replace in-place; do not duplicate. Remove any now-obsolete advice such as "Use `replace_symbol_body` instead" from the old entry.

**Step B — Adapter + wrapper doc comments (D-05)**. Verify the following doc-comment text exists (post-Plan-01 Task 2 may have added some of this; only add what is missing):

In `internal/kernel/lspool/quirks.go` on `RustAnalyzerAdapter` (lines 87-92 area):
```go
// RustAnalyzerAdapter provides Rust-specific quirks for rust-analyzer.
// ... (existing text from Plan 01 Task 2) ...
//
// D-05: Semantic-accuracy limits of the client-side rename fallback
// (see edit.RustAnalyzerRenameOverride) are documented, not eliminated:
// cross-crate trait-impl discovery, macro-expansion corners, and some
// re-export scenarios are NOT covered with native rust-analyzer fidelity.
// Tracked upstream as BUG-DEFER-02.
```

In `internal/kernel/edit/rename_override.go` on `RustAnalyzerRenameOverride`:
```go
// RustAnalyzerRenameOverride wraps *lspool.RustAnalyzerAdapter with a
// RenameOverrider implementation for the lspool -> edit import-cycle seam.
//
// Semantic limits (D-05, BUG-DEFER-02):
//   - cross-crate trait-impl rename: references across crate boundaries
//     may be missing from rust-analyzer's textDocument/references response
//     when crate-graph resolution is incomplete;
//   - macro-expansion call sites: rename does not propagate into macro
//     bodies beyond what references covers;
//   - re-exports that rename at the re-export site are not automatically
//     adjusted.
//
// The wrapper scope is INTENTIONALLY narrow to rust-analyzer. DO NOT
// generalize to other LSes without revisiting the references-range ==
// identifier-range assumption (see RESEARCH Pitfall 3).
```

**Step C — Run final gates**:
```
go vet ./...
go test ./... -count=1
```
Both MUST be green (redundant after Task 1 but serves as the phase ship-gate per D-10).

Commit: `docs(47): document rust rename hybrid in USAGE.md + adapter doc comments USAGE.md internal/kernel/lspool/quirks.go internal/kernel/edit/rename_override.go`.
  </action>
  <verify>
    <automated>grep -q 'strategy: rust-client-side' USAGE.md &amp;&amp; grep -q 'BUG-DEFER-02' USAGE.md &amp;&amp; grep -q 'BUG-DEFER-02' internal/kernel/lspool/quirks.go &amp;&amp; grep -q 'BUG-DEFER-02' internal/kernel/edit/rename_override.go &amp;&amp; go vet ./... &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'strategy: rust-client-side' USAGE.md` returns 0.
    - `grep -q 'strategy: lsp-native' USAGE.md` returns 0.
    - `grep -q 'BUG-DEFER-02' USAGE.md` returns 0.
    - `grep -q 'BUG-DEFER-02' internal/kernel/lspool/quirks.go` returns 0 (D-05 doc on adapter).
    - `grep -q 'BUG-DEFER-02' internal/kernel/edit/rename_override.go` returns 0 (D-05 doc on wrapper).
    - `grep -qE 'cross-crate|trait-impl' internal/kernel/edit/rename_override.go` returns 0.
    - `grep -qE 'cross-crate|trait-impl' USAGE.md` returns 0.
    - `! grep -q 'Use .replace_symbol_body. instead' USAGE.md` (obsolete advice removed from the rename troubleshooting entry).
    - `go vet ./...` exits 0.
    - `go test ./... -count=1` exits 0.
  </acceptance_criteria>
  <done>USAGE.md ships the documented workaround entry (SC #2). Adapter + wrapper doc comments codify D-05 limits with BUG-DEFER-02 pointers. Phase ship-gate (`go vet` + `go test ./...`) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test harness -> rust-analyzer subprocess | Live LSP session writes to the temp-workspace filesystem via `rename_symbol`. |
| Documentation -> users | USAGE.md claims about the fallback's semantic limits must be accurate to prevent users from trusting the override for cross-crate rename. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-11 | Repudiation | USAGE.md claims about strategy behavior | mitigate | Language pulled directly from D-05 + BUG-DEFER-02 requirement text; reviewers cross-check. |
| T-47-12 | Denial of Service | Integration test hangs in CI | accept | D-10 explicitly scopes CI concerns to TOOL-01 / Phase 50; Phase 47 ship-gate is developer-machine only. |
| T-47-13 | Tampering | Unskipping the test masks a future regression | mitigate | Assertion is strict (regex must match exactly one of two known strategy values); weakening it in future phases would require an explicit commit. |
</threat_model>

<verification>
Final phase-level gates:

```
go vet ./...
go test ./... -count=1
go test -tags integration -run 'TestEdit_RustFixture|TestSymbols_RustFixture' ./test/integration/... -count=1   # requires rust-analyzer
grep -q 'strategy: rust-client-side' USAGE.md
grep -q 'BUG-DEFER-02' USAGE.md
grep -q 'BUG-DEFER-02' internal/kernel/lspool/quirks.go
grep -q 'BUG-DEFER-02' internal/kernel/edit/rename_override.go
! grep -nE 't\.Skip\(.*rename' test/integration/rust_test.go
test -s .planning/phases/47-bug-rust-analyzer-rename/47-RCA.md
```

All nine commands must succeed (the `! grep` is an expected-no-match).
</verification>

<success_criteria>
- Phase 47 Success Criterion #1: default `go test ./...` is green; integration test runs on dev machine with rust-analyzer.
- Phase 47 Success Criterion #2: USAGE.md Troubleshooting entry documents the hybrid + fallback limits, and RustAnalyzerRenameOverride doc comment documents D-05 with BUG-DEFER-02 pointer.
- Phase 47 Success Criterion #3: TestSymbols_RustFixture is green (hover/references/find_implementations unchanged).
- Phase 47 Success Criterion #4: already satisfied by Plan 01's 47-RCA.md; this plan does not regress it.
</success_criteria>

<output>
After completion, create `.planning/phases/47-bug-rust-analyzer-rename/47-03-SUMMARY.md` documenting:
- Which strategy (`lsp-native` vs. `rust-client-side`) actually triggered during the integration test on the developer machine (copy the `t.Logf("rename: %s", text)` line).
- Any deviations from USAGE.md text proposed above.
- Final phase ship-gate run output (summarized).

After Plan 03 SUMMARY is committed, the phase is ready for `/gsd-verify-work 47`.
</output>
