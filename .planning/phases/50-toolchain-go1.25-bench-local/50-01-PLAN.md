---
phase: 50
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/bench.yml
  - .github/workflows/capture-baseline.yml
  - test/bench/cmd/benchgate/main.go
  - test/bench/cmd/benchgate/main_test.go
  - test/bench/baselines/v1.1-github-hosted.txt
  - test/bench/baselines/v1.2-phase10-github-hosted.txt
  - test/bench/baselines/v1.2-phase11-github-hosted.txt
  - test/bench/baselines/v1.2-phase12-github-hosted.txt
autonomous: true
requirements:
  - TOOL-02
tags:
  - infra
  - ci
  - cleanup
  - benchmarks
must_haves:
  truths:
    - ".github/workflows/bench.yml is gone (CI no longer runs benches)"
    - ".github/workflows/capture-baseline.yml is gone (no manual CI baseline capture)"
    - "test/bench/cmd/benchgate package is gone (no in-tree comparison CLI)"
    - "test/bench/baselines/v1.*-github-hosted.txt files are gone (no committed CI baselines)"
    - "No source file or non-archived doc references the string 'benchgate' (excluding .planning/ history)"
  artifacts:
    - path: ".github/workflows/go-test.yml"
      provides: "CI build/vet/test gate on ubuntu-latest with Go 1.25 (untouched, must remain green)"
      contains: "go-version: '1.25.x'"
  key_links:
    - from: ".github/workflows/go-test.yml"
      to: "internal/langregistry"
      via: "runtime LS install (unchanged)"
      pattern: "go vet ./..."
---

<objective>
Tear down all hosted-CI bench plumbing in one atomic plan: delete the two CI bench workflows, the local `benchgate` Go CLI package, and the four committed `*-github-hosted.txt` baseline files. After this plan, the only CI workflow exercising Go is `.github/workflows/go-test.yml` (already correct, untouched), and the repo no longer contains any `benchgate` reference outside the immutable `.planning/` history.

Purpose: TOOL-02's first half — remove the artifacts that violate the project's "benchmarks are local-only — never on CI" rule (locked decisions D-02, D-03, D-04). This plan does NOT add the local Make targets or rewrite docs — those land in Plan 02 and Plan 03 (Wave 2), which depend on this plan.

Output: 8 file deletions, an entire directory removed, and a clean grep for `benchgate` across in-tree code/docs/yml/Makefile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-toolchain-go1.25-bench-local/50-CONTEXT.md
@.planning/phases/50-toolchain-go1.25-bench-local/50-RESEARCH.md
@.planning/phases/50-toolchain-go1.25-bench-local/50-PATTERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the two hosted-CI bench workflows</name>
  <files>
    .github/workflows/bench.yml,
    .github/workflows/capture-baseline.yml
  </files>
  <read_first>
    - .github/workflows/bench.yml (verify content before deletion — must NOT be referenced via `needs:` from another workflow)
    - .github/workflows/capture-baseline.yml (verify content before deletion)
    - .github/workflows/go-test.yml (confirm it has no `needs:` line referencing bench — assumption A2 in 50-RESEARCH.md)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-CONTEXT.md (decisions D-02, D-03)
  </read_first>
  <action>
    Delete both files unconditionally per D-02:

    ```sh
    rm .github/workflows/bench.yml
    rm .github/workflows/capture-baseline.yml
    ```

    These are committed YAML; deleting them removes them from the next push/PR (RESEARCH §"Live service config"). No external service consumes their outputs (no badge endpoint, no Codecov hook). Verify no other workflow has a `needs:` reference to either file before committing.

    Per CONTEXT D-01 "Claude's Discretion": both deletions land in the same plan (this one). Whether to commit them as one commit or two is the implementer's call; one commit is simpler and the diff is small enough to review.
  </action>
  <verify>
    <automated>! test -f .github/workflows/bench.yml && ! test -f .github/workflows/capture-baseline.yml && ! grep -rn 'needs:[[:space:]]*bench\b\|needs:[[:space:]]*\[bench\b\|needs:[[:space:]]*capture-baseline' .github/workflows/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f .github/workflows/bench.yml` (file gone)
    - `! test -f .github/workflows/capture-baseline.yml` (file gone)
    - No remaining workflow file under `.github/workflows/` contains `needs: bench` or `needs: capture-baseline` (or those names inside a YAML list)
    - `.github/workflows/go-test.yml` is untouched (`git diff --name-only HEAD~1 HEAD -- .github/workflows/go-test.yml` shows nothing)
  </acceptance_criteria>
  <done>Both hosted-CI bench workflow files are removed from the repo per D-02; no other workflow depends on them.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the test/bench/cmd/benchgate Go package and the four hosted-CI baseline files</name>
  <files>
    test/bench/cmd/benchgate/main.go,
    test/bench/cmd/benchgate/main_test.go,
    test/bench/baselines/v1.1-github-hosted.txt,
    test/bench/baselines/v1.2-phase10-github-hosted.txt,
    test/bench/baselines/v1.2-phase11-github-hosted.txt,
    test/bench/baselines/v1.2-phase12-github-hosted.txt
  </files>
  <read_first>
    - test/bench/cmd/benchgate/main.go (read content before deletion — confirm it is `package main`, no other package imports it)
    - test/bench/cmd/benchgate/main_test.go
    - .planning/phases/50-toolchain-go1.25-bench-local/50-CONTEXT.md (decisions D-03, D-04)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-RESEARCH.md §"Common Pitfalls — Pitfall 1: Deleting workflows but leaving stale references"
    - .planning/phases/50-toolchain-go1.25-bench-local/50-PATTERNS.md §"Deletions (no analog)"
  </read_first>
  <action>
    Delete the entire `test/bench/cmd/benchgate/` directory and the four `v1.*-github-hosted.txt` baseline files in one atomic operation per D-03 and D-04:

    ```sh
    rm -rf test/bench/cmd/benchgate
    rm test/bench/baselines/v1.1-github-hosted.txt
    rm test/bench/baselines/v1.2-phase10-github-hosted.txt
    rm test/bench/baselines/v1.2-phase11-github-hosted.txt
    rm test/bench/baselines/v1.2-phase12-github-hosted.txt
    ```

    Notes from PATTERNS.md §"Deletions (no analog)":
    - `benchgate` is `package main` and is never imported elsewhere — deleting the directory cannot break a build.
    - The four hosted baselines reference an old comparison flow being dismantled; D-04 explicitly forbids "historical-local" preservation or rename.
    - Do NOT touch `test/bench/baselines/README.md` here — it gets rewritten in Plan 02.
    - Do NOT touch `test/bench/baselines/` directory itself — it stays in the repo per D-05.
    - Do NOT touch any `*_test.go` file under `test/bench/` other than the benchgate package — the bench suite (`test/bench/tools_bench_test.go` etc.) stays.

    After deletion, `go vet ./...` must still pass (no compilation breakage from a stale import) and `go test ./... -count=1 -short` must still pass (no test depends on `benchgate`).
  </action>
  <verify>
    <automated>! test -d test/bench/cmd/benchgate && ! ls test/bench/baselines/v1.*-github-hosted.txt 2>/dev/null && go vet ./... && go test ./... -count=1 -short -run='Nothing^' >/dev/null 2>&1; vet=$?; ls test/bench/cmd/ 2>/dev/null; test "$vet" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `! test -d test/bench/cmd/benchgate` (directory gone)
    - `! test -f test/bench/cmd/benchgate/main.go` and `! test -f test/bench/cmd/benchgate/main_test.go` (files gone)
    - `! ls test/bench/baselines/v1.*-github-hosted.txt 2>/dev/null` returns nothing (all four gone)
    - `test/bench/baselines/README.md` still exists (untouched in this task — Plan 02 rewrites it)
    - `test/bench/tools_bench_test.go` still exists (bench suite preserved)
    - `go vet ./...` exits 0 (no stale import broke the build)
  </acceptance_criteria>
  <done>The benchgate package and all four committed CI-runner baselines are removed per D-03/D-04; `test/bench/baselines/` directory and `test/bench/baselines/README.md` are intact (rewritten by Plan 02); `go vet ./...` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify no in-tree code, docs, yml, or Makefile references 'benchgate' outside .planning/ history</name>
  <files></files>
  <read_first>
    - .planning/phases/50-toolchain-go1.25-bench-local/50-RESEARCH.md §"Common Pitfalls — Pitfall 1" (the canonical grep recipe)
    - .planning/phases/50-toolchain-go1.25-bench-local/50-CONTEXT.md §"Bench infra removal" (D-03 cleanup intent)
  </read_first>
  <action>
    Run the canonical grep matrix from RESEARCH.md Pitfall 1 to prove no surviving `benchgate` reference remains in active code, docs, or build config (ignoring `.planning/` history and `_superseded/`):

    ```sh
    # Must return no matches (and exit non-zero, which is what we want)
    grep -rn 'benchgate' \
      --include='*.go' \
      --include='*.md' \
      --include='*.yml' \
      --include='*.yaml' \
      --include='Makefile' \
      --exclude-dir=_superseded \
      --exclude-dir=.planning \
      --exclude-dir=legacy \
      .
    ```

    Likewise confirm the deleted workflow files are not referenced by name anywhere active:

    ```sh
    grep -rn 'bench\.yml\|capture-baseline\.yml' \
      --include='*.go' --include='*.md' --include='*.yml' --include='*.yaml' --include='Makefile' \
      --exclude-dir=_superseded --exclude-dir=.planning --exclude-dir=legacy \
      .
    ```

    Expected: at this point in Wave 1, CONTRIBUTING.md still mentions `bench.yml` / `capture-baseline.yml` (those edits land in Plan 03), and USAGE.md still mentions `capture-baseline.yml` (also Plan 03). That's acceptable — record any surviving hits as "to be cleaned by Plan 03." The HARD requirement for THIS plan is only the `benchgate` grep — the workflow-name grep is informational, intended to enumerate what Plan 03 must clean.

    Do not edit any file in this task — this is a verification-only step. If the `benchgate` grep returns ANY hit outside `.planning/` and `_superseded/` and `legacy/`, that hit MUST be removed (it indicates Task 2 missed a reference); add it to the deletion or fail the plan.
  </action>
  <verify>
    <automated>! grep -rn 'benchgate' --include='*.go' --include='*.md' --include='*.yml' --include='*.yaml' --include='Makefile' --exclude-dir=_superseded --exclude-dir=.planning --exclude-dir=legacy .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'benchgate' --include='*.go' --include='*.md' --include='*.yml' --include='*.yaml' --include='Makefile' --exclude-dir=_superseded --exclude-dir=.planning --exclude-dir=legacy .` returns NO matches (exit code 1)
    - The grep for `bench.yml`/`capture-baseline.yml` is allowed to surface CONTRIBUTING.md and USAGE.md hits (those are cleaned by Plan 03); document any such hits in the plan summary
    - `go vet ./...` still exits 0 after all Wave 1 deletions
  </acceptance_criteria>
  <done>The repo's active code/docs/build surface no longer references `benchgate`; remaining `bench.yml`/`capture-baseline.yml` mentions are catalogued for Plan 03 to clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → CI runners | Only `.github/workflows/*.yml` files cross into GitHub-hosted runner state. No code path within Serena trusts these workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Information Disclosure | `.github/workflows/bench.yml`, `capture-baseline.yml` | accept | Deletion REDUCES attack surface — removes a CI workflow that previously executed `go test` with elevated runner permissions. No replacement workflow is introduced. |
| T-50-02 | Tampering | `test/bench/cmd/benchgate/` deletion | accept | Removes a `package main` Go binary that contributors could have `go install`-ed locally. The binary read benchstat-formatted text files only — no privileged side effects. Stale local installs are harmless and documented in PATTERNS.md as a non-blocking cleanup hint. |
| T-50-03 | Denial of Service | Removal of CI bench gate | accept | The bench gate was advisory (no merge-block on green-only main per project rule); its removal does not weaken any defense. CI build/vet/test gate (`go-test.yml`) is unchanged. |

**Net security impact:** Deletions reduce attack surface; no new code paths, no new I/O, no new secrets. `block_on: high` — none of these threats reach high severity.
</threat_model>

<verification>
After all three tasks land in this plan:

```sh
# Deletion checks
! test -f .github/workflows/bench.yml
! test -f .github/workflows/capture-baseline.yml
! test -d test/bench/cmd/benchgate
! ls test/bench/baselines/v1.*-github-hosted.txt 2>/dev/null

# Reference cleanliness (the load-bearing one)
! grep -rn 'benchgate' --include='*.go' --include='*.md' --include='*.yml' --include='*.yaml' --include='Makefile' --exclude-dir=_superseded --exclude-dir=.planning --exclude-dir=legacy .

# Build still green
go vet ./...
go test ./... -count=1 -short
```
</verification>

<success_criteria>
- All 8 target files (2 workflows + 2 benchgate sources + 4 hosted baselines) are removed
- The `benchgate` grep returns no matches in active code/docs/build files
- `go vet ./...` and `go test ./... -count=1 -short` are green
- `test/bench/baselines/` directory and its `README.md` are intact (Plan 02 rewrites the README)
- `.github/workflows/go-test.yml` is untouched (Plan 04 verifies it stays green)
- The `bench.yml` / `capture-baseline.yml` references that survive in CONTRIBUTING.md and USAGE.md are catalogued for Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/50-toolchain-go1.25-bench-local/50-01-SUMMARY.md` recording:
- Files deleted (paths)
- Result of the `benchgate` grep (must be empty)
- Catalogue of surviving `bench.yml`/`capture-baseline.yml` references for Plan 03 to clean
- `go vet` / `go test -short` exit codes
</output>
