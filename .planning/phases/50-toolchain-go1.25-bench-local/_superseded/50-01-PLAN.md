---
phase: 50
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/bench/baselines/v1.9-github-hosted.txt
autonomous: false
requirements:
  - TOOL-02
tags:
  - ci
  - benchmark
  - baseline

must_haves:
  truths:
    - "test/bench/baselines/v1.9-github-hosted.txt exists in the repo"
    - "The file was produced by capture-baseline.yml on ubuntu-latest with Go 1.25 + gopls v0.21.1"
    - "The capture used the same flags the PR gate enforces (-short -bench=. -benchmem -count=10 -run=^$ ./test/bench/...)"
  artifacts:
    - path: "test/bench/baselines/v1.9-github-hosted.txt"
      provides: "v1.9 ubuntu-latest benchmark baseline for PR gate comparison"
      contains: "Benchmark"
  key_links:
    - from: "capture-baseline.yml workflow_dispatch (milestone=v1.9)"
      to: "test/bench/baselines/v1.9-github-hosted.txt"
      via: "stefanzweifel/git-auto-commit-action@v5"
      pattern: "Benchmark.*\\sns/op"
---

<objective>
Produce `test/bench/baselines/v1.9-github-hosted.txt` by triggering the existing
`capture-baseline.yml` workflow with `milestone=v1.9` (D-01). This baseline file
must exist on the working branch BEFORE Plan 02 swaps `bench.yml` to reference it,
or the verification PR's own `bench.yml` run will fail with "no such file" and
mask real bench results (Phase-50-specific gotcha in RESEARCH.md).

Purpose: TOOL-02 needs an apples-to-apples ubuntu-latest baseline so the
PR-tier bench gate (15% time / 25% allocs at p<0.05) compares ubuntu-latest
PR runs against an ubuntu-latest baseline instead of the legacy darwin/arm64
v1.1 placeholder.

Output: One new file `test/bench/baselines/v1.9-github-hosted.txt`, auto-committed
to the working branch by `stefanzweifel/git-auto-commit-action@v5`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-PATTERNS.md
@.github/workflows/capture-baseline.yml
@test/bench/baselines/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger capture-baseline.yml on the working branch with milestone=v1.9</name>
  <read_first>
    - .github/workflows/capture-baseline.yml (the producer workflow — verify lines 29-33 env block parity with bench.yml; lines 53-57 bench command; lines 59-66 verify-capture step requiring at least 5 Benchmark lines; lines 68-72 auto-commit)
    - test/bench/baselines/README.md (refresh policy)
  </read_first>
  <files>test/bench/baselines/v1.9-github-hosted.txt</files>
  <action>
    From the working branch (the gsd phase branch — current `git branch --show-current`), trigger the existing capture-baseline workflow via gh CLI (per D-01):

    ```sh
    BRANCH=$(git branch --show-current)
    gh workflow run capture-baseline.yml -f milestone=v1.9 --ref "$BRANCH"
    ```

    The workflow will:
    1. Checkout `$BRANCH` on `ubuntu-latest`
    2. Set up Go 1.25.x via `actions/setup-go@v5`
    3. Install `gopls@v0.21.1`
    4. Run `go test -short -bench=. -benchmem -count=10 -run=^$ ./test/bench/...` (exact PR-gate flag parity per D-04 / Pitfall 1)
    5. Tee output into `test/bench/baselines/v1.9-github-hosted.txt`
    6. Verify the capture has at least 5 lines starting with `Benchmark`
    7. Auto-commit the new file via `stefanzweifel/git-auto-commit-action@v5` to `$BRANCH`

    Do NOT hand-author the baseline file. Do NOT introduce any new env vars or change the bench flags — Pitfall 1 parity is preserved by simply invoking the existing workflow as-is. Do NOT change `capture-baseline.yml` itself (it is a read-only invocation per RESEARCH.md and PATTERNS.md "no edits").

    After triggering, monitor the run with:

    ```sh
    gh run watch $(gh run list --workflow=capture-baseline.yml --branch "$BRANCH" --limit 1 --json databaseId -q '.[0].databaseId')
    ```

    When the run completes green, pull the auto-commit:

    ```sh
    git pull --ff-only origin "$BRANCH"
    ```
  </action>
  <verify>
    <automated>test -f test/bench/baselines/v1.9-github-hosted.txt &amp;&amp; [ "$(grep -c '^Benchmark' test/bench/baselines/v1.9-github-hosted.txt)" -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/bench/baselines/v1.9-github-hosted.txt` exits 0
    - `grep -c '^Benchmark' test/bench/baselines/v1.9-github-hosted.txt` returns a number >= 5 (matches the verify step in capture-baseline.yml lines 59-66)
    - `head -3 test/bench/baselines/v1.9-github-hosted.txt` shows `goos: linux` and `goarch: amd64` (proves ubuntu-latest provenance, not darwin/arm64)
    - `git log -1 --format=%s -- test/bench/baselines/v1.9-github-hosted.txt` matches `bench: capture v1.9 baseline from ubuntu-latest CI` (auto-commit message from capture-baseline.yml line 71)
    - `ls test/bench/baselines/` STILL contains v1.1-github-hosted.txt and v1.2-phase10/11/12-github-hosted.txt (D-03 — historical baselines NOT deleted)
  </acceptance_criteria>
  <done>
    The new ubuntu-latest baseline file exists on the working branch with at least 5 Benchmark lines, ubuntu/amd64 headers, and the standard auto-commit message. All historical darwin/arm64 baselines remain intact (D-03).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies baseline capture artifact and content</name>
  <read_first>
    - test/bench/baselines/v1.9-github-hosted.txt (the artifact produced by Task 1)
    - .github/workflows/capture-baseline.yml (lines 59-66 — the in-workflow verify-capture step the human is reproducing)
  </read_first>
  <files>test/bench/baselines/v1.9-github-hosted.txt</files>
  <action>
    PAUSE for human verification. Task 1 triggered `capture-baseline.yml` with
    `milestone=v1.9` on the working branch. The workflow ran on `ubuntu-latest`
    with Go 1.25.x and gopls v0.21.1 using the exact same flags as the PR gate
    (Pitfall 1 parity). Output was auto-committed to
    `test/bench/baselines/v1.9-github-hosted.txt`.

    Verification steps for the human:

    1. Run `gh run list --workflow=capture-baseline.yml --branch $(git branch --show-current) --limit 1` and confirm the most recent run is green (status=completed, conclusion=success).
    2. Open `test/bench/baselines/v1.9-github-hosted.txt`. Confirm:
       - First few lines include `goos: linux` and `goarch: amd64`
       - File contains at least 5 lines starting with `Benchmark`
       - No `Benchmark...FullRepoSmoke` line (excluded by `-short`, per D-04)
       - Each Benchmark line has `ns/op` and `B/op`/`allocs/op` columns (proves `-benchmem` was active)
    3. Run `git log -1 -- test/bench/baselines/v1.9-github-hosted.txt` and confirm it was committed by the auto-commit action.
    4. Confirm the four historical baseline files (v1.1, v1.2-phase10/11/12) are still present in `test/bench/baselines/` (D-03 — do not delete).

    Resume signal: Type "approved" if all four checks pass, or describe what failed
    (e.g., "workflow run is red", "file has 3 Benchmark lines not 5", "darwin
    headers instead of linux"). On failure, the most likely fix is to re-run the
    workflow manually via `gh workflow run capture-baseline.yml -f milestone=v1.9 --ref <branch>`.
  </action>
  <verify>
    <manual>Human runs the four checks above against the working branch and the GitHub Actions UI / CLI. No automated command — this checkpoint exists specifically to gate on human eyes confirming ubuntu-latest provenance and content shape before downstream Plan 02 references this file.</manual>
  </verify>
  <acceptance_criteria>
    - Human responds "approved" in chat.
    - `gh run list --workflow=capture-baseline.yml --branch $(git branch --show-current) --limit 1` shows the latest run with `conclusion: success`.
    - `head -3 test/bench/baselines/v1.9-github-hosted.txt | grep -q 'goos: linux'` succeeds.
    - `grep -c '^Benchmark' test/bench/baselines/v1.9-github-hosted.txt` returns a number >= 5.
    - `! grep -q 'BenchmarkFullRepoSmoke' test/bench/baselines/v1.9-github-hosted.txt` succeeds (D-04 — `-short` excludes it).
    - `ls test/bench/baselines/v1.1-github-hosted.txt test/bench/baselines/v1.2-phase10-github-hosted.txt test/bench/baselines/v1.2-phase11-github-hosted.txt test/bench/baselines/v1.2-phase12-github-hosted.txt` all exist (D-03).
  </acceptance_criteria>
  <done>
    Human responds "approved"; the v1.9 baseline file is confirmed to be ubuntu/amd64-sourced with the correct Benchmark count, the auto-commit landed on the working branch, and historical baselines are intact. Plan 02 may now reference this file.
  </done>
</task>

</tasks>

<verification>
- `test -f test/bench/baselines/v1.9-github-hosted.txt` succeeds
- `grep -c '^Benchmark' test/bench/baselines/v1.9-github-hosted.txt` >= 5
- `head -3 test/bench/baselines/v1.9-github-hosted.txt | grep -q 'goos: linux'` succeeds
- All four historical baseline files still present in `test/bench/baselines/`
- The capture workflow run is green on the working branch
</verification>

<success_criteria>
A new `test/bench/baselines/v1.9-github-hosted.txt` exists on the working branch,
produced by `capture-baseline.yml` on `ubuntu-latest` with the exact PR-gate
flag set (D-04 / Pitfall 1 parity), containing >= 5 ubuntu/amd64 Benchmark lines.
Historical darwin/arm64 baselines preserved per D-03. Plan 02 can now safely
swap `bench.yml` to reference this file.
</success_criteria>

<output>
After completion, create `.planning/phases/50-toolchain-go1.25-gopls-ci/50-01-SUMMARY.md`
documenting: workflow run ID, baseline file size, Benchmark line count, and the
auto-commit SHA.
</output>
