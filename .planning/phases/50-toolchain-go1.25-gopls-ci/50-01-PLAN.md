---
phase: 50-toolchain-go1.25-gopls-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/bench.yml
  - .github/workflows/capture-baseline.yml
  - test/bench/baselines/README.md
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
tags:
  - ci
  - benchmarks
  - github-actions

must_haves:
  truths:
    - "CI no longer attempts to enforce a benchmark gate on PRs."
    - "CI no longer attempts to capture benchmark baselines on shared GitHub-hosted runners."
    - "test/bench/baselines/README.md describes the local-first benchmark workflow and labels the historical CI baselines as captured under the retired CI gate."
  artifacts:
    - path: ".github/workflows/bench.yml"
      provides: "MUST NOT EXIST after this plan"
      contains: "DELETED"
    - path: ".github/workflows/capture-baseline.yml"
      provides: "MUST NOT EXIST after this plan"
      contains: "DELETED"
    - path: "test/bench/baselines/README.md"
      provides: "Rewritten local-first benchmark workflow doc per D-A5"
      contains: "captured under the retired CI gate"
  key_links:
    - from: ".github/workflows/"
      to: "no bench.yml / no capture-baseline.yml"
      via: "git rm"
      pattern: "ls .github/workflows/ -> bench.yml absent AND capture-baseline.yml absent"
    - from: "test/bench/baselines/README.md"
      to: "CONTRIBUTING.md Benchmarks section"
      via: "prose cross-reference"
      pattern: "grep -F 'CONTRIBUTING.md' test/bench/baselines/README.md"
---

<objective>
Surgically remove the CI benchmark gate per D-A2 (delete `.github/workflows/bench.yml`
and `.github/workflows/capture-baseline.yml` outright) and rewrite
`test/bench/baselines/README.md` per D-A5 to describe the post-pivot local-first
workflow and label the historical CI-hosted baseline files as historical
references captured under the retired CI gate.

Purpose: Two failed `capture-baseline.yml` runs proved GitHub-hosted shared-CPU
runners are unsuitable for benchmark baselines and PR-blocking gates (RESEARCH.md
Pitfall 5; CONTEXT.md Pivot 2026-04-25). Honest signal: no enforcement, no
half-measures. The Go 1.25 + gopls v0.21.1 work is already done; `go-test.yml`
remains green on ubuntu-latest with Go 1.25 and continues to satisfy TOOL-01.
TOOL-02 is being formally cancelled (marking lives in Plan 50-02).

Output:
- `.github/workflows/bench.yml` removed.
- `.github/workflows/capture-baseline.yml` removed.
- `test/bench/baselines/README.md` rewritten with new framing.
- Historical baseline `.txt` files preserved on disk untouched (D-A5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-PATTERNS.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md

<interfaces>
<!-- Files to delete (paths verified to exist on disk 2026-04-25): -->
- .github/workflows/bench.yml (4140 bytes, last touched 2026-04-11)
- .github/workflows/capture-baseline.yml (2263 bytes, last touched 2026-04-11)

<!-- Files to keep (do NOT delete or modify in this plan): -->
- .github/workflows/go-test.yml (sole TOOL-01 evidence path; UNCHANGED per D-A1)
- test/bench/cmd/benchgate/ (kept as local pre-release tool per D-A3)
- test/bench/baselines/v1.1-github-hosted.txt (historical, KEEP per D-A5)
- test/bench/baselines/v1.2-phase10-github-hosted.txt (historical, KEEP per D-A5)
- test/bench/baselines/v1.2-phase11-github-hosted.txt (historical, KEEP per D-A5)
- test/bench/baselines/v1.2-phase12-github-hosted.txt (historical, KEEP per D-A5)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete bench.yml and capture-baseline.yml workflow files</name>
  <files>.github/workflows/bench.yml, .github/workflows/capture-baseline.yml</files>
  <read_first>
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md (D-A1, D-A2, D-A14, Pivot 2026-04-25)
    - .github/workflows/bench.yml (read once before deleting so the executor confirms
      this is the right file — header should reference "Serena benchmark regression gate")
    - .github/workflows/capture-baseline.yml (read once before deleting — header should
      reference baseline capture / `milestone` input)
  </read_first>
  <action>
Delete both files using `git rm` so the deletion is staged in a single atomic
commit:

```sh
git rm .github/workflows/bench.yml
git rm .github/workflows/capture-baseline.yml
```

Per D-A2: outright deletion. No rename, no demotion to `workflow_dispatch`-only,
no commenting-out. Both files are removed by Phase 50.

Per D-A14: do NOT touch `docker.yml`, `junie.yml`, `pytest.yml`, `docs.yaml`,
`publish.yml`, `codespell.yml`, or `go-test.yml`. Only `bench.yml` and
`capture-baseline.yml` are removed.

Do NOT use `rm` (the deletion must be staged for the verification PR commit).

Do NOT delete any file under `test/bench/baselines/`. The historical `.txt`
files stay on disk per D-A5 (covered by Task 2's README rewrite).

Do NOT delete `test/bench/cmd/benchgate/`. It remains as a local pre-release
tool per D-A3.
  </action>
  <verify>
    <automated>test ! -e .github/workflows/bench.yml &amp;&amp; test ! -e .github/workflows/capture-baseline.yml &amp;&amp; git status --porcelain .github/workflows/bench.yml .github/workflows/capture-baseline.yml | grep -E '^D' | wc -l | grep -qx '       2'</automated>
  </verify>
  <acceptance_criteria>
    - `ls .github/workflows/bench.yml` exits non-zero (file absent).
    - `ls .github/workflows/capture-baseline.yml` exits non-zero (file absent).
    - `git status --porcelain` shows BOTH paths with status `D` (staged deletion), not `??` or `M`.
    - `git grep -l 'capture-baseline\|bench\.yml' -- '.github/'` returns nothing
      (no leftover references inside `.github/`).
    - `.github/workflows/go-test.yml` is UNCHANGED (still on disk; `git status`
      shows no modification of this file).
    - No file under `test/bench/baselines/` is removed or modified by this task
      (`git status -- test/bench/baselines/` shows nothing for this task).
    - No file under `test/bench/cmd/benchgate/` is removed or modified.
  </acceptance_criteria>
  <done>
    Both workflow files staged for deletion via `git rm`; no other `.github/`
    workflow file is touched; the bench tooling under `test/bench/` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test/bench/baselines/README.md for the local-first workflow</name>
  <files>test/bench/baselines/README.md</files>
  <read_first>
    - test/bench/baselines/README.md (current text — describes the now-deleted
      three-step CI rollout and refresh-via-`capture-baseline.yml` policy)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md (D-A3, D-A4, D-A5,
      D-A6, D-A7, Pivot 2026-04-25)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md Pitfall 5
      (shared-CPU runner variance — the rationale for the pivot)
  </read_first>
  <action>
Replace the entire contents of `test/bench/baselines/README.md` with the
following Markdown verbatim. The new framing matches D-A5 (historical files
labelled as "captured under the retired CI gate"), D-A4 (local-first capture
filename pattern), D-A3 (benchgate kept as local tool), and D-A6 (no v1.9
baseline produced this phase).

Use the Write tool to overwrite the file with EXACTLY this content:

```markdown
# `test/bench/baselines/` — local-first benchmark baselines

This directory holds reference benchmark output for Serena. After Phase 50
(milestone v1.9), baselines are captured **locally pre-release** on stable
maintainer hardware. The CI bench gate has been retired — see
`.planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md` "Pivot
2026-04-25" for the rationale.

## How to capture a new baseline

Pre-release, on stable maintainer hardware, run the same flags the retired
CI gate used (parity preserved for direct comparison against historical
files):

```sh
go test -short -bench=. -benchmem -count=10 -run=^$ \
  ./test/bench/... | tee test/bench/baselines/v<version>-local-<goos>-<goarch>.txt
```

Filename pattern: `v<version>-local-<goos>-<goarch>.txt` (e.g.
`v1.9-local-darwin-arm64.txt`, `v1.10-local-linux-amd64.txt`).

For a human-readable diff against an earlier baseline:

```sh
benchstat test/bench/baselines/<old>.txt test/bench/baselines/<new>.txt
```

For a regression decision against the v1.2 PR-tier thresholds (15% time /
25% allocs at p<0.05) using the in-tree gate tool:

```sh
go run ./test/bench/cmd/benchgate \
  --baseline test/bench/baselines/<old>.txt \
  --new      test/bench/baselines/<new>.txt
```

Exit code zero means no regression at the configured tier.

## Why CI is no longer enforcing this

GitHub-hosted `ubuntu-latest` runners are shared-CPU hosts whose noise floor
and cold-start variance proved incompatible with PR-blocking benchmark
enforcement (two `capture-baseline.yml` runs failed on this exact symptom
in April 2026). See
`.planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md` Pitfall 5
and the "Pivot 2026-04-25" block in 50-CONTEXT.md. The strategy was the
problem, not the timeouts. The CONTRIBUTING.md "Benchmarks" section
documents the contributor-facing workflow.

## Historical baselines (captured under the retired CI gate)

The following files were captured on GitHub-hosted `ubuntu-latest` while
the CI bench gate was active. They are kept on disk as historical
reference and MUST NOT be used as the comparison baseline for new local
captures (different hardware class — see Pitfall 5).

| File | Captured under | Status |
|------|----------------|--------|
| `v1.1-github-hosted.txt` | retired CI gate (Phase 9–15) | historical reference |
| `v1.2-phase10-github-hosted.txt` | retired CI gate (Phase 10) | historical reference |
| `v1.2-phase11-github-hosted.txt` | retired CI gate (Phase 11) | historical reference |
| `v1.2-phase12-github-hosted.txt` | retired CI gate (Phase 12) | historical reference |

These files are intentionally not deleted — they document the v1.1/v1.2
performance plateau and remain useful as cross-architecture reference
points. They are NOT consumed by any active workflow.

## What ships in Phase 50 (v1.9)

Phase 50 documents the new local-first workflow but does NOT capture a
v1.9 baseline. The first local baseline lands when the next release-driven
capture happens on the maintainer's hardware. See CONTRIBUTING.md
"Benchmarks" for when and how to run.
```

Notes for the executor:

- The literal heading levels above (`#`, `##`) match the surrounding
  documentation style in `CONTRIBUTING.md`.
- Do NOT add or remove any `.txt` file in this directory.
- The four historical `.txt` files referenced in the table must continue to
  exist on disk after this task (verify in acceptance criteria).
  </action>
  <verify>
    <automated>grep -F 'local-first benchmark baselines' test/bench/baselines/README.md &amp;&amp; grep -F 'captured under the retired CI gate' test/bench/baselines/README.md &amp;&amp; grep -F 'v<version>-local-<goos>-<goarch>.txt' test/bench/baselines/README.md &amp;&amp; grep -F 'CONTRIBUTING.md' test/bench/baselines/README.md &amp;&amp; ! grep -q 'three-step rollout' test/bench/baselines/README.md &amp;&amp; ! grep -q 'capture-baseline.yml' test/bench/baselines/README.md &amp;&amp; test -f test/bench/baselines/v1.1-github-hosted.txt &amp;&amp; test -f test/bench/baselines/v1.2-phase10-github-hosted.txt &amp;&amp; test -f test/bench/baselines/v1.2-phase11-github-hosted.txt &amp;&amp; test -f test/bench/baselines/v1.2-phase12-github-hosted.txt</automated>
  </verify>
  <acceptance_criteria>
    - `test/bench/baselines/README.md` contains the literal heading `# \`test/bench/baselines/\` — local-first benchmark baselines`.
    - `grep -F 'captured under the retired CI gate' test/bench/baselines/README.md` matches at least once.
    - `grep -F 'v<version>-local-<goos>-<goarch>.txt' test/bench/baselines/README.md` matches.
    - `grep -F 'go run ./test/bench/cmd/benchgate' test/bench/baselines/README.md` matches (D-A3 local invocation documented).
    - `grep -F 'CONTRIBUTING.md' test/bench/baselines/README.md` matches (cross-reference present per D-A7 framing).
    - `grep -q 'three-step rollout' test/bench/baselines/README.md` returns NON-ZERO (old framing gone).
    - `grep -q 'capture-baseline.yml' test/bench/baselines/README.md` returns NON-ZERO (no references to the deleted workflow).
    - `grep -q 'warn-only' test/bench/baselines/README.md` returns NON-ZERO (rollout history removed).
    - All four historical `.txt` files still exist on disk: `v1.1-github-hosted.txt`,
      `v1.2-phase10-github-hosted.txt`, `v1.2-phase11-github-hosted.txt`,
      `v1.2-phase12-github-hosted.txt`.
    - No new `.txt` file (e.g. `v1.9-*.txt`) is added by this task — D-A6 explicitly
      defers the first v1.9 capture to the maintainer's release ritual.
  </acceptance_criteria>
  <done>
    README.md rewritten with the local-first framing; historical baseline files
    untouched; no references to the deleted workflows remain in this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan modifies CI configuration and a README only. No new code, no new third-party action, no change to `.github/workflows/go-test.yml` permissions or runtime trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | T (tampering) | `.github/workflows/` | accept | Removing two workflows reduces CI attack surface (fewer YAML files parsed, fewer third-party action invocations such as `stefanzweifel/git-auto-commit-action@v5`). No new surface introduced. |
| T-50-02 | I (information disclosure) | `test/bench/baselines/README.md` | accept | Doc changes only; no secrets, credentials, or PII referenced. The cross-reference to CONTEXT.md is to a planning-tree path that already exists in the repo. |
| T-50-03 | D (denial of service) | CI pipeline | accept | Deleting `bench.yml` removes a known unstable PR-blocking job. Net availability of the CI pipeline improves; `go-test.yml` (the sole TOOL-01 path) is untouched. |
</threat_model>

<verification>
Phase-wide verification (mirrors D-A13 bullets 2 and 6 — bullets 1, 3, 4, 5
are validated in Plan 50-02):

1. `bench.yml` and `capture-baseline.yml` are absent from `.github/workflows/`
   (D-A13 bullet 2):
   ```sh
   ! test -e .github/workflows/bench.yml \
     && ! test -e .github/workflows/capture-baseline.yml
   ```
2. `test/bench/baselines/README.md` rewritten per D-A5 (D-A13 bullet 6):
   ```sh
   grep -F 'local-first benchmark baselines' test/bench/baselines/README.md
   grep -F 'captured under the retired CI gate' test/bench/baselines/README.md
   ```
3. Historical baseline files preserved (D-A5):
   ```sh
   ls test/bench/baselines/v1.1-github-hosted.txt \
      test/bench/baselines/v1.2-phase10-github-hosted.txt \
      test/bench/baselines/v1.2-phase11-github-hosted.txt \
      test/bench/baselines/v1.2-phase12-github-hosted.txt
   ```
4. `go-test.yml` (TOOL-01 evidence per D-A1) UNCHANGED:
   ```sh
   git status --porcelain .github/workflows/go-test.yml | wc -l | tr -d ' '
   # expected: 0
   ```
5. No leftover textual references to the deleted workflows inside `.github/`:
   ```sh
   git grep -l 'capture-baseline\|bench\.yml' -- '.github/'
   # expected: empty
   ```
</verification>

<success_criteria>
This plan is complete when:

- `.github/workflows/bench.yml` is staged for deletion (`git status` shows `D`).
- `.github/workflows/capture-baseline.yml` is staged for deletion (`git status` shows `D`).
- `test/bench/baselines/README.md` matches the rewrite specified in Task 2 (all
  acceptance-criteria greps pass).
- The four historical baseline `.txt` files are still on disk and unmodified.
- `.github/workflows/go-test.yml` is unmodified.
- `test/bench/cmd/benchgate/` is unmodified.
- `git grep -l 'capture-baseline\|bench\.yml' -- '.github/'` returns nothing.

Plan 50-02 is responsible for D-A13 bullets 1, 3, 4, 5 (CONTRIBUTING.md
sections, PROJECT.md rewrite, REQUIREMENTS.md TOOL-02 cancellation, and the
PR's own `go-test.yml` green run on ubuntu-latest with Go 1.25).
</success_criteria>

<output>
After completion, create `.planning/phases/50-toolchain-go1.25-gopls-ci/50-01-SUMMARY.md`
documenting:
- Files deleted (with their byte sizes for the audit trail).
- Confirmation that `go-test.yml` and `test/bench/cmd/benchgate/` are untouched.
- Confirmation that the four historical baseline `.txt` files survived.
- The new framing in `test/bench/baselines/README.md` (one-line summary).
</output>
