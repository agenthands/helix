---
phase: 50-toolchain-go1.25-gopls-ci
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - Makefile
  - .planning/ROADMAP.md
  - .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
tags:
  - docs
  - requirements
  - makefile

must_haves:
  truths:
    - "CONTRIBUTING.md tells contributors when, how, where, and why to run benchmarks locally pre-release."
    - "CONTRIBUTING.md documents the gopls v0.21.1 pin, where it lives, and the bump policy."
    - "PROJECT.md tech-debt paragraph reflects the post-pivot state — bench gate intentionally not enforced on CI; baselines are local."
    - "REQUIREMENTS.md marks TOOL-02 as cancelled (Phase 50) with a strikethrough and inline note."
    - "TOOL-01 is still open and traceable to Phase 50 via go-test.yml on ubuntu-latest."
    - "Optional Makefile bench targets are present iff Plan 50-02 added them, with consistent `## help-text` comments."
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Benchmarks section + Gopls Pin section"
      contains: "## Benchmarks"
    - path: "CONTRIBUTING.md"
      provides: "Gopls Pin subsection per D-A8"
      contains: "v0.21.1"
    - path: ".planning/PROJECT.md"
      provides: "Rewritten tech-debt sentence per D-A12"
      contains: "Benchmark gate intentionally not enforced on CI"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TOOL-02 marked cancelled per D-A10"
      contains: "Cancelled (Phase 50)"
  key_links:
    - from: "CONTRIBUTING.md ## Benchmarks"
      to: "test/bench/baselines/README.md"
      via: "prose cross-reference"
      pattern: "grep -F 'test/bench/baselines/' CONTRIBUTING.md"
    - from: "CONTRIBUTING.md ## Benchmarks"
      to: "test/bench/cmd/benchgate"
      via: "prose cross-reference (D-A3 local tool)"
      pattern: "grep -F './test/bench/cmd/benchgate' CONTRIBUTING.md"
    - from: "REQUIREMENTS.md TOOL-02 row"
      to: "Phase 50 cancellation note"
      via: "strikethrough + inline note"
      pattern: "grep -F '~~TOOL-02~~' .planning/REQUIREMENTS.md"
    - from: "PROJECT.md known tech debt"
      to: "CONTRIBUTING.md Benchmarks"
      via: "prose cross-reference"
      pattern: "grep -F 'CONTRIBUTING.md' .planning/PROJECT.md"
---

<objective>
Land the four post-pivot doc changes in a single coherent edit set:

1. Add a `## Benchmarks` section to `CONTRIBUTING.md` covering when/how/where/
   how-to-compare/why-not-CI per D-A7.
2. Add a `## gopls pin` section (or `### Gopls Pin` subsection) to
   `CONTRIBUTING.md` covering pin version, rationale, location, and bump
   policy per D-A8.
3. Rewrite the bench tech-debt sentence in `.planning/PROJECT.md` per D-A12.
4. Mark TOOL-02 as cancelled in `.planning/REQUIREMENTS.md` per D-A10.

Plus, optionally per D-A9: add `bench-capture` and `bench-compare` targets to
`Makefile` if (and only if) the addition stays under ~20 lines and matches the
existing `## help-text` annotation style.

Purpose: Lock in the strategy change captured in CONTEXT.md Pivot 2026-04-25
in repo-facing documentation so future contributors do not "re-enable" the gate
without understanding the noise/fragility history. TOOL-02 cancellation must be
explicit in REQUIREMENTS.md so the v1.9 traceability table reads clean.

Output:
- `CONTRIBUTING.md` with two new sections.
- `.planning/PROJECT.md` tech-debt sentence rewritten in place.
- `.planning/REQUIREMENTS.md` TOOL-02 row + traceability row marked cancelled.
- `Makefile` with optional `bench-capture` / `bench-compare` targets (executor's
  discretion per D-A9 + Task 5 acceptance criteria).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-PATTERNS.md
@.planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md

<interfaces>
<!-- Existing CONTRIBUTING.md heading style (verified 2026-04-25): -->
- Top-level sections use `##` (e.g. "## Scope of Contributions", "## Prerequisites",
  "## Development Commands", "## Project Structure", "## Running Integration Tests",
  "## Oracle Test Suite", "## Running Benchmarks", "## Adding a New MCP Tool",
  "## Adding Language Support", "## Benchmark CI Gate", "## Legacy Python").
- The existing "## Benchmark CI Gate" section (lines 178–184) is now stale — it
  describes the deleted workflow. Task 1 supersedes it (see action for handling).
- File paths and env-var names are in backticks throughout the file.

<!-- Existing PROJECT.md tech-debt paragraph (line ~139, lead-in `**Known tech debt:**`): -->
- Single paragraph, period-separated independent clauses.
- Sentences: (a) bench baselines [REWRITE PER D-A12], (b) 3 redundant
  GrammarRegistry [Phase 49 done — sentence may already be edited or kept],
  (c) rust-analyzer rename quirk, (d) jdtls cold-start.

<!-- Existing REQUIREMENTS.md (verified line numbers): -->
- Line 20: `- [ ] **TOOL-02**: The CI benchmark gate runs on `ubuntu-latest`...`
- Line 79: traceability table row `| TOOL-02 | Phase 50 | Pending |`
- Line 78: `| TOOL-01 | Phase 50 | Pending |` (unchanged this phase per D-A11)

<!-- Existing Makefile (verified): -->
- Has `## help-text` style on `docs:`, `clean-jdtls-cache:`, `bench-jdtls-warm:`.
- `.PHONY` line at top declares all phony targets explicitly.
- `GO=go` macro used throughout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ## Benchmarks and ## gopls pin sections to CONTRIBUTING.md and retire the stale ## Benchmark CI Gate section</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (entire file — verify heading style and locate the existing
      "## Running Benchmarks" section ~lines 130–152 and the stale
      "## Benchmark CI Gate" section ~lines 178–184)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md (D-A7, D-A8,
      D-A3, D-A14, Pivot 2026-04-25)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md Pitfall 4
      and Pitfall 5 (referenced from the gopls pin rationale)
  </read_first>
  <action>
Make three coordinated edits to `CONTRIBUTING.md`. Use the Edit tool for each
edit (Read the file once first, then Edit; do not Write the whole file).

**Edit A — Replace the stale `## Benchmark CI Gate` section (currently
lines 178–184) with a new `## Benchmarks` section.**

The current section reads:

```markdown
## Benchmark CI Gate

The benchmark CI gate prevents performance regressions from landing:

- `.github/workflows/bench.yml` runs on PRs, comparing PR benchmarks against committed baselines via `benchstat`.
- Uses tiered thresholds: PR tier (>15% time / >25% allocs) and release tier (>10% time / >20% allocs).
- `.github/workflows/capture-baseline.yml` captures new baselines (manual dispatch on GitHub Actions).
```

Replace EXACTLY that section (heading + intro + three bullets) with this new
section verbatim:

```markdown
## Benchmarks

Benchmarks are captured **locally pre-release** on stable maintainer hardware.
The CI bench gate was retired in Phase 50 (milestone v1.9) — see
`.planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md` "Pivot 2026-04-25"
for the rationale. The short version: GitHub-hosted shared-CPU runners are too
noisy for PR-blocking benchmark enforcement, and two `capture-baseline.yml`
runs failed on exactly that symptom.

**When to run:** before tagging a release, or when investigating a suspected
regression on a specific subsystem.

**How to run** (same flags the retired CI gate used, for direct comparison
against historical baselines):

```sh
go test -short -bench=. -benchmem -count=10 -run=^$ ./test/bench/...
```

**Where to put the result:**

```sh
go test -short -bench=. -benchmem -count=10 -run=^$ ./test/bench/... \
  | tee test/bench/baselines/v<version>-local-<goos>-<goarch>.txt
```

Filename pattern: `v<version>-local-<goos>-<goarch>.txt`
(e.g. `v1.9-local-darwin-arm64.txt`).

**How to compare** against an earlier baseline:

```sh
benchstat test/bench/baselines/<old>.txt test/bench/baselines/<new>.txt
```

For a regression decision against the v1.2 PR-tier thresholds (15% time / 25%
allocs at p<0.05) using the in-tree gate tool:

```sh
go run ./test/bench/cmd/benchgate \
  --baseline test/bench/baselines/<old>.txt \
  --new      test/bench/baselines/<new>.txt
```

**Why CI no longer enforces this:** GitHub-hosted `ubuntu-latest` runners are
shared-CPU hosts with high variance on cold-start and warm-reuse benchmarks.
Re-enabling the gate without first provisioning a dedicated runner would
reintroduce the same flake pattern Phase 50 retired. See
`test/bench/baselines/README.md` and the Pivot 2026-04-25 block in 50-CONTEXT.md.
```

**Edit B — Insert a new `## gopls pin` section immediately after the new
`## Benchmarks` section (and before the existing `## Legacy Python` section).**

Insert this content verbatim:

```markdown
## gopls pin

Serena pins `gopls` to **`v0.21.1`** in dev tooling.

**Why pinned:**

1. **Compatibility.** `gopls v0.17.1` is incompatible with Go 1.25 on
   `linux/amd64`. The `v0.21.1` pin resolves this. (This was the original
   driver for milestone v1.9 TOOL-01.)
2. **Bench stability.** Cold-start and warm-reuse metrics
   (`BenchmarkLSPIndex_Cold`, `BenchmarkLSPIndex_Warm`) are sensitive to gopls
   internal indexing changes — see Pitfall 4 in
   `.planning/phases/50-toolchain-go1.25-gopls-ci/50-RESEARCH.md`.

**Where the pin lives:** the `Makefile` and dev-tooling install scripts. The
pin is **not** declared in any `.github/workflows/` file — `bench.yml` and
`capture-baseline.yml` were deleted in Phase 50, and no other workflow needs
gopls.

**Bump policy:** any change to the gopls pin should trigger a fresh local
benchmark baseline before the next release (see the Benchmarks section above
for the capture procedure). Do not bump in a PR that does not also re-capture
the baseline — comparisons against pre-bump baselines will be misleading.
```

**Edit C — Update the `## Running Benchmarks` section (currently around
lines 130–152) to remove the stale CI claim.**

Find this bullet inside the `## Running Benchmarks` section:

```markdown
- CI runs the benchstat regression gate via `.github/workflows/bench.yml`.
- PR thresholds: >15% time regression or >25% allocs regression blocks merge.
- Release thresholds: >10% time or >20% allocs.
```

Replace those three bullets with this single bullet:

```markdown
- Benchmarks are captured locally pre-release; see the **Benchmarks** section
  below for the capture procedure and historical thresholds.
```

(Reason: the `## Running Benchmarks` section gives the bare `go test -bench`
mechanics; the new `## Benchmarks` section owns the workflow + thresholds
narrative. Avoid duplicating the policy in two places.)

**Constraints:**

- Use `##` for both new sections (matches existing top-level heading style).
- All file paths in backticks; all env-var names in backticks; benchmark
  identifiers (`BenchmarkLSPIndex_Cold`) in backticks.
- Do NOT modify any other section of CONTRIBUTING.md (Prerequisites,
  Development Commands, Project Structure, etc.).
- Do NOT add emoji or horizontal rules between subsections.
- Section ordering after these edits should be: ... `## Running Benchmarks`,
  ..., `## Benchmarks`, `## gopls pin`, `## Legacy Python`. (The new sections
  replace the old `## Benchmark CI Gate` slot.)
  </action>
  <verify>
    <automated>grep -F '## Benchmarks' CONTRIBUTING.md &amp;&amp; grep -F '## gopls pin' CONTRIBUTING.md &amp;&amp; grep -F 'v0.21.1' CONTRIBUTING.md &amp;&amp; grep -F 'go run ./test/bench/cmd/benchgate' CONTRIBUTING.md &amp;&amp; grep -F 'v<version>-local-<goos>-<goarch>.txt' CONTRIBUTING.md &amp;&amp; grep -F 'Pivot 2026-04-25' CONTRIBUTING.md &amp;&amp; ! grep -q '## Benchmark CI Gate' CONTRIBUTING.md &amp;&amp; ! grep -qF '.github/workflows/bench.yml' CONTRIBUTING.md &amp;&amp; ! grep -qF '.github/workflows/capture-baseline.yml' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Benchmarks$' CONTRIBUTING.md` returns `1`.
    - `grep -c '^## gopls pin$' CONTRIBUTING.md` returns `1`.
    - `grep -c '^## Benchmark CI Gate$' CONTRIBUTING.md` returns `0` (stale heading retired).
    - `grep -F 'v0.21.1' CONTRIBUTING.md` matches at least once (gopls pin documented).
    - `grep -F 'go run ./test/bench/cmd/benchgate' CONTRIBUTING.md` matches (D-A3 local tool documented).
    - `grep -F 'v<version>-local-<goos>-<goarch>.txt' CONTRIBUTING.md` matches (D-A4 filename pattern documented).
    - `grep -F 'go test -short -bench=. -benchmem -count=10 -run=^$' CONTRIBUTING.md` matches (D-A7 same-flags-as-retired-gate continuity).
    - `grep -F 'Pivot 2026-04-25' CONTRIBUTING.md` matches (rationale anchor present).
    - `grep -F 'Pitfall 4' CONTRIBUTING.md` matches (D-A8 explicit Pitfall 4 reference).
    - `grep -F '.github/workflows/bench.yml' CONTRIBUTING.md` returns NON-ZERO (no
      stale workflow reference left in the file).
    - `grep -F '.github/workflows/capture-baseline.yml' CONTRIBUTING.md` returns NON-ZERO.
    - `grep -F 'GOPLS_VERSION' CONTRIBUTING.md` returns NON-ZERO (the env-var was
      only set inside the deleted workflows; per D-A8 the pin lives in Makefile /
      dev tooling, not workflows — referencing the deleted env-var would mislead).
    - All other top-level section headings present before the edit are still
      present after (`## Scope of Contributions`, `## Prerequisites`,
      `## Development Commands`, `## Project Structure`, `## Running Integration
      Tests`, `## Oracle Test Suite`, `## Running Benchmarks`, `## Adding a New
      MCP Tool`, `## Adding Language Support`, `## Legacy Python`).
  </acceptance_criteria>
  <done>
    CONTRIBUTING.md contains the new Benchmarks and gopls pin sections; the
    stale Benchmark CI Gate section is gone; the Running Benchmarks bullets no
    longer claim CI enforcement.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the bench tech-debt sentence in .planning/PROJECT.md per D-A12</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (line 139 area — the paragraph beginning
      `**Known tech debt:**`)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md (D-A12 verbatim
      target wording)
  </read_first>
  <action>
Locate the paragraph in `.planning/PROJECT.md` that begins with
`**Known tech debt:**`. The current first sentence reads (verified
2026-04-25):

```
Benchmark baselines captured locally (darwin/arm64) instead of CI ubuntu-latest due to gopls v0.17.1 incompatibility with Go 1.25 on linux/amd64.
```

Replace **only** that first sentence with this exact wording (per D-A12,
verbatim):

```
Benchmark gate intentionally not enforced on CI; baselines are captured locally pre-release. See CONTRIBUTING.md "Benchmarks" section.
```

Constraints:

- Keep the bold lead-in `**Known tech debt:**` exactly as-is.
- Keep all other sentences in the paragraph unchanged: the GrammarRegistry
  sentence, the rust-analyzer rename sentence, and the jdtls cold-start
  sentence. (Phase 49 may have edited the GrammarRegistry sentence already;
  if so, leave whatever wording is currently there.)
- The paragraph stays a single paragraph; do not split into bullets.
- Do NOT modify any other paragraph in PROJECT.md.

Use the Edit tool with `old_string` matching the full original first sentence
(including the trailing period and the leading space if present after the
`**Known tech debt:**` label) and `new_string` containing the replacement.
  </action>
  <verify>
    <automated>grep -F 'Benchmark gate intentionally not enforced on CI; baselines are captured locally pre-release.' .planning/PROJECT.md &amp;&amp; grep -F 'CONTRIBUTING.md "Benchmarks" section' .planning/PROJECT.md &amp;&amp; ! grep -qF 'Benchmark baselines captured locally (darwin/arm64)' .planning/PROJECT.md &amp;&amp; ! grep -qF 'gopls v0.17.1 incompatibility with Go 1.25 on linux/amd64' .planning/PROJECT.md &amp;&amp; grep -F '**Known tech debt:**' .planning/PROJECT.md &amp;&amp; grep -F 'rust-analyzer' .planning/PROJECT.md &amp;&amp; grep -F 'jdtls cold-start' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'Benchmark gate intentionally not enforced on CI; baselines are captured locally pre-release.' .planning/PROJECT.md` matches.
    - `grep -F 'CONTRIBUTING.md "Benchmarks" section' .planning/PROJECT.md` matches.
    - `grep -F 'Benchmark baselines captured locally (darwin/arm64)' .planning/PROJECT.md` returns NON-ZERO (old wording gone).
    - `grep -F 'gopls v0.17.1 incompatibility with Go 1.25 on linux/amd64' .planning/PROJECT.md` returns NON-ZERO.
    - `grep -F '**Known tech debt:**' .planning/PROJECT.md` matches (bold lead-in preserved).
    - `grep -F 'rust-analyzer' .planning/PROJECT.md` matches (other tech-debt items preserved).
    - `grep -F 'jdtls cold-start' .planning/PROJECT.md` matches.
    - `git diff --stat .planning/PROJECT.md` shows roughly 1 paragraph touched
      (one line removed, one added, possibly with surrounding flow tweaks);
      no other PROJECT.md sections changed.
  </acceptance_criteria>
  <done>
    PROJECT.md "Known tech debt" paragraph's first sentence rewritten verbatim
    per D-A12; other sentences in the paragraph untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark TOOL-02 as cancelled in .planning/REQUIREMENTS.md per D-A10</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 20 — TOOL-02 entry; line 79 — traceability row)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md (D-A10 verbatim
      cancellation note text; D-A11 confirms TOOL-01 stays unchanged)
  </read_first>
  <action>
Two edits to `.planning/REQUIREMENTS.md`. Use the Edit tool for each.

**Edit A — line ~20, the TOOL-02 entry under "### Infra — Toolchain".**

Current text:

```markdown
- [ ] **TOOL-02**: The CI benchmark gate runs on `ubuntu-latest` (not just darwin/arm64 locally) and enforces PR-tier thresholds on every PR — closes the benchmarks tech-debt note in PROJECT.md
```

Replace with this exact text (per D-A10 — strikethrough on the ID, leading
checkbox flipped to checked-cancelled, and the inline note appended):

```markdown
- [x] ~~**TOOL-02**~~: ~~The CI benchmark gate runs on `ubuntu-latest` (not just darwin/arm64 locally) and enforces PR-tier thresholds on every PR — closes the benchmarks tech-debt note in PROJECT.md~~ — Cancelled in Phase 50 (CI-hosted bench gate proven unreliable on shared-CPU GitHub runners across two failed capture attempts; benchmarks now run locally pre-release. See `.planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md` "Pivot 2026-04-25").
```

Rationale: the leading `[x]` marks the requirement as resolved-by-cancellation
so the milestone checklist no longer counts it as outstanding work. The
strikethrough makes the cancellation visually obvious. The inline note cites
the specific phase and CONTEXT.md anchor so the audit trail is one click away.

**Edit B — line ~79, the traceability table row for TOOL-02.**

Current text:

```markdown
| TOOL-02 | Phase 50 | Pending |
```

Replace with:

```markdown
| TOOL-02 | Phase 50 | Cancelled (Phase 50) |
```

Constraints:

- Do NOT touch the TOOL-01 row (line 19) — D-A11 says TOOL-01 wording stays
  as-is and Phase 50 satisfies TOOL-01 via `go-test.yml` on ubuntu-latest.
- Do NOT touch any other row in the traceability table.
- Do NOT modify the "**Coverage:**" summary block at the bottom of the
  traceability section — the "Mapped to phases: 14" count remains accurate
  (cancelled requirements still count as mapped).
- Do NOT remove TOOL-02 from the file. The strikethrough + cancellation note
  preserves audit trail; outright removal would erase history.
  </action>
  <verify>
    <automated>grep -F '~~**TOOL-02**~~' .planning/REQUIREMENTS.md &amp;&amp; grep -F 'Cancelled in Phase 50' .planning/REQUIREMENTS.md &amp;&amp; grep -F 'Cancelled (Phase 50)' .planning/REQUIREMENTS.md &amp;&amp; grep -F 'Pivot 2026-04-25' .planning/REQUIREMENTS.md &amp;&amp; grep -E '^- \[ \] \*\*TOOL-01\*\*:' .planning/REQUIREMENTS.md &amp;&amp; grep -E '^\| TOOL-01 \| Phase 50 \| Pending \|$' .planning/REQUIREMENTS.md &amp;&amp; ! grep -qE '^\| TOOL-02 \| Phase 50 \| Pending \|$' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '~~**TOOL-02**~~' .planning/REQUIREMENTS.md` matches (strikethrough applied).
    - `grep -F 'Cancelled in Phase 50' .planning/REQUIREMENTS.md` matches (inline note present).
    - `grep -F 'Cancelled (Phase 50)' .planning/REQUIREMENTS.md` matches (traceability row updated).
    - `grep -E '^- \[ \] \*\*TOOL-01\*\*:' .planning/REQUIREMENTS.md` matches (TOOL-01 unchanged per D-A11).
    - `grep -E '^\| TOOL-01 \| Phase 50 \| Pending \|$' .planning/REQUIREMENTS.md` matches (TOOL-01 row unchanged).
    - `grep -E '^\| TOOL-02 \| Phase 50 \| Pending \|$' .planning/REQUIREMENTS.md` returns NON-ZERO (old row replaced).
    - The "## Future Requirements" section is unchanged (no TOOL-02 entry added there).
    - The "## Out of Scope" table is unchanged (TOOL-02 is cancelled, not out-of-scope-from-the-start).
  </acceptance_criteria>
  <done>
    TOOL-02 row marked cancelled with strikethrough and inline note pointing to
    50-CONTEXT.md Pivot 2026-04-25; traceability row updated; TOOL-01 untouched.
  </done>
</task>


<task type="auto">
  <name>Task 5: (Optional, per D-A9) Add bench-capture and bench-compare Makefile targets</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (verify `## help-text` style on existing targets like `docs:`,
      `clean-jdtls-cache:`, `bench-jdtls-warm:`; verify `.PHONY` line at top)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md (D-A9 — optional;
      executor's discretion if Makefile structure accommodates and addition is
      <20 lines)
  </read_first>
  <action>
This task is OPTIONAL per D-A9. The executor MUST decide before editing:

**Decision criteria — only proceed if ALL of:**
1. The targets can be added in <20 total new lines (Makefile body + `.PHONY`
   declaration).
2. The targets follow the existing `## help-text` annotation style.
3. The targets do not duplicate functionality already provided by the
   `bench-jdtls-warm:` target (they don't — that's jdtls-specific).
4. No new external tool dependency is introduced (benchstat is documented in
   CONTRIBUTING.md as a one-line `go install`; the targets should NOT
   `go install` automatically — they assume `benchstat` is on PATH and surface
   a clear error if missing).

If any criterion fails, **skip this task** and document the skip in
`50-02-SUMMARY.md`. The acceptance criteria below allow the skipped path.

**If proceeding, make these two edits to `Makefile`:**

**Edit A — extend the `.PHONY` line at the top of the file.**

Current line 1:

```makefile
.PHONY: build clean proto test vet fmt docs clean-jdtls-cache bench-jdtls-warm
```

Append `bench-capture bench-compare` to the list:

```makefile
.PHONY: build clean proto test vet fmt docs clean-jdtls-cache bench-jdtls-warm bench-capture bench-compare
```

**Edit B — append two new targets at the END of the file** (after the existing
`bench-jdtls-warm:` target). Insert this block verbatim:

```makefile

bench-capture: ## Capture a local pre-release benchmark baseline (override OUT=test/bench/baselines/v<version>-local-<goos>-<goarch>.txt)
	@OUT="$${OUT:-test/bench/baselines/v-local-$$($(GO) env GOOS)-$$($(GO) env GOARCH).txt}"; \
	echo "Capturing baseline -> $$OUT"; \
	$(GO) test -short -bench=. -benchmem -count=10 -run=^$$ ./test/bench/... | tee "$$OUT"

bench-compare: ## Diff two baselines via benchstat (usage: make bench-compare OLD=<path> NEW=<path>)
	@if [ -z "$$OLD" ] || [ -z "$$NEW" ]; then \
	  echo "usage: make bench-compare OLD=<path> NEW=<path>" >&2; exit 2; \
	fi
	@command -v benchstat >/dev/null 2>&1 || { echo "benchstat not on PATH; install via: go install golang.org/x/perf/cmd/benchstat@latest" >&2; exit 2; }
	benchstat "$$OLD" "$$NEW"
```

Constraints:

- Use TAB indentation (Makefile recipe lines), not spaces.
- The `OUT=` default deliberately lacks a `<version>` token (the maintainer
  must override `OUT` with the real version-tagged filename per CONTRIBUTING.md
  Benchmarks section). The default exists so the target does not fail on a
  bare `make bench-capture` invocation during smoke testing.
- Do NOT auto-install benchstat. The target prints an install hint and exits 2.
- Use the existing `$(GO)` macro for the `go test` and `go env` invocations.
- The two new targets must be the LAST targets in the file (preserves
  appendable structure).

**If updating CONTRIBUTING.md to mention these targets** (D-A9 sub-clause):
optional, but if added, the mention should be a single bullet inside the
`## Benchmarks` section like:

```markdown
- Or use the convenience wrappers: `make bench-capture` (with `OUT=...`) and
  `make bench-compare OLD=... NEW=...`.
```

Insert this bullet at the end of the existing "How to compare" block in the
`## Benchmarks` section. This sub-edit is optional too — only add the bullet
if the targets were actually added.
  </action>
  <verify>
    <automated>{ if grep -qE '^bench-capture:' Makefile; then grep -F 'bench-capture' Makefile &amp;&amp; grep -F 'bench-compare' Makefile &amp;&amp; grep -F 'bench-capture bench-compare' Makefile &amp;&amp; grep -F 'benchstat' Makefile &amp;&amp; make -n bench-capture &gt;/dev/null 2&gt;&amp;1 &amp;&amp; make -n bench-compare OLD=a NEW=b &gt;/dev/null 2&gt;&amp;1; else true; fi; }</automated>
  </verify>
  <acceptance_criteria>
    Two acceptable end-states:

    **End-state A — targets added:**
    - `grep -E '^bench-capture:' Makefile` matches.
    - `grep -E '^bench-compare:' Makefile` matches.
    - `grep -F 'bench-capture bench-compare' Makefile` matches inside the `.PHONY` line.
    - Both targets carry a `## help-text` comment after the colon (matches
      existing style).
    - `make -n bench-capture` (dry run) exits 0 and prints a `go test ...` line.
    - `make -n bench-compare OLD=a NEW=b` (dry run) exits 0 and prints a
      `benchstat a b` line.
    - `make -n bench-compare` without OLD/NEW exits non-zero with the usage
      message routed to stderr.
    - The total Makefile diff is under 20 added lines.
    - If a CONTRIBUTING.md bullet was added, it is inside the `## Benchmarks`
      section and references both target names.

    **End-state B — targets skipped:**
    - `grep -E '^bench-capture:' Makefile` returns NON-ZERO.
    - `grep -E '^bench-compare:' Makefile` returns NON-ZERO.
    - `Makefile` is unchanged from its pre-Phase-50 state (`git diff Makefile`
      empty for this task).
    - `50-02-SUMMARY.md` documents the skip with a one-sentence reason
      (e.g. "skipped per D-A9 discretion: target additions did not fit under
      20 lines without harming readability" or "skipped to keep PR diff
      minimal").
  </acceptance_criteria>
  <done>
    Either the two targets are added with help-text, the `.PHONY` line is
    extended, and the `make -n` dry runs pass; OR the task is intentionally
    skipped with a SUMMARY note. Both outcomes are accepted per D-A9.
  </done>
</task>

<task type="auto">
  <name>Task 6: Update ROADMAP.md line 24 to retire pre-pivot wording</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (line 24 — the Phase 50 one-liner under the milestone list)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md ("Pivot 2026-04-25" block; D-A10 cancellation; D-A12 tech-debt rewrite; D-A13 verification bullets)
  </read_first>
  <action>
The Phase 50 detailed entry (later in ROADMAP.md) already reflects the pivot,
but the milestone-list one-liner on line 24 still says "restore the CI
benchmark gate" — pre-pivot intent. The plan-checker flagged this as a
context_compliance warning. Rewrite that one line to match the post-pivot
reality before the verification PR opens.

Use the Edit tool with EXACT old_string / new_string (single-line replacement,
no surrounding context required because the line is unique in the file).

**Edit — replace line 24 verbatim.**

old_string:
```
- [ ] **Phase 50: toolchain-go1.25-gopls-ci** -- Unblock Go 1.25 / gopls on `ubuntu-latest` and restore the CI benchmark gate
```

new_string:
```
- [ ] **Phase 50: toolchain-go1.25-gopls-ci** -- Make CI green on ubuntu-latest with Go 1.25 and retire the CI benchmark gate
```

Constraints:

- This is a single one-line replacement on the milestone-list line ONLY. Do
  NOT touch the detailed Phase 50 entry elsewhere in ROADMAP.md (it already
  reflects the pivot).
- Do NOT touch the Phase 51..55 lines or any other milestone bullet.
- Keep the `- [ ]` checkbox unchecked (Phase 50 is not yet complete).
- Keep the bold `**Phase 50: toolchain-go1.25-gopls-ci**` label exactly as-is.
- The new wording must use the verbatim phrase "retire the CI benchmark gate"
  (the inverse of the old "restore the CI benchmark gate") so the absence-of-
  pre-pivot-wording assertion below catches any accidental partial edit.
  </action>
  <verify>
    <automated>grep -F 'retire the CI benchmark gate' .planning/ROADMAP.md &amp;&amp; grep -F 'Make CI green on ubuntu-latest with Go 1.25' .planning/ROADMAP.md &amp;&amp; ! grep -qF 'restore the CI benchmark gate' .planning/ROADMAP.md &amp;&amp; ! grep -qF 'Unblock Go 1.25 / gopls on `ubuntu-latest` and restore' .planning/ROADMAP.md &amp;&amp; grep -E '^- \[ \] \*\*Phase 50: toolchain-go1.25-gopls-ci\*\*' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'retire the CI benchmark gate' .planning/ROADMAP.md` matches (new wording present).
    - `grep -F 'Make CI green on ubuntu-latest with Go 1.25 and retire the CI benchmark gate' .planning/ROADMAP.md` matches the full new one-liner.
    - `grep -F 'restore the CI benchmark gate' .planning/ROADMAP.md` returns NON-ZERO (pre-pivot wording fully removed).
    - `grep -E '^- \[ \] \*\*Phase 50: toolchain-go1.25-gopls-ci\*\*' .planning/ROADMAP.md` matches (checkbox + bold label preserved).
    - `grep -c '^- \[ \] \*\*Phase 50:' .planning/ROADMAP.md` returns `1` (only the milestone-list entry; the detailed entry uses a different prefix).
    - `git diff --stat .planning/ROADMAP.md` shows exactly one line changed
      (one removed, one added) on or near line 24.
    - The Phase 51..55 milestone-list lines are unchanged.
  </acceptance_criteria>
  <done>
    ROADMAP.md line 24 reads the new post-pivot one-liner; no occurrence of
    "restore the CI benchmark gate" remains anywhere in the file.
  </done>
</task>

<task type="auto">
  <name>Task 7: Refresh 50-VALIDATION.md per-task verification map and manual-only rows for the post-pivot plans</name>
  <files>.planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md</files>
  <read_first>
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md (the stale Per-Task Verification Map at lines 37–47 and the Manual-Only Verifications table at lines 57–62)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-CONTEXT.md (D-A1, D-A5, D-A7, D-A8, D-A10, D-A12, D-A13 — verification bullets the new map must address)
    - .planning/phases/50-toolchain-go1.25-gopls-ci/50-01-PLAN.md (Task 1 deletes bench.yml + capture-baseline.yml; Task 2 rewrites baselines README — referenced as 50-01-01, 50-01-02)
    - This plan (50-02-PLAN.md) post-edit (Tasks 1–7 above) — the verification map must reference the heading text those tasks add (e.g. `## Benchmarks`, `## gopls pin`, "retire the CI benchmark gate", "Cancelled (Phase 50)")
  </read_first>
  <action>
The current 50-VALIDATION.md per-task verification map and Manual-Only table
were written before the 2026-04-25 pivot. They reference `bench.yml` grep
counts, a `bench-gate` CI job, and a `capture-baseline.yml` workflow run —
all of which Plan 50-01 deletes. Rewrite both tables so every row maps cleanly
to a post-pivot plan task.

Use the Edit tool for two replacements. Read the file first; do NOT Write the
whole file (preserve unrelated sections like Test Infrastructure, Sampling
Rate, Wave 0 Requirements, and Validation Sign-Off).

Also update the Test Infrastructure block (lines 18–24): the `Config file`
row currently lists the deleted workflows; replace with the post-pivot
docs/baselines surface. Do NOT alter the Sampling Rate, Wave 0 Requirements,
or Validation Sign-Off sections — those remain accurate.

**Edit A — replace the Test Infrastructure `Config file` and `Estimated runtime` rows.**

old_string (lines 20–24, table rows verbatim):
```
| **Framework** | go test + GitHub Actions workflows |
| **Config file** | `.github/workflows/bench.yml`, `.github/workflows/capture-baseline.yml` |
| **Quick run command** | `go vet ./... && go build ./...` |
| **Full suite command** | `go test ./...` |
| **Estimated runtime** | ~120 seconds (local); CI bench gate ~10 min |
```

new_string:
```
| **Framework** | go test + GitHub Actions `go-test.yml` (post-pivot: no bench gate on CI) |
| **Config file** | `.github/workflows/go-test.yml`, `CONTRIBUTING.md` (Benchmarks + gopls pin sections), `test/bench/baselines/README.md` |
| **Quick run command** | `go vet ./... && go build ./...` |
| **Full suite command** | `go test ./...` |
| **Estimated runtime** | ~120 seconds (local); `go-test.yml` on PR ~5 min on ubuntu-latest |
```

Also update the Sampling Rate bullet that mentions the bench gate.

old_string (line 32):
```
- **Before `/gsd-verify-work`:** Full suite green AND bench-gate green on PR CI
```

new_string:
```
- **Before `/gsd-verify-work`:** Full suite green AND `go-test.yml` green on the verification PR (D-A13 bullet 1); bench gate intentionally retired (D-A12)
```

old_string (line 34):
```
- **Max feedback latency:** 120 seconds local, ~10 min for CI bench gate
```

new_string:
```
- **Max feedback latency:** 120 seconds local, ~5 min for `go-test.yml` on PR
```

**Edit B — replace the entire Per-Task Verification Map table (header row + 5 data rows, lines ~39–45) with a new table covering 50-01-01, 50-01-02, 50-02-01..07 and 50-02-08 (the consistency sweep).**

old_string (lines 39–45 verbatim — the header row, the `*Status: ⬜ pending...*` legend stays but the table body is replaced):
```
| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 50-01-01 | 01 | 1 | TOOL-01 | — | N/A (CI-only) | manual | `gh workflow run capture-baseline.yml -f milestone=v1.9` | ✅ | ⬜ pending |
| 50-02-01 | 02 | 2 | TOOL-02 | — | N/A | unit | `grep -c "v1.9-github-hosted.txt" .github/workflows/bench.yml` (==2) | ✅ | ⬜ pending |
| 50-02-02 | 02 | 2 | TOOL-01 | — | Documents gopls strategy | unit | `grep -q "gopls v0.21.1" CONTRIBUTING.md` | ✅ | ⬜ pending |
| 50-02-03 | 02 | 2 | TOOL-01 | — | Removes tech-debt note | unit | `! grep -q "benchmarks tech-debt" PROJECT.md` | ✅ | ⬜ pending |
| 50-02-04 | 02 | 2 | TOOL-01,TOOL-02 | — | CI green | manual | PR-CI run reports `bench-gate` and `test` jobs green | ✅ | ⬜ pending |
```

new_string:
```
| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 50-01-01 | 01 | 1 | TOOL-02 | T-50-01 | Removes deleted CI bench gate | unit | `! test -e .github/workflows/bench.yml && ! test -e .github/workflows/capture-baseline.yml` | ✅ | ⬜ pending |
| 50-01-02 | 01 | 1 | TOOL-01 | — | Documents local-first baseline workflow | unit | `grep -F 'Capturing a Baseline (local pre-release)' test/bench/baselines/README.md && grep -F 'Pivot 2026-04-25' test/bench/baselines/README.md` | ✅ | ⬜ pending |
| 50-02-01 | 02 | 1 | TOOL-01 | — | Documents benchmarks + gopls pin | unit | `grep -c '^## Benchmarks$' CONTRIBUTING.md` (==1) `&& grep -c '^## gopls pin$' CONTRIBUTING.md` (==1) `&& grep -F 'v0.21.1' CONTRIBUTING.md && ! grep -q '^## Benchmark CI Gate$' CONTRIBUTING.md` | ✅ | ⬜ pending |
| 50-02-02 | 02 | 1 | TOOL-01 | — | Tech-debt sentence reflects pivot | unit | `grep -F 'Benchmark gate intentionally not enforced on CI; baselines are captured locally pre-release.' .planning/PROJECT.md && ! grep -qF 'Benchmark baselines captured locally (darwin/arm64)' .planning/PROJECT.md` | ✅ | ⬜ pending |
| 50-02-03 | 02 | 1 | TOOL-02 | T-50-06 | Cancels TOOL-02 with audit trail | unit | `grep -F '~~**TOOL-02**~~' .planning/REQUIREMENTS.md && grep -F 'Cancelled (Phase 50)' .planning/REQUIREMENTS.md && grep -E '^\| TOOL-01 \| Phase 50 \| Pending \|$' .planning/REQUIREMENTS.md` | ✅ | ⬜ pending |
| 50-02-05 | 02 | 1 | TOOL-01 | T-50-04 | Optional Makefile bench targets (D-A9) | unit | End-state A: `make -n bench-capture >/dev/null && make -n bench-compare OLD=a NEW=b >/dev/null`; End-state B: `git diff Makefile` empty (skip path) | ✅ | ⬜ pending |
| 50-02-06 | 02 | 1 | TOOL-02 | — | ROADMAP one-liner reflects pivot | unit | `grep -F 'retire the CI benchmark gate' .planning/ROADMAP.md && ! grep -qF 'restore the CI benchmark gate' .planning/ROADMAP.md` | ✅ | ⬜ pending |
| 50-02-07 | 02 | 1 | TOOL-01,TOOL-02 | — | Validation map reflects post-pivot tasks | unit | `! grep -qF 'bench.yml' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md && ! grep -qF 'capture-baseline.yml' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md && grep -F 'go-test.yml is green' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` | ✅ | ⬜ pending |
| 50-02-08 | 02 | 1 | TOOL-01,TOOL-02 | — | Cross-doc consistency sweep | unit | `! grep -rn -F '.github/workflows/bench.yml' CONTRIBUTING.md .planning/PROJECT.md .planning/REQUIREMENTS.md test/bench/baselines/README.md && ! grep -rn -F '.github/workflows/capture-baseline.yml' CONTRIBUTING.md .planning/PROJECT.md .planning/REQUIREMENTS.md test/bench/baselines/README.md` | ✅ | ⬜ pending |
```

(Note: row 50-02-04 is intentionally omitted from the new map because the
post-pivot Plan 50-02 Task 4 is the cross-doc consistency sweep that has been
renumbered to 50-02-08. The new 50-02-08 row covers it. There is no separate
50-02-04 task in the post-pivot plan.)

**Edit C — replace the Manual-Only Verifications table body (lines 59–62) with a single post-pivot row.**

old_string:
```
| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Capture baseline workflow produces `v1.9-github-hosted.txt` artifact | TOOL-02 | Requires GitHub Actions runner | Trigger `capture-baseline.yml` with `milestone=v1.9`; confirm artifact uploaded to `baselines/v1.9-github-hosted.txt` and committed |
| PR-CI bench gate fires when thresholds exceeded | TOOL-02 | Requires real PR with regression | After verification PR opens, confirm `bench-gate` job ran on `ubuntu-latest` with `--p50 0.15 --p95 0.25` |
```

new_string:
```
| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| go-test.yml is green on the verification PR on ubuntu-latest with Go 1.25 (D-A13 bullet 1) | TOOL-01 | Requires real PR triggering GitHub Actions on ubuntu-latest | Open the verification PR after both plans land; confirm `go-test.yml` reports a green check on ubuntu-latest with the Go 1.25 toolchain. This is the only D-A13 bullet that cannot be asserted locally. |
```

Constraints:

- Preserve the `*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*` legend line
  immediately after the Per-Task Verification Map.
- Preserve all other sections (`## Wave 0 Requirements`, `## Validation Sign-Off`,
  the frontmatter at the top including `nyquist_compliant: true`).
- Update the frontmatter `created:` field if you choose, but it is not required
  — the file's git history is the authoritative trail.
- Do NOT introduce new top-level headings; this is a refresh, not a restructure.
  </action>
  <verify>
    <automated>! grep -qF 'bench.yml' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md &amp;&amp; ! grep -qF 'capture-baseline.yml' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md &amp;&amp; ! grep -qF 'bench-gate' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md &amp;&amp; ! grep -qF 'v1.9-github-hosted.txt' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md &amp;&amp; grep -F 'go-test.yml is green' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md &amp;&amp; grep -F '50-02-08' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md &amp;&amp; grep -F '50-02-06' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md &amp;&amp; grep -F '50-02-07' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md &amp;&amp; grep -F '50-01-02' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md &amp;&amp; grep -F 'Pivot' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'bench.yml' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` returns NON-ZERO (deleted workflow no longer referenced).
    - `grep -qF 'capture-baseline.yml' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` returns NON-ZERO.
    - `grep -qF 'bench-gate' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` returns NON-ZERO (no stale CI job reference).
    - `grep -F '50-01-01' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` matches AND its row asserts absence of bench.yml + capture-baseline.yml.
    - `grep -F '50-01-02' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` matches AND its row greps for the new baselines README heading.
    - `grep -F '50-02-06' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` matches (ROADMAP retire-wording row present).
    - `grep -F '50-02-07' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` matches (validation-refresh self-row present).
    - `grep -F '50-02-08' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` matches (sweep row renumbered).
    - `grep -F 'go-test.yml is green' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` matches in the Manual-Only table body.
    - `grep -F 'D-A13 bullet 1' .planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` matches.
    - The frontmatter (`---` block at top) is unchanged or, if `nyquist_compliant`/`status` are touched, they remain truthy/approved.
    - `## Wave 0 Requirements`, `## Validation Sign-Off`, and the
      `*Status: ⬜ pending...*` legend lines are still present in the file.
  </acceptance_criteria>
  <done>
    50-VALIDATION.md per-task verification map covers every post-pivot task
    (50-01-01, 50-01-02, 50-02-01, 50-02-02, 50-02-03, 50-02-05, 50-02-06,
    50-02-07, 50-02-08); all references to deleted workflows are gone; the
    Manual-Only table cites the verification PR's `go-test.yml` run.
  </done>
</task>

<task type="auto">
  <name>Task 8: Final consistency sweep — confirm cross-doc references align</name>
  <files>(no edits — verification-only sweep)</files>
  <read_first>
    - CONTRIBUTING.md (post-Task-1 state)
    - .planning/PROJECT.md (post-Task-2 state)
    - .planning/REQUIREMENTS.md (post-Task-3 state)
    - test/bench/baselines/README.md (state after Plan 50-01 Task 2)
  </read_first>
  <action>
This task performs a final cross-doc consistency check. It writes nothing — it
runs greps and confirms every artifact produced by Plan 50-01 + Plan 50-02 is
internally consistent and that no stale reference to the deleted workflows
remains anywhere in the repo's primary documentation surface.

If any check fails, the executor must go back to the relevant prior task and
fix the underlying file. Do NOT paper over a failure here with a new edit
that contradicts a prior task's specification.

Run each command from the repo root.
  </action>
  <verify>
    <automated>! grep -rn --include='*.md' -F '.github/workflows/bench.yml' CONTRIBUTING.md .planning/PROJECT.md .planning/REQUIREMENTS.md test/bench/baselines/README.md &amp;&amp; ! grep -rn --include='*.md' -F '.github/workflows/capture-baseline.yml' CONTRIBUTING.md .planning/PROJECT.md .planning/REQUIREMENTS.md test/bench/baselines/README.md &amp;&amp; grep -F 'CONTRIBUTING.md' test/bench/baselines/README.md &amp;&amp; grep -F 'test/bench/baselines/' CONTRIBUTING.md &amp;&amp; grep -F 'CONTRIBUTING.md' .planning/PROJECT.md &amp;&amp; grep -F 'Pivot 2026-04-25' CONTRIBUTING.md &amp;&amp; grep -F 'Pivot 2026-04-25' test/bench/baselines/README.md &amp;&amp; grep -F 'Pivot 2026-04-25' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - No reference to `.github/workflows/bench.yml` exists in CONTRIBUTING.md,
      PROJECT.md, REQUIREMENTS.md, or test/bench/baselines/README.md.
    - No reference to `.github/workflows/capture-baseline.yml` exists in any of
      those four files.
    - `test/bench/baselines/README.md` cross-references `CONTRIBUTING.md`.
    - `CONTRIBUTING.md` cross-references `test/bench/baselines/`.
    - `.planning/PROJECT.md` cross-references `CONTRIBUTING.md` (the new
      tech-debt sentence per D-A12).
    - `Pivot 2026-04-25` anchor appears in CONTRIBUTING.md, baselines README,
      and REQUIREMENTS.md (so any reader landing on any of those files can
      trace to the rationale).
    - `git status --porcelain` for this task lists no modified files (this is a
      verification task; if it requires file changes, that means a prior task
      missed something).
  </acceptance_criteria>
  <done>
    All four primary docs are internally consistent; the deleted workflows are
    not mentioned anywhere; cross-references to the rationale anchor are in place.
  </done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan modifies only Markdown documentation and (optionally) the Makefile. No new code, no new third-party action, no change to runtime trust surface. The Makefile additions invoke `go test`, `go env`, and `benchstat` — all already documented as safe-to-run developer tooling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | T (tampering) | `Makefile` `bench-capture` target | mitigate | The default `OUT` path stays under `test/bench/baselines/` so an unintentional invocation cannot overwrite arbitrary repo files. The target uses `tee` (append-once with truncation), not piped redirection that could shell-inject through user-controlled `OUT`. The `OUT` env-var is quoted to defend against IFS / glob expansion. |
| T-50-05 | I (information disclosure) | `bench-compare` target | accept | The target reads two user-specified paths and pipes them to `benchstat`. No secrets, no network. The path arguments are quoted; benchstat itself does not write outside stdout. |
| T-50-06 | T (tampering) | `.planning/REQUIREMENTS.md` strikethrough edit | mitigate | The strikethrough preserves the original requirement text under `~~...~~` (audit trail) rather than deleting it. Reviewers can reconstruct the cancelled requirement verbatim from the diff. |
| T-50-07 | I (information disclosure) | CONTRIBUTING.md cross-references to `.planning/` | accept | The cross-references point to repo-internal planning paths that already exist in git history. No external URL, no credential, no PII. |
</threat_model>

<verification>
Phase-wide verification (mirrors D-A13 bullets 1, 3, 4, 5; bullets 2 and 6 are
validated in Plan 50-01).

**D-A13 bullet 1 — `go-test.yml` green on the verification PR:**
This is validated by the PR's own CI run after both plans are merged into the
verification branch. There is no local command equivalent. Documented as the single Manual-Only row in 50-VALIDATION.md
("go-test.yml is green on the verification PR on ubuntu-latest with Go 1.25").

**D-A13 bullet 3 — CONTRIBUTING.md has the new sections:**
```sh
grep -c '^## Benchmarks$' CONTRIBUTING.md   # expected: 1
grep -c '^## gopls pin$' CONTRIBUTING.md    # expected: 1
grep -F 'v0.21.1' CONTRIBUTING.md
grep -F 'go run ./test/bench/cmd/benchgate' CONTRIBUTING.md
```

**D-A13 bullet 4 — PROJECT.md tech-debt rewritten per D-A12:**
```sh
grep -F 'Benchmark gate intentionally not enforced on CI; baselines are captured locally pre-release.' .planning/PROJECT.md
! grep -qF 'Benchmark baselines captured locally (darwin/arm64)' .planning/PROJECT.md
```

**D-A13 bullet 5 — REQUIREMENTS.md TOOL-02 cancelled per D-A10:**
```sh
grep -F '~~**TOOL-02**~~' .planning/REQUIREMENTS.md
grep -F 'Cancelled in Phase 50' .planning/REQUIREMENTS.md
grep -F 'Cancelled (Phase 50)' .planning/REQUIREMENTS.md
```

**Cross-doc consistency (Task 8):**
```sh
! grep -rn -F '.github/workflows/bench.yml' CONTRIBUTING.md .planning/PROJECT.md .planning/REQUIREMENTS.md test/bench/baselines/README.md
! grep -rn -F '.github/workflows/capture-baseline.yml' CONTRIBUTING.md .planning/PROJECT.md .planning/REQUIREMENTS.md test/bench/baselines/README.md
```

**Optional Makefile (Task 5) — accept either end-state:**
- If added: `make -n bench-capture` and `make -n bench-compare OLD=a NEW=b`
  both succeed.
- If skipped: `git diff Makefile` empty.

**Project-instruction sanity check (CLAUDE.md):**
- This phase modifies workflows (Plan 50-01) and docs (this plan); it does NOT
  modify Go source. `go vet ./...` and `go test ./...` are not strictly required
  by D-A13, but the verification PR's `go-test.yml` run satisfies them as a
  byproduct (D-A13 bullet 1).
</verification>

<success_criteria>
This plan is complete when:

- `CONTRIBUTING.md` has exactly one `## Benchmarks` heading and exactly one
  `## gopls pin` heading; the stale `## Benchmark CI Gate` heading is gone;
  the `## Running Benchmarks` section no longer claims CI enforcement.
- `.planning/PROJECT.md` "Known tech debt" paragraph's first sentence reads
  exactly as specified by D-A12; the other three tech-debt sentences are
  preserved.
- `.planning/REQUIREMENTS.md` line ~20 shows `~~**TOOL-02**~~` strikethrough
  with the cancellation note; the traceability table row reads
  `| TOOL-02 | Phase 50 | Cancelled (Phase 50) |`; the TOOL-01 row is
  unchanged.
- No `.md` file under CONTRIBUTING.md, .planning/PROJECT.md,
  .planning/REQUIREMENTS.md, or test/bench/baselines/README.md references the
  deleted workflows by filename.
- The "Pivot 2026-04-25" anchor is reachable from CONTRIBUTING.md, the
  baselines README, and REQUIREMENTS.md.
- Either `Makefile` has the two new targets (with `## help-text` and a
  `.PHONY` extension) and both `make -n` dry runs succeed, OR the Makefile is
  unchanged and 50-02-SUMMARY.md notes the skip per D-A9.
- `.planning/ROADMAP.md` line 24 reads the post-pivot one-liner ("retire the
  CI benchmark gate"); no occurrence of "restore the CI benchmark gate"
  remains anywhere in the file (Task 6).
- `.planning/phases/50-toolchain-go1.25-gopls-ci/50-VALIDATION.md` per-task
  verification map covers 50-01-01, 50-01-02, 50-02-01..03, 50-02-05..08; the
  Manual-Only table cites the verification PR's `go-test.yml` run; no
  `bench.yml` / `capture-baseline.yml` / `bench-gate` references remain (Task 7).
- Together with Plan 50-01, all six D-A13 verification bullets are addressable
  from the verification PR.
</success_criteria>

<output>
After completion, create `.planning/phases/50-toolchain-go1.25-gopls-ci/50-02-SUMMARY.md`
documenting:
- The exact CONTRIBUTING.md sections added/replaced (heading text + line ranges
  in the post-edit file).
- The PROJECT.md sentence diff (one line removed, one added).
- The REQUIREMENTS.md TOOL-02 cancellation (both edit locations).
- Whether the optional Makefile targets were added (Task 5 end-state A) or
  skipped (end-state B), with the reason for the chosen path.
- Confirmation that no stale `.github/workflows/bench.yml` /
  `capture-baseline.yml` reference remains in any of the four docs.
- The ROADMAP.md line 24 rewrite (Task 6) and the VALIDATION.md refresh
  (Task 7) — quote the new ROADMAP one-liner and list the post-pivot row IDs
  now in the verification map.
- A "ready for verification PR" checklist mapping each D-A13 bullet to the
  artifact that satisfies it (Plan 50-01 covers bullets 2 and 6; this plan
  covers bullets 3, 4, 5; bullet 1 is the PR's own `go-test.yml` run).
</output>
