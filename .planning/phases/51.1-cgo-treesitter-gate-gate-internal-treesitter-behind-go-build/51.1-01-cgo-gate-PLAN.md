---
phase: 51.1-cgo-treesitter-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/treesitter/registry.go
  - internal/treesitter/registry_cgo.go
  - internal/treesitter/registry_nocgo.go
  - internal/treesitter/available_cgo.go
  - internal/treesitter/available_nocgo.go
  - internal/treesitter/registry_test.go
  - internal/treesitter/bindings/r/binding_nocgo.go
  - internal/treesitter/bindings/swift/binding_nocgo.go
  - internal/repomap/extractor.go
  - internal/repomap/extractor_nocgo.go
  - internal/repomap/elide.go
  - internal/repomap/elide_nocgo.go
  - internal/repomap/extractor_test.go
  - internal/repomap/elide_test.go
  - internal/repomap/polyglot_rank_test.go
  - internal/repomap/render_test.go
  - internal/kernel/edit/treesitter.go
  - internal/kernel/edit/treesitter_nocgo.go
  - internal/kernel/edit/treesitter_test.go
  - internal/skill/repomap/skill_test.go
  - internal/skill/repomap/cache_persistence_test.go
  - internal/skill/repomap/skill_integration_test.go
  - internal/daemon/daemon.go
  - CONTRIBUTING.md
  - .planning/phases/51-packaging-goreleaser/deferred-items.md
autonomous: true
requirements:
  - DEF-51-02
  - SC-1-Phase51
  - AC-1
  - AC-2
  - AC-3
  - AC-4

must_haves:
  truths:
    - "CGO_ENABLED=0 go build ./cmd/serena exits 0 (AC-1)"
    - "CGO_ENABLED=0 go vet ./... exits 0 (AC-1b)"
    - "CGO_ENABLED=0 go test ./... exits 0 (AC-1c, tagged tests excluded at compile-time)"
    - "CGO_ENABLED=1 go test ./... exits 0 with all 23 treesitter languages registered (AC-2)"
    - "make release-snapshot produces 6 archives in dist/ (AC-3)"
    - "Daemon under !cgo build returns non-zero from daemon.New() with a message naming tree-sitter/CGO and the rebuild remediation (D-02)"
    - "internal/skill/repomap/skill.go compiles unchanged under CGO_ENABLED=0 (A3 verification)"
    - "DEF-51-02 in deferred-items.md shows Status: RESOLVED with pointer to phase 51.1 (D-05)"
    - "CONTRIBUTING.md no longer flags release-snapshot dry-run as expected-fail (D-04)"
  artifacts:
    - path: "internal/treesitter/registry_cgo.go"
      provides: "CGO grammar registry — verbatim former registry.go contents"
      contains: "//go:build cgo"
    - path: "internal/treesitter/registry_nocgo.go"
      provides: "!cgo stub GrammarRegistry (no GetLanguage method, callers gated)"
      contains: "//go:build !cgo"
    - path: "internal/treesitter/available_cgo.go"
      provides: "const Available = true"
    - path: "internal/treesitter/available_nocgo.go"
      provides: "const Available = false"
    - path: "internal/repomap/extractor_nocgo.go"
      provides: "Stub TagExtractor under !cgo"
    - path: "internal/repomap/elide_nocgo.go"
      provides: "Stub ElisionRenderer under !cgo"
    - path: "internal/kernel/edit/treesitter_nocgo.go"
      provides: "Stub BodyExtractor under !cgo"
  key_links:
    - from: "internal/daemon/daemon.go"
      to: "internal/treesitter.Available"
      via: "if !treesitter.Available { return nil, fmt.Errorf(...) } before line 196"
      pattern: "treesitter\\.Available"
    - from: "internal/repomap/extractor.go (cgo)"
      to: "internal/treesitter.GrammarRegistry.GetLanguage"
      via: "GetLanguage only declared in registry_cgo.go; callers all //go:build cgo"
      pattern: "GetLanguage"
    - from: "CONTRIBUTING.md"
      to: ".github/workflows/release.yml reproducibility gate"
      via: "expected-fail callout deleted; gate now expected-pass"
      pattern: "DEF-51-02"
---

<objective>
Gate the CGO-dependent tree-sitter codepaths behind `//go:build cgo` with a `//go:build !cgo` stub set, plus a daemon refusal hook, so `CGO_ENABLED=0 go build ./cmd/serena` exits 0 and goreleaser can produce 6 archives. CGO=1 builds remain byte-identical with all 23 grammars registered. Closes DEF-51-02 via Path 2.

Purpose: Unblock Phase 51 SC-1 (cross-compile matrix) and Phase 52 (packaging channels) without re-introducing CGO into goreleaser (preserves the CLAUDE.md "single Go binary, no CGO dependencies" invariant).

Output: Build-tagged file pairs across `internal/treesitter`, `internal/repomap`, and `internal/kernel/edit`; daemon refusal block; tagged tests; flipped CONTRIBUTING.md markers; DEF-51-02 status flipped to RESOLVED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/51.1-CONTEXT.md
@.planning/phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/RESEARCH.md
@.planning/phases/51-packaging-goreleaser/deferred-items.md
@internal/treesitter/registry.go
@internal/daemon/daemon.go
@internal/repomap/extractor.go
@internal/repomap/elide.go
@internal/kernel/edit/treesitter.go
@internal/skill/repomap/skill.go
@CONTRIBUTING.md

<interfaces>
<!-- Key contracts callers depend on. -->

From internal/treesitter/registry.go (CGO half, post-rename):
```go
type GrammarRegistry struct { /* ... */ }
func NewGrammarRegistry() *GrammarRegistry
func (r *GrammarRegistry) SupportsLanguage(name string) bool
func (r *GrammarRegistry) SupportedLanguages() []string
func (r *GrammarRegistry) GetLanguage(name string) (*tree_sitter.Language, bool)  // CGO-only
```

!cgo stub MUST preserve: type name, NewGrammarRegistry, SupportsLanguage, SupportedLanguages.
!cgo stub MUST OMIT: GetLanguage (its return type *tree_sitter.Language is CGO-only;
all callers of GetLanguage are themselves gated //go:build cgo per RESEARCH §4).

From RESEARCH.md §4 — files to gate //go:build cgo (cannot compile without CGO):
- internal/repomap/extractor.go        (uses tree_sitter.Query, Node, NewQuery, NewParser, NewQueryCursor)
- internal/repomap/elide.go            (uses tree_sitter.Language, Node, NewParser)
- internal/kernel/edit/treesitter.go   (uses tree_sitter.NewParser, Node)

From RESEARCH.md §2 — test files to tag //go:build cgo (9 files):
- internal/treesitter/registry_test.go
- internal/repomap/{extractor,elide,polyglot_rank,render}_test.go
- internal/skill/repomap/{skill,cache_persistence,skill_integration}_test.go
- internal/kernel/edit/treesitter_test.go

Daemon hook (per RESEARCH §3, before daemon.go:196):
```go
if !treesitter.Available {
    return nil, fmt.Errorf("tree-sitter is unavailable in this build " +
        "(CGO_ENABLED=0): rebuild with CGO_ENABLED=1 or download the " +
        "CGO=1 release binary (see CONTRIBUTING.md > Releasing)")
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gate internal/treesitter package and add Available constant</name>
  <files>
    internal/treesitter/registry.go (rename to registry_cgo.go),
    internal/treesitter/registry_cgo.go,
    internal/treesitter/registry_nocgo.go,
    internal/treesitter/available_cgo.go,
    internal/treesitter/available_nocgo.go,
    internal/treesitter/registry_test.go,
    internal/treesitter/bindings/r/binding_nocgo.go (DELETE),
    internal/treesitter/bindings/swift/binding_nocgo.go (DELETE)
  </files>
  <behavior>
    - CGO=1 build: `treesitter.NewGrammarRegistry()` returns the same registry as today with all 23 languages, `treesitter.Available == true`.
    - CGO=0 build: `treesitter` package compiles; `NewGrammarRegistry()` returns a zero-value `*GrammarRegistry`; `SupportsLanguage(any)` returns false; `SupportedLanguages()` returns nil; `GetLanguage` is NOT declared in the !cgo half (per D-01 + RESEARCH §4 — callers are themselves gated).
    - `treesitter.Available` resolves to `false` under !cgo, `true` under cgo, at compile time.
  </behavior>
  <action>
    Per D-01 and RESEARCH §1 + §5:
    1. `git mv internal/treesitter/registry.go internal/treesitter/registry_cgo.go` and prepend `//go:build cgo\n\n` (line 1 = build tag, line 2 = blank — pitfall 3 in RESEARCH).
    2. Create `internal/treesitter/registry_nocgo.go` with `//go:build !cgo`, `package treesitter`, declaring only `type GrammarRegistry struct{}`, `func NewGrammarRegistry() *GrammarRegistry { return &GrammarRegistry{} }`, `func (*GrammarRegistry) SupportsLanguage(string) bool { return false }`, `func (*GrammarRegistry) SupportedLanguages() []string { return nil }`. Do NOT declare `GetLanguage` here (RESEARCH §4 refinement).
    3. Create `internal/treesitter/available_cgo.go` (`//go:build cgo`, `const Available = true`) and `internal/treesitter/available_nocgo.go` (`//go:build !cgo`, `const Available = false`).
    4. Prepend `//go:build cgo\n\n` to `internal/treesitter/registry_test.go` (D-03).
    5. Delete `internal/treesitter/bindings/r/binding_nocgo.go` and `internal/treesitter/bindings/swift/binding_nocgo.go` — dead code under D-01 since the whole package is now gated (RESEARCH §5). Keep the `//go:build cgo` `binding.go` files and `src/` dirs untouched.
    Choice rationale: single rename + minimum-surface stub minimizes diff churn (CONTEXT line 64 discretion).
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix &amp;&amp; CGO_ENABLED=0 go vet ./internal/treesitter/... &amp;&amp; CGO_ENABLED=1 go test ./internal/treesitter/... -count=1</automated>
  </verify>
  <done>
    - `CGO_ENABLED=0 go vet ./internal/treesitter/...` exits 0.
    - `CGO_ENABLED=1 go test ./internal/treesitter/... -count=1` exits 0 (registry_test.go gated, runs).
    - `git ls-files internal/treesitter/bindings/{r,swift}/binding_nocgo.go` returns empty.
    - `grep -c '^//go:build cgo$' internal/treesitter/registry_cgo.go` returns 1.
    - `grep -c '^//go:build !cgo$' internal/treesitter/registry_nocgo.go` returns 1.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Gate caller files (repomap + kernel/edit) with !cgo stubs and tag downstream tests</name>
  <files>
    internal/repomap/extractor.go,
    internal/repomap/extractor_nocgo.go,
    internal/repomap/elide.go,
    internal/repomap/elide_nocgo.go,
    internal/repomap/extractor_test.go,
    internal/repomap/elide_test.go,
    internal/repomap/polyglot_rank_test.go,
    internal/repomap/render_test.go,
    internal/kernel/edit/treesitter.go,
    internal/kernel/edit/treesitter_nocgo.go,
    internal/kernel/edit/treesitter_test.go,
    internal/skill/repomap/skill_test.go,
    internal/skill/repomap/cache_persistence_test.go,
    internal/skill/repomap/skill_integration_test.go
  </files>
  <behavior>
    - CGO=1 build/test: all repomap and kernel/edit functionality unchanged; all tagged tests run and pass.
    - CGO=0 build: `internal/repomap` and `internal/kernel/edit` packages compile via stub siblings; their public types (`TagExtractor`, `ElisionRenderer`, `BodyExtractor`) exist with the constructors that `internal/skill/repomap/skill.go` and `internal/daemon/daemon.go` reference; constructors return either a zero stub or an error sentinel — never reached at runtime because the daemon refuses to start (see Task 3).
    - `internal/skill/repomap/skill.go` is NOT modified and compiles under CGO=0 (A3 verification — RESEARCH Assumptions Log).
  </behavior>
  <action>
    Per RESEARCH §4 (caller compile verification — the surprise broader than CONTEXT framed):
    1. Prepend `//go:build cgo\n\n` to `internal/repomap/extractor.go`, `internal/repomap/elide.go`, `internal/kernel/edit/treesitter.go`.
    2. Create `internal/repomap/extractor_nocgo.go` (`//go:build !cgo`, `package repomap`) with stub `type TagExtractor struct{}` and `func NewTagExtractor(*treesitter.GrammarRegistry) *TagExtractor { return &TagExtractor{} }` plus stub method signatures matching the public surface that `internal/skill/repomap/skill.go` reaches transitively. Read the cgo `extractor.go` to enumerate the exact public API and mirror it; methods should return zero values + nil errors (unreachable under D-02). Import the `treesitter` package (which now has its own !cgo stub from Task 1).
    3. Create `internal/repomap/elide_nocgo.go` similarly — mirror the public surface that callers outside the cgo file reference; bodies return zero values.
    4. Create `internal/kernel/edit/treesitter_nocgo.go` similarly — mirror the `BodyExtractor` constructor (`NewBodyExtractor(*treesitter.GrammarRegistry) *BodyExtractor`) and any methods called from `internal/kernel/edit/edit.go` or other ungated kernel/edit files. If a method's signature names a `tree_sitter.*` type, that method's caller MUST already be gated cgo — verify by grep.
    5. Prepend `//go:build cgo\n\n` to all 9 test files listed in `<files>` (D-03 — full set per RESEARCH §2).
    6. Verify A3 explicitly: run `CGO_ENABLED=0 go vet ./internal/skill/repomap/...` — must exit 0 with `internal/skill/repomap/skill.go` UNCHANGED. If a transitive `tree_sitter.*` reference is found, surface it and tag `skill.go` itself.
    7. Iteration loop: run `CGO_ENABLED=0 go vet ./...` and resolve any additional file flagged. The set above is exhaustive per RESEARCH but the canonical check is the toolchain.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix &amp;&amp; CGO_ENABLED=0 go vet ./... &amp;&amp; CGO_ENABLED=0 go test ./... -count=1 &amp;&amp; CGO_ENABLED=1 go test ./... -count=1</automated>
  </verify>
  <done>
    - `CGO_ENABLED=0 go build ./cmd/serena` exits 0 (AC-1).
    - `CGO_ENABLED=0 go vet ./...` exits 0 (AC-1b).
    - `CGO_ENABLED=0 go test ./... -count=1` exits 0 (AC-1c — tagged tests excluded at compile time).
    - `CGO_ENABLED=1 go test ./... -count=1` exits 0 with all packages green (AC-2 partial — full byte-identical check at task 3).
    - `git diff internal/skill/repomap/skill.go` is empty (A3 satisfied).
    - `grep -L '^//go:build cgo$' internal/repomap/{extractor,elide}.go internal/kernel/edit/treesitter.go` is empty (all three carry the tag).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire daemon refusal hook, flip CI marker, update DEF-51-02 status, prove byte-identical CGO=1 release</name>
  <files>
    internal/daemon/daemon.go,
    CONTRIBUTING.md,
    .planning/phases/51-packaging-goreleaser/deferred-items.md
  </files>
  <behavior>
    - CGO=0: `daemon.New()` returns a non-nil error whose message names "tree-sitter", "CGO_ENABLED=0", and the rebuild remediation; `cmd/serena` exits non-zero (D-02 + A1).
    - CGO=1: daemon construction unchanged byte-for-byte (the refusal block is dead code at compile time when `treesitter.Available == true` constant — Go compiler eliminates it).
    - `make release-snapshot` invokes goreleaser under `CGO_ENABLED=0` (per .goreleaser.yaml, A2) and produces 6 archives (AC-3).
    - CONTRIBUTING.md no longer flags the dry-run as expected-fail (D-04).
    - DEF-51-02 in `.planning/phases/51-packaging-goreleaser/deferred-items.md` shows `Status: RESOLVED` with a pointer to phase 51.1 (D-05).
  </behavior>
  <action>
    Per D-02, D-04, D-05 and RESEARCH §3, §6, §7:
    1. In `internal/daemon/daemon.go`, immediately before line 196 (the `treesitter.NewGrammarRegistry()` call), insert the refusal block from RESEARCH §3 example:
       ```go
       // 6a. Refuse to start under CGO_ENABLED=0 (DEF-51-02 / Phase 51.1 / D-02).
       if !treesitter.Available {
           return nil, fmt.Errorf("tree-sitter is unavailable in this build " +
               "(CGO_ENABLED=0): rebuild with CGO_ENABLED=1 or download the " +
               "CGO=1 release binary (see CONTRIBUTING.md > Releasing)")
       }
       ```
       `fmt` is already imported (RESEARCH §3 verified). Verify A1 by reading `cmd/serena/main.go` to confirm a non-nil error from daemon construction propagates to `os.Exit` (or equivalent). If it does not, add explicit handling — A1 risk is LOW per RESEARCH but the planner re-verifies.
    2. In `CONTRIBUTING.md`:
       - Around line 169: delete the entire `>` blockquote callout introduced by commit 1a1ce93a flagging DEF-51-02 as expected-fail. Use `git show 1a1ce93a -- CONTRIBUTING.md` if needed to identify the exact block boundaries.
       - Around line 215: replace the "Once DEF-51-02 closes…" sentence with the pre-1a1ce93a wording: `"The dry-run still validates the build matrix, archive packaging, and checksums.txt generation."` (RESEARCH §6 verbatim).
       Per D-04 — no `.github/workflows/*.yml` edits; the existing release.yml reproducibility gate (lines 82-113) IS the smoke gate.
    3. In `.planning/phases/51-packaging-goreleaser/deferred-items.md` line 127, replace the `**Status:** OPEN ...` line with the RESOLVED block from RESEARCH §7 (verbatim, pointer to phase 51.1 PLAN/SUMMARY) (D-05). Optionally append a DEF-51-03 entry capturing the publish-or-not policy concern (D-06 / discretion).
    4. Run a CGO=1 release-snapshot to validate AC-2 (byte-identical) + AC-3 (6 archives): `make release-snapshot` then `ls dist/*.tar.gz dist/*.zip 2>/dev/null | wc -l` should equal 6 (the 5 tarballs + 1 windows zip per Phase 51 SC-1; verify exact archive set against `.goreleaser.yaml`). Note: per Memory rule (no CI benchmarks), this is a LOCAL verification step.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix &amp;&amp; CGO_ENABLED=0 go build -o /tmp/serena-nocgo ./cmd/serena &amp;&amp; /tmp/serena-nocgo daemon 2>&amp;1 | grep -qE 'tree-sitter.*CGO_ENABLED=0.*rebuild' &amp;&amp; make release-snapshot &amp;&amp; test "$(ls dist/serena_*.tar.gz dist/serena_*.zip 2>/dev/null | wc -l | tr -d ' ')" = "6"</automated>
  </verify>
  <done>
    - CGO=0 binary refuses to start with a message containing "tree-sitter", "CGO_ENABLED=0", and "rebuild" (D-02).
    - `make release-snapshot` exits 0 and produces exactly 6 archives in `dist/` (AC-3).
    - `CGO_ENABLED=1 go test ./... -count=1` exits 0 with all 23 grammars registered (AC-2 — assert via existing `registry_test.go` language-count assertion).
    - `grep -c 'expected to fail until DEF-51-02' CONTRIBUTING.md` returns 0 (D-04 marker removed).
    - `grep -A1 'DEF-51-02' .planning/phases/51-packaging-goreleaser/deferred-items.md | grep -v '^#' | grep -c 'RESOLVED'` returns ≥ 1 (D-05).
    - `git diff --stat .github/workflows/` is empty (D-04: no workflow YAML edits).
  </done>
</task>

</tasks>

<verification>
After all three tasks, run end-to-end (per RESEARCH Validation Architecture > Sampling Rate > Phase gate):

```bash
CGO_ENABLED=0 go build ./cmd/serena                       # AC-1
CGO_ENABLED=0 go vet ./...                                # AC-1b
CGO_ENABLED=0 go test ./... -count=1                      # AC-1c
CGO_ENABLED=1 go test ./... -count=1                      # AC-2
make release-snapshot                                      # AC-3
ls dist/serena_*.tar.gz dist/serena_*.zip | wc -l          # AC-3 → 6
grep -c 'expected to fail until DEF-51-02' CONTRIBUTING.md # D-04 → 0
grep 'Status:.*RESOLVED' .planning/phases/51-packaging-goreleaser/deferred-items.md  # D-05
```

CI smoke gate (AC-4) is the existing `.github/workflows/release.yml` reproducibility gate — already wired, becomes expected-pass at next tag push without YAML edits (D-04 + RESEARCH §6).

A3 explicit re-verify: `git diff internal/skill/repomap/skill.go` MUST be empty after this phase.
</verification>

<success_criteria>
- AC-1: `CGO_ENABLED=0 go build ./cmd/serena` exits 0 ✓
- AC-2: `CGO_ENABLED=1` build byte-identical, all 23 languages registered, full test suite green ✓
- AC-3: `make release-snapshot` produces 6 archives ✓
- AC-4: CGO=0 build smoke gated in CI via existing release.yml reproducibility gate (CONTRIBUTING.md marker flipped to expected-pass) ✓
- D-01: !cgo stub returns errors / sentinels loudly; no silent fallback ✓
- D-02: daemon refuses to start under CGO=0 with cause + remediation ✓
- D-03: 9 tree-sitter-exercising tests tagged `//go:build cgo`; no !cgo stub-assertion tests added ✓
- D-04: CONTRIBUTING.md "expected to fail until DEF-51-02" callout deleted; no new CI workflow ✓
- D-05: DEF-51-02 status RESOLVED with pointer to phase 51.1 ✓
- A3 risk verified: `internal/skill/repomap/skill.go` unchanged and compiles under CGO=0 ✓
</success_criteria>

<output>
After completion, create `.planning/phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/51.1-01-SUMMARY.md` documenting:
- Final file split shape (treesitter, repomap, kernel/edit splits applied; binding_nocgo.go files deleted)
- A3 verification result (skill.go unchanged confirmation)
- Local `make release-snapshot` archive count proof
- Exact lines flipped in CONTRIBUTING.md and deferred-items.md
- Whether DEF-51-03 entry was filed (D-06 discretion outcome)
</output>
