---
phase: 52
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: [PKG-02, PKG-03, PKG-04]
tags: [packaging, goreleaser, github, pat]
user_setup:
  - service: github
    why: "Create serena-packages repo and PACKAGES_PAT secret required by Phase 52 brews/scoops auto-publish (D-01, D-04)"
    env_vars:
      - name: PACKAGES_PAT
        source: "GitHub Settings -> Developer settings -> Personal access tokens -> Fine-grained tokens; scope: postfix/serena-packages only; permissions: contents:write"
    dashboard_config:
      - task: "Create empty postfix/serena-packages repo with initial README commit (so default branch main exists)"
        location: "https://github.com/new"
      - task: "Mint fine-grained PAT scoped exclusively to postfix/serena-packages with contents:write; expiration <=90 days"
        location: "https://github.com/settings/personal-access-tokens/new"
      - task: "Set PACKAGES_PAT secret on postfix/serena repo"
        location: "https://github.com/postfix/serena/settings/secrets/actions"

must_haves:
  truths:
    - "github.com/postfix/serena-packages exists with default branch main and an initial commit"
    - "Fine-grained PAT exists with contents:write limited to postfix/serena-packages only"
    - "PACKAGES_PAT secret is set on postfix/serena repo"
    - "Repo LICENSE SPDX identifier (MIT) is captured for use in subsequent plans"
  artifacts:
    - path: "github.com/postfix/serena-packages (out-of-band)"
      provides: "publish target for brews + scoops blocks"
      contains: "default branch main with initial commit"
    - path: ".planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md"
      provides: "record of bootstrap completion + PAT expiration date for rotation tracking"
  key_links:
    - from: "PACKAGES_PAT secret on postfix/serena"
      to: "postfix/serena-packages repo"
      via: "fine-grained PAT scoped to single repo with contents:write"
      pattern: "least-privilege; D-04 rejects classic PAT and GitHub App for this phase"
---

<objective>
Bootstrap the human-only prerequisites for Phase 52: create the `postfix/serena-packages` monorepo (single tap + bucket destination per D-01), mint the fine-grained `PACKAGES_PAT` (D-04), wire it as a GitHub Actions secret on `postfix/serena`, and confirm the LICENSE SPDX identifier (MIT) for substitution into Wave 1's goreleaser blocks.

Purpose: Plans 02 and 03 cannot run until the publish target repo and the auth secret exist. This plan is autonomous=false because creating GitHub repos, minting fine-grained PATs, and setting secrets all require human interaction with the GitHub web UI (no CLI-only path that satisfies D-04's least-privilege scoping for fine-grained PATs).

Output:
- `postfix/serena-packages` repo with default branch `main` and initial commit (avoids Pitfall 7 in 52-RESEARCH.md)
- `PACKAGES_PAT` secret set on `postfix/serena`
- Confirmed SPDX identifier `MIT` for downstream plans
- 52-01-SUMMARY.md recording bootstrap completion
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-packaging-distribution-channels/52-CONTEXT.md
@.planning/phases/52-packaging-distribution-channels/52-RESEARCH.md
@LICENSE
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Bootstrap serena-packages repo + PACKAGES_PAT secret (human-only)</name>
  <read_first>
    - LICENSE (confirm SPDX identifier — verified MIT in PATTERNS.md, but re-confirm at execution)
    - .planning/phases/52-packaging-distribution-channels/52-CONTEXT.md (D-01, D-04, D-05)
    - .planning/phases/52-packaging-distribution-channels/52-RESEARCH.md "Pitfall 6" (PAT rotation) and "Pitfall 7" (initial commit required)
  </read_first>
  <files>(none — human GitHub UI work; no repo files modified in this task)</files>
  <action>Human-only checkpoint: maintainer performs the GitHub bootstrap (gh repo create postfix/serena-packages --add-readme; mint fine-grained PAT scoped to that repo with contents:write via web UI; gh secret set PACKAGES_PAT --repo postfix/serena). See &lt;how-to-verify&gt; below for the exact 4-step ritual and paste-back evidence format. No automation — D-04 fine-grained PAT scoping requires the GitHub web UI.</action>
  <verify>
    <automated>head -1 LICENSE | grep -F "MIT License" &amp;&amp; gh repo view postfix/serena-packages --json defaultBranchRef -q .defaultBranchRef.name | grep -q main &amp;&amp; gh secret list --repo postfix/serena | grep -q PACKAGES_PAT</automated>
  </verify>
  <what-built>
    Pre-flight: confirm LICENSE SPDX (`head -3 LICENSE` should print "MIT License"). No automation has run; this checkpoint exists because creating a GitHub repo, minting a fine-grained PAT, and setting a repo secret cannot be done autonomously per D-04's scoping requirement (the PAT must be limited to postfix/serena-packages ONLY, which the GitHub web UI configures interactively).
  </what-built>
  <how-to-verify>
    Maintainer performs the following 4 steps and pastes the resulting evidence back to the executor:

    1. Run `head -3 LICENSE` in the repo root and confirm output begins with "MIT License". Expected: SPDX identifier is `MIT` (NOT `Apache-2.0`).

    2. Create the empty publish-target repo (use `gh repo create` with `--add-readme` so the default branch exists per Pitfall 7):
       ```bash
       gh repo create postfix/serena-packages --public \
         --description "Homebrew tap and Scoop bucket for Serena" \
         --add-readme
       ```
       Verify: `gh repo view postfix/serena-packages --json defaultBranchRef -q .defaultBranchRef.name` returns `main`.

    3. Mint a fine-grained PAT in the GitHub web UI (cannot be done via CLI per D-04 scoping):
       - Visit https://github.com/settings/personal-access-tokens/new
       - Token name: `serena-packages-publish`
       - Expiration: 90 days (per Pitfall 6 — record expiration date in summary for rotation tracking)
       - Resource owner: postfix
       - Repository access: "Only select repositories" -> select ONLY `postfix/serena-packages`
       - Permissions -> Repository permissions -> Contents: Read and write
       - Generate token and copy it immediately

    4. Set the PAT as the `PACKAGES_PAT` secret on `postfix/serena`:
       ```bash
       gh secret set PACKAGES_PAT --repo postfix/serena
       # paste the token when prompted
       ```
       Verify: `gh secret list --repo postfix/serena | grep PACKAGES_PAT` shows the secret.

    Expected paste-back evidence (all 4 lines):
    - "LICENSE SPDX: MIT"
    - "serena-packages default branch: main"
    - "PAT expiration: YYYY-MM-DD"
    - "PACKAGES_PAT secret set on postfix/serena: confirmed"
  </how-to-verify>
  <resume-signal>Type "bootstrap complete" and paste the 4 evidence lines, OR describe what failed (e.g., gh auth issue, PAT scope rejected)</resume-signal>
  <acceptance_criteria>
    - `head -3 LICENSE` first line is exactly `MIT License`
    - `gh repo view postfix/serena-packages --json defaultBranchRef -q .defaultBranchRef.name` returns `main`
    - `gh secret list --repo postfix/serena` output contains `PACKAGES_PAT`
    - PAT expiration date recorded for Pitfall 6 rotation tracking
  </acceptance_criteria>
  <done>
    All 4 evidence lines pasted; downstream Wave 1 plans can reference SPDX=MIT and trust that PACKAGES_PAT resolves at release time.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 52-01-SUMMARY.md recording bootstrap evidence</name>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (summary template)
    - .planning/phases/51-packaging-goreleaser/51-01-SUMMARY.md (analog summary style from preceding phase)
  </read_first>
  <files>.planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md</files>
  <action>
    Create the SUMMARY using the standard template. Capture verbatim the 4 evidence lines from Task 1's resume-signal paste-back:
    - LICENSE SPDX: MIT (per D-01/D-04 substitution into Wave 1 plans)
    - serena-packages default branch: main (Pitfall 7 mitigated)
    - PACKAGES_PAT expiration: <YYYY-MM-DD from Task 1>
    - PACKAGES_PAT secret set: confirmed on postfix/serena

    Add a "Rotation reminder" sub-section noting the expiration date verbatim and pointing at the future Plan 03 RELEASING.md "Rotating PACKAGES_PAT" section so the calendar reminder cycle is documented. Include "License substituted as MIT (NOT Apache-2.0 — research doc placeholder)" so downstream plans cannot regress to the wrong SPDX.
  </action>
  <verify>
    <automated>test -f .planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md &amp;&amp; grep -q "MIT" .planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md &amp;&amp; grep -q "PACKAGES_PAT" .planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md &amp;&amp; grep -q "main" .planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md
    - Contains string "MIT" (LICENSE SPDX confirmed)
    - Contains string "PACKAGES_PAT" (secret name)
    - Contains string "main" (default branch confirmed)
    - Contains a recorded PAT expiration date (YYYY-MM-DD format)
  </acceptance_criteria>
  <done>
    Summary committed; Wave 1 plans can begin and will substitute `license: "MIT"` into goreleaser blocks per evidence captured here.
  </done>
</task>

</tasks>

<verification>
- `head -3 LICENSE` first line equals `MIT License`
- `gh repo view postfix/serena-packages` succeeds and reports default branch `main`
- `gh secret list --repo postfix/serena` includes `PACKAGES_PAT`
- 52-01-SUMMARY.md exists with all 4 evidence lines
</verification>

<success_criteria>
- Wave 1 plans (02) can resolve `${{ secrets.PACKAGES_PAT }}` at release time
- The publish-target repo exists with a default branch (Pitfall 7 avoided)
- LICENSE SPDX identifier is captured authoritatively as `MIT` for substitution into all Wave 1 goreleaser blocks (Pitfall: research doc placeholder was Apache-2.0 — must NOT propagate)
- PAT expiration date is recorded so Pitfall 6 rotation reminder can be set
</success_criteria>

<output>
After completion, create `.planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md`
</output>
