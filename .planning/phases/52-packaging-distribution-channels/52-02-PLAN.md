---
phase: 52
plan: 02
type: execute
wave: 1
depends_on: [52-01]
files_modified:
  - .goreleaser.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [PKG-02, PKG-03, PKG-04]
tags: [packaging, goreleaser, homebrew, scoop, nfpms]

must_haves:
  truths:
    - "goreleaser check passes against augmented .goreleaser.yml"
    - "goreleaser release --snapshot --clean produces dist/homebrew/Formula/serena.rb, dist/scoop/bucket/serena.json, dist/serena_*.deb, dist/serena_*.rpm"
    - "Pre-release tags do NOT push the Formula or Scoop manifest (skip_upload: auto honored)"
    - "Linux deb/rpm declare NO runtime dependencies (musl-static binary, Pitfall 3)"
    - "PACKAGES_PAT is wired ONLY on the goreleaser step env (not workflow-level — Anti-Pattern guard)"
  artifacts:
    - path: ".goreleaser.yml"
      provides: "brews:, scoops:, nfpms: top-level blocks"
      contains: "brews:"
    - path: ".goreleaser.yml"
      provides: "Scoop publishing config"
      contains: "scoops:"
    - path: ".goreleaser.yml"
      provides: "deb+rpm packaging config"
      contains: "nfpms:"
    - path: ".github/workflows/release.yml"
      provides: "PACKAGES_PAT env on goreleaser step"
      contains: "PACKAGES_PAT"
  key_links:
    - from: ".goreleaser.yml brews:"
      to: "github.com/postfix/serena-packages"
      via: "repository.token = {{ .Env.PACKAGES_PAT }}"
      pattern: "PACKAGES_PAT"
    - from: ".goreleaser.yml scoops:"
      to: "github.com/postfix/serena-packages"
      via: "repository.token = {{ .Env.PACKAGES_PAT }}"
      pattern: "PACKAGES_PAT"
    - from: ".goreleaser.yml brews/scoops"
      to: ".goreleaser.yml archives.id=archive"
      via: "ids: [archive] cross-reference"
      pattern: "ids:.*archive"
    - from: ".github/workflows/release.yml goreleaser step env"
      to: "${{ secrets.PACKAGES_PAT }}"
      via: "step-level env (NOT workflow-level — Anti-Pattern in 52-RESEARCH.md)"
      pattern: "PACKAGES_PAT.*secrets\\.PACKAGES_PAT"
---

<objective>
Append three additive top-level blocks (`nfpms:`, `brews:`, `scoops:`) to `.goreleaser.yml` and wire `PACKAGES_PAT` onto the goreleaser step in `release.yml`. This satisfies PKG-02 (Homebrew Formula auto-publish), PKG-03 (Scoop manifest auto-publish), and PKG-04 (deb+rpm via embedded nFPM) — all three downstream of Phase 51's existing `archives:` entry (id=`archive`) and Phase 51's keyless cosign signing of the checksum file (which transitively covers deb/rpm via checksums.txt).

Purpose: Phase 52's three install paths share one configuration file (`.goreleaser.yml`) and one new auth env var (`PACKAGES_PAT`). They are bundled into a single plan because they all modify the same file (forcing serial editing) and they form one schema-validated unit (`goreleaser check` validates the entire file atomically). Splitting across three plans would force shared-file conflicts and triple the plans' goreleaser-snapshot verification overhead.

Output:
- `.goreleaser.yml` with `nfpms:`, `brews:`, `scoops:` blocks appended after `release:` (line 146)
- `release.yml` with `PACKAGES_PAT: ${{ secrets.PACKAGES_PAT }}` added under the goreleaser step's `env:` map
- `goreleaser check` passes
- `goreleaser release --snapshot --clean` produces all four expected artifact families locally
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-packaging-distribution-channels/52-CONTEXT.md
@.planning/phases/52-packaging-distribution-channels/52-RESEARCH.md
@.planning/phases/52-packaging-distribution-channels/52-PATTERNS.md
@.planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md
@.goreleaser.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append nfpms:, brews:, scoops: blocks to .goreleaser.yml</name>
  <read_first>
    - .goreleaser.yml (entire file — preserve all existing blocks; append at EOF after release: block, line 146)
    - .planning/phases/52-packaging-distribution-channels/52-RESEARCH.md "Pattern: Reference .goreleaser.yml additions" and "Pitfall 1, 3, 4, 5"
    - .planning/phases/52-packaging-distribution-channels/52-PATTERNS.md "Shared Patterns" — schema pin, decision-ID comments, ids:[archive], skip_upload:auto, license MIT
    - .planning/phases/52-packaging-distribution-channels/52-CONTEXT.md (D-01 monorepo path, D-02 deb+rpm, D-04 PACKAGES_PAT, D-05 direct push)
    - LICENSE (re-confirm SPDX = MIT — NOT Apache-2.0; placeholder in research doc must NOT propagate)
  </read_first>
  <files>.goreleaser.yml</files>
  <action>
    Append the three blocks below VERBATIM after the existing `release:` block (currently ending at line 146). Do NOT modify any line above 146. Order matters: nfpms first (no external repo dependency, lowest blast radius), then brews, then scoops.

    Substitute `license: "MIT"` everywhere — the research doc used `Apache-2.0` as a placeholder; PATTERNS.md confirmed the repo's actual LICENSE is MIT.

    Use `ids: [archive]` to cross-reference Phase 51's existing archives entry (line 100, `- id: archive`). Use `skip_upload: auto` on brews+scoops only (Pitfall 5: prevents pre-release tags from updating stable Formula/manifest). Do NOT add skip_upload to nfpms (deb/rpm should attach to pre-release GitHub Releases for testers).

    Do NOT declare `dependencies:` in nfpms (Pitfall 3: Linux binaries are zig-built static against musl per Phase 51 D-13 — declaring libc6 would break installs on minimal containers and Alpine derivatives).

    Append exactly this content (preserve indentation and comments verbatim):

    ```yaml

    # Phase 52 — packaging-distribution-channels. See .planning/phases/52-packaging-distribution-channels/.
    # Locked decisions: D-01 (single packages monorepo postfix/serena-packages),
    # D-02 (deb+rpm via nfpms; AUR deferred), D-04 (fine-grained PACKAGES_PAT),
    # D-05 (direct push to default branch — no PR gate).

    # PKG-04: Linux native packages (deb+rpm) via embedded nFPM.
    # Linux only; macOS/Windows skipped automatically. NO dependencies declared
    # — Phase 51 builds zig-static against musl, no runtime libc resolution.
    # See 52-RESEARCH.md Pitfall 3.
    nfpms:
      - id: linux-pkg
        package_name: serena
        builds:
          - serena
        vendor: "postfix"
        homepage: "https://github.com/postfix/serena"
        maintainer: "postfix <noreply@github.com>"
        description: |
          Serena — The IDE for your coding agent.
          LSP-backed MCP runtime providing semantic code retrieval, editing,
          and refactoring across 52 languages.
        license: "MIT"
        formats:
          - deb
          - rpm
        bindir: /usr/bin
        # No contents: stanza — goreleaser auto-installs the binary at bindir.
        # No dependencies — static musl binary (Pitfall 3).
        # No scripts — nothing to set up beyond the binary.
        # rpm requires a one-line summary distinct from description.
        rpm:
          summary: "LSP-backed MCP runtime for coding agents"

    # PKG-02: Homebrew Formula auto-publish to postfix/serena-packages.
    # Decision: use brews: (NOT homebrew_casks:) because casks are macOS-only;
    # success criteria require Linux amd64 brew install. brews: is deprecated in
    # goreleaser v2.10 but still functional through v2.x; revisit at v3.
    # See 52-RESEARCH.md Pitfall 1 and Q1.
    brews:
      - name: serena
        ids:
          - archive
        skip_upload: auto   # Pitfall 5: don't push Formula on pre-release tags
        repository:
          owner: postfix
          name: serena-packages
          branch: main
          token: "{{ .Env.PACKAGES_PAT }}"
        directory: Formula
        homepage: "https://github.com/postfix/serena"
        description: "Serena — The IDE for your coding agent. LSP-backed MCP runtime."
        license: "MIT"
        install: |
          bin.install "serena"
        test: |
          system "#{bin}/serena", "--version"
        commit_author:
          name: goreleaserbot
          email: bot@goreleaser.com
        commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"

    # PKG-03: Scoop manifest auto-publish (Windows amd64).
    # Plural `scoops:` is v2 schema (was singular `scoop:` in v1).
    scoops:
      - name: serena
        ids:
          - archive
        skip_upload: auto   # Pitfall 5: don't push manifest on pre-release tags
        repository:
          owner: postfix
          name: serena-packages
          branch: main
          token: "{{ .Env.PACKAGES_PAT }}"
        directory: bucket
        homepage: "https://github.com/postfix/serena"
        description: "Serena — The IDE for your coding agent. LSP-backed MCP runtime."
        license: "MIT"
        commit_author:
          name: goreleaserbot
          email: bot@goreleaser.com
        commit_msg_template: "Scoop update for {{ .ProjectName }} version {{ .Tag }}"
    ```
  </action>
  <verify>
    <automated>goreleaser check &amp;&amp; grep -c "^nfpms:" .goreleaser.yml | grep -q "^1$" &amp;&amp; grep -c "^brews:" .goreleaser.yml | grep -q "^1$" &amp;&amp; grep -c "^scoops:" .goreleaser.yml | grep -q "^1$" &amp;&amp; grep -c "skip_upload: auto" .goreleaser.yml | grep -q "^2$" &amp;&amp; ! grep -E "^\s+dependencies:" .goreleaser.yml &amp;&amp; ! grep -F 'license: "Apache-2.0"' .goreleaser.yml</automated>
  </verify>
  <acceptance_criteria>
    - `goreleaser check` exit code 0
    - Exactly one `nfpms:` top-level block (grep -c "^nfpms:" returns 1)
    - Exactly one `brews:` top-level block (grep -c "^brews:" returns 1)
    - Exactly one `scoops:` top-level block (grep -c "^scoops:" returns 1)
    - Exactly two `skip_upload: auto` lines (one each for brews + scoops, NONE for nfpms)
    - NO `dependencies:` field anywhere in the appended blocks (Pitfall 3 guard)
    - NO `Apache-2.0` license string anywhere in file (research-doc placeholder must NOT propagate; LICENSE is MIT per Plan 01)
    - `ids: [archive]` appears under both brews and scoops (cross-references Phase 51 archives.id)
    - `token: "{{ .Env.PACKAGES_PAT }}"` appears under both brews.repository and scoops.repository
    - All existing Phase 51 blocks (builds, archives, signs, release) unchanged from baseline
  </acceptance_criteria>
  <done>
    `.goreleaser.yml` validates with `goreleaser check`; pre-release tag detection (release.prerelease: auto from line 144) propagates to brews+scoops via skip_upload: auto.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PACKAGES_PAT env var to release.yml goreleaser step</name>
  <read_first>
    - .github/workflows/release.yml (entire file — modify ONLY the env: block at lines 55-56 of the goreleaser step)
    - .planning/phases/52-packaging-distribution-channels/52-PATTERNS.md "release.yml — ONE LINE ADDED" section
    - .planning/phases/52-packaging-distribution-channels/52-RESEARCH.md "Anti-Patterns" — workflow-level env leaks secret; MUST be step-level
  </read_first>
  <files>.github/workflows/release.yml</files>
  <action>
    Edit the existing `env:` map under the `Run goreleaser` step (currently lines 55-56). Add ONE line `PACKAGES_PAT: ${{ secrets.PACKAGES_PAT }}` immediately after the existing `GITHUB_TOKEN` line, with a trailing comment naming the decision ID.

    The resulting env block must read EXACTLY:

    ```yaml
            env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              PACKAGES_PAT: ${{ secrets.PACKAGES_PAT }}   # Phase 52 (D-04): brews/scoops push to postfix/serena-packages
    ```

    DO NOT:
    - Move PACKAGES_PAT to workflow-level `env:` — leaks secret into every step (Anti-Pattern in 52-RESEARCH.md)
    - Modify the job-level `permissions:` block (lines 20-22) — PACKAGES_PAT auth lives entirely in the destination repo, not in GITHUB_TOKEN's scope set
    - Modify the goreleaser-action version (`v6.4.0`), the `args: release --clean`, the trigger (`push: tags: ['v*']`), or any step ordering
    - Touch `setup-go`, `setup-zig`, or `cosign-installer` steps
  </action>
  <verify>
    <automated>grep -F 'PACKAGES_PAT: ${{ secrets.PACKAGES_PAT }}' .github/workflows/release.yml &amp;&amp; ! awk '/^on:/,/^jobs:/' .github/workflows/release.yml | grep -q PACKAGES_PAT &amp;&amp; grep -c "goreleaser-action@v6.4.0" .github/workflows/release.yml | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'PACKAGES_PAT: \${{ secrets.PACKAGES_PAT }}' .github/workflows/release.yml` finds exactly one line
    - That line lives under the goreleaser step's `env:` map (verified by absence in the workflow-level pre-`jobs:` region — `awk '/^on:/,/^jobs:/' release.yml | grep PACKAGES_PAT` returns no matches)
    - `goreleaser-action@v6.4.0` pin unchanged (still appears exactly once)
    - Job-level permissions block (`contents: write`, `id-token: write`) unchanged
    - Trigger `on: push: tags: ['v*']` unchanged
  </acceptance_criteria>
  <done>
    On the next tag push, the goreleaser step receives PACKAGES_PAT and brews+scoops blocks can push to postfix/serena-packages.
  </done>
</task>

<task type="auto">
  <name>Task 3: Snapshot smoke — produce all four artifact families locally</name>
  <read_first>
    - .planning/phases/52-packaging-distribution-channels/52-VALIDATION.md "Sampling Rate" section
    - .planning/phases/51-packaging-goreleaser/51-01-SUMMARY.md (snapshot ritual reference)
  </read_first>
  <files></files>
  <action>
    Run `goreleaser release --snapshot --clean` from repo root. This invokes the full pipeline locally without signing (cosign keyless requires OIDC; signs entries fail in snapshot — expected and ignorable per Phase 51's snapshot ritual).

    Capture the produced artifact list under `dist/`:
    - `dist/homebrew/Formula/serena.rb` (brews block output — Phase 52 NEW)
    - `dist/scoop/bucket/serena.json` (scoops block output — Phase 52 NEW)
    - `dist/serena_*_amd64.deb`, `dist/serena_*_arm64.deb` (nfpms deb — Phase 52 NEW)
    - `dist/serena_*_x86_64.rpm`, `dist/serena_*_aarch64.rpm` (nfpms rpm — Phase 52 NEW; note rpm uses x86_64/aarch64 per Pitfall 4)
    - 6 archives + checksums.txt (Phase 51 baseline; verify they still appear)

    Record the EXACT filenames of the deb and rpm artifacts emitted by goreleaser — Plan 03's release-verify.yml and INSTALL.md will reference these strings verbatim. The arch-token convention (deb=amd64/arm64, rpm=x86_64/aarch64 per Pitfall 4) MUST be confirmed from this snapshot output before Plan 03 hardcodes URLs.

    If any artifact family is missing, do NOT proceed — re-inspect the .goreleaser.yml block for the missing channel, fix, and re-run `goreleaser release --snapshot --clean`.

    Note: the `signs:` step will fail in snapshot mode (no OIDC token); this is expected per Phase 51's snapshot ritual. Use `--skip=sign` if you want a clean exit, but inspecting the artifacts under `dist/` does not require a clean exit.
  </action>
  <verify>
    <automated>goreleaser release --snapshot --clean --skip=sign &amp;&amp; ls dist/homebrew/Formula/serena.rb &amp;&amp; ls dist/scoop/bucket/serena.json &amp;&amp; ls dist/serena_*_amd64.deb &amp;&amp; ls dist/serena_*_arm64.deb &amp;&amp; ls dist/serena_*_x86_64.rpm &amp;&amp; ls dist/serena_*_aarch64.rpm</automated>
  </verify>
  <acceptance_criteria>
    - `dist/homebrew/Formula/serena.rb` exists and is non-empty
    - `dist/scoop/bucket/serena.json` exists and parses as JSON (`python3 -c "import json,sys;json.load(open(sys.argv[1]))" dist/scoop/bucket/serena.json` exits 0)
    - `dist/serena_*_amd64.deb` and `dist/serena_*_arm64.deb` both exist
    - `dist/serena_*_x86_64.rpm` and `dist/serena_*_aarch64.rpm` both exist (rpm arch tokens per Pitfall 4)
    - The Formula's `url` field references a `tar.gz` (NOT a `.zip`) for macOS+Linux (sanity-check: `grep -E 'url.*tar\.gz' dist/homebrew/Formula/serena.rb`)
    - The Scoop manifest references a `.zip` for windows (sanity-check: `grep -F '.zip' dist/scoop/bucket/serena.json`)
    - The exact deb+rpm filenames emitted by goreleaser are noted (will be referenced verbatim by Plan 03)
  </acceptance_criteria>
  <done>
    Snapshot proves all three new blocks generate expected artifacts locally; Plan 03 has authoritative deb/rpm filename strings to reference.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> postfix/serena-packages | PACKAGES_PAT crosses here (writes Formula/manifest commits) |
| Released binary -> end-user system | deb/rpm packages install a binary into /usr/bin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Information disclosure | PACKAGES_PAT secret | mitigate | Step-level `env:` only (NOT workflow-level); fine-grained PAT scoped to single repo per D-04 |
| T-52-02 | Tampering | brews/scoops Formula push | mitigate | Push only succeeds with valid PACKAGES_PAT; commit author = goreleaserbot (audit trail); `release.prerelease: auto` + `skip_upload: auto` prevents pre-release tags from updating stable manifests (Pitfall 5) |
| T-52-03 | Elevation of privilege | nfpms package install | accept | deb/rpm install requires root (`sudo dpkg -i` / `rpm -i`); user is already privileged. Static musl binary, no setuid, no scripts. No additional attack surface beyond the raw binary download path Phase 51 already documented |
| T-52-04 | Denial of service | Bad release pushed via direct branch (D-05, no PR gate) | accept | D-05 explicitly accepts this risk; mitigation = roll forward via patch tag. Plan 03 release-verify.yml catches binary/manifest breakage within ~10 min of publish |
| T-52-05 | Information disclosure | PACKAGES_PAT expiration without rotation | mitigate | Plan 01 records expiration date; Plan 03 RELEASING.md documents rotation ritual; release-verify.yml failure surfaces expiry as 403 from goreleaser (Pitfall 6) |
</threat_model>

<verification>
- `goreleaser check` exits 0
- `goreleaser release --snapshot --clean --skip=sign` produces brews/scoops/nfpms artifacts under `dist/`
- `release.yml` env block contains PACKAGES_PAT only at the goreleaser step level
- All Phase 51 baseline blocks unmodified (verify via `git diff` line count: existing block ranges show 0 changes)
- `go vet ./...` and `go test ./...` still pass (CLAUDE.md mandate; no Go code changed but baseline must remain green)
</verification>

<success_criteria>
- All three goreleaser blocks (brews, scoops, nfpms) added with concrete fields per RESEARCH.md
- License substituted as MIT (NOT Apache-2.0)
- skip_upload: auto on brews+scoops only (Pitfall 5)
- No dependencies: in nfpms (Pitfall 3)
- ids: [archive] cross-reference to Phase 51 archives entry
- PACKAGES_PAT step-level env added (Anti-Pattern guard against workflow-level)
- Local snapshot produces Formula, manifest, deb (amd64+arm64), and rpm (x86_64+aarch64)
- Phase 51 reproducibility ritual still passes (Phase 51 build matrix and signs unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/52-packaging-distribution-channels/52-02-SUMMARY.md` recording the exact deb+rpm filenames emitted by goreleaser for downstream Plan 03 reference.
</output>
