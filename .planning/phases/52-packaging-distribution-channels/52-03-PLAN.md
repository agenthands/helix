---
phase: 52
plan: 03
type: execute
wave: 2
depends_on: [52-02]
files_modified:
  - .github/workflows/release-verify.yml
  - INSTALL.md
  - RELEASING.md
autonomous: true
requirements: [PKG-02, PKG-03, PKG-04]
tags: [packaging, ci, docs, smoke-test]

must_haves:
  truths:
    - "release-verify.yml runs on release: published with 4 matrix legs (brew-macos, scoop-windows, deb-ubuntu, rpm-rocky) and asserts serena --version contains the tag"
    - "On any matrix leg failure, a GitHub issue is auto-opened (D-06)"
    - "INSTALL.md documents Homebrew, Scoop, deb, and rpm install paths between Verify and Quick Start sections"
    - "RELEASING.md documents one-time serena-packages bootstrap and PACKAGES_PAT rotation"
    - "RELEASING.md `## Out of scope` no longer lists Homebrew/Scoop/Linux native (since Phase 52 ships them)"
    - "RELEASING.md reproducibility diff exclude list includes *.deb and *.rpm"
  artifacts:
    - path: ".github/workflows/release-verify.yml"
      provides: "post-release install smoke matrix"
      contains: "release:"
    - path: "INSTALL.md"
      provides: "user-facing install guide for 4 channels"
      contains: "Install via Homebrew"
    - path: "RELEASING.md"
      provides: "maintainer ritual for serena-packages bootstrap and PAT rotation"
      contains: "One-time bootstrap of serena-packages"
  key_links:
    - from: ".github/workflows/release-verify.yml"
      to: "github.com/postfix/serena-packages"
      via: "brew tap / scoop bucket add commands"
      pattern: "postfix/serena-packages"
    - from: "INSTALL.md Homebrew section"
      to: "github.com/postfix/serena-packages"
      via: "brew tap URL form (D-01 install command)"
      pattern: "brew tap postfix/serena-packages"
    - from: "RELEASING.md bootstrap section"
      to: "Plan 01 evidence"
      via: "documented gh repo create + gh secret set ritual"
      pattern: "gh repo create postfix/serena-packages"
---

<objective>
Land the post-release verification workflow and the user-facing + maintainer-facing documentation that close out Phase 52. After this plan, an end user reading INSTALL.md sees four install paths (download/verify, brew, scoop, deb, rpm), a maintainer reading RELEASING.md sees the one-time bootstrap and the rotation ritual, and the CI catches publish breakage automatically by smoke-installing on macos-latest, windows-latest, ubuntu:24.04, and rockylinux:9 within minutes of every release.

Purpose: Plan 02 produces the goreleaser config that publishes artifacts. This plan creates the proof-of-publish (release-verify.yml on `release: published`) and the user-readable install instructions that point at those artifacts. Bundled into one plan because the three files form a single user-facing surface (one workflow + two markdown files); separating CI from docs would force a second plan with no semantic boundary.

Output:
- `.github/workflows/release-verify.yml` (NEW): 4-leg matrix + open-issue-on-failure
- `INSTALL.md` AMENDED: 4 new sections appended after `## Verify a release binary` and before `## Quick Start`
- `RELEASING.md` AMENDED: bootstrap section after Prerequisites, rotation section after Post-merge verification, diff-exclude extended for deb/rpm, Out-of-scope cleaned of completed Phase 52 items
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-packaging-distribution-channels/52-CONTEXT.md
@.planning/phases/52-packaging-distribution-channels/52-RESEARCH.md
@.planning/phases/52-packaging-distribution-channels/52-PATTERNS.md
@.planning/phases/52-packaging-distribution-channels/52-02-SUMMARY.md
@.github/workflows/release.yml
@INSTALL.md
@RELEASING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create release-verify.yml smoke matrix</name>
  <read_first>
    - .github/workflows/release.yml (entire file — copy header style, action pin convention, comment voice)
    - .planning/phases/52-packaging-distribution-channels/52-RESEARCH.md "Pattern: release-verify.yml skeleton" (lines 370-473) — full skeleton to copy
    - .planning/phases/52-packaging-distribution-channels/52-PATTERNS.md "release-verify.yml — NET NEW" — header pattern, narrowed permissions, action pinning convention
    - .planning/phases/52-packaging-distribution-channels/52-02-SUMMARY.md (verify exact deb/rpm filename strings emitted by snapshot — substitute into URLs verbatim per Pitfall 4)
  </read_first>
  <files>.github/workflows/release-verify.yml</files>
  <action>
    Create `.github/workflows/release-verify.yml` using the skeleton from 52-RESEARCH.md lines 370-473 verbatim, with the following adjustments:

    1. Header comment block (mirror release.yml lines 1-3 style):
       ```yaml
       # Phase 52 — release verification smoke test. See .planning/phases/52-packaging-distribution-channels/.
       # Locked decisions: D-06 (CI smoke is the gate, not manual ritual),
       # D-01 (tap = postfix/serena-packages monorepo), D-03 (deb/rpm direct download).
       # Pinned action versions; bump intentionally, not auto.
       name: release-verify
       ```

    2. Trigger: `release: published` (NOT `push: tags` — Anti-Pattern in 52-RESEARCH.md: tag fires before goreleaser uploads finish).

    3. Permissions narrowed: `contents: read` + `issues: write` (only writes issues on smoke failure).

    4. Four matrix legs as separate jobs (NOT a single matrix — they have different runners/containers):
       - `brew-macos` on macos-latest, timeout-minutes: 10
       - `scoop-windows` on windows-latest, timeout-minutes: 10 (must install scoop first via the official one-liner — NOT preinstalled on windows runners)
       - `deb-ubuntu` on ubuntu-latest with `container: ubuntu:24.04`, timeout-minutes: 10
       - `rpm-rocky` on ubuntu-latest with `container: rockylinux:9`, timeout-minutes: 10

    5. Version assertion in each leg: extract `${TAG#v}` (Linux/macOS) or `$env:TAG.TrimStart('v')` (Windows) and grep for it in `serena --version` output.

    6. Final job `open-issue-on-failure`:
       - `needs: [brew-macos, scoop-windows, deb-ubuntu, rpm-rocky]`
       - `if: failure()`
       - Uses `actions/github-script@v7` to call `github.rest.issues.create` with title `release-verify failed for ${tag}` and label `release-verify-failure`

    7. deb/rpm download URLs: substitute the EXACT filename strings from 52-02-SUMMARY.md (e.g., `serena_${VERSION}_amd64.deb`, `serena_${VERSION}_x86_64.rpm`). Do NOT guess — the Pitfall 4 arch convention is deb=amd64/rpm=x86_64 but goreleaser's full template may include extras. If 52-02-SUMMARY.md is unavailable at runtime, run `goreleaser release --snapshot --clean --skip=sign` and `ls dist/serena_*.{deb,rpm}` to capture the strings before writing.

    8. Action pins (match release.yml convention from PATTERNS.md):
       - `actions/checkout@v4` (if needed; the smoke jobs don't checkout the repo, they download from Releases — but if any leg uses checkout, pin v4)
       - `actions/github-script@v7` (for the issue-creation job)

    Use the full skeleton from 52-RESEARCH.md lines 370-473 as the starting point — copy it verbatim, then adjust per the 8 points above. Critical lines from the skeleton:

    ```yaml
    on:
      release:
        types: [published]

    permissions:
      contents: read
      issues: write
    ```

    macOS leg body (verbatim from skeleton):
    ```yaml
    brew-macos:
      runs-on: macos-latest
      timeout-minutes: 10
      steps:
        - name: brew tap + install + version assert
          env:
            TAG: ${{ github.event.release.tag_name }}
          run: |
            set -euo pipefail
            brew tap postfix/serena-packages https://github.com/postfix/serena-packages
            brew install serena
            OUT=$(serena --version)
            echo "$OUT"
            VERSION="${TAG#v}"
            echo "$OUT" | grep -F "$VERSION"
    ```

    Windows leg (verbatim, includes the scoop install one-liner):
    ```yaml
    scoop-windows:
      runs-on: windows-latest
      timeout-minutes: 10
      steps:
        - name: install scoop
          shell: pwsh
          run: |
            Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
            irm get.scoop.sh -outfile 'install.ps1'
            .\install.ps1 -RunAsAdmin
        - name: scoop bucket add + install + version assert
          shell: pwsh
          env:
            TAG: ${{ github.event.release.tag_name }}
          run: |
            scoop bucket add serena https://github.com/postfix/serena-packages
            scoop install serena
            $out = serena --version
            Write-Host $out
            $version = $env:TAG.TrimStart('v')
            if ($out -notmatch [regex]::Escape($version)) {
              throw "version mismatch: expected $version in '$out'"
            }
    ```

    deb leg (verbatim — uses ubuntu:24.04 container; deb arch token = amd64):
    ```yaml
    deb-ubuntu:
      runs-on: ubuntu-latest
      container: ubuntu:24.04
      timeout-minutes: 10
      steps:
        - name: install + version assert
          env:
            TAG: ${{ github.event.release.tag_name }}
          run: |
            set -euo pipefail
            apt-get update && apt-get install -y curl
            VERSION="${TAG#v}"
            curl -LO "https://github.com/postfix/serena/releases/download/${TAG}/serena_${VERSION}_amd64.deb"
            dpkg -i "serena_${VERSION}_amd64.deb"
            serena --version | grep -F "$VERSION"
    ```

    rpm leg (verbatim — uses rockylinux:9 container; rpm arch token = x86_64):
    ```yaml
    rpm-rocky:
      runs-on: ubuntu-latest
      container: rockylinux:9
      timeout-minutes: 10
      steps:
        - name: install + version assert
          env:
            TAG: ${{ github.event.release.tag_name }}
          run: |
            set -euo pipefail
            dnf install -y curl
            VERSION="${TAG#v}"
            curl -LO "https://github.com/postfix/serena/releases/download/${TAG}/serena_${VERSION}_x86_64.rpm"
            rpm -i "serena_${VERSION}_x86_64.rpm"
            serena --version | grep -F "$VERSION"
    ```

    Final issue-on-failure job (verbatim):
    ```yaml
    open-issue-on-failure:
      needs: [brew-macos, scoop-windows, deb-ubuntu, rpm-rocky]
      if: failure()
      runs-on: ubuntu-latest
      steps:
        - uses: actions/github-script@v7
          with:
            script: |
              const tag = context.payload.release.tag_name;
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: `release-verify failed for ${tag}`,
                body: `One or more install smoke tests failed for release ${tag}. See workflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
                labels: ['release-verify-failure']
              });
    ```

    DO NOT add cosign verify steps to release-verify.yml (D-06 scopes the smoke to install + version assertion only; signature verification is documented as optional in INSTALL.md per D-03 and is out of scope per Open Question 4 in 52-RESEARCH.md).
  </action>
  <verify>
    <automated>test -f .github/workflows/release-verify.yml &amp;&amp; grep -F "release:" .github/workflows/release-verify.yml &amp;&amp; grep -F "types: [published]" .github/workflows/release-verify.yml &amp;&amp; grep -F "macos-latest" .github/workflows/release-verify.yml &amp;&amp; grep -F "windows-latest" .github/workflows/release-verify.yml &amp;&amp; grep -F "ubuntu:24.04" .github/workflows/release-verify.yml &amp;&amp; grep -F "rockylinux:9" .github/workflows/release-verify.yml &amp;&amp; grep -F "actions/github-script@v7" .github/workflows/release-verify.yml &amp;&amp; grep -F "release-verify-failure" .github/workflows/release-verify.yml &amp;&amp; grep -F "postfix/serena-packages" .github/workflows/release-verify.yml &amp;&amp; ! grep -F "push:" .github/workflows/release-verify.yml &amp;&amp; ! grep -F "cosign verify" .github/workflows/release-verify.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/release-verify.yml` exists
    - Trigger is `release: published` (NOT `push: tags`)
    - Permissions: `contents: read` + `issues: write` (NOT `contents: write`)
    - Four jobs present: `brew-macos`, `scoop-windows`, `deb-ubuntu`, `rpm-rocky`
    - `scoop-windows` job installs scoop via `irm get.scoop.sh` before invoking it
    - deb URL uses `_amd64.deb`, rpm URL uses `_x86_64.rpm` (Pitfall 4 arch tokens)
    - `open-issue-on-failure` job uses `actions/github-script@v7` and creates issue with label `release-verify-failure`
    - All jobs have `timeout-minutes: 10`
    - No cosign verify steps (out of scope per D-06)
    - If `actionlint` is available locally, `actionlint .github/workflows/release-verify.yml` exits 0 (no required-context errors, no unknown actions)
  </acceptance_criteria>
  <done>
    Workflow lints clean and will fire on every `release: published` event after Plan 02 ships. Issue-on-failure surfaces broken publishes within ~10 minutes of release.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append 4 install sections to INSTALL.md</name>
  <read_first>
    - INSTALL.md (entire file — confirm Phase 51 section ordering: Prerequisites L5-29, Download L31-51, Verify L53-85, Quick Start L87+; insert at L86 between Verify and Quick Start)
    - .planning/phases/52-packaging-distribution-channels/52-PATTERNS.md "INSTALL.md — APPEND 4 SECTIONS" (heading style + env-var snippet pattern)
    - .planning/phases/52-packaging-distribution-channels/52-CONTEXT.md (D-01 install commands, D-03 deb/rpm direct download)
  </read_first>
  <files>INSTALL.md</files>
  <action>
    Insert 4 new sections AFTER `## Verify a release binary` (line 85 of INSTALL.md) and BEFORE `## Quick Start` (line 87). Use exact `## Install via <channel>` heading form per PATTERNS.md.

    Each section uses the env-var-driven snippet style established by Phase 51's `## Download a prebuilt binary` (lines 37-47). Brew + Scoop sections are simpler (no checksum dance — package manager handles it). deb + rpm sections include a cross-reference link to `## Verify a release binary` (anchor `#verify-a-release-binary`) since cosign verify is documented as optional per D-03.

    Append exactly the following block at line 86 (between Verify and Quick Start):

    ```markdown
    ## Install via Homebrew (macOS, Linux)

    Serena is published as a Homebrew Formula in the `postfix/serena-packages`
    tap (single monorepo for tap + Scoop bucket per phase 52 D-01). Works on
    macOS (arm64 + amd64) and Linux (amd64 via Linuxbrew).

    ```bash
    brew tap postfix/serena-packages https://github.com/postfix/serena-packages
    brew install serena
    serena --version
    ```

    Update on a new release:

    ```bash
    brew update && brew upgrade serena
    ```

    ## Install via Scoop (Windows)

    Serena is published as a Scoop manifest in the same `postfix/serena-packages`
    bucket. Works on Windows amd64.

    ```powershell
    scoop bucket add serena https://github.com/postfix/serena-packages
    scoop install serena
    serena --version
    ```

    Update on a new release:

    ```powershell
    scoop update serena
    ```

    ## Install via .deb (Debian, Ubuntu)

    Direct download from GitHub Release assets (no hosted apt repo per phase 52
    D-03 — updates are manual: redownload on a new release).

    ```bash
    VERSION=v1.9.0   # set to the release tag you want
    ARCH=amd64       # one of: amd64, arm64
    VER=${VERSION#v}

    curl -LO "https://github.com/postfix/serena/releases/download/${VERSION}/serena_${VER}_${ARCH}.deb"
    sudo dpkg -i "serena_${VER}_${ARCH}.deb"
    serena --version
    ```

    Optional: verify the binary's cosign signature first — see
    [Verify a release binary](#verify-a-release-binary).

    ## Install via .rpm (Fedora, RHEL, openSUSE)

    Direct download from GitHub Release assets. Note: rpm uses `x86_64`/`aarch64`
    arch tokens (deb uses `amd64`/`arm64`).

    ```bash
    VERSION=v1.9.0   # set to the release tag you want
    ARCH=x86_64      # one of: x86_64, aarch64
    VER=${VERSION#v}

    curl -LO "https://github.com/postfix/serena/releases/download/${VERSION}/serena_${VER}_${ARCH}.rpm"
    sudo rpm -i "serena_${VER}_${ARCH}.rpm"
    serena --version
    ```

    Optional: verify the binary's cosign signature first — see
    [Verify a release binary](#verify-a-release-binary).
    ```

    Use Edit tool on INSTALL.md — locate `## Quick Start` (line 87) and insert the new block immediately before it. Preserve all other content unchanged.
  </action>
  <verify>
    <automated>grep -c "^## Install via Homebrew" INSTALL.md | grep -q "^1$" &amp;&amp; grep -c "^## Install via Scoop" INSTALL.md | grep -q "^1$" &amp;&amp; grep -c "^## Install via \.deb" INSTALL.md | grep -q "^1$" &amp;&amp; grep -c "^## Install via \.rpm" INSTALL.md | grep -q "^1$" &amp;&amp; grep -F "brew tap postfix/serena-packages https://github.com/postfix/serena-packages" INSTALL.md &amp;&amp; grep -F "scoop bucket add serena https://github.com/postfix/serena-packages" INSTALL.md &amp;&amp; grep -F "_amd64.deb" INSTALL.md &amp;&amp; grep -F "_x86_64.rpm" INSTALL.md &amp;&amp; grep -F "#verify-a-release-binary" INSTALL.md &amp;&amp; awk '/^## Verify a release binary/{v=NR} /^## Install via Homebrew/{h=NR} /^## Quick Start/{q=NR} END{exit !(v &lt; h &amp;&amp; h &lt; q)}' INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `## Install via Homebrew` heading
    - Exactly one `## Install via Scoop` heading
    - Exactly one `## Install via .deb` heading
    - Exactly one `## Install via .rpm` heading
    - All 4 new sections appear AFTER `## Verify a release binary` and BEFORE `## Quick Start` (verified by line-number ordering: V < H < Q)
    - Brew install command matches D-01 verbatim (`brew tap postfix/serena-packages https://github.com/postfix/serena-packages`)
    - Scoop install command matches D-01 verbatim (`scoop bucket add serena https://github.com/postfix/serena-packages`)
    - deb section uses `_amd64.deb` arch token; rpm section uses `_x86_64.rpm` (Pitfall 4)
    - Both deb and rpm sections include cross-reference anchor `#verify-a-release-binary`
    - Existing sections (Prerequisites, Download, Verify, Quick Start, Manual Configuration, etc.) unchanged
  </acceptance_criteria>
  <done>
    A user reading INSTALL.md from top to bottom now sees 4 package-manager paths between Verify and Quick Start.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend RELEASING.md with bootstrap, rotation, and reproducibility-exclude updates</name>
  <read_first>
    - RELEASING.md (entire file — sections: Prerequisites L7-25, Pre-tag reproducibility ritual L27-72, Tag and push L74-90, Post-merge verification L92-106, Out of scope L108-116)
    - .planning/phases/52-packaging-distribution-channels/52-PATTERNS.md "RELEASING.md — APPEND 2 SECTIONS" (heading style, numbered ritual blocks, decision-ID suffix)
    - .planning/phases/52-packaging-distribution-channels/52-RESEARCH.md "Pitfall 6" (rotation) and Open Question 3 (deb/rpm reproducibility)
    - .planning/phases/52-packaging-distribution-channels/52-CONTEXT.md (D-01, D-04)
    - .planning/phases/52-packaging-distribution-channels/52-01-SUMMARY.md (re-use the gh repo create + gh secret set commands recorded there for consistency)
  </read_first>
  <files>RELEASING.md</files>
  <action>
    Make four targeted edits to RELEASING.md:

    **EDIT 1: Insert `## One-time bootstrap of serena-packages (D-01, D-04)` AFTER `## Prerequisites` (line 25) and BEFORE `## Pre-tag reproducibility ritual (D-07)` (line 27).**

    Content:

    ```markdown
    ## One-time bootstrap of serena-packages (D-01, D-04)

    Before the first Phase-52-aware tag, the maintainer creates the publish
    target and the PAT that authenticates pushes to it. This is a one-time
    step; subsequent releases reuse the same repo and PAT.

    1. Create the empty publish-target repo (with an initial commit so `main`
       exists — see 52-RESEARCH.md Pitfall 7):

       ```bash
       gh repo create postfix/serena-packages --public \
         --description "Homebrew tap and Scoop bucket for Serena" \
         --add-readme
       ```

       Expected: `gh repo view postfix/serena-packages --json defaultBranchRef -q .defaultBranchRef.name` returns `main`.

    2. Mint a fine-grained PAT scoped ONLY to `postfix/serena-packages` with
       `contents: write`. The CLI cannot scope fine-grained PATs to a single
       repo; use the web UI:

       - Visit https://github.com/settings/personal-access-tokens/new
       - Token name: `serena-packages-publish`
       - Expiration: 90 days (52-RESEARCH.md Pitfall 6 — set a calendar reminder)
       - Resource owner: postfix
       - Repository access: "Only select repositories" → `postfix/serena-packages` only
       - Permissions → Repository permissions → Contents: Read and write
       - Generate token; copy immediately

    3. Set the PAT as the `PACKAGES_PAT` secret on `postfix/serena`:

       ```bash
       gh secret set PACKAGES_PAT --repo postfix/serena
       # paste the token when prompted
       ```

       Expected: `gh secret list --repo postfix/serena | grep PACKAGES_PAT` shows the secret.

    Once these three steps complete, every subsequent tag push triggers the
    goreleaser pipeline, which uses `PACKAGES_PAT` to push the auto-generated
    Formula and Scoop manifest directly to `serena-packages:main` (D-05 — no PR gate).
    ```

    **EDIT 2: Insert `## Rotating PACKAGES_PAT (D-04)` AFTER `## Post-merge verification (D-22)` (line 106) and BEFORE `## Out of scope` (line 108).**

    Content:

    ```markdown
    ## Rotating PACKAGES_PAT (D-04)

    Fine-grained PATs expire after at most 1 year (GitHub enforcement). The
    bootstrap step set the PAT to a 90-day expiration. Rotate before expiry
    to avoid release-verify.yml opening an issue on the next tag.

    1. Visit https://github.com/settings/personal-access-tokens, find
       `serena-packages-publish`, click Regenerate.
    2. Confirm scope (postfix/serena-packages only, Contents: write) and
       expiration (90 days).
    3. Update the secret on `postfix/serena`:

       ```bash
       gh secret set PACKAGES_PAT --repo postfix/serena
       ```

    Warning sign of an expired PAT: goreleaser step in `release.yml` fails
    with `403` when pushing to `serena-packages`; release-verify.yml smoke
    test fails with "Formula not found" because the publish never happened.
    The smoke-test issue is the secondary alarm; rotate proactively.
    ```

    **EDIT 3: Extend the existing reproducibility-diff exclude list inside `## Pre-tag reproducibility ritual` (around lines 46-53). Add `--exclude='*.deb'` and `--exclude='*.rpm'` to the diff invocation, with surrounding prose explaining that deb/rpm internal timestamps may not be byte-stable across snapshot runs (52-RESEARCH.md Open Question 3).**

    Locate the existing diff command in RELEASING.md `## Pre-tag reproducibility ritual` (lines ~46-53; the Phase 51 ritual lists `--exclude='*.sig' --exclude='*.pem'` or similar). Add deb/rpm exclusions in the same command, and append a one-paragraph note immediately after the command:

    ```markdown
    deb and rpm artifacts are excluded defensively because nFPM embeds a build
    timestamp inside the package metadata that may not always be byte-stable
    across snapshot runs (52-RESEARCH.md Open Question 3). The binary INSIDE
    the package is reproducible (it carries through `mod_timestamp` and
    `-trimpath` from `builds:`); the package wrapper is not strictly verified.
    ```

    **EDIT 4: In `## Out of scope` (lines 108-116), REMOVE the line that defers Homebrew/Scoop/Linux native packages to Phase 52 (Phase 51 noted: "Homebrew, Scoop, Linux native packages — Phase 52 (D-23)" or similar phrasing). Phase 52 ships these now. Leave Docker, SBOM, notarization, and in-binary auto-update entries intact.**

    Use Read to locate the exact phrasing in lines 108-116, then Edit to delete only that one bullet.
  </action>
  <verify>
    <automated>grep -c "^## One-time bootstrap of serena-packages" RELEASING.md | grep -q "^1$" &amp;&amp; grep -c "^## Rotating PACKAGES_PAT" RELEASING.md | grep -q "^1$" &amp;&amp; grep -F "gh repo create postfix/serena-packages" RELEASING.md &amp;&amp; grep -F "gh secret set PACKAGES_PAT" RELEASING.md &amp;&amp; grep -F "exclude='*.deb'" RELEASING.md &amp;&amp; grep -F "exclude='*.rpm'" RELEASING.md &amp;&amp; ! grep -E "Homebrew.*Phase 52" RELEASING.md &amp;&amp; awk '/^## Prerequisites/{p=NR} /^## One-time bootstrap of serena-packages/{b=NR} /^## Pre-tag reproducibility ritual/{r=NR} /^## Post-merge verification/{v=NR} /^## Rotating PACKAGES_PAT/{rot=NR} /^## Out of scope/{o=NR} END{exit !(p &lt; b &amp;&amp; b &lt; r &amp;&amp; v &lt; rot &amp;&amp; rot &lt; o)}' RELEASING.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `## One-time bootstrap of serena-packages` heading
    - Exactly one `## Rotating PACKAGES_PAT` heading
    - Bootstrap section contains `gh repo create postfix/serena-packages` and `gh secret set PACKAGES_PAT`
    - Rotation section documents 90-day expiration, regenerate UI path, and 403 warning sign
    - Reproducibility ritual diff command now includes `--exclude='*.deb'` AND `--exclude='*.rpm'`
    - Section ordering preserved: Prerequisites < Bootstrap < Pre-tag reproducibility < ... < Post-merge verification < Rotation < Out of scope
    - `## Out of scope` no longer references Phase 52 / Homebrew / Scoop deferral (since Phase 52 ships them)
    - All other Phase 51 sections (Pre-tag reproducibility ritual core content, Tag and push, Post-merge verification) unchanged
  </acceptance_criteria>
  <done>
    A maintainer reading RELEASING.md before the first post-Phase-52 release can: (a) bootstrap the publish target if not already done, (b) rotate the PAT proactively, (c) reproduce a build snapshot with deb/rpm excluded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> public package mirrors | release-verify.yml downloads + installs from GitHub Releases CDN (public, signed by Phase 51 cosign keyless) |
| Maintainer browser -> GitHub web UI | Bootstrap + rotation flows mint PATs (untrusted-by-default; scope must be limited per D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06 | Spoofing | release-verify.yml downloading malicious deb/rpm | accept | Smoke test downloads from the same Release the workflow trigger fires on; can't be spoofed without compromising the GitHub Release publish (which would fail Phase 51's cosign signing). Optional cosign verify documented in INSTALL.md per D-03 |
| T-52-07 | Information disclosure | RELEASING.md leaking PAT in shell history | accept | `gh secret set` reads from prompt (not argv); maintainer responsibility. Alternative `gh secret set --body "$PAT"` discouraged in docs |
| T-52-08 | Tampering | release-verify.yml issue creation forged | mitigate | `actions/github-script@v7` runs in the same repo's GH context; `issues: write` is the only elevated permission; `contents: read` ensures no source mutation |
| T-52-09 | Repudiation | Bad release shipped without manual review (D-05) | accept | D-05 explicitly accepts; mitigation = release-verify.yml issue auto-opens within ~10 min on smoke fail; roll-forward via patch tag |
</threat_model>

<verification>
- `actionlint .github/workflows/release-verify.yml` passes (or the workflow runs successfully on the next test release)
- INSTALL.md has 4 new sections in the correct position (between Verify and Quick Start)
- RELEASING.md has 2 new sections in the correct positions
- RELEASING.md `## Out of scope` no longer lists Phase-52-completed items
- `go vet ./...` and `go test ./...` still pass (CLAUDE.md mandate; baseline preserved)
- `goreleaser check` still passes (no regression to Plan 02 work)
</verification>

<success_criteria>
- 4-leg release-verify.yml on `release: published` with auto-issue on failure (D-06)
- INSTALL.md documents brew/scoop/deb/rpm install paths with D-01-correct commands (Pitfall 4 arch tokens used correctly)
- RELEASING.md documents one-time bootstrap (gh repo create + PAT minting + secret set) and rotation ritual (Pitfall 6)
- Reproducibility ritual extended for deb/rpm artifacts (Open Question 3)
- Phase 52 success criteria #1-#4 from ROADMAP.md all achievable: brew install works (PKG-02), scoop install works (PKG-03), deb/rpm direct-download install documented (PKG-04), tag triggers automated formula/manifest update without manual editing (D-05 + Plan 02 wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/52-packaging-distribution-channels/52-03-SUMMARY.md`
</output>
