---
phase: 53-obs-metrics-gaps
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - USAGE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [OBS-03]
tags: [observability, docs, roadmap, helix]
must_haves:
  truths:
    - "USAGE.md §\"Prometheus Metrics\" documents all 5 new metric families with their labels, value enums, and one-line semantics"
    - "USAGE.md adds a hit-ratio PromQL example using helix_lspool_lookups_total{result=\"hit\"}"
    - "USAGE.md adds a per-extractor latency PromQL example for helix_repomap_extract_duration_seconds"
    - "USAGE.md notes that transport=http phase=ended is best-effort (no SDK lifecycle hook in v1.5.0) per Q-1 Option 2"
    - "ROADMAP.md success-criterion-1 strings rewritten from serena_* to helix_* per D-13"
    - "Documentation reflects only the metrics that actually shipped in plans 01-05 (no aspirational entries)"
  artifacts:
    - path: "USAGE.md"
      provides: "5 new rows in the Prometheus Metrics table + 2 new PromQL examples + best-effort caveat for http ended"
      contains: "helix_lspool_lookups_total"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 53 success-criterion-1 with helix_* prefix"
      contains: "helix_lspool_lookups_total"
  key_links:
    - from: "USAGE.md Prometheus Metrics table"
      to: "the 5 vectors landed in Plan 01"
      via: "name + label list + semantics row per family"
      pattern: "helix_(lspool_lookups|repomap_lookups|repomap_extract_duration_seconds|session_lifecycle|edit_outcome)_total"
    - from: "ROADMAP.md Phase 53 success criteria"
      to: "actual production metric names (Plan 01)"
      via: "find/replace serena_* → helix_*"
      pattern: "helix_lspool_lookups_total.*helix_repomap_lookups_total.*helix_repomap_extract_duration_seconds.*helix_session_lifecycle_total.*helix_edit_outcome_total"
---

<objective>
Close the documentation half of OBS-03. Update USAGE.md §"Prometheus Metrics" to enumerate the 5 new families, add 2 PromQL examples (hit-ratio and per-extractor latency), and document the http best-effort `ended` semantic. Update ROADMAP.md Phase 53 success-criterion-1 to use the actual `helix_*` metric names per D-13 (the existing wording uses stale `serena_*` strings from before the Phase 52 brand rename).

Purpose: Without this plan, operators have no map from `/metrics` output to its meaning, and the roadmap permanently displays incorrect metric names. ROADMAP correction is in scope per D-13 — shipping new metrics with the `serena_*` prefix would re-introduce the brand inconsistency Phase 52 just removed (and the metric names actually emitted by plans 01-05 are `helix_*`).
Output: USAGE.md §"Prometheus Metrics" extended with 5 rows + 2 PromQL examples + http caveat; ROADMAP.md Phase 53 success-criterion-1 strings corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/53-obs-metrics-gaps/53-CONTEXT.md
@.planning/phases/53-obs-metrics-gaps/53-RESEARCH.md
@.planning/phases/53-obs-metrics-gaps/53-PATTERNS.md
@.planning/phases/53-obs-metrics-gaps/53-01-SUMMARY.md
@.planning/phases/53-obs-metrics-gaps/53-02-SUMMARY.md
@.planning/phases/53-obs-metrics-gaps/53-03-SUMMARY.md
@.planning/phases/53-obs-metrics-gaps/53-04-SUMMARY.md
@.planning/phases/53-obs-metrics-gaps/53-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Extend USAGE.md §Prometheus Metrics with 5 new families + 2 PromQL examples + http caveat</name>
  <files>USAGE.md</files>
  <read_first>
    - USAGE.md (lines 634-678 — the existing §"Prometheus Metrics" table; preserve its column structure exactly)
    - .planning/phases/53-obs-metrics-gaps/53-CONTEXT.md (D-01 to D-13 — name + label authority)
    - .planning/phases/53-obs-metrics-gaps/53-RESEARCH.md (Q-1 Option 2 — best-effort caveat to mention)
    - .planning/phases/53-obs-metrics-gaps/53-01-SUMMARY.md (the bucket layout actually shipped + label values)
    - .planning/phases/53-obs-metrics-gaps/53-05-SUMMARY.md (any deviations from PATTERNS.md HTTP middleware sketch)
  </read_first>
  <action>
    Extend USAGE.md §"Prometheus Metrics" (find the table around lines 644-677). Preserve the existing rows verbatim; APPEND 5 new rows with the EXACT column structure used by the existing table (read the file first to confirm — likely Metric Name | Type | Labels | Description).

    1. Add 5 rows at the end of the table, BEFORE any closing prose:

       | `helix_lspool_lookups_total` | counter | `language`, `result` | LSP worker-pool cache lookups by result. `result` ∈ `{hit, miss}` — hit = shared warm worker via `workerForKeyLocked`; miss = spawn (or refusal due to circuit-open / max-workers). Dirty acquires count as misses (Phase 53 D-02). |
       | `helix_repomap_lookups_total` | counter | `language`, `result` | RepoMap TagCache lookups by result. `result` ∈ `{hit, miss}` — hit = mtime match; miss = extractor invoked (Phase 53 D-03). |
       | `helix_repomap_extract_duration_seconds` | histogram | `language`, `extractor` | RepoMap extractor latency, cache-miss path only. `extractor` ∈ `{treesitter, lsp, fallback}`. Custom buckets `{1ms, 2.5ms, 5ms, 10ms, 25ms, 50ms, 100ms, 250ms, 500ms, 1s, 2.5s}` give p50 visibility for tree-sitter and tail visibility for LSP fallback (Phase 53 D-05/D-06). |
       | `helix_session_lifecycle_total` | counter | `phase`, `transport` | MCP session lifecycle phase transitions by transport. `phase` ∈ `{started, ended, error}`; `transport` ∈ `{stdio, http}`. **Note:** for `transport=http`, `phase=ended` is best-effort — emitted on `DELETE /mcp` only; sessions that disappear due to server-side timeout do not register (the MCP SDK v1.5.0 does not expose a per-session lifecycle hook). For `transport=stdio`, all three phases are reliably emitted from the forwarder stream lifecycle. (Phase 53 D-08/D-09 + Q-1 Option 2.) |
       | `helix_edit_outcome_total` | counter | `tool_name`, `outcome`, `strategy` | Edit-tool handler outcomes. `tool_name` ∈ the 7 production edit/fileops tools (`replace_symbol_body`, `insert_before_symbol`, `insert_after_symbol`, `rename_symbol`, `safe_delete_symbol`, `replace_in_file`, `fuzzy_edit`); `outcome` ∈ `{success, no_match, ambiguous_match, validation_failed, ls_error, internal}`; `strategy` ∈ `{exact, whitespace_normalized, indentation_flexible, none}`. `none` is emitted by non-fuzzy tools and by failure paths where no fuzzy strategy ran. The existing `helix_rename_strategy_total` (Phase 47) tracks an orthogonal LSP-native vs. client-side dispatch dimension and continues to coexist (Phase 53 D-10/D-11/D-12). |

    2. After the table, find the existing "Example PromQL" / "Examples" subsection (or add one if none exists). Append two new PromQL examples:

       ```promql
       # Phase 53 D-01: lspool cache hit-ratio (5-minute window).
       # Closer to 1.0 = more sessions reuse warm workers; closer to 0 = more spawns.
       sum(rate(helix_lspool_lookups_total{result="hit"}[5m]))
         / sum(rate(helix_lspool_lookups_total[5m]))
       ```

       ```promql
       # Phase 53 D-05/D-06: per-extractor p95 RepoMap extraction latency.
       # Surfaces "is the LSP fallback dragging us down for $LANG?" in one query.
       histogram_quantile(0.95,
         sum by (le, extractor) (rate(helix_repomap_extract_duration_seconds_bucket[5m])))
       ```

    3. If a "Caveats" / "Known limitations" subsection exists in §Observability, append a short note:

       > **HTTP session `ended` is best-effort.** `helix_session_lifecycle_total{transport="http", phase="ended"}` only fires on a client-issued `DELETE /mcp` (the MCP-spec clean termination signal). Sessions that disappear because the SDK times them out internally are NOT counted as `ended`. For reliable session tracking on HTTP, use `started` minus `error` and treat the gap as outstanding sessions plus quietly-timed-out ones. For stdio transport (the default), all three phases are reliably emitted.

       If no such subsection exists, add this caveat as a paragraph immediately after the PromQL examples.

    Do NOT add aspirational entries (e.g. workspace-activation cold-start) — those are deferred to Phase 54 / OBS-04. Do NOT re-document existing metrics; only add the 5 new ones.
  </action>
  <verify>
    <automated>grep -q 'helix_lspool_lookups_total' USAGE.md && grep -q 'helix_repomap_lookups_total' USAGE.md && grep -q 'helix_repomap_extract_duration_seconds' USAGE.md && grep -q 'helix_session_lifecycle_total' USAGE.md && grep -q 'helix_edit_outcome_total' USAGE.md && grep -q 'best-effort' USAGE.md && grep -q 'histogram_quantile' USAGE.md && grep -q 'rate(helix_lspool_lookups_total' USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'helix_lspool_lookups_total\|helix_repomap_lookups_total\|helix_repomap_extract_duration_seconds\|helix_session_lifecycle_total\|helix_edit_outcome_total' USAGE.md` returns at least 5 (one occurrence per family in the table; PromQL examples may add more)
    - `grep -q '`hit`, `miss`\|hit, miss\|{hit, miss}' USAGE.md` — result enum documented
    - `grep -q '`treesitter`, `lsp`, `fallback`\|treesitter, lsp, fallback\|{treesitter, lsp, fallback}' USAGE.md` — extractor enum documented
    - `grep -q '`started`, `ended`, `error`\|started, ended, error\|{started, ended, error}' USAGE.md` — phase enum documented
    - `grep -q '`stdio`, `http`\|stdio, http\|{stdio, http}' USAGE.md` — transport enum documented
    - `grep -q 'best-effort' USAGE.md` — Q-1 Option 2 caveat present
    - `grep -q 'rate(helix_lspool_lookups_total{result="hit"}' USAGE.md` — hit-ratio PromQL exact
    - `grep -q 'histogram_quantile.*helix_repomap_extract_duration_seconds_bucket' USAGE.md` — per-extractor PromQL exact
    - `! grep -q 'serena_lspool_lookups_total\|serena_repomap_lookups_total\|serena_repomap_extract_duration_seconds\|serena_session_lifecycle_total\|serena_edit_outcome_total' USAGE.md` — no serena_* leakage in new content
    - `! grep -q 'workspace-activation\|workspace_activation_cold_start\|workspace.*activation.*duration' USAGE.md` — no aspirational deferred metrics
  </acceptance_criteria>
  <done>USAGE.md documents the 5 new families with labels, value enums, semantics, two PromQL examples, and the http best-effort caveat.</done>
</task>

<task type="auto">
  <name>Task 6.2: Correct ROADMAP.md Phase 53 success-criterion-1 strings (serena_* → helix_*)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 183-193 — Phase 53 section; success-criterion-1 has stale serena_* names)
    - .planning/phases/53-obs-metrics-gaps/53-01-SUMMARY.md (the actual production names that shipped)
    - .planning/phases/53-obs-metrics-gaps/53-CONTEXT.md (D-13 — the rename mandate; D-01 — `*_cache_hits_total` interpreted as a category, not a literal name)
  </read_first>
  <action>
    Open `.planning/ROADMAP.md` and find the Phase 53 success criteria block (line ~189):

    Original (stale strings):
    ```
    1. `/metrics` exposes new series: `serena_lspool_cache_hits_total`, `serena_repomap_cache_hits_total`, `serena_repomap_extract_duration_seconds` (histogram), `serena_session_lifecycle_total` (counter by phase), `serena_edit_outcome_total` (counter by tool + outcome).
    ```

    Replace with the corrected names per D-01 + D-13:
    ```
    1. `/metrics` exposes new series: `helix_lspool_lookups_total{language, result}` (counter; `result` ∈ {hit, miss}), `helix_repomap_lookups_total{language, result}` (counter), `helix_repomap_extract_duration_seconds{language, extractor}` (histogram), `helix_session_lifecycle_total{phase, transport}` (counter), `helix_edit_outcome_total{tool_name, outcome, strategy}` (counter).
    ```

    Notes for the executor:
    - The original wording `*_cache_hits_total` is a category, not a literal name. Plan 01 shipped `*_lookups_total` with a `result={hit,miss}` carve-out (D-01: hit-ratio is a PromQL expression; one counter per family is preferred over two counters). The ROADMAP wording is updated to match what actually shipped.
    - DO NOT change other success criteria (criteria 2/3/4 — cardinality bound, USAGE.md, noop-default — are accurate as-is).
    - DO NOT change the Phase 53 Goal line (line 184) or the Requirements/Sizing/Plans lines.

    Run a comprehensive serena_* check across the entire ROADMAP file as a guard:

    ```bash
    grep -n 'serena_' .planning/ROADMAP.md
    ```

    Any other `serena_*` metric names in the ROADMAP that reference real Helix metrics (i.e. they describe v1.9 work, not legacy Python references) should be corrected. References to `legacy/serena/...` filesystem paths are NOT a concern — leave those untouched.
  </action>
  <verify>
    <automated>grep -q 'helix_lspool_lookups_total' .planning/ROADMAP.md && grep -q 'helix_repomap_lookups_total' .planning/ROADMAP.md && grep -q 'helix_repomap_extract_duration_seconds' .planning/ROADMAP.md && grep -q 'helix_session_lifecycle_total' .planning/ROADMAP.md && grep -q 'helix_edit_outcome_total' .planning/ROADMAP.md && ! grep -q 'serena_lspool_cache_hits_total\|serena_repomap_cache_hits_total\|serena_repomap_extract_duration_seconds\|serena_session_lifecycle_total\|serena_edit_outcome_total' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'helix_lspool_lookups_total' .planning/ROADMAP.md` — new name present
    - `grep -q 'helix_repomap_lookups_total' .planning/ROADMAP.md`
    - `grep -q 'helix_repomap_extract_duration_seconds' .planning/ROADMAP.md`
    - `grep -q 'helix_session_lifecycle_total' .planning/ROADMAP.md`
    - `grep -q 'helix_edit_outcome_total' .planning/ROADMAP.md`
    - `! grep -q 'serena_lspool_cache_hits_total' .planning/ROADMAP.md` — D-13 fix applied
    - `! grep -q 'serena_repomap_cache_hits_total' .planning/ROADMAP.md`
    - `! grep -q 'serena_repomap_extract_duration_seconds' .planning/ROADMAP.md`
    - `! grep -q 'serena_session_lifecycle_total' .planning/ROADMAP.md`
    - `! grep -q 'serena_edit_outcome_total' .planning/ROADMAP.md`
    - Phase 53 Goal line unchanged: `grep -q 'Operators can observe cache hit-rate, RepoMap extraction latency, session lifecycle, and edit-tool outcomes' .planning/ROADMAP.md`
    - Requirements line unchanged: `grep -q '\*\*Requirements\*\*: OBS-03' .planning/ROADMAP.md`
  </acceptance_criteria>
  <done>ROADMAP success-criterion-1 reflects shipped reality; no serena_* metric strings linger in v1.9 phase definitions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.3: Human verifies USAGE.md docs render and PromQL examples are runnable</name>
  <files>USAGE.md, .planning/ROADMAP.md</files>
  <what-built>USAGE.md updated with 5 new metric rows, 2 PromQL examples, and the http best-effort caveat. ROADMAP.md Phase 53 success-criterion-1 now uses helix_* names.</what-built>
  <how-to-verify>
    1. Open `USAGE.md` in your editor or render it via `glow USAGE.md` / GitHub preview.
    2. Confirm the §"Prometheus Metrics" table shows 5 new rows in the same column structure as the existing rows (Name | Type | Labels | Description).
    3. Confirm the table renders cleanly — no broken Markdown pipes, no escaping issues from the `{result}` braces or backticks in the description prose.
    4. Confirm the two PromQL examples are formatted as code blocks with `promql` syntax tag.
    5. Confirm the best-effort caveat is visible and reads as a clear operator-facing limitation, not buried prose.
    6. (Optional, if you have a running Helix daemon with /metrics scraped into Prometheus) Run the hit-ratio PromQL example against your local Prometheus instance and confirm it returns a numeric value (or, if no traffic yet, an empty result with no syntax error).
    7. Open `.planning/ROADMAP.md`, locate the Phase 53 success criteria block, and confirm:
       - Success criterion 1 lists all 5 helix_* names with their label sets.
       - Goal line, Requirements, Sizing, and Plans lines are unchanged.
       - No serena_* strings remain in success criterion 1.
    8. Run `grep -c 'serena_' .planning/ROADMAP.md` and confirm any remaining matches refer to legacy/serena filesystem paths, not metric names.
  </how-to-verify>
  <action>Pause execution until the human reviewer completes the how-to-verify steps above and types the resume signal. The executor MUST NOT auto-approve this checkpoint — its purpose is to catch Markdown rendering issues that no automated check can catch (broken pipes from {} or backticks, code-block syntax tag misses, prose ambiguity in the best-effort caveat). When the human types "approved" (or equivalent), proceed to the SUMMARY step.</action>
  <verify>
    <automated>grep -q 'helix_lspool_lookups_total' USAGE.md &amp;&amp; grep -q 'best-effort' USAGE.md &amp;&amp; grep -q 'helix_lspool_lookups_total' .planning/ROADMAP.md &amp;&amp; ! grep -q 'serena_lspool_cache_hits_total' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Human reviewer types "approved" via the resume-signal channel
    - All 5 grep checks from Tasks 6.1 + 6.2 still pass at checkpoint time (regression guard if anything was reverted)
    - Reviewer confirms in their approval message that the table renders without broken pipes
  </acceptance_criteria>
  <done>Human approval received; documentation surface for Phase 53 is final.</done>
  <resume-signal>Type "approved" to continue, or describe any rendering / PromQL / ROADMAP issues to address before approval.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs surface | Documentation files have no runtime trust boundary; the threat is information drift between docs and code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-17 | Information Disclosure | docs reveal internal-only metrics | accept | helix_* metric names are already exposed at /metrics; docs cannot disclose more than the scrape endpoint |
| T-53-18 | Tampering (information drift) | USAGE.md vs. actual emitted metric names diverge | mitigate | acceptance_criteria verify that every documented family name appears AND that no serena_* names leak; future renames must update both surfaces in lockstep |
</threat_model>

<verification>
- `grep -q 'helix_lspool_lookups_total' USAGE.md && grep -q 'helix_lspool_lookups_total' .planning/ROADMAP.md`
- `grep -q 'helix_repomap_lookups_total' USAGE.md && grep -q 'helix_repomap_lookups_total' .planning/ROADMAP.md`
- `grep -q 'helix_repomap_extract_duration_seconds' USAGE.md && grep -q 'helix_repomap_extract_duration_seconds' .planning/ROADMAP.md`
- `grep -q 'helix_session_lifecycle_total' USAGE.md && grep -q 'helix_session_lifecycle_total' .planning/ROADMAP.md`
- `grep -q 'helix_edit_outcome_total' USAGE.md && grep -q 'helix_edit_outcome_total' .planning/ROADMAP.md`
- `! grep -q 'serena_lspool_cache_hits_total\|serena_repomap_cache_hits_total\|serena_repomap_extract_duration_seconds\|serena_session_lifecycle_total\|serena_edit_outcome_total' .planning/ROADMAP.md`
- `grep -q 'best-effort' USAGE.md`
- `grep -q 'histogram_quantile.*helix_repomap_extract_duration_seconds_bucket' USAGE.md`
- Human-verify checkpoint approved (Task 6.3)
</verification>

<success_criteria>
- USAGE.md §"Prometheus Metrics" lists all 5 new families with labels + value enums + semantics
- Two new PromQL examples present (hit-ratio + per-extractor p95)
- HTTP best-effort `ended` caveat documented operator-facing
- ROADMAP.md Phase 53 success-criterion-1 uses helix_* names per D-13
- Human verifies the renderings (Task 6.3 checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/53-obs-metrics-gaps/53-06-SUMMARY.md` with:
- Final list of metric families documented (cross-check against actual /metrics output if a local daemon is running)
- The exact ROADMAP diff (line-numbered before/after)
- Confirmation that the human-verify checkpoint was approved
- Any operator-facing prose decisions (e.g., choice of caveat phrasing) that the executor made within Claude's discretion
</output>
