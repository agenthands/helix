---
phase: 54-obs-dashboards-runbooks
plan: 03
type: execute
wave: 1
depends_on: [54-01]
files_modified:
  - docs/runbooks/ErrCircuitOpen.md
  - docs/runbooks/deadline-timeouts.md
  - docs/runbooks/ls-crash-restart.md
  - docs/runbooks/memory-pressure-eviction.md
  - docs/runbooks/README.md
autonomous: true
requirements: [OBS-02]

must_haves:
  truths:
    - "Each of the four runbooks has all six required ## sections (Symptoms, Detect, Triage, Remediate, Verify, Escalate)"
    - "Each runbook deep-links to specific dashboard panels via ${GRAFANA}/d/<uid>/<slug>?viewPanel=<id> with correct uids and panel ids matching 54-02"
    - "Every fenced ```promql block uses a registered metric and valid closed-enum label values"
    - "docs/runbooks/README.md indexes all four runbooks plus the screenshot-regen recipe"
  artifacts:
    - path: "docs/runbooks/ErrCircuitOpen.md"
      provides: "Runbook for lspool circuit-open events"
      contains: "## Symptoms"
    - path: "docs/runbooks/deadline-timeouts.md"
      provides: "Runbook for tool-call deadline timeouts"
      contains: "## Symptoms"
    - path: "docs/runbooks/ls-crash-restart.md"
      provides: "Runbook for LS process crash + restart events"
      contains: "## Symptoms"
    - path: "docs/runbooks/memory-pressure-eviction.md"
      provides: "Runbook for memory-pressure-driven worker eviction"
      contains: "## Symptoms"
    - path: "docs/runbooks/README.md"
      provides: "Runbook index + screenshot-regen recipe"
      contains: "Regenerating the screenshot"
  key_links:
    - from: "docs/runbooks/*.md"
      to: "deploy/grafana/serena-overview.json + serena-lspool.json"
      via: "?viewPanel=<id> deep links"
      pattern: "viewPanel="
    - from: "docs/runbooks/*.md"
      to: "internal/obs/dashboards_test.go"
      via: "promql fence blocks parsed and validated"
      pattern: "```promql"
---

<objective>
Author the four operational runbooks per CONTEXT D-13..D-16 and Pattern 3 in research, plus an index README. Implements OBS-02.

Purpose: Operator's incident path "alert/symptom → runbook → dashboard panel → fix → verify" lives here. The screenshot-regen recipe in the index is referenced by the inline drift comment USAGE.md will gain in 54-04.
Output: Four runbook MDs + index README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-obs-dashboards-runbooks/54-CONTEXT.md
@.planning/phases/54-obs-dashboards-runbooks/54-RESEARCH.md
@.planning/phases/53-obs-metrics-gaps/53-CONTEXT.md
@internal/obs/labelsets.go
@.planning/phases/54-obs-dashboards-runbooks/54-01-SUMMARY.md

<interfaces>
<!-- Same metric inventory as 54-02 — listed here for self-containment. -->
<!-- Runbook PromQL palette from 54-RESEARCH.md "Code Examples §" verbatim. -->

Detect / Verify queries per runbook (verbatim from research, validated against metric inventory):

ErrCircuitOpen:
  Detect: max by (language) (serena_lspool_circuit_state) >= 2
  Verify: max by (language) (serena_lspool_circuit_state) == 0

deadline-timeouts:
  Detect: histogram_quantile(0.95, sum by (le, tool_name) (rate(serena_tool_duration_seconds_bucket[5m])))
          PLUS sum by (tool_name) (rate(serena_tool_calls_total{outcome="timeout"}[5m])) > 0
  Verify: sum by (tool_name) (rate(serena_tool_calls_total{outcome="timeout"}[5m])) == 0

  NOTE: 54-01-SUMMARY.md confirms outcome="timeout" emission. If 54-01 reported it MISSING, swap "timeout" for the actual middleware-emitted value (likely "circuit_open" or other from outcomeEnum).

ls-crash-restart:
  Detect: rate(serena_lspool_restarts_total[5m]) > 0  OR  rate(serena_lspool_evictions_total{reason="crash"}[5m]) > 0
  Verify: rate(serena_lspool_restarts_total[10m]) == 0  AND  rate(serena_lspool_evictions_total{reason="crash"}[10m]) == 0

memory-pressure-eviction:
  Detect: rate(serena_lspool_evictions_total{reason="pressure"}[5m]) > 0
  Verify: rate(serena_lspool_evictions_total{reason="pressure"}[10m]) == 0

Dashboard deep-link targets (panel IDs from 54-02):
  Overview (uid serena-overview):  100 102 104 / 200 202 / 300 302 / 400 402
  LSPool   (uid serena-lspool):    100 102 / 200 202 / 300 / 400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/runbooks/ErrCircuitOpen.md and ls-crash-restart.md (lspool-circuit pair)</name>
  <files>docs/runbooks/ErrCircuitOpen.md, docs/runbooks/ls-crash-restart.md</files>
  <read_first>
    - .planning/phases/54-obs-dashboards-runbooks/54-RESEARCH.md (Pattern 3 ErrCircuitOpen example skeleton — copy structure verbatim)
    - .planning/phases/54-obs-dashboards-runbooks/54-CONTEXT.md (D-13 six sections; D-14 deep-link form; D-15 escape hatch)
    - deploy/grafana/serena-lspool.json (panel IDs 102, 200, 300)
    - internal/obs/labelsets.go (lspool reason allowlist {idle,pressure,crash,shutdown})
  </read_first>
  <behavior>
    - Each runbook has six `##` sections in order: Symptoms, Detect, Triage, Remediate, Verify, Escalate.
    - Each Detect and Verify section contains at least one fenced ```promql block; expressions parse cleanly via the 54-01 test.
    - Each Detect/Verify section contains at least one Grafana deep link of the form `${GRAFANA}/d/<uid>/<uid>?viewPanel=<id>` referencing a panel ID that EXISTS in the corresponding dashboard JSON.
    - Each Remediate section ends with the escape hatch line: "If none of the above resolves: file an issue with the output of `curl -s http://127.0.0.1:9100/metrics` and the daemon log from the last 10 minutes."
    - No absolute Grafana URLs (e.g., `http://grafana.example.com`); only the `${GRAFANA}` placeholder.
    - Lowercase ` ```promql ` fence info-string (Pitfall 2). The 54-01 test regex is case-insensitive but the convention is lowercase.
  </behavior>
  <action>
    **File 1: `docs/runbooks/ErrCircuitOpen.md`** — copy the Pattern 3 skeleton at 54-RESEARCH.md lines 316-387 verbatim, then adjust:
    - Detect deep link: `${GRAFANA}/d/serena-lspool/serena-lspool?viewPanel=200` (Circuit state stat panel id=200 in lspool dashboard).
    - Verify deep link: same `viewPanel=200`.
    - Detect PromQL fence:
      ```promql
      max by (language) (serena_lspool_circuit_state) >= 2
      ```
    - Triage cross-check fences (two PromQL blocks):
      ```promql
      rate(serena_lspool_restarts_total[5m]) > 0
      ```
      ```promql
      rate(serena_lspool_evictions_total{reason="pressure"}[5m]) > 0
      ```
    - Remediate code blocks: `pkill -USR1 serena` shell block, plus the YAML config edit raising `consecutive_failures` (verbatim from research).
    - Verify PromQL fences:
      ```promql
      max by (language) (serena_lspool_circuit_state) == 0
      ```
      ```promql
      rate(serena_lspool_restarts_total[5m]) == 0
      ```
    - Escalate section identical to Pattern 3 example (curl + daemon log + serena status --json --verbose).

    **File 2: `docs/runbooks/ls-crash-restart.md`** — same six-section template, content focused on LS crash + restart loops:

    `## Symptoms`:
    - Specific languages report frequent disconnects from agent tool calls.
    - LSPool dashboard "LS restarts/sec by language" panel (id 102) shows non-zero rate.
    - Daemon logs contain repeated `worker spawn` / `worker exit` pairs for the same language.

    `## Detect`:
    Deep link: `${GRAFANA}/d/serena-lspool/serena-lspool?viewPanel=102` (Restarts panel) and `${GRAFANA}/d/serena-lspool/serena-lspool?viewPanel=300` (Evictions by reason).
    PromQL fences:
    ```promql
    rate(serena_lspool_restarts_total[5m]) > 0
    ```
    ```promql
    rate(serena_lspool_evictions_total{reason="crash"}[5m]) > 0
    ```

    `## Triage` (decision tree as bulleted prose):
    1. Is a single language affected, or multiple? Single language → likely LS-binary issue. Multiple → host-level (memory, fd limits).
    2. Are evictions tagged `reason="crash"` or `reason="pressure"`? Crash → see this runbook. Pressure → cross-link to `memory-pressure-eviction.md`.
    3. Is the LS binary the version Serena auto-managed, or a PATH override? PATH override is the more common foot-gun.
    PromQL cross-check fence:
    ```promql
    sum by (reason) (rate(serena_lspool_evictions_total[5m]))
    ```

    `## Remediate` (concrete commands):
    - Force a fresh worker spawn:
      ```bash
      pkill -USR1 serena
      ```
    - Pin to the auto-managed LS by removing the PATH override in `~/.serena/serena_config.yml`:
      ```yaml
      languages:
        java:
          ls_binary_override: ""   # remove PATH override; use auto-managed
      ```
    - Cap concurrent workers per language to reduce thrash:
      ```yaml
      lspool:
        max_workers_per_language: 2
      ```
    - Escape hatch: "If none of the above resolves: file an issue with the output of `curl -s http://127.0.0.1:9100/metrics` and the daemon log from the last 10 minutes."

    `## Verify` (deep link viewPanel=102):
    ```promql
    rate(serena_lspool_restarts_total[10m]) == 0
    ```
    ```promql
    rate(serena_lspool_evictions_total{reason="crash"}[10m]) == 0
    ```

    `## Escalate`: same as ErrCircuitOpen Pattern 3 — issue link, curl-metrics-grep, daemon log, `serena status --json --verbose`.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena &amp;&amp; for f in docs/runbooks/ErrCircuitOpen.md docs/runbooks/ls-crash-restart.md; do for h in '## Symptoms' '## Detect' '## Triage' '## Remediate' '## Verify' '## Escalate'; do grep -q "^$h" "$f" || { echo "MISSING $h in $f"; exit 1; }; done; grep -q 'viewPanel=' "$f" || { echo "MISSING deep link in $f"; exit 1; }; grep -q '127.0.0.1:9100/metrics' "$f" || { echo "MISSING escape hatch in $f"; exit 1; }; done &amp;&amp; go test ./internal/obs/... -run TestDashboards -count=1</automated>
  </verify>
  <done>
    - Both files exist; each contains all six `## ` headings (Symptoms..Escalate).
    - Each contains at least one `viewPanel=` deep link with valid uid (`serena-lspool`) and a panel id that exists in `deploy/grafana/serena-lspool.json` (102, 200, or 300).
    - Each contains the curl-metrics escape hatch line.
    - `go test ./internal/obs/... -run TestDashboards -count=1` exits 0 — every runbook PromQL fence parses and validates.
    - No absolute Grafana URL: `! grep -E 'https?://[^$].*grafana' docs/runbooks/ErrCircuitOpen.md docs/runbooks/ls-crash-restart.md` (URLs only via `${GRAFANA}`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author docs/runbooks/deadline-timeouts.md and memory-pressure-eviction.md</name>
  <files>docs/runbooks/deadline-timeouts.md, docs/runbooks/memory-pressure-eviction.md</files>
  <read_first>
    - docs/runbooks/ErrCircuitOpen.md (just-authored — six-section structure to mirror)
    - .planning/phases/54-obs-dashboards-runbooks/54-01-SUMMARY.md (CRITICAL: confirms outcome="timeout" or fallback value)
    - deploy/grafana/serena-overview.json (panel ids 102, 104 for tool errors / duration; 302 for session timeouts)
    - deploy/grafana/serena-lspool.json (panel id 300 for evictions)
    - internal/obs/labelsets.go (tool-family outcome enum; lspool eviction reasons)
  </read_first>
  <behavior>
    - Same six-section template + escape hatch + ${GRAFANA} convention as Task 1.
    - `deadline-timeouts.md` deep-links into Overview (id 102 errors, id 104 duration p95, id 302 session timeouts).
    - `memory-pressure-eviction.md` deep-links into LSPool (id 300 evictions, id 100 workers, id 102 restarts).
    - Every fenced ```promql block uses metrics from the registered set and closed-enum values from `ClosedEnumLabelValues`.
  </behavior>
  <action>
    **File 1: `docs/runbooks/deadline-timeouts.md`**

    `## Symptoms`:
    - Tool calls return errors with deadline-exceeded messaging to the agent.
    - Overview dashboard panel "Tool error rate %" (id 102) shows elevated `outcome="timeout"` share.
    - Overview dashboard panel "Tool duration p95 (s)" (id 104) shows latency spikes for affected tools.

    `## Detect`:
    Deep links:
    - `${GRAFANA}/d/serena-overview/serena-overview?viewPanel=104` — duration p95 by tool.
    - `${GRAFANA}/d/serena-overview/serena-overview?viewPanel=102` — error rate.

    Alert PromQL:
    ```promql
    sum by (tool_name) (rate(serena_tool_calls_total{outcome="timeout"}[5m])) > 0
    ```
    Latency context:
    ```promql
    histogram_quantile(0.95, sum by (le, tool_name) (rate(serena_tool_duration_seconds_bucket[5m])))
    ```

    NOTE TO EXECUTOR: If 54-01-SUMMARY.md says outcome="timeout" is NOT emitted by middleware, replace `outcome="timeout"` above with the value middleware actually emits (read from outcomeEnum in metrics.go: success / invalid_args / not_found / circuit_open / ls_crash / timeout / internal). The Wave 0 grep step in 54-01 records this finding.

    `## Triage`:
    1. Is the timeout localized to one tool? PromQL:
       ```promql
       topk(5, sum by (tool_name) (rate(serena_tool_calls_total{outcome="timeout"}[5m])))
       ```
    2. Does it correlate with a circuit-open event? Cross-link to `ErrCircuitOpen.md`.
    3. Is the per-tool deadline budget too tight? Inspect `serena_config.yml` `mcp.tool_deadlines:` overrides.
    4. Is session activation timing out (different from tool timeout)? Check Overview panel id 302:
       ```promql
       sum by (language) (rate(serena_session_lifecycle_total{phase="timeout"}[5m]))
       ```

    `## Remediate`:
    - Raise the per-tool deadline in `~/.serena/serena_config.yml`:
      ```yaml
      mcp:
        tool_deadlines:
          find_symbols: "30s"   # default 10s
      ```
    - For session-activation timeouts, raise the activation budget:
      ```yaml
      kernel:
        activation_timeout: "60s"
      ```
    - Restart the daemon to apply config changes:
      ```bash
      pkill -USR1 serena
      ```
    - Escape hatch line.

    `## Verify` (deep link viewPanel=102 + viewPanel=104):
    ```promql
    sum by (tool_name) (rate(serena_tool_calls_total{outcome="timeout"}[5m])) == 0
    ```

    `## Escalate`: same as ErrCircuitOpen Escalate.

    **File 2: `docs/runbooks/memory-pressure-eviction.md`**

    `## Symptoms`:
    - LSPool dashboard panel "Evictions/sec by reason" (id 300) shows non-zero `reason="pressure"` rate.
    - Workers gauge (id 100) drops faster than steady-state.
    - Daemon logs contain `evicting worker due to memory pressure` entries.

    `## Detect`:
    Deep link: `${GRAFANA}/d/serena-lspool/serena-lspool?viewPanel=300`.
    ```promql
    rate(serena_lspool_evictions_total{reason="pressure"}[5m]) > 0
    ```
    Resident memory context (process-level metrics from prom client_golang):
    ```promql
    process_resident_memory_bytes
    ```

    `## Triage`:
    1. Is the host genuinely memory-pressured (free RAM low) or is Serena's eviction threshold too sensitive? Check `vm_stat` (macOS) / `/proc/meminfo` (Linux).
    2. Are evictions concentrated on one heavy LS (e.g., jdtls)? PromQL:
       ```promql
       sum by (language) (rate(serena_lspool_evictions_total{reason="pressure"}[5m]))
       ```
    3. Are restarts following the evictions (thrash)? Cross-check panel id 102:
       ```promql
       rate(serena_lspool_restarts_total[5m])
       ```

    `## Remediate`:
    - Raise the memory-pressure threshold in `~/.serena/serena_config.yml`:
      ```yaml
      lspool:
        eviction:
          memory_pressure_threshold_pct: 85   # default 75
      ```
    - Cap concurrent heavy LSes:
      ```yaml
      lspool:
        max_workers_per_language: 1
      ```
    - Restart daemon to apply:
      ```bash
      pkill -USR1 serena
      ```
    - Escape hatch line.

    `## Verify` (deep link viewPanel=300):
    ```promql
    rate(serena_lspool_evictions_total{reason="pressure"}[10m]) == 0
    ```

    `## Escalate`: same.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena &amp;&amp; for f in docs/runbooks/deadline-timeouts.md docs/runbooks/memory-pressure-eviction.md; do for h in '## Symptoms' '## Detect' '## Triage' '## Remediate' '## Verify' '## Escalate'; do grep -q "^$h" "$f" || { echo "MISSING $h in $f"; exit 1; }; done; grep -q 'viewPanel=' "$f" || exit 1; grep -q '127.0.0.1:9100/metrics' "$f" || exit 1; done &amp;&amp; go test ./internal/obs/... -run TestDashboards -count=1</automated>
  </verify>
  <done>
    - Both files exist with all six headings.
    - `grep -c '```promql' docs/runbooks/deadline-timeouts.md` ≥ 4.
    - `grep -c '```promql' docs/runbooks/memory-pressure-eviction.md` ≥ 4.
    - `go test ./internal/obs/... -run TestDashboards -count=1` exits 0.
    - No absolute Grafana URLs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author docs/runbooks/README.md (index + screenshot regen recipe)</name>
  <files>docs/runbooks/README.md</files>
  <read_first>
    - docs/runbooks/{ErrCircuitOpen,ls-crash-restart,deadline-timeouts,memory-pressure-eviction}.md (just-authored)
    - .planning/phases/54-obs-dashboards-runbooks/54-CONTEXT.md (D-18 — drift recipe lives here)
    - .planning/phases/54-obs-dashboards-runbooks/54-RESEARCH.md (Open Question 4 — synthetic data recipe text)
  </read_first>
  <behavior>
    - Index lists all four runbooks with one-line summary.
    - Documents the `${GRAFANA}` placeholder convention.
    - Includes the "Regenerating the screenshot" section (Pattern 3 + Open Question 4 in research) — this is what the inline HTML drift comment in USAGE.md (added by 54-04) points at.
  </behavior>
  <action>
    Create `docs/runbooks/README.md` with this exact content:

    ```markdown
    # Serena Operational Runbooks

    Six-section runbooks (Symptoms → Detect → Triage → Remediate → Verify → Escalate) for the most common operational failure modes.

    ## Index

    | Runbook | One-liner |
    |---------|-----------|
    | [`ErrCircuitOpen.md`](./ErrCircuitOpen.md) | LSPool circuit breaker has opened for one or more languages; new acquires fail with `ErrCircuitOpen` until the breaker half-opens. |
    | [`deadline-timeouts.md`](./deadline-timeouts.md) | Tool calls are exceeding their per-tool deadline budget; agent sees timeout errors. |
    | [`ls-crash-restart.md`](./ls-crash-restart.md) | Language-server processes are crashing and being respawned; the lspool restart counter is non-zero. |
    | [`memory-pressure-eviction.md`](./memory-pressure-eviction.md) | LSPool is evicting workers under host memory pressure faster than steady-state. |

    ## Convention: `${GRAFANA}` placeholder

    Runbook deep links use `${GRAFANA}` as the Grafana base URL placeholder. Replace it with your own (e.g. `http://grafana.example.com:3000`) when copying URLs:

    `${GRAFANA}/d/serena-overview/serena-overview?viewPanel=100`

    Operators typically prepend their host once mentally; the runbooks are portable across deployments.

    ## Regenerating the screenshot

    `USAGE.md` § "Grafana Dashboards" embeds a screenshot at `docs/img/grafana-overview.png`. Regenerate when the panel set in `deploy/grafana/serena-overview.json` changes:

    1. Stand up Grafana 10+ locally (e.g. `docker run -p 3000:3000 grafana/grafana:10.4.0`).
    2. Add a Prometheus datasource pointing at a Serena instance scraped on `127.0.0.1:9100/metrics`.
    3. Drive Serena against your own dev workspace for ~10 minutes with both Java and Go LSes warmed up — this produces realistic-looking traffic across ~12 languages with healthy hit-rates and at least one obvious anomaly (e.g. a Java timeout). No synthetic-traffic generator needed.
    4. Import `deploy/grafana/serena-overview.json` (Dashboards → Import → Upload JSON file).
    5. Wait ~5 minutes for the 1h time window to populate.
    6. Capture the dashboard at ~1600px wide and overwrite `docs/img/grafana-overview.png`.
    7. Commit alongside any matching dashboard JSON change.
    ```
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena &amp;&amp; grep -q 'ErrCircuitOpen.md' docs/runbooks/README.md &amp;&amp; grep -q 'deadline-timeouts.md' docs/runbooks/README.md &amp;&amp; grep -q 'ls-crash-restart.md' docs/runbooks/README.md &amp;&amp; grep -q 'memory-pressure-eviction.md' docs/runbooks/README.md &amp;&amp; grep -q '## Regenerating the screenshot' docs/runbooks/README.md &amp;&amp; grep -q '\${GRAFANA}' docs/runbooks/README.md</automated>
  </verify>
  <done>
    - All four runbook filenames appear in the index table.
    - "Regenerating the screenshot" heading present.
    - `${GRAFANA}` placeholder convention documented.
  </done>
</task>

</tasks>

<verification>
- All four runbooks pass the six-section grep gate.
- All ```promql fences parse and validate via 54-01's test.
- Every viewPanel= deep link references a panel id that exists in 54-02's dashboards.
- Index README links to all four runbooks plus the screenshot-regen recipe.
</verification>

<success_criteria>
- OBS-02 covered: four runbooks shipped per CONTEXT D-16 naming.
- 54-VALIDATION.md task 54-03-01 flips to ✅.
- 54-04 has the regen-recipe URL it needs to point the inline drift comment at.
</success_criteria>

<output>
After completion, create `.planning/phases/54-obs-dashboards-runbooks/54-03-SUMMARY.md` recording the PromQL palette actually used per runbook (note any swap from `outcome="timeout"` to a fallback value if 54-01 reported the timeout outcome MISSING).
</output>
