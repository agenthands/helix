---
phase: 54-obs-dashboards-runbooks
plan: 04
type: execute
wave: 2
depends_on: [54-02, 54-03]
files_modified:
  - USAGE.md
  - docs/img/grafana-overview.png
autonomous: false
requirements: [OBS-01]

must_haves:
  truths:
    - "USAGE.md has a new ### Grafana Dashboards subsection between line 640 (### Prometheus Metrics) and line 685 (#### Phase 53 metric families)"
    - "Subsection includes import instructions, the embedded screenshot, and links to deploy/grafana/ and docs/runbooks/"
    - "An inline HTML drift comment immediately precedes the screenshot pointing at the regen recipe in docs/runbooks/README.md"
    - "docs/img/grafana-overview.png exists, is committed, and is ~1600px wide (manual capture)"
  artifacts:
    - path: "USAGE.md"
      provides: "Operator-facing dashboard documentation"
      contains: "### Grafana Dashboards"
    - path: "docs/img/grafana-overview.png"
      provides: "Primary dashboard screenshot embedded in USAGE.md"
      contains: "PNG image (binary)"
  key_links:
    - from: "USAGE.md ### Grafana Dashboards"
      to: "docs/img/grafana-overview.png"
      via: "Markdown image link"
      pattern: "docs/img/grafana-overview.png"
    - from: "USAGE.md ### Grafana Dashboards"
      to: "deploy/grafana/ + docs/runbooks/"
      via: "Markdown link references"
      pattern: "deploy/grafana"
    - from: "USAGE.md drift comment"
      to: "docs/runbooks/README.md#regenerating-the-screenshot"
      via: "Inline HTML comment"
      pattern: "Regenerate when"
---

<objective>
Wire the dashboards into USAGE.md per CONTEXT D-19, and capture the committed screenshot per D-17/D-18. Closes the OBS-01 USAGE.md success criterion.

Purpose: Operator-discoverability — a user reading USAGE.md sees what the dashboards look like and follows two links to import + read runbooks.
Output: Modified USAGE.md + new committed PNG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-obs-dashboards-runbooks/54-CONTEXT.md
@.planning/phases/54-obs-dashboards-runbooks/54-RESEARCH.md
@USAGE.md
@deploy/grafana/serena-overview.json
@deploy/grafana/serena-lspool.json
@deploy/grafana/README.md
@docs/runbooks/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ### Grafana Dashboards subsection to USAGE.md (D-19 anchor)</name>
  <files>USAGE.md</files>
  <read_first>
    - USAGE.md (specifically lines 600-700 — the Observability Quickstart section; locate exact line of `### Prometheus Metrics` (currently 640) and `#### Phase 53 metric families` (currently 685))
    - .planning/phases/54-obs-dashboards-runbooks/54-CONTEXT.md (D-19 — insertion point + content list)
    - deploy/grafana/README.md (just-authored — duplicate the import recipe to keep USAGE.md self-contained)
    - docs/runbooks/README.md (link target for the screenshot drift comment)
  </read_first>
  <behavior>
    - New `### Grafana Dashboards` subsection inserted AFTER the existing `### Prometheus Metrics` subsection (currently ending around line 684) and BEFORE `#### Phase 53 metric families` (currently line 685).
    - Subsection contains: (1) one-paragraph orientation, (2) import recipe (UI: Dashboards → Import → Upload JSON file → pick Prometheus datasource), (3) inline HTML drift comment, (4) screenshot embed, (5) links to `deploy/grafana/` and `docs/runbooks/`.
    - Inline HTML comment text matches D-18 verbatim.
    - The screenshot embed uses a relative repo path so GitHub renders it correctly.
  </behavior>
  <action>
    Locate the line after the existing `### Prometheus Metrics` subsection content ends and immediately before the line `#### Phase 53 metric families` (currently line 685). Insert the following block exactly (with one blank line on each side of the new heading):

    ```markdown
    ### Grafana Dashboards

    Two ready-to-import Grafana 10+ dashboards live under `deploy/grafana/`:

    - **`serena-overview.json`** — operator landing page. RED metrics (rate / errors / duration), edit-tool outcomes, session lifecycle, and RepoMap extract latency + cache hit-rate.
    - **`serena-lspool.json`** — LS pool drill-down. Workers, circuit state, evictions by reason, lspool cache hit-rate.

    To import:

    1. Open Grafana → **Dashboards** → **Import**.
    2. Choose **Upload JSON file** and pick `serena-overview.json` (or `serena-lspool.json`).
    3. Select your Prometheus datasource (the one scraping `127.0.0.1:9100/metrics`) and click **Import**.

    <!-- Regenerate when serena-overview.json panel set changes. Recipe: docs/runbooks/README.md#regenerating-the-screenshot. -->

    ![Serena Overview dashboard](docs/img/grafana-overview.png)

    Each dashboard panel is deep-linked from the runbooks under [`docs/runbooks/`](docs/runbooks/) — replace `${GRAFANA}` in any runbook URL with your Grafana base URL. See [`deploy/grafana/README.md`](deploy/grafana/README.md) for the full list.

    ```

    Verify the surrounding lines are intact: line above the new block is the last line of the existing `### Prometheus Metrics` subsection; line below is `#### Phase 53 metric families`.

    Do NOT touch any other USAGE.md content. Do NOT renumber or reformat the existing Observability Quickstart section.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena &amp;&amp; grep -q '^### Grafana Dashboards' USAGE.md &amp;&amp; grep -q 'docs/img/grafana-overview.png' USAGE.md &amp;&amp; grep -q 'Regenerate when serena-overview.json' USAGE.md &amp;&amp; grep -q 'deploy/grafana/README.md' USAGE.md &amp;&amp; grep -q 'docs/runbooks/' USAGE.md &amp;&amp; awk '/^### Grafana Dashboards/{p=NR} /^#### Phase 53 metric families/{q=NR} END{exit !(p &amp;&amp; q &amp;&amp; p<q)}' USAGE.md</automated>
  </verify>
  <done>
    - `### Grafana Dashboards` heading exists.
    - The heading appears AFTER `### Prometheus Metrics` and BEFORE `#### Phase 53 metric families` (awk ordering check).
    - Inline HTML drift comment present with exact wording from D-18.
    - Screenshot reference `docs/img/grafana-overview.png` present.
    - Links to both `deploy/grafana/README.md` and `docs/runbooks/` present.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture and commit docs/img/grafana-overview.png (D-17 manual capture)</name>
  <files>docs/img/grafana-overview.png</files>
  <read_first>
    - docs/runbooks/README.md (the regen recipe authored in 54-03 — operator follows this)
    - deploy/grafana/serena-overview.json (the dashboard being screenshotted)
  </read_first>
  <what-built>
    USAGE.md now references `docs/img/grafana-overview.png` but the file does not yet exist (D-17 mandates manual capture). The runbook regen recipe in `docs/runbooks/README.md#regenerating-the-screenshot` documents the exact steps. Claude has set up everything except the image bytes; the operator must follow the recipe and commit the PNG.
  </what-built>
  <how-to-verify>
    1. Stand up Grafana 10+ locally:
       ```
       docker run --rm -d -p 3000:3000 --name serena-grafana grafana/grafana:10.4.0
       ```
       (Or use any Grafana 10+ instance you already operate.)
    2. Add a Prometheus datasource pointing at a Serena instance scraped on `127.0.0.1:9100/metrics`.
    3. Drive Serena against your own dev workspace for ~10 minutes with at least Java and Go LSes warmed (any normal coding session against this repo qualifies). This produces realistic traffic across multiple languages.
    4. In Grafana: **Dashboards → Import → Upload JSON file** → select `deploy/grafana/serena-overview.json` → pick the Prometheus datasource → **Import**.
    5. Wait ~5 minutes so the 1h time window has populated panels.
    6. Capture the full dashboard view at ~1600px wide. Use your OS screenshot tool (e.g. `Cmd-Shift-4` on macOS, scoped to the dashboard area).
    7. Save to `docs/img/grafana-overview.png` (overwrite if present).
    8. Confirm the file exists and is committed:
       ```
       ls -la docs/img/grafana-overview.png
       file docs/img/grafana-overview.png   # expect "PNG image data, 1600 x ..."
       ```
    9. Type "approved" once the screenshot looks operator-friendly (~12 languages visible, healthy hit-rates, at least one obvious anomaly). Or describe what's missing/wrong.
  </how-to-verify>
  <resume-signal>Type "approved" once docs/img/grafana-overview.png is captured, ~1600px wide, and committed. Or describe the issue.</resume-signal>
</task>

</tasks>

<verification>
- USAGE.md gains the `### Grafana Dashboards` subsection at the correct anchor.
- `docs/img/grafana-overview.png` exists in the repo and renders in USAGE.md when viewed on GitHub.
- `go vet ./...` and `go test ./...` still green (no Go code touched, but sanity check).
</verification>

<success_criteria>
- OBS-01 USAGE.md success criterion satisfied (success criterion 3 in ROADMAP Phase 54).
- 54-VALIDATION.md task 54-04-01 flips to ✅.
</success_criteria>

<output>
After completion, create `.planning/phases/54-obs-dashboards-runbooks/54-04-SUMMARY.md` noting the exact USAGE.md line range of the new subsection and confirming the PNG dimensions.
</output>
