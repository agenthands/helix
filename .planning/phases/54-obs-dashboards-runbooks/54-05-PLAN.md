---
phase: 54-obs-dashboards-runbooks
plan: 05
type: execute
wave: 3
depends_on: [54-01, 54-02, 54-03, 54-04]
files_modified:
  - internal/obs/dashboards_test.go
autonomous: true
requirements: [OBS-01, OBS-02]

must_haves:
  truths:
    - "go vet ./... is green"
    - "go test ./... is green (full suite, not just internal/obs)"
    - "TestDashboardsAndRunbooks_PromQLValidates counts > 0 PromQL expressions and asserts that count"
    - "Wave 0 permissive 'len(exprs) == 0 → Logf+return' guard is REMOVED, replaced by t.Fatal"
  artifacts:
    - path: "internal/obs/dashboards_test.go"
      provides: "Tightened test that fatals if no PromQL expressions are discovered"
      contains: "t.Fatal"
  key_links:
    - from: "internal/obs/dashboards_test.go"
      to: "deploy/grafana/*.json + docs/runbooks/*.md"
      via: "len(exprs) > 0 invariant"
      pattern: "len\\(exprs\\)"
---

<objective>
Final phase gate. Tighten the Wave 0 permissive test to enforce non-zero PromQL coverage now that artifacts ship, and run the full Go suite to confirm Phase 54 is shippable.

Purpose: Prevent future regressions where dashboards/runbooks get accidentally deleted (the test would silently pass under the Wave 0 guard).
Output: One small edit to dashboards_test.go + green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-obs-dashboards-runbooks/54-CONTEXT.md
@.planning/phases/54-obs-dashboards-runbooks/54-VALIDATION.md
@internal/obs/dashboards_test.go
@deploy/grafana/serena-overview.json
@deploy/grafana/serena-lspool.json
@docs/runbooks/ErrCircuitOpen.md
@docs/runbooks/deadline-timeouts.md
@docs/runbooks/ls-crash-restart.md
@docs/runbooks/memory-pressure-eviction.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten dashboards_test.go — enforce len(exprs) > 0</name>
  <files>internal/obs/dashboards_test.go</files>
  <read_first>
    - internal/obs/dashboards_test.go (Wave 0 skeleton — locate the `if len(exprs) == 0 { t.Logf(...); return }` block)
    - .planning/phases/54-obs-dashboards-runbooks/54-VALIDATION.md (task 54-05-01 expectation)
    - deploy/grafana/serena-overview.json (count expressions)
    - deploy/grafana/serena-lspool.json (count expressions)
    - docs/runbooks/*.md (count fences)
  </read_first>
  <behavior>
    - Replace the Wave 0 permissive guard with a hard fatal: `if len(exprs) == 0 { t.Fatal("expected >0 PromQL expressions across deploy/grafana and docs/runbooks; ship the artifacts") }`.
    - Add an explicit minimum-coverage assertion: dashboards contribute ≥ 9 (overview) + 6 (lspool) = 15 expressions; runbooks contribute ≥ 4 (one Detect + one Verify minimum) × 4 runbooks = 8 expressions; total ≥ 23. Use a slightly conservative floor of `len(exprs) >= 15` to avoid brittleness while still catching wholesale deletion.
  </behavior>
  <action>
    Open `internal/obs/dashboards_test.go`. Locate the Wave 0 guard:

    ```go
    if len(exprs) == 0 {
        t.Logf("found 0 PromQL expressions across deploy/grafana and docs/runbooks — Wave 1 not yet shipped")
        return
    }
    ```

    Replace it with:

    ```go
    if len(exprs) == 0 {
        t.Fatal("expected >0 PromQL expressions across deploy/grafana and docs/runbooks; the artifacts may have been deleted")
    }
    if len(exprs) < 15 {
        t.Errorf("PromQL coverage regression: found %d expressions, expected >=15 across both dashboards + four runbooks", len(exprs))
    }
    t.Logf("validated %d PromQL expressions", len(exprs))
    ```

    Do not change any other test logic.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena &amp;&amp; grep -q 't.Fatal("expected >0 PromQL expressions' internal/obs/dashboards_test.go &amp;&amp; ! grep -q 'Wave 1 not yet shipped' internal/obs/dashboards_test.go &amp;&amp; go test ./internal/obs/... -run TestDashboards -count=1 -v 2>&amp;1 | grep -E 'validated [0-9]+ PromQL expressions'</automated>
  </verify>
  <done>
    - Wave 0 permissive `Logf+return` block is removed.
    - New `t.Fatal` and `>=15` floor are present.
    - Test output line `validated N PromQL expressions` shows N ≥ 15.
    - `go test ./internal/obs/... -run TestDashboards -count=1` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final gate — go vet ./... and go test ./... full suite</name>
  <files>(none — verification-only)</files>
  <read_first>
    - CLAUDE.md (project mandates `go vet` + `go test` before completing)
    - .planning/phases/54-obs-dashboards-runbooks/54-VALIDATION.md (task 54-05-01 commands)
  </read_first>
  <behavior>
    - `go vet ./...` exits 0.
    - `go test ./... -count=1` exits 0 — the full suite, not just `internal/obs`.
    - If anything is red, the executor fixes it before declaring the task done. No skipping, no `t.Skip`, no build tags.
  </behavior>
  <action>
    Run from repo root:
    ```
    go vet ./...
    go test ./... -count=1
    ```

    If either command fails:
    1. Read the failure carefully — Phase 54 should not have introduced any failures outside `internal/obs/...` since it only modified static assets and one test file. A failure elsewhere is most likely a flaky test or a pre-existing issue surfaced by `-count=1`.
    2. If the failure is in `internal/obs/dashboards_test.go`: a PromQL expression in dashboards/runbooks doesn't validate. Read the error line:column, fix the offending expression in the corresponding artifact (do NOT loosen the test).
    3. If the failure is elsewhere and clearly unrelated to Phase 54 (e.g. an integration test that depends on jdtls/rust-analyzer being installed): re-run with `-short` (`go test -short ./...`) and record both outcomes in the SUMMARY. Do NOT silence the failure with a skip.

    Record the final command outputs (PASS lines) in 54-05-SUMMARY.md.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena &amp;&amp; go vet ./... &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <done>
    - `go vet ./...` exits 0.
    - `go test ./... -count=1` exits 0.
    - SUMMARY records pass-line snippets and any environment caveats (e.g. integration tests that needed `-short`).
  </done>
</task>

</tasks>

<verification>
- The full Go suite is green.
- The PromQL test now refuses to silently pass on an empty artifact set.
- Phase 54 is ready for `/gsd-verify-work`.
</verification>

<success_criteria>
- ROADMAP Phase 54 success criterion 4 ("validated by test") is hard-enforced, not permissive.
- 54-VALIDATION.md task 54-05-01 flips to ✅.
- All four ROADMAP success criteria satisfied: (1) ≥2 dashboards in deploy/grafana, (2) 4 runbooks in docs/runbooks, (3) USAGE.md links + screenshot, (4) PromQL→registry test green with N>0.
</success_criteria>

<output>
After completion, create `.planning/phases/54-obs-dashboards-runbooks/54-05-SUMMARY.md` recording:
- Final expression count (from the `validated N PromQL expressions` log line).
- `go vet ./...` and `go test ./... -count=1` pass confirmation.
- Any flaky/integration caveats encountered.
</output>
