---
phase: 54
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/runbooks_test.go
autonomous: true
requirements: [OBS-02]
must_haves:
  truths:
    - "All four runbook files exist at the exact paths specified by ROADMAP criterion #3: docs/runbooks/ErrCircuitOpen.md, docs/runbooks/deadline-timeouts.md, docs/runbooks/ls-crash-restart.md, docs/runbooks/memory-pressure-eviction.md."
    - "Each runbook contains the required sections (symptoms with log lines, triage as local commands, remediation as config knobs / restart procedure)."
    - "No runbook contains any of the forbidden strings: 'Grafana', 'Grafana Cloud', 'PromQL', 'panel deep-link', '${GRAFANA_URL}'."
    - "No runbook contains a fenced ```promql code block."
    - "docs/runbooks/README.md indexes all four runbooks (cross-link integrity)."
    - "These invariants are enforced by a Go test in CI so future doc edits cannot silently regress."
  artifacts:
    - path: "docs/runbooks/runbooks_test.go"
      provides: "TestRunbookCompliance — file existence + required-section grep + forbidden-string grep + README index"
      contains: "func TestRunbookCompliance"
  key_links:
    - from: "docs/runbooks/runbooks_test.go"
      to: "docs/runbooks/*.md"
      via: "os.ReadFile + string/regexp asserts on each runbook"
      pattern: "ErrCircuitOpen\\.md|deadline-timeouts\\.md|ls-crash-restart\\.md|memory-pressure-eviction\\.md"
---

<objective>
Add a Go test that enforces the four runbooks (already shipped in commit `780a3eda`) continue to comply with ROADMAP criterion #3: each file exists, contains the required sections, and contains none of the forbidden Grafana/PromQL strings. The runbook content itself is NOT rewritten — researcher verified compliance in `54-RESEARCH.md` § "Runbook Verification". This plan only adds the regression guard.

Purpose: Satisfy ROADMAP criterion #3 by making compliance machine-checkable in CI. Without this test, a future doc edit could re-introduce forbidden references and the criterion would silently regress.

Output: A single new test file `docs/runbooks/runbooks_test.go` that fails CI if any runbook is deleted, missing required sections, or contains forbidden strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/54-obs-inbinary-page/54-RESEARCH.md
@.planning/phases/54-obs-inbinary-page/54-VALIDATION.md
@CLAUDE.md
@docs/runbooks/README.md
@docs/runbooks/ErrCircuitOpen.md
@docs/runbooks/deadline-timeouts.md
@docs/runbooks/ls-crash-restart.md
@docs/runbooks/memory-pressure-eviction.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runbook compliance test (file existence + section grep + forbidden-string gate)</name>
  <read_first>
    - docs/runbooks/README.md (verify the four runbooks listed match expected names; check link format used in the index)
    - docs/runbooks/ErrCircuitOpen.md (skim — confirm section headings used: e.g. ## Symptoms, ## Triage, ## Remediation; pick exact heading text actually present)
    - docs/runbooks/deadline-timeouts.md
    - docs/runbooks/ls-crash-restart.md
    - docs/runbooks/memory-pressure-eviction.md
    - .planning/phases/54-obs-inbinary-page/54-RESEARCH.md (§ "Runbook Verification" — researcher's full compliance matrix; the test should encode the same gates)
    - .planning/phases/54-obs-inbinary-page/54-VALIDATION.md (row 54-02-01 — automated command shape)
  </read_first>
  <files>docs/runbooks/runbooks_test.go</files>
  <behavior>
    - TestRunbookCompliance asserts all four runbook files exist relative to the test file (`./ErrCircuitOpen.md`, `./deadline-timeouts.md`, `./ls-crash-restart.md`, `./memory-pressure-eviction.md`).
    - For each runbook, asserts the file contains a "Symptoms" section heading, a "Triage" or "Inspect" heading (whichever the actual files use — confirm during read_first), and a "Remediation" or "Remediate" heading. The test uses case-insensitive substring matching against `## ` headings to tolerate minor heading-text variations across the four files. If a runbook uses a different but semantically equivalent heading, prefer fixing the test to match the existing files over editing the runbook (this plan is verification, not rewrite).
    - For each of the four runbook files PLUS README.md, asserts NONE of these case-insensitive substrings appear: `Grafana`, `Grafana Cloud`, `PromQL`, `panel deep-link`, `${GRAFANA_URL}`.
    - For each of the four runbook files, asserts no triple-backtick `promql` fenced code block appears (regex matching `^` + three backticks + `promql` word boundary).
    - Asserts README.md contains a link or reference to each of the four runbook filenames (substring match on the four basenames).
    - Test is a single function in `package runbooks_test` so it lives in `docs/runbooks/runbooks_test.go` without polluting any production package.
  </behavior>
  <action>
    Create `docs/runbooks/runbooks_test.go` with the structure described in `<behavior>` above. Outline:

    - Package: `runbooks_test`
    - Imports: `os`, `regexp`, `strings`, `testing`
    - Top-level slice `runbookFiles` with the four basenames.
    - Top-level slice `forbiddenSubstrings` (lowercased): `"grafana"`, `"grafana cloud"`, `"promql"`, `"panel deep-link"`, `"${grafana_url}"`.
    - Top-level `regexp.MustCompile` for promql fenced block: pattern is `(?m)^` + backtick-backtick-backtick + `promql\b`.
    - `TestRunbookCompliance` iterates `runbookFiles` with `t.Run(name, ...)` sub-tests; each sub-test reads the file, lowercases for substring checks, asserts headings (`## symptoms`, one of `## triage` / `## inspect`, `## remediat`), then iterates `forbiddenSubstrings` and `promqlFence.MatchString`.
    - Final `t.Run("README_indexes_all_runbooks", ...)` sub-test reads README.md, asserts each of the four basenames appears in the text, and runs the same forbidden-string gate.

    During `read_first`, if any runbook actually uses heading text that doesn't match `## Symptoms`, `## Triage` / `## Inspect`, or `## Remediat...`, adjust the heading list in the test to match the existing files — DO NOT rewrite the runbooks. The researcher already certified content compliance; this plan only encodes the gate.

    Run `gofmt -w docs/runbooks/runbooks_test.go`, then `go vet ./docs/runbooks/...`, then `go test ./docs/runbooks/ -run TestRunbookCompliance -count=1 -v`. All sub-tests must pass.

    Commit as `test(54-02): add runbook compliance test (file existence, forbidden Grafana/PromQL strings)`.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena &amp;&amp; gofmt -l docs/runbooks/runbooks_test.go &amp;&amp; go vet ./docs/runbooks/... &amp;&amp; go test ./docs/runbooks/ -run TestRunbookCompliance -count=1 &amp;&amp; for f in docs/runbooks/ErrCircuitOpen.md docs/runbooks/deadline-timeouts.md docs/runbooks/ls-crash-restart.md docs/runbooks/memory-pressure-eviction.md docs/runbooks/README.md; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/runbooks/runbooks_test.go` exists.
    - `gofmt -l docs/runbooks/runbooks_test.go` prints nothing.
    - `go vet ./docs/runbooks/...` exits 0.
    - `go test ./docs/runbooks/ -run TestRunbookCompliance -count=1` exits 0 with all sub-tests green (one per runbook + the README sub-test = 5 sub-tests).
    - All four runbook files plus README.md still exist on disk (sanity check via the `for f in ... do test -f` loop).
    - The test file references all four runbook basenames and all five forbidden substrings (verified by `grep -c` on the test file: at least 4 runbook names, at least 5 forbidden tokens).
  </acceptance_criteria>
  <done>Runbook compliance is now a CI gate: any future edit that reintroduces Grafana/PromQL references or removes a runbook file fails `go test ./...`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc author edits → CI test | Untrusted edits enter via PRs; the test is the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07 | Tampering | Future PR reintroduces a Grafana/PromQL reference | mitigate | TestRunbookCompliance fails CI on any forbidden substring. |
| T-54-08 | Tampering | Future PR deletes a runbook | mitigate | Test fails on missing file at known path. |
| T-54-09 | Repudiation | Doc drift over time | mitigate | Test runs on every `go test ./...` invocation, including CI go-test.yml. |
</threat_model>

<verification>
- `go vet ./docs/runbooks/...` clean.
- `go test ./docs/runbooks/ -run TestRunbookCompliance -count=1` green.
- `go test ./...` green (no regression elsewhere; pure additive test file).
- All four runbook files still exist (no accidental deletion).
</verification>

<success_criteria>
- ROADMAP criterion #3 (runbooks present, no Grafana panel deep-links, no PromQL fences) is now machine-enforced in CI, not just verified once during research.
- OBS-02 is closed: written runbooks shipped in commit `780a3eda`, compliance gated by `TestRunbookCompliance`.
</success_criteria>

<output>
After completion, create `.planning/phases/54-obs-inbinary-page/54-02-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
