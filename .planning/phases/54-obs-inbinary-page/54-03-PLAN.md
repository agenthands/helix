---
phase: 54
plan: 03
type: execute
wave: 2
depends_on: [54-01, 54-02]
files_modified:
  - USAGE.md
  - .planning/REQUIREMENTS.md
  - docs/usage_test.go
autonomous: true
requirements: [OBS-01, OBS-02]
must_haves:
  truths:
    - "USAGE.md ## Observability Quickstart section gains a new ### In-Binary Metrics Page subsection pointing at http://127.0.0.1:9100/ and docs/runbooks/."
    - "The new paragraph mentions NO third-party software (Prometheus, Grafana, Docker, Podman) as a prerequisite — those words MAY appear elsewhere in USAGE.md as optional add-ons but not in the new paragraph."
    - "REQUIREMENTS.md OBS-01 description is updated from the stale 'JSON Grafana dashboards in deploy/grafana/' wording to reflect the in-binary HTML page approach landed in commit 82d39f87."
    - "A Go test guards the USAGE.md observability paragraph in CI so the doc cannot silently regress."
  artifacts:
    - path: "USAGE.md"
      provides: "### In-Binary Metrics Page subsection in ## Observability Quickstart"
      contains: "In-Binary Metrics Page"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated OBS-01 description matching the in-binary approach"
      contains: "in-binary"
    - path: "docs/usage_test.go"
      provides: "TestUSAGEObservability — asserts USAGE.md mentions http://127.0.0.1:9100/ and docs/runbooks/"
      contains: "func TestUSAGEObservability"
  key_links:
    - from: "USAGE.md ## Observability Quickstart"
      to: "internal/daemon/status_page.go (delivered by plan 01)"
      via: "URL http://127.0.0.1:9100/"
      pattern: "http://127\\.0\\.0\\.1:9100/"
    - from: "USAGE.md ## Observability Quickstart"
      to: "docs/runbooks/ (verified by plan 02)"
      via: "relative link"
      pattern: "docs/runbooks"
---

<objective>
Wire the user-facing documentation: USAGE.md gets a one-paragraph pointer to the in-binary metrics page (criterion #4), REQUIREMENTS.md OBS-01 description is corrected to match the post-2026-04-28 replan, and a Go test guards the USAGE.md paragraph in CI.

Purpose: Close ROADMAP criterion #4 ("USAGE.md Observability section gains one short paragraph pointing at http://127.0.0.1:9100/ and docs/runbooks/. No third-party software mentioned as a prerequisite") and reconcile REQUIREMENTS.md with the actual phase implementation. Depends on plans 01 + 02 so the doc points at working features, not vapor.

Output: USAGE.md updated with new subsection, REQUIREMENTS.md OBS-01 updated, and `docs/usage_test.go` ensuring the paragraph cannot regress.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-obs-inbinary-page/54-RESEARCH.md
@.planning/phases/54-obs-inbinary-page/54-VALIDATION.md
@CLAUDE.md
@USAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update USAGE.md with In-Binary Metrics Page subsection and update REQUIREMENTS.md OBS-01 description</name>
  <read_first>
    - USAGE.md (lines 600-700 — current ## Observability Quickstart structure: Enable Admin Listener at line 606+, Health Checks block ending around line 640, Prometheus Metrics block starting around line 642)
    - .planning/REQUIREMENTS.md (find the OBS-01 row — the stale text says "JSON Grafana dashboards in deploy/grafana/")
    - .planning/phases/54-obs-inbinary-page/54-RESEARCH.md (§ "USAGE.md Update Plan (Criterion #4)" — has the exact paragraph to insert)
    - .planning/ROADMAP.md (Phase 54 success criterion #4 — for the prerequisite-language constraint)
  </read_first>
  <files>USAGE.md, .planning/REQUIREMENTS.md</files>
  <action>
    **Step A — USAGE.md edit.** Locate `## Observability Quickstart` (around line 606). After the "Health Checks" subsection (which ends with the `/healthz` and `/readyz` curl examples around line 640) and BEFORE the "Prometheus Metrics" subsection (around line 642), insert a new subsection:

    ```markdown
    ### In-Binary Metrics Page

    Once the admin listener is enabled, point a browser at `http://127.0.0.1:9100/`
    to see a live, self-contained HTML page summarising tool RED metrics, lspool
    worker state, circuit breakers, recent eviction reasons, repomap cache
    hit-rate, and process RSS / goroutines. The page reads the same registry
    exposed at `/metrics` -- press F5 to refresh. No additional software is
    required to view it. For triage walkthroughs, see [`docs/runbooks/`](./docs/runbooks/).
    ```

    The wording is taken from `54-RESEARCH.md` § "USAGE.md Update Plan", with one substitution: replace "No Prometheus, Grafana, or Docker required." with "No additional software is required to view it." This satisfies criterion #4's "no third-party software is mentioned as a prerequisite" while still conveying the same point. The pre-existing "Prometheus Metrics" subsection that follows is clearly framed as an optional remote-monitoring path, so it remains untouched.

    **Step B — REQUIREMENTS.md edit.** Find the OBS-01 row. The stale description currently says (per researcher's note) something like "JSON Grafana dashboards in `deploy/grafana/`". Replace it with:

    > In-binary HTML metrics page on the admin listener (`http://127.0.0.1:9100/`) rendering RED metrics, lspool workers, circuit state, evictions, repomap cache hit-rate, and process info from the same `prometheus.Registry` exposed at `/metrics`. No third-party services required. (Replanned 2026-04-28; original Grafana scope reverted in commit `82d39f87`.)

    Keep the OBS-01 ID, status column, and any cross-link columns intact — only the description column changes. If the file uses a different row format, preserve it; only the description text is in scope.

    **Step C — verify the edits.** Run:

    ```bash
    grep -q '### In-Binary Metrics Page' USAGE.md
    grep -q 'http://127.0.0.1:9100/' USAGE.md
    grep -q 'docs/runbooks' USAGE.md
    grep -q -i 'in-binary\|admin listener' .planning/REQUIREMENTS.md
    ```

    The new USAGE.md paragraph (the body between `### In-Binary Metrics Page` and the next `###` heading) MUST NOT contain the words `Prometheus`, `Grafana`, `Docker`, or `Podman` — those words may appear elsewhere in the file as optional add-ons. The Go test in Task 2 will enforce this.

    Commit as `docs(54-03): point USAGE.md observability section at in-binary metrics page; reconcile OBS-01`.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena &amp;&amp; grep -q '### In-Binary Metrics Page' USAGE.md &amp;&amp; grep -q 'http://127.0.0.1:9100/' USAGE.md &amp;&amp; grep -q 'docs/runbooks' USAGE.md &amp;&amp; grep -i -q 'in-binary\|admin listener' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q 'JSON Grafana dashboards in `deploy/grafana/`' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '### In-Binary Metrics Page' USAGE.md` succeeds.
    - `grep -q 'http://127.0.0.1:9100/' USAGE.md` succeeds (the new paragraph references the admin URL).
    - `grep -q 'docs/runbooks' USAGE.md` succeeds (the new paragraph references the runbooks).
    - `.planning/REQUIREMENTS.md` OBS-01 row no longer contains the literal phrase `JSON Grafana dashboards in \`deploy/grafana/\``.
    - `.planning/REQUIREMENTS.md` OBS-01 row mentions the in-binary approach (case-insensitive grep).
  </acceptance_criteria>
  <done>USAGE.md and REQUIREMENTS.md reflect the actual delivered Phase 54 surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add USAGE.md observability regression test</name>
  <read_first>
    - USAGE.md (the new ### In-Binary Metrics Page subsection from Task 1)
    - docs/runbooks/runbooks_test.go (from plan 02 — package layout pattern to mirror)
    - .planning/phases/54-obs-inbinary-page/54-VALIDATION.md (row 54-03-01)
  </read_first>
  <files>docs/usage_test.go</files>
  <behavior>
    - TestUSAGEObservability reads `../USAGE.md` (test runs with cwd=`docs/`; USAGE.md lives at the repo root).
    - Asserts the file content contains all of these literal substrings: `### In-Binary Metrics Page`, `http://127.0.0.1:9100/`, `docs/runbooks`.
    - Extracts the body of the `### In-Binary Metrics Page` subsection (text between that heading and the next `###` or `##` heading) using a regexp, and asserts that body contains NONE of the case-insensitive substrings: `prometheus`, `grafana`, `docker`, `podman`. The rest of USAGE.md may freely mention them.
    - Test is a single Go test in `package docs_test` so it lives in `docs/usage_test.go` with no other Go files needed alongside it.
  </behavior>
  <action>
    Create `docs/usage_test.go`:

    ```go
    package docs_test

    import (
        "os"
        "regexp"
        "strings"
        "testing"
    )

    func TestUSAGEObservability(t *testing.T) {
        data, err := os.ReadFile("../USAGE.md")
        if err != nil {
            t.Fatalf("read USAGE.md: %v", err)
        }
        text := string(data)

        for _, want := range []string{
            "### In-Binary Metrics Page",
            "http://127.0.0.1:9100/",
            "docs/runbooks",
        } {
            if !strings.Contains(text, want) {
                t.Errorf("USAGE.md missing required substring %q", want)
            }
        }

        // Extract the In-Binary Metrics Page subsection body (between its heading
        // and the next ### or ## heading, or end-of-file).
        re := regexp.MustCompile("(?ms)^### In-Binary Metrics Page\\s*\\n(.*?)(?:^### |^## |\\z)")
        match := re.FindStringSubmatch(text)
        if len(match) < 2 {
            t.Fatalf("could not isolate ### In-Binary Metrics Page body in USAGE.md")
        }
        body := strings.ToLower(match[1])
        for _, bad := range []string{"prometheus", "grafana", "docker", "podman"} {
            if strings.Contains(body, bad) {
                t.Errorf("In-Binary Metrics Page paragraph mentions third-party software %q (criterion #4 forbids prerequisites)", bad)
            }
        }
    }
    ```

    Run `gofmt -w docs/usage_test.go`, `go vet ./docs/...`, `go test ./docs/... -run TestUSAGEObservability -count=1`. Test must pass.

    Commit as `test(54-03): guard USAGE.md observability paragraph against regression`.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/postfix/serena &amp;&amp; gofmt -l docs/usage_test.go &amp;&amp; go vet ./docs/... &amp;&amp; go test ./docs/... -run TestUSAGEObservability -count=1 &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/usage_test.go` exists.
    - `gofmt -l docs/usage_test.go` prints nothing.
    - `go vet ./docs/...` exits 0.
    - `go test ./docs/... -run TestUSAGEObservability -count=1` exits 0.
    - Full suite `go test ./...` exits 0 (no regression from Task 1's USAGE.md / REQUIREMENTS.md edits or this test addition).
    - The test file references all three required substrings (`### In-Binary Metrics Page`, `http://127.0.0.1:9100/`, `docs/runbooks`) and all four forbidden substrings (`prometheus`, `grafana`, `docker`, `podman`).
  </acceptance_criteria>
  <done>USAGE.md observability paragraph is now machine-checked in CI; criterion #4 closed and gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc author edits → CI test | Future PRs may reintroduce Prometheus/Grafana prerequisites in the canonical paragraph; the test is the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-10 | Tampering | Future PR reintroduces "requires Grafana" wording in the In-Binary Metrics Page paragraph | mitigate | TestUSAGEObservability fails CI when the paragraph body contains `prometheus`/`grafana`/`docker`/`podman`. |
| T-54-11 | Tampering | Future PR removes the http://127.0.0.1:9100/ pointer | mitigate | Test asserts the literal URL substring exists. |
| T-54-12 | Information Disclosure | Stale REQUIREMENTS.md drives reviewers to wrong implementation | mitigate | OBS-01 description rewritten to match the actual phase artifact. |
</threat_model>

<verification>
- `gofmt -l docs/usage_test.go` empty.
- `go vet ./...` clean.
- `go test ./...` green (full suite, including plans 01 + 02 tests).
- USAGE.md, REQUIREMENTS.md, and docs/usage_test.go all committed.
</verification>

<success_criteria>
- ROADMAP criterion #4 satisfied: USAGE.md Observability section now points at `http://127.0.0.1:9100/` and `docs/runbooks/` with no third-party software prerequisite, and the constraint is machine-checked.
- REQUIREMENTS.md OBS-01 reconciled with the post-2026-04-28 replan.
- All five ROADMAP success criteria are now closed across plans 01 (#1, #2, #5), 02 (#3), and 03 (#4 + reconciliation).
</success_criteria>

<output>
After completion, create `.planning/phases/54-obs-inbinary-page/54-03-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
