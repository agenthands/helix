---
phase: 55-obs-trace-coverage-audit
plan: 02
type: execute
wave: 2
depends_on:
  - "55-01"
files_modified:
  - internal/mcp/coverage_test.go
  - internal/obs/attribute_allowlist_test.go
autonomous: true
requirements:
  - OBS-04
tags:
  - observability
  - tracing
  - opentelemetry
  - testing
  - audit

must_haves:
  truths:
    - "An automated test fails if any registered MCP tool name (kernel OR skill) does not produce both the parent 'daemon.mcp.tools.call' span AND a corresponding child span (`kernel.tool.*` or `skill.tool.*`) when invoked."
    - "An automated test fails if any captured span carries an attribute key not in the closed allowlist (parent: tool_name, profile, mode, language, outcome; ls.request: lsp.method, lsp.language, lsp.duration_ms; kernel.tool.*/skill.tool.*: empty)."
    - "Both tests run in CI as part of `go test ./internal/mcp/... ./internal/obs/...` and execute in under 60 seconds."
  artifacts:
    - path: "internal/mcp/coverage_test.go"
      provides: "Registry-driven test asserting every tool from ToolRegistry.Names() emits parent + child spans"
      contains: "TestEveryRegisteredToolEmitsSpans"
    - path: "internal/obs/attribute_allowlist_test.go"
      provides: "Closed allowlist of span attribute keys; mirror of metrics_labels_test.go"
      contains: "TestSpanAttributeAllowlist"
  key_links:
    - from: "internal/mcp/coverage_test.go"
      to: "internal/mcp/registry.go"
      via: "uses Registry().Names() to enumerate every registered tool"
      pattern: "Registry\\(\\)\\.Names\\(\\)"
    - from: "internal/obs/attribute_allowlist_test.go"
      to: "internal/obs/metrics_labels_test.go"
      via: "mirrors the metric-label allowlist pattern (Phase 11 / 53)"
      pattern: "allowed.*Attrs|allowedSpanAttrs"
---

<objective>
Add the two CI-enforced gates that keep Phase 55's coverage from regressing: a registry-driven span coverage audit and a closed attribute-key allowlist.

Purpose: 55-RESEARCH.md identifies the third gap as the absence of an automated audit. Today, span coverage is "the developer remembered to wrap it." OBS-04 success criterion #1 mandates an automated audit; criterion #2 mandates attribute hygiene certification. Mechanizing both as Go tests turns "audit" into a CI gate that fails when a new tool is added without span wiring or when a new attribute escapes the allowlist.

Output:
- `internal/mcp/coverage_test.go`: drives every tool from `mcp.ToolRegistry.Names()` through the daemon's MCP middleware against `tracetest.InMemoryExporter`; asserts each tool produces a parent `daemon.mcp.tools.call` span AND a child span whose name starts with `kernel.tool.` or `skill.tool.`.
- `internal/obs/attribute_allowlist_test.go`: mirror of `internal/obs/metrics_labels_test.go` (Phase 11 / 53). Closed allowlist of span attribute keys; the test fails on any unlisted key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-obs-trace-coverage-audit/55-RESEARCH.md
@.planning/phases/55-obs-trace-coverage-audit/55-01-SUMMARY.md

@internal/mcp/registry.go
@internal/mcp/server.go
@internal/mcp/middleware.go
@internal/mcp/telemetry_span_test.go
@internal/kernel/spanwrap.go
@internal/kernel/spanwrap_test.go
@internal/obs/metrics_labels_test.go
@internal/obs/obs.go
@internal/daemon/daemon.go

<interfaces>
From `internal/mcp/registry.go:51`:
```go
func (r *ToolRegistry) Names() []string  // returns every registered tool name (kernel + skill + diagnostic)
```

From `internal/mcp/telemetry_span_test.go:24-29` (canonical exporter setup):
```go
exp := tracetest.NewInMemoryExporter()
tp := sdktrace.NewTracerProvider(
    sdktrace.WithSyncer(exp),
    sdktrace.WithSampler(sdktrace.AlwaysSample()),
)
provider := obs.NewForTest(tp)
```

From `internal/obs/metrics_labels_test.go` (the pattern to mirror — read this file first; the structure is the model):
- It declares an `allowedLabels map[string]map[string]struct{}` keyed by metric name.
- It walks every metric in the registry and asserts each label key is in the allowlist for that metric.
- Failing the test produces a clear "metric X has unexpected label Y" error.

From `tracetest.InMemoryExporter` API:
- `exp.GetSpans()` returns `[]tracetest.SpanStub` after `provider.ForceFlush()` (or with sync syncer, immediately).
- Each `SpanStub` has `.Name`, `.Attributes`, `.Parent.SpanID`, `.SpanContext.SpanID`, `.Status`, `.Events`.

Span allowlist (from 55-RESEARCH Pattern 2):
```go
var allowedSpanAttrs = map[string]map[string]struct{}{
    "daemon.mcp.tools.call": {
        "tool_name": {}, "profile": {}, "mode": {}, "language": {}, "outcome": {},
    },
    // Wildcards for the per-tool child spans — D-07 forbids attributes here:
    "kernel.tool.*":  {}, // empty set; any attr fails
    "skill.tool.*":   {}, // empty set; any attr fails
    "ls.request": {
        "lsp.method": {}, "lsp.language": {}, "lsp.duration_ms": {},
    },
}
```
The wildcard keys (`kernel.tool.*`, `skill.tool.*`) are matched by prefix in the test loop (the map key has the trailing `.*` stripped before prefix-comparison).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry-driven coverage audit (TestEveryRegisteredToolEmitsSpans)</name>
  <files>internal/mcp/coverage_test.go</files>
  <read_first>
    - internal/mcp/registry.go (Names() method at line ~51 + Register call sites)
    - internal/mcp/server.go (NewSerenaMCPServer constructor; AddSkillTool flow; how tools are invoked through the SDK in tests)
    - internal/mcp/telemetry_span_test.go (full file — copy the exporter setup, the in-process tool invocation pattern, and the in-memory daemon bootstrap helper if one exists)
    - internal/daemon/daemon.go (look for a test-only bootstrap helper; otherwise the test must build a minimal SerenaMCPServer with a stub kernel + at least one fake skill executor that registers ≥ 1 skill tool)
    - internal/kernel/spanwrap_test.go (reference for how kernel tools are exercised in unit tests)
    - 55-01-SUMMARY.md (constructor signature changes from plan 55-01 — needed to wire the tracer)
  </read_first>
  <behavior>
    - Test 1 (TestEveryRegisteredToolEmitsSpans): Build a SerenaMCPServer with at least one tool from EACH registration path: (a) one kernel tool wrapped by `WrapToolSpan`, (b) one skill tool registered via `AddSkillTool`. Use the AlwaysSample tracer + InMemoryExporter setup from telemetry_span_test.go. For each name returned by `server.Registry().Names()` that has an executable handler:
      1. Reset the exporter.
      2. Invoke the tool through the MCP SDK with empty/minimal args (handler errors are tolerated — we only assert on span shape, not on tool result).
      3. Capture spans via `exp.GetSpans()`.
      4. Assert at least one span is named `"daemon.mcp.tools.call"` (parent).
      5. Assert at least one span name has prefix `"kernel.tool."` OR `"skill.tool."` (child).
      6. Assert the child span's Parent.SpanID matches the parent span's SpanContext.SpanID.
      Failure message must include the tool name (e.g., `tool %q missing child span`) so a regression points at the offender.
    - Test 2 (TestRegistryNamesNonEmpty): Sanity guard — the test bootstrap MUST register ≥ 2 tools (one kernel, one skill); fails the test if zero tools are registered, preventing a silently-passing audit.
  </behavior>
  <action>
    1. Read `internal/mcp/telemetry_span_test.go` end-to-end to understand the existing in-test SDK invocation pattern. If it includes a `bootstrapTestServer` or similar helper, REUSE it; if not, create one in this new file. Do not duplicate setup logic if the existing test file already has it — extract to a shared `*_test.go` helper if needed.

    2. Create `internal/mcp/coverage_test.go` with package `mcp`. Imports needed:
       ```go
       import (
           "context"
           "strings"
           "testing"
           "github.com/stretchr/testify/assert"
           "github.com/stretchr/testify/require"
           sdktrace "go.opentelemetry.io/otel/sdk/trace"
           "go.opentelemetry.io/otel/sdk/trace/tracetest"
           "github.com/postfix/serena/internal/obs"
       )
       ```
       (Use the exact module path from `go.mod`; if the prefix differs, match it.)

    3. Build a minimal test bootstrap that:
       - Creates an InMemoryExporter + AlwaysSample TracerProvider + `obs.NewForTest(tp)`.
       - Constructs a SerenaMCPServer with the `obs.Provider.Tracer("serena.mcp")` injected (per 55-01 changes).
       - Installs the TelemetryMiddleware (so the parent `daemon.mcp.tools.call` span is emitted).
       - Registers AT LEAST: (a) one kernel tool via `WrapToolSpan` whose handler returns a fixed string, (b) one skill tool via `AddSkillTool` backed by a stub `SkillToolExecutor` that returns a fixed string.

    4. Implement `TestEveryRegisteredToolEmitsSpans` per `<behavior>` Test 1. Use a subtest per tool (`t.Run(name, func(t *testing.T) { ... })`) so failures are scoped per tool.

    5. Implement `TestRegistryNamesNonEmpty` per `<behavior>` Test 2 — a one-liner guarding against the bootstrap silently registering zero tools (which would make Test 1 vacuously pass).

    6. Tools that have NO executable handler in the test bootstrap (e.g., catalog-only registrations like `activate_project`, `ping`, `echo`) MUST be skipped via a known-skip list documented inline with the rationale: "skipped because catalog-only — exercised by integration tests." This skip list is the ONLY allowed exclusion; adding a new tool without instrumentation must NOT silently land here. Document each skipped name with a one-line reason. Keep the skip list small (≤ 5 entries); if it grows, file a follow-up.

    7. Run `go vet ./internal/mcp/...` and `go test ./internal/mcp/... -run "TestEveryRegisteredToolEmitsSpans|TestRegistryNamesNonEmpty" -v`. Both MUST pass.

    8. Manually break the audit to prove the gate works: temporarily comment out the `WrapToolSpan` wrapping in the test bootstrap's kernel tool registration; re-run the test; confirm it fails with a clear message naming the unwrapped tool. Restore the wrapping. (Document in SUMMARY.md.)
  </action>
  <verify>
    <automated>go vet ./internal/mcp/... &amp;&amp; go test ./internal/mcp/... -run "TestEveryRegisteredToolEmitsSpans|TestRegistryNamesNonEmpty" -v -timeout 60s</automated>
  </verify>
  <acceptance_criteria>
    - Test passes with the production server bootstrap (every executable tool produces both spans).
    - Test FAILS if the kernel-tool wrapping is removed (manually verified during execution; documented in SUMMARY.md).
    - Test FAILS if `AddSkillTool` no longer wraps (verified by temporarily reverting the 55-01 change in a local sandbox; documented in SUMMARY.md).
    - Skip-list contains only catalog-only tools, each with a one-line rationale comment.
    - Test runs in under 60 seconds (timeout enforced).
  </acceptance_criteria>
  <done>
    Coverage regression gate is live. Adding a new tool without span instrumentation will fail CI with a message naming the offending tool.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Span attribute allowlist (TestSpanAttributeAllowlist)</name>
  <files>internal/obs/attribute_allowlist_test.go</files>
  <read_first>
    - internal/obs/metrics_labels_test.go (full file — this is the pattern to mirror; copy its structure)
    - internal/mcp/middleware.go (find every `span.SetAttributes` call to know the full set of parent-span attribute keys actually emitted in production: `tool_name`, `profile`, `mode`, `language`, `outcome`)
    - internal/kernel/lspool/worker.go (after 55-01 changes — the `ls.request` child span attributes: `lsp.method`, `lsp.language`, `lsp.duration_ms`)
    - internal/kernel/spanwrap.go (confirm kernel.tool.* spans emit zero attributes per D-07)
    - 55-01-SUMMARY.md (final attribute set after Wave 1)
  </read_first>
  <behavior>
    - Test 1 (TestSpanAttributeAllowlist): Drive a representative production-shaped span flow through an InMemoryExporter — at minimum: one parent `daemon.mcp.tools.call` invocation that triggers a child `kernel.tool.*` span AND a child `skill.tool.*` span AND a leaf `ls.request` span (use a stub LS Conn or invoke `Worker.Request` directly with a fake Conn). Then walk every captured span and assert every attribute key is in the allowlist for that span's name (with prefix-matching for `kernel.tool.*` and `skill.tool.*`). Test FAILS with `span %q has disallowed attribute %q` if any unlisted key appears.
    - Test 2 (TestSpanAllowlistIsExhaustive): Static check — for each entry in the allowlist, ensure the value set is non-nil (the empty `kernel.tool.*` / `skill.tool.*` entries are intentional sentinels, not nil maps). Guards against typos that would skip enforcement for that span name.
  </behavior>
  <action>
    1. Open `internal/obs/metrics_labels_test.go` and read it end-to-end. Note (a) how the allowlist map is structured, (b) how the test iterates over the registry, (c) the failure-message format. Mirror this exactly.

    2. Create `internal/obs/attribute_allowlist_test.go` with package `obs`. Imports needed:
       ```go
       import (
           "context"
           "strings"
           "testing"
           "github.com/stretchr/testify/assert"
           "github.com/stretchr/testify/require"
           sdktrace "go.opentelemetry.io/otel/sdk/trace"
           "go.opentelemetry.io/otel/sdk/trace/tracetest"
       )
       ```

    3. Declare the closed allowlist exactly as in `<interfaces>`:
       ```go
       var allowedSpanAttrs = map[string]map[string]struct{}{
           "daemon.mcp.tools.call": {
               "tool_name": {}, "profile": {}, "mode": {}, "language": {}, "outcome": {},
           },
           "kernel.tool.*": {},
           "skill.tool.*":  {},
           "ls.request": {
               "lsp.method": {}, "lsp.language": {}, "lsp.duration_ms": {},
           },
       }
       ```

    4. Implement `lookupAllowed(spanName string) (map[string]struct{}, bool)` helper that:
       - First tries an exact match on `allowedSpanAttrs[spanName]`.
       - Then tries every key ending in `.*`: strips the `.*` and checks whether `spanName` has that prefix.
       - Returns the allowed-set + true on hit, nil + false on miss (which itself is a test failure: "unknown span name").

    5. Implement `TestSpanAttributeAllowlist`:
       - Build an InMemoryExporter + AlwaysSample TracerProvider.
       - Drive a representative flow that triggers all four span shapes. Cross-package import is fine (this test lives in `internal/obs` but may import `internal/mcp` and `internal/kernel/lspool` for the drivers — verify no import cycle; if a cycle exists, move the test to `internal/obs/integration_test.go` with build-tag `integration`, OR put it in `internal/mcp` package alongside the coverage test).
       - For each captured span: call `lookupAllowed(span.Name)`. If miss, fail with `unknown span %q (add to allowlist or remove emission)`. If hit, walk `span.Attributes` and assert each key is in the allowed set.

    6. Implement `TestSpanAllowlistIsExhaustive` per `<behavior>` Test 2.

    7. **Import cycle check:** Before finalizing, run `go build ./internal/obs/...`. If the test imports `internal/mcp` AND `internal/mcp` imports `internal/obs`, you have a cycle. Resolution: place the integration-style test in `internal/mcp` package (call it `attribute_allowlist_integration_test.go`) and keep only `TestSpanAllowlistIsExhaustive` in `internal/obs/attribute_allowlist_test.go`. Document the placement decision in SUMMARY.md.

    8. Run `go vet ./internal/obs/... ./internal/mcp/...` and `go test ./internal/obs/... ./internal/mcp/... -run "TestSpanAttributeAllowlist|TestSpanAllowlistIsExhaustive" -v`. Both MUST pass.

    9. Prove the gate works: temporarily add `span.SetAttributes(attribute.String("workspace_path", "/tmp/foo"))` to the parent middleware span; re-run the test; confirm it fails with `span "daemon.mcp.tools.call" has disallowed attribute "workspace_path"`. Revert. (Document in SUMMARY.md.)
  </action>
  <verify>
    <automated>go vet ./internal/obs/... ./internal/mcp/... &amp;&amp; go test ./internal/obs/... ./internal/mcp/... -run "TestSpanAttributeAllowlist|TestSpanAllowlistIsExhaustive" -v -timeout 60s</automated>
  </verify>
  <acceptance_criteria>
    - Test passes against the post-55-01 production span set.
    - Manually verified: adding `workspace_path` (or any other unlisted key) makes the test fail with a clear, attributed error message (documented in SUMMARY.md).
    - Manually verified: introducing a typo'd allowlist entry triggers `TestSpanAllowlistIsExhaustive`.
    - No import cycles between `internal/obs` and `internal/mcp` (resolution path documented if test moved).
    - Test runs in under 60 seconds.
  </acceptance_criteria>
  <done>
    Attribute hygiene is now CI-enforced. Any future code that adds an off-list span attribute key (PII, file path, unbounded ID) breaks the build. The closed set documented here is the input to TRACE-AUDIT.md (plan 55-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production span emitters | Tests drive the same code paths CI uses; they don't read external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-05 | Information Disclosure (regression) | Future code adds PII span attribute | mitigate | `TestSpanAttributeAllowlist` fails CI on any unlisted attribute key. The allowlist is the SINGLE point of attribute review for this codebase. |
| T-55-06 | Tampering (silent allowlist bypass) | Future change widens allowlist without review | accept | Allowlist lives in a test file under `internal/obs/`; widening it requires a code review + commit. We accept that PR review is the gate (no automated alert on allowlist diffs). |
| T-55-07 | Denial of Service (cardinality regression) | Future code adds unbounded attribute value | accept | Attribute-key allowlist does NOT enforce value cardinality. Mitigation: TRACE-AUDIT.md (plan 55-03) certifies the value space of each allowlisted attribute; PR review enforces. |
| T-55-08 | Coverage gap regression | Future tool added without span wrapping | mitigate | `TestEveryRegisteredToolEmitsSpans` fails CI with the offending tool name. |
</threat_model>

<verification>
- `go vet ./...` clean.
- `go test ./internal/mcp/... ./internal/obs/...` green, including the four new tests.
- Manual gate-validation steps documented in SUMMARY.md (each new test was proven to actually fail when its invariant is broken).
</verification>

<success_criteria>
1. Adding a new MCP tool without span instrumentation fails `TestEveryRegisteredToolEmitsSpans` with the tool name in the error.
2. Adding any new span attribute (kernel or skill) fails `TestSpanAttributeAllowlist` with the attribute name in the error.
3. Both tests run in under 60 seconds (Nyquist budget).
4. No import cycles introduced.
5. The closed attribute set is now the authoritative input to plan 55-03's TRACE-AUDIT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/55-obs-trace-coverage-audit/55-02-SUMMARY.md` documenting:
- Final allowlist contents (copy of the map verbatim).
- Final skip-list of catalog-only tools (with rationale per entry).
- Manual gate-validation evidence: the exact failure messages produced when each test's invariant was deliberately broken (and then reverted).
- Test placement decision (single package vs. split) if an import cycle was hit.
- Confirmation: `go test ./internal/mcp/... ./internal/obs/...` green.
</output>
