---
phase: 55-obs-trace-coverage-audit
plan: 03
type: execute
wave: 3
depends_on:
  - "55-01"
  - "55-02"
files_modified:
  - .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md
  - .planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md
  - USAGE.md
  - usage_test.go
autonomous: false
requirements:
  - OBS-04
tags:
  - observability
  - tracing
  - documentation
  - usage
  - audit
  - human-uat

must_haves:
  truths:
    - "TRACE-AUDIT.md exists and lists every span name + every attribute key/value-shape, certifying no PII and no unbounded cardinality."
    - "USAGE.md Observability section documents tracing_sample_ratio (head-only in-binary; tail requires otel-collector) and how to set it via ObservabilityConfig + the smoke-test recommendation of 1.0."
    - "55-HUMAN-UAT.md captures a smoke-trace verification ticket against a live OTLP collector showing the full request path with no orphan spans."
  artifacts:
    - path: ".planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md"
      provides: "Review artifact certifying every span attribute (success criterion #2)"
      contains: "ls.request"
      min_lines: 60
    - path: "USAGE.md"
      provides: "Sampling configuration documentation (success criterion #3)"
      contains: "tracing_sample_ratio"
    - path: ".planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md"
      provides: "Smoke-trace verification ticket against live OTLP collector (success criterion #4)"
      contains: "otel/opentelemetry-collector-contrib"
    - path: "usage_test.go"
      provides: "Regression gate that USAGE.md continues to document the sampling section"
      contains: "TestUsageDocumentsTracingSampling"
  key_links:
    - from: ".planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md"
      to: "internal/obs/attribute_allowlist_test.go"
      via: "TRACE-AUDIT.md is the human-readable certification of the same closed allowlist that the test enforces mechanically"
      pattern: "allowedSpanAttrs|attribute_allowlist"
    - from: "USAGE.md"
      to: "internal/config/config.go"
      via: "Doc references ObservabilityConfig.SampleRatio (tracing_sample_ratio YAML key)"
      pattern: "tracing_sample_ratio"
    - from: "55-HUMAN-UAT.md"
      to: "USAGE.md"
      via: "UAT steps reference the USAGE.md sampling section for sample_ratio guidance"
      pattern: "sample_ratio: 1\\.0|tracing_sample_ratio: 1\\.0"
---

<objective>
Author the human-facing deliverables that close out OBS-04 success criteria #2, #3, and #4: the TRACE-AUDIT review artifact, the USAGE.md sampling documentation, and the live-OTLP-collector smoke-trace HUMAN-UAT ticket.

Purpose: Plans 55-01 and 55-02 closed the code gaps and added mechanical gates. This plan turns the closed attribute set into a publishable certification (so an operator or auditor can read one document instead of grepping tests), expands USAGE.md so operators know how to enable tracing for the smoke test, and creates the HUMAN-UAT ticket that captures the criterion-#4 evidence (which can only be verified end-to-end with a live collector).

Output:
- `.planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` — every span name × every attribute, certifying value bounding (enum / hashed / bucketed / closed-set), explicit "no PII / no unbounded cardinality" statement, and the "no client trace-context propagation" property.
- `USAGE.md` — expanded Observability section covering `tracing_sample_ratio` (default + smoke-test recommendation), head-vs-tail sampling honesty, ObservabilityConfig YAML examples, and the dev-only `otel/opentelemetry-collector-contrib` smoke-collector recipe.
- `usage_test.go` — extend with `TestUsageDocumentsTracingSampling` (mirror of the Phase 54 `TestUsageDocumentsObservabilityPage` pattern).
- `.planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md` — Phase 54-shaped UAT ticket with the docker collector recipe, env-var setup, sample tool invocation, and trace-tree expectations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-obs-trace-coverage-audit/55-RESEARCH.md
@.planning/phases/55-obs-trace-coverage-audit/55-01-SUMMARY.md
@.planning/phases/55-obs-trace-coverage-audit/55-02-SUMMARY.md
@.planning/phases/54-obs-inbinary-page/54-HUMAN-UAT.md

@USAGE.md
@usage_test.go
@internal/config/config.go
@internal/obs/tracing.go
@internal/obs/obs.go

<verified_assumptions>
A2 (ShutdownTracing wired in daemon shutdown): VERIFIED during planning — `internal/daemon/shutdown.go:48` calls `d.obs.ShutdownTracing(flushCtx)`. UAT can rely on a clean shutdown to flush the BatchSpanProcessor.

A1 (debug vs logging exporter): The current otel-collector-contrib uses `debug` (renamed from `logging` in v0.86, 2023-09). Use `debug` in the smoke-collector YAML; if `debug` is unavailable in the operator's collector image, `logging` works as a fallback. Document both.
</verified_assumptions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author TRACE-AUDIT.md (success criterion #2)</name>
  <files>.planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md</files>
  <read_first>
    - 55-RESEARCH.md (Architectural Responsibility Map, Pattern 2 allowlist, Security Domain section)
    - 55-01-SUMMARY.md (final span shapes after Wave 1)
    - 55-02-SUMMARY.md (final allowlist verbatim)
    - internal/obs/attribute_allowlist_test.go (the mechanical source of truth — the doc must match this map exactly)
    - internal/mcp/middleware.go (parent span attributes — confirm the production code matches the allowlist)
  </read_first>
  <action>
    Create `.planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` with the following sections (use the exact section headings):

    1. `# Trace Attribute Audit (OBS-04)` with frontmatter:
       ```yaml
       ---
       phase: 55-obs-trace-coverage-audit
       reviewed: <today's date>
       reviewer: <human reviewer or "automated by 55-02 allowlist test">
       ---
       ```

    2. `## Span Inventory` — table with columns: Span Name | Origin (file:line) | Parent | Lifetime | Notes. Cover EVERY span name produced by the daemon: `daemon.mcp.tools.call`, `kernel.tool.*` (one row, list which packages emit it), `skill.tool.*` (one row), `ls.request`. Confirm via grep `grep -rn 'tracer\.Start' internal/` that no other span name exists outside this list.

    3. `## Attribute Certification` — one subsection per span name. For each:
       - Table with columns: Attribute Key | Type | Value Shape (enum / int / bounded string / hashed / bucketed) | Cardinality Bound | PII Risk | Disposition.
       - For `daemon.mcp.tools.call`:
         - `tool_name`: enum, ≤ 50 (registry), no PII, ALLOW
         - `profile`: enum {claude-code, codex, ide-assistant, ci-bot, full}, no PII, ALLOW
         - `mode`: enum {read, edit, review, admin}, no PII, ALLOW
         - `language`: enum, ≤ 52 (langregistry), no PII, ALLOW
         - `outcome`: enum {success, error_user, error_internal, timeout, circuit_open, ...} — list the actual values from `internal/mcp/middleware.go`, no PII, ALLOW
       - For `kernel.tool.*` and `skill.tool.*`: state explicitly "ZERO attributes per Phase 12 D-07; the parent span carries them. Allowlist test enforces emptiness."
       - For `ls.request`:
         - `lsp.method`: enum from LSP 3.17 spec, ≤ ~30 in our usage (textDocument/hover, textDocument/definition, etc.), no PII, ALLOW
         - `lsp.language`: enum, ≤ 52 (langregistry), no PII, ALLOW
         - `lsp.duration_ms`: int64, bounded by deadline, no PII, ALLOW

    4. `## Anti-list (deliberately excluded)` — bulleted list of attributes that MUST NEVER be added without a follow-up audit:
       - `workspace_path` / `repo_root` (PII + unbounded cardinality)
       - `file_path` / `file_uri` (PII + unbounded)
       - `symbol_name` (sometimes PII; unbounded)
       - `user_id` / `user_email` / `session_id` (PII + per-user unbounded)
       - `request_id` / `trace_id` (use OTel-native trace ID, do NOT duplicate as attribute)
       - Free-form `error` strings on attribute keys (use `span.RecordError(err)` instead — already standard in middleware.go:246-247)

    5. `## Cardinality & PII Certification` — explicit signed-off statement:
       > "As of <date>, this audit certifies that the four span names above and their attribute sets contain no personally-identifiable information and have a total cardinality bounded by the product of fixed enums (≤ 50 tools × 5 profiles × 4 modes × 52 languages × 7 outcomes ≈ 364,000 unique parent span fingerprints in the worst case; LS child fingerprints ≤ 30 methods × 52 languages = 1,560). The mechanical enforcement is `TestSpanAttributeAllowlist` (`internal/obs/attribute_allowlist_test.go`)."

    6. `## Trace Context Propagation Policy` — one paragraph stating: "Serena does NOT honor inbound trace context from MCP clients. All Serena spans are root spans within the daemon. This is a deliberate property pending V2/V3 (auth) — propagating client-supplied trace IDs would let a malicious client correlate spans across operators' collectors. Re-evaluate when authenticated MCP transport lands."

    7. `## Mechanical Enforcement References` — bullet list:
       - Coverage gate: `internal/mcp/coverage_test.go::TestEveryRegisteredToolEmitsSpans` (plan 55-02)
       - Attribute allowlist gate: `internal/obs/attribute_allowlist_test.go::TestSpanAttributeAllowlist` (plan 55-02)
       - Source of truth for sampler: `internal/obs/tracing.go:62` (ParentBased(TraceIDRatioBased(ratio)))
       - Source of truth for shutdown flush: `internal/daemon/shutdown.go:48`

    8. `## Update Procedure` — instruction for future contributors: "If you add a span attribute, you MUST: (a) update `allowedSpanAttrs` in `internal/obs/attribute_allowlist_test.go`, (b) add a row to the appropriate table here in TRACE-AUDIT.md including PII/cardinality justification, (c) get a code-review sign-off. The test will fail CI without (a); this doc keeps the human review trail."

    Verify the file:
    - `wc -l TRACE-AUDIT.md` ≥ 60.
    - `grep -c 'ls.request\\|kernel.tool\\|skill.tool\\|daemon.mcp.tools.call' TRACE-AUDIT.md` ≥ 4.
    - All four span names appear.
    - Cross-check: every key in `allowedSpanAttrs` (from 55-02) appears in this doc; every key in this doc appears in `allowedSpanAttrs`. Note any drift inline.
  </action>
  <verify>
    <automated>test -f .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md &amp;&amp; test "$(wc -l &lt; .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md)" -ge 60 &amp;&amp; grep -q "daemon.mcp.tools.call" .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md &amp;&amp; grep -q "ls.request" .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md &amp;&amp; grep -q "kernel.tool" .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md &amp;&amp; grep -q "skill.tool" .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md &amp;&amp; grep -q "Anti-list" .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md &amp;&amp; grep -q "Trace Context Propagation" .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists, ≥ 60 lines, all four span names documented.
    - Every attribute in `allowedSpanAttrs` (from `internal/obs/attribute_allowlist_test.go`) has a row in this doc.
    - Anti-list section explicitly enumerates `workspace_path`, `file_path`, `symbol_name`, `user_id`, `request_id`, free-form error strings.
    - Cardinality math is shown (worst-case product of enums).
    - Trace-context-propagation policy paragraph is present.
    - Mechanical-enforcement references list both the coverage and attribute tests by file path.
  </acceptance_criteria>
  <done>
    OBS-04 success criterion #2 satisfied — a single human-readable artifact certifies the entire span attribute surface and is mechanically backed by the allowlist test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand USAGE.md sampling section + regression test (success criterion #3)</name>
  <files>USAGE.md, usage_test.go</files>
  <read_first>
    - USAGE.md (current Observability section near lines 336-344 and 772-783 per 55-RESEARCH source list — search for "tracing" / "Observability" / "OTLP")
    - usage_test.go (root-level — find existing assertions to mirror; the Phase 54 commit added `TestUsageDocumentsObservabilityPage` or similar; mirror its style)
    - internal/config/config.go (lines 37-57 — ObservabilityConfig YAML keys: confirm whether the field is `tracing_sample_ratio` or another name)
    - internal/obs/tracing.go (sampler construction — confirm ParentBased + TraceIDRatioBased)
    - 55-RESEARCH.md Pitfall 3 (sampling default = 0.0 silently drops smoke trace)
    - 55-RESEARCH.md Pattern: head-vs-tail honesty
  </read_first>
  <behavior>
    - Test (TestUsageDocumentsTracingSampling): USAGE.md MUST contain (a) the literal string `tracing_sample_ratio`, (b) the recommendation `1.0` for smoke testing, (c) a head-vs-tail-sampling explanation (look for keywords "head" AND "tail" within the same section), (d) reference to `ObservabilityConfig`, (e) the smoke-collector docker recipe (literal substring `otel/opentelemetry-collector-contrib`). Test fails with a clear missing-substring message for each.
  </behavior>
  <action>
    1. Read the current USAGE.md Observability section. Identify whether tracing is documented at all today and what the surrounding structure looks like (heading levels, code-fence style).

    2. Add or expand a subsection titled `### Sampling` (or similar — match adjacent heading style) under the Observability section. Required content:
       - One sentence: what the sampler does. (`ParentBased(TraceIDRatioBased(ratio))` — root spans sampled at `ratio`; child spans inherit parent decision.)
       - Default: `tracing_sample_ratio: 0.0` (tracing OFF by default — call out that the noop tracer is used when ratio is 0 OR endpoint is empty).
       - Smoke-test / dev recommendation: `tracing_sample_ratio: 1.0` (100% sampling; honest about the cost).
       - Production recommendation: `0.01`–`0.10` for low-overhead representative sampling.
       - YAML example showing `observability:` block with `tracing_endpoint` + `tracing_sample_ratio`.
       - Honest "head vs tail sampling" note: head-only in-binary; for tail-based sampling, run the otel-collector with the `tail_sampling` processor between Serena and your backend. Cite the OTel collector docs (URL OK; planner does not pin a specific URL — pick the canonical one).

    3. Add (or expand) a `### Smoke-Testing the Pipeline` subsection with the dev-only collector recipe from 55-RESEARCH Code Examples #4: the YAML `otel-collector-config.yaml` snippet using the `debug` exporter, the `docker run` command using `otel/opentelemetry-collector-contrib`, and the matching `observability:` config block on the Serena side (`tracing_endpoint: 127.0.0.1:4317`, `tracing_sample_ratio: 1.0`). Explicitly note: "This collector is a dev-only verification tool. Serena's runtime requires no external services — see TRACE-AUDIT.md."

    4. Add a `### Trace Coverage` subsection (one paragraph): "Every MCP tool handler emits a span (`daemon.mcp.tools.call` parent + `kernel.tool.*` or `skill.tool.*` child). Every outbound LS JSON-RPC call emits an `ls.request` child span. Coverage is mechanically enforced by `internal/mcp/coverage_test.go`. The full attribute audit is in `.planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md`."

    5. Add `TestUsageDocumentsTracingSampling` to `usage_test.go` (mirror the existing usage-test pattern). The test loads USAGE.md and asserts each required substring from `<behavior>` appears, with a failure message naming the missing substring. Use `t.Run(substring, ...)` per assertion for clear per-substring failures.

    6. Run `go vet . && go test . -run TestUsageDocumentsTracingSampling -v`. MUST pass.

    7. Verify cross-doc consistency: `grep -n tracing_sample_ratio USAGE.md` returns ≥ 2 matches (sampling section + smoke-test section). `grep -n otel/opentelemetry-collector-contrib USAGE.md` returns ≥ 1 match.
  </action>
  <verify>
    <automated>go vet . &amp;&amp; go test . -run TestUsageDocumentsTracingSampling -v -timeout 60s &amp;&amp; grep -q "tracing_sample_ratio" USAGE.md &amp;&amp; grep -q "otel/opentelemetry-collector-contrib" USAGE.md &amp;&amp; grep -v '^#' USAGE.md | grep -qi "head" &amp;&amp; grep -v '^#' USAGE.md | grep -qi "tail"</automated>
  </verify>
  <acceptance_criteria>
    - USAGE.md contains the new Sampling, Smoke-Testing, and Trace Coverage subsections.
    - `TestUsageDocumentsTracingSampling` passes with subtests for each required substring.
    - Head-vs-tail sampling distinction is explicit in the doc body (not just headings).
    - Smoke-collector recipe is complete enough that an operator can copy-paste-run it.
    - References to TRACE-AUDIT.md and the coverage test exist.
  </acceptance_criteria>
  <done>
    OBS-04 success criterion #3 satisfied. Operators have a complete sampling-config story, and the regression test ensures the sampling docs cannot silently regress.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 55-HUMAN-UAT.md for the live OTLP smoke trace (success criterion #4 setup)</name>
  <files>.planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/54-obs-inbinary-page/54-HUMAN-UAT.md (template / shape to mirror exactly — frontmatter, "Current Test", "Tests" with expected/result, "Summary" counts, "Gaps")
    - 55-RESEARCH.md Code Examples #4 (collector YAML + docker run + Serena config)
    - USAGE.md (after Task 2 — the smoke-test recipe you're about to reference)
    - 55-01-SUMMARY.md (so the expected trace tree shape is accurate post-Wave-1)
  </read_first>
  <action>
    Create `.planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md` mirroring `54-HUMAN-UAT.md` shape:

    1. Frontmatter:
       ```yaml
       ---
       status: pending
       phase: 55-obs-trace-coverage-audit
       source: [55-VERIFICATION.md]
       started: <today's date>
       updated: <today's date>
       ---
       ```

    2. `## Current Test` — `[awaiting human testing]`.

    3. `## Tests` — TWO scenarios:

       **Test 1: Smoke trace shows full request path with no orphan spans (criterion #4)**
       - expected: Run the dev-only OTLP collector via the docker recipe in USAGE.md "Smoke-Testing the Pipeline" (uses `otel/opentelemetry-collector-contrib` with `debug` exporter exposing :4317). Configure Serena with `tracing_endpoint: 127.0.0.1:4317` and `tracing_sample_ratio: 1.0`. Start the daemon, invoke ANY MCP tool that triggers an LS call (e.g., `find_symbol` against a Go file, which fans out to gopls). Within ~5 seconds, the collector's stdout shows a trace tree containing exactly: one `daemon.mcp.tools.call` parent span, one `kernel.tool.find_symbol` child, and at least one `ls.request` grandchild with `lsp.method=textDocument/...` and `lsp.language=go` attributes. Every span has a non-zero parent SpanID EXCEPT the top-level `daemon.mcp.tools.call`. NO span has Parent.SpanID == 0 OTHER than the root.
       - result: [pending]

       **Test 2: Skill tool produces a `skill.tool.*` child span (criterion #1 end-to-end)**
       - expected: With the same collector running, invoke a skill tool (e.g., `memory_write` or `get_repo_map`). The collector shows `daemon.mcp.tools.call` parent + `skill.tool.<name>` child (e.g., `skill.tool.memory_write`). NO orphan spans; no events named `ls.request` (those should be child spans now, not events).
       - result: [pending]

    4. `## Summary` block:
       ```
       total: 2
       passed: 0
       issues: 0
       pending: 2
       skipped: 0
       blocked: 0
       ```

    5. `## Gaps` (empty until human runs the UAT).

    6. `## Operator Setup Notes` (helpful for the human running this):
       - Docker required (only for the collector; Serena itself remains single-binary).
       - Cross-reference USAGE.md "Smoke-Testing the Pipeline" for the copy-paste recipe — do NOT duplicate the YAML here (single source of truth in USAGE.md).
       - If `debug` exporter is unavailable in the collector image, `logging` is the fallback (older naming).
       - Daemon shutdown MUST be clean (`SIGTERM`, not `SIGKILL`) so `BatchSpanProcessor.Shutdown` flushes — already wired (`internal/daemon/shutdown.go:48`); a kill-9 will lose the smoke trace.
  </action>
  <verify>
    <automated>test -f .planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md &amp;&amp; grep -q "status: pending" .planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md &amp;&amp; grep -q "otel/opentelemetry-collector-contrib" .planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md &amp;&amp; grep -q "ls.request" .planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md &amp;&amp; grep -q "skill.tool" .planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md &amp;&amp; grep -q "total: 2" .planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the Phase-54-shaped frontmatter and section structure.
    - Both UAT scenarios present (full LS-call path + skill-tool path).
    - References USAGE.md for the recipe (no recipe duplication).
    - Operator setup notes flag the SIGTERM-not-SIGKILL caveat (Pitfall 4 from research).
    - Status is `pending`; human runs the UAT separately from automated execution.
  </acceptance_criteria>
  <done>
    OBS-04 success criterion #4 has a concrete verification ticket awaiting a human. The phase can be marked complete once a human runs the UAT and updates `result:` from `pending` to a captured collector-stdout snippet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human runs the live-OTLP smoke trace and updates 55-HUMAN-UAT.md</name>
  <files>.planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md</files>
  <what-built>
    All three deliverables for OBS-04:
    - TRACE-AUDIT.md certifying every span attribute (criterion #2)
    - USAGE.md sampling docs + smoke-collector recipe (criterion #3)
    - 55-HUMAN-UAT.md scenarios for the live smoke trace (criterion #4 setup)
    Plus the Wave 1/2 mechanical gates (LS child span, skill child span, coverage test, allowlist test).
  </what-built>
  <action>
    Pause execution and present the verification steps below to the human operator. Wait for the resume-signal before continuing. The operator must run a live OTLP collector locally, exercise the daemon end-to-end, and update the UAT file with captured evidence — none of these steps can be automated by Claude (intrinsically a human-in-the-loop verification per OBS-04 success criterion #4).
  </action>
  <how-to-verify>
    1. Read USAGE.md "Smoke-Testing the Pipeline" subsection — confirm the recipe is complete and copy-paste runnable.
    2. Run the dev-only OTLP collector per the recipe (Docker required ONLY for this verification step).
    3. Configure your local Serena daemon with `observability.tracing_endpoint: 127.0.0.1:4317` and `observability.tracing_sample_ratio: 1.0`.
    4. Start the daemon (`SIGTERM`-clean — do NOT SIGKILL).
    5. Trigger a tool that fans into an LS call (e.g., via `claude` in a Go project: ask it to find a symbol).
    6. Within ~5 seconds, the collector's stdout should print a trace tree showing `daemon.mcp.tools.call` → `kernel.tool.find_symbol` → `ls.request` (with `lsp.method` attribute). Verify NO orphan spans.
    7. Trigger a skill tool (e.g., a memory_write). Confirm `daemon.mcp.tools.call` → `skill.tool.memory_write` appears.
    8. Send `SIGTERM` to the daemon. Confirm the collector shows the final batch was flushed (no truncated trace).
    9. Open `.planning/phases/55-obs-trace-coverage-audit/55-HUMAN-UAT.md` and update each `result: [pending]` to a brief captured snippet from the collector's stdout (or a description of the observed tree). Update the Summary block counts.
  </how-to-verify>
  <verify>Human operator confirms both UAT scenarios passed and 55-HUMAN-UAT.md is updated with captured evidence (Summary block counts reflect outcomes; results no longer say `[pending]`).</verify>
  <done>Both UAT scenarios in 55-HUMAN-UAT.md show captured evidence (collector stdout snippet or descriptive trace tree), Summary counters updated, and operator types "approved" on the resume-signal.</done>
  <resume-signal>
    Type "approved" once both UAT scenarios show the expected trace shape AND 55-HUMAN-UAT.md is updated with the captured evidence. Or describe issues (orphan spans, missing children, attributes off-list) and we'll loop back.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live OTLP collector | Smoke test runs against a local collector under operator control; no production data leaves the host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-09 | Information Disclosure (UAT artifact) | Captured collector stdout pasted into 55-HUMAN-UAT.md | mitigate | UAT instructions explicitly say "brief captured snippet OR description" — operator should redact any incidental file paths or workspace info before pasting. TRACE-AUDIT.md anti-list covers what should never appear in spans, so well-behaved Serena should not produce sensitive content for the operator to redact. |
| T-55-10 | Information Disclosure (USAGE.md misleading) | Operator follows recipe with `WithInsecure()` against a real backend | accept | Phase 12 already documented `WithInsecure()` as dev-mode. USAGE.md sampling section reiterates: smoke-test setup is for local collectors only; production deployment requires TLS (deferred per Phase 12 scope). |
| T-55-11 | Denial of Service (operator runs `tracing_sample_ratio: 1.0` in production) | Sampling docs | mitigate | USAGE.md sampling section explicitly distinguishes smoke-test (1.0) from production (0.01–0.10) and explains the cost of full sampling. Operator informed-choice. |
</threat_model>

<verification>
- TRACE-AUDIT.md exists, ≥ 60 lines, mentions all four span names + anti-list + propagation policy.
- USAGE.md grep checks pass; `TestUsageDocumentsTracingSampling` green.
- 55-HUMAN-UAT.md exists with `status: pending` and both scenarios.
- `go vet ./... && go test ./...` clean (only Task 2 introduces a Go test; Tasks 1 + 3 are doc-only).
- Checkpoint Task 4 awaits human signoff before phase is marked complete.
</verification>

<success_criteria>
1. TRACE-AUDIT.md publishable as the OBS-04 attribute hygiene certification (criterion #2).
2. USAGE.md operator can configure tracing for smoke testing without reading source (criterion #3).
3. 55-HUMAN-UAT.md gives a human the exact steps to capture criterion #4's evidence.
4. Mechanical gates from 55-02 (allowlist + coverage tests) and human review (TRACE-AUDIT.md sign-off) jointly satisfy OBS-04 end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/55-obs-trace-coverage-audit/55-03-SUMMARY.md` documenting:
- TRACE-AUDIT.md final attribute count (rows in each table) + cardinality math result.
- USAGE.md sections added/expanded (line ranges before/after).
- `TestUsageDocumentsTracingSampling` test invocation evidence (command + green output).
- Status of 55-HUMAN-UAT.md (pending / passed / issues — depends on human running checkpoint Task 4).
- Any deviations from the recommended doc structure in 55-RESEARCH.md.
</output>
