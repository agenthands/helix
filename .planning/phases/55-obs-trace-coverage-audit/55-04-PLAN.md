---
phase: 55-obs-trace-coverage-audit
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md
autonomous: true
requirements: [OBS-04]
tags: [observability, tracing, audit, documentation, hygiene-review]

must_haves:
  truths:
    - "TRACE-AUDIT.md exists at the canonical path .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md (success criterion 2 path)"
    - "Every span in the Phase 55 inventory has its own H2 section with an attribute table"
    - "Every attribute has a Source, PII risk, Cardinality, and Verdict (PASS/FLAG/FAIL)"
    - "The dual representation (ls.request span EVENT on parent + new lspool.lsp.{method} child span) is explicitly reconciled with PASS rationale"
    - "No attribute is FAIL — phase is shippable only with all-PASS or PASS-with-justified-FLAG"
  artifacts:
    - path: ".planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md"
      provides: "Per-span hygiene review tables for all five Phase 55 spans"
      min_lines: 120
      contains: "## kernel.tool.{name}"
  key_links:
    - from: "TRACE-AUDIT.md"
      to: "internal/mcp/middleware.go:316 + spanwrap.go + jsonrpc/conn.go + worker.go:309-314"
      via: "Source column file:line citations"
      pattern: "internal/"
---

<objective>
Author `.planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` — the per-span hygiene review that satisfies OBS-04 success criterion 2 ("a review artifact lists every span attribute and certifies: no PII, no unbounded cardinality"). One H2 section per span, each with an attribute table (`Attribute | Source | PII risk | Cardinality | Verdict`).

The file is reviewed by the user during `/gsd-verify-work` and remains in the repo as a durable artifact (security/operational reviewers can audit the trace surface from a single document).

Purpose: Closes OBS-04 success criterion 2. Without this document, the attribute hygiene of the trace surface is undocumented and reviewers must re-derive the answer from source code on every audit cycle.
Output: One new file `TRACE-AUDIT.md` at the canonical path, ~150-200 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-obs-trace-coverage-audit/55-CONTEXT.md
@.planning/phases/55-obs-trace-coverage-audit/55-RESEARCH.md
@internal/mcp/middleware.go
@internal/kernel/spanwrap.go
@internal/kernel/jsonrpc/conn.go
@internal/kernel/lspool/worker.go
@internal/forwarder/forwarder.go

<interfaces>
Spans in scope (per RESEARCH §"Initial span inventory" line 454):

| Span | Source | Owner | Status |
|------|--------|-------|--------|
| `forwarder.tools.call` | `internal/forwarder/forwarder.go:140` | Forwarder root span | Phase 12 (existing) |
| `daemon.mcp.tools.call` | `internal/mcp/middleware.go:316` | TelemetryMiddleware | Phase 12 (existing) |
| `kernel.tool.{name}` | `internal/kernel/spanwrap.go:30` + (post-Plan 02) `internal/mcp/server.go AddSkillTool` | WrapToolSpan + AddSkillTool | Phase 12 + Phase 55 |
| `lspool.lsp.{method}` | `internal/kernel/jsonrpc/conn.go` (Plan 01) | Conn.Call | Phase 55 (new) |
| `lspool.lsp.notify.{method}` | `internal/kernel/jsonrpc/conn.go` (Plan 01) | Conn.Notify | Phase 55 (new) |

Plus: `ls.request` span EVENT on the `kernel.tool.{name}` span, from `internal/kernel/lspool/worker.go:309-314`. This is an EVENT, not a span — it lives under the `kernel.tool.{name}` H2 section as a sub-table.

Attribute set authoritative source — read these files BEFORE writing tables:
- `internal/mcp/middleware.go` lines ~280-330: read the full `tracer.Start(...)` call + every `span.SetAttributes(...)` and `attribute.String(...)` call. Capture every attribute key set by TelemetryMiddleware.
- `internal/kernel/spanwrap.go`: confirms zero attributes (D-07).
- `internal/kernel/jsonrpc/conn.go` (post-Plan 01): single attribute `lsp.method`.
- `internal/forwarder/forwarder.go:140`: read the `tracer.Start(...)` arguments to capture attributes on the root forwarder span.
- `internal/kernel/lspool/worker.go:309-314`: confirms event has three attributes (lsp.method, lsp.language, lsp.duration_ms).

Verdict semantics (from CONTEXT.md "Cardinality verdict guidance"):
- **PASS** = bounded enum or registered/deterministic-set (e.g., tool_name from registry, profile from 5-element enum, lsp.method from LSP spec).
- **FLAG** = high-cardinality but justified (e.g., lsp.method has ~50 distinct values — bounded but information-rich).
- **FAIL** = unbounded (file paths, user input, raw IDs, request bodies). Phase 55 must NOT ship any FAIL — if discovered, the attribute is removed from the span before this plan completes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author TRACE-AUDIT.md with per-span H2 sections + attribute tables + ls.request event reconciliation</name>
  <read_first>
    - internal/mcp/middleware.go (CRITICAL — extract the EXACT set of attributes set on daemon.mcp.tools.call span; do not paraphrase from CONTEXT — read source as ground truth)
    - internal/kernel/spanwrap.go (confirm zero attributes per D-07; cite the comment)
    - internal/kernel/jsonrpc/conn.go (post-Plan 01: confirm only `lsp.method` attribute on Call/Notify spans)
    - internal/kernel/lspool/worker.go lines 290-320 (ls.request event attributes — three attrs: lsp.method, lsp.language, lsp.duration_ms)
    - internal/forwarder/forwarder.go around line 140 (forwarder.tools.call attributes)
    - internal/mcp/server.go (post-Plan 02: AddSkillTool wrap — confirm zero attributes on the kernel.tool.{name} span emitted from the skill path)
    - .planning/phases/55-obs-trace-coverage-audit/55-CONTEXT.md "TRACE-AUDIT.md Format" decision block (canonical table column order)
    - .planning/phases/55-obs-trace-coverage-audit/55-RESEARCH.md Pitfall 4 (ls.request event reconciliation language)
  </read_first>
  <action>
    Step 1 — Create `.planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` with the following document structure (NO `_test.go` patterns; this is documentation, not code):

    ```markdown
    ---
    phase: 55-obs-trace-coverage-audit
    artifact: trace-attribute-hygiene-review
    last_reviewed: <today's ISO date>
    reviewers: claude/55-04-executor
    ---

    # Phase 55 Trace Attribute Hygiene Review

    Per OBS-04 success criterion 2: every span attribute on the helix tracing surface
    listed below is certified for PII risk and cardinality.

    ## Spans Covered

    | Span Name | Source | Phase Introduced |
    |-----------|--------|------------------|
    | `forwarder.tools.call` | `internal/forwarder/forwarder.go:<line>` | 12 |
    | `daemon.mcp.tools.call` | `internal/mcp/middleware.go:<line>` | 12 |
    | `kernel.tool.{name}` (kernel path) | `internal/kernel/spanwrap.go:<line>` | 12 |
    | `kernel.tool.{name}` (skill path) | `internal/mcp/server.go:<line>` (AddSkillTool) | 55 |
    | `lspool.lsp.{method}` | `internal/kernel/jsonrpc/conn.go:<line>` | 55 |
    | `lspool.lsp.notify.{method}` | `internal/kernel/jsonrpc/conn.go:<line>` | 55 |

    ## Verdict Legend

    | Verdict | Meaning |
    |---------|---------|
    | PASS | Bounded enum or registered/deterministic set; safe for production. |
    | FLAG | High-cardinality but justified (e.g., bounded LSP method enum at ~50 values). |
    | FAIL | Unbounded or PII-leaking. Phase 55 ships zero FAIL attributes. |

    ## forwarder.tools.call

    [attribute table — extract from source]

    ## daemon.mcp.tools.call

    [attribute table — extract every attribute set by TelemetryMiddleware]

    ## kernel.tool.{name}

    [Note: D-07 — NO attributes on this span; TelemetryMiddleware owns all attributes.
    Both kernel-path (WrapToolSpan) and skill-path (AddSkillTool) emit zero-attribute spans.]

    ### Span Event: `ls.request`

    A v1.2 timing breadcrumb event lives on this span (NOT a child span — Phase 12 D-06 froze it).
    Source: `internal/kernel/lspool/worker.go:<line>`. Attributes:

    | Attribute | Type | PII risk | Cardinality | Verdict |
    |-----------|------|----------|-------------|---------|
    | `lsp.method` | string | none | bounded LSP enum (~50 methods) | FLAG (justified — bounded by spec) |
    | `lsp.language` | string | none | bounded language registry (~52 entries) | PASS |
    | `lsp.duration_ms` | int64 | none | continuous numeric (NOT an attribute key explosion) | PASS |

    Reconciliation with `lspool.lsp.{method}` child span: the EVENT is the v1.2 timing
    breadcrumb on the parent span. The new child span (Phase 55) is the v1.9 first-class
    operation. Both PASS — neither leaks PII or unbounded values. They serve different
    consumer queries (event for in-context aggregate; child span for dedicated `lspool.*`
    filter in Jaeger). NOT double-instrumentation.

    ## lspool.lsp.{method}

    | Attribute | Source | PII risk | Cardinality | Verdict |
    |-----------|--------|----------|-------------|---------|
    | `lsp.method` | conn.go Call | none | bounded LSP enum (~50 methods) | FLAG (justified — bounded by spec) |

    Cardinality discipline: `language` is intentionally NOT attached here — it lives on
    the parent `daemon.mcp.tools.call` span via TelemetryMiddleware. Re-attaching would
    duplicate telemetry without adding query power.

    ## lspool.lsp.notify.{method}

    [identical structure — attribute lsp.method only]

    ## Verdict Summary

    - PASS: <N> attributes
    - FLAG: <M> attributes (all justified — bounded LSP/language/profile/outcome enums)
    - FAIL: 0 attributes

    Phase 55 ships with zero unbounded-cardinality attributes and zero PII-bearing attributes.
    OBS-04 success criterion 2 satisfied.

    ## Maintenance Contract

    Adding a new span attribute to any of the spans above requires:
    1. Add a row to that span's table in this file with PII risk + Cardinality + Verdict.
    2. If Verdict is FAIL, the attribute MUST NOT ship — change the design.
    3. Bump `last_reviewed` date in frontmatter.

    ## Code references

    - `internal/forwarder/forwarder.go:<line>` — forwarder.tools.call root span
    - `internal/mcp/middleware.go:<line>` — daemon.mcp.tools.call + attributes
    - `internal/kernel/spanwrap.go:<line>` — WrapToolSpan + D-07 zero-attribute invariant
    - `internal/mcp/server.go:<line>` — AddSkillTool span wrap (Phase 55)
    - `internal/kernel/jsonrpc/conn.go:<line>` — lspool.lsp.{method} + lspool.lsp.notify.{method} (Phase 55)
    - `internal/kernel/lspool/worker.go:<line>` — ls.request span event (Phase 12 D-06 frozen)
    - `internal/obs/trace_audit_test.go` — automated audit gate (Phase 55)
    ```

    Step 2 — Read the actual source files in `<read_first>` and fill in EVERY `<line>` placeholder with real, current line numbers. Fill in EVERY `[attribute table — extract...]` block with the actual attribute keys set by that span's source. Use `grep -n "tracer.Start\|span.SetAttributes\|attribute.String\|attribute.Int" <file>` to enumerate attributes.

    Step 3 — Specifically for `daemon.mcp.tools.call`: read `internal/mcp/middleware.go` lines 270-340 to find every `attribute.String(...)` and `attribute.Int(...)` call associated with the TelemetryMiddleware span. CONTEXT.md mentions: tool_name, profile, mode, language, outcome — verify these exact keys against source. Each gets a row. PII risk: tool_name = registered set (none); profile = 5-elem enum (none); mode = 4-elem enum (none); language = 52-elem registry (none); outcome = 7-elem enum (none). Cardinality verdict: all PASS.

    Step 4 — Run sanity checks:
    - `grep -F 'FAIL' TRACE-AUDIT.md | grep -v 'zero FAIL\|FAIL: 0'` returns nothing (no FAIL attributes shipped).
    - `grep -cE '^## ' TRACE-AUDIT.md` returns >= 6 (one H2 per span + Verdict Summary + Maintenance Contract + Code references).
    - Every line number cited in TRACE-AUDIT.md exists in the referenced file (spot-check 3 with `sed -n 'Np' file`).

    Step 5 — Commit: `docs(55-04): author TRACE-AUDIT.md per-span attribute hygiene review`. Body: "Closes OBS-04 success criterion 2. Per-span H2 sections with PII/cardinality/verdict tables. ls.request event reconciled with new lspool.lsp.{method} child span (both PASS, dual representation justified). Zero FAIL attributes shipped."
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix &amp;&amp; test -f .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md &amp;&amp; wc -l .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md | awk '$1 &gt;= 120 { exit 0 } { exit 1 }' &amp;&amp; ! grep -E '\bFAIL\b' .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md | grep -v 'zero FAIL\|FAIL: 0\|FAIL = '</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` exits 0.
    - `wc -l .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` >= 120 lines.
    - `grep -cE '^## (forwarder\\.tools\\.call|daemon\\.mcp\\.tools\\.call|kernel\\.tool\\.\\{name\\}|lspool\\.lsp\\.\\{method\\}|lspool\\.lsp\\.notify\\.\\{method\\})' TRACE-AUDIT.md` returns 5 (one H2 per span).
    - `grep -F '### Span Event: `ls.request`' .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` matches (the dual-representation section under kernel.tool.{name}).
    - `grep -F 'FAIL: 0' .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` matches (Verdict Summary explicitly states zero FAIL).
    - No table row contains a verdict of "FAIL" (all attributes PASS or FLAG-with-justification). Verify: `grep -E '\\| FAIL \\|' TRACE-AUDIT.md` returns nothing.
    - Every cited file path under "Code references" has a valid line number. Spot-check: pick 3 cited file:line pairs and verify they exist via `grep -c '' <file>` AND the content at that line is what TRACE-AUDIT.md claims.
    - Frontmatter has `phase`, `artifact`, `last_reviewed`, `reviewers` keys.
    - `## Maintenance Contract` section exists (instructs future contributors how to extend the document).
  </acceptance_criteria>
  <done>TRACE-AUDIT.md exists with five span H2 sections, ls.request event reconciliation, zero FAIL attributes, Maintenance Contract, and verified file:line citations; OBS-04 success criterion 2 satisfied.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` exits 0.
- `grep -cE '^## (forwarder|daemon|kernel|lspool)' .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` returns 5.
- `grep -E '\| FAIL \|' .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` returns nothing.
</verification>

<success_criteria>
Plan 04 is complete when:
- TRACE-AUDIT.md exists at the canonical path.
- Every span has an attribute table with PII / Cardinality / Verdict columns.
- ls.request event is explicitly reconciled with the new lspool.lsp.{method} child span.
- Zero FAIL attributes shipped.
- Maintenance Contract section instructs future contributors.
- OBS-04 success criterion 2 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/55-obs-trace-coverage-audit/55-04-SUMMARY.md` documenting:
- Final attribute counts (PASS / FLAG / FAIL).
- Any attribute discoveries that diverged from CONTEXT/RESEARCH (e.g., a previously-undocumented attribute on `daemon.mcp.tools.call`).
- Commit hash.
</output>
