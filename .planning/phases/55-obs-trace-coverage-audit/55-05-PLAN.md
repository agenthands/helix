---
phase: 55-obs-trace-coverage-audit
plan: 05
type: execute
wave: 3
depends_on: [03]
files_modified:
  - USAGE.md
autonomous: true
requirements: [OBS-04]
tags: [observability, tracing, documentation, sampling]

must_haves:
  truths:
    - "USAGE.md has a new H3 `### Trace Sampling` subsection immediately after the existing `### Enable Tracing` H3 (line 734)"
    - "The new section documents the head-based ParentBased(TraceIDRatioBased(SampleRatio)) sampler"
    - "The new section documents how to set `tracing_sample_ratio` and `tracing_endpoint` via ObservabilityConfig"
    - "The new section explicitly notes the noop short-circuit when tracing_endpoint is empty (D-11/D-17)"
    - "Tail-sampling is mentioned ONCE as a future-scope note (deferred per CONTEXT)"
    - "No existing USAGE.md content is modified — pure-insert edit"
  artifacts:
    - path: "USAGE.md"
      provides: "### Trace Sampling H3 subsection"
      contains: "### Trace Sampling"
  key_links:
    - from: "USAGE.md ### Trace Sampling"
      to: "internal/obs/tracing.go (ParentBased + TraceIDRatioBased sampler)"
      via: "explicit reference to tracing_sample_ratio config key"
      pattern: "tracing_sample_ratio"
---

<objective>
Insert a new `### Trace Sampling` H3 subsection into USAGE.md immediately after the existing `### Enable Tracing` H3 at line 734. Reconciles RESEARCH Pitfall 5 / Assumption A5: the planner overrides CONTEXT.md's "before Prometheus Metrics" guidance because that guidance pre-dated awareness of the existing `### Enable Tracing` H3. Placing the new section adjacent to `### Enable Tracing` produces a coherent operator flow: Enable Tracing → Trace Sampling → (existing) Enable pprof.

Purpose: OBS-04 success criterion 3 ("USAGE.md Observability section documents sampling configuration: ratio, head vs. tail, and how to adjust it via ObservabilityConfig").
Output: USAGE.md modified with one new H3 subsection (~25-35 lines inserted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-obs-trace-coverage-audit/55-CONTEXT.md
@.planning/phases/55-obs-trace-coverage-audit/55-RESEARCH.md
@.planning/phases/54-obs-dashboards-runbooks/54-05-SUMMARY.md
@USAGE.md
@internal/obs/tracing.go
@internal/config/config.go

<interfaces>
Existing `### Enable Tracing` block at USAGE.md:734-744 (verbatim — read before editing):
```yaml
### Enable Tracing

Helix supports distributed tracing via OpenTelemetry (OTLP/gRPC):

```yaml
observability:
  tracing_endpoint: "localhost:4317"   # OTLP/gRPC collector address
  tracing_sample_ratio: 0.1           # 10% sampling
```

Traces include spans for tool execution, language server communication, and worker pool operations. Connect to any OTLP-compatible backend (Jaeger, Tempo, Honeycomb, etc.).

### Enable pprof
```

The new `### Trace Sampling` H3 inserts BETWEEN `### Enable Tracing`'s closing prose ("...Connect to any OTLP-compatible backend...") and the existing `### Enable pprof` H3. This is a pure-insert edit — zero existing lines are modified.

ObservabilityConfig source (read for accuracy of field names):
- `internal/config/config.go:37-58` — confirm exact YAML keys: `tracing_endpoint`, `tracing_sample_ratio`, `service_name`.
- `internal/obs/tracing.go:65-67` — confirm sampler is `ParentBased(TraceIDRatioBased(SampleRatio))`.
- `internal/obs/tracing.go` D-11 — noop short-circuit when `TracingEndpoint == ""`.
- `internal/obs/tracing.go` D-17 — zero allocation when tracing is off.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert ### Trace Sampling H3 subsection after ### Enable Tracing in USAGE.md</name>
  <read_first>
    - USAGE.md lines 730-760 (current ### Enable Tracing block + surrounding context)
    - internal/obs/tracing.go FULL FILE (verify exact sampler type names, D-11/D-17 wording, default ratio behavior)
    - internal/config/config.go lines 37-58 (verify exact YAML key names: tracing_endpoint, tracing_sample_ratio, service_name)
    - .planning/phases/54-obs-dashboards-runbooks/54-05-SUMMARY.md (Phase 54 USAGE.md insertion pattern; confirms pure-insert edits via `git diff USAGE.md | awk '/^-[^-]/'` returning empty)
    - .planning/phases/55-obs-trace-coverage-audit/55-CONTEXT.md "Sampling Documentation Scope" decision block
  </read_first>
  <action>
    Step 1 — Confirm USAGE.md line 734 still contains `### Enable Tracing` and line 746 still contains `### Enable pprof`. If line numbers have shifted, locate the actual current lines via `grep -n '^### Enable Tracing\|^### Enable pprof' USAGE.md`. The insertion goes between the closing paragraph of "Enable Tracing" and the "### Enable pprof" header.

    Step 2 — Use the Edit tool to insert the following new section. Insertion point: AFTER the line `Traces include spans for tool execution, language server communication, and worker pool operations. Connect to any OTLP-compatible backend (Jaeger, Tempo, Honeycomb, etc.).` and BEFORE the next blank line that precedes `### Enable pprof`.

    New section content:
    ```markdown

    ### Trace Sampling

    Helix uses head-based sampling — the decision to record a trace is made at the
    root span and propagated to every child via the W3C tracecontext spec. The
    sampler is `ParentBased(TraceIDRatioBased(tracing_sample_ratio))` (see
    `internal/obs/tracing.go`).

    **Configuration:**

    ```yaml
    observability:
      tracing_endpoint: "localhost:4317"   # OTLP/gRPC collector; empty = tracing OFF
      tracing_sample_ratio: 0.1            # 0.0 = none, 1.0 = all, 0.1 = 10%
      service_name: "helix"                # populates resource.service.name
    ```

    **Behavior:**

    | `tracing_endpoint` | `tracing_sample_ratio` | Result |
    |--------------------|-----------------------|--------|
    | empty / unset      | (any)                 | Tracing fully OFF — noop tracer, zero allocations on hot path. |
    | set                | `0.0`                 | Tracer wired, ratio sampler drops every trace. Useful for verifying the export path without volume. |
    | set                | `0.1`                 | 10% of root traces are sampled; child spans inherit the parent decision. |
    | set                | `1.0`                 | Every trace is sampled; recommended only for debugging or smoke runs. |

    **Operational guidance:**

    - Production: start at `0.01` (1%); raise during incident response.
    - Development: `1.0` to capture every trace.
    - Smoke tests: see `docs/runbooks/trace-smoke.md` for a one-command Jaeger smoke setup using `1.0`.

    **What is NOT shipped today:**

    - **Tail-sampling** (deciding to keep/drop traces *after* all spans complete, e.g.
      "always keep traces with errors"). Helix uses head-only sampling. Tail-sampling
      is on the future-scope backlog; if you need it today, run a tail-aware OTLP
      collector (Grafana Agent, OpenTelemetry Collector with `tail_sampling`
      processor) downstream of Helix.

    ```

    Step 3 — Run pure-insert verification: `git diff USAGE.md | awk '/^-[^-]/'` MUST return empty (zero existing lines deleted/modified). Phase 54-05 D-02 lesson: pure-insert is the safest USAGE.md edit pattern.

    Step 4 — Verify the new section sits between the existing tracing block and pprof block:
    - `grep -n '^### Enable Tracing\|^### Trace Sampling\|^### Enable pprof' USAGE.md` returns three lines in that order.
    - The line number of `### Trace Sampling` is between the line numbers of `### Enable Tracing` and `### Enable pprof`.

    Step 5 — Commit: `docs(55-05): add ### Trace Sampling H3 to USAGE.md (after ### Enable Tracing)`. Body: "Closes OBS-04 success criterion 3. Documents head-based ParentBased(TraceIDRatioBased) sampler, tracing_sample_ratio + tracing_endpoint config keys, behavior matrix, operational guidance, and tail-sampling future-scope note. Pure-insert edit — zero existing lines modified. Placement reconciled with RESEARCH Pitfall 5 / A5: AFTER existing ### Enable Tracing (line 734), NOT before ### Prometheus Metrics, for coherent operator flow."
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix &amp;&amp; grep -c '^### Trace Sampling$' USAGE.md | grep -q '^1$' &amp;&amp; awk '/^### Enable Tracing$/{a=NR} /^### Trace Sampling$/{b=NR} /^### Enable pprof$/{c=NR} END{exit !(a&lt;b &amp;&amp; b&lt;c)}' USAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### Trace Sampling$' USAGE.md` returns exactly 1.
    - The line number of `### Trace Sampling` is greater than the line number of `### Enable Tracing` AND less than the line number of `### Enable pprof`. Verify via the awk one-liner in `<verify>`.
    - `grep -F 'tracing_sample_ratio' USAGE.md | wc -l` returns >= 2 (at least once in `### Enable Tracing` and once in `### Trace Sampling`).
    - `grep -F 'ParentBased(TraceIDRatioBased' USAGE.md` matches exactly once (in `### Trace Sampling`).
    - `grep -iF 'tail-sampling' USAGE.md` matches at least once (the future-scope note).
    - `grep -F 'noop' USAGE.md | grep -F 'tracer'` matches (the noop-when-empty behavior is documented).
    - `git diff USAGE.md | awk '/^-[^-]/'` returns empty (pure-insert edit; zero existing lines modified).
    - `grep -F '### Trace Sampling' USAGE.md` does NOT appear anywhere BEFORE `### Prometheus Metrics` (sanity: the section is in the tracing cluster, not the metrics cluster).
  </acceptance_criteria>
  <done>USAGE.md has a new ### Trace Sampling H3 subsection placed between ### Enable Tracing and ### Enable pprof; pure-insert edit verified; head-based sampler + config keys + behavior matrix + tail-sampling future-scope note all present.</done>
</task>

</tasks>

<verification>
- `grep -c '^### Trace Sampling$' USAGE.md` returns 1.
- `git diff USAGE.md | awk '/^-[^-]/'` returns empty.
- Order of headers: `### Enable Tracing` < `### Trace Sampling` < `### Enable pprof`.
</verification>

<success_criteria>
Plan 05 is complete when:
- USAGE.md ships a `### Trace Sampling` H3 immediately after `### Enable Tracing`.
- The section documents the sampler type, config keys, behavior matrix, operational guidance, and tail-sampling future-scope.
- Pure-insert edit (no existing lines modified).
- OBS-04 success criterion 3 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/55-obs-trace-coverage-audit/55-05-SUMMARY.md` documenting:
- Final line numbers of the three relevant H3 headers.
- Pure-insert verification result.
- Commit hash.
- Note on Pitfall 5 / A5 reconciliation: chose AFTER ### Enable Tracing (NOT before ### Prometheus Metrics) per planner override of CONTEXT.md.
</output>
