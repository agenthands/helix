---
phase: 55-obs-trace-coverage-audit
plan: 06
type: execute
wave: 3
depends_on: [03]
files_modified:
  - docs/runbooks/trace-smoke.md
  - docs/images/trace-smoke-jaeger.png
autonomous: false
requirements: [OBS-04]
tags: [observability, tracing, runbook, smoke-test, manual-checkpoint]

must_haves:
  truths:
    - "docs/runbooks/trace-smoke.md exists with Phase 54-04 frontmatter pattern (severity, since_phase, last_reviewed)"
    - "Procedure leads with Jaeger via jaegertracing/all-in-one Docker image (per Q2 resolution)"
    - "Procedure includes a one-line note that any OTLP/gRPC collector on localhost:4317 works"
    - "docs/images/trace-smoke-jaeger.png exists (placeholder PNG acceptable per Phase 54-05 deferral pattern)"
    - "Manual checkpoint task pauses for human-driven screenshot capture"
  artifacts:
    - path: "docs/runbooks/trace-smoke.md"
      provides: "Operator-runnable smoke procedure verifying full trace path"
      min_lines: 70
      contains: "jaegertracing/all-in-one"
    - path: "docs/images/trace-smoke-jaeger.png"
      provides: "Screenshot of full trace path in Jaeger UI (placeholder until real capture)"
      contains: "PNG"
  key_links:
    - from: "docs/runbooks/trace-smoke.md"
      to: "USAGE.md ### Trace Sampling"
      via: "explicit reference linking sampling config to smoke procedure"
      pattern: "Trace Sampling"
---

<objective>
Author `docs/runbooks/trace-smoke.md` — the operator-runnable smoke procedure that verifies the full Helix trace path renders correctly against a live OTLP collector. Capture a one-time Jaeger UI screenshot at `docs/images/trace-smoke-jaeger.png` (manual checkpoint; placeholder if executor cannot drive Docker locally — Phase 54-05 D-22 deferral pattern).

Purpose: OBS-04 success criterion 4 ("a smoke trace captured against a live OTLP collector shows the full request path from MCP handler -> LS call with no orphan spans").
Output: Two new files: `docs/runbooks/trace-smoke.md` and `docs/images/trace-smoke-jaeger.png`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-obs-trace-coverage-audit/55-CONTEXT.md
@.planning/phases/55-obs-trace-coverage-audit/55-RESEARCH.md
@.planning/phases/54-obs-dashboards-runbooks/54-04-SUMMARY.md
@.planning/phases/54-obs-dashboards-runbooks/54-05-SUMMARY.md
@docs/runbooks/ErrCircuitOpen.md
@docs/runbooks/deadline-timeouts.md
@USAGE.md
@internal/obs/tracing.go

<interfaces>
Phase 54-04 D-15 frontmatter pattern: title, severity, metric, since_phase, last_reviewed.

Phase 54-04 D-16 H2 order (adapted for smoke procedure): Prerequisites -> Procedure -> Expected Result -> Troubleshooting -> Related -> Code references.

Phase 54-05 screenshot deferral pattern (54-05 D-22): a 1x1 placeholder PNG is acceptable when manual checkpoint cannot be driven; SUMMARY records "screenshot deferred" with last_reviewed follow-up date.

Span chain to verify in Jaeger:
forwarder.tools.call -> daemon.mcp.tools.call -> kernel.tool.{name} -> lspool.lsp.{method}

Jaeger Docker command (verbatim from CONTEXT.md): docker run --rm -p 4317:4317 -p 16686:16686 jaegertracing/all-in-one
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/runbooks/trace-smoke.md with Phase 54-04 frontmatter and smoke procedure</name>
  <read_first>
    - docs/runbooks/ErrCircuitOpen.md FULL FILE (Phase 54-04 frontmatter + H2 ordering pattern + ## Code references trailer)
    - docs/runbooks/deadline-timeouts.md (second example to triangulate runbook style)
    - .planning/phases/54-obs-dashboards-runbooks/54-04-SUMMARY.md decisions block (D-15, D-16, D-17 specifics)
    - USAGE.md ### Trace Sampling section (Plan 05 just added it; runbook links back to it)
    - .planning/phases/55-obs-trace-coverage-audit/55-CONTEXT.md "Smoke-Trace Artifact" decision block (Jaeger Docker command + procedure outline)
  </read_first>
  <action>
    Step 1 — Create `docs/runbooks/trace-smoke.md` with this content (replace `TODAYS_DATE` with the actual current ISO date):

    ```markdown
    ---
    title: Smoke-Test the Trace Pipeline
    severity: info
    metric: none — smoke procedure
    since_phase: 55
    last_reviewed: TODAYS_DATE
    ---

    # Smoke-Test the Trace Pipeline

    Verify the Helix trace pipeline produces the full span chain
    (`forwarder.tools.call` -> `daemon.mcp.tools.call` -> `kernel.tool.{name}` -> `lspool.lsp.{method}`)
    against a live OTLP/gRPC collector, with no orphan spans.

    Run this procedure when:
    - You add a new MCP tool and want to confirm its span path is wired correctly.
    - You change tracing-related code in `internal/obs/`, `internal/kernel/spanwrap.go`,
      `internal/mcp/middleware.go`, or `internal/kernel/jsonrpc/conn.go`.
    - You suspect a sampling or exporter regression.

    ## Prerequisites

    - Docker (for the Jaeger all-in-one image; any OTLP/gRPC collector on
      `localhost:4317` works — Jaeger is the example because the all-in-one image
      is one command).
    - A working `helix` binary (built via `make build` or downloaded from a release).
    - A workspace with at least one supported language (Go, TypeScript, Python, etc.).

    ## Procedure

    ### 1. Start a local Jaeger collector

    ```bash
    docker run --rm -p 4317:4317 -p 16686:16686 jaegertracing/all-in-one
    ```

    Ports: `4317` = OTLP/gRPC ingest (Helix -> Jaeger); `16686` = Jaeger Query UI (browser).

    ### 2. Configure Helix to export traces

    Create or update `~/.helix/helix_config.yml`:

    ```yaml
    observability:
      tracing_endpoint: "localhost:4317"
      tracing_sample_ratio: 1.0    # sample EVERY trace for the smoke run
      service_name: "helix"
    ```

    See `USAGE.md` ### Trace Sampling for production guidance (typical production
    ratio is `0.01`, NOT `1.0`).

    ### 3. Start Helix and trigger a representative tool flow

    Launch the daemon, then from any MCP client (Claude Code, Codex, etc.) invoke
    a tool that exercises an LS call — for example `goto_definition` on any file.
    `read_file` will NOT trigger an LS call and will not produce an `lspool.lsp.*`
    span.

    ### 4. Open Jaeger and inspect the trace

    Navigate to `http://localhost:16686/` -> Service: `helix` -> Find Traces.

    ## Expected Result

    The most recent trace expands to show this span chain:

    ```
    forwarder.tools.call          (root)
    +-- daemon.mcp.tools.call     (TelemetryMiddleware; attrs: tool_name, profile, mode, language, outcome)
        +-- kernel.tool.goto_definition   (WrapToolSpan or AddSkillTool; zero attrs per D-07)
            +-- lspool.lsp.textDocument/definition   (Phase 55; attr: lsp.method)
    ```

    ![Trace path in Jaeger](../images/trace-smoke-jaeger.png)

    Verify:
    - **No orphan spans.** Every span has a parent except `forwarder.tools.call`.
    - **`lsp.method` attribute present** on the `lspool.lsp.*` span.
    - **Zero attributes on `kernel.tool.{name}`** (D-07 — TelemetryMiddleware owns attributes).
    - **`ls.request` event** appears on the `kernel.tool.{name}` span (Phase 12 D-06
      frozen breadcrumb; see `.planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md`
      for the dual-representation rationale).

    ## Troubleshooting

    | Symptom | Likely Cause | Fix |
    |---------|--------------|-----|
    | No traces in Jaeger UI | `tracing_endpoint` empty or wrong port | Confirm `localhost:4317` matches Docker port mapping. |
    | Helix logs `OTLP exporter failure: ...` | Jaeger not running | Re-run the `docker run` command in Step 1. |
    | Traces appear but `lspool.lsp.*` missing | LS path not exercised | Use a tool that hits the LS (`goto_definition`, `find_references`); `read_file` won't. |
    | Only the root span appears | `tracing_sample_ratio = 0.0` | Set ratio to `1.0` for the smoke run. |
    | `kernel.tool.{name}` span missing | Skill tool registered without WrapToolSpan/AddSkillTool wrap | Run `go test ./internal/obs/ -run TestEveryRegisteredToolWrappedWithKernelSpan` — the audit gate should have caught this. |

    ## Related

    - `.planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` — per-span attribute hygiene review.
    - `internal/obs/trace_audit_test.go` — automated audit gate.
    - `USAGE.md` ### Enable Tracing / ### Trace Sampling.

    ## Code references

    - `internal/obs/tracing.go` — TracerProvider construction (Phase 12).
    - `internal/mcp/middleware.go` — `daemon.mcp.tools.call` span emission (TelemetryMiddleware).
    - `internal/kernel/spanwrap.go` — `kernel.tool.{name}` span emission (kernel-tool path).
    - `internal/mcp/server.go` — `kernel.tool.{name}` span emission (skill-tool path; Phase 55).
    - `internal/kernel/jsonrpc/conn.go` — `lspool.lsp.{method}` + `lspool.lsp.notify.{method}` (Phase 55).
    - `internal/kernel/lspool/worker.go` — `ls.request` span event (Phase 12 D-06 frozen breadcrumb).
    ```

    Step 2 — Replace `TODAYS_DATE` with the current ISO date.

    Step 3 — Verify file:line accuracy under `## Code references` (Phase 54-04 D-17 pattern). Append `:<line>` anchors where they aid the reader. Line numbers within ±5 are acceptable; ranges (`23-30`) also fine.

    Step 4 — Commit: `docs(55-06): add docs/runbooks/trace-smoke.md`.
  </action>
  <verify>
    <automated>test -f docs/runbooks/trace-smoke.md &amp;&amp; grep -c '^## ' docs/runbooks/trace-smoke.md | awk '$1 &gt;= 5 {exit 0} {exit 1}' &amp;&amp; grep -F 'jaegertracing/all-in-one' docs/runbooks/trace-smoke.md &amp;&amp; grep -F 'lspool.lsp.' docs/runbooks/trace-smoke.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/runbooks/trace-smoke.md` exits 0.
    - `wc -l docs/runbooks/trace-smoke.md` returns >= 70 lines.
    - `grep -c '^## ' docs/runbooks/trace-smoke.md` returns >= 5 (Prerequisites + Procedure + Expected Result + Troubleshooting + Related + Code references).
    - Frontmatter contains `title:`, `severity:`, `since_phase: 55`, `last_reviewed:` keys.
    - `grep -F 'jaegertracing/all-in-one' docs/runbooks/trace-smoke.md` matches.
    - `grep -F 'localhost:4317' docs/runbooks/trace-smoke.md` matches.
    - `grep -F 'forwarder.tools.call' docs/runbooks/trace-smoke.md` matches.
    - `grep -F 'daemon.mcp.tools.call' docs/runbooks/trace-smoke.md` matches.
    - `grep -F 'kernel.tool.' docs/runbooks/trace-smoke.md` matches.
    - `grep -F 'lspool.lsp.' docs/runbooks/trace-smoke.md` matches.
    - `grep -F '../images/trace-smoke-jaeger.png' docs/runbooks/trace-smoke.md` matches (image embed using project-relative path).
    - `grep -F 'Trace Sampling' docs/runbooks/trace-smoke.md` matches (link back to USAGE.md section from Plan 05).
    - `go test ./internal/obs/... -count=1` exits 0 (Plan 03's audit test still green; this runbook does not break anything).
  </acceptance_criteria>
  <done>trace-smoke.md exists with Phase 54-04-style frontmatter, six H2 sections, Jaeger Docker procedure, full span-chain expected result, troubleshooting table, image embed, code references with file anchors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (manual checkpoint): Capture Jaeger screenshot at docs/images/trace-smoke-jaeger.png</name>
  <what-built>
    Plan 06 Task 1 authored `docs/runbooks/trace-smoke.md`. Now the smoke procedure must be executed end-to-end and the resulting Jaeger UI view captured as `docs/images/trace-smoke-jaeger.png`.

    The screenshot is the snapshot evidence at phase completion that satisfies OBS-04 success criterion 4. The runbook itself is the durable artifact (anyone can re-run it), but the criterion calls for a captured trace.
  </what-built>
  <how-to-verify>
    Two execution paths — pick whichever is feasible.

    **Path A — Real capture (preferred):**
    1. Follow `docs/runbooks/trace-smoke.md` Steps 1-4 to start Jaeger and exercise a tool that triggers an LS call (e.g., `goto_definition` against a Go workspace).
    2. In the Jaeger UI at `http://localhost:16686/`, expand the most recent trace.
    3. Confirm the span tree visible: `forwarder.tools.call` -> `daemon.mcp.tools.call` -> `kernel.tool.{name}` -> `lspool.lsp.{method}` with no orphan spans, `lsp.method` attribute on the `lspool.lsp.*` span, `ls.request` event on the `kernel.tool.{name}` span.
    4. Capture a screenshot of the expanded trace at ~1600x900 (full window, all spans visible).
    5. Save the PNG to `docs/images/trace-smoke-jaeger.png`.
    6. Verify file: `file docs/images/trace-smoke-jaeger.png` reports `PNG image data`.
    7. Commit: `docs(55-06): capture Jaeger trace-smoke screenshot`.

    **Path B — Placeholder (Phase 54-05 deferral pattern):**
    1. If you cannot drive Docker locally right now, commit a 1x1 placeholder PNG so the file-magic acceptance gate passes and the missing artifact is loudly visible (renders broken or as a 1x1 dot in any markdown viewer).
    2. Generate a 1x1 PNG: any tool works; e.g., a 68-byte minimal PNG file. The Phase 54-05 placeholder at `docs/images/helix-overview-dashboard.png` (already in repo at 68 bytes per 54-05 SUMMARY) is reusable as a template.
    3. Save to `docs/images/trace-smoke-jaeger.png`.
    4. In `55-06-SUMMARY.md`, explicitly record "screenshot deferred — placeholder committed, last_reviewed follow-up date set" per the 54-05 deferral pattern.
    5. Commit: `docs(55-06): add placeholder for trace-smoke Jaeger screenshot`.

    **Acceptance verification (either path):**
    - `test -f docs/images/trace-smoke-jaeger.png` exits 0.
    - `file docs/images/trace-smoke-jaeger.png | grep -q "PNG image"` exits 0.
    - The runbook's image embed `../images/trace-smoke-jaeger.png` resolves to this file.
  </how-to-verify>
  <action>Manual checkpoint — no autonomous action. Executor pauses; human follows Path A (real capture) or Path B (placeholder) per how-to-verify. After human confirms, executor commits the resulting docs/images/trace-smoke-jaeger.png.</action>
  <verify>
    <automated>test -f docs/images/trace-smoke-jaeger.png &amp;&amp; file docs/images/trace-smoke-jaeger.png | grep -q 'PNG image'</automated>
  </verify>
  <done>docs/images/trace-smoke-jaeger.png exists as a valid PNG (real capture preferred; 1x1 placeholder accepted with deferral note in SUMMARY).</done>
  <resume-signal>
    Reply "approved-real" if Path A (real capture) was completed, "approved-placeholder" if Path B (deferred placeholder) was committed, or describe blockers.
  </resume-signal>
</task>

</tasks>

<verification>
- `test -f docs/runbooks/trace-smoke.md` exits 0.
- `test -f docs/images/trace-smoke-jaeger.png` exits 0.
- `file docs/images/trace-smoke-jaeger.png | grep -q "PNG image"` exits 0.
- `grep -c '^## ' docs/runbooks/trace-smoke.md` returns >= 5.
</verification>

<success_criteria>
Plan 06 is complete when:
- `docs/runbooks/trace-smoke.md` ships with frontmatter + 5+ H2 sections + Jaeger procedure + expected span chain + troubleshooting + code references.
- `docs/images/trace-smoke-jaeger.png` exists as either a real Jaeger capture (preferred) OR a 1x1 placeholder (Phase 54-05 deferral pattern).
- The runbook's image embed path resolves to the committed PNG.
- OBS-04 success criterion 4 is satisfied (with deferral note in SUMMARY if placeholder is used).
</success_criteria>

<output>
After completion, create `.planning/phases/55-obs-trace-coverage-audit/55-06-SUMMARY.md` documenting:
- Final runbook line count + section count.
- Screenshot path: real capture vs. placeholder (with last_reviewed follow-up date if placeholder).
- Commit hashes (Task 1 + Task 2).
</output>
