---
phase: 55-obs-trace-coverage-audit
plan: 07
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06]
files_modified: []
autonomous: true
requirements: [OBS-04]
tags: [observability, tracing, gate, verification, final]

must_haves:
  truths:
    - "go vet ./... exits 0 (project-wide)"
    - "go test ./... exits 0 (project-wide; default suite includes the new audit test)"
    - "All four OBS-04 success criteria are satisfied (audit test, TRACE-AUDIT.md, USAGE.md sampling, smoke runbook+screenshot)"
    - "All Phase 12 invariants preserved: D-01, D-07, D-09, D-11, D-17"
  artifacts: []
  key_links: []
---

<objective>
Final phase gate — run the full project test + vet suite, run a grep-based invariant audit, and confirm all four OBS-04 success criteria are satisfied. No code changes; this plan is a verification gate that produces evidence and triggers the executor's wrap-up commit only if every gate passes.

Purpose: Ensure no regression slipped in across Plans 01-06; ensure all Phase 12 invariants survived (D-01 no global tracer, D-07 no kernel.tool attributes, D-11 noop on empty endpoint); produce a single-source-of-truth gate result for `/gsd-verify-work`.
Output: A gate-result block embedded in `55-07-SUMMARY.md`. No code modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-obs-trace-coverage-audit/55-CONTEXT.md
@.planning/phases/55-obs-trace-coverage-audit/55-RESEARCH.md
@.planning/phases/55-obs-trace-coverage-audit/55-01-PLAN.md
@.planning/phases/55-obs-trace-coverage-audit/55-02-PLAN.md
@.planning/phases/55-obs-trace-coverage-audit/55-03-PLAN.md
@.planning/phases/55-obs-trace-coverage-audit/55-04-PLAN.md
@.planning/phases/55-obs-trace-coverage-audit/55-05-PLAN.md
@.planning/phases/55-obs-trace-coverage-audit/55-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final go vet + go test gate; record evidence</name>
  <read_first>
    - .planning/phases/55-obs-trace-coverage-audit/55-01-SUMMARY.md (Plan 01 result)
    - .planning/phases/55-obs-trace-coverage-audit/55-02-SUMMARY.md
    - .planning/phases/55-obs-trace-coverage-audit/55-03-SUMMARY.md
    - .planning/phases/55-obs-trace-coverage-audit/55-04-SUMMARY.md
    - .planning/phases/55-obs-trace-coverage-audit/55-05-SUMMARY.md
    - .planning/phases/55-obs-trace-coverage-audit/55-06-SUMMARY.md
  </read_first>
  <action>
    Step 1 — Run `go vet ./...` from repo root. Capture output. Tolerate only pre-existing warnings unrelated to Phase 55 (per project convention; e.g., the `tmp/` fixture warnings noted in Phase 54 SUMMARYs and the benign `TOKEN_COUNT macro redefined` from the swift tree-sitter binding). Record the exact stderr in the SUMMARY.

    Step 2 — Run `go test ./... -count=1` from repo root. Must exit 0. Specifically confirm:
    - `internal/obs/...` includes the new `TestEveryRegisteredToolWrappedWithKernelSpan*` tests + `TestConnCallProducesLspoolSpan` + `TestConnNotifyProducesLspoolNotifySpan` (Plan 03).
    - `internal/kernel/jsonrpc/...` includes the `TestConnCall_EmitsLspoolSpan*` + `TestConnNotify_EmitsLspoolNotifySpan` + noop + parent-span tests (Plan 01).
    - `internal/mcp/...` includes the `TestAddSkillTool_*` tests (Plan 02).
    - All previously-passing tests still pass.

    Step 3 — Run grep-based invariant audit:

    a) **D-01 (no global tracer):**
    ```bash
    grep -rn 'otel\.GetTracerProvider\|otel\.SetTracerProvider' \
      internal/kernel/jsonrpc/ internal/kernel/lspool/ internal/mcp/server.go internal/obs/
    ```
    Must return zero matches (excluding any pre-existing matches outside Phase 55 scope; the daemon.go `WithTracerProvider(d.obs.TracerProvider())` is OTLP exporter wiring and is NOT a global tracer registration).

    b) **D-07 (no attributes on kernel.tool spans):**
    ```bash
    grep -B2 -A8 'tracer\.Start.*"kernel\.tool\.' internal/mcp/server.go internal/kernel/spanwrap.go
    ```
    Visually verify that the captured `tracer.Start(...)` calls do NOT include `trace.WithAttributes(...)` and the immediate body does NOT include `span.SetAttributes(...)`. Record verdict in SUMMARY.

    c) **D-11 (noop when endpoint empty):** read `internal/obs/tracing.go`; verify the noop short-circuit comment is intact and the test `TestDefaultSamplerOff` still passes.
    ```bash
    go test ./internal/obs/ -run TestDefaultSamplerOff -count=1
    ```

    d) **D-17 (zero allocation when off):** confirm Plan 01's `TestConnCall_NoopTracerZeroAllocations` test exists and passes.
    ```bash
    go test ./internal/kernel/jsonrpc/ -run TestConnCall_NoopTracerZeroAllocations -count=1
    ```

    Step 4 — Run the OBS-04 success-criteria checklist:

    | # | Criterion | Verification |
    |---|-----------|-------------|
    | 1 | Audit confirms every MCP tool emits a span; every outbound LS call wrapped | `go test ./internal/obs/ -run "TestEveryRegisteredToolWrappedWithKernelSpan|TestConnCallProducesLspoolSpan|TestConnNotifyProducesLspoolNotifySpan" -count=1` exits 0 |
    | 2 | TRACE-AUDIT.md lists every attribute, certifies no PII / no unbounded cardinality | `test -f .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md && grep -c '\\| FAIL \\|' .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` reports zero FAIL rows |
    | 3 | USAGE.md documents sampling configuration | `grep -c '^### Trace Sampling$' USAGE.md` returns 1 |
    | 4 | Smoke trace captured against live OTLP collector | `test -f docs/runbooks/trace-smoke.md && test -f docs/images/trace-smoke-jaeger.png && file docs/images/trace-smoke-jaeger.png \| grep -q "PNG image"` exits 0 (placeholder OR real per Plan 06 Task 2 outcome) |

    Step 5 — Record full evidence (commands run + outputs) in `55-07-SUMMARY.md` per the standard summary template. If any gate fails, do NOT commit a "phase complete" marker — instead, leave SUMMARY in failed state and surface the failure to the orchestrator for `/gsd-verify-work` review.

    Step 6 — If ALL gates pass: commit `chore(55-07): final phase gate — go vet + go test + invariant audit green`. Body: enumerate the four OBS-04 criteria with PASS markers and cite the test commands.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix &amp;&amp; go vet ./... &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./...` exits 0 (only pre-existing tmp/ warnings tolerated; document in SUMMARY).
    - `go test ./... -count=1` exits 0.
    - `go test ./internal/obs/ -run TestEveryRegisteredToolWrappedWithKernelSpan -count=1` exits 0.
    - `go test ./internal/obs/ -run TestEveryRegisteredToolWrappedWithKernelSpan_catchesDrift -count=1` exits 0.
    - `go test ./internal/obs/ -run TestConnCallProducesLspoolSpan -count=1` exits 0.
    - `go test ./internal/obs/ -run TestConnNotifyProducesLspoolNotifySpan -count=1` exits 0.
    - `go test ./internal/kernel/jsonrpc/ -run TestConnCall_EmitsLspoolSpanWithLspMethodAttribute -count=1` exits 0.
    - `go test ./internal/mcp/ -run TestAddSkillTool_EmitsKernelToolSpan -count=1` exits 0.
    - `grep -rn 'otel\.GetTracerProvider\|otel\.SetTracerProvider' internal/kernel/jsonrpc/ internal/kernel/lspool/ internal/mcp/server.go internal/obs/` returns zero matches.
    - `test -f .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` exits 0.
    - `grep -E '\\| FAIL \\|' .planning/phases/55-obs-trace-coverage-audit/TRACE-AUDIT.md` returns nothing.
    - `grep -c '^### Trace Sampling$' USAGE.md` returns 1.
    - `test -f docs/runbooks/trace-smoke.md` exits 0.
    - `test -f docs/images/trace-smoke-jaeger.png` exits 0.
    - `file docs/images/trace-smoke-jaeger.png | grep -q "PNG image"` exits 0.
  </acceptance_criteria>
  <done>All four OBS-04 success criteria verified with concrete commands; full project go vet + go test green; D-01/D-07/D-11/D-17 invariants confirmed by grep + targeted tests; phase ready for /gsd-verify-work.</done>
</task>

</tasks>

<verification>
- `go vet ./...` exits 0.
- `go test ./... -count=1` exits 0.
- All four OBS-04 success criteria satisfied (verified by commands in acceptance_criteria).
- All Phase 12 invariants (D-01/D-07/D-09/D-11/D-17) verified by grep + targeted tests.
</verification>

<success_criteria>
Plan 07 is complete when:
- The four OBS-04 success criteria are demonstrably met with command-line evidence.
- Project-wide go vet + go test are green.
- Phase 12 invariants are not regressed.
- 55-07-SUMMARY.md records evidence ready for /gsd-verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/55-obs-trace-coverage-audit/55-07-SUMMARY.md` documenting:
- Verbatim outputs of go vet + go test.
- The four OBS-04 success-criteria checklist with PASS markers and command evidence.
- Invariant audit results (D-01 grep, D-07 visual inspection, D-11 + D-17 test outputs).
- Final commit hash.
</output>
