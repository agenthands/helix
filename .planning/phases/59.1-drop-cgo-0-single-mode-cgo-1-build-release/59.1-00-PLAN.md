---
phase: 59.1-drop-cgo-0-single-mode-cgo-1-build-release
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - Makefile
autonomous: false
requirements: [INT-BLOCKER-01]
tags: [release-pipeline, cgo, zig, wave-0, pre-execution-probe]
must_haves:
  truths:
    - "darwin zig cc cross-compile is verified locally before the goreleaser flip lands (de-risks Wave 2's first failure mode)"
    - "`make release-smoke` Makefile target exists with help annotation and is referenced from VALIDATION.md (Wave 0 gap closed before Wave 5 needs it)"
    - "D-14 windows-arm64 Path-3 stub decision is confirmed in writing on the plan thread before Wave 1 task ordering executes"
  artifacts:
    - path: "Makefile"
      provides: "NEW `release-smoke` target with `## help` annotation; appears in `.PHONY` declaration"
      contains: ["release-smoke:", "## Smoke-test the linux-amd64 release archive"]
    - path: ".planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md"
      provides: "Local probe results: `CC=\"zig cc -target aarch64-macos\" CGO_ENABLED=1 GOOS=darwin GOARCH=arm64 go build ./cmd/helix` exit code + Mach-O magic bytes; D-14 lock confirmation."
      contains: ["darwin probe", "windows-arm64 D-14"]
  key_links:
    - from: "Wave 0 probe results"
      to: "Wave 2 .goreleaser.yaml flip task"
      via: "If darwin probe fails, Wave 2 cannot start without a darwin contingency (osxcross / separate macOS runner)"
      pattern: "59.1-00-PROBE-NOTES.md"
    - from: "Wave 0 release-smoke target"
      to: "Wave 5 release-smoke task"
      via: "Wave 5 invokes `make release-smoke` against dist/helix_v*_linux_amd64.tar.gz"
      pattern: "make release-smoke"
---

<objective>
De-risk Wave 2 (.goreleaser.yaml flip) and Wave 5 (smoke + reproducibility verification) by running cheap pre-execution probes BEFORE any code-mutating wave starts. Authors the NEW `make release-smoke` target so Wave 5 has a callable harness, and forces explicit re-confirmation of the D-14 windows-arm64 Path-3 lock from RESEARCH.md `## Post-Research Decision Lock`.

Purpose: The single most expensive failure mode for this phase is "Wave 2 lands the goreleaser flip, CI runs, darwin link fails because zig cc cannot reach darwin without an Apple SDK". RESEARCH.md A1 flags this as MEDIUM risk verifiable with a 5-minute local probe. This plan runs that probe + two cheap companions before any deletion happens.

Output:
- `Makefile` `release-smoke` target authored (placeholder body acceptable; finalized in Wave 5)
- Probe-notes file at `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` capturing darwin zig cc exit code + Mach-O magic verification
- Explicit D-14 lock confirmation recorded in the same notes file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-VALIDATION.md
@Makefile
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Probe darwin zig cc cross-compile (5-min de-risk)</name>
  <read_first>
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md (A1 + Open Question 2)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md (D-01 single-runner constraint)
  </read_first>
  <files>(no source files modified — produces .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md)</files>
  <action>See &lt;what-built&gt; below — run the local darwin zig cc probe and capture results in 59.1-00-PROBE-NOTES.md.</action>
  <verify>
    <automated>test -f .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md &amp;&amp; grep -qE '(darwin probe exit=0|Wave 2 contingency:)' .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md</automated>
  </verify>
  <done>Probe-notes file exists with verdict (PASS or contingency-decided); zig version recorded.</done>
  <what-built>
    Run the local darwin zig cc probe documented in RESEARCH.md A1:
    ```bash
    rm -f /tmp/helix-darwin-arm64-probe
    CC="zig cc -target aarch64-macos" \
      CGO_ENABLED=1 GOOS=darwin GOARCH=arm64 \
      go build -o /tmp/helix-darwin-arm64-probe ./cmd/helix
    echo "exit=$?"
    test -f /tmp/helix-darwin-arm64-probe && \
      hexdump -n 4 -C /tmp/helix-darwin-arm64-probe   # expect Mach-O ARM64 magic: cf fa ed fe
    file /tmp/helix-darwin-arm64-probe                 # expect "Mach-O 64-bit executable arm64"
    ```
    Then write `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` with the exit code, hexdump output, file output, and pass/fail verdict. Also record local zig version (`zig version`) for the audit trail.
  </what-built>
  <how-to-verify>
    1. Inspect `59.1-00-PROBE-NOTES.md` — must contain non-empty exit-code line and hexdump output
    2. If exit code = 0 AND hexdump shows `cf fa ed fe` (Mach-O ARM64 magic): probe PASSES, Wave 2 is safe to proceed with darwin zig cc
    3. If exit code != 0 OR magic bytes wrong: probe FAILS — record the goreleaser/Go error verbatim in the notes file; the human reviewer must decide between (a) trying `-target aarch64-macos-none` syntax variant, (b) bumping local zig (note: local zig may be 0.16.0 vs CI's pinned 0.14.1; per RESEARCH if it works on 0.16.0 it works on 0.14.1), or (c) escalating to discuss-phase for darwin-runner contingency
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` exists
    - File contains exact line `darwin probe exit=0` (PASS path) OR a non-zero exit-code line plus an explicit human-acknowledged "Wave 2 contingency:" sentence
    - If PASS: `file /tmp/helix-darwin-arm64-probe` output line in notes contains substring `Mach-O 64-bit executable arm64`
    - `command -v zig >/dev/null 2>&1 && zig version` output recorded in notes (audit trail)
  </acceptance_criteria>
  <resume-signal>Type "darwin probe pass" if Mach-O ARM64 binary produced, OR "darwin contingency: &lt;decision&gt;" with the chosen path</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author `make release-smoke` Makefile target (Wave 0 test-infra gap)</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (especially lines 50-58 — the `bench` / `bench-baseline` / `release-snapshot` target shapes)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md (Pattern A7 — verbatim target body to lift)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md (D-04 — fixture choice authority)
  </read_first>
  <action>
    Append a NEW `release-smoke` target to `Makefile` AFTER the existing `release-snapshot` recipe. Use the EXACT verbatim body from PATTERNS.md Pattern A7:

    ```makefile
    release-smoke: ## Smoke-test the linux-amd64 release archive: extract, run daemon, hit MCP tools/list (D-04)
    	@test -f dist/helix_v*_linux_amd64.tar.gz || { \
    	  echo "dist/helix_v*_linux_amd64.tar.gz not found; run \`make release-snapshot\` first"; exit 1; }
    	@# Body: extract archive, boot daemon against testdata/fixtures/go/, hit tools/list,
    	@# assert >= 41 tools and tree-sitter grammar count == 23. Wave 5 finalizes the harness.
    	@echo "release-smoke: TODO — Wave 5 finalizes fixture path + assertions per D-04"
    	@false  # placeholder so the target fails-loud until wired
    ```

    Then append `release-smoke` to the existing `.PHONY:` declaration on line 1 (currently:
    `.PHONY: build clean proto test vet fmt docs clean-jdtls-cache bench-jdtls-warm bench bench-baseline release-snapshot update-trust-root`).
    New line: `.PHONY: build clean proto test vet fmt docs clean-jdtls-cache bench-jdtls-warm bench bench-baseline release-snapshot release-smoke update-trust-root`.

    The `@false` sentinel is INTENTIONAL — Wave 5 wires the real harness; Wave 0 only needs the target to exist so VALIDATION.md's "Wave 0 release-smoke target authored" gate flips green.
  </action>
  <verify>
    <automated>grep -E '^release-smoke:' Makefile && grep -E '^\.PHONY:.*release-smoke' Makefile && make -n release-smoke 2>&1 | grep -q 'TODO'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^release-smoke:' Makefile` returns 1
    - `grep -c 'release-smoke' Makefile | grep -q '^[12]$'` (twice: target + .PHONY) — exact: `[ "$(grep -c release-smoke Makefile)" = "2" ]`
    - `grep -q '^\.PHONY:.*release-smoke' Makefile` succeeds
    - Running `make release-smoke` exits non-zero with stdout containing the literal string `release-smoke: TODO` (placeholder behavior)
    - `make build` still works (zero behavior regression on existing targets): `make build && test -f $(go env GOEXE && echo helix || echo bin/helix)`
  </acceptance_criteria>
  <done>
    - Target stub committed; Wave 5 has a callable hook
    - No existing target's behavior changed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm D-14 windows-arm64 Path-3 lock in writing</name>
  <read_first>
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md (## Post-Research Decision Lock — D-14, and Risks > CRITICAL > Path 3)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-VALIDATION.md (Path-3 windows-arm64 stub validation block)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md (Pattern A3 — target shape for `duckdb_winarm64.go`)
  </read_first>
  <files>.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md</files>
  <action>See &lt;what-built&gt; below — append the D-14 lock confirmation section to the probe-notes file.</action>
  <verify>
    <automated>grep -qF 'D-14 lock acknowledged: yes' .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md</automated>
  </verify>
  <done>D-14 lock acknowledgement line present; Wave 1 unblocked.</done>
  <what-built>
    Append to `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` a section titled exactly `## D-14 windows-arm64 lock confirmation` containing:
    - Verbatim restate of the Path-3 decision: "`internal/semantic/store/duckdb.go` gets `//go:build !(windows && arm64)`; new `duckdb_winarm64.go` stub returns `serr.Unsupported`. Stub is platform-conditional, NOT cgo-conditional. 6-archive matrix preserved. Restoration tracked as DEF-51-04."
    - Confirmation that this lock is binding for Wave 1 task ordering: `duckdb_winarm64.go` MUST be created BEFORE `duckdb.go` gains its `//go:build !(windows && arm64)` constraint, and BEFORE any flatten in `internal/semantic/store/`. Otherwise the windows-arm64 build link-fails between Wave 1 commits.
    - Reviewer signoff line: `D-14 lock acknowledged: <yes|no>` — must be `yes` to unblock Wave 1.
  </what-built>
  <how-to-verify>
    Inspect `59.1-00-PROBE-NOTES.md` for the `## D-14 windows-arm64 lock confirmation` header followed by the three required items. The reviewer's signoff line must read `D-14 lock acknowledged: yes`.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -F '## D-14 windows-arm64 lock confirmation' .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` returns 1 hit
    - `grep -F 'D-14 lock acknowledged: yes' .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` returns 1 hit
    - `grep -F 'platform-conditional, NOT cgo-conditional' .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` returns 1 hit
    - `grep -F 'duckdb_winarm64.go MUST be created BEFORE' .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` returns 1 hit
  </acceptance_criteria>
  <resume-signal>Type "D-14 confirmed" once the notes file contains the lock confirmation and the explicit acknowledgement line</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev shell → /tmp probe binary | Probe binary is throwaway; not signed; never published |
| Probe-notes file → planner state | Notes inform Wave 1 ordering; bad notes → bad Wave 1; mitigation = explicit acknowledgement line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59.1-W0-01 | Tampering | Probe binary at /tmp/helix-darwin-arm64-probe | accept | Throwaway probe; never published; no trust placed in this artifact beyond verifying compile success |
| T-59.1-W0-02 | Repudiation | D-14 lock confirmation could be modified after the fact | mitigate | Acknowledgement line is committed to git as part of the probe-notes file; git history is the audit trail |
| T-59.1-W0-03 | Tampering | Local zig version drift (0.16.0 dev vs 0.14.1 CI) | accept | RESEARCH §A1: 0.14.1 syntax is target-stable on 0.16.0; CI-pinned version is the authority; local probe is best-effort |
</threat_model>

<verification>
After all tasks complete:
- `cat .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` shows darwin probe verdict + D-14 confirmation
- `make -n release-smoke` shows the placeholder body
- `make build` still works (no regression)
</verification>

<success_criteria>
- darwin zig cc probe verdict captured (PASS or contingency-decided)
- `make release-smoke` target exists; placeholder body returns failure with TODO message
- D-14 windows-arm64 Path-3 lock acknowledgement committed in `59.1-00-PROBE-NOTES.md`
- Wave 1 has explicit signoff to proceed with the documented ordering constraint
</success_criteria>

<output>
After completion, create `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-SUMMARY.md` recording:
- darwin probe exit code + Mach-O verdict
- Local zig version
- D-14 lock confirmation timestamp
- Any contingency decisions if darwin probe failed
</output>
