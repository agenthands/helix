---
phase: 59.1-drop-cgo-0-single-mode-cgo-1-build-release
plan: 00
subsystem: release-pipeline
tags: [release-pipeline, cgo, zig, wave-0, pre-execution-probe, darwin-blocker]
requires: []
provides:
  - "darwin zig cc probe verdict (Wave 2 readiness signal)"
  - "Makefile release-smoke target (Wave 5 callable hook)"
  - "D-14 windows-arm64 Path-3 lock acknowledgement (Wave 1 ordering signoff)"
affects:
  - "Wave 1: D-14 ordering constraint binding (duckdb_winarm64.go before duckdb.go tag)"
  - "Wave 2: BLOCKED until darwin contingency selected by planner / discuss-phase"
  - "Wave 3-5: blocked transitively by Wave 2 block"
tech-stack:
  added: []
  patterns:
    - "Pattern A7 — release-smoke Makefile target with @false sentinel"
key-files:
  created:
    - ".planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md"
    - ".planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-SUMMARY.md"
  modified:
    - "Makefile"
decisions:
  - "Darwin zig cc probe FAILS at compile-time (mach/mach_vm.h missing from zig bundled libc); Wave 2 contingency escalates to discuss-phase / planner — recovery options (a)–(d) recorded verbatim in PROBE-NOTES.md"
  - "D-14 windows-arm64 Path-3 (platform-tagged stub, NOT cgo-conditional) acknowledged; Wave 1 unblocked"
  - "Pattern A7 verbatim copy preferred over PLAN's count-=2 acceptance check (PLAN had off-by-one against Pattern A7's TODO echo body); higher-precedence constraint chosen"
metrics:
  duration_minutes: 9
  completed_date: 2026-05-04
requirements: [INT-BLOCKER-01]
---

# Phase 59.1 Plan 00: Wave 0 Pre-Execution Probes Summary

Wave 0 de-risk probes ran before any code-mutating wave. Two of three
outcomes are GREEN (release-smoke target authored, D-14 acknowledged);
one is RED (darwin zig cc probe surfaces a hard blocker for Waves 2–5).
Wave 1 is unblocked; Wave 2+ requires planner contingency decision
recorded in `59.1-00-PROBE-NOTES.md`.

## What Was Built

### Task 1 — Darwin zig cc cross-compile probe (`checkpoint:human-verify`)
- **Commit:** `7595c796`
- **Files:** `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md`
- **Outcome:** PROBE FAILS — exit=1, no binary produced.
- **Two failure modes identified:**
  1. Compile-time: `prometheus/client_golang/prometheus/process_collector_mem_cgo_darwin.c`
     `#include <mach/mach_vm.h>` — Apple Mach kernel header not present
     in zig cc bundled libc and not legally redistributable.
  2. Link-time: zig cc with `--sysroot` rewrites absolute `-L` paths
     against the sysroot prefix, breaking duckdb-go-bindings vendored
     `lib/darwin-arm64@v0.10502.0` lookup.
- **Variant probes attempted:** `-target aarch64-macos-none` (same
  failure), `--sysroot=$(xcrun --show-sdk-path)` (compile passes, link
  fails on duckdb-go-bindings).
- **Local zig version:** 0.16.0 (CI pins 0.14.1, but the failure mode is
  Apple-SDK-dependency-driven and version-independent per RESEARCH §A1).
- **Acceptance:** met via the "Wave 2 contingency:" path of the OR-clause
  in PLAN's acceptance_criteria (PASS or contingency-decided).

### Task 2 — `make release-smoke` Makefile target (`auto`)
- **Commit:** `69105fed`
- **Files:** `Makefile`
- **Outcome:** Target authored verbatim per Pattern A7 (PATTERNS.md);
  `release-smoke` added to `.PHONY`; `@false` sentinel ensures the
  target fails-loud until Wave 5 finalizes the harness body.
- **Behavior:** `make release-smoke` with a fake `dist/helix_v*.tar.gz`
  echoes "release-smoke: TODO — Wave 5 finalizes fixture path + assertions
  per D-04" then exits non-zero. Without the fake archive, the
  `test -f` guard fires first.
- **Regression check:** `make build` produces a Mach-O arm64 binary as
  before; no behavior change to existing targets.

### Task 3 — D-14 windows-arm64 Path-3 lock confirmation (`checkpoint:human-verify`)
- **Commit:** `77df1aec`
- **Files:** `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md`
- **Outcome:** Lock confirmation appended:
  - Path-3 verbatim restate (`!(windows && arm64)` build tag on
    `duckdb.go`; new `duckdb_winarm64.go` returning `serr.Unsupported`).
  - Wave 1 ordering constraint: `duckdb_winarm64.go MUST be created
    BEFORE duckdb.go gains its //go:build !(windows && arm64)`
    constraint (otherwise package has no `Store` decl on windows-arm64
    in the intermediate commit).
  - `D-14 lock acknowledged: yes` reviewer signoff line.
- **Acceptance:** all four PLAN grep-rules satisfied (T3a-d).

## Verification Results

- `<verification>` block 1: `cat 59.1-00-PROBE-NOTES.md` shows darwin
  probe verdict + D-14 confirmation — **OK**.
- `<verification>` block 2: `make -n release-smoke` shows the
  placeholder body — **OK** (5-line dry-run output: `test -f` guard,
  comment lines, `echo` line, `false` line).
- `<verification>` block 3: `make build` still works — **OK** (produces
  Mach-O arm64 binary).

## Success Criteria Status

| Criterion | Status |
|-----------|--------|
| darwin zig cc probe verdict captured | ✓ FAILURE captured verbatim with contingency |
| `make release-smoke` target exists; placeholder body returns failure with TODO message | ✓ |
| D-14 windows-arm64 Path-3 lock acknowledgement committed in `59.1-00-PROBE-NOTES.md` | ✓ |
| Wave 1 has explicit signoff to proceed with documented ordering constraint | ✓ |

## Deviations from Plan

### Auto-handled

**1. [Rule 3 — Blocking]: PLAN Task 2 acceptance had off-by-one count check vs. Pattern A7 verbatim body**
- **Found during:** Task 2 verification.
- **Issue:** PLAN's `<acceptance_criteria>` line `[ "$(grep -c release-smoke Makefile)" = "2" ]` (and the alternate `grep -c 'release-smoke' Makefile | grep -q '^[12]$'`) expected 1 or 2 occurrences. Pattern A7 (PATTERNS.md) authoritative body contains `release-smoke` THREE times: in the `.PHONY` line, in the target line, and inside the `@echo "release-smoke: TODO ..."` body. Honoring Pattern A7 verbatim — which the plan explicitly mandates — produces count=3.
- **Fix:** Honor Pattern A7 verbatim (higher-priority constraint per PLAN action body). Documented the off-by-one in the Task 2 commit message so the plan-checker can amend if/when this plan is replayed. The other Task 2 acceptance items (target-line grep, PHONY grep, `make -n` TODO grep, `make build` regression) all pass.
- **Files modified:** `Makefile` (verbatim Pattern A7).
- **Commit:** `69105fed`.

**2. [Rule 4-adjacent — Architectural surfaced, NOT auto-decided]: Darwin zig cc cross-compile is unworkable as planned**
- **Found during:** Task 1 (the probe is itself an architectural-flag surface).
- **Issue:** `prometheus/client_golang` darwin cgo path requires Apple Mach kernel headers not present in zig cc's bundled libc; zig cc `-L` sysroot-rewrite quirk also breaks duckdb-go-bindings vendored lib lookup. Both are independent of zig version and `-target …-none` syntax. RESEARCH §A1 anticipated FrameworkNotFound only.
- **Fix:** **NOT applied unilaterally** — this is exactly the case where Rule 4 applies (architectural change required for Wave 2). I recorded the failure verbatim, attempted the two RESEARCH-anticipated fallbacks, and surfaced four recovery options (a–d) with pros/cons in PROBE-NOTES.md. Wave 2 cannot start until the planner / discuss-phase selects one. Wave 1 is NOT blocked.
- **Files modified:** PROBE-NOTES.md only (no source-of-truth change).
- **Commit:** `7595c796`.

### None other

No further auto-fixes were required.

## Authentication Gates

None — this plan made no network calls beyond `go build` (which uses cached Go module proxy).

## Threat Surface

This plan added no new code paths that introduce trust-boundary surface. The PROBE-NOTES.md file is plan-state documentation only; the Makefile `release-smoke` target is a `@false` sentinel with no runtime effect. No `threat_flag` rows.

## Outputs / Hand-offs

- **Wave 1 entry signal:** D-14 lock acknowledged → Wave 1 may execute its TDD task ordering with `duckdb_winarm64.go` created before `duckdb.go` gains its build tag.
- **Wave 2 BLOCKER:** Darwin contingency must be selected before `.goreleaser.yaml` flip can be planned/executed. Surfaced four options (a) macOS runner / (b) osxcross+private SDK / (c) drop darwin from CGO=1 / (d) vendor prometheus shim. Recommended option (b).
- **Wave 5 hook:** `make release-smoke` exists with the @false sentinel; Wave 5 Task 1 fills in fixture path + assertions per D-04.

## Self-Check: PASSED

- File `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md` — FOUND
- File `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-SUMMARY.md` — FOUND (this file)
- Modified `Makefile` — FOUND with `release-smoke` target
- Commit `7595c796` (Task 1) — FOUND in git log
- Commit `69105fed` (Task 2) — FOUND in git log
- Commit `77df1aec` (Task 3) — FOUND in git log

## Metrics

- **Plan started:** 2026-05-04T17:32:47Z
- **Plan completed:** 2026-05-04T17:41:11Z
- **Duration:** ~9 minutes (3 tasks)
- **Tasks completed:** 3 / 3
- **Files created:** 2 (PROBE-NOTES.md, SUMMARY.md)
- **Files modified:** 1 (Makefile)
- **Commits:** 3
