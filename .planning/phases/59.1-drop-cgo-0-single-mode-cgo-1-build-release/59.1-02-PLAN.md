---
phase: 59.1-drop-cgo-0-single-mode-cgo-1-build-release
plan: 02
type: execute
wave: 2
depends_on: [59.1-00, 59.1-01]
files_modified:
  - .goreleaser.yaml
  - Makefile
autonomous: false
requirements: [INT-BLOCKER-01]
tags: [goreleaser, zig-cc, cross-compile, makefile, wave-2, d-01, d-02, d-03]
must_haves:
  truths:
    - ".goreleaser.yaml builds all 6 archives under CGO_ENABLED=1 with per-target `CC=zig cc -target <triple>` set via templated `env:` block (D-01)"
    - "`make release-snapshot` exits 0 locally and writes 6 archives to `dist/`; archive sizes are materially larger than the prior CGO=0 baseline (sanity-check that CGO is actually enabled)"
    - "Makefile's `release-snapshot` recipe fails-loud upfront if `zig` is not on PATH (D-03); `make build` is unchanged and continues to use host CC"
    - "Locked: `version: 2`, `project_name: helix`, 6 archives, `mod_timestamp: '{{ .CommitTimestamp }}'`, `name_template`, cosign sign block, `flags: [-trimpath, -buildvcs=false]`, `ldflags` with `-s -w` and main.{version,commit,date} all preserved verbatim (Shared 5)"
  artifacts:
    - path: ".goreleaser.yaml"
      provides: "Single-mode CGO=1 release config; per-target CC=zig cc env block; reproducibility flags preserved verbatim"
      contains: ["CGO_ENABLED=1", "CC=zig cc -target x86_64-linux-musl", "CC=zig cc -target aarch64-linux-musl", "CC=zig cc -target x86_64-windows-gnu", "CC=zig cc -target aarch64-windows-gnu", "CC=zig cc -target x86_64-macos", "CC=zig cc -target aarch64-macos", "-trimpath", "-buildvcs=false", "mod_timestamp: '{{ .CommitTimestamp }}'"]
    - path: "Makefile"
      provides: "release-snapshot recipe gains zig-presence guard (mirrors goreleaser guard); build target unchanged"
      contains: ["command -v zig >/dev/null 2>&1", "zig not installed (required for CGO=1 cross-compile)"]
    - path: ".planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SNAPSHOT-BASELINE.md"
      provides: "First CGO=1 local snapshot baseline: archive shapes, sha256s, file sizes (NOT committed for reproducibility — local-vs-CI byte parity is not guaranteed; CONTRIBUTING.md will document this)"
      contains: ["dist/helix_v", "tar.gz", "sha256"]
  key_links:
    - from: ".goreleaser.yaml `env:` block"
      to: "zig cc on the runner PATH"
      via: "templated CC=zig cc -target <triple> per goos/goarch combination"
      pattern: "CC=zig cc -target"
    - from: "Makefile release-snapshot recipe"
      to: "command -v zig"
      via: "fail-loud guard before invoking goreleaser"
      pattern: "command -v zig"
    - from: ".goreleaser.yaml `archives:` / `signs:` blocks"
      to: "Phase 58 cosign keyless flow"
      via: "archive-name shape and sigstore.json bundle layout UNCHANGED — `helix upgrade` consumer (Phase 58 REL-01) is not affected"
      pattern: "name_template|cosign sign-blob"
---

<objective>
Flip `.goreleaser.yaml` from `CGO_ENABLED=0` to single-mode `CGO_ENABLED=1` with per-target `CC=zig cc -target <triple>` set via the canonical templated `env:` block, and add a zig-presence guard to the `Makefile`'s `release-snapshot` recipe so contributors get a clean upfront error if zig is missing.

Purpose: Wave 1 closed INT-BLOCKER-01 at the source-tree level; Wave 2 closes it at the release-pipeline level. After this wave, `make release-snapshot` produces 6 functional CGO=1 archives locally (the prior CGO=0 placeholders are gone). The Phase 58 cosign keyless flow is untouched — only the binary-build step changes.

Output:
- `.goreleaser.yaml` with the verbatim PATTERNS.md A4 target shape
- `Makefile` `release-snapshot` recipe with the verbatim PATTERNS.md A6 target shape
- A local snapshot baseline file (NOT committed) recording the first CGO=1 archive sha256s for the SUMMARY's audit trail

Wave 2 has a checkpoint at the end (Task 3) because it asks the human to capture and acknowledge the local snapshot baseline; the snapshot is run via `make release-snapshot` which is an autonomous task, but inspecting the dist/ output and recording sha256s in a non-committed notes file is best done with human-in-the-loop confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-VALIDATION.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-00-PROBE-NOTES.md
@.goreleaser.yaml
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip `.goreleaser.yaml` to CGO=1 + per-target zig cc env (Pattern A4)</name>
  <files>.goreleaser.yaml</files>
  <read_first>
    - .goreleaser.yaml (full file — lines 9-138; only lines 13-15 mutate, but the executor must read the full file to confirm Phase 58 cosign block + archives + checksum + signs are not accidentally disturbed)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md (Pattern A4 — verbatim source-of-truth excerpt for current shape AND target shape; Shared 5 — reproducibility-flag invariant)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md ("Pattern 1" / "Code Examples" — full templated env block + the goreleaser/example-zig-cgo source URL)
  </read_first>
  <action>
    In `.goreleaser.yaml`, replace ONLY lines 13-15 (the current `env:` block — `- CGO_ENABLED=0` and `- GOFLAGS=-mod=readonly`) with the verbatim target shape from PATTERNS.md A4 (also matches RESEARCH.md "Code Examples" §1 verbatim):

    ```yaml
        env:
          - CGO_ENABLED=1
          - GOFLAGS=-mod=readonly
          - >-
            {{- if eq .Os "linux" }}
              {{- if eq .Arch "amd64" }}CC=zig cc -target x86_64-linux-musl{{- end }}
              {{- if eq .Arch "arm64" }}CC=zig cc -target aarch64-linux-musl{{- end }}
            {{- else if eq .Os "windows" }}
              {{- if eq .Arch "amd64" }}CC=zig cc -target x86_64-windows-gnu{{- end }}
              {{- if eq .Arch "arm64" }}CC=zig cc -target aarch64-windows-gnu{{- end }}
            {{- else if eq .Os "darwin" }}
              {{- if eq .Arch "amd64" }}CC=zig cc -target x86_64-macos{{- end }}
              {{- if eq .Arch "arm64" }}CC=zig cc -target aarch64-macos{{- end }}
            {{- end }}
    ```

    Indentation: 4 spaces for `env:`, 6 spaces for `- ...` items (matches the existing block; copy the indentation from line 12 / line 13 in HEAD verbatim).

    DO NOT touch any other line in the file:
    - `goos:` / `goarch:` (lines 16-22) UNCHANGED
    - `flags: [-trimpath, -buildvcs=false]` (lines 23-30 incl. comment) UNCHANGED — Shared 5 invariant
    - `ldflags:` block (lines 31-35) UNCHANGED — Shared 5 invariant
    - `mod_timestamp: '{{ .CommitTimestamp }}'` (line 36) UNCHANGED — Shared 5 invariant
    - `archives:` block (Phase 58 REL-01 / `helix upgrade` artifact-shape contract) UNCHANGED
    - `checksum:` block UNCHANGED
    - `signs:` block (Phase 58 cosign keyless) UNCHANGED
    - `release:` block UNCHANGED
    - `version: 2` (line 1) UNCHANGED

    Locked from CONTEXT.md > Canonical References > Release pipeline files: `version: 2`, `project_name: helix`, 6 archives, `mod_timestamp: '{{ .CommitTimestamp }}'`, `name_template`, cosign sign block.

    Verify with `actionlint` or `goreleaser check` if available:
    ```bash
    goreleaser check 2>&1 | tee /tmp/goreleaser-check-output
    # exit 0 expected
    ```
  </action>
  <verify>
    <automated>grep -qE '^\s+- CGO_ENABLED=1$' .goreleaser.yaml && grep -qF 'CC=zig cc -target x86_64-linux-musl' .goreleaser.yaml && grep -qF 'CC=zig cc -target aarch64-linux-musl' .goreleaser.yaml && grep -qF 'CC=zig cc -target x86_64-windows-gnu' .goreleaser.yaml && grep -qF 'CC=zig cc -target aarch64-windows-gnu' .goreleaser.yaml && grep -qF 'CC=zig cc -target x86_64-macos' .goreleaser.yaml && grep -qF 'CC=zig cc -target aarch64-macos' .goreleaser.yaml && grep -qF '-trimpath' .goreleaser.yaml && grep -qF '-buildvcs=false' .goreleaser.yaml && grep -qF "mod_timestamp: '{{ .CommitTimestamp }}'" .goreleaser.yaml && ! grep -qF 'CGO_ENABLED=0' .goreleaser.yaml && goreleaser check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CC=zig cc -target' .goreleaser.yaml` returns 6 (one per target triple)
    - All 6 target strings present verbatim: `x86_64-linux-musl`, `aarch64-linux-musl`, `x86_64-windows-gnu`, `aarch64-windows-gnu`, `x86_64-macos`, `aarch64-macos`
    - `grep -qE '^\s+- CGO_ENABLED=1$' .goreleaser.yaml` matches; `! grep -qF 'CGO_ENABLED=0' .goreleaser.yaml`
    - Reproducibility flags preserved verbatim: `grep -qF '-trimpath' .goreleaser.yaml && grep -qF '-buildvcs=false' .goreleaser.yaml && grep -qF "mod_timestamp: '{{ .CommitTimestamp }}'" .goreleaser.yaml`
    - `version: 2` still on line 1: `head -1 .goreleaser.yaml | grep -qF 'version: 2'`
    - Phase 58 cosign sign block intact: `grep -qF 'cosign' .goreleaser.yaml` (cosign references preserved)
    - `goreleaser check` exits 0 (config validates)
    - `goos:` / `goarch:` arrays unchanged: `grep -A2 '^\s\+goos:' .goreleaser.yaml | grep -qF 'darwin' && grep -A3 '^\s\+goos:' .goreleaser.yaml | grep -qF 'windows'`
  </acceptance_criteria>
  <done>
    - `.goreleaser.yaml` flipped to CGO=1 with per-target zig cc CC
    - All non-`env:` blocks preserved verbatim
    - `goreleaser check` validates the config
  </done>
</task>

<task type="auto">
  <name>Task 2: Add zig-presence guard to `Makefile` `release-snapshot` recipe (Pattern A6)</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (full file — lines 50-60; the `release-snapshot` recipe and the surrounding `bench` / self-doc style)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md (Pattern A6 + Shared 3 — verbatim guard shape)
  </read_first>
  <action>
    In `Makefile`, locate the `release-snapshot:` recipe (currently lines 56-58 per PATTERNS.md). Replace it with the verbatim Pattern A6 target shape:

    ```makefile
    release-snapshot: ## Run a local goreleaser dry-run; writes archives to dist/ (overwrites; gitignored)
    	@command -v goreleaser >/dev/null 2>&1 || { \
    	  echo "goreleaser not installed; see CONTRIBUTING.md (Releasing). brew install goreleaser"; exit 1; }
    	@command -v zig >/dev/null 2>&1 || { \
    	  echo "zig not installed (required for CGO=1 cross-compile); see CONTRIBUTING.md (Releasing). brew install zig (macOS) or apt install zig (Ubuntu 22.04+)"; exit 1; }
    	goreleaser release --snapshot --clean --skip=sign
    ```

    Tabs (not spaces) for the recipe-body indentation — Make is whitespace-sensitive. The continuation backslashes match the existing goreleaser guard's exact style.

    Critical: `make build` recipe (Makefile lines 6-7 per PATTERNS.md A6) is UNCHANGED — local dev keeps using host CC. D-03: zig is required for `release-snapshot` only.

    Wave 0 already added the `release-smoke` target and updated `.PHONY`. This task touches ONLY `release-snapshot`. Do not modify `release-smoke` or `.PHONY` here (Wave 5 will finalize `release-smoke`).
  </action>
  <verify>
    <automated>grep -qF 'command -v zig >/dev/null 2>&1' Makefile && grep -qF 'zig not installed (required for CGO=1 cross-compile)' Makefile && grep -qF 'brew install zig (macOS) or apt install zig (Ubuntu 22.04+)' Makefile && grep -qF 'goreleaser release --snapshot --clean --skip=sign' Makefile && grep -E '^build:' Makefile | head -1</antomated></automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'command -v zig' Makefile` returns at least 1
    - `grep -qF 'zig not installed (required for CGO=1 cross-compile); see CONTRIBUTING.md (Releasing). brew install zig (macOS) or apt install zig (Ubuntu 22.04+)' Makefile` — verbatim hint string present
    - `grep -qF 'goreleaser release --snapshot --clean --skip=sign' Makefile` — recipe body preserved
    - `make build` recipe (lines 6-7) unchanged: `grep -A1 '^build:' Makefile | grep -qF 'go build'`
    - `make -n release-snapshot` (dry-run) prints both guards (`command -v goreleaser` and `command -v zig`) before the goreleaser invocation
    - Negative test: temporarily prepend a non-existent zig path and confirm guard fires:
      `PATH=/dev/null:$PATH make release-snapshot 2>&1 | grep -qF 'zig not installed'` exits 0 (the error message fires; recipe exits non-zero overall)
  </acceptance_criteria>
  <done>
    - Zig-presence guard installed mirroring goreleaser guard
    - `make build` unchanged
    - Negative test confirms guard fires when zig is absent from PATH
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run `make release-snapshot` locally; capture baseline; confirm 6 archives</name>
  <read_first>
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-VALIDATION.md (Build validation block)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md (Pitfall 4 — `dist-pass-1` cleanup) and (Risks > LOW > Local zig version mismatch — local vs CI parity caveat)
  </read_first>
  <files>.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SNAPSHOT-BASELINE.md</files>
  <action>See &lt;what-built&gt; below — run `make release-snapshot` then capture sha256s and archive sizes into the baseline file.</action>
  <verify>
    <automated>test -f .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SNAPSHOT-BASELINE.md &amp;&amp; test "$(grep -cE '^[0-9a-f]{64}' .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SNAPSHOT-BASELINE.md)" = "6"</automated>
  </verify>
  <done>6 archives produced; baseline captured; reviewer acknowledged.</done>
  <what-built>
    Run the local snapshot:
    ```bash
    rm -rf dist/                                # ensure no stale CGO=0 artifacts (Pitfall 4)
    make release-snapshot                       # uses local zig (likely 0.16.0; CI uses pinned 0.14.1)
    ls -la dist/*.tar.gz dist/*.zip 2>/dev/null # expect 5 .tar.gz + 1 .zip = 6 archives total per goreleaser name_template
    ```

    Capture a baseline (NOT committed — for SUMMARY audit trail only):
    ```bash
    {
      echo "# Phase 59.1 Wave 2 — first local CGO=1 snapshot baseline"
      echo "# Date: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
      echo "# Local zig version: $(zig version)"
      echo "# Note: local-vs-CI byte parity is NOT guaranteed (Risks > LOW)."
      echo ""
      echo "## Archive sizes"
      ls -la dist/helix_v*_*.{tar.gz,zip} 2>/dev/null
      echo ""
      echo "## sha256 of each archive"
      shasum -a 256 dist/helix_v*_*.{tar.gz,zip} 2>/dev/null
      echo ""
      echo "## Sanity: each binary's go version -m"
      for archive in dist/helix_v*_*.tar.gz; do
        echo "=== $archive ==="
        tar -tzf "$archive" | head -5
      done
    } > .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SNAPSHOT-BASELINE.md
    ```
  </what-built>
  <how-to-verify>
    1. `ls dist/*.tar.gz dist/*.zip 2>/dev/null | wc -l` returns exactly 6 (5 tar.gz + 1 zip per goreleaser default for windows). If goreleaser is configured with `format_overrides` such that windows is also tar.gz, expect 6 .tar.gz; either way, the count is 6.

    2. Each archive's binary is materially larger than the prior CGO=0 baseline. The CGO=0 placeholder binaries from prior runs were ~10-15 MB; CGO=1 binaries with statically linked tree-sitter + duckdb are expected to be ~25-50 MB. (The exact threshold is environment-dependent; the human reviewer confirms "looks bigger" is enough; the absolute number goes into the SUMMARY.)

    3. `cat .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SNAPSHOT-BASELINE.md` — confirm 6 sha256 lines, 6 archive sizes, the local zig version, and the human-readable timestamp are present.

    4. The Path-3 windows-arm64 archive is present and the binary inside it is functional-looking (non-empty, has the helix entry point). The `semantic_index` feature is degraded on this target by design — that's not verifiable from a structural snapshot, only at runtime on real Windows-on-ARM hardware (post-merge follow-up per VALIDATION Manual-Only Verifications row 2).

    5. Cleanup not required: `dist/` is gitignored; goreleaser's `--clean` flag wipes it on the next run anyway (Pitfall 4 — no Pass-3-style state leak).
  </how-to-verify>
  <acceptance_criteria>
    - `find dist -maxdepth 1 -name 'helix_v*' -type f | wc -l` returns at least 6
    - The baseline file `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SNAPSHOT-BASELINE.md` exists and contains 6 sha256 lines (`grep -cE '^[0-9a-f]{64}' .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SNAPSHOT-BASELINE.md` returns 6)
    - Baseline file records `zig version` output (audit trail for local-vs-CI version delta)
    - Baseline file records archive sizes (sanity-check that CGO=1 archives are materially larger than the deleted CGO=0 placeholders)
    - `dist/helix_v*_Windows_arm64.*` archive exists (D-14 Path-3 stub built; the `semantic_index` runtime behavior is verified post-merge per VALIDATION Manual-Only Verifications)
    - The reviewer types `snapshot ok: <archive count>, <total size>` to acknowledge
  </acceptance_criteria>
  <resume-signal>Type "snapshot ok: 6 archives" once `dist/` contains 6 archives and the baseline file is recorded</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree → goreleaser → zig cc → C linker | New supply-chain hop: zig cc is the C frontend. Mitigated in Wave 3 via SHA-pinned setup-zig action; locally users install via Homebrew/apt (best-effort) |
| `.goreleaser.yaml` → release artifact shape | Phase 58 REL-01 contract: archive name pattern + sigstore.json bundle layout MUST NOT change — `helix upgrade` consumer depends on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59.1-W2-01 | Tampering | Local zig binary used in `make release-snapshot` (contributor's host) | accept | RESEARCH Risks > LOW: local-vs-CI byte parity is not guaranteed; CONTRIBUTING.md will document this. The CI-side reproducibility gate (Wave 3 + 5) is the authority |
| T-59.1-W2-02 | Tampering | `.goreleaser.yaml` per-target CC env block silently fails on a target (e.g. typo in target triple) | mitigate | `goreleaser check` validates the config statically; `make release-snapshot` exercises every target locally; Wave 3 + 5 reproducibility gate is the second backstop |
| T-59.1-W2-03 | DoS | Phase 58 cosign signing block accidentally edited; release publishing breaks | mitigate | Task 1 acceptance criteria explicitly checks `grep -qF 'cosign' .goreleaser.yaml`; the `signs:` and `archives:` blocks are off-limits in the diff |
| T-59.1-W2-04 | Supply-chain | Local zig version drift introduces non-determinism | accept | D-09 / D-10: only the CI Pass-1 ≡ Pass-2 gate is binding; local snapshot is for shape-validation only; CONTRIBUTING.md will document this |
| T-59.1-W2-05 | DoS | windows/arm64 build link-fails because the D-14 stub method-set drifted from `duckdb.go` | mitigate | Wave 1 Task 6 cross-compile gate caught this BEFORE Wave 2; if it surfaces here despite that, escalate — Wave 1 needs a fix-pass |
</threat_model>

<verification>
- `goreleaser check` exits 0
- `make release-snapshot` exits 0; produces 6 archives in `dist/`
- `make release-snapshot` recipe fails-loud upfront if zig is removed from PATH
- `make build` is unchanged and continues to use host CC
- Baseline file recorded with archive sha256s and local zig version
- All Phase 58 cosign signing config lines preserved verbatim
- `helix upgrade` artifact-name shape (`helix_v{Version}_{Os}_{Arch}.tar.gz`) is unchanged
</verification>

<success_criteria>
- `.goreleaser.yaml` flipped to CGO=1 with per-target zig cc CC
- Reproducibility flags + cosign sign block + archives block preserved verbatim
- `make release-snapshot` produces 6 archives locally
- `Makefile` `release-snapshot` recipe fails-loud if zig is absent
- `make build` unchanged
- Local snapshot baseline captured (uncommitted); recorded in plan summary
- Wave 3 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SUMMARY.md` recording:
- `.goreleaser.yaml` diff hunk (the env: block before/after)
- `Makefile` diff hunk (the new zig guard)
- Local snapshot result: archive count, total size, local zig version
- A copy of the baseline file's sha256 lines (for the audit trail)
- Confirmation that `goreleaser check` passes and `make build` is unchanged
</output>
