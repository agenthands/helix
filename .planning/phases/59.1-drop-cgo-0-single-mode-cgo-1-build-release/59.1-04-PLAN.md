---
phase: 59.1-drop-cgo-0-single-mode-cgo-1-build-release
plan: 04
type: execute
wave: 4
depends_on: [59.1-01]
files_modified:
  - CLAUDE.md
  - CONTRIBUTING.md
  - README.md
  - CHANGELOG.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md
  - .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md
  - .planning/milestones/v1.10-ROADMAP.md
autonomous: true
requirements: [INT-BLOCKER-01]
tags: [docs, doc-sweep, project-md, changelog, deferred-items, wave-4, d-11, d-12, d-13]
must_haves:
  truths:
    - "User-facing single-binary framing is preserved verbatim (D-12): PROJECT.md Core Value, README hero, CLAUDE.md intro all keep `single self-contained binary; no Python, Docker, or runtime dependencies` style"
    - "Build-side framing is flipped (D-12): CLAUDE.md Technology Stack and CONTRIBUTING.md Releasing now say CGO=1 build / zig cc cross-compile / runtime statically linked"
    - "PROJECT.md Key Decisions table (D-13): NEW row for Phase 59.1 added; existing 51.1 row gets `~~strikethrough~~` of the Outcome cell + ` — superseded by Phase 59.1 (v1.10)` tail"
    - "CHANGELOG.md (D-11): NEW v1.10 entry framed as `build pipeline: single-mode CGO=1, replaces Phase 51.1 D-02 stub policy` with sub-sections for Build pipeline / Known Issues (windows-arm64) / Removed (the 12 stubs)"
    - "deferred-items.md (D-11): DEF-51-03 closed with pointer to 59.1 SUMMARY; NEW DEF-51-04 entry tracks windows-arm64 duckdb-go-bindings restoration"
    - "Phase 51.1 SUMMARY.md gets a one-line back-pointer to 59.1 as the supersede event"
    - "v1.10-ROADMAP.md adds a Phase 59.1 entry between Phase 59 and Phase 60 (currently absent there per RESEARCH A7)"
    - "Validation gate: `! grep -in 'CGO=0\\|no CGO\\|CGO_ENABLED=0' README.md CLAUDE.md CONTRIBUTING.md` returns zero hits (current-tense user-facing prose; historical mentions in CHANGELOG and `.planning/` are allowed and intentional)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Tech Stack section flipped to CGO=1 build framing; daemon-bootstrap step-6a callout removed; user-facing project intro UNCHANGED"
      contains: ["CGO=1", "zig cc", "statically linked", "single binary"]
    - path: "CONTRIBUTING.md"
      provides: "Releasing section gains zig install hint + new build flow + local-vs-CI parity caveat"
      contains: ["zig", "0.14.1", "release-snapshot", "local goreleaser dry-run validates build matrix shape, not byte-for-byte CI parity"]
    - path: "README.md"
      provides: "Any current-tense `CGO=0` / `no CGO` prose flipped or removed; user-facing single-binary message unchanged"
      contains: ["single self-contained binary"]
    - path: "CHANGELOG.md"
      provides: "NEW v1.10 entry per Pattern A10"
      contains: ["v1.10", "Single-mode CGO=1", "mlugg/setup-zig@v2.2.1", "0.14.1", "12 stub files", "windows-arm64 semantic_index unavailable", "DEF-51-04"]
    - path: ".planning/PROJECT.md"
      provides: "NEW Phase 59.1 row in Key Decisions; 51.1 row gets ~~strikethrough~~ of the Outcome cell"
      contains: ["Phase 59.1", "INT-BLOCKER-01 from v1.10-MILESTONE-AUDIT.md", "12 stubs deleted", "~~", "superseded by Phase 59.1"]
    - path: ".planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md"
      provides: "DEF-51-03 closed via 59.1 (Path 3); NEW DEF-51-04 tracks windows-arm64 restoration"
      contains: ["DEF-51-03: Re-enable CGO in goreleaser", "RESOLVED via Phase 59.1", "DEF-51-04: windows-arm64 semantic_index unavailable", "duckdb-go-bindings/lib/windows-arm64", "OPEN"]
    - path: ".planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md"
      provides: "Cross-reference back-pointer to 59.1 as the supersede event"
      contains: ["Superseded by Phase 59.1", "v1.10"]
    - path: ".planning/milestones/v1.10-ROADMAP.md"
      provides: "Phase 59.1 entry inserted between Phase 59 and Phase 60"
      contains: ["Phase 59.1", "drop-cgo-0-single-mode-cgo-1-build-release", "INT-BLOCKER-01"]
  key_links:
    - from: ".planning/PROJECT.md Key Decisions row for Phase 51.1"
      to: ".planning/PROJECT.md Key Decisions row for Phase 59.1"
      via: "strikethrough Outcome cell + tail `— superseded by Phase 59.1 (v1.10)`; future readers see the decision-history trail in one place"
      pattern: "~~.*~~ — superseded by Phase 59.1"
    - from: ".planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md DEF-51-03"
      to: ".planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/"
      via: "RESOLVED status pointer to PLAN/SUMMARY"
      pattern: "RESOLVED via Phase 59.1"
    - from: "CHANGELOG.md v1.10 entry"
      to: "DEF-51-04 (windows-arm64)"
      via: "Known Issues subsection cross-references the deferred-item ID"
      pattern: "DEF-51-04|windows-arm64 semantic_index unavailable"
---

<objective>
Sweep all documentation to reflect the single-mode CGO=1 invariant landed in Waves 1-3. The sweep covers user-facing docs (CLAUDE.md / CONTRIBUTING.md / README.md / CHANGELOG.md), planning docs (PROJECT.md / REQUIREMENTS.md / v1.10-ROADMAP.md), and milestone-history docs (51-deferred-items.md / 51.1-SUMMARY.md). The user/builder seam reword (D-12) keeps the user-facing single-binary message intact while flipping the build-side framing.

Purpose: Without this wave, the source-tree truth (Wave 1) and the release-pipeline truth (Waves 2-3) would be out of sync with the prose users and future maintainers read. PROJECT.md Key Decisions becomes the canonical decision-history trail (D-13: 51.1 strikethrough + 59.1 new row). The deferred-items.md DEF-51-03 closure plus the new DEF-51-04 entry preserves the "we know about windows-arm64; here's the restoration trigger" honesty.

Output: 9 files updated. Validation: `! grep -in 'CGO=0\\|no CGO\\|CGO_ENABLED=0' README.md CLAUDE.md CONTRIBUTING.md` returns zero hits.

This wave depends on Wave 1 (source-tree state must reflect what the prose claims). It is INDEPENDENT of Waves 2 and 3 (which mutate `.goreleaser.yaml` / `release.yml`); the prose can be written based on the locked decisions D-01..D-14 without waiting for the CI gate to pass — the gate's pass/fail is recorded in Wave 5's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-VALIDATION.md
@CLAUDE.md
@CONTRIBUTING.md
@README.md
@CHANGELOG.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md
@.planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md
@.planning/milestones/v1.10-ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md (Technology Stack + Daemon Bootstrap) and README.md (current-tense CGO=0 prose) per D-11/D-12</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>
    - CLAUDE.md (full file — Technology Stack section + Daemon Bootstrap section + any "Project" / "Constraints" prose mentioning CGO state)
    - README.md (full file — hero / install instructions / build instructions; flag any current-tense `CGO=0` / `no CGO` claims)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md (D-11 + D-12 — verbatim reword guidance)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md (Wave 4 doc-sweep table for analogs)
  </read_first>
  <action>
    **CLAUDE.md edits (D-12 — user/builder seam reword):**

    1. **Technology Stack section.** Find the bullet that currently says something like "modernc.org/sqlite (CGO-free, for FTS5 memory search)" — KEEP this unchanged (modernc still works under CGO=1; D-11). Find any bullet or sentence that frames the project as "no CGO required" or "CGO=0 supported"; replace with build-side prose: "Build requires `CGO_ENABLED=1`; CI uses `zig cc` 0.14.1 as a hermetic cross-compiler for all 6 archives (linux/{amd64,arm64} + windows/{amd64,arm64} + darwin/{amd64,arm64}). Runtime is statically-linked single binary with no external dependencies."

    2. **Daemon Bootstrap section.** Find any callout referencing step 6a / `treesitter.Available` / "daemon refuses to start under CGO_ENABLED=0". DELETE the callout (the behavior is gone — Wave 1 deleted step 6a). The middleware install order section (steps 14/14b/14c LIFO) is UNCHANGED — do not touch it.

    3. **Constraints section.** If any line says "build with CGO=0 supported" or similar, flip to "Build requires CGO=1; runtime is a single statically-linked binary." Do NOT touch the user-facing single-binary message in the Project intro.

    **README.md edits (D-12 — keep user-facing prose, flip any installer/build prose):**

    1. **Hero / value prop / install instructions.** KEEP: "single self-contained binary; no Python, Docker, or runtime dependencies" — this is the user-facing message and it remains TRUE post-phase. Do NOT reword.

    2. **Build instructions** (if any). If README.md has a "Building from source" or "Contributing" section that references CGO state, flip to: "Build requires `CGO_ENABLED=1`. Local development: `make build` uses your host C compiler. Releases: `make release-snapshot` requires `zig` 0.14.1 on PATH; CI installs it via the pinned `mlugg/setup-zig@v2.2.1` action."

    3. **Stale minisign mention** (RESEARCH §"State of the Art" — README.md line 65 reportedly says "with SHA-256 checksums and minisign signatures"). This is a Phase 58 stale (cosign keyless replaced minisign). FIX OPPORTUNISTICALLY if it's a one-liner: replace `minisign signatures` with `cosign keyless signatures (Sigstore)`. If the surrounding paragraph requires deeper restructuring, flag in the SUMMARY for a follow-up phase — do not block 59.1 on it.

    Validation:
    ```bash
    # No current-tense user-facing CGO=0 / no-CGO prose remains
    ! grep -in 'CGO=0\|no CGO\|CGO_ENABLED=0\|without CGO' README.md CLAUDE.md
    # Single-binary user-facing message preserved
    grep -qF 'single self-contained binary' README.md || grep -qF 'single binary' README.md
    grep -qF 'single binary' CLAUDE.md
    ```
  </action>
  <verify>
    <automated>! grep -in 'CGO=0\|no CGO\|CGO_ENABLED=0\|without CGO' README.md CLAUDE.md && grep -qE 'CGO_ENABLED=1|CGO=1' CLAUDE.md && grep -qF 'zig' CLAUDE.md && ! grep -F 'treesitter.Available' CLAUDE.md && ! grep -F 'daemon refuses to start under CGO_ENABLED=0' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -inE 'CGO=0|no CGO|CGO_ENABLED=0|without CGO' README.md CLAUDE.md` (zero hits — current-tense prose flipped)
    - `grep -qE 'CGO_ENABLED=1|CGO=1' CLAUDE.md` (build framing landed)
    - `grep -qF 'zig' CLAUDE.md` (zig cc mentioned)
    - `grep -qF 'statically' CLAUDE.md` (static-link framing landed)
    - `! grep -F 'treesitter.Available' CLAUDE.md` (step-6a callout retired)
    - `! grep -F 'daemon refuses to start under CGO_ENABLED=0' CLAUDE.md`
    - User-facing single-binary message preserved: `grep -qF 'single binary' CLAUDE.md` AND (`grep -qF 'single self-contained binary' README.md` OR `grep -qF 'single binary' README.md`)
    - Middleware install order section in CLAUDE.md unchanged (the LIFO steps 14/14b/14c invariant): `grep -qF 'LazyInitMiddleware' CLAUDE.md && grep -qF 'TelemetryMiddleware' CLAUDE.md && grep -qF 'ProfileFilterMiddleware' CLAUDE.md && grep -qF 'SuggestionMiddleware' CLAUDE.md`
  </acceptance_criteria>
  <done>
    - CLAUDE.md Tech Stack flipped; step-6a callout retired; middleware order untouched
    - README.md user-facing prose preserved; build instructions flipped if present
    - User-facing single-binary message intact in both files
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CONTRIBUTING.md "Releasing" section (D-11) — zig install + build flow + local-vs-CI parity caveat</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (full file — focus on the "Releasing" section)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md (D-03, D-11)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md (Risks > LOW > Local zig version mismatch — verbatim parity caveat phrasing)
  </read_first>
  <action>
    In CONTRIBUTING.md "Releasing" section, add (or update existing prose to include) the following:

    1. **Prerequisite:** zig is required for `make release-snapshot`. Install:
       - macOS: `brew install zig`
       - Ubuntu 22.04+: `apt install zig` (ships ≥ 0.10; for byte-parity with CI install 0.14.1 directly: `curl -sSL https://ziglang.org/download/0.14.1/zig-linux-x86_64-0.14.1.tar.xz | tar -xJ`)
       - Linux x86_64 (manual): direct download from https://ziglang.org/download/0.14.1/

    2. **Build flow (post-59.1):**
       - `make build` — local development, uses your host C compiler. No zig dependency.
       - `make release-snapshot` — local goreleaser dry-run. Builds 6 CGO=1 archives via `zig cc -target <triple>` per goos/goarch. Writes to `dist/` (gitignored).
       - `make release-smoke` — runtime smoke test of the linux-amd64 archive (Wave 5 finalizes this).

    3. **Reproducibility-gate notes:** Two concurrent CI passes (Pass-1 and Pass-2) must produce byte-identical archives. The gate uses zig 0.14.1 pinned via `mlugg/setup-zig@v2.2.1`. If a future change introduces non-determinism, the WR-07 forensic dump (per-file sha256, tar entry metadata, gzip header bytes, `go version -m` diff) is the diagnostic. Do NOT soften the gate (CONTEXT.md D-10) — investigate via the forensic dump and add `-fdebug-prefix-map=$PWD=.` or `-Wl,--build-id=none` flags if necessary.

    4. **Local-vs-CI parity caveat (verbatim from RESEARCH.md Risks > LOW > Local zig version mismatch):** "local goreleaser dry-run validates build matrix shape, not byte-for-byte CI parity." Contributors who run a local snapshot may produce sha256-different archives from CI because their host zig version (e.g. 0.16.0 from Homebrew) is not the CI-pinned 0.14.1. This is acceptable — the CI-side Pass-1 ≡ Pass-2 gate is the authority on byte parity, not local-vs-CI parity.

    5. **Pitfall 4 note:** If you previously ran `make release-snapshot` under CGO=0 (pre-59.1), run `rm -rf dist/` before your first post-phase snapshot so you don't compare against stale CGO=0 archives. (Phase 59.1 specific; remove after a few months.)

    6. **DELETE:** any prose that says CGO=0 is supported, that the daemon refuses to start under CGO=0, or that `_nocgo.go` stubs exist. Those are all false post-phase.
  </action>
  <verify>
    <automated>grep -qF 'zig' CONTRIBUTING.md && grep -qF '0.14.1' CONTRIBUTING.md && grep -qF 'release-snapshot' CONTRIBUTING.md && grep -qF 'local goreleaser dry-run validates build matrix shape, not byte-for-byte CI parity' CONTRIBUTING.md && ! grep -inE 'CGO=0|no CGO|CGO_ENABLED=0|_nocgo\.go|daemon refuses to start' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'zig' CONTRIBUTING.md` AND `grep -qF '0.14.1' CONTRIBUTING.md` (zig 0.14.1 named)
    - `grep -qF 'mlugg/setup-zig@v2.2.1' CONTRIBUTING.md` (pinned action named)
    - `grep -qF 'brew install zig' CONTRIBUTING.md` (macOS install hint)
    - `grep -qF 'release-snapshot' CONTRIBUTING.md` AND `grep -qF 'release-smoke' CONTRIBUTING.md` (both targets named)
    - `grep -qF 'local goreleaser dry-run validates build matrix shape, not byte-for-byte CI parity' CONTRIBUTING.md` (verbatim parity caveat)
    - `grep -qF 'WR-07' CONTRIBUTING.md` OR `grep -qF 'forensic dump' CONTRIBUTING.md` (D-10 forensic-dump remediation path mentioned)
    - `! grep -inE 'CGO=0|no CGO|CGO_ENABLED=0|_nocgo\.go|daemon refuses to start' CONTRIBUTING.md` (no stale prose)
  </acceptance_criteria>
  <done>
    - Releasing section names zig 0.14.1, install commands, and the release-snapshot / release-smoke flow
    - Local-vs-CI parity caveat present verbatim
    - WR-07 forensic-dump remediation path referenced
    - No stale CGO=0 / `_nocgo.go` prose
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CHANGELOG.md v1.10 entry (Pattern A10) per D-11</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-55 — the existing v1.9 entry is the verbatim shape template; current top-of-file marker)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md (Pattern A10 — verbatim entry text to insert)
  </read_first>
  <action>
    Insert a NEW v1.10 entry between the file's intro (lines 1-4) and the existing `## v1.9 — Polish & Infra` heading (line 5). The entry text is taken verbatim from PATTERNS.md A10:

    ```markdown
    ## v1.10 — Release pipeline single-mode CGO=1 (TBD)

    ### Build pipeline

    - **Single-mode CGO=1 release.** `.goreleaser.yaml` flips from `CGO_ENABLED=0` to `CGO_ENABLED=1` for all 6 build targets; per-target `CC=zig cc -target <triple>` is set via templated `env:` block. Phase 51.1 D-02 (daemon refuses CGO=0) is superseded — the binary cannot be built without CGO; `go build` fails at the import boundary instead of the daemon refusing at runtime.
    - **`mlugg/setup-zig@v2.2.1` pinned by SHA in release.yml.** Mirrors WR-04 / WR-07 hardening posture used by `goreleaser-action` and `cosign-installer`. zig 0.14.1 is the pinned toolchain version.
    - **All `_nocgo.go` stubs and `//go:build cgo` tags removed.** 12 stub files deleted, 24 production+test files lose their CGO build tag, `treesitter.Available` constant retired, daemon step-6a refusal block deleted.
    - **Reproducibility gate preserved.** Pass-1 ≡ Pass-2 byte-identical archives gate the real release pass under CGO=1; the WR-07 forensic dump is the divergence-triage tool.

    ### Known Issues

    - **windows-arm64 semantic_index unavailable.** `duckdb-go-bindings/lib/windows-arm64` does not exist upstream; the windows-arm64 archive ships with `internal/semantic/store` stubbed via a platform-tagged build constraint. Other tools (tree-sitter, RepoMap, fuzzy edit) work normally on windows-arm64. Tracked as DEF-51-04.

    ### Removed

    - Phase 51.1's `_nocgo.go` stub apparatus (12 files, ~250 LOC). The CGO=0 build path is gone; future phases that touch `internal/semantic/extract` / `internal/semantic/store` / `internal/treesitter` no longer need to maintain CGO=0 stub mirrors.

    ```

    Date placeholder is `(TBD)` per CONTEXT.md `<deferred>` — release-cut timing is a separate decision after the reproducibility gate passes (Wave 5).

    Insertion point: between the file's intro/preamble and the v1.9 heading. Verify by reading `head -10 CHANGELOG.md` after the edit — the v1.10 heading should appear before the v1.9 heading.
  </action>
  <verify>
    <automated>grep -qF '## v1.10 — Release pipeline single-mode CGO=1' CHANGELOG.md && grep -qF 'Single-mode CGO=1 release' CHANGELOG.md && grep -qF 'mlugg/setup-zig@v2.2.1' CHANGELOG.md && grep -qF '0.14.1' CHANGELOG.md && grep -qF 'windows-arm64 semantic_index unavailable' CHANGELOG.md && grep -qF 'DEF-51-04' CHANGELOG.md && awk '/## v1\.10/{a=NR} /## v1\.9/{b=NR} END{exit !(a>0 && b>0 && a<b)}' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## v1.10 — Release pipeline single-mode CGO=1' CHANGELOG.md` returns 1
    - `grep -qF 'mlugg/setup-zig@v2.2.1' CHANGELOG.md`
    - `grep -qF '0.14.1' CHANGELOG.md`
    - `grep -qF '12 stub files deleted' CHANGELOG.md` AND `grep -qF '24 production+test files' CHANGELOG.md`
    - `grep -qF 'windows-arm64 semantic_index unavailable' CHANGELOG.md`
    - `grep -qF 'DEF-51-04' CHANGELOG.md`
    - `grep -qF 'Pass-1 ≡ Pass-2' CHANGELOG.md` OR `grep -qF 'byte-identical' CHANGELOG.md`
    - v1.10 heading precedes v1.9 heading: `awk '/## v1\.10/{a=NR} /## v1\.9/{b=NR} END{exit !(a>0 && b>0 && a<b)}' CHANGELOG.md` exits 0
    - `grep -c '^## v1\.9 — Polish & Infra' CHANGELOG.md` returns 1 (existing v1.9 heading preserved)
  </acceptance_criteria>
  <done>
    - v1.10 entry inserted at top of CHANGELOG (after intro, before v1.9)
    - All four sub-sections (Build pipeline / Known Issues / Removed) present
    - DEF-51-04 cross-reference present
  </done>
</task>

<task type="auto">
  <name>Task 4: Update PROJECT.md Key Decisions table (Pattern A8) per D-13</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full file — Key Decisions table starts around line 180; the 51.1 row to amend is around line 219 per PATTERNS.md A8)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md (Pattern A8 — verbatim source-of-truth excerpt + target shape)
  </read_first>
  <action>
    Two edits to the Key Decisions table:

    **Edit 1 — Strikethrough the 51.1 row's Outcome cell** (PATTERNS.md A8 source-of-truth at line 219):

    Find the existing line:
    ```
    | Phase 51.1 emergent: CGO=0 build path via `//go:build cgo` stubs (v1.9) | DEF-51-01 blocker — goreleaser cross-compile fails when treesitter requires CGO | ✓ Good — daemon refuses CGO=0 with remediation; CGO=1 byte-identical |
    ```

    Replace with the target shape:
    ```
    | Phase 51.1 emergent: CGO=0 build path via `//go:build cgo` stubs (v1.9) | DEF-51-01 blocker — goreleaser cross-compile fails when treesitter requires CGO | ~~✓ Good — daemon refuses CGO=0 with remediation; CGO=1 byte-identical~~ — superseded by Phase 59.1 (v1.10) |
    ```

    Critical: apply `~~ ~~` ONLY to the Outcome cell content. The Decision (left) and Rationale (middle) cells stay plain so the decision-history is searchable. The trailing ` — superseded by Phase 59.1 (v1.10)` tail goes OUTSIDE the strikethrough.

    **Edit 2 — Insert NEW row for Phase 59.1** (after the 51.1 row, before the next existing row in the table):

    ```
    | Phase 59.1: drop CGO=0 stubs, single-mode CGO=1 release (v1.10) | INT-BLOCKER-01 from v1.10-MILESTONE-AUDIT.md — stub apparatus added cost without value once goreleaser pinned CGO=0 | ✓ Good — 12 stubs deleted; CGO=1 release pipeline via zig cc; reproducibility-gate preserved |
    ```

    The columns must align with the existing table header (Decision | Rationale | Outcome). Verify by reading the table header line (around line 180) before editing.
  </action>
  <verify>
    <automated>grep -qF '~~✓ Good — daemon refuses CGO=0 with remediation; CGO=1 byte-identical~~ — superseded by Phase 59.1 (v1.10)' .planning/PROJECT.md && grep -qF 'Phase 59.1: drop CGO=0 stubs, single-mode CGO=1 release' .planning/PROJECT.md && grep -qF 'INT-BLOCKER-01 from v1.10-MILESTONE-AUDIT.md' .planning/PROJECT.md && grep -qF '12 stubs deleted' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '~~✓ Good — daemon refuses CGO=0 with remediation; CGO=1 byte-identical~~' .planning/PROJECT.md` returns 1 (strikethrough applied to exactly one cell)
    - `grep -qF '— superseded by Phase 59.1 (v1.10)' .planning/PROJECT.md` (tail outside the strikethrough)
    - `grep -c 'Phase 59.1: drop CGO=0 stubs, single-mode CGO=1 release' .planning/PROJECT.md` returns 1 (new row added once)
    - `grep -qF 'INT-BLOCKER-01 from v1.10-MILESTONE-AUDIT.md' .planning/PROJECT.md`
    - The 51.1 row's Decision cell is unchanged: `grep -qF 'Phase 51.1 emergent: CGO=0 build path via' .planning/PROJECT.md`
    - User-facing prose at top of PROJECT.md (Core Value) is unchanged — `grep -qF 'single self-contained binary' .planning/PROJECT.md` if that's the existing user-facing phrasing
  </acceptance_criteria>
  <done>
    - 51.1 Outcome cell struck through with supersede tail
    - Phase 59.1 row added with INT-BLOCKER-01 cite + 12-stub-delete framing
    - Table column alignment preserved
  </done>
</task>

<task type="auto">
  <name>Task 5: Close DEF-51-03 + add NEW DEF-51-04 (Pattern A9) in deferred-items.md</name>
  <files>.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md</files>
  <read_first>
    - .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md (full file — focus on existing DEF-51-02 RESOLVED block at line 127 per PATTERNS.md A9, and the existing DEF-51-03 candidate placeholder)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md (Pattern A9 — verbatim target shape for the two entries)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md (D-14 windows-arm64 path 3 lock)
  </read_first>
  <action>
    Append the two blocks below to `.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`. Use the exact verbatim text from PATTERNS.md A9.

    **Block 1 — DEF-51-03 closure:**

    ```markdown
    ## DEF-51-03: Re-enable CGO in goreleaser (Path 3, follow-up to DEF-51-02)

    **Status:** RESOLVED via Phase 59.1 (v1.10). See
    `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/` for
    PLAN, RESEARCH, and SUMMARY. `.goreleaser.yaml` now sets
    `CGO_ENABLED=1` with per-target `CC=zig cc -target ...` (templated env);
    `release.yml` adds a pinned `mlugg/setup-zig@v2.2.1` step before
    goreleaser; the Pass-1 ≡ Pass-2 reproducibility gate continues to hold
    under CGO=1. All `_nocgo.go` stubs and `//go:build cgo` tags are
    deleted; the binary cannot be built under `CGO_ENABLED=0`.
    ```

    **Block 2 — NEW DEF-51-04 (windows-arm64 restoration trigger):**

    ```markdown
    ## DEF-51-04: windows-arm64 semantic_index unavailable until duckdb-go-bindings ships lib/windows-arm64

    **Severity:** LOW — affects one of six release archives, only on the `semantic_index` feature path (other tools work).

    **What:** `github.com/duckdb/duckdb-go-bindings/lib/` does not ship
    `lib/windows-arm64` (verified 2026-05-04 against
    github.com/duckdb/duckdb-go-bindings). Phase 59.1 ships a
    platform-tagged stub `internal/semantic/store/duckdb_winarm64.go`
    returning `serr.Unsupported` on that target only (D-14 Path 3). The
    6-archive matrix is preserved; `semantic_index` is gracefully
    unavailable on windows-arm64.

    **Restoration trigger:** When upstream duckdb-go-bindings publishes
    `lib/windows-arm64`, a follow-up phase deletes
    `internal/semantic/store/duckdb_winarm64.go` and removes the
    `//go:build !(windows && arm64)` constraint from `duckdb.go`.

    **Status:** OPEN.
    ```

    If the existing file already has a placeholder/candidate block for DEF-51-03 with a different status, REPLACE the body of that block with the RESOLVED text above (preserving the section heading position). DEF-51-04 is fully NEW — append it after the DEF-51-03 RESOLVED block.
  </action>
  <verify>
    <automated>grep -qF 'DEF-51-03: Re-enable CGO in goreleaser' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'RESOLVED via Phase 59.1' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'DEF-51-04: windows-arm64 semantic_index unavailable' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'duckdb-go-bindings/lib/windows-arm64' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'platform-tagged stub' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF '**Status:** OPEN' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DEF-51-03: Re-enable CGO in goreleaser' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` returns 1
    - `grep -qF 'RESOLVED via Phase 59.1' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`
    - `grep -c 'DEF-51-04: windows-arm64 semantic_index unavailable' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` returns 1
    - `grep -qF 'duckdb-go-bindings/lib/windows-arm64' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`
    - `grep -qF '**Status:** OPEN' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` (DEF-51-04 is OPEN)
    - `grep -qF 'serr.Unsupported' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` (the platform-stub behavior named)
    - `grep -qF 'D-14 Path 3' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`
    - Existing DEF-51-02 RESOLVED block is unchanged: `grep -qF 'RESOLVED via Phase 51.1' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`
  </acceptance_criteria>
  <done>
    - DEF-51-03 closed with pointer to 59.1 PLAN/SUMMARY
    - DEF-51-04 created with restoration trigger
    - DEF-51-02 (Phase 51.1) RESOLVED block preserved
  </done>
</task>

<task type="auto">
  <name>Task 6: Add 51.1 SUMMARY back-pointer + add Phase 59.1 to v1.10-ROADMAP.md + update REQUIREMENTS.md</name>
  <files>
    .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md
    .planning/milestones/v1.10-ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md (current head)
    - .planning/milestones/v1.10-ROADMAP.md (full file — find the Phase 59 / Phase 60 entries; insert between them)
    - .planning/REQUIREMENTS.md (search for any REQ that references CGO=0 stub policy / Phase 51.1 D-02 / treesitter.Available — those need flipping or striking)
  </read_first>
  <action>
    **Edit 1 — Phase 51.1 SUMMARY.md back-pointer:**

    At the top of the file (immediately after the existing frontmatter / title), add a one-line callout:

    ```markdown
    > **Superseded by Phase 59.1 (v1.10).** Phase 59.1 inverts the D-02 invariant: the daemon no longer refuses to start under CGO_ENABLED=0 because `CGO_ENABLED=0 go build ./cmd/helix` no longer compiles. All 12 `_nocgo.go` stubs and the `treesitter.Available` constant are deleted. See `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/`.
    ```

    Position: at the very top of the body, after the H1 heading. Do NOT modify any of the existing body content — the historical record stands.

    **Edit 2 — v1.10-ROADMAP.md insertion (RESEARCH A7):**

    Find the Phase 59 entry and the Phase 60 entry in `.planning/milestones/v1.10-ROADMAP.md`. Insert a new Phase 59.1 entry between them, matching the format of adjacent entries. Suggested text:

    ```markdown
    ### Phase 59.1: drop-cgo-0-single-mode-cgo-1-build-release (URGENT, inserted)

    **Goal:** Flip Phase 51.1 D-02. Single-mode CGO=1 build & release; close INT-BLOCKER-01 from v1.10-MILESTONE-AUDIT.md by deleting the CGO=0 stub apparatus rather than patching the missing stub.

    **Plans:** 5 plans (waves 0-5)
    - [ ] 59.1-00-PLAN.md — Wave 0 pre-execution probes (darwin zig cc, release-smoke target, D-14 lock)
    - [ ] 59.1-01-PLAN.md — Wave 1 stub deletion + tag strip + Path-3 platform stub + daemon step-6a removal
    - [ ] 59.1-02-PLAN.md — Wave 2 .goreleaser.yaml CGO=1 flip + Makefile zig guard
    - [ ] 59.1-03-PLAN.md — Wave 3 release.yml setup-zig step (mlugg/setup-zig@v2.2.1)
    - [ ] 59.1-04-PLAN.md — Wave 4 doc sweep (CLAUDE/README/CONTRIBUTING/CHANGELOG/PROJECT/REQUIREMENTS/deferred-items/v1.10-ROADMAP)
    - [ ] 59.1-05-PLAN.md — Wave 5 release-smoke + reproducibility verification (HARD GATE)

    **Requirements:** [INT-BLOCKER-01]

    **Status:** Inserted between Phase 59 and Phase 60 to prevent further `_nocgo.go` accumulation in subsequent v1.10 phases.
    ```

    Adjust formatting to match adjacent entries' exact style (heading depth, bullet style, etc.).

    **Edit 3 — REQUIREMENTS.md sweep:**

    ```bash
    grep -niE 'CGO=0|no CGO|CGO_ENABLED=0|treesitter\.Available|_nocgo\.go|stub policy|Phase 51\.1 D-02' .planning/REQUIREMENTS.md
    ```

    For each hit:
    - If the REQ describes a behavior that is now flipped (e.g. "daemon refuses to start under CGO_ENABLED=0"), strike it (`~~ ~~`) and append `— superseded by Phase 59.1 (v1.10)`.
    - If the REQ is purely descriptive of the historical state (e.g. notes in a section header), update tense to past or add a "(historical, superseded by Phase 59.1)" callout.
    - If the REQ is unrelated to CGO state (false positive), leave it.

    If REQUIREMENTS.md has no matching content, document that fact in the SUMMARY (no edits needed).
  </action>
  <verify>
    <automated>grep -qF 'Superseded by Phase 59.1 (v1.10)' .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md && grep -qF 'Phase 59.1: drop-cgo-0-single-mode-cgo-1-build-release' .planning/milestones/v1.10-ROADMAP.md && grep -qF 'INT-BLOCKER-01' .planning/milestones/v1.10-ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Superseded by Phase 59.1' .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md` returns at least 1
    - `grep -c 'Phase 59.1: drop-cgo-0-single-mode-cgo-1-build-release' .planning/milestones/v1.10-ROADMAP.md` returns at least 1
    - `grep -qF 'INT-BLOCKER-01' .planning/milestones/v1.10-ROADMAP.md`
    - Phase 59.1 entry positioned between Phase 59 and Phase 60: `awk '/Phase 59:/{a=NR} /Phase 59\.1:/{b=NR} /Phase 60:/{c=NR} END{exit !(a>0 && b>0 && c>0 && a<b && b<c)}' .planning/milestones/v1.10-ROADMAP.md` exits 0 (or matches whatever exact heading text adjacent entries use)
    - REQUIREMENTS.md sweep: either zero current-tense CGO=0 references remain, OR each remaining reference has a `~~ ~~` strikethrough OR `(historical, superseded by Phase 59.1)` callout. The SUMMARY documents which path was taken.
  </acceptance_criteria>
  <done>
    - 51.1 SUMMARY has back-pointer
    - v1.10-ROADMAP.md has Phase 59.1 entry between 59 and 60
    - REQUIREMENTS.md sweep complete (or "no edits required" documented)
  </done>
</task>

<task type="auto">
  <name>Task 7: Wave-4 close — full doc-sweep validation grep audit</name>
  <files>(verification-only — no files modified)</files>
  <read_first>
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-VALIDATION.md (Doc-invariant validation block)
  </read_first>
  <action>
    Run the doc-sweep exit-gate audit. Every command MUST behave as documented; if any fails, the wave is NOT ready to merge.

    ```bash
    # 1. User-facing docs flipped — current-tense CGO=0 prose gone
    ! grep -inE 'CGO=0|no CGO|CGO_ENABLED=0' README.md CLAUDE.md CONTRIBUTING.md INSTALL.md 2>/dev/null

    # 2. Build framing landed in CLAUDE.md
    grep -qE 'CGO_ENABLED=1|CGO=1' CLAUDE.md
    grep -qF 'zig' CLAUDE.md

    # 3. CONTRIBUTING.md Releasing section completeness
    grep -qF 'zig' CONTRIBUTING.md
    grep -qF '0.14.1' CONTRIBUTING.md
    grep -qF 'release-snapshot' CONTRIBUTING.md
    grep -qF 'local goreleaser dry-run validates build matrix shape, not byte-for-byte CI parity' CONTRIBUTING.md

    # 4. CHANGELOG.md v1.10 entry present and well-formed
    grep -qF '## v1.10 — Release pipeline single-mode CGO=1' CHANGELOG.md
    grep -qF 'mlugg/setup-zig@v2.2.1' CHANGELOG.md
    grep -qF 'DEF-51-04' CHANGELOG.md

    # 5. PROJECT.md Key Decisions table updated
    grep -c 'Phase 59.1' .planning/PROJECT.md  # at least 1 (the new row)
    grep -qF '~~✓ Good — daemon refuses CGO=0 with remediation; CGO=1 byte-identical~~ — superseded by Phase 59.1 (v1.10)' .planning/PROJECT.md

    # 6. Deferred-items DEF-51-03 closed + DEF-51-04 created
    grep -qF 'DEF-51-03: Re-enable CGO in goreleaser' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md
    grep -qF 'RESOLVED via Phase 59.1' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md
    grep -qF 'DEF-51-04: windows-arm64 semantic_index unavailable' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md

    # 7. Phase 51.1 SUMMARY has back-pointer
    grep -qF 'Superseded by Phase 59.1' .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md

    # 8. v1.10-ROADMAP.md has Phase 59.1
    grep -qF 'Phase 59.1' .planning/milestones/v1.10-ROADMAP.md

    echo "Wave 4 doc-sweep audit PASSED"
    ```
  </action>
  <verify>
    <automated>! grep -inE 'CGO=0|no CGO|CGO_ENABLED=0' README.md CLAUDE.md CONTRIBUTING.md && grep -qE 'CGO_ENABLED=1|CGO=1' CLAUDE.md && grep -qF '## v1.10 — Release pipeline single-mode CGO=1' CHANGELOG.md && grep -qF 'Phase 59.1' .planning/PROJECT.md && grep -qF 'DEF-51-04' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'Superseded by Phase 59.1' .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md && grep -qF 'Phase 59.1' .planning/milestones/v1.10-ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - All 8 audit categories from the action block exit 0 / match as expected
    - `! grep -inE 'CGO=0|no CGO|CGO_ENABLED=0' README.md CLAUDE.md CONTRIBUTING.md` (zero current-tense user-facing hits)
    - All 8 documented files have at least one verifiable post-phase marker
    - No user-facing single-binary message has been diluted (regression check from Task 1)
  </acceptance_criteria>
  <done>
    - Doc-sweep exit gate green
    - All 9 files reflect post-phase truth
    - Wave 5 unblocked (verification + smoke)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo prose → contributors / users | Docs are the contract for how to build, release, and recover from divergence; stale prose in CONTRIBUTING.md leads to wrong contributor expectations |
| Decision history (PROJECT.md / deferred-items.md) → future maintainers | The strikethrough + tail pattern is the audit trail for "we tried X, then changed our minds because Y" — getting this wrong erases institutional memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59.1-W4-01 | Repudiation | Phase 51.1 decision history erased instead of struck-through (D-13 violation) | mitigate | Task 4 acceptance criteria explicitly verify the strikethrough syntax `~~ ~~` AND the trailing supersede tail; the historical Decision and Rationale cells are preserved unchanged |
| T-59.1-W4-02 | Tampering | User-facing single-binary message diluted in PROJECT.md / README.md (D-12 violation) | mitigate | Task 1 + Task 7 explicitly verify `single binary` / `single self-contained binary` prose preserved |
| T-59.1-W4-03 | Information Disclosure | DEF-51-04 windows-arm64 limitation hidden from CHANGELOG / Known Issues (regression honesty) | mitigate | Task 3 acceptance criterion verifies `DEF-51-04` cross-reference in CHANGELOG.md Known Issues; Task 5 verifies the deferred-item entry exists with restoration trigger |
| T-59.1-W4-04 | Repudiation | Reproducibility-gate softening framing slips into CONTRIBUTING.md (D-10 violation) | mitigate | Task 2 instructs the executor to NAME the WR-07 forensic dump as the remediation path; "do NOT soften the gate" prose is the explicit anti-pattern callout |
| T-59.1-W4-05 | Tampering | Middleware install-order section in CLAUDE.md accidentally edited | mitigate | Task 1 acceptance criterion verifies all four middleware names (`LazyInitMiddleware`, `TelemetryMiddleware`, `ProfileFilterMiddleware`, `SuggestionMiddleware`) still present |
</threat_model>

<verification>
- 9 files updated; all 8 audit categories pass
- User-facing single-binary message preserved in PROJECT.md, CLAUDE.md, README.md
- Build-side framing flipped in CLAUDE.md Tech Stack and CONTRIBUTING.md Releasing
- PROJECT.md Key Decisions: 51.1 row struck through with supersede tail; new 59.1 row added
- CHANGELOG.md v1.10 entry present with Build pipeline / Known Issues (windows-arm64) / Removed sections
- DEF-51-03 closed; DEF-51-04 created
- Phase 51.1 SUMMARY back-pointer added
- v1.10-ROADMAP.md has Phase 59.1 between Phase 59 and Phase 60
- Middleware install-order section in CLAUDE.md unchanged
</verification>

<success_criteria>
- All 9 documentation files updated in line with D-11/D-12/D-13
- VALIDATION.md "Doc-invariant validation" block all green
- Decision history preserved (strikethrough + tail; not erased)
- DEF-51-04 windows-arm64 restoration trigger documented
- Wave 5 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-04-SUMMARY.md` recording:
- Per-file diff summary (what changed in each of the 9 files)
- Verbatim copy of the new PROJECT.md row + the strikethrough on the 51.1 row
- The CHANGELOG.md v1.10 entry (full block)
- The DEF-51-03 closure + DEF-51-04 entry (verbatim)
- Confirmation of all 8 audit categories
- Note on REQUIREMENTS.md sweep result (edits made or no-op confirmed)
</output>
