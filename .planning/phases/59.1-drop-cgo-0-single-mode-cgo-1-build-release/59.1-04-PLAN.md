---
phase: 59.1-drop-cgo-0-single-mode-cgo-1-build-release
plan: 04
type: execute
wave: 4
depends_on: [59.1-00, 59.1-01, 59.1-02, 59.1-03]
files_modified:
  - CLAUDE.md
  - CONTRIBUTING.md
  - README.md
  - INSTALL.md
  - CHANGELOG.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/deferred-items.md
  - .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md
  - .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md
  - .planning/milestones/v1.10-ROADMAP.md
autonomous: true
requirements: [INT-BLOCKER-01, WR-04]
tags: [docs, doc-sweep, single-mode-cgo, fallback-b, multi-build-id, gatekeeper, deferred-items, d-11, d-12, d-13, d-15, d-16, d-19, d-20, wave-4]
decisions:
  honored:
    - D-11 (full doc sweep across CLAUDE/CONTRIBUTING/README/CHANGELOG/PROJECT/REQUIREMENTS + deferred-items + 51.1 SUMMARY + v1.10-ROADMAP)
    - D-12 (user/builder seam: user-facing single-binary message intact; build-side mentions BOTH toolchain paths — zig cc on linux for non-darwin, Apple clang on macos for darwin)
    - D-13 (PROJECT.md Key Decisions table — new row for 59.1, strikethrough/supersede tail on 51.1)
    - D-15 (split-runner architecture documented in CONTRIBUTING "Releasing")
    - D-16 (macOS job tag-gated; nightly canary deferred — tracked as DEF-59-DARWIN-CANARY)
    - D-19 (no Apple notarization; Gatekeeper "right-click → Open" workaround in INSTALL.md / README.md; Apple notarization tracked as DEF-59-NOTARIZE)
    - D-20 (FALLBACK-B-MULTI-BUILD-ID is canonical; path-A references replaced or marked superseded)
  notes: |
    No path-A references (`partial: { by_target: true }`, `release --split`, `goreleaser continue --merge`)
    in any user-facing prose. Where historical context demands the path-A trail (e.g. v1.10-ROADMAP
    milestone summary), the prose explicitly notes the FALLBACK-B realization with a pointer to
    `59.1-02-SPLIT-PROBE-NOTES.md` for the GoReleaser Pro lock-in evidence.

    Three NEW deferred items recorded in `.planning/deferred-items.md` (project-level — created
    in this plan): DEF-59-NOTARIZE (D-19), DEF-59-DARWIN-CANARY (D-16), DEF-59-WIN-ARM64-RESTORE
    (already tracked elsewhere; verify entry exists or create if missing).

    The 51-packaging-goreleaser/deferred-items.md gets DEF-51-03 closure pointing at 59.1 PLAN/SUMMARY
    + the FALLBACK-B mechanism note. The 51.1 SUMMARY.md gets a cross-reference to 59.1 as the
    supersede event for D-02 (daemon refuses CGO=0 invariant flipped).
must_haves:
  truths:
    - "User-facing prose (PROJECT.md Core Value, README hero, CLAUDE.md intro) unchanged on `single self-contained binary; no Python, Docker, or runtime dependencies` (D-12 user seam)"
    - "Build-side prose (CLAUDE.md Technology Stack, CONTRIBUTING.md Releasing) updated: builds with CGO=1; CI uses BOTH zig cc on ubuntu-22.04 for linux+windows AND Apple clang on macos-14 for darwin; locally `make build` uses host CC (D-12 builder seam)"
    - "CONTRIBUTING.md `Releasing` section names canonical FALLBACK-B local invocations: darwin host `goreleaser build --snapshot --clean --id helix-darwin` (2 binaries); linux host `goreleaser build --snapshot --clean --id helix-non-darwin` (4 binaries); full 6-archive matrix is CI-only"
    - "CONTRIBUTING.md documents the darwin-host caveat for `make release-snapshot` (windows_amd64_v1 zig cc link fails on darwin host because of libc++/libstdc++ stdlib mismatch in duckdb_static.a; this is a darwin-host-local limitation, NOT a config defect — CI ubuntu runner does not hit it because it invokes `--id helix-non-darwin` and never collides both stdlibs)"
    - "INSTALL.md and README.md include the Gatekeeper `right-click → Open` workaround for unsigned darwin binaries with explicit pointer to D-19 / DEF-59-NOTARIZE"
    - "CHANGELOG.md has a v1.10.x entry framed as `build pipeline: single-mode CGO=1 + split-runner (linux+windows zig cc, darwin Apple clang) + FALLBACK-B-MULTI-BUILD-ID`, replaces Phase 51.1 D-02 stub policy"
    - "`.planning/PROJECT.md` Key Decisions table: NEW row for Phase 59.1; existing 51.1 row carries `~~strikethrough~~` of `preserved` verb + ` — superseded by Phase 59.1 (v1.10)` tail (D-13)"
    - "`.planning/REQUIREMENTS.md` reflects single-mode CGO=1 invariant; any REQ rows referencing CGO=0 stub policy get updated text or struck row"
    - "`.planning/deferred-items.md` (NEW project-level file) created with three new entries: DEF-59-NOTARIZE (D-19 — Apple Developer ID signing + notarization), DEF-59-DARWIN-CANARY (D-16 — nightly darwin rebuild for Xcode-image drift detection), DEF-59-WIN-ARM64-RESTORE (Path-3 stub from Wave 1 — duckdb-go-bindings windows-arm64 lib restoration)"
    - "`.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` gets DEF-51-03 closure note pointing at 59.1 PLAN/SUMMARY + FALLBACK-B mechanism rationale (GoReleaser Pro lock-in)"
    - "`.planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md` gets a cross-reference back to 59.1 as the supersede event for D-02"
    - "`.planning/milestones/v1.10-ROADMAP.md` lists Phase 59.1 with FALLBACK-B realized-mechanism summary and cross-reference to `59.1-02-SPLIT-PROBE-NOTES.md`"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Technology Stack updated for CGO=1 + split-toolchain build (zig cc linux+windows, Apple clang darwin); user-facing single-binary message UNCHANGED"
      contains: ["CGO_ENABLED=1", "zig cc", "Apple clang", "macos-14", "ubuntu-22.04"]
    - path: "CONTRIBUTING.md"
      provides: "Releasing section: zig install hint, FALLBACK-B canonical per-host invocations, darwin-host stdlib-mismatch caveat, split-runner reproducibility-gate notes (D-17)"
      contains: ["goreleaser build --snapshot --clean --id helix-non-darwin", "goreleaser build --snapshot --clean --id helix-darwin", "FALLBACK-B", "macos-14", "darwin host", "linux host", "Pass-1", "Pass-2"]
    - path: "README.md"
      provides: "Single-binary user-facing message preserved; Gatekeeper workaround appended where appropriate (or pointer to INSTALL.md)"
      contains: ["single self-contained binary"]
    - path: "INSTALL.md"
      provides: "Gatekeeper `right-click → Open` workaround for unsigned darwin binaries; pointer to DEF-59-NOTARIZE deferred item"
      contains: ["Gatekeeper", "right-click", "Open", "DEF-59-NOTARIZE"]
    - path: "CHANGELOG.md"
      provides: "v1.10.x entry recording the single-mode CGO=1 + split-runner + FALLBACK-B realization"
      contains: ["v1.10", "CGO=1", "single-mode", "FALLBACK-B"]
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table: new 59.1 row + 51.1 row strikethrough/supersede tail"
      contains: ["Phase 59.1", "59.1", "superseded by Phase 59.1", "~~"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-level cleanup of any CGO=0 stub-policy references; single-mode CGO=1 invariant reflected"
      contains: ["CGO_ENABLED=1"]
    - path: ".planning/deferred-items.md"
      provides: "Project-level deferred-items file (NEW — created in this plan); three new entries: DEF-59-NOTARIZE, DEF-59-DARWIN-CANARY, DEF-59-WIN-ARM64-RESTORE"
      contains: ["DEF-59-NOTARIZE", "DEF-59-DARWIN-CANARY", "DEF-59-WIN-ARM64-RESTORE", "D-19", "D-16"]
    - path: ".planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md"
      provides: "DEF-51-03 closure entry pointing at 59.1 PLAN/SUMMARY + FALLBACK-B mechanism note"
      contains: ["DEF-51-03", "59.1", "FALLBACK-B", "GoReleaser Pro"]
    - path: ".planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md"
      provides: "Cross-reference back to 59.1 as the supersede event for D-02"
      contains: ["59.1", "superseded"]
    - path: ".planning/milestones/v1.10-ROADMAP.md"
      provides: "Phase 59.1 listed with FALLBACK-B realized-mechanism summary"
      contains: ["59.1", "FALLBACK-B", "single-mode CGO=1"]
  key_links:
    - from: "CLAUDE.md Technology Stack section"
      to: "CGO=1 + zig cc + Apple clang + macos-14 + ubuntu-22.04"
      via: "build-side prose update per D-12"
      pattern: "CGO_ENABLED=1.*zig cc|Apple clang.*macos-14"
    - from: "CONTRIBUTING.md Releasing section"
      to: "FALLBACK-B canonical per-host invocations + darwin-host caveat"
      via: "verbatim from `59.1-02-SPLIT-PROBE-NOTES.md` Hand-off block"
      pattern: "goreleaser build --snapshot --clean --id"
    - from: "INSTALL.md / README.md darwin section"
      to: "Gatekeeper right-click → Open workaround"
      via: "D-19 unsigned-binary user UX guidance"
      pattern: "right-click.*Open|Gatekeeper"
    - from: ".planning/deferred-items.md DEF-59-NOTARIZE"
      to: "D-19 Apple Developer Program + notarytool requirements"
      via: "deferred-item entry citing the cost ($99/yr) and credential management"
      pattern: "DEF-59-NOTARIZE"
    - from: ".planning/PROJECT.md Key Decisions table"
      to: "59.1 NEW row + 51.1 strikethrough/supersede tail"
      via: "D-13 decision-trail preservation"
      pattern: "Phase 59.1|superseded by Phase 59.1"
    - from: ".planning/milestones/v1.9-phases/51.1-.../SUMMARY.md"
      to: "59.1 supersede cross-reference"
      via: "D-11 lineage note"
      pattern: "Phase 59.1|superseded"
---

<objective>
Documentation sweep across 11 files reflecting the single-mode CGO=1 + split-runner + FALLBACK-B-MULTI-BUILD-ID reality. Three buckets:

1. **User/builder seam (D-12):** CLAUDE.md, CONTRIBUTING.md, README.md, INSTALL.md, CHANGELOG.md
   - User-facing single-binary message INTACT (PROJECT.md / README hero / CLAUDE.md intro stay verbatim).
   - Build-side prose (CLAUDE.md Technology Stack, CONTRIBUTING.md Releasing) updated to mention BOTH toolchain paths — `zig cc` on `ubuntu-22.04` for linux+windows; Apple clang on `macos-14` for darwin.
   - INSTALL.md / README.md gain the Gatekeeper "right-click → Open" workaround (D-19) for unsigned darwin binaries.
   - CHANGELOG.md gains a v1.10.x entry framed as the single-mode CGO=1 + split-runner + FALLBACK-B realization.

2. **Planning lineage (D-11, D-13):** `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, the 51-packaging-goreleaser deferred-items file, the 51.1 SUMMARY.md, the v1.10-ROADMAP.md.
   - PROJECT.md Key Decisions table: NEW row for 59.1; existing 51.1 row strikethrough/supersede tail (D-13).
   - REQUIREMENTS.md: any CGO=0 stub-policy text gets updated or struck.
   - 51-packaging deferred-items: DEF-51-03 closed pointing at 59.1 + FALLBACK-B mechanism note.
   - 51.1 SUMMARY.md: cross-reference back to 59.1 as supersede event.
   - v1.10-ROADMAP.md: Phase 59.1 listed with realized-mechanism summary.

3. **Project-level deferred items (NEW file):** `.planning/deferred-items.md`
   - This file does not yet exist. Create it as the project-level deferred-items index.
   - Seed with three entries from this phase: DEF-59-NOTARIZE (D-19 Apple notarization), DEF-59-DARWIN-CANARY (D-16 nightly darwin rebuild), DEF-59-WIN-ARM64-RESTORE (Wave 1 windows-arm64 platform stub restoration).

Purpose:
- D-11 doc sweep — without this Wave, future readers do not know the build pipeline mechanic changed (CGO=0 → CGO=1) or that the split-runner architecture is canonical.
- D-12 user/builder seam: do NOT dilute the user-facing single-binary promise. Builder-side text expands but user-side text is preserved verbatim.
- D-13 decision history: future readers see the trail "we tried CGO=0 stub apparatus (51.1 D-02), then changed our minds in 59.1 because the goreleaser+release-cut path made it pointless." Strikethrough + supersede tail makes the trail visible in one place (PROJECT.md).
- D-19 Gatekeeper workaround: real Mac users will hit "helix is damaged and can't be opened" on first run because we don't notarize. The workaround MUST be documented before v1.10.x ships, otherwise install-time breakage is silent.
- D-16 macOS job is tag-gated: nightly darwin canary is deferred but worth tracking so future readers know it was considered.
- DEF-59-NOTARIZE is worth tracking for the moment a real Mac user files a Gatekeeper-blocking install bug.

Output:
- 11 files modified or created (1 new: `.planning/deferred-items.md`).
- No path-A references in any user-facing prose (`partial: { by_target: true }`, `release --split`, `goreleaser continue --merge`).
- All FALLBACK-B canonical commands present verbatim where contributors will copy them.
- CHANGELOG entry written as user-facing release notes (build-pipeline change, but framed for the user reader who wants to understand what changed in v1.10.x).

This plan is **autonomous** (no checkpoint — pure docs work). Wave 5 (subsequent plan) is the human-verify checkpoint where the workflow is exercised end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-PATTERNS.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-VALIDATION.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SPLIT-PROBE-NOTES.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SUMMARY.md
@.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-01-SUMMARY.md
@CLAUDE.md
@CONTRIBUTING.md
@README.md
@INSTALL.md
@CHANGELOG.md
@.planning/milestones/v1.10-ROADMAP.md
@.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md
@.planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md
</context>

<canon>
**FALLBACK-B canonical local invocations** (verbatim from `59.1-02-SUMMARY.md` lines 254-256 + `59.1-02-SPLIT-PROBE-NOTES.md` Hand-off block):

- darwin host: `goreleaser build --snapshot --clean --id helix-darwin` → 2 darwin binaries
- linux host:  `goreleaser build --snapshot --clean --id helix-non-darwin` → 4 linux+windows binaries
- Full 6-archive matrix is CI-only (assembled in `release-merge` job per Wave 3).

**darwin-host caveat for `make release-snapshot`** (verbatim from `59.1-02-SUMMARY.md` Deviations §2):

> On a darwin host, `make release-snapshot` invokes `goreleaser release --snapshot --clean --skip=sign`,
> which builds BOTH `helix-non-darwin` and `helix-darwin` entries in a single goreleaser invocation
> (because the OSS CLI lacks `--split` to constrain to a single entry). On a darwin host the
> `helix-non-darwin/windows_amd64_v1` build fails at zig cc link time with C++ stdlib undefined
> symbols (`std::ostream`, `std::basic_streambuf`, `std::rethrow_exception`...) because
> `libduckdb_static.a` was built against libstdc++ but zig defaults to libc++; lld-link cannot
> resolve cross-stdlib symbols. **This is a darwin-host-local limitation, NOT a config defect.**
> CI ubuntu runner does not hit it because it invokes `goreleaser build --id helix-non-darwin`
> (constrained to non-darwin entry).

**Gatekeeper workaround prose** (D-19 — for INSTALL.md / README.md darwin section):

> macOS users: the darwin binaries are signed with Sigstore cosign keyless attestation but are
> NOT signed with an Apple Developer ID, and they are NOT notarized through Apple's notary
> service. On first launch, macOS Gatekeeper will refuse to open the binary with an error like
> "helix can't be opened because Apple cannot check it for malicious software" or "helix is
> damaged and can't be opened."
>
> **Workaround:** Open Finder, locate the extracted `helix` binary, **right-click** (or
> Control-click) on it, and select **Open** from the context menu. macOS will prompt
> "Are you sure you want to open it?" — click **Open** to confirm. After this one-time
> approval, the binary runs without further prompts.
>
> Alternatively, you can clear the Gatekeeper quarantine flag from a terminal:
>
> ```bash
> xattr -d com.apple.quarantine /path/to/helix
> ```
>
> Apple Developer ID signing + notarization is tracked as `DEF-59-NOTARIZE` in
> `.planning/deferred-items.md` and may land in a future v1.10.x or v1.11+ release.

**v1.10.x CHANGELOG entry shape** (D-11 — write under the v1.10.x version heading; create the
heading if it doesn't exist):

```markdown
## [Unreleased] — v1.10.x

### Build & Release Pipeline (Phase 59.1)

- **Single-mode CGO=1 build & release.** Helix now builds with `CGO_ENABLED=1`
  unconditionally; the previous CGO=0 stub apparatus (Phase 51.1 D-02) is removed.
  This closes `INT-BLOCKER-01` (the goreleaser CGO=0 build that broke when
  `internal/semantic/extract/provider.go` unconditionally imported
  `tree_sitter.Language`) by removing the CGO=0 build path entirely instead of
  patching the missing stub.
- **Split-runner CI.** Linux + Windows archives (4 of 6) build on `ubuntu-22.04`
  with `zig cc` as a hermetic cross-compiler. Darwin archives (2 of 6) build
  natively on `macos-14` (Apple Silicon, Xcode 15.x) with Apple clang. A merge
  job stitches both runners' artifacts and runs the existing Sigstore cosign
  keyless attestation uniformly across all 6 archives.
- **FALLBACK-B-MULTI-BUILD-ID.** The `.goreleaser.yaml` file declares two
  `builds:` entries (`helix-non-darwin`, `helix-darwin`); each runner invokes
  `goreleaser build --id <runner-id>`; the merge job runs
  `goreleaser release --skip=build` to assemble archives, checksums, and
  signatures from the pre-built binaries. Wave 2 prerequisite probe found
  that GoReleaser's `partial: { by_target: true }` + `release --split` +
  `goreleaser continue --merge` mechanic is GoReleaser Pro exclusive (not
  available in the OSS distribution); FALLBACK-B is the OSS-supported
  equivalent.
- **`helix upgrade` artifact-name and `.sigstore.json` bundle layout
  preserved.** Phase 58 REL-01 contract holds. Existing `helix upgrade`
  consumers see no change.
- **Reproducibility gate (per-runner).** Each runner asserts byte-identical
  Pass-1 ≡ Pass-2 over its own target subset. Cross-runner byte-equality
  is not asserted (different machines, different SDKs, different clang
  versions — that comparison was never meaningful and is explicitly not a
  gate).
- **Darwin binaries unsigned on Apple side.** Apple Developer ID signing
  and notarization are deferred (see `.planning/deferred-items.md` →
  `DEF-59-NOTARIZE`). On first launch, macOS Gatekeeper will block the
  unsigned binary; users must right-click → Open once to bypass. See
  `INSTALL.md` for the workaround.
```
</canon>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: User-facing + builder-side prose updates (CLAUDE.md, CONTRIBUTING.md, README.md, INSTALL.md, CHANGELOG.md)</name>
  <files>CLAUDE.md, CONTRIBUTING.md, README.md, INSTALL.md, CHANGELOG.md</files>
  <read_first>
    - CLAUDE.md (full file — Technology Stack section is the build-side prose target per D-12; "Project" section has the user-facing single-binary message that must stay verbatim)
    - CONTRIBUTING.md (full file — `Releasing` section is the primary update target; pre-existing zig install hint may need expansion to FALLBACK-B canonical commands)
    - README.md (full file — hero / install section gets the Gatekeeper workaround pointer; user-facing single-binary message stays verbatim per D-12)
    - INSTALL.md (full file — verify install instructions exist; append the Gatekeeper workaround for darwin)
    - CHANGELOG.md (full file — verify v1.10.x heading exists or create it; append the Phase 59.1 build-pipeline entry)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md (D-11, D-12, D-19 — locked decisions)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SPLIT-PROBE-NOTES.md (FALLBACK-B canonical commands)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SUMMARY.md (Hand-off to Wave 4 — darwin-host caveat verbatim)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-01-SUMMARY.md (Wave 1 — single-mode CGO=1 invariant established at source-tree level)
  </read_first>
  <action>
    **EDIT 1 — CLAUDE.md (Technology Stack section, build-side prose per D-12).**

    Locate the `## Technology Stack` section. The current bullet list (per project context) describes the runtime stack. Update it (or add a new bullet group) so the build-side prose mentions BOTH toolchain paths:

    Find the `## Technology Stack` section. Append (or update existing build-related bullet) to read:

    ```markdown
    ## Technology Stack

    [...existing runtime bullets unchanged...]

    **Build pipeline (CGO=1, split-runner per Phase 59.1):**
    - **Source tree:** single-mode `CGO_ENABLED=1`. The previous CGO=0 stub
      apparatus (Phase 51.1 D-02) was removed in Phase 59.1; the source tree
      no longer carries `//go:build cgo` constraints (except the platform-
      conditional `internal/semantic/store/duckdb.go` `!(windows && arm64)`
      from D-14, which is platform-conditional, not CGO-conditional).
    - **Local builds:** `make build` uses your host CC. zig is NOT required for
      local development.
    - **Local releases (partial):** `make release-snapshot` requires `zig` on
      PATH; produces a host-platform partial matrix only (a darwin contributor
      gets 2 darwin archives via Apple clang; a linux contributor with zig
      gets 4 linux+windows archives via zig cc). Full 6-archive matrix is
      CI-only.
    - **CI builds (split-runner per D-15):**
      - **`ubuntu-22.04`:** builds linux/{amd64,arm64} + windows/{amd64,arm64}
        (4 archives) via `CC="zig cc -target <triple>"`. zig pinned to
        0.14.1 via SHA-pinned `mlugg/setup-zig` action.
      - **`macos-14`:** builds darwin/{amd64,arm64} (2 archives) natively via
        Apple clang against the Xcode 15.x SDK. NOT signed with Apple
        Developer ID; cosign Sigstore keyless attestation is the only
        signature.
      - **Merge job (`ubuntu-22.04`):** stitches both runners' partial
        artifacts; runs cosign keyless attestation uniformly across all 6
        archives; publishes the GitHub Release.
    - **Reproducibility (per-target within-runner Pass-1 ≡ Pass-2):** each
      runner rebuilds its own targets twice in the same job and asserts
      byte-identical sha256s. Cross-runner byte-equality is not asserted.
    ```

    The existing user-facing prose ("Helix ships as a single Go binary with no Python, Docker, or runtime dependencies. ...") MUST remain verbatim per D-12 user seam — do not reword it.

    **EDIT 2 — CONTRIBUTING.md (Releasing section).**

    Locate the `Releasing` (or "Release Process" / "Cutting a Release") section. Replace its body with the canonical FALLBACK-B prose. Keep any existing release-cut-tag / version-bump checklist; replace only the build-tooling portion.

    The new prose (insert as appropriate near the existing release section):

    ```markdown
    ## Releasing

    Helix uses [GoReleaser](https://goreleaser.com/) (OSS v2.15.4) to build
    cross-platform archives. Phase 59.1 introduced a split-runner architecture
    because GoReleaser's native `partial: { by_target: true }` / `release
    --split` / `goreleaser continue --merge` mechanic is **GoReleaser Pro
    exclusive** (verified directly against
    https://goreleaser.com/customization/partial/). Helix runs on the OSS
    distribution. The OSS-supported equivalent is the
    **FALLBACK-B-MULTI-BUILD-ID** pattern: `.goreleaser.yaml` declares two
    `builds:` entries (`helix-non-darwin`, `helix-darwin`), each runner
    invokes `goreleaser build --id <runner-id>`, and a merge job runs
    `goreleaser release --skip=build` to assemble archives + checksums +
    signatures from the pre-built binaries.

    ### Local snapshot builds (per-host partial matrix)

    The full 6-archive matrix only assembles in CI. Locally, you can build
    the subset your host can natively compile:

    **darwin host (e.g. an Apple Silicon Mac):**

    ```bash
    # Apple clang produces darwin/{amd64,arm64} (2 binaries):
    goreleaser build --snapshot --clean --id helix-darwin
    ```

    Output: `dist/helix-darwin_darwin_amd64_v1/helix` and
    `dist/helix-darwin_darwin_arm64_v8.0/helix`.

    **linux host (with `zig` on PATH, version 0.14.1+):**

    ```bash
    # zig cc cross-compiles linux/{amd64,arm64} + windows/{amd64,arm64}
    # (4 binaries):
    goreleaser build --snapshot --clean --id helix-non-darwin
    ```

    Output: `dist/helix-non-darwin_linux_amd64_v1/helix`,
    `dist/helix-non-darwin_linux_arm64_v8.0/helix`,
    `dist/helix-non-darwin_windows_amd64_v1/helix.exe`,
    `dist/helix-non-darwin_windows_arm64_v8.0/helix.exe`.

    ### `make release-snapshot` (convenience target)

    `make release-snapshot` invokes `goreleaser release --snapshot --clean
    --skip=sign`, which attempts to build BOTH `helix-non-darwin` AND
    `helix-darwin` entries in a single invocation. Behavior depends on host:

    - **On a linux host (with zig 0.14.1+):** builds 4 linux+windows binaries
      successfully; darwin entries fail (linux host cannot natively produce
      darwin binaries because zig cc cannot reach the Apple SDK headers
      `mach/mach_vm.h` — Wave 0 probe blocker).
    - **On a darwin host:** builds 2 darwin binaries successfully; the
      `helix-non-darwin/windows_amd64_v1` zig cc link step then fails with
      C++ stdlib undefined symbols (`std::ostream`, `std::basic_streambuf`,
      etc.) because `libduckdb_static.a` was built against libstdc++ but zig
      defaults to libc++ — lld-link cannot resolve cross-stdlib symbols.
      **This is a darwin-host-local limitation, NOT a config defect.**

    The recommended local workflow is to use the per-`--id` invocations
    above instead of `make release-snapshot` if you only need to validate
    the subset for your host. CI ubuntu runner does not hit the
    cross-stdlib link error because it invokes `--id helix-non-darwin`
    (constrained to the non-darwin entry).

    ### CI release flow

    A real release is cut by pushing a `vX.Y.Z` tag. The
    `.github/workflows/release.yml` workflow runs three jobs:

    1. **`release-linux`** (`ubuntu-22.04`): SHA-pinned `mlugg/setup-zig` action
       installs zig 0.14.1; runs Pass-1 then Pass-2 of `goreleaser build
       --id helix-non-darwin`; asserts per-target byte-identical sha256
       (D-17); uploads partial dist as `dist-partial-linux`.
    2. **`release-darwin`** (`macos-14`, tag-gated per D-16): selects Xcode
       15.x; records `xcodebuild -version` + `clang --version` provenance;
       runs Pass-1 then Pass-2 of `goreleaser build --id helix-darwin`;
       asserts per-target byte-identical sha256; uploads partial dist as
       `dist-partial-darwin`.
    3. **`release-merge`** (`ubuntu-22.04`, `needs:` both): downloads both
       partial dists into a unified `dist/` tree; runs `goreleaser release
       --clean --skip=build`; cosign keyless attestation runs uniformly
       across all 6 archives; publishes the GitHub Release.

    ### Reproducibility gate

    The reproducibility gate is **per-target within-runner Pass-1 ≡ Pass-2**.
    Each runner rebuilds its own targets twice in the same workflow job and
    fails-loud if the sha256s diverge. **Cross-runner byte-equality is NOT
    asserted** (different machines, different SDKs, different clang versions
    — that comparison was never meaningful). On Pass-1 ≢ Pass-2 divergence,
    a per-runner WR-07 forensic dump (per-file sha256, tar entry metadata,
    gzip header bytes, Mach-O LC_UUID + codesign timestamp on darwin,
    `go version -m` diff) drops into the workflow logs.

    ### Snapshot dispatch (rehearsal)

    To exercise the full pipeline against a draft branch without cutting a
    real tag (e.g. for first-CI-repro evidence per Wave 5), use the
    `workflow_dispatch` trigger:

    ```bash
    gh workflow run release.yml --ref <draft-branch>
    ```

    The dispatch path runs `goreleaser release --snapshot --clean --skip=build,sign`
    in the merge job: archives + checksums are produced, but cosign signing
    and GitHub release publishing are skipped.

    ### Toolchain prerequisites (local)

    | Tool | Required for | Install hint |
    |------|--------------|--------------|
    | Go 1.25.x | `make build`, `go test ./...` | https://go.dev/dl/ |
    | goreleaser v2.15.4 | `make release-snapshot`, local `goreleaser build --id <id>` | `brew install goreleaser` (macOS) or [release page](https://github.com/goreleaser/goreleaser/releases/tag/v2.15.4) |
    | zig 0.14.1+ | `make release-snapshot` (linux+windows portion); local `--id helix-non-darwin` invocation | `brew install zig` (macOS, picks up latest), `apt install zig` (Ubuntu 22.04+), or https://ziglang.org/download/ |
    | cosign 2.4.1+ | local sign verification only (CI uses keyless OIDC, no local cosign needed for the build itself) | `brew install cosign` |
    ```

    **EDIT 3 — README.md (preserve user-facing single-binary; add Gatekeeper pointer).**

    Locate the install / download section. The user-facing single-binary message ("single self-contained binary; no Python, Docker, or runtime dependencies" or similar) MUST remain verbatim per D-12. Append a brief macOS-specific note pointing to INSTALL.md:

    Find the install/quickstart section (look for `helix daemon` or `download` keywords). Append at the END of the install section (NOT replacing existing prose):

    ```markdown
    ### macOS users: Gatekeeper workaround

    Helix's darwin binaries are signed with [Sigstore](https://www.sigstore.dev/)
    cosign keyless attestation but are **not signed with an Apple Developer ID**
    and **not notarized** through Apple's notary service. On first launch, macOS
    Gatekeeper will block the binary. Use the right-click → Open workaround
    documented in [INSTALL.md](./INSTALL.md#macos-gatekeeper-workaround).

    Apple Developer ID signing + notarization is tracked as `DEF-59-NOTARIZE`
    in `.planning/deferred-items.md` and may land in a future release.
    ```

    The hero section, the "single self-contained binary" prose, the feature
    bullets, and the existing install instructions remain untouched.

    **EDIT 4 — INSTALL.md (Gatekeeper workaround section).**

    Locate the install instructions. Append a new section (preferably after the existing macOS install instructions; if no macOS section exists, add one):

    ```markdown
    ## macOS Gatekeeper workaround

    Helix's darwin binaries are signed with [Sigstore](https://www.sigstore.dev/)
    cosign keyless attestation but are **not signed with an Apple Developer ID**
    and **not notarized** through Apple's notary service. On first launch, macOS
    Gatekeeper will block the binary with an error like:

    > "helix" can't be opened because Apple cannot check it for malicious software.

    Or:

    > "helix" is damaged and can't be opened. You should move it to the Trash.

    **Workaround (one-time per binary):**

    1. Open Finder and navigate to the extracted `helix` binary.
    2. **Right-click** (or Control-click) on the `helix` binary.
    3. Select **Open** from the context menu.
    4. macOS will prompt: "Are you sure you want to open it?" — click **Open**.

    After this one-time approval, the binary runs without further Gatekeeper
    prompts.

    **Alternative (terminal):**

    Clear the Gatekeeper quarantine flag from a terminal:

    ```bash
    xattr -d com.apple.quarantine /path/to/helix
    ```

    **Why this is required:**

    Apple Developer ID signing requires an Apple Developer Program membership
    ($99/year) and Apple-credential management infrastructure. Phase 59.1 is
    scoped to the CGO=1 build pipeline; signing/notarization is out-of-scope.
    The Sigstore cosign keyless attestation that Helix DOES apply works on all
    platforms uniformly (linux, windows, darwin) and is verified by `helix
    upgrade` against the public-good Sigstore TUF root.

    Apple Developer ID signing + notarization is tracked as `DEF-59-NOTARIZE`
    in `.planning/deferred-items.md`. It may land in a future v1.10.x or v1.11+
    release.
    ```

    **EDIT 5 — CHANGELOG.md (v1.10.x entry).**

    Locate the top of CHANGELOG.md. If a `## [Unreleased]` or `## v1.10.x`
    or similar version heading exists at the top, append the Phase 59.1
    entry to it. If no such heading exists, create one. Insert the verbatim
    block from `<canon>` ("v1.10.x CHANGELOG entry shape") at the top
    immediately under the file header.

    **Negative checks for ALL 5 files:**

    ```bash
    # No path-A references (D-20):
    ! grep -qE 'partial: ?\{ ?by_target' CLAUDE.md CONTRIBUTING.md README.md INSTALL.md CHANGELOG.md
    ! grep -qE 'release --split|goreleaser continue --merge' CLAUDE.md CONTRIBUTING.md README.md INSTALL.md CHANGELOG.md
    # User-facing single-binary message preserved (D-12):
    grep -qF 'single' README.md   # baseline check; verify hero text intact in manual review
    ```

    Commit message: `docs(phase-59.1-04): user/builder seam doc sweep (CLAUDE/CONTRIBUTING/README/INSTALL/CHANGELOG) per D-11/D-12/D-19/D-20`.
  </action>
  <verify>
    <automated>grep -qF 'CGO_ENABLED=1' CLAUDE.md && grep -qF 'zig cc' CLAUDE.md && grep -qF 'Apple clang' CLAUDE.md && grep -qF 'macos-14' CLAUDE.md && grep -qF 'goreleaser build --snapshot --clean --id helix-non-darwin' CONTRIBUTING.md && grep -qF 'goreleaser build --snapshot --clean --id helix-darwin' CONTRIBUTING.md && grep -qF 'FALLBACK-B' CONTRIBUTING.md && grep -qF 'darwin host' CONTRIBUTING.md && grep -qF 'linux host' CONTRIBUTING.md && grep -qF 'GoReleaser Pro' CONTRIBUTING.md && grep -qF 'Gatekeeper' INSTALL.md && grep -qF 'right-click' INSTALL.md && grep -qF 'DEF-59-NOTARIZE' INSTALL.md && grep -qF 'xattr -d com.apple.quarantine' INSTALL.md && grep -qF 'Gatekeeper' README.md && grep -qF 'CGO=1' CHANGELOG.md && grep -qF 'FALLBACK-B' CHANGELOG.md && grep -qF 'single-mode' CHANGELOG.md && ! grep -qE 'partial: ?\{ ?by_target' CLAUDE.md CONTRIBUTING.md README.md INSTALL.md CHANGELOG.md && ! grep -qE 'release --split|goreleaser continue --merge' CLAUDE.md CONTRIBUTING.md README.md INSTALL.md CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - **CLAUDE.md (D-12 builder seam):** `grep -qF 'CGO_ENABLED=1' CLAUDE.md && grep -qF 'zig cc' CLAUDE.md && grep -qF 'Apple clang' CLAUDE.md && grep -qF 'macos-14' CLAUDE.md && grep -qF 'ubuntu-22.04' CLAUDE.md`
    - **CLAUDE.md (D-12 user seam — preserved):** `grep -qF 'single' CLAUDE.md` and the existing user-facing single-binary text is unchanged (manual review confirms no rewording of the hero / Project section)
    - **CONTRIBUTING.md FALLBACK-B canonical commands:**
      - `grep -qF 'goreleaser build --snapshot --clean --id helix-non-darwin' CONTRIBUTING.md`
      - `grep -qF 'goreleaser build --snapshot --clean --id helix-darwin' CONTRIBUTING.md`
    - **CONTRIBUTING.md FALLBACK-B + GoReleaser Pro context:** `grep -qF 'FALLBACK-B' CONTRIBUTING.md && grep -qF 'GoReleaser Pro' CONTRIBUTING.md`
    - **CONTRIBUTING.md per-host partial matrix prose:** `grep -qF 'darwin host' CONTRIBUTING.md && grep -qF 'linux host' CONTRIBUTING.md && grep -qF '2 darwin binaries' CONTRIBUTING.md || grep -qF '2 binaries' CONTRIBUTING.md`
    - **CONTRIBUTING.md darwin-host caveat:** `grep -qF 'libstdc++' CONTRIBUTING.md && grep -qF 'libc++' CONTRIBUTING.md && grep -qF 'darwin-host-local limitation, NOT a config defect' CONTRIBUTING.md`
    - **CONTRIBUTING.md split-runner job names + reproducibility gate prose:** `grep -qF 'release-linux' CONTRIBUTING.md && grep -qF 'release-darwin' CONTRIBUTING.md && grep -qF 'release-merge' CONTRIBUTING.md && grep -qF 'Pass-1' CONTRIBUTING.md && grep -qF 'Pass-2' CONTRIBUTING.md`
    - **CONTRIBUTING.md zig prerequisite:** `grep -qF 'zig 0.14.1' CONTRIBUTING.md && grep -qF 'mlugg/setup-zig' CONTRIBUTING.md`
    - **README.md user-facing single-binary preserved:** `grep -qF 'single' README.md` and the hero / install section is unchanged except for the appended Gatekeeper pointer
    - **README.md Gatekeeper pointer:** `grep -qF 'Gatekeeper' README.md && grep -qF 'INSTALL.md' README.md`
    - **INSTALL.md Gatekeeper workaround section:** `grep -qF 'Gatekeeper' INSTALL.md && grep -qF 'right-click' INSTALL.md && grep -qF 'xattr -d com.apple.quarantine' INSTALL.md && grep -qF 'DEF-59-NOTARIZE' INSTALL.md && grep -qF 'Apple Developer ID' INSTALL.md`
    - **CHANGELOG.md v1.10.x entry:** `grep -qF 'CGO=1' CHANGELOG.md && grep -qF 'single-mode' CHANGELOG.md && grep -qF 'FALLBACK-B' CHANGELOG.md && grep -qF 'split-runner' CHANGELOG.md && grep -qF 'Phase 59.1' CHANGELOG.md`
    - **CHANGELOG.md REL-01 contract preservation note:** `grep -qF 'REL-01' CHANGELOG.md || grep -qF 'helix upgrade' CHANGELOG.md`
    - **Negative checks (all 5 files):**
      - `! grep -qE 'partial: ?\{ ?by_target' CLAUDE.md CONTRIBUTING.md README.md INSTALL.md CHANGELOG.md`
      - `! grep -qE 'release --split|goreleaser continue --merge' CLAUDE.md CONTRIBUTING.md README.md INSTALL.md CHANGELOG.md`
    - All 5 files commit cleanly (no merge conflicts; gofmt/markdownlint not required but sensible)
  </acceptance_criteria>
  <done>
    - CLAUDE.md Technology Stack updated with split-toolchain build prose (D-12 builder seam); user-facing prose intact
    - CONTRIBUTING.md Releasing section authored with FALLBACK-B canonical per-host commands, darwin-host caveat verbatim from canon, split-runner CI flow, reproducibility-gate notes
    - README.md Gatekeeper pointer appended; user-facing single-binary message preserved
    - INSTALL.md Gatekeeper workaround section added with right-click + xattr workaround + DEF-59-NOTARIZE pointer
    - CHANGELOG.md v1.10.x entry appended with full Phase 59.1 build-pipeline framing
    - No path-A references in any user-facing prose
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Planning lineage updates (PROJECT.md Key Decisions table per D-13, REQUIREMENTS.md CGO=1 invariant, 51-packaging deferred-items DEF-51-03 closure, 51.1 SUMMARY supersede note, v1.10-ROADMAP.md realized-mechanism summary)</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md, .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md, .planning/milestones/v1.10-ROADMAP.md</files>
  <read_first>
    - .planning/PROJECT.md (full file — locate the `Key Decisions` table; identify the existing 51.1 row that needs strikethrough/supersede tail; identify the position to insert the new 59.1 row)
    - .planning/REQUIREMENTS.md (full file — search for any rows referencing `CGO_ENABLED=0`, `CGO=0 stub policy`, `treesitter.Available`, or related strings that need updating to the single-mode CGO=1 invariant)
    - .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md (full file — locate DEF-51-03 candidate or DEF-51-02 RESOLVED status; add closure note pointing at 59.1 + FALLBACK-B mechanism rationale)
    - .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md (full file — locate D-02 or "preserved" verb to add the 59.1 supersede cross-reference)
    - .planning/milestones/v1.10-ROADMAP.md (full file — verify Phase 59.1 is listed; if not, add it with realized-mechanism summary; if listed, update to reflect FALLBACK-B realization)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md (D-11, D-13)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SUMMARY.md (FALLBACK-B realization for cross-reference)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md (Post-Probe Decision Lock — supersede statement source)
  </read_first>
  <action>
    **EDIT 1 — `.planning/PROJECT.md` Key Decisions table (D-13).**

    Use Read with grep to locate the `Key Decisions` table:
    ```bash
    grep -n 'Key Decisions\|Phase 51.1\|Phase 51' .planning/PROJECT.md
    ```

    Identify the existing 51.1 row. Apply two edits:

    1. **Strikethrough + supersede tail on the existing 51.1 row.** The 51.1 row's outcome verb is "preserved" or similar (e.g. "Good — preserves CGO=0 build path via stub apparatus"). Wrap the affected verb / phrase in `~~strikethrough~~` and append ` — superseded by Phase 59.1 (v1.10)`. Example transformation:

    Before:
    ```
    | Phase 51.1: gate internal/treesitter behind //go:build cgo | ... | ✓ Good — preserves CGO=0 build path via stub apparatus |
    ```
    After:
    ```
    | Phase 51.1: gate internal/treesitter behind //go:build cgo | ... | ~~✓ Good — preserves CGO=0 build path via stub apparatus~~ — superseded by Phase 59.1 (v1.10) |
    ```

    2. **Insert a NEW row for Phase 59.1.** After the 51.1 row (or in chronological order with other v1.10 phases), insert:

    ```
    | Phase 59.1: drop CGO=0 stubs, single-mode CGO=1 release pipeline (split-runner; FALLBACK-B-MULTI-BUILD-ID per D-20) | INT-BLOCKER-01 from v1.10-MILESTONE-AUDIT.md — stub apparatus added cost without value once goreleaser pinned CGO=0 | ✓ Good — single-mode CGO=1 source tree; ubuntu-22.04 zig cc for linux+windows + macos-14 Apple clang for darwin; uniform cosign keyless attestation across 6 archives in the merge job; per-runner Pass-1≡Pass-2 reproducibility (D-17) |
    ```

    Match the column count and shape of the existing table; if the table has a 4-column "Phase | Trigger | Decision | Outcome" structure, adjust the row to fit. Re-verify the Markdown table renders correctly (column separator counts match).

    **EDIT 2 — `.planning/REQUIREMENTS.md` CGO=1 invariant.**

    Search for any text referencing CGO=0 stub policy:
    ```bash
    grep -nE 'CGO_ENABLED=0|CGO=0|treesitter\.Available|CGO=0 stub' .planning/REQUIREMENTS.md
    ```

    For each match:
    - If the row is a current-tense REQ (e.g. "Helix MUST build with CGO_ENABLED=0"), update text to single-mode CGO=1: "Helix builds with CGO_ENABLED=1 (single-mode per Phase 59.1; the prior CGO=0 stub policy from Phase 51.1 D-02 was removed)."
    - If the row is historical context (e.g. an audit row noting an old behavior), strike-through the row instead of updating: `~~| ... | CGO=0 stub policy | ... |~~ — superseded by Phase 59.1 (v1.10)`.

    If no rows match, the file is already CGO-agnostic; record this in the commit message (no edit needed for REQUIREMENTS.md).

    Add a new entry to the "Future Requirements (deferred to v1.10.x or later)" section if it doesn't already cover Apple notarization (D-19) and darwin nightly canary (D-16):

    ```markdown
    ### Deferred (Phase 59.1)

    - **Apple Developer ID signing + notarization** (D-19) — see
      `.planning/deferred-items.md` → DEF-59-NOTARIZE. Tracked separately
      because it requires an Apple Developer Program membership ($99/yr).
    - **Nightly darwin canary workflow** (D-16) — see
      `.planning/deferred-items.md` → DEF-59-DARWIN-CANARY. Tracked
      separately to bound macos-14 runner cost.
    - **windows-arm64 native duckdb-go-bindings restoration** (D-14
      Path-3) — see `.planning/deferred-items.md` → DEF-59-WIN-ARM64-RESTORE.
      Tracked separately to await upstream `duckdb-go-bindings/lib/windows-arm64`.
    ```

    **EDIT 3 — `.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` (DEF-51-03 closure).**

    The file already contains DEF-51-01 (RESOLVED via 51-03), DEF-51-02 (RESOLVED via 51.1). Append a new section at the END of the file:

    ```markdown
    ---

    ## DEF-51-03: re-enable CGO in goreleaser (Path 3 closure tracking)

    **Originally referenced in:** Phase 51.1 SUMMARY discussion of "DEF-51-03
    candidate" (Path 3 from DEF-51-02 resolution paths).

    **Status:** RESOLVED via Phase 59.1 (FALLBACK-B-MULTI-BUILD-ID).

    **Resolution:**

    Phase 59.1 closes DEF-51-03 by inverting Phase 51.1 D-02 (the daemon's
    `treesitter.Available` refusal under CGO=0) into single-mode CGO=1. See:

    - `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md`
      — locked decisions D-01..D-20 (D-15..D-19 are the split-runner contingency;
      D-20 is the GoReleaser Pro lock-in / FALLBACK-B canonicalization)
    - `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-01-SUMMARY.md`
      — Wave 1: source-tree CGO=1 flatten (12 `_nocgo.go` deletions; 24
      `//go:build cgo` strips; daemon step-6a deletion; D-14 Path-3
      windows-arm64 platform stub)
    - `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SUMMARY.md`
      — Wave 2: `.goreleaser.yaml` flipped to CGO=1 with two `builds:`
      entries (`helix-non-darwin` zig cc + `helix-darwin` Apple clang);
      Makefile zig-presence guard
    - `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SPLIT-PROBE-NOTES.md`
      — Wave 2 prerequisite probe: GoReleaser Pro lock-in evidence
      (`partial.by_target` / `release --split` / `goreleaser continue` are
      Pro-exclusive at every v2.x patch level)
    - `.github/workflows/release.yml` (Wave 3) — split-runner pipeline:
      `release-linux` (ubuntu-22.04, zig cc) + `release-darwin` (macos-14,
      Apple clang, tag-gated) + `release-merge` (uniform cosign across 6
      archives)

    **Mechanism note (FALLBACK-B-MULTI-BUILD-ID):**

    The original DEF-51-02 Path 3 sketch assumed cross-compile complexity
    would be solved by GoReleaser's `partial: { by_target: true }` + `release
    --split` mechanic. Wave 2 prerequisite probe (linked above) discovered
    that mechanic is **GoReleaser Pro exclusive** (verified directly against
    https://goreleaser.com/customization/partial/). Helix runs on the OSS
    distribution and FALLBACK-A (bumping the goreleaser pin) does not unlock
    the feature because GoReleaser Pro is a paid commercial license, not a
    different OSS tag. The OSS-supported equivalent is FALLBACK-B-MULTI-BUILD-ID:
    each runner invokes `goreleaser build --id <runner-id>` with `--snapshot
    --clean`; the merge job runs `goreleaser release --skip=build` to assemble
    archives + checksums + signatures from the pre-built binaries.
    Architectural intent (split topology, per-runner reproducibility, uniform
    cosign attestation across 6 archives) is preserved entirely; only the
    syntactic mechanism differs from the original sketch.

    **`helix upgrade` artifact-name and `.sigstore.json` bundle layout
    preserved.** Phase 58 REL-01 contract holds.
    ```

    **EDIT 4 — `.planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md` (supersede cross-reference).**

    Locate the section discussing D-02 (daemon refuses CGO=0) or the conclusion / hand-off section. Append a brief supersede note (do NOT edit the historical decisions; the supersede note goes at the end of the file or in a new "Subsequent History" section):

    ```markdown
    ---

    ## Subsequent History (added 2026-05-04)

    Phase 51.1 D-02 (daemon refuses to start under `CGO_ENABLED=0` with a clear
    remediation message) was **inverted** by Phase 59.1. Rather than maintaining
    the CGO=0 stub apparatus (`treesitter.Available` runtime check, ~12 stub
    files), Phase 59.1 removed the CGO=0 build path entirely:

    - The source tree no longer carries `//go:build cgo` constraints (except
      the platform-conditional `internal/semantic/store/duckdb.go` `!(windows
      && arm64)` from 59.1 D-14, which is platform-conditional, not
      CGO-conditional).
    - The daemon's step-6a `treesitter.Available` refusal block is gone; the
      `treesitter.Available` constant itself is removed.
    - The release pipeline ships single-mode CGO=1 archives via a split-runner
      pipeline (linux+windows zig cc on ubuntu-22.04; darwin Apple clang on
      macos-14).

    Phase 51.1 closed `INT-BLOCKER-01` symptomatically (daemon refused under
    CGO=0 with a clear message); Phase 59.1 closed it root-cause (no more
    CGO=0 build path to refuse). See:

    - `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/`
      (CONTEXT.md, RESEARCH.md, PLAN files, SUMMARY files)

    The decision-trail strikethrough/supersede tail on the 51.1 row in
    `.planning/PROJECT.md` Key Decisions table records this inversion for
    future readers in one place.
    ```

    **EDIT 5 — `.planning/milestones/v1.10-ROADMAP.md` Phase 59.1 listing.**

    Locate the existing Phase 59.1 entry (or section) in the ROADMAP. Update the description / mechanism summary to reflect the FALLBACK-B realization:

    Find the `Phase 59.1` heading or table row. Update the body text to include:

    ```markdown
    ### Phase 59.1: drop-cgo-0-single-mode-cgo-1-build-release

    **Status:** in progress (Waves 0/1/2 complete; Waves 3/4/5 in selective replan).

    **Goal:** Close `INT-BLOCKER-01` from v1.10-MILESTONE-AUDIT.md by removing
    the CGO=0 build path entirely (inverting Phase 51.1 D-02). Establish
    single-mode CGO=1 build and release pipeline.

    **Realized mechanism (FALLBACK-B-MULTI-BUILD-ID per D-20, locked 2026-05-04):**

    - **Source tree (Wave 1, complete):** single-mode CGO=1; 12 `_nocgo.go`
      stubs deleted; 24 `//go:build cgo` tags stripped; daemon step-6a
      removed; `internal/semantic/store/duckdb_winarm64.go` Path-3 platform
      stub added.
    - **Goreleaser config (Wave 2, complete):** `.goreleaser.yaml` declares
      two `builds:` entries — `helix-non-darwin` (linux/{amd64,arm64} +
      windows/{amd64,arm64} with `CC=zig cc -target <triple>`) and
      `helix-darwin` (darwin/{amd64,arm64} with no CC override; Apple clang
      on macos-14 is host default). `archives.ids: [helix-non-darwin,
      helix-darwin]`.
    - **Release workflow (Wave 3):** three jobs in `.github/workflows/release.yml`:
      - `release-linux` (ubuntu-22.04, SHA-pinned `mlugg/setup-zig` + zig
        0.14.1, Pass-1≡Pass-2 reproducibility over 4 helix-non-darwin
        binaries)
      - `release-darwin` (macos-14 Apple Silicon, tag-gated per D-16, Xcode
        15.x, Pass-1≡Pass-2 reproducibility over 2 helix-darwin binaries)
      - `release-merge` (ubuntu-22.04, `needs:` both, downloads partial
        dists, runs `goreleaser release --skip=build`, uniform cosign
        keyless attestation across all 6 archives, publishes GitHub Release)
    - **Doc sweep (Wave 4):** CLAUDE.md / CONTRIBUTING.md / README.md /
      INSTALL.md / CHANGELOG.md / .planning/PROJECT.md / REQUIREMENTS.md /
      deferred-items + 51-packaging deferred-items + 51.1 SUMMARY +
      v1.10-ROADMAP — D-11/D-12/D-13/D-19/D-20 cross-references and Gatekeeper
      workaround for unsigned darwin binaries.
    - **End-to-end exercise (Wave 5):** `make release-smoke` finalization +
      first-CI-repro evidence captured via `gh workflow run release.yml --ref
      <draft-branch>` (workflow_dispatch trigger added in Wave 3); all 6
      archives appear with correct names; cosign attestation runs uniformly;
      per-runner Pass-1≡Pass-2 evidence recorded into
      `59.1-05-FIRST-CI-REPRO-LOG.md`.

    **Mechanism source-of-truth:**

    `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-02-SPLIT-PROBE-NOTES.md`
    — Wave 2 prerequisite probe verdict (line 137): GoReleaser's
    `partial: { by_target: true }` + `release --split` + `goreleaser continue
    --merge` are GoReleaser Pro exclusive. Helix runs OSS. FALLBACK-A
    (bump goreleaser pin) was rejected because Pro is a paid commercial
    license. FALLBACK-B is the OSS-supported equivalent — same architectural
    intent, different syntax.
    ```

    **Negative checks across all 5 files:**

    ```bash
    # No path-A references in current-tense planning prose:
    ! grep -qE 'partial: ?\{ ?by_target' .planning/milestones/v1.10-ROADMAP.md
    ! grep -qE 'release --split|goreleaser continue --merge' .planning/milestones/v1.10-ROADMAP.md
    # PROJECT.md preserves the strikethrough trail:
    grep -qF '~~' .planning/PROJECT.md && grep -qF 'superseded by Phase 59.1' .planning/PROJECT.md
    # 51.1 SUMMARY supersede note:
    grep -qF 'Phase 59.1' .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md
    # 51-packaging deferred-items DEF-51-03 closure:
    grep -qF 'DEF-51-03' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md
    grep -qF 'FALLBACK-B' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md
    ```

    Commit message: `docs(phase-59.1-04): planning lineage doc sweep (PROJECT/REQUIREMENTS/51-deferred/51.1-SUMMARY/v1.10-ROADMAP) per D-11/D-13/D-20`.
  </action>
  <verify>
    <automated>grep -qF 'Phase 59.1' .planning/PROJECT.md && grep -qF 'superseded by Phase 59.1' .planning/PROJECT.md && grep -qF '~~' .planning/PROJECT.md && grep -qF 'DEF-51-03' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'FALLBACK-B' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF '59.1' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'GoReleaser Pro' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'Phase 59.1' .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md && grep -qF '59.1' .planning/milestones/v1.10-ROADMAP.md && grep -qF 'FALLBACK-B' .planning/milestones/v1.10-ROADMAP.md && grep -qF 'single-mode CGO=1' .planning/milestones/v1.10-ROADMAP.md && ! grep -qE 'partial: ?\{ ?by_target' .planning/milestones/v1.10-ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - **PROJECT.md Key Decisions table (D-13):**
      - NEW row for Phase 59.1: `grep -qF 'Phase 59.1' .planning/PROJECT.md`
      - 59.1 row references D-20 / FALLBACK-B (or split-runner): `grep -qE 'FALLBACK-B|split-runner|D-20' .planning/PROJECT.md`
      - Existing 51.1 row strikethrough/supersede tail: `grep -qF '~~' .planning/PROJECT.md && grep -qF 'superseded by Phase 59.1' .planning/PROJECT.md`
    - **REQUIREMENTS.md CGO=1 update or strikethrough:**
      - Either `grep -qF 'CGO_ENABLED=1' .planning/REQUIREMENTS.md` (single-mode CGO=1 invariant present), OR commit message records "no current-tense CGO=0 references found; no edits needed"
      - Deferred entries: `grep -qF 'DEF-59-NOTARIZE' .planning/REQUIREMENTS.md && grep -qF 'DEF-59-DARWIN-CANARY' .planning/REQUIREMENTS.md && grep -qF 'DEF-59-WIN-ARM64-RESTORE' .planning/REQUIREMENTS.md`
    - **51-packaging-goreleaser/deferred-items.md DEF-51-03 closure:**
      - `grep -qF 'DEF-51-03' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`
      - DEF-51-03 status RESOLVED + 59.1 cross-reference: `grep -qF 'RESOLVED' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'Phase 59.1' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`
      - GoReleaser Pro lock-in note: `grep -qF 'GoReleaser Pro' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md && grep -qF 'FALLBACK-B' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`
      - REL-01 preservation note: `grep -qF 'REL-01' .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`
    - **51.1 SUMMARY.md supersede cross-reference:**
      - `grep -qF 'Phase 59.1' .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md`
      - Inversion note present: `grep -qF 'inverted' .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md || grep -qF 'superseded' .planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/SUMMARY.md`
    - **v1.10-ROADMAP.md Phase 59.1 listing:**
      - `grep -qF '59.1' .planning/milestones/v1.10-ROADMAP.md`
      - Realized-mechanism summary: `grep -qF 'FALLBACK-B' .planning/milestones/v1.10-ROADMAP.md && grep -qF 'single-mode CGO=1' .planning/milestones/v1.10-ROADMAP.md && grep -qF 'split-runner' .planning/milestones/v1.10-ROADMAP.md`
      - Wave breakdown present (0/1/2 complete; 3/4/5 selective replan): `grep -qE 'Wave 1|Wave 2|Wave 3|Wave 4|Wave 5' .planning/milestones/v1.10-ROADMAP.md`
      - Source-of-truth pointer to 59.1-02-SPLIT-PROBE-NOTES.md: `grep -qF '59.1-02-SPLIT-PROBE-NOTES.md' .planning/milestones/v1.10-ROADMAP.md`
    - **Negative checks:**
      - `! grep -qE 'partial: ?\{ ?by_target' .planning/milestones/v1.10-ROADMAP.md`
      - `! grep -qE 'release --split|goreleaser continue --merge' .planning/milestones/v1.10-ROADMAP.md`
    - All 5 files commit cleanly (Markdown table column counts preserved in PROJECT.md)
  </acceptance_criteria>
  <done>
    - PROJECT.md Key Decisions table has new 59.1 row + 51.1 strikethrough/supersede tail (D-13)
    - REQUIREMENTS.md reflects single-mode CGO=1 invariant; deferred entries surface DEF-59-NOTARIZE / DEF-59-DARWIN-CANARY / DEF-59-WIN-ARM64-RESTORE
    - 51-packaging deferred-items.md has DEF-51-03 RESOLVED entry pointing at 59.1 + FALLBACK-B + GoReleaser Pro lock-in note + REL-01 preservation
    - 51.1 SUMMARY.md has "Subsequent History" section noting Phase 59.1 inversion of D-02
    - v1.10-ROADMAP.md Phase 59.1 listing has full realized-mechanism summary with wave breakdown + source-of-truth pointer
    - No path-A references in current-tense planning prose
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create `.planning/deferred-items.md` (NEW project-level file) with three new entries: DEF-59-NOTARIZE (D-19), DEF-59-DARWIN-CANARY (D-16), DEF-59-WIN-ARM64-RESTORE (Wave 1 platform stub restoration)</name>
  <files>.planning/deferred-items.md</files>
  <read_first>
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md (D-16 nightly canary deferred + D-19 notarization deferred sections)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md (Post-Research Decision Lock D-14: windows-arm64 platform stub closed by Wave 1; restoration = upstream lib availability)
    - .planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-01-SUMMARY.md (Wave 1 — duckdb_winarm64.go shipped; restoration tracking note)
    - .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md (existing milestone-level deferred-items file — formatting / shape template; project-level file mirrors this shape but is index of all deferred items across milestones)
  </read_first>
  <action>
    The file `.planning/deferred-items.md` does NOT yet exist (verified: project-context grep returned only milestone-level deferred-items files, no project-level one). This task CREATES it.

    Use the Write tool (NOT heredoc) to create the file with this content:

    ```markdown
    # Helix Deferred Items (project-level index)

    > Project-level register of items that were considered, scoped, but
    > intentionally deferred to a later phase or milestone. Each entry has a
    > stable `DEF-<phase>-<slug>` ID, a citation back to the phase/decision
    > that deferred it, and the trigger condition that would justify
    > reconsidering.

    > Milestone-level deferred-items files
    > (`.planning/milestones/<milestone>/<phase>/deferred-items.md`) remain
    > the source-of-truth for phase-internal scoping; this file is the
    > project-level INDEX surfacing items that may matter to future
    > milestones.

    ---

    ## DEF-59-NOTARIZE: Apple Developer ID code signing + notarization for darwin archives

    **Deferred by:** Phase 59.1 D-19 (locked 2026-05-04).

    **Source artifacts:**
    - `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md` (D-19)
    - `INSTALL.md` "macOS Gatekeeper workaround" section
    - `README.md` macOS install note

    **What was deferred:**

    Apple Developer ID code signing + notarization through Apple's notary
    service (`notarytool`). Without these signatures, macOS Gatekeeper blocks
    the unsigned darwin binaries on first launch with errors like:

    > "helix" can't be opened because Apple cannot check it for malicious
    > software.

    Or:

    > "helix" is damaged and can't be opened. You should move it to the Trash.

    Users currently must use the right-click → Open workaround (documented in
    `INSTALL.md`) or clear the quarantine xattr from a terminal.

    **Why deferred:**

    - Apple Developer Program membership costs $99/year — a real expense
      that requires user / org sign-off.
    - Apple-credential management infrastructure (`AC_PASSWORD` /
      `AC_USERNAME` / Apple ID / app-specific-password) needs design.
    - Phase 59.1 is scoped to the CGO=1 build pipeline; signing/notarization
      is a separate concern.

    **Trigger to reconsider:**

    - A real Mac user files a Gatekeeper-blocking install bug.
    - Helix moves toward "production binary distribution" milestone in v1.11+.
    - Annual macOS release (Sequoia / Tahoe / etc.) tightens Gatekeeper
      defaults to the point that the right-click workaround no longer works.

    **Implementation sketch:**

    1. Provision an Apple Developer Program account.
    2. Add `notarytool` step in the `release-darwin` job:
       ```yaml
       - name: Notarize darwin binaries
         env:
           AC_USERNAME: ${{ secrets.APPLE_ID }}
           AC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
           AC_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
         run: |
           xcrun notarytool submit dist/helix-darwin_*/helix \
             --apple-id "$AC_USERNAME" \
             --password "$AC_PASSWORD" \
             --team-id "$AC_TEAM_ID" \
             --wait
       ```
    3. Add `goreleaser` `notarize:` block (if goreleaser supports it) or
       perform notarization out-of-band before `goreleaser release`.

    **Cost:** ~$99/yr (Apple Developer Program) + ~$0/run (notarytool is
    free). Implementation effort: 1-2 days including credential setup.

    ---

    ## DEF-59-DARWIN-CANARY: Nightly darwin rebuild canary workflow

    **Deferred by:** Phase 59.1 D-16 (locked 2026-05-04).

    **Source artifacts:**
    - `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-CONTEXT.md` (D-16)
    - `.github/workflows/release.yml` (`release-darwin` job has tag-gated `if:` per D-16)

    **What was deferred:**

    A separate workflow (`.github/workflows/darwin-canary.yml`) that rebuilds
    darwin from `main` on a daily schedule. Detects Xcode-image drift on
    macos-14 between tag-cut runs (which can be months apart), surfacing
    breakage early instead of at the next release-cut moment.

    **Why deferred:**

    - GitHub bills macos-14 at ~10× the linux rate.
    - 30 macOS-minutes/day × 365 days = ~180 macOS-hours/year of CI billing
      for an unused-most-of-the-time canary.
    - PR validation already covers linux+windows; darwin breakage on PRs is
      intentionally not gated and surfaces at tag-cut time (D-16).

    **Trigger to reconsider:**

    - A tag run fails on darwin due to Xcode-image drift between tags.
    - Tag-cut frequency drops below quarterly (drift window widens beyond
      tolerable).
    - GitHub introduces a cheaper macOS runner tier.

    **Implementation sketch:**

    Mirror `release-darwin` job into a separate workflow:
    ```yaml
    name: darwin-canary
    on:
      schedule:
        - cron: '0 6 * * *'   # daily at 06:00 UTC
      workflow_dispatch:
    jobs:
      canary:
        runs-on: macos-14
        steps:
          # ...same Xcode select + build as release-darwin, but no upload
          # to merge; just `goreleaser build --id helix-darwin --snapshot`
          # and a green/red signal.
    ```
    Failure triggers a Slack/email alert.

    **Cost:** ~30 macOS-minutes/day = ~$2-4/month at GitHub's macos-14 rate.

    ---

    ## DEF-59-WIN-ARM64-RESTORE: Restore native windows-arm64 semantic-store path

    **Deferred by:** Phase 59.1 D-14 (Path-3 platform stub, closed by Wave 1).

    **Source artifacts:**
    - `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-RESEARCH.md` (Post-Research Decision Lock D-14)
    - `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-01-SUMMARY.md` (Wave 1 — duckdb_winarm64.go shipped)
    - `internal/semantic/store/duckdb_winarm64.go` (the platform stub)
    - `internal/semantic/store/duckdb.go` (carries `//go:build !(windows && arm64)`)

    **What was deferred:**

    Native windows-arm64 support for the semantic-store / duckdb backend.
    Currently `internal/semantic/store/duckdb_winarm64.go` returns
    `serr.Unsupported` for the semantic-store entry points on the
    `windows && arm64` target. This keeps the 6-archive goreleaser matrix
    intact while honestly signaling that the semantic-store feature is
    unavailable on win-arm64.

    **Why deferred:**

    - `duckdb-go-bindings` does NOT ship a `lib/windows-arm64` artifact
      upstream.
    - Building `duckdb_static.a` for windows-arm64 from source requires
      MSVC ARM64 cross-compile tooling and is out-of-scope for Phase 59.1.
    - The platform stub is **platform**-conditional, NOT CGO-conditional, so
      it does not violate the phase's "no more `_nocgo.go` accumulation"
      invariant.

    **Trigger to reconsider:**

    - `duckdb-go-bindings` upstream ships a `lib/windows-arm64` artifact.
    - A real Windows-on-ARM user files a "semantic_index unavailable" bug.
    - We adopt MSVC ARM64 cross-compile tooling for other reasons.

    **Implementation sketch:**

    1. Watch `github.com/duckdb/duckdb-go-bindings` for a `lib/windows-arm64`
       directory.
    2. When available: delete `internal/semantic/store/duckdb_winarm64.go`;
       drop the `//go:build !(windows && arm64)` constraint from
       `internal/semantic/store/duckdb.go` (rename if needed).
    3. Update `.goreleaser.yaml` to add `windows/arm64` to the
       `helix-non-darwin` build entry's matrix (currently already there;
       verify).
    4. Validate via a manual Windows-on-ARM device or VM.

    **Cost:** ~1 day of work once upstream lib is available; primary cost
    is the upstream wait.

    ---

    ## Index of milestone-level deferred-items files

    | Milestone | Phase | File |
    |-----------|-------|------|
    | v1.1 | 06-test-harness-go-dogfooding | `.planning/milestones/v1.1-phases/06-test-harness-go-dogfooding/deferred-items.md` |
    | v1.1 | 08-advanced-testing | `.planning/milestones/v1.1-phases/08-advanced-testing/deferred-items.md` |
    | v1.9 | 51-packaging-goreleaser | `.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` |
    | v1.9 | 51.1-cgo-treesitter-gate | `.planning/milestones/v1.9-phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/deferred-items.md` |
    | v1.9 | 52-packaging-distribution-channels | `.planning/milestones/v1.9-phases/52-packaging-distribution-channels/deferred-items.md` |
    ```

    After writing, run `wc -l .planning/deferred-items.md` to verify file size is reasonable (~150-250 lines) — too short means content was truncated; too long means accidental duplication.

    Commit message: `docs(phase-59.1-04): create project-level .planning/deferred-items.md with DEF-59-NOTARIZE/DARWIN-CANARY/WIN-ARM64-RESTORE`.
  </action>
  <verify>
    <automated>test -f .planning/deferred-items.md && grep -qF 'DEF-59-NOTARIZE' .planning/deferred-items.md && grep -qF 'DEF-59-DARWIN-CANARY' .planning/deferred-items.md && grep -qF 'DEF-59-WIN-ARM64-RESTORE' .planning/deferred-items.md && grep -qF 'D-19' .planning/deferred-items.md && grep -qF 'D-16' .planning/deferred-items.md && grep -qF 'D-14' .planning/deferred-items.md && grep -qF 'Apple Developer Program' .planning/deferred-items.md && grep -qF 'macos-14' .planning/deferred-items.md && grep -qF 'duckdb-go-bindings' .planning/deferred-items.md && grep -qF '99' .planning/deferred-items.md && test "$(wc -l < .planning/deferred-items.md)" -ge 100</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/deferred-items.md`
    - File length sane (≥100 lines, ≤400 lines): `LINES=$(wc -l < .planning/deferred-items.md); [ "$LINES" -ge 100 ] && [ "$LINES" -le 400 ]`
    - Three deferred-item IDs present:
      - `grep -qF 'DEF-59-NOTARIZE' .planning/deferred-items.md`
      - `grep -qF 'DEF-59-DARWIN-CANARY' .planning/deferred-items.md`
      - `grep -qF 'DEF-59-WIN-ARM64-RESTORE' .planning/deferred-items.md`
    - Each entry has the four standard fields:
      - **Deferred by:** with phase/decision citation: `grep -cE '^\*\*Deferred by:\*\*' .planning/deferred-items.md` returns at least 3
      - **What was deferred:** description: `grep -cE 'What was deferred' .planning/deferred-items.md` returns at least 3
      - **Why deferred:** rationale: `grep -cE 'Why deferred' .planning/deferred-items.md` returns at least 3
      - **Trigger to reconsider:** condition: `grep -cE 'Trigger to reconsider' .planning/deferred-items.md` returns at least 3
    - DEF-59-NOTARIZE specifics:
      - `grep -qF 'Apple Developer Program' .planning/deferred-items.md`
      - `grep -qF '$99' .planning/deferred-items.md` (cost framing)
      - `grep -qF 'notarytool' .planning/deferred-items.md`
      - `grep -qF 'D-19' .planning/deferred-items.md`
    - DEF-59-DARWIN-CANARY specifics:
      - `grep -qF 'macos-14' .planning/deferred-items.md`
      - `grep -qF '10×' .planning/deferred-items.md || grep -qF '10x' .planning/deferred-items.md` (cost-bounding rationale)
      - `grep -qF 'D-16' .planning/deferred-items.md`
      - `grep -qF 'darwin-canary.yml' .planning/deferred-items.md || grep -qF 'cron:' .planning/deferred-items.md`
    - DEF-59-WIN-ARM64-RESTORE specifics:
      - `grep -qF 'duckdb-go-bindings' .planning/deferred-items.md`
      - `grep -qF 'lib/windows-arm64' .planning/deferred-items.md`
      - `grep -qF 'D-14' .planning/deferred-items.md`
      - `grep -qF 'duckdb_winarm64.go' .planning/deferred-items.md`
    - Index of milestone-level deferred-items files present (helps future readers locate phase-internal items): `grep -qF 'milestone-level deferred-items' .planning/deferred-items.md`
    - Header / introduction explains project-level vs milestone-level scope: `grep -qF 'project-level' .planning/deferred-items.md && grep -qF 'milestone-level' .planning/deferred-items.md`
    - Cross-references intact:
      - `grep -qF 'INSTALL.md' .planning/deferred-items.md`
      - `grep -qF '59.1-CONTEXT.md' .planning/deferred-items.md`
  </acceptance_criteria>
  <done>
    - `.planning/deferred-items.md` created as project-level deferred-items index
    - Three new entries seeded from Phase 59.1: DEF-59-NOTARIZE, DEF-59-DARWIN-CANARY, DEF-59-WIN-ARM64-RESTORE
    - Each entry has Deferred-by / What / Why / Trigger / Implementation-sketch / Cost fields
    - Header explains project-level vs milestone-level scope
    - Index of milestone-level deferred-items files included for future-reader navigation
    - INSTALL.md / 59.1-CONTEXT.md cross-references intact
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User reading docs ↔ actual build pipeline behavior | If docs drift from reality, contributors hit "I followed the doc but it failed" — D-12 user/builder seam preservation is the mitigation |
| Future-reader of PROJECT.md Key Decisions ↔ historical decision trail | Strikethrough/supersede tail (D-13) makes the inversion visible in one place; if it's missing, future readers don't know D-02 was flipped |
| User running darwin binary ↔ macOS Gatekeeper | D-19 unsigned-binary UX: Gatekeeper blocks on first run; INSTALL.md workaround is the only thing keeping users from filing "helix doesn't work on macOS" bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59.1-W4-01 | Repudiation | Future reader doesn't understand why CGO=0 path was removed (no decision trail) | mitigate | D-13 strikethrough/supersede tail on the 51.1 row in PROJECT.md Key Decisions table; 51.1 SUMMARY.md "Subsequent History" section; 51-packaging deferred-items DEF-51-03 closure entry — three independent pointers to 59.1 |
| T-59.1-W4-02 | DoS | Real Mac user can't run helix because Gatekeeper blocks it | mitigate | INSTALL.md "macOS Gatekeeper workaround" section with right-click + xattr workaround + DEF-59-NOTARIZE pointer; README.md hero install section appends Gatekeeper pointer to INSTALL.md |
| T-59.1-W4-03 | Tampering | Doc drift to path-A references (`partial: { by_target: true }`) misleads contributors who copy-paste old prose | mitigate | Negative greps in acceptance criteria block path-A YAML / CLI strings in all 11 files; CHANGELOG / CONTRIBUTING / ROADMAP explicitly call out FALLBACK-B + GoReleaser Pro lock-in evidence |
| T-59.1-W4-04 | Information Disclosure | Internal phase IDs (`59.1-XX-PLAN.md`) leak into user-facing docs (CHANGELOG / README) | accept | Phase IDs in user-facing docs are intentional cross-reference for future maintainers; not a security issue |
| T-59.1-W4-05 | DoS | DEF-59-NOTARIZE never gets reconsidered → user-facing pain compounds | mitigate | DEF-59-NOTARIZE entry has explicit "Trigger to reconsider" conditions (real Mac user files bug, production-binary milestone, Gatekeeper tightening) — surfaces in `.planning/deferred-items.md` index for any future maintainer doing a deferred-items review |
| T-59.1-W4-06 | Tampering | User-facing single-binary message gets diluted in the doc sweep | mitigate | D-12 user seam preservation explicit in plan: hero / Project section / README install hero MUST stay verbatim; Task 1 acceptance criteria flag this for manual review |
| T-59.1-W4-07 | DoS | `.planning/deferred-items.md` doesn't exist yet — risk of writing it in wrong location | mitigate | Task 3 explicitly creates the file at `.planning/deferred-items.md` (verified during planning that no such file exists at that path) |
</threat_model>

<verification>
- 11 files modified or created: 5 from Task 1 (CLAUDE/CONTRIBUTING/README/INSTALL/CHANGELOG), 5 from Task 2 (PROJECT/REQUIREMENTS/51-deferred/51.1-SUMMARY/v1.10-ROADMAP), 1 created from Task 3 (.planning/deferred-items.md)
- D-12 user/builder seam preserved (manual review confirms hero / Project / README install hero unchanged; build-side prose expanded in CLAUDE.md Technology Stack and CONTRIBUTING.md Releasing)
- D-13 strikethrough/supersede tail on 51.1 row present in PROJECT.md
- D-19 Gatekeeper workaround present in INSTALL.md (right-click + xattr) + README.md pointer
- Three new deferred items in `.planning/deferred-items.md`: DEF-59-NOTARIZE / DEF-59-DARWIN-CANARY / DEF-59-WIN-ARM64-RESTORE
- 51-packaging deferred-items.md has DEF-51-03 closure pointing at 59.1
- 51.1 SUMMARY.md has supersede cross-reference
- v1.10-ROADMAP.md has FALLBACK-B realized-mechanism summary with wave breakdown
- CHANGELOG.md v1.10.x entry has Phase 59.1 framing
- No path-A references (`partial: { by_target: true }`, `release --split`, `goreleaser continue --merge`) in any user-facing or planning prose
- All 11 files commit cleanly; Markdown table column counts preserved
</verification>

<success_criteria>
- All 11 files modified or created per files_modified frontmatter
- D-11 doc sweep complete: every cited file has the appropriate update
- D-12 user seam preserved: user-facing single-binary message untouched
- D-13 decision trail visible in PROJECT.md (strikethrough + supersede + new row)
- D-19 Gatekeeper workaround in INSTALL.md + README.md
- D-20 FALLBACK-B mechanism documented; path-A references purged from current-tense prose
- Three NEW deferred items recorded with full Trigger/Cost/Implementation fields
- 51-packaging deferred-items DEF-51-03 closed with FALLBACK-B + GoReleaser Pro lock-in note
- 51.1 SUMMARY.md has supersede cross-reference back to 59.1
- v1.10-ROADMAP.md has realized-mechanism summary with wave breakdown
- CHANGELOG v1.10.x entry frames the Phase 59.1 build-pipeline change for the user reader
</success_criteria>

<output>
After completion, create `.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/59.1-04-SUMMARY.md` recording:
- Per-file diff hunks (concise — most edits are appends or replacements)
- Key text excerpts:
  - PROJECT.md 51.1 row before/after (showing strikethrough)
  - PROJECT.md new 59.1 row content
  - CONTRIBUTING.md FALLBACK-B canonical commands block
  - INSTALL.md Gatekeeper workaround
  - CHANGELOG.md v1.10.x entry
- Three NEW deferred-item IDs and their cost/trigger fields
- Confirmation that user-facing single-binary prose is unchanged (manual diff check)
- Negative-grep sweep results (path-A absent across all 11 files)
- Hand-off note for Wave 5: ROADMAP / CHANGELOG / INSTALL are now in user-facing-ready state for the v1.10.x release-cut moment
</output>
