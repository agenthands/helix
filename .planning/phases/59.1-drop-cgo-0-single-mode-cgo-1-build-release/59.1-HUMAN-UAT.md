---
status: partial
phase: 59.1-drop-cgo-0-single-mode-cgo-1-build-release
source: [59.1-VERIFICATION.md]
started: 2026-05-04T22:00:00Z
updated: 2026-05-05T08:30:00Z
---

## Current Test

number: 2
name: Linux+Windows CI build path resolution (DEF-59.1-LINUX-ZIG-LIBSTDCXX)
expected: |
  After picking one of 4 documented resolution options in `.planning/deferred-items.md` (zig-musl→gnu / host gcc cross / drop targets / compile duckdb from source), `release-linux` job exits 0, produces 4 `helix-non-darwin` archives, per-runner Pass-1≡Pass-2 PASS on linux runner, `release-merge` succeeds end-to-end with full 6-archive matrix and signed sigstore bundles. This is Phase 59.2 scope — the user must select an option and either schedule 59.2 now or defer.
awaiting: user response

## Tests

### 1. Real-tag cosign self-test
expected: All archives published and signed on a real `vX.Y.Z` tag push; `cosign verify-blob --new-bundle-format` succeeds against the downloaded `.sigstore.json` bundle (real cosign CLI, NOT in-process CA library substitution per project memory rule `feedback_release_artifact_self_test.md`).
result: blocked
blocked_by: prior-phase
reason: |
  Tag `v1.10.1` cut at HEAD `70622ae0` and pushed to origin 2026-05-05T08:06Z. Run `25365003032` (push trigger, real tag) ran 15m 35s. release-darwin succeeded (3m 22s, 2 archives). release-linux failed (15m 25s) at zig cc link step with 18 libstdc++ undefined-symbol errors from `libduckdb_static.a` — same DEF-59.1-LINUX-ZIG-LIBSTDCXX symptom as Wave 5 dispatch run 25342130151. release-merge skipped (needs failed). No GitHub Release published, no .sigstore.json bundle exists. cosign verify-blob has no signed artifact to verify against — test is physically not runnable until DEF-59.1-LINUX-ZIG-LIBSTDCXX is resolved (Phase 59.2). Evidence: 59.1-05-v1.10.1-release-linux-failed.log, 59.1-05-v1.10.1-run-summary.json, FIRST-CI-REPRO-LOG.md "Real-tag-push verification" section. Path-independence between trigger types now empirically proven (workflow_dispatch + tag-push both reproduce identical failure).

### 2. Linux+Windows CI build path resolution (DEF-59.1-LINUX-ZIG-LIBSTDCXX)
expected: After picking one of 4 documented options (zig-musl→gnu / host gcc cross / drop targets / compile duckdb from source), `release-linux` job exits 0, produces 4 `helix-non-darwin` archives, per-runner Pass-1≡Pass-2 PASS on linux runner, `release-merge` succeeds end-to-end with full 6-archive matrix.
result: [pending]
why_human: Architectural decision per Rule 4 — requires maintainer to select among trade-offs (hermeticity vs scope vs effort). Phase 59.2 scope, not 59.1.

### 3. Darwin Gatekeeper workaround UX validation
expected: A real Mac user can clear Gatekeeper on the unsigned darwin archives using the documented workflow (right-click → Open OR `xattr -d com.apple.quarantine`) without external help. INSTALL.md documentation is sufficient.
result: [pending]
why_human: User-experience verification of unsigned binary install path; cannot be programmatically asserted.

## Summary

total: 3
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 1

## Gaps
