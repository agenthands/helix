---
status: partial
phase: 59.1-drop-cgo-0-single-mode-cgo-1-build-release
source: [59.1-VERIFICATION.md]
started: 2026-05-04T22:00:00Z
updated: 2026-05-05T09:55:00Z
---

## Current Test

number: 3
name: Darwin Gatekeeper workaround UX validation
expected: |
  A real Mac user can clear Gatekeeper on the unsigned darwin archives using the
  documented INSTALL.md workflow (right-click → Open OR `xattr -d com.apple.quarantine`)
  without external help. User-experience verification of the unsigned binary install
  path; cannot be programmatically asserted.
awaiting: user response

## Tests

### 1. Real-tag cosign self-test
expected: All archives published and signed on a real `vX.Y.Z` tag push; `cosign verify-blob --new-bundle-format` succeeds against the downloaded `.sigstore.json` bundle (real cosign CLI, NOT in-process CA library substitution per project memory rule `feedback_release_artifact_self_test.md`).
result: pass
verified: 2026-05-05T09:55:00Z
evidence: |
  Tag v1.10.7 cut at HEAD `2bfa036f` and pushed 2026-05-05T09:48Z. Run
  `25369361893` (push trigger, real tag) — all 3 jobs green:
    - release-linux: success
    - release-darwin: success
    - release-merge: success (manual tar+sign+publish pipeline)
  GitHub Release v1.10.7 published with 9 assets (4 archives + 4 .sigstore.json
  bundles + checksums.txt). All 4 archives verified end-to-end with the real
  cosign CLI (v2.4.1), `--new-bundle-format`, --certificate-identity-regexp
  pinned to the v1.10.7 release.yml workflow at refs/tags/v1.10.7, and
  --certificate-oidc-issuer pinned to https://token.actions.githubusercontent.com:
    - helix_v1.10.7_Linux_amd64.tar.gz   → Verified OK
    - helix_v1.10.7_Linux_arm64.tar.gz   → Verified OK
    - helix_v1.10.7_Darwin_amd64.tar.gz  → Verified OK
    - helix_v1.10.7_Darwin_arm64.tar.gz  → Verified OK
  Phase 58 REL-01 contract preserved exactly (artifact name shape,
  .sigstore.json bundle layout, --new-bundle-format protobuf bundle, public-good
  TSA at timestamp.sigstore.dev). Verification command logged in
  59.1-05-FIRST-CI-REPRO-LOG.md "Real-tag-push verification (v1.10.7)" section.

### 2. Linux+Windows CI build path resolution (DEF-59.1-LINUX-ZIG-LIBSTDCXX)
expected: After picking one of 4 documented options (zig-musl→gnu / host gcc cross / drop targets / compile duckdb from source), `release-linux` job exits 0, produces helix-non-darwin archives, per-runner Pass-1≡Pass-2 PASS on linux runner, `release-merge` succeeds end-to-end with the published archive matrix.
result: pass
verified: 2026-05-05T09:55:00Z
resolution: |
  Resolved inline during Wave 5 hot-fix loop (v1.10.1 → v1.10.7) by selecting
  Option 2 (host gcc + apt-installed cross toolchain) and excluding windows
  entirely. Empirical iteration log:
    - v1.10.1 (zig+musl)      → ld.lld libstdc++ undefined symbols
    - v1.10.2 (zig+gnu)       → identical libstdc++ symbol failure (musl→gnu
                                only changes libc, not libcxx)
    - v1.10.3 (host gcc linux + zig windows) → linux PASSED; windows lld-link
                                identical libstdc++ failure (zig's mingw bundle
                                also ships libc++ exclusively)
    - v1.10.4 (apt mingw-w64-x86-64-default-threads + drop windows/arm64)
                              → win32 thread model: pthread_mutex_* undefined
    - v1.10.5 (mingw-w64-posix-threads + winpthread static link) → pthread errors
                                resolved; libstdc++ version drift exposed
                                (12 unique missing symbols including
                                std::__throw_bad_array_new_length, UCRT-vs-msvcrt
                                drift on __imp___timezone, __imp___tzname,
                                __stdio_common_v(s)nprintf_s)
    - v1.10.6 (drop windows entirely; --skip=build merge) → linux+darwin builds
                                PASSED but `goreleaser release --skip=build`
                                rejected by OSS goreleaser v2.15.4 (Pro-only)
    - v1.10.7 (manual tar+sign+publish merge step)        → ALL GREEN, all 4
                                archives signed and verified.
  Final architecture (locked under D-20 amended):
    - linux/amd64    → ubuntu-22.04 host gcc / g++ (host default; libstdc++/glibc)
    - linux/arm64    → gcc-aarch64-linux-gnu / g++-aarch64-linux-gnu (apt cross)
    - darwin/{amd64,arm64} → macos-14 Apple clang (host default; libc++/Mach-O)
    - windows/{amd64,arm64} → EXCLUDED. Tracked under DEF-59.1-WINDOWS-CGO-MINGW
                                (amd64) and DEF-59-WIN-ARM64-RESTORE (arm64) for
                                a future phase resolution path
                                (llvm-mingw / clang-cl on windows runner /
                                duckdb from source under matching toolchain).
  Phase 59.2 carries the windows restoration; 59.1's 4-archive matrix is the
  shipped baseline.

### 3. Darwin Gatekeeper workaround UX validation
expected: A real Mac user can clear Gatekeeper on the unsigned darwin archives using the documented workflow (right-click → Open OR `xattr -d com.apple.quarantine`) without external help. INSTALL.md documentation is sufficient.
result: [pending]
why_human: User-experience verification of unsigned binary install path; cannot be programmatically asserted.

## Summary

total: 3
passed: 2
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
