# Phase 59.1: drop-cgo-0-single-mode-cgo-1-build-release - Pattern Map

**Mapped:** 2026-05-04
**Files analyzed:** 51 (12 stub deletes + 1 nocgo-test delete + 24 tag strips + 4 file renames + 1 NEW platform stub + 9 doc/release/Makefile edits)
**Analogs found:** 51 / 51 (every file has a concrete analog in HEAD)

## File Classification

### Wave 1 — Deletions (13 files)

| File to delete | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `internal/treesitter/registry_nocgo.go` | stub deletion | n/a | `internal/treesitter/registry_cgo.go` (sibling, becomes plain `.go`) | exact |
| `internal/treesitter/available_nocgo.go` | stub deletion | n/a | `internal/treesitter/available_cgo.go` (entire pair deleted; `Available` const goes) | exact |
| `internal/treesitter/available_cgo.go` | full file deletion | n/a | (no analog — `treesitter.Available` constant retired entirely per D-07) | full delete |
| `internal/repomap/extractor_nocgo.go` | stub deletion | n/a | `internal/repomap/extractor.go` (sibling) | exact |
| `internal/repomap/elide_nocgo.go` | stub deletion | n/a | `internal/repomap/elide.go` (sibling) | exact |
| `internal/kernel/edit/treesitter_nocgo.go` | stub deletion | n/a | `internal/kernel/edit/treesitter.go` (sibling) | exact |
| `internal/semantic/store/duckdb_nocgo.go` | stub deletion | n/a | `internal/semantic/store/duckdb.go` (sibling) | exact |
| `internal/semantic/store/duckdb_nocgo_test.go` | nocgo test deletion | n/a | (no analog — tests retire with the stub they cover) | full delete |
| `internal/semantic/extract/extract_nocgo.go` | stub deletion | n/a | `internal/semantic/extract/extract_cgo.go` (sibling, renamed → `extract.go`) | exact |
| `internal/semantic/extract/golang/provider_nocgo.go` | stub deletion | n/a | `internal/semantic/extract/golang/provider.go` (sibling) | exact |
| `internal/semantic/extract/python/provider_nocgo.go` | stub deletion | n/a | `internal/semantic/extract/python/provider.go` (sibling) | exact |
| `internal/semantic/extract/typescript/provider_nocgo.go` | stub deletion | n/a | `internal/semantic/extract/typescript/provider.go` (sibling) | exact |
| `internal/semantic/extract/testutil/grammar_nocgo.go` | stub deletion | n/a | `internal/semantic/extract/testutil/grammar.go` (sibling) | exact |

### Wave 1 — Renames / flattens (4 files)

| File | Role | Action | Analog (= the file itself, post-rename) |
|---|---|---|---|
| `internal/treesitter/registry_cgo.go` | tag-strip + rename | `git mv` to `registry.go`, drop `//go:build cgo` line 1 | self-rename; same content minus header |
| `internal/semantic/store/migrations_registry_cgo.go` | tag-strip + rename | `git mv` to `migrations_registry.go`, drop `//go:build cgo` | self-rename |
| `internal/semantic/extract/extract_cgo.go` | tag-strip + rename | `git mv` to `extract.go`, drop `//go:build cgo` | self-rename |
| `internal/semantic/store/duckdb.go` | tag-strip only (filename already plain) | drop `//go:build cgo` line 1 | (no rename) |

### Wave 1 — Tag strips on production files (5 files, no rename)

| File | Role | Closest Analog | Notes |
|---|---|---|---|
| `internal/repomap/extractor.go` | tag strip | any other file in `internal/repomap/` without a build tag | drop `//go:build cgo` only |
| `internal/repomap/elide.go` | tag strip | (same) | drop `//go:build cgo` only |
| `internal/kernel/edit/treesitter.go` | tag strip | any non-tagged file in `internal/kernel/edit/` (e.g. `replace.go`) | drop `//go:build cgo` only |
| `internal/semantic/store/migrations.go` | tag strip | `internal/semantic/store/migrations_types.go` (untagged sibling) | drop `//go:build cgo` only |
| `internal/semantic/extract/{golang,python,typescript}/provider.go` | tag strip × 3 | the package's other untagged files (e.g. `internal/semantic/extract/registry.go`) | drop `//go:build cgo` only |
| `internal/semantic/extract/testutil/grammar.go` | tag strip | `internal/semantic/extract/registry.go` (sibling-package untagged) | drop `//go:build cgo` only |
| `internal/treesitter/bindings/r/binding.go` | tag strip | `internal/treesitter/bindings/swift/binding.go` (parallel binding pair) | drop `//go:build cgo` only |
| `internal/treesitter/bindings/swift/binding.go` | tag strip | `internal/treesitter/bindings/r/binding.go` | drop `//go:build cgo` only |

### Wave 1 — Tag strips on test files (16 files)

| File | Build tag now | Build tag after | Notes |
|---|---|---|---|
| `internal/daemon/daemon_extraction_test.go` | `//go:build cgo` | (removed) | plain test |
| `internal/daemon/daemon_grammar_test.go` | `//go:build cgo` | (removed) | plain test |
| `internal/daemon/daemon_semantic_test.go` | `//go:build cgo && integration` | `//go:build integration` | **PARTIAL — Pitfall 5: drop only the `cgo` half, keep `integration`** |
| `internal/kernel/edit/treesitter_test.go` | `//go:build cgo` | (removed) | plain test |
| `internal/repomap/elide_test.go` | `//go:build cgo` | (removed) | plain test |
| `internal/repomap/extractor_test.go` | `//go:build cgo` | (removed) | plain test |
| `internal/repomap/polyglot_rank_test.go` | `//go:build cgo` | (removed) | plain test |
| `internal/repomap/render_test.go` | `//go:build cgo` | (removed) | plain test |
| `internal/semantic/extract/golang/{provider,smoke,stable_id}_test.go` | `//go:build cgo` × 3 | (removed) | plain tests |
| `internal/semantic/extract/python/{provider,smoke,stable_id}_test.go` | `//go:build cgo` × 3 | (removed) | plain tests |
| `internal/semantic/extract/typescript/{provider,smoke,stable_id}_test.go` | `//go:build cgo` × 3 | (removed) | plain tests |
| `internal/semantic/store/migrations_test.go` | `//go:build cgo` | (removed) | plain test |
| `internal/semantic/store/store_test.go` | `//go:build cgo` | (removed) | plain test |
| `internal/skill/repomap/{cache_persistence,metrics_emit,skill_integration,skill}_test.go` | `//go:build cgo` × 4 | (removed) | plain tests |
| `internal/treesitter/registry_test.go` | `//go:build cgo` | (removed) | plain test |

### Wave 1 — In-place edits

| File | Role | Analog | Notes |
|---|---|---|---|
| `internal/daemon/daemon.go` | step-6a refusal block deletion (lines 226-236) + step-6b comment fix (lines 243-244) | (self — concrete diff in Pattern A1 below) | drops `if !treesitter.Available {...}` and the "Under CGO=0..." comment |
| `internal/treesitter/sig_assert.go` | comment cleanup (lines 8-11 — drop the `GetLanguage intentionally omitted` comment) | (self) | the assertion itself stays; just retire the comment about `registry_nocgo.go` (which no longer exists) |

### Wave 1 — NEW file (Path-3 platform stub)

| New file | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `internal/semantic/store/duckdb_winarm64.go` | new platform stub | `serr.Unsupported` short-circuit | **`internal/semantic/store/duckdb_nocgo.go`** (deleted in same wave; copy its shape) | exact (only the build-tag predicate changes from `!cgo` to `windows && arm64`) |

Plus the corresponding header on the flattened `internal/semantic/store/duckdb.go`: it grows a `//go:build !(windows && arm64)` constraint (the only build tag that survives the phase, per D-14).

### Wave 2 — Release pipeline edits

| File | Role | Closest Analog | Match Quality |
|---|---|---|---|
| `.goreleaser.yaml` | config flip — `env:` block | (self lines 13-15 — `CGO_ENABLED=0`, `GOFLAGS=-mod=readonly`) + `goreleaser/example-zig-cgo` master `.goreleaser.yaml` | exact for `env:` shape; templated `CC=zig cc -target ...` from upstream example (RESEARCH §"Pattern 1") |
| `Makefile` | tool-presence guard | `Makefile:56-58` (existing `command -v goreleaser` clause in `release-snapshot`) | exact |
| **NEW** `make release-smoke` target | doc-helper Makefile target (D-04 functional validation) | `Makefile:50-54` (`bench` / `bench-baseline` self-doc style) | role-match (one-line `## help` annotation, runs the snapshot artifact) |

### Wave 3 — CI workflow edit

| File | Role | Closest Analog | Match Quality |
|---|---|---|---|
| `.github/workflows/release.yml` (insert `Set up Zig` step between current line 35 and line 37) | CI step — pinned-by-SHA action | `release.yml:32-35` (`actions/setup-go@3041bf56...  # v5.2.0`) and `release.yml:37-40` (`sigstore/cosign-installer@7e8b541e... # v3.10.1`) | exact |

### Wave 4 — Doc sweep (9 files)

| File | Role | Closest Analog | Match Quality |
|---|---|---|---|
| `CLAUDE.md` (Technology Stack) | doc edit | (self — current `legacy/` / "single binary" framing) | role-match |
| `CONTRIBUTING.md` (Releasing) | doc edit | (self — existing "Releasing" section) | role-match |
| `README.md` | doc edit | (self) | role-match |
| `CHANGELOG.md` (NEW v1.10.x entry) | version-headed entry | `CHANGELOG.md` lines 5-51 (the `## v1.9 — Polish & Infra (...)` block — same prose shape) | exact (Pattern A6 below) |
| `.planning/PROJECT.md` Key Decisions | doc edit | `.planning/PROJECT.md:218-226` (existing rows; line 219 is the row to strikethrough) | exact (Pattern A4 below) |
| `.planning/REQUIREMENTS.md` | doc edit (search-replace) | (self) | role-match |
| `.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` | DEF-51-03 close + NEW deferred entry | `deferred-items.md:127` (DEF-51-02 RESOLVED block — same "Status: RESOLVED via Phase X" framing) | exact (Pattern A5 below) |
| `.planning/milestones/v1.9-phases/51.1-.../SUMMARY.md` | back-pointer | (self) | role-match (one-line "Superseded by Phase 59.1" tail) |
| `.planning/milestones/v1.10-ROADMAP.md` | new entry between Phase 59 and Phase 60 | adjacent entries in same file | role-match |

## Pattern Assignments

### Pattern A1 — Daemon step 6a deletion (`internal/daemon/daemon.go`)

**Source-of-truth excerpt (lines 220-244, read 2026-05-04):**

```go
// 5. Create kernel (fail-fast). obs.Metrics is wired as the lspool sink;
// the compile-time check lives in internal/daemon/wiring_test.go.
k := kernel.NewKernel(workspaces, langReg, installer, kernel.KernelConfig{Pool: poolCfg}, pressure, logger, observability.Metrics(), observability.Tracer())

// 6a. Refuse to start under CGO_ENABLED=0 (DEF-51-02 / Phase 51.1 / D-02).
//     The CGO=0 binary is a goreleaser archive-count placeholder; tree-sitter
//     parsing, RepoMap tag extraction, and replace_symbol_body all depend on
//     CGO bindings. Surface the unavailability loudly rather than starting in
//     a permanently degraded state. Under CGO=1, treesitter.Available is a
//     compile-time const true and the Go compiler eliminates this branch.
if !treesitter.Available {
    return nil, fmt.Errorf("tree-sitter is unavailable in this build " +
        "(CGO_ENABLED=0): rebuild with CGO_ENABLED=1 or download the " +
        "CGO=1 release binary (see CONTRIBUTING.md > Releasing)")
}

// 6b. Open semantic fact store when enabled (Phase 57, STORE-01..06).
//     Fail-fast core subsystem; on Tier-2 corruption auto-quarantines to
//     `<path>.corrupt.<ts>` and rebuilds fresh (SPEC §29.1, STORE-01).
//     Tier-3 (rebuild failed) returns an error so the daemon refuses to
//     start. When semantic_index.enabled=false, semanticStore is nil and
//     downstream consumers (P64+ tools) MUST guard. Under CGO=0, step 6a
//     already returned, so this code is unreachable in production stubs.
```

**Target shape (Wave 1, daemon.go after edit):**

```go
// 5. Create kernel (fail-fast). obs.Metrics is wired as the lspool sink;
// the compile-time check lives in internal/daemon/wiring_test.go.
k := kernel.NewKernel(workspaces, langReg, installer, kernel.KernelConfig{Pool: poolCfg}, pressure, logger, observability.Metrics(), observability.Tracer())

// 6b. Open semantic fact store when enabled (Phase 57, STORE-01..06).
//     Fail-fast core subsystem; on Tier-2 corruption auto-quarantines to
//     `<path>.corrupt.<ts>` and rebuilds fresh (SPEC §29.1, STORE-01).
//     Tier-3 (rebuild failed) returns an error so the daemon refuses to
//     start. When semantic_index.enabled=false, semanticStore is nil and
//     downstream consumers (P64+ tools) MUST guard.
```

**Subtleties:**
- `treesitter` import in `daemon.go` may or may not still be needed — verify after the edit; if it was only used for `treesitter.Available`, drop the import too. (Per A1 below: `treesitter.NewGrammarRegistry()` is also called at line 258, so the import stays.)
- DO NOT renumber `6b → 6a`. Anti-pattern per RESEARCH.md "Anti-Patterns to Avoid": "Renumber daemon bootstrap steps after deleting 6a" — Phase 57 already established forward-only numbering precedent.
- Keep `fmt` import — it's used elsewhere in daemon.go.

---

### Pattern A2 — `_nocgo.go` shape (canonical template, deleted in Wave 1)

**Source: `internal/repomap/extractor_nocgo.go` (29 lines, full file):**

```go
//go:build !cgo

package repomap

import (
    "fmt"

    "github.com/agenthands/helix/internal/treesitter"
)

// TagExtractor under !cgo is a stub; the daemon refuses to start before any
// instance is exercised at runtime (see internal/daemon/daemon.go).
type TagExtractor struct{}

// NewTagExtractor under !cgo returns a non-nil empty stub to mirror sibling
// stubs (NewBodyExtractor, NewElisionRenderer). Loud failure is delivered by
// the daemon refusal hook before any caller is exercised; methods on this
// stub still return errors as defense-in-depth if the refusal is bypassed.
func NewTagExtractor(_ *treesitter.GrammarRegistry) (*TagExtractor, error) {
    return &TagExtractor{}, nil
}

// Extract is unreachable under !cgo at runtime (daemon refuses). Returns an
// error sentinel for safety if the stub is ever exercised directly.
func (*TagExtractor) Extract(_ []byte, _ string, _ string) ([]Tag, error) {
    return nil, fmt.Errorf("repomap.TagExtractor.Extract: tree-sitter unavailable (CGO_ENABLED=0)")
}

// Close is a no-op under !cgo.
func (*TagExtractor) Close() {}
```

**Action:** delete this file (Wave 1). Same shape lives in 11 other `_nocgo.go` files; deletion is mechanical via `git rm`.

**Same-shape stub-file inventory (all use `serr.ErrUnsupported` or hand-rolled `fmt.Errorf`, all delete in Wave 1):**

- `internal/treesitter/registry_nocgo.go` (uses neither — empty struct + `SupportsLanguage` bool false; closest to template)
- `internal/treesitter/available_nocgo.go` (1-const file)
- `internal/repomap/elide_nocgo.go` (returns `""` strings)
- `internal/repomap/extractor_nocgo.go` (this template)
- `internal/kernel/edit/treesitter_nocgo.go` (returns `fmt.Errorf` + 0,0)
- `internal/semantic/store/duckdb_nocgo.go` (returns `serr.ErrUnsupported`; canonical Phase 57 template — see A3)
- `internal/semantic/store/duckdb_nocgo_test.go` (asserts `errors.Is(err, serr.ErrUnsupported)`; deletes with stub)
- `internal/semantic/extract/extract_nocgo.go` (10-line empty body + comment)
- `internal/semantic/extract/{golang,python,typescript}/provider_nocgo.go` (returns `nil` from `NewProvider`)
- `internal/semantic/extract/testutil/grammar_nocgo.go` (returns `t.Skip(...)` and `any` instead of typed registry)

---

### Pattern A3 — `serr.Unsupported` semantic-store stub (canonical Phase 57 template; SHAPE for the NEW `duckdb_winarm64.go`)

**Source: `internal/semantic/store/duckdb_nocgo.go` (52 lines, full file shown):**

```go
//go:build !cgo

package store

import (
    "context"
    "log/slog"

    serr "github.com/agenthands/helix/internal/errors"
    "github.com/agenthands/helix/internal/obs"
    "github.com/agenthands/helix/internal/semantic"
)

// Store under !cgo is a stub; the daemon refuses to start at step 6a before
// any instance is exercised at runtime (see internal/daemon/daemon.go and
// internal/repomap/extractor_nocgo.go for the parallel pattern from Phase
// 51.1).
type Store struct{}

// Open under !cgo always returns serr.ErrUnsupported (defense-in-depth —
// daemon already hard-fails at step 6a).
func Open(_ context.Context, _ semantic.Config, _ *slog.Logger, _ *obs.Metrics) (*Store, error) {
    return nil, serr.ErrUnsupported
}

// Close on the !cgo stub returns serr.ErrUnsupported.
func (*Store) Close() error { return serr.ErrUnsupported }

// Available on the !cgo stub returns false. Mirrors the
// repomap.TagExtractor / treesitter.Available pattern.
func (*Store) Available() bool { return false }

// QueryEffectiveFiles on the !cgo stub returns serr.ErrUnsupported.
func (*Store) QueryEffectiveFiles(_ context.Context, _, _ any) (any, error) {
    return nil, serr.ErrUnsupported
}

// QueryEffectiveSymbols on the !cgo stub returns serr.ErrUnsupported.
func (*Store) QueryEffectiveSymbols(_ context.Context, _ any) ([]any, error) {
    return nil, serr.ErrUnsupported
}

// QueryEffectiveReferences on the !cgo stub returns serr.ErrUnsupported.
func (*Store) QueryEffectiveReferences(_ context.Context, _ any) ([]any, error) {
    return nil, serr.ErrUnsupported
}

// QueryEffectiveEdges on the !cgo stub returns serr.ErrUnsupported.
func (*Store) QueryEffectiveEdges(_ context.Context, _ any) ([]any, error) {
    return nil, serr.ErrUnsupported
}
```

**Target shape: `internal/semantic/store/duckdb_winarm64.go` (Path-3, NEW, D-14):**

```go
//go:build windows && arm64

// Package store stub for windows/arm64. Upstream duckdb-go-bindings does not
// ship lib/windows-arm64 (verified 2026-05-04 against
// github.com/duckdb/duckdb-go-bindings); this file keeps the
// `internal/semantic/store` package buildable on that target so the
// 6-archive goreleaser matrix is preserved. Restoration tracked in
// .planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md
// (DEF-51-04 candidate, contingent on upstream lib/windows-arm64 ship).
//
// This stub is PLATFORM-conditional, NOT CGO-conditional — it does not
// reintroduce the //go:build cgo pattern that Phase 59.1 retires.
package store

import (
    "context"
    "log/slog"

    serr "github.com/agenthands/helix/internal/errors"
    "github.com/agenthands/helix/internal/obs"
    "github.com/agenthands/helix/internal/semantic"
)

// Store on windows/arm64 is a platform-tagged stub. semantic_index is
// unavailable on this target until duckdb-go-bindings ships
// lib/windows-arm64; the daemon should detect this via
// (*Store).Available() == false and downgrade gracefully (no hard refusal).
type Store struct{}

func Open(_ context.Context, _ semantic.Config, _ *slog.Logger, _ *obs.Metrics) (*Store, error) {
    return nil, serr.ErrUnsupported
}
func (*Store) Close() error                                                                  { return serr.ErrUnsupported }
func (*Store) Available() bool                                                               { return false }
func (*Store) QueryEffectiveFiles(_ context.Context, _, _ any) (any, error)                  { return nil, serr.ErrUnsupported }
func (*Store) QueryEffectiveSymbols(_ context.Context, _ any) ([]any, error)                 { return nil, serr.ErrUnsupported }
func (*Store) QueryEffectiveReferences(_ context.Context, _ any) ([]any, error)              { return nil, serr.ErrUnsupported }
func (*Store) QueryEffectiveEdges(_ context.Context, _ any) ([]any, error)                   { return nil, serr.ErrUnsupported }
```

**Subtleties / open questions for planner:**
- The flattened `internal/semantic/store/duckdb.go` must carry `//go:build !(windows && arm64)` as its FIRST line (no blank line before `package`). This is the **only** build tag surviving the phase.
- `Store` struct and method-set must be IDENTICAL between the two files (Go compiler enforces this only as long as both compile; mismatched method sets surface at the call-site under the wrong target). The `migrations.go` / `migrations_registry.go` flatten in this same package must NOT carry the `!(windows && arm64)` tag — they're pure Go, no cgo. Only `duckdb.go` (which holds the `_ "github.com/duckdb/duckdb-go/v2"` import per its line 21) needs the platform gate.
- The daemon's step-6b (post-deletion of step-6a) currently calls `semanticstore.Open`. On `windows && arm64`, `Open` will return `serr.ErrUnsupported` and the current step-6b code path returns an error → daemon refuses to start. **Action item for the planner:** decide whether step-6b should soft-degrade on `errors.Is(err, serr.ErrUnsupported)` (semantic_index = unavailable but daemon proceeds) or whether `cfg.SemanticIndex.Enabled` should be auto-flipped to false on windows/arm64. Recommend the former (cleaner; matches the existing fail-soft posture for Tier-2 quarantine in Phase 57). Same dispatch shape as `if errors.Is(err, serr.ErrUnsupported) { logger.Warn(...); semanticStore = nil } else { return nil, fmt.Errorf("opening semantic store: %w", err) }`.

---

### Pattern A4 — `.goreleaser.yaml` per-build env block flip

**Source-of-truth excerpt (`.goreleaser.yaml` lines 9-36, read 2026-05-04):**

```yaml
builds:
  - id: helix
    main: ./cmd/helix
    binary: helix
    env:
      - CGO_ENABLED=0
      - GOFLAGS=-mod=readonly
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
      # WR-07: -buildvcs=false strips runtime/debug VCS metadata
      # (vcs.revision/time/modified) that can drift between two snapshot
      # builds in the same CI run when the working tree gains untracked
      # files (e.g. dist-pass-1/) between Pass-1 and Pass-2. Identity is
      # already pinned via -X main.commit; VCS embedding is redundant.
      - -buildvcs=false
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.CommitDate}}
    mod_timestamp: '{{ .CommitTimestamp }}'
```

**Target shape (Wave 2, replaces lines 13-15 only — everything else is preserved verbatim):**

```yaml
    env:
      - CGO_ENABLED=1
      - GOFLAGS=-mod=readonly
      - >-
        {{- if eq .Os "linux" }}
          {{- if eq .Arch "amd64" }}CC=zig cc -target x86_64-linux-musl{{- end }}
          {{- if eq .Arch "arm64" }}CC=zig cc -target aarch64-linux-musl{{- end }}
        {{- else if eq .Os "windows" }}
          {{- if eq .Arch "amd64" }}CC=zig cc -target x86_64-windows-gnu{{- end }}
          {{- if eq .Arch "arm64" }}CC=zig cc -target aarch64-windows-gnu{{- end }}
        {{- else if eq .Os "darwin" }}
          {{- if eq .Arch "amd64" }}CC=zig cc -target x86_64-macos{{- end }}
          {{- if eq .Arch "arm64" }}CC=zig cc -target aarch64-macos{{- end }}
        {{- end }}
```

**Subtleties:**
- Source: verbatim adapted from `goreleaser/example-zig-cgo` master `.goreleaser.yaml` (RESEARCH.md §"Code Examples" line 351-368).
- Lines 16-22 (`goos`, `goarch`) are unchanged — both arms still target amd64+arm64 across {darwin,linux,windows}.
- Lines 23-36 (`flags`, `ldflags`, `mod_timestamp`) are unchanged — D-09 reproducibility flags are preserved.
- Lines 38-138 (archives, checksum, signs, changelog, release) are unchanged — Phase 58 cosign block intact.

---

### Pattern A5 — `release.yml` `Set up Zig` step insertion

**Source-of-truth excerpt (`.github/workflows/release.yml` lines 31-40, read 2026-05-04):**

```yaml
      - name: Set up Go
        uses: actions/setup-go@3041bf56c941b39c61721a86cd11f3bb1338122a  # v5.2.0
        with:
          go-version: '1.25.x'
          cache: true

      - name: Install cosign  # Phase 58 D-02: keyless signing via Fulcio + Rekor.
        uses: sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5  # v3.10.1
        with:
          cosign-release: 'v2.4.1'
```

**Target shape (Wave 3, NEW step inserted between line 35 and line 37):**

```yaml
      - name: Set up Go
        uses: actions/setup-go@3041bf56c941b39c61721a86cd11f3bb1338122a  # v5.2.0
        with:
          go-version: '1.25.x'
          cache: true

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29  # v2.2.1
        with:
          version: 0.14.1

      - name: Install cosign  # Phase 58 D-02: keyless signing via Fulcio + Rekor.
        uses: sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5  # v3.10.1
        with:
          cosign-release: 'v2.4.1'
```

**Subtleties:**
- SHA `d1434d08867e3ee9daa34448df10607b98908d29` for `mlugg/setup-zig@v2.2.1` was verified via `git ls-remote` on 2026-05-04 (RESEARCH.md §"Standard Stack" line 42).
- zig version `0.14.1` (released 2025-05-21) chosen explicitly over 0.15.x / 0.16.0 for reproducibility-gate stability (RESEARCH.md §"Alternatives Considered").
- The pin posture matches WR-04 / WR-07 used by `goreleaser-action@ec59f474... # v7.0.0` and `cosign-installer@7e8b541e... # v3.10.1`.
- Lines 42-138 of release.yml (the entire reproducibility gate Pass-1/Pass-2 + forensic dump + real release) are PRESERVED VERBATIM. WR-07 forensic dump is the triage tool if Pass-1/Pass-2 diverges under CGO=1 (Pitfall 1, D-10).

---

### Pattern A6 — Makefile zig-presence guard

**Source-of-truth excerpt (`Makefile` lines 56-59, read 2026-05-04):**

```makefile
release-snapshot: ## Run a local goreleaser dry-run; writes archives to dist/ (overwrites; gitignored)
	@command -v goreleaser >/dev/null 2>&1 || { \
	  echo "goreleaser not installed; see CONTRIBUTING.md (Releasing). brew install goreleaser"; exit 1; }
	goreleaser release --snapshot --clean --skip=sign
```

**Target shape (Wave 2, replaces lines 56-59):**

```makefile
release-snapshot: ## Run a local goreleaser dry-run; writes archives to dist/ (overwrites; gitignored)
	@command -v goreleaser >/dev/null 2>&1 || { \
	  echo "goreleaser not installed; see CONTRIBUTING.md (Releasing). brew install goreleaser"; exit 1; }
	@command -v zig >/dev/null 2>&1 || { \
	  echo "zig not installed (required for CGO=1 cross-compile); see CONTRIBUTING.md (Releasing). brew install zig (macOS) or apt install zig (Ubuntu 22.04+)"; exit 1; }
	goreleaser release --snapshot --clean --skip=sign
```

**Subtleties:**
- `make build` (Makefile lines 6-7: `$(GO) build -o $(BINARY) ./cmd/helix`) is UNCHANGED — local dev uses host CC, no zig dependency added there (D-03).
- `.PHONY` line 1 (`.PHONY: build clean proto test vet fmt docs clean-jdtls-cache bench-jdtls-warm bench bench-baseline release-snapshot update-trust-root`) gains `release-smoke` if the planner adds the NEW target per A6 below.

---

### Pattern A7 — NEW `make release-smoke` target (D-04 functional validation)

**Closest analog: `Makefile` lines 50-54 (`bench` / `bench-baseline` self-doc help-annotation style):**

```makefile
bench: ## Run the bench suite once and print results to stdout
	$(GO) test -short -bench=. -benchmem -count=10 -run=^$$ ./test/bench/...

bench-baseline: ## Capture a local baseline into test/bench/baselines/local.txt (gitignored, overwrites)
	$(GO) test -short -bench=. -benchmem -count=10 -run=^$$ ./test/bench/... | tee test/bench/baselines/local.txt
```

**Target shape (Wave 0 / Wave 5, NEW target appended after `release-snapshot`):**

```makefile
release-smoke: ## Smoke-test the linux-amd64 release archive: extract, run daemon, hit MCP tools/list (D-04)
	@test -f dist/helix_v*_linux_amd64.tar.gz || { \
	  echo "dist/helix_v*_linux_amd64.tar.gz not found; run \`make release-snapshot\` first"; exit 1; }
	@# Body: extract archive, boot daemon against testdata/fixtures/go/, hit tools/list,
	@# assert >= 41 tools and tree-sitter grammar count == 23. Planner finalizes the harness.
	@echo "release-smoke: TODO — planner picks fixture path + assertions per D-04"
	@false  # placeholder so the target fails-loud until wired
```

**Subtleties:**
- A6 in RESEARCH.md (Recommended): NEW target rather than fold-into `release-snapshot` because release-snapshot is a build dry-run while release-smoke is a runtime smoke. Planner can override.
- The exact harness mechanics (HTTP transport vs. stdio, log-grep vs. JSON parse) are RESEARCHER discretion per CONTEXT.md — pick the simplest form that asserts (a) daemon starts cleanly, (b) `tools/list` returns ≥ 41 entries, (c) the registry log line confirms 23 tree-sitter grammars registered.
- `.PHONY` declaration must include `release-smoke`.

---

### Pattern A8 — PROJECT.md Key Decisions row (D-13 — strikethrough + new row)

**Source-of-truth excerpt (`.planning/PROJECT.md` line 219, read 2026-05-04):**

```markdown
| Phase 51.1 emergent: CGO=0 build path via `//go:build cgo` stubs (v1.9) | DEF-51-01 blocker — goreleaser cross-compile fails when treesitter requires CGO | ✓ Good — daemon refuses CGO=0 with remediation; CGO=1 byte-identical |
```

**Target shape (Wave 4, line 219 modified + new row after line 226):**

```markdown
| Phase 51.1 emergent: CGO=0 build path via `//go:build cgo` stubs (v1.9) | DEF-51-01 blocker — goreleaser cross-compile fails when treesitter requires CGO | ~~✓ Good — daemon refuses CGO=0 with remediation; CGO=1 byte-identical~~ — superseded by Phase 59.1 (v1.10) |
| Phase 59.1: drop CGO=0 stubs, single-mode CGO=1 release (v1.10) | INT-BLOCKER-01 from v1.10-MILESTONE-AUDIT.md — stub apparatus added cost without value once goreleaser pinned CGO=0 | ✓ Good — 12 stubs deleted; CGO=1 release pipeline via zig cc; reproducibility-gate preserved |
```

**Subtleties:**
- The `~~strikethrough~~` markup applies to the Outcome cell content only; the Decision cell (left) and Rationale cell (middle) stay plain so the decision-history is searchable.
- D-12 reword: user-facing "single self-contained binary" framing is UNCHANGED in PROJECT.md Core Value (top of file) and CLAUDE.md Project section. Only the Tech Stack / Build framing flips ("build requires CGO=1; CI uses zig cc; runtime is statically linked single binary").
- No precedent for strikethrough in the table exists yet; this is the first such row. Future readers see the "we tried it then changed our minds" trail in one place per CONTEXT.md D-13.

---

### Pattern A9 — DEF-51-03 close + NEW DEF-51-04 candidate (deferred-items.md)

**Source-of-truth excerpt (`.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` line 127, read 2026-05-04):**

```markdown
**Status:** RESOLVED via Phase 51.1 (Path 2). See `.planning/phases/51.1-cgo-treesitter-gate-gate-internal-treesitter-behind-go-build/` for PLAN, RESEARCH, and SUMMARY. The `internal/treesitter` package is now gated behind `//go:build cgo`; the daemon refuses to start under `CGO_ENABLED=0` with a clear remediation message. CGO=0 archives build but are placeholders -- DEF-51-03 (re-enable CGO in goreleaser, Path 3) tracks the follow-up to make them functional.
```

**Target shape (Wave 4, new section appended for DEF-51-03 RESOLVED + a fresh DEF-51-04 entry for windows-arm64 restoration):**

```markdown
## DEF-51-03: Re-enable CGO in goreleaser (Path 3, follow-up to DEF-51-02)

**Status:** RESOLVED via Phase 59.1 (v1.10). See
`.planning/phases/59.1-drop-cgo-0-single-mode-cgo-1-build-release/` for
PLAN, RESEARCH, and SUMMARY. `.goreleaser.yaml` now sets
`CGO_ENABLED=1` with per-target `CC=zig cc -target ...` (templated env);
`release.yml` adds a pinned `mlugg/setup-zig@v2.2.1` step before
goreleaser; the Pass-1 ≡ Pass-2 reproducibility gate continues to hold
under CGO=1. All `_nocgo.go` stubs and `//go:build cgo` tags are
deleted; the binary cannot be built under `CGO_ENABLED=0`.

## DEF-51-04: windows-arm64 semantic_index unavailable until duckdb-go-bindings ships lib/windows-arm64

**Severity:** LOW — affects one of six release archives, only on the `semantic_index` feature path (other tools work).

**What:** `github.com/duckdb/duckdb-go-bindings/lib/` does not ship
`lib/windows-arm64` (verified 2026-05-04 against
github.com/duckdb/duckdb-go-bindings). Phase 59.1 ships a
platform-tagged stub `internal/semantic/store/duckdb_winarm64.go`
returning `serr.Unsupported` on that target only (D-14 Path 3). The
6-archive matrix is preserved; `semantic_index` is gracefully
unavailable on windows-arm64.

**Restoration trigger:** When upstream duckdb-go-bindings publishes
`lib/windows-arm64`, a follow-up phase deletes
`internal/semantic/store/duckdb_winarm64.go` and removes the
`//go:build !(windows && arm64)` constraint from `duckdb.go`.

**Status:** OPEN.
```

**Subtleties:**
- DEF-51-03 status changes OPEN → RESOLVED in the same edit; SUMMARY of Phase 59.1 contains the back-pointer.
- DEF-51-04 is NEW from D-14 lock — even though the stub is platform-conditional (not CGO-conditional), the deferred-item is honest about the regression risk.
- Path-3 (`_winarm64.go` platform-tag stub) is structurally distinct from `_nocgo.go` so it does not violate the phase's "no more `_nocgo.go` accumulation" invariant — but the user-visible behavior parallel (`Available()` returns false → `serr.ErrUnsupported`) is identical.

---

### Pattern A10 — CHANGELOG v1.10.x entry (NEW)

**Source-of-truth excerpt (`CHANGELOG.md` lines 5-19, read 2026-05-04):**

```markdown
## v1.9 — Polish & Infra (2026-04-30)

### Breaking Changes (v1.8 → v1.9)

This release renames the binary and reorganizes user-visible state. Existing users must take one manual step after upgrading. See `INSTALL.md` > **Upgrading** for the full upgrade workflow.

- **Binary renamed:** `serena` → `helix`. The archive shipped on GitHub Releases is now `helix_v1.9.0_<os>_<arch>.tar.gz` (was `serena_v*`). Scripts that invoke `serena` will fail with "command not found"; rename the symlink or update the script. There is no backwards-compat symlink (Phase 52 D-04).
```

**Target shape (Wave 4, NEW entry between line 4 and line 5):**

```markdown
## v1.10 — Release pipeline single-mode CGO=1 (TBD)

### Build pipeline

- **Single-mode CGO=1 release.** `.goreleaser.yaml` flips from `CGO_ENABLED=0` to `CGO_ENABLED=1` for all 6 build targets; per-target `CC=zig cc -target <triple>` is set via templated `env:` block. Phase 51.1 D-02 (daemon refuses CGO=0) is superseded — the binary cannot be built without CGO; `go build` fails at the import boundary instead of the daemon refusing at runtime.
- **`mlugg/setup-zig@v2.2.1` pinned by SHA in release.yml.** Mirrors WR-04 / WR-07 hardening posture used by `goreleaser-action` and `cosign-installer`. zig 0.14.1 is the pinned toolchain version.
- **All `_nocgo.go` stubs and `//go:build cgo` tags removed.** 12 stub files deleted, 24 production+test files lose their CGO build tag, `treesitter.Available` constant retired, daemon step-6a refusal block deleted.
- **Reproducibility gate preserved.** Pass-1 ≡ Pass-2 byte-identical archives gate the real release pass under CGO=1; the WR-07 forensic dump is the divergence-triage tool.

### Known Issues

- **windows-arm64 semantic_index unavailable.** `duckdb-go-bindings/lib/windows-arm64` does not exist upstream; the windows-arm64 archive ships with `internal/semantic/store` stubbed via a platform-tagged build constraint. Other tools (tree-sitter, RepoMap, fuzzy edit) work normally on windows-arm64. Tracked as DEF-51-04.

### Removed

- Phase 51.1's `_nocgo.go` stub apparatus (12 files, ~250 LOC). The CGO=0 build path is gone; future phases that touch `internal/semantic/extract` / `internal/semantic/store` / `internal/treesitter` no longer need to maintain CGO=0 stub mirrors.
```

**Subtleties:**
- Section order matches the v1.9 entry (Breaking Changes → feature sections → Removed → Known Issues).
- Phrasing matches CONTEXT.md D-11: "build pipeline: single-mode CGO=1, replaces Phase 51.1 D-02 stub policy".
- Date is TBD; the actual release-cut decision is deferred (Out of scope per CONTEXT.md `<deferred>`).

## Shared Patterns

### Shared 1 — `serr.Unsupported` defense-in-depth posture

**Source:** `internal/errors/kinds.go` (already exports `serr.ErrUnsupported`; unchanged by this phase).

**Apply to:** the new `duckdb_winarm64.go` stub. Method-set must mirror `duckdb_nocgo.go` exactly so the platform-tag fork compiles cleanly.

**Pattern:**
```go
return nil, serr.ErrUnsupported              // for constructors
return serr.ErrUnsupported                   // for void-result methods
return false                                 // for Available() / SupportsLanguage() type predicates
```

### Shared 2 — Pinned-by-SHA action invariant (WR-04 / WR-07)

**Source:** `.github/workflows/release.yml` lines 27, 32, 38, 43, 65, 133 — every third-party action pinned to a 40-char commit SHA with a trailing `# v<x.y.z>` comment.

**Apply to:** the new `Set up Zig` step in Wave 3.

**Pattern:**
```yaml
- name: Set up <Tool>
  uses: <org>/<action>@<40-char-sha>  # v<x.y.z>
  with:
    <key>: <value>
```

### Shared 3 — Tool-presence guard at the top of a Make recipe

**Source:** `Makefile:57-58` (`command -v goreleaser`).

**Apply to:** Wave 2 `release-snapshot` recipe (zig guard) and any future tool-dependent target.

**Pattern:**
```makefile
@command -v <tool> >/dev/null 2>&1 || { \
  echo "<tool> not installed; <hint>"; exit 1; }
```

### Shared 4 — Build-tag constraint as the FIRST line of a Go file

**Source:** every `_cgo.go` and `_nocgo.go` file in HEAD (line 1 is `//go:build <expr>`, followed by a blank line, followed by `package …`).

**Apply to:**
- `internal/semantic/store/duckdb.go` (post-flatten — gains `//go:build !(windows && arm64)`).
- `internal/semantic/store/duckdb_winarm64.go` (NEW — `//go:build windows && arm64`).

**Pattern:**
```go
//go:build <expr>

package <pkg>
```

**Anti-pattern (Pitfall 5):** never strip a compound build tag with regex; if the file has `//go:build cgo && integration`, hand-edit it to `//go:build integration`. Only `daemon_semantic_test.go` is affected.

### Shared 5 — Reproducibility-gate flag invariant (D-09)

**Source:** `.goreleaser.yaml` lines 23-36 — `flags: [-trimpath, -buildvcs=false]`, `ldflags: [-s -w, -X main.version=…, -X main.commit=…, -X main.date=…]`, `mod_timestamp: '{{ .CommitTimestamp }}'`.

**Apply to:** the Wave 2 `.goreleaser.yaml` edit MUST preserve all six lines verbatim. The CGO=1 + per-target CC change is additive only.

**Validation:** RESEARCH.md `validation` table — Pass-1 ≡ Pass-2 must hold on first CI trigger; no Pass-3-style softening (D-10).

## No Analog Found

| File | Role | Reason | Mitigation |
|---|---|---|---|
| `internal/semantic/store/duckdb_winarm64.go` (NEW) | platform-tagged stub | This is the first PLATFORM-conditional (not CGO-conditional) stub in the repo. The shape borrows from `duckdb_nocgo.go` (deleted in same wave); the build-tag predicate is novel | Use Pattern A3 verbatim, swap `!cgo` → `windows && arm64`, add a header comment explaining why the file exists and the DEF-51-04 restoration pointer |
| `make release-smoke` (NEW Make target) | runtime smoke harness | No prior runtime-validation target exists in the Makefile (`bench` runs `go test`; release-snapshot is a build dry-run) | Use Pattern A7 — borrow the `## help-annotation` style from `bench` / `bench-baseline`; planner finalizes harness mechanics |
| PROJECT.md row strikethrough | first ~~strikethrough~~ in the Key Decisions table | No precedent yet | Use Pattern A8 — apply `~~ ~~` only to the Outcome cell, append " — superseded by Phase 59.1 (v1.10)" tail outside the strikethrough |

## Metadata

**Analog search scope:**
- `internal/treesitter/`, `internal/repomap/`, `internal/kernel/edit/`, `internal/semantic/store/`, `internal/semantic/extract/{,golang,python,typescript,testutil}`, `internal/skill/repomap/`, `internal/daemon/`
- `.github/workflows/release.yml`, `.goreleaser.yaml`, `Makefile`
- `.planning/PROJECT.md`, `CHANGELOG.md`, `.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md`

**Files scanned:** 51 (12 stub deletes + 1 nocgo-test + 24 tag strips + 4 renames + 1 NEW stub + 9 doc/release/Makefile edits)

**Files read for excerpt extraction:** 14
- `internal/repomap/extractor_nocgo.go` (canonical Phase 51.1 stub — Pattern A2)
- `internal/repomap/elide_nocgo.go` (sibling shape variant)
- `internal/kernel/edit/treesitter_nocgo.go` (sibling shape variant)
- `internal/semantic/store/duckdb_nocgo.go` (canonical Phase 57 `serr.Unsupported` stub — Pattern A3)
- `internal/semantic/store/duckdb_nocgo_test.go` (proves the stub-test pairing for retire-with-stub)
- `internal/semantic/extract/extract_nocgo.go` (10-line empty-doc variant)
- `internal/semantic/extract/golang/provider_nocgo.go` (returns `nil` from constructor variant)
- `internal/semantic/extract/testutil/grammar_nocgo.go` (`t.Skip` variant)
- `internal/treesitter/registry_nocgo.go` + `available_nocgo.go` + `available_cgo.go` (three of the four `treesitter` package stubs)
- `internal/treesitter/sig_assert.go` (line 8-11 comment retire)
- `internal/treesitter/registry_cgo.go` (line 1 — flatten target)
- `internal/semantic/store/duckdb.go` line 1 (flatten target — gains `!(windows && arm64)`)
- `internal/semantic/extract/extract_cgo.go` (line 1 — flatten target)
- `internal/kernel/edit/treesitter.go` (line 1 — flatten target)
- `internal/repomap/elide.go` (line 1 — flatten target)
- `internal/daemon/daemon.go` (lines 220-244 — Pattern A1 deletion source)
- `.goreleaser.yaml` (full file — Pattern A4 source)
- `.github/workflows/release.yml` (full file — Pattern A5 + Shared 2 + Shared 5 source)
- `Makefile` (full file — Pattern A6 + A7 + Shared 3 source)
- `.planning/PROJECT.md` lines 180-227 (Pattern A8 source)
- `CHANGELOG.md` lines 1-55 (Pattern A10 source)
- `.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` line 127 (Pattern A9 source)

**Pattern extraction date:** 2026-05-04
