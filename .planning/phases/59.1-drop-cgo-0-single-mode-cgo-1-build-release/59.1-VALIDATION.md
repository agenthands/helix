---
phase: 59.1
slug: drop-cgo-0-single-mode-cgo-1-build-release
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-04
---

# Phase 59.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (existing) + goreleaser snapshot + bash assertions |
| **Config file** | none — uses repo-root `go.mod`, `.goreleaser.yaml`, `Makefile` |
| **Quick run command** | `go vet ./... && go test -short ./...` |
| **Full suite command** | `go test ./... && make release-snapshot` |
| **Estimated runtime** | ~120 seconds (quick), ~600 seconds (full incl. release-snapshot) |

---

## Sampling Rate

- **After every task commit:** Run `go vet ./... && go test -short ./...` for any task touching `internal/**/*.go`. Skip for pure-doc commits.
- **After every plan wave:** Run `go test ./...` (full suite, no `-short`).
- **Before `/gsd-verify-work`:** Full suite green AND `make release-snapshot` produces all expected archives AND `release.yml` Pass-1 ≡ Pass-2 reproducibility check passes in CI.
- **Max feedback latency:** 120 seconds for the per-task quick path.

---

## Per-Task Verification Map

> Filled by the planner. Every PLAN.md task gets a row keyed by its `{phase}-{plan}-{task}` ID.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 59.1-XX-XX | XX | N | INT-BLOCKER-01 / D-XX | — | (n/a — release plumbing) | (unit / build / smoke) | `(command)` | ✅ / ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Validation Architecture (from RESEARCH.md)

### Build validation
- [ ] `CGO_ENABLED=1 go build ./cmd/helix` succeeds locally on darwin-arm64.
- [ ] `make build` succeeds (host CC, no zig dependency).
- [ ] `make release-snapshot` produces 6 archives matching `name_template` shape: `helix_v{Version}_{Os}_{Arch}.tar.gz` (× 5) + `helix_v{Version}_Windows_{Arch}.zip` (× 1) — modulo Path-3 windows-arm64 stub semantics.
- [ ] Archive sizes / SHAs recorded as a one-time baseline in CI logs (no commit).

### Reproducibility validation (HARD GATE)
- [ ] `release.yml` Pass-1 ≡ Pass-2 byte-identical check (lines 41–119) is preserved verbatim — no edits to the gate logic, only insertion of the `setup-zig` step at the documented index.
- [ ] First CGO=1 CI run passes the gate. If it diverges, WR-07 forensic dump (per-file sha256, tar entry metadata, gzip header bytes, `go version -m`) is the diagnostic; remediation is added zig/`-fdebug-prefix-map` flags, NOT softening the gate.
- [ ] Phase merge is blocked until gate passes under CGO=1 (D-10).

### Runtime validation (linux-amd64 daemon smoke, D-04)
- [ ] `./helix daemon` starts from the linux-amd64 archive and answers an MCP `tools/list`.
- [ ] `get_repo_map` (or equivalent tool chosen by planner) returns tree-sitter-backed tags for a fixture under `testdata/fixtures/`.
- [ ] All 23 tree-sitter grammars register at startup (verified via startup logs or via tools count).
- [ ] darwin / windows archives validated structurally only: archive shape correct, binary not empty, `go version -m` buildinfo present.

### Stub-removal validation
- [ ] `find . -type f -name '*_nocgo.go' -not -path './legacy/*'` returns zero hits.
- [ ] `grep -rn '//go:build cgo' --include='*.go' .` returns zero hits across the entire tree (excluding `legacy/`).
- [ ] `grep -rn 'treesitter\.Available' --include='*.go' .` returns zero hits.
- [ ] The daemon-bootstrap step-6a refusal block is gone (no `if !treesitter.Available` in `internal/daemon/daemon.go`).

### Path-3 windows-arm64 stub validation (D-14)
- [ ] `internal/semantic/store/duckdb.go` carries `//go:build !(windows && arm64)`.
- [ ] `internal/semantic/store/duckdb_winarm64.go` exists with `//go:build windows && arm64`, returns `serr.Unsupported` from semantic-store entry points.
- [ ] `CGO_ENABLED=1 GOOS=windows GOARCH=arm64 go build ./cmd/helix` succeeds (verifies stub satisfies the type signatures and link succeeds).
- [ ] No file in the diff matches `*_nocgo.go` — Path-3 invariant is **platform**-conditional, not CGO-conditional.

### Doc-invariant validation
- [ ] `grep -E 'CGO=0|no CGO|CGO_ENABLED=0' CLAUDE.md README.md CONTRIBUTING.md` returns no current-tense claims (historical mentions in CHANGELOG / .planning/ are allowed).
- [ ] `CONTRIBUTING.md` Releasing section names zig as a prerequisite with an install hint.
- [ ] `Makefile` `release-snapshot` target has a zig-presence detection clause mirroring the goreleaser one.
- [ ] `.planning/PROJECT.md` Key Decisions table has a NEW row referencing Phase 59.1 AND the existing 51.1 row carries a `~~strikethrough~~` of the "preserved" verb plus "— superseded by Phase 59.1 (v1.10)" tail.
- [ ] `CHANGELOG.md` has a v1.10.x entry framed as "build pipeline: single-mode CGO=1, replaces Phase 51.1 D-02 stub policy".
- [ ] `.planning/milestones/v1.9-phases/51-packaging-goreleaser/deferred-items.md` has DEF-51-03 closure entry pointing at 59.1 PLAN/SUMMARY AND a NEW deferred-item for windows-arm64 duckdb-go-bindings restoration.
- [ ] `.planning/milestones/v1.10-ROADMAP.md` lists Phase 59.1 (currently only the root ROADMAP.md does).

### Regression validation
- [ ] `go vet ./...` clean.
- [ ] `go test ./...` clean (no test failures, no skipped-by-default tests that would have been gated by the deleted `//go:build cgo` tag and now run by default).
- [ ] `cmd/vet-noduckdb` analyzer passes (the package-boundary invariant is unchanged).
- [ ] Existing `make release-snapshot` exit code ≡ 0 locally.
- [ ] `make build` (host CC) still works — no new mandatory zig dependency for dev workflows.
- [ ] No new `_nocgo.go` files anywhere in the diff.

---

## Wave 0 Requirements

- [ ] **Probe darwin zig cc.** Cheapest way to de-risk Wave 2: `CC="zig cc -target aarch64-macos" CGO_ENABLED=1 GOOS=darwin GOARCH=arm64 go build ./cmd/helix` on a linux runner; binary should produce a Mach-O ARM64 executable. Recommend running this BEFORE the goreleaser `.yaml` flip lands so we know the toolchain reaches darwin without an Apple SDK.
- [ ] **`make release-smoke` target authored** (NEW make target proposed by RESEARCH.md): wraps the linux-amd64 daemon-smoke per D-04 so it can run identically locally and in CI.
- [ ] **D-14 windows-arm64 path locked** (already done — captured in RESEARCH.md `## Post-Research Decision Lock` and this file's Path-3 validation section).

*If none of the above probes are run:* Wave 2 (`.goreleaser.yaml` flip) becomes the first place we discover whether zig cc reaches darwin. That's the more expensive failure mode.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real cosign signing on the new CGO=1 archives works end-to-end (Phase 58 REL-01 invariant) | INT-BLOCKER-01 (transitive) | Cosign keyless flow runs only in real-CI on tag push, not in `release-snapshot` | After phase merge, on the next `vX.Y.Z` tag push, verify each `*.sigstore.json` bundle verifies via `cosign verify-blob`. |
| windows-arm64 stub UX (running `helix` on win-arm64 produces a clear "semantic_index unavailable on this target" error, not a crash) | D-14 | Windows-arm64 hardware not in CI matrix | Run the win-arm64 archive on a real Windows-on-ARM device or VM; confirm `helix daemon` starts AND any semantic-store-touching tool returns `serr.Unsupported` with a readable message. Tracked as a post-merge follow-up. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (darwin zig cc probe, `make release-smoke` target)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s (quick path)
- [ ] `nyquist_compliant: true` set in frontmatter
- [ ] First CI Pass-1 ≡ Pass-2 reproducibility run passes under CGO=1 (HARD GATE)

**Approval:** pending
