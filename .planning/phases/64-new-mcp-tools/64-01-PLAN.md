---
phase: 64-new-mcp-tools
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - bench/semantic_bench_test.go
  - bench/fixtures/synthetic_50k_go/README.md
  - bench/fixtures/synthetic_50k_go/gen.go
autonomous: true
requirements:
  - TOOL-04
must_haves:
  truths:
    - "A 50k-symbol synthetic Go fixture exists and is generatable from a fixed seed."
    - "A binary-size measurement shows go binary growth from adding bleve v2.4.x."
    - "An indexing-throughput measurement shows bleve vs DuckDB FTS5 on the 50k fixture."
    - "A clear PASS/FAIL verdict is committed in the bench report (size <50MB AND indexing not >5x slower than DuckDB FTS5 → PASS bleve; otherwise FAIL → planner reroutes)."
  artifacts:
    - path: bench/fixtures/synthetic_50k_go/gen.go
      provides: "Deterministic 50k-symbol Go fixture generator (fixed seed=42)."
    - path: bench/semantic_bench_test.go
      provides: "Benchmarks: BenchmarkBleveBinarySize, BenchmarkBleveIndexThroughput, BenchmarkDuckDBFTSIndexThroughput."
    - path: bench/semantic_bench_REPORT.md
      provides: "Verdict (PASS bleve / FAIL → fallback DuckDB FTS5) with measurements."
  key_links:
    - from: bench/semantic_bench_test.go
      to: bench/fixtures/synthetic_50k_go/gen.go
      via: "go test reads/regenerates fixture"
      pattern: "synthetic_50k_go"
---

<objective>
[BLOCKING WAVE-0 GATE per CONTEXT.md D-08] Empirically verify the bleve v2 dependency meets the size + throughput thresholds before any Wave 1 plan touches bleve. Generate a 50k-symbol synthetic Go fixture (re-usable by Phase 67), measure binary growth, measure indexing throughput vs DuckDB FTS5 baseline, and commit a written verdict.

Purpose: CONTEXT.md D-08 explicitly gates Phase 64's bleve commit on a researcher-driven measurement. RESEARCH.md §"bleve verdict" leaves throughput + binary growth UNMEASURED. If bleve fails the gate, the planner reroutes Wave 1 plans to DuckDB FTS5 inside *Store. This MUST run before P03/P06/P07 begin.

Output: The fixture generator + bench file + REPORT.md verdict. Wave 1 plans that import bleve (P07) read this REPORT to confirm PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-new-mcp-tools/64-CONTEXT.md
@.planning/phases/64-new-mcp-tools/64-RESEARCH.md
@.planning/phases/64-new-mcp-tools/64-PATTERNS.md

<thresholds>
PASS criteria (both must hold):
1. Binary size growth from `go build ./cmd/helix` before vs after adding `github.com/blevesearch/bleve/v2 v2.4.x` is **less than 50 MiB**.
2. Indexing 50k symbols into a fresh bleve scorch index completes in **no more than 5x** the time it takes to index the same 50k symbols into a DuckDB FTS5 virtual table.

FAIL: either threshold busted → write REPORT.md verdict "FAIL — fallback to DuckDB FTS5"; subsequent Wave 1 plans MUST switch to DuckDB FTS5 routing inside *Store.
</thresholds>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 50k-symbol synthetic Go fixture generator</name>
  <files>bench/fixtures/synthetic_50k_go/gen.go, bench/fixtures/synthetic_50k_go/README.md</files>
  <read_first>
    - bench/ (list to confirm directory layout; create bench/fixtures/synthetic_50k_go/ if missing)
    - internal/repomap/cache.go (for symbol-row shape pattern reference)
  </read_first>
  <action>
    Create `bench/fixtures/synthetic_50k_go/gen.go` (`//go:build ignore` build tag so it doesn't ship with the binary; invoke via `go run gen.go`). Generator signature:
    ```go
    package main
    func main() {
      seed := int64(42)
      r := rand.New(rand.NewSource(seed))
      // Write 500 .go files into ./out/, each with ~100 exported funcs/types.
      // Each function gets a 3-line godoc comment with 2-3 random English words drawn from a fixed wordlist of 200 terms.
      // Names: camelCase 2-3 word combos drawn from same wordlist.
      // File paths: out/pkg{0..49}/file{0..9}.go.
      // Total target: 50_000 declarations across 500 files.
    }
    ```
    Persist a fixed wordlist (200 lowercase English nouns/verbs) inline in gen.go so output is byte-deterministic across machines. README.md documents: invocation `go run gen.go`, fixture-size 50_000 symbols, deterministic seed, intended consumers (P64-01 bench, future Phase 67 eval).
  </action>
  <verify>
    <automated>cd bench/fixtures/synthetic_50k_go && go run gen.go && find ./out -name '*.go' | wc -l | tr -d ' ' | grep -q '^500$' && grep -rE '^func ' ./out/pkg0/file0.go | wc -l | tr -d ' ' | grep -qE '^[0-9]+$'</automated>
  </verify>
  <acceptance_criteria>
    - File `bench/fixtures/synthetic_50k_go/gen.go` exists.
    - Running `go run bench/fixtures/synthetic_50k_go/gen.go` produces exactly 500 .go files under `bench/fixtures/synthetic_50k_go/out/`.
    - Across all 500 files, total declarations match the formula: `grep -rE '^func ' bench/fixtures/synthetic_50k_go/out | wc -l` returns >= 25000 (allow type/var declarations to take the rest up to 50000).
    - `bench/fixtures/synthetic_50k_go/README.md` exists and contains the strings "seed", "50000", "deterministic".
    - Re-running `go run gen.go` produces byte-identical output (run twice, sha256 matches).
  </acceptance_criteria>
  <done>50k-symbol Go fixture generator emits deterministic output; README pins the contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add bleve v2, write bench (size + throughput), commit verdict REPORT.md</name>
  <files>bench/semantic_bench_test.go, bench/semantic_bench_REPORT.md, go.mod, go.sum</files>
  <read_first>
    - go.mod (current pins; bleve must be added)
    - bench/fixtures/synthetic_50k_go/gen.go (fixture entrypoint)
    - .planning/phases/64-new-mcp-tools/64-RESEARCH.md (lines 60-75, "bleve verdict" gate criteria)
  </read_first>
  <action>
    1. Run `go list -m -versions github.com/blevesearch/bleve/v2 | tail -5` to capture latest. Pin to the latest v2.4.x: `go get github.com/blevesearch/bleve/v2@v2.4.x`. Commit go.mod / go.sum.

    2. Measure binary size:
       - Run `go build -o /tmp/helix-pre ./cmd/helix` BEFORE the bleve import lands somewhere (use a probe file `bench/_blevprobe/main.go` with `import _ "github.com/blevesearch/bleve/v2"` and a stub `package blevprobe` consumer; build a separate target to isolate the size delta).
       - Concretely: write `bench/_blevprobe/probe.go` containing `//go:build blevprobe` `package main; import _ "github.com/blevesearch/bleve/v2"; func main(){}`. `go build -tags=''      -o /tmp/helix-without-bleve ./cmd/helix` and `go build -tags='blevprobe' -o /tmp/helix-with-bleve ./bench/_blevprobe`. Subtract sizes via `stat -f%z` on darwin / `stat -c%s` on linux.
       - Record `delta_mib = (with - without) / 1048576`.

    3. Implement two benchmarks in `bench/semantic_bench_test.go`:
       - `BenchmarkBleveIndexThroughput_50k(b *testing.B)` — opens a fresh `bleve.New(tempDir, mapping)` with scorch backend, batches 50k SymbolDoc{ID, Name, Path, Doc, CommentWindow} from the fixture using `bleve.NewBatch()` in groups of 1000, calls `idx.Batch(batch)`. Reports `b.SetBytes(int64(50000))`.
       - `BenchmarkDuckDBFTSIndexThroughput_50k(b *testing.B)` — opens a temp DuckDB DB via `internal/semantic/store.Open(...)` (use existing public Open helper or fall back to `database/sql.Open("duckdb", ...)` if Open isn't exported), creates an FTS table, inserts the same 50k rows in a single tx. Reports `b.SetBytes(int64(50000))`.

    4. Run `go test -run=^$ -bench='Benchmark.*Index' -benchtime=1x -timeout=10m ./bench/...` and capture ns/op.

    5. Write `bench/semantic_bench_REPORT.md` with the EXACT format below:
       ```
       # Phase 64 — Bleve Gate Report (D-08)

       Date: <YYYY-MM-DD>
       Bleve version: v2.4.x
       Host: <darwin/linux> arm64/amd64

       ## Binary size

       | Build | Bytes | MiB |
       |-------|------:|----:|
       | without bleve | <bytes> | <mib> |
       | with bleve    | <bytes> | <mib> |
       | **delta**     |         | **<delta_mib>** |

       Threshold: < 50 MiB. Result: PASS / FAIL.

       ## Indexing throughput (50k symbols)

       | Backend | ns/op | Time (s) | Symbols/s |
       |---------|------:|---------:|----------:|
       | bleve scorch | <ns> | <s> | <sps> |
       | duckdb fts5  | <ns> | <s> | <sps> |
       | **ratio**    |      |     | **<ratio>** |

       Threshold: bleve ≤ 5x slower than DuckDB FTS5. Result: PASS / FAIL.

       ## Verdict

       **PASS — proceed with bleve** | **FAIL — fallback to DuckDB FTS5**

       Subsequent Wave 1 plans (P64-07 retrieval engine) consume this verdict.
       ```
       Fill in measured numbers; replace "PASS / FAIL" with the actual verdict per the thresholds above.
  </action>
  <verify>
    <automated>test -f bench/semantic_bench_REPORT.md && grep -E '^(\*\*PASS|\*\*FAIL)' bench/semantic_bench_REPORT.md && go vet ./bench/... && go test -run=^$ -bench='BenchmarkBleveIndexThroughput_50k|BenchmarkDuckDBFTSIndexThroughput_50k' -benchtime=1x -timeout=10m ./bench/... | grep -E 'ok|PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` contains `github.com/blevesearch/bleve/v2 v2.4` (some patch).
    - `bench/semantic_bench_test.go` contains both `BenchmarkBleveIndexThroughput_50k` and `BenchmarkDuckDBFTSIndexThroughput_50k`.
    - `bench/semantic_bench_REPORT.md` exists with all four sections (Binary size, Indexing throughput, Verdict, plus header).
    - REPORT body contains either "**PASS — proceed with bleve**" OR "**FAIL — fallback to DuckDB FTS5**" verbatim.
    - `go vet ./bench/...` exits 0.
    - `go test ./bench/... -count=1 -run=TestNothing -timeout=30s` exits 0 (no test failures from package compile).
  </acceptance_criteria>
  <done>Bleve gate decision committed; downstream Wave-1 plans either proceed with bleve (PASS) or trigger orchestrator-side replan to DuckDB FTS5 (FAIL).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| go.mod ↔ supply chain | Pulling new dependency `github.com/blevesearch/bleve/v2` and its transitive graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01-01 | Tampering | bleve dependency supply chain | mitigate | `go.sum` checksum-pinned via `go get`; bleve is Apache-2.0 [VERIFIED in RESEARCH.md]. CI runs `go mod verify`. |
| T-64-01-02 | DoS | Bench fixture generator unbounded run | accept | Fixed seed + 500-file cap; build-tag `//go:build ignore` keeps gen.go out of release binary. |
| T-64-01-03 | Information Disclosure | REPORT.md leaking host info | accept | Only OS/arch surfaced; no secrets. |
</threat_model>

<verification>
- `go vet ./bench/...` passes
- `gofmt -l bench/` returns empty
- `go.mod` and `go.sum` updated atomically
- REPORT.md verdict line matches one of two exact strings
</verification>

<success_criteria>
1. 50k-symbol fixture generator deterministic with seed=42.
2. Bench file measures both bleve and DuckDB FTS5 indexing throughput.
3. REPORT.md contains a binding PASS/FAIL verdict the orchestrator reads at Wave 1 entry.
4. If FAIL, orchestrator reroutes — out of this plan's scope; orchestrator triggers `/gsd-plan-phase --revision`.
</success_criteria>

<output>
Create `.planning/phases/64-new-mcp-tools/64-01-SUMMARY.md` after completion. Include the verdict + numbers.
</output>
