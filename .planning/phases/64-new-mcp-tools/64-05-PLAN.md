---
phase: 64-new-mcp-tools
plan: 05
type: tdd
wave: 2
depends_on: ["64-03-PLAN", "64-04-PLAN"]
files_modified:
  - internal/skill/semantic/tools_refresh.go
  - internal/skill/semantic/tools_refresh_test.go
  - internal/skill/semantic/skill.go
autonomous: true
requirements:
  - TOOL-02
  - TOOL-05
must_haves:
  truths:
    - "refresh_semantic_graph drains live changes via live.Service.OnWorkspaceChanged."
    - "refresh NEVER calls BeginSnapshot/CommitSnapshot/AbortSnapshot or compactor.OnFlush (D-09 / D-13). Tests assert via a recorder-style mock CompactorAccessor (declared in 64-03's accessors.go) and a recorder StoreAccessor."
    - "paths filter is strict subset (D-11): only requested paths processed; remainder remains queued."
    - "wait_for_lsp:true blocks up to max_wait_ms via LaneQueue.DepthAll polling; returns pending_lsp:true on timeout (D-12)."
    - "Mode-tier check is read+ (every session passes); path-traversal validation rejects .. and out-of-root paths."
    - "freshness reflects what completed: fresh on full LSP drain; structurally_fresh_semantically_pending on LSP timeout."
    - "tools_refresh.go is a NEW file; 64-05 does not modify accessors.go (CompactorAccessor was hoisted into 64-03 — closes checker B3)."
  artifacts:
    - path: internal/skill/semantic/tools_refresh.go
      provides: "registerRefreshSemanticGraph + RefreshSemanticGraphArgs typed-args + handler body. Owns const refreshHelp."
      contains: "registerRefreshSemanticGraph"
  key_links:
    - from: internal/skill/semantic/tools_refresh.go
      to: internal/semantic/live/service/service.go
      via: "live.OnWorkspaceChanged drives the drain (consumed via LiveAccessor declared in 64-03)"
      pattern: "OnWorkspaceChanged"
    - from: internal/skill/semantic/tools_refresh.go
      to: internal/semantic/lspenrich/queue.go
      via: "LaneQueue.DepthAll polling for wait_for_lsp (consumed via QueueAccessor declared in 64-03)"
      pattern: "DepthAll"
---

<objective>
Implement refresh_semantic_graph (TOOL-02): typed-args registration, mode-tier check (read+), path validation, drain via live.Service, wait_for_lsp polling, RefreshResult envelope. **Hard invariant: must NOT call any snapshot-write or compactor API** — D-09 and D-13.

Purpose: refresh is the read+ tier counterpart to index. Drains pending live changes into the overlay; never produces a new committed snapshot. The boundary discipline (D-13) is the central correctness invariant.

**File ownership (closes B1+B3+B4):** This plan ADDS `tools_refresh.go` (new file) and DELETES the `refreshHelp` line from `skill.go`'s help-text stub block. It does NOT edit `accessors.go` — `CompactorAccessor` was hoisted into 64-03 so the interface surface is final at end-of-W0. This plan only CONSUMES `CompactorAccessor` via the recorder-style mock in tests.

Output: tools_refresh.go (new) + comprehensive tests (tools_refresh_test.go, new) including the snapshot-invariant test (using mock StoreAccessor + mock CompactorAccessor as recorders) and the strict-subset-paths test. Plus single-line stub deletion in skill.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/64-new-mcp-tools/64-CONTEXT.md
@.planning/phases/64-new-mcp-tools/64-PATTERNS.md
@.planning/phases/64-new-mcp-tools/64-RESEARCH.md

<interfaces>
From envelope.go (P64-03):
  type RefreshResult struct {
    CommonEnvelope            // Freshness, GraphVersion, OverlayActive
    FilesUpdated    int
    PendingLSP      bool
    PendingLSPFiles int
  }

From accessors.go (P64-03 — FINAL; this plan only consumes):
  type LiveAccessor interface {
    OnWorkspaceChanged(ws workspace.WorkspaceKey, paths []string) error
    LastFlushAt(ws workspace.WorkspaceKey) int64
  }
  type QueueAccessor interface {
    DepthAll(ws workspace.WorkspaceKey) int
    LastEnqueueAt(ws workspace.WorkspaceKey) int64
  }
  type StoreAccessor interface {
    CurrentGraphVersion(ctx, repoID) (uint64, error)
    OverlayHasPendingRows(repoID) bool
    LatestCommittedSnapshot(ctx, repoID) (uint64, error)
    QueryEffectiveAdjacency(ctx, repoID, projection) (...)
  }
  type CompactorAccessor interface {                         // Hoisted into 64-03 (closes checker B3).
    OnFlush(ws workspace.WorkspaceKey) error
  }

From handler_helpers.go (P64-04 — shared, this plan consumes read-only):
  errorResult, jsonResult, validatePaths

From skill.go (P64-03 — this plan only DELETES one stub line):
  s.sessionSnapshot(ctx), s.workspaceKey(ctx), s.live, s.queue, s.store, s.compactor

From internal/semantic/live/service/service.go: OnWorkspaceChanged(ws, paths) at ~line 184; LastFlushAt(ws) at ~line 79.
From internal/semantic/lspenrich/queue.go: LaneQueue.DepthAll() at ~line 87; LastEnqueueAt at ~line 71.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Failing tests for refresh handler (snapshot invariant + paths-strict-subset + wait_for_lsp)</name>
  <files>internal/skill/semantic/tools_refresh_test.go</files>
  <read_first>
    - internal/skill/semantic/skill.go (current SemanticSkill shape from 64-03 + tools_index.go)
    - internal/skill/semantic/envelope.go (RefreshResult)
    - internal/skill/semantic/accessors.go (LiveAccessor, QueueAccessor, StoreAccessor, CompactorAccessor — ALL declared in 64-03)
    - internal/skill/semantic/tools_index_test.go (mock pattern from P64-04)
    - .planning/phases/64-new-mcp-tools/64-PATTERNS.md (lines 177-206 — tools_refresh.go pattern excerpt)
  </read_first>
  <behavior>
    - TestRefreshHandler_HappyPath_DrainsLive: mock live.OnWorkspaceChanged returns nil (3 files updated); store.CurrentGraphVersion returns (42, nil); queue.DepthAll returns 0 after drain. Returns RefreshResult{FilesUpdated:3, GraphVersion:42, PendingLSP:false, Freshness: fresh}.
    - TestRefreshHandler_NoSnapshotCommit_Invariant: use a recorder-style mock StoreAccessor that fails the test on any call to BeginSnapshot/CommitSnapshot/AbortSnapshot/WriteSnapshotFacts. Drive the handler through the happy path; assert the recorder counted ZERO forbidden calls. (Note: StoreAccessor interface exposes only the read surface from 64-03, but the recorder type wraps an extended fake that ALSO has the forbidden methods purely so we can assert non-call. The recorder fake type lives in tools_refresh_test.go, package-local; it is not part of the production accessor.)
    - TestRefreshHandler_NoCompactorCall_Invariant: recorder-style mock CompactorAccessor (interface declared in 64-03's accessors.go). The mock's OnFlush method calls t.Fatal if invoked. Drive the handler; assert no call.
    - TestRefreshHandler_PathsStrictSubset: args.Paths=["a.go"]. Mock live.OnWorkspaceChanged: assert it received ONLY ["a.go"]. Mock should be invoked exactly once with that subset.
    - TestRefreshHandler_WaitForLSP_TimeoutReportsPending: queue.DepthAll returns 5 (constant — never drains). args.WaitForLSP=true, MaxWaitMs=200. Handler returns RefreshResult{PendingLSP:true, PendingLSPFiles:5, Freshness: structurally_fresh_semantically_pending}. Test asserts the call took 200ms ± 100ms.
    - TestRefreshHandler_WaitForLSP_DrainsBeforeTimeout: queue.DepthAll returns 3 then drops to 0 after 50ms. args.WaitForLSP=true, MaxWaitMs=2000. Returns PendingLSP:false, Freshness: fresh.
    - TestRefreshHandler_PathTraversalRejected: args.Paths=["../../etc"]; rejected with InvalidArgs.
    - TestRefreshHandler_ModeReadAccepted: session.Mode="read"; handler proceeds (read+ tier).
  </behavior>
  <action>
    Create internal/skill/semantic/tools_refresh_test.go with all 8 tests. Define test-local recorder fakes:
      - `recorderStoreAccessor` implements StoreAccessor PLUS extends with `BeginSnapshot/CommitSnapshot/AbortSnapshot/WriteSnapshotFacts` methods (all calling t.Fatal on invocation). Note: the production handler can only call methods on StoreAccessor — these extended methods serve as canaries, asserting via the type assertion `if _, ok := s.store.(*recorderStoreAccessor); ok { ... }` that the production handler never even reach-ins through type-asserts.
      - `recorderCompactorAccessor` implements CompactorAccessor; OnFlush calls t.Fatal.

    Compile gate: write a stub tools_refresh.go that exports registerRefreshSemanticGraph and the handler with `panic("not implemented")` so tests compile.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/... && ! go test ./internal/skill/semantic/ -run TestRefreshHandler_ -count=1 -timeout=30s</automated>
  </verify>
  <acceptance_criteria>
    - tools_refresh_test.go contains all 8 named tests.
    - go test ./internal/skill/semantic/ -run TestRefreshHandler_ -count=1 exits NON-ZERO (RED).
    - go vet ./internal/skill/semantic/... exits 0.
    - The "no snapshot commit" test references a recorder that fails on BeginSnapshot/CommitSnapshot/AbortSnapshot/WriteSnapshotFacts — verify by `grep -c "BeginSnapshot\|CommitSnapshot" internal/skill/semantic/tools_refresh_test.go` is at least 4.
    - The "no compactor call" test references CompactorAccessor (interface from 64-03) — verify by `grep -c "CompactorAccessor" internal/skill/semantic/tools_refresh_test.go` is at least 1.
    - tools_refresh_test.go does NOT add CompactorAccessor declaration itself — that interface is sourced from accessors.go.
  </acceptance_criteria>
  <done>RED gate met. Recorder-style fakes for D-09/D-13 invariants in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement tools_refresh.go (handler body) + delete refreshHelp stub from skill.go</name>
  <files>internal/skill/semantic/tools_refresh.go, internal/skill/semantic/skill.go</files>
  <read_first>
    - internal/skill/semantic/tools_refresh_test.go (RED tests drive impl)
    - internal/skill/semantic/tools_index.go (typed-args registration analog from P64-04)
    - internal/skill/semantic/handler_helpers.go (errorResult, jsonResult, validatePaths from P64-04)
    - internal/semantic/live/service/service.go (lines 79, 184 — OnWorkspaceChanged signature)
    - internal/semantic/lspenrich/queue.go (lines 71-92 — DepthAll signature)
    - .planning/phases/64-new-mcp-tools/64-PATTERNS.md (lines 177-206)
  </read_first>
  <action>
    1) Create internal/skill/semantic/tools_refresh.go (NEW file):
       - Top of file comment: "INVARIANT (D-09 / D-13): refresh_semantic_graph MUST NOT call BeginSnapshot, CommitSnapshot, AbortSnapshot, WriteSnapshotFacts, or CompactorAccessor.OnFlush. The grep gate in CI enforces this; the recorder mocks in tools_refresh_test.go enforce it under unit test."
       - Declare `const refreshHelp = "..."` at package scope (verbose multi-line: parameter docs for paths/wait_for_lsp/max_wait_ms + return shape + D-11 strict-subset semantics warning).
       - RefreshSemanticGraphArgs typed struct:
           Paths      []string `json:"paths,omitempty"           jsonschema:"strict-subset path filter"`
           WaitForLSP bool     `json:"wait_for_lsp,omitempty"`
           MaxWaitMs  int      `json:"max_wait_ms,omitempty"     jsonschema:"default 3000"`
       - registerRefreshSemanticGraph(server, s, tracer): mirror tools_index.go shape; calls mcpsdk.AddTool with Name="refresh_semantic_graph", then server.Registry().Register(&mcp.ToolDef{...HelpText: refreshHelp}).
       - Handler body:
           1. snap := s.sessionSnapshot(ctx); if err := checkMode(snap, modeTierRead); err != nil { return errorResult(err.Error()), nil, nil } (every session passes)
           2. ws := s.workspaceKey(ctx); if err := validatePaths(args.Paths, ws.RepoRoot()); err != nil { return errorResult(err.Error()), nil, nil }
           3. Default MaxWaitMs to 3000 when <= 0.
           4. preDepth := s.queue.DepthAll(ws)
           5. if err := s.live.OnWorkspaceChanged(ws, args.Paths); err != nil { return errorResult(err.Error()), nil, nil }
           6. gv, _ := s.store.CurrentGraphVersion(ctx, ws.RepoID())
           7. filesUpdated := len(args.Paths). If args.Paths is empty, the live drain processes whatever the coalescer flushes — fall back to a reported count from live.OnWorkspaceChanged if the API returns one; otherwise leave as 0 and document in helpText. (RESEARCH says count is "files updated" — confirm shape via service.go signature; if OnWorkspaceChanged returns only an error, fall back to len(args.Paths) when paths supplied, else 0.)
           8. pendingLSP := false; pendingLSPFiles := s.queue.DepthAll(ws)
              if args.WaitForLSP {
                deadline := time.Now().Add(time.Duration(args.MaxWaitMs) * time.Millisecond)
                ticker := time.NewTicker(50 * time.Millisecond); defer ticker.Stop()
                for time.Now().Before(deadline) {
                  if s.queue.DepthAll(ws) == 0 { break }
                  select { case <-ticker.C: case <-ctx.Done(): }
                }
                pendingLSPFiles = s.queue.DepthAll(ws)
                pendingLSP = pendingLSPFiles > 0
              }
           9. freshness := FreshnessFresh
              overlayActive := s.store.OverlayHasPendingRows(ws.RepoID())
              if pendingLSP || overlayActive {
                freshness = FreshnessStructurallyFreshSemanticallyPending
              }
              return jsonResult(RefreshResult{
                CommonEnvelope:  CommonEnvelope{Freshness: freshness, GraphVersion: gv, OverlayActive: overlayActive},
                FilesUpdated:    filesUpdated,
                PendingLSP:      pendingLSP,
                PendingLSPFiles: pendingLSPFiles,
              }), nil, nil

       Hard invariant (compile-time + grep-time): tools_refresh.go MUST NOT contain the strings BeginSnapshot, CommitSnapshot, AbortSnapshot, WriteSnapshotFacts, OnFlush.

    2) Update internal/skill/semantic/skill.go: SINGLE-LINE DELETE of `refreshHelp = "refresh_semantic_graph: stub help (replaced by P64-05)"` from the stub-var block. The `Tools()` reference to `refreshHelp` now resolves to the const declared in tools_refresh.go.

       This is the ONLY edit to skill.go in this plan.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/... && go test ./internal/skill/semantic/ -run TestRefreshHandler_ -count=1 -timeout=30s && ! grep -E '(BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshotFacts|OnFlush)' internal/skill/semantic/tools_refresh.go && ! grep -q 'refreshHelp = "refresh_semantic_graph: stub help' internal/skill/semantic/skill.go && grep -q 'const refreshHelp' internal/skill/semantic/tools_refresh.go && gofmt -l internal/skill/semantic/tools_refresh.go internal/skill/semantic/skill.go | tr -d ' \n' | grep -q '^$'</automated>
  </verify>
  <acceptance_criteria>
    - internal/skill/semantic/tools_refresh.go contains "func registerRefreshSemanticGraph(" (substring match).
    - File contains "type RefreshSemanticGraphArgs struct" (substring match).
    - File contains "checkMode(" (substring match).
    - File declares `const refreshHelp = ` (substring match).
    - grep -E '(BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshotFacts|OnFlush)' internal/skill/semantic/tools_refresh.go returns NO matches (D-09 / D-13 invariant).
    - go test ./internal/skill/semantic/ -run TestRefreshHandler_ -count=1 exits 0 (all 8 tests pass).
    - go vet ./internal/skill/semantic/... exits 0.
    - skill.go does NOT contain `refreshHelp = "refresh_semantic_graph: stub help`.
    - skill.go is unchanged except the single-line stub deletion (verify by checking no other diff lines beyond that one).
    - accessors.go is NOT in this plan's diff (verify via `git diff --name-only` does not list it).
  </acceptance_criteria>
  <done>TOOL-02 ships with the snapshot-invariant test + strict-subset paths test green. CONTEXT.md acceptance test #2 closed. File ownership preserved per checker B1+B3+B4 (this plan ADDED tools_refresh.go and DELETED one stub line; did not edit accessors.go).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP request -> refresh handler | Paths + wait_for_lsp + max_wait_ms are untrusted |
| refresh -> live.Service -> overlay | Drain bounded by classifier+coalescer (Phase 60); refresh never touches snapshot tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-05-01 | Tampering | Path traversal in args.Paths | mitigate | validatePaths rejects ".." and absolute-path-outside-root; same helper as P64-04. |
| T-64-05-02 | EoP | Caller bypasses read+ tier (already passes — read+ accepts all) | accept | read+ is the floor; mode_check.go modeTierRead returns nil for any session. |
| T-64-05-03 | DoS | wait_for_lsp blocks daemon for unbounded time | mitigate | MaxWaitMs capped at 3000 default; TelemetryMiddleware BudgetFunc additionally caps total tool time. |
| T-64-05-04 | Tampering | Refresh writes a snapshot (D-09 violation) | mitigate | Compile-time: tools_refresh.go imports only StoreAccessor's read surface (no BeginSnapshot, no CompactorAccessor.OnFlush). Grep-time: pattern returns no matches. Test-time: TestRefreshHandler_NoSnapshotCommit_Invariant fails the build if a future change introduces such a call. |
| T-64-05-05 | DoS | Refresh forces compactor (D-13 violation) | mitigate | Same compile-time + grep-time + test-time gates as T-04 covering OnFlush call site (recorder mock CompactorAccessor t.Fatal-s on OnFlush). |
</threat_model>

<verification>
- go vet ./internal/skill/semantic/... passes
- All 8 refresh tests pass
- grep gate: no snapshot/compactor symbols in tools_refresh.go
- gofmt -l empty
- accessors.go not edited; only tools_refresh.go (new) + tools_refresh_test.go (new) + skill.go (single-line delete)
</verification>

<success_criteria>
1. TOOL-02 handler ships with read+ gating, paths strict-subset, wait_for_lsp polling.
2. CONTEXT.md acceptance test #2 (refresh never commits a snapshot) — green via property test + grep gate.
3. CONTEXT.md D-09 + D-13 invariants enforced at multiple layers.
4. File ownership preserved (checker B1+B3+B4): tools_refresh.go (new), tools_refresh_test.go (new), skill.go (single-line stub delete only). accessors.go untouched.
</success_criteria>

<output>
Create .planning/phases/64-new-mcp-tools/64-05-SUMMARY.md.
</output>
