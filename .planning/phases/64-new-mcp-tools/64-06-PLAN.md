---
phase: 64-new-mcp-tools
plan: 06
type: execute
wave: 2
depends_on: ["64-02-PLAN", "64-03-PLAN", "64-04-PLAN"]
files_modified:
  - internal/skill/semantic/tools_status.go
  - internal/skill/semantic/tools_status_test.go
  - internal/skill/semantic/skill.go
autonomous: true
requirements:
  - TOOL-03
  - TOOL-05
must_haves:
  truths:
    - "get_semantic_graph_status returns SPEC §23.3 envelope: latest_snapshot_id, graph_version, overlay_active, pending_lsp_files, freshness, score_status (per projection), cluster_status (structured ClusterStatus value), last_live_update_ms."
    - "cluster_status is sourced via SchedulerAccessor.ClusterStatus (declared in 64-03's accessors.go); the production adapter wired by 64-08 returns ClusterStatus{State: \"unknown\", Reason: \"phase-62-clustering-no-status-accessor\"} until Phase 65/67 wires a live source — closes checker W1."
    - "Empty store returns zero-valued envelope with no error."
    - "Populated store surfaces score_status closed-enum values from Phase 62 RankScheduler."
    - "freshness enum matches SPEC §26.2 closed values."
    - "Mode-tier check is read+ (any session)."
    - "tools_status.go is a NEW file; this plan does not modify accessors.go (closes checker B1+B4: SchedulerAccessor.ClusterStatus was hoisted into 64-03)."
  artifacts:
    - path: internal/skill/semantic/tools_status.go
      provides: "registerGetSemanticGraphStatus + GetSemanticGraphStatusArgs typed-args (zero fields) + handler body. Owns const statusHelp."
      contains: "registerGetSemanticGraphStatus"
  key_links:
    - from: internal/skill/semantic/tools_status.go
      to: internal/skill/semantic/accessors.go
      via: "consumes Store/Scheduler/Queue/Live accessors and the ClusterStatus method declared in 64-03"
      pattern: "s\\.store\\.|s\\.scheduler\\.|s\\.queue\\.|s\\.live\\."
---

<objective>
Implement get_semantic_graph_status (TOOL-03): read+ mode tier; fan-out of read-only accessor calls into the SPEC §23.3 envelope. Pure read path — no writes.

Purpose: Status surfaces the runtime state agents need to decide whether to call refresh/index. Every accessor exists today (or has a documented W1 placeholder); the handler is a composition.

**File ownership (closes B1+B4):** This plan ADDS `tools_status.go` (new file) and DELETES the `statusHelp` line from `skill.go`'s help-text stub block. It does NOT edit `accessors.go` — `SchedulerAccessor.ClusterStatus` was hoisted into 64-03 so the interface surface is final at end-of-W0.

**Closes checker W1:** `cluster_status` is sourced via `SchedulerAccessor.ClusterStatus(repoID) ClusterStatus` (declared in 64-03's accessors.go). The production adapter (wired in P64-08) returns `ClusterStatus{State: "unknown", Reason: "phase-62-clustering-no-status-accessor"}` until Phase 65/67 wires a live cluster-status source. The response shape is correct from day one; observability captures the gap via the structured `Reason` field.

Output: tools_status.go (new) + table-driven tests covering empty + populated states + freshness-enum closure + cluster_status default-unknown shape. Plus single-line stub deletion in skill.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/64-new-mcp-tools/64-CONTEXT.md
@.planning/phases/64-new-mcp-tools/64-PATTERNS.md
@.planning/phases/64-new-mcp-tools/64-RESEARCH.md

<interfaces>
From envelope.go (P64-03):
  type StatusResult struct {
    CommonEnvelope                          // Freshness, GraphVersion, OverlayActive
    LatestSnapshotID    uint64
    ScoreStatus         map[string]string   // projection -> status string
    ClusterStatus       ClusterStatus       // structured per W1
    LastLiveUpdateMs    int64
    PendingLSPFiles     int
    RetrievalPending    bool
  }
  type ClusterStatus struct {
    State  string `json:"state"`              // "unknown" | "current" | "stale" | "building"
    Reason string `json:"reason,omitempty"`
  }

From accessors.go (P64-03 — FINAL; this plan only consumes):
  type SchedulerAccessor interface {
    IsQuiescent(repoID string) bool
    ScoreStatus(repoID, projection string) graph.ScoreStatus
    ClusterStatus(repoID string) ClusterStatus            // ← W1 closure
  }
  StoreAccessor.LatestCommittedSnapshot, .CurrentGraphVersion, .OverlayHasPendingRows
  QueueAccessor.DepthAll
  LiveAccessor.LastFlushAt

From handler_helpers.go (P64-04 — shared, this plan consumes read-only):
  errorResult, jsonResult

From skill.go (P64-03): s.sessionSnapshot, s.workspaceKey, s.store, s.scheduler, s.queue, s.live, s.retrieval

Projections to surface (per Phase 62 ships): "CALL_GRAPH_PAGERANK", "REFERENCE_PAGERANK", "FILE_DEPENDENCY_PAGERANK".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement tools_status.go handler + 5 unit tests; delete statusHelp stub from skill.go</name>
  <files>internal/skill/semantic/tools_status.go, internal/skill/semantic/tools_status_test.go, internal/skill/semantic/skill.go</files>
  <read_first>
    - internal/skill/semantic/skill.go (current state — has stub `statusHelp` in stub-var block; this task DELETES that line)
    - internal/skill/semantic/envelope.go (StatusResult shape + ClusterStatus struct from 64-03)
    - internal/skill/semantic/accessors.go (all accessor signatures including SchedulerAccessor.ClusterStatus)
    - internal/skill/semantic/tools_index.go (typed-args registration analog from P64-04)
    - internal/skill/semantic/handler_helpers.go (errorResult, jsonResult from P64-04)
    - internal/semantic/graph/status.go (lines 6-24 — ScoreStatus closed enum)
    - .planning/phases/64-new-mcp-tools/64-PATTERNS.md (lines 209-231 — tools_status.go fan-out pattern)
    - .planning/phases/64-new-mcp-tools/64-RESEARCH.md (lines 482-490 — accessor source map)
  </read_first>
  <action>
    1) Create internal/skill/semantic/tools_status.go (NEW file):
       - Declare `const statusHelp = "..."` at package scope (verbose multi-line: output shape + closed-enum freshness values + closed-enum cluster_status states).
       - GetSemanticGraphStatusArgs struct (no fields needed but declare empty struct for jsonschema consistency).
       - registerGetSemanticGraphStatus(server, s, tracer): mirror tools_index.go shape; Name="get_semantic_graph_status".
       - Handler body fan-out:
           1. snap := s.sessionSnapshot(ctx); if err := checkMode(snap, modeTierRead); err != nil { return errorResult(err.Error()), nil, nil } (passes always)
           2. ws := s.workspaceKey(ctx); repoID := ws.RepoID()
           3. Read every accessor in sequence, tolerating individual errors (log + zero-value the field; never fail the whole call):
                latestSnapID, _ := s.store.LatestCommittedSnapshot(ctx, repoID)
                gv, _ := s.store.CurrentGraphVersion(ctx, repoID)
                overlayActive := s.store.OverlayHasPendingRows(repoID)
                pendingLSP := s.queue.DepthAll(ws)
                lastFlush := s.live.LastFlushAt(ws)
                projections := []string{"CALL_GRAPH_PAGERANK", "REFERENCE_PAGERANK", "FILE_DEPENDENCY_PAGERANK"}
                scoreStatus := make(map[string]string, 3)
                for _, p := range projections {
                  scoreStatus[p] = string(s.scheduler.ScoreStatus(repoID, p))
                }
                clusterStatus := s.scheduler.ClusterStatus(repoID)   // ← W1 closure: production adapter returns
                                                                     //   ClusterStatus{State:"unknown", Reason:"phase-62-clustering-no-status-accessor"}.
                                                                     //   Until Phase 65/67 wires a live source, the State will be "unknown".
                retrievalPending := false
                if s.retrieval != nil { retrievalPending = s.retrieval.RetrievalPending(ws) }
           4. Compute freshness:
                switch {
                case retrievalPending:                  freshness = FreshnessStale
                case overlayActive && pendingLSP > 0:   freshness = FreshnessStructurallyFreshSemanticallyPending
                case overlayActive:                     freshness = FreshnessOverlayActive
                default:                                freshness = FreshnessFresh
                }
           5. return jsonResult(StatusResult{
                CommonEnvelope:    CommonEnvelope{Freshness: freshness, GraphVersion: gv, OverlayActive: overlayActive},
                LatestSnapshotID:  latestSnapID,
                ScoreStatus:       scoreStatus,
                ClusterStatus:     clusterStatus,
                LastLiveUpdateMs:  lastFlush,
                PendingLSPFiles:   pendingLSP,
                RetrievalPending:  retrievalPending,
              }), nil, nil

    2) Create internal/skill/semantic/tools_status_test.go with these tests:
       - TestStatusHandler_EmptyStore: all accessors return zero values. mockScheduler.ClusterStatus returns ClusterStatus{State:"unknown", Reason:"phase-62-clustering-no-status-accessor"} (mirrors what production adapter will do per W1). Result has LatestSnapshotID=0, GraphVersion=0, OverlayActive=false, ScoreStatus has 3 entries all "unknown" (or whatever zero ScoreStatus is — confirm by reading internal/semantic/graph/status.go line 6-24), Freshness=fresh, PendingLSPFiles=0, ClusterStatus.State="unknown", ClusterStatus.Reason starts with "phase-62-".
       - TestStatusHandler_PopulatedStore: mock store returns LatestCommittedSnapshot=42, CurrentGraphVersion=99, OverlayHasPendingRows=true; queue.DepthAll=5; scheduler.ScoreStatus returns "exact" for CALL_GRAPH, "stale" for REFERENCE, "approximate" for FILE_DEPENDENCY; scheduler.ClusterStatus returns ClusterStatus{State:"unknown", Reason:"phase-62-clustering-no-status-accessor"}. Result reflects all values; Freshness=structurally_fresh_semantically_pending (overlay + pending LSP).
       - TestStatusHandler_ClusterStatus_DefaultUnknown_ReasonPopulated (NEW — closes W1 testing): mock scheduler.ClusterStatus returns ClusterStatus{State:"unknown", Reason:"phase-62-clustering-no-status-accessor"}. Assert result.ClusterStatus.State == "unknown" AND result.ClusterStatus.Reason == "phase-62-clustering-no-status-accessor". Marshal to JSON and assert the field shape `{"state":"unknown","reason":"..."}`.
       - TestStatusHandler_FreshnessEnum_AllValuesValid: write a table over (overlayActive, pendingLSP, retrievalPending) tuples; assert Freshness is one of the four closed-enum values.
       - TestStatusHandler_RetrievalPending_FreshnessStale: retrieval.RetrievalPending=true. Result Freshness=stale, RetrievalPending=true.

    3) Update internal/skill/semantic/skill.go: SINGLE-LINE DELETE of `statusHelp = "get_semantic_graph_status: stub help (replaced by P64-06)"` from the stub-var block. The `Tools()` reference to `statusHelp` now resolves to the const declared in tools_status.go.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/... && go test ./internal/skill/semantic/ -run TestStatusHandler_ -count=1 -timeout=30s && ! grep -q 'statusHelp = "get_semantic_graph_status: stub help' internal/skill/semantic/skill.go && grep -q 'const statusHelp' internal/skill/semantic/tools_status.go && gofmt -l internal/skill/semantic/tools_status.go internal/skill/semantic/skill.go | tr -d ' \n' | grep -q '^$'</automated>
  </verify>
  <acceptance_criteria>
    - internal/skill/semantic/tools_status.go contains "func registerGetSemanticGraphStatus(" (substring match).
    - File body invokes ALL of: s.store.LatestCommittedSnapshot, s.store.CurrentGraphVersion, s.store.OverlayHasPendingRows, s.queue.DepthAll, s.live.LastFlushAt, s.scheduler.ScoreStatus, s.scheduler.ClusterStatus (grep returns hits for each).
    - File declares `const statusHelp = ` (substring match).
    - tools_status_test.go contains all 5 tests (including TestStatusHandler_ClusterStatus_DefaultUnknown_ReasonPopulated).
    - go test ./internal/skill/semantic/ -run TestStatusHandler_ -count=1 exits 0.
    - go vet ./internal/skill/semantic/... exits 0.
    - skill.go does NOT contain `statusHelp = "get_semantic_graph_status: stub help`.
    - accessors.go is NOT in this plan's diff.
  </acceptance_criteria>
  <done>TOOL-03 handler ships. CONTEXT.md acceptance test #3 closed (status envelope under empty + populated states with closed-enum freshness). W1 closure proven via TestStatusHandler_ClusterStatus_DefaultUnknown_ReasonPopulated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP request -> status handler | No untrusted input; handler is a pure read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-06-01 | Information Disclosure | Status leaks repo path / file counts | accept | Same data is already surfaced via get_health (existing tool); no new disclosure surface. |
| T-64-06-02 | DoS | Slow accessor blocks tool call | mitigate | Each accessor read is O(1) atomic / single SQL row; TelemetryMiddleware BudgetFunc bounds total tool time. |
| T-64-06-03 | Tampering | Spoofed projection name in ScoreStatus | mitigate | Hard-coded list of three projections — caller cannot inject. |
| T-64-06-04 | Information Disclosure | ClusterStatus.Reason leaks internal phase numbering | accept | "phase-62-clustering-no-status-accessor" is a known-public observability anchor; no secrets implicated. |
</threat_model>

<verification>
- go vet ./internal/skill/semantic/... passes
- All 5 status tests pass
- gofmt -l empty
- accessors.go not edited; only tools_status.go (new) + tools_status_test.go (new) + skill.go (single-line delete)
</verification>

<success_criteria>
1. TOOL-03 ships with all five state-coverage tests.
2. CONTEXT.md acceptance test #3 closed.
3. Closed-enum freshness values pinned by test.
4. W1 closed: cluster_status is structurally correct from day one (ClusterStatus.State="unknown", Reason="phase-62-clustering-no-status-accessor"); observability captures the gap.
5. File ownership preserved: tools_status.go (new), tools_status_test.go (new), skill.go (single-line stub delete only). accessors.go untouched.
</success_criteria>

<output>
Create .planning/phases/64-new-mcp-tools/64-06-SUMMARY.md.
</output>
