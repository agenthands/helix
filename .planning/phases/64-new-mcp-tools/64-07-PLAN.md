---
phase: 64-new-mcp-tools
plan: 07
type: tdd
wave: 2
depends_on: ["64-01-PLAN", "64-02-PLAN", "64-03-PLAN", "64-04-PLAN"]
files_modified:
  - internal/semantic/retrieval/bleve.go
  - internal/semantic/retrieval/corpus.go
  - internal/semantic/retrieval/rrf.go
  - internal/semantic/retrieval/recovery.go
  - internal/semantic/retrieval/config.go
  - internal/semantic/retrieval/rrf_test.go
  - internal/semantic/retrieval/bleve_test.go
  - internal/semantic/retrieval/recovery_test.go
  - internal/skill/semantic/tools_context.go
  - internal/skill/semantic/tools_context_test.go
  - internal/skill/semantic/skill.go
autonomous: true
requirements:
  - TOOL-04
  - TOOL-05
must_haves:
  truths:
    - "Bleve scorch index opens at <workspaceDir>/.helix/semantic.bleve/."
    - "Symbol corpus indexes (name + docstring + path + 5-line comment window) per D-06."
    - "Weighted RRF fuses text + graph rankings with K=60, w_text=1, w_graph=1 defaults (Go-internal constants per D-07)."
    - "RRF.Fuse output sorted (score desc, graph_version desc, symbol_id asc) — Phase 62 sort-before-iterate doctrine."
    - "Bleve recovery procedure: on workspace activation, compare bleve.last_indexed_snapshot_id vs Store.LatestCommittedSnapshot; rebuild from snapshot via Store.IterateCommittedSymbols (declared by 64-02) if mismatched."
    - "During rebuild, get_semantic_context returns retrieval_pending=true, freshness=stale."
    - "get_semantic_context determinism: 10 repeated calls produce byte-identical result order."
    - "Hybrid retrieval combines bleve full-text rankings with Phase 62 personalized PageRank scores via weighted RRF (D-05)."
    - "max_tokens clamped to [64, 32768]; default 2048."
    - "tools_context.go is a NEW file; this plan does not modify accessors.go (TopEdgesFor was hoisted into 64-03 — closes checker B4) or internal/semantic/store/effective_graph.go (IterateCommittedSymbols was hoisted into 64-02 — closes checker B2). 64-07 consumes both via interfaces only."
    - "Every behavior-adding task ships a RED phase (failing tests against stub signatures) followed by a GREEN phase (implementation that turns the tests green) — closes revision-W2."
  artifacts:
    - path: internal/semantic/retrieval/bleve.go
      provides: "Engine type wrapping bleve scorch index; Open/Upsert/QueryBleve/Close."
      contains: "bleve.Index"
    - path: internal/semantic/retrieval/rrf.go
      provides: "Fuse function implementing weighted RRF with deterministic tiebreak."
      contains: "Reciprocal Rank Fusion"
    - path: internal/semantic/retrieval/recovery.go
      provides: "Probe(ws) lifecycle: detect mismatch + spawn rebuild goroutine consuming Store.IterateCommittedSymbols via the StoreReader seam."
      contains: "IterateCommittedSymbols"
    - path: internal/skill/semantic/tools_context.go
      provides: "registerGetSemanticContext + GetSemanticContextArgs typed-args + handler body. Owns const contextHelp."
      contains: "registerGetSemanticContext"
  key_links:
    - from: internal/skill/semantic/tools_context.go
      to: internal/semantic/retrieval/bleve.go
      via: "QueryBleve(task, anchors)"
      pattern: "QueryBleve"
    - from: internal/skill/semantic/tools_context.go
      to: internal/semantic/retrieval/rrf.go
      via: "Fuse(textRanks, graphRanks, RRFConfig)"
      pattern: "rrf\\.Fuse|Fuse\\("
    - from: internal/semantic/retrieval/recovery.go
      to: internal/semantic/store/effective_graph.go
      via: "consumes Store.LatestCommittedSnapshot AND Store.IterateCommittedSymbols (both declared by 64-02) via the local StoreReader seam — NEVER modifies effective_graph.go"
      pattern: "IterateCommittedSymbols|LatestCommittedSnapshot"
---

<objective>
Build the retrieval engine (bleve + corpus mapper + RRF + recovery) and the get_semantic_context handler. PRECONDITION: P64-01 verdict must be PASS for bleve. If FAIL, this plan is rerouted by orchestrator to use DuckDB FTS5.

Purpose: get_semantic_context is the high-value tool agents use repeatedly. Determinism + foreground-budget responsiveness are the central correctness invariants.

**File ownership (closes B2+B4):** This plan ADDS files in `internal/semantic/retrieval/` (new package) and `internal/skill/semantic/tools_context.go` (new file). It DELETES the `contextHelp` line from `skill.go`'s help-text stub block. It does NOT modify:
- `internal/skill/semantic/accessors.go` — `RetrievalAccessor.TopEdgesFor` was hoisted into 64-03 (closes B4).
- `internal/semantic/store/effective_graph.go` — `IterateCommittedSymbols` was hoisted into 64-02 (closes B2).
- `internal/skill/semantic/skill.go` (beyond the single-line `contextHelp` stub deletion) — `workspaceKey` was hoisted into 64-03 (closes revision-W1 in sibling plan).

The Recoverer consumes `Store.IterateCommittedSymbols` and `Store.LatestCommittedSnapshot` via a LOCAL `StoreReader` interface seam declared in `recovery.go` (typical of "consumer-defined interfaces" Go idiom). Production wiring (P64-08) passes a `*Store` implementing that seam — the implementations are in 64-02's effective_graph.go.

**Closes revision-W2 (RED/GREEN structure):** This plan now ships in 6 atomic tasks — three RED phases producing failing tests against stub signatures, each immediately followed by its GREEN phase. Test files are listed BEFORE implementation files in each `<files>` block to make ordering visible. Mirrors the discipline used in 64-02/04/05.

Output: 5-file retrieval package (new) + tool handler (new) + bleve+RRF+recovery tests + determinism harness + tools_context tests, all driven by RED-first TDD. Plus single-line stub deletion in skill.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/64-new-mcp-tools/64-CONTEXT.md
@.planning/phases/64-new-mcp-tools/64-RESEARCH.md
@.planning/phases/64-new-mcp-tools/64-PATTERNS.md
@bench/semantic_bench_REPORT.md

<interfaces>
From envelope.go (P64-03):
  type ContextResult struct {
    CommonEnvelope
    FreshnessMode    FreshnessMode
    PendingLSPFiles  int
    RetrievalPending bool
    Candidates       []ContextCandidate
  }
  type ContextCandidate struct { SymbolID string; Confidence float64; Evidence ContextEvidence }
  type ContextEvidence struct { TextRank int; GraphRank int; MatchedTerms []string; TopEdges []string }
  type TextRank struct { SymbolID string; Score float64 }
  type GraphRank struct { SymbolID string; Score float64 }

From accessors.go (P64-03 — FINAL; this plan only consumes):
  type RetrievalAccessor interface {
    QueryBleve(task string, anchors []string) ([]TextRank, error)
    PersonalizedPageRank(ctx, repoID, anchors) ([]GraphRank, error)
    RetrievalPending(ws workspace.WorkspaceKey) bool
    TopEdgesFor(ctx context.Context, repoID, symbolID string) ([]string, error)   // ← B4 closure
  }

From skill.go (P64-03 — FINAL; this plan CONSUMES, never re-edits beyond single-line stub deletion):
  func (s *SemanticSkill) sessionSnapshot(ctx) mcp.SessionSnapshot
  func (s *SemanticSkill) workspaceKey(ctx) workspace.WorkspaceKey

From P64-02 *Store:
  LatestCommittedSnapshot(ctx, repoID) (uint64, error)
  IterateCommittedSymbols(ctx, snapshotID, fn func(SymbolRow) bool) error          // ← B2 closure
  QueryEffectiveAdjacency(ctx, repoID, projection) (out, in maps, err)
  type SymbolRow struct { SymbolID, Name, Path, Docstring, FileID string; LineStart int }

Bleve v2 API (per pkg.go.dev/github.com/blevesearch/bleve/v2):
  bleve.New(path, indexMapping) (Index, error)
  bleve.Open(path) (Index, error)
  Index.Index(id string, data interface{}) error
  Index.Batch(*Batch) error
  Index.Search(*SearchRequest) (*SearchResult, error)
  Index.SetInternal(key, val []byte) error
  Index.GetInternal(key) ([]byte, error)  // for storing last_indexed_snapshot_id
  Index.Close() error
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Failing tests for retrieval primitives (RRF + bleve smoke + corpus mapping)</name>
  <files>internal/semantic/retrieval/rrf_test.go, internal/semantic/retrieval/bleve_test.go, internal/semantic/retrieval/config.go, internal/semantic/retrieval/corpus.go, internal/semantic/retrieval/rrf.go, internal/semantic/retrieval/bleve.go</files>
  <read_first>
    - bench/semantic_bench_REPORT.md (gate verdict — abort + reroute if FAIL)
    - go.mod (confirm bleve v2 pin from P64-01)
    - .planning/phases/64-new-mcp-tools/64-PATTERNS.md (lines 421-515 — bleve/corpus/rrf pattern excerpts)
    - .planning/phases/64-new-mcp-tools/64-RESEARCH.md (lines 481-538 — Recovery + RRF formula)
    - internal/repomap/pagerank.go (deterministic ranking algorithm pattern reference)
    - internal/semantic/store/effective_graph.go (from 64-02 — read SymbolRow shape; recovery.go reuses this struct via the StoreReader seam)
  </read_first>
  <behavior>
    rrf_test.go (table-driven; tests fail because Fuse panic("not impl")):
    - TestRRF_EqualWeights_OrderByScore: text=[A,B,C], graph=[C,B,A]. K=60. Compute by hand and assert order.
    - TestRRF_TextOnly_NoGraph: empty graph -> ranks driven solely by text.
    - TestRRF_GraphOnly_NoText: empty text -> ranks driven solely by graph.
    - TestRRF_Determinism_TieScore: two symbols with equal RRF scores; tiebreak by graph_version desc, then symbol_id asc. Run Fuse 10 times — assert identical output across all 10 runs.
    - TestRRF_WeightedSplit: WText=2, WGraph=0.5; verify a text-strong symbol outranks a graph-strong one.

    bleve_test.go (tests fail because Engine methods panic("not impl")):
    - TestBleve_NewOpenClose: create + open + close cycle in tempdir; assert no error.
    - TestBleve_UpsertAndQuery: insert 5 SymbolDocs; query "foo bar"; assert at least one result with score > 0.
    - TestBleve_MetaRoundTrip: SetMeta("last_snap", []byte("42")); GetMeta returns same bytes.
  </behavior>
  <action>
    1) Create internal/semantic/retrieval/config.go (REAL — pure constants, no impl to stub):
       package retrieval
       type RRFConfig struct {
         K       int     // 60 default
         WText   float64 // 1.0 default
         WGraph  float64 // 1.0 default
       }
       func DefaultRRFConfig() RRFConfig { return RRFConfig{K: 60, WText: 1, WGraph: 1} }

       const (
         DefaultContextBudget = 2048
         MinTokenBudget       = 64
         MaxTokenBudget       = 32768
         CommentWindowLines   = 5
         TopEdgesPerCandidate = 5
       )

    2) Create STUB internal/semantic/retrieval/rrf.go (compiles; methods panic):
       package retrieval
       type FusedCandidate struct {
         SymbolID    string
         Score       float64
         GraphVersion uint64
         TextRank    int
         GraphRank   int
         MatchedTerms []string
       }
       func Fuse(text []TextRank, graph []GraphRank, cfg RRFConfig, gvLookup func(symbolID string) uint64) []FusedCandidate {
         panic("not implemented (RED gate — Task 2 GREEN fills this in)")
       }

    3) Create STUB internal/semantic/retrieval/bleve.go (compiles; methods panic):
       package retrieval
       type Engine struct{ /* fields filled in GREEN */ }
       type SymbolDoc struct { ID, Name, Path, Doc, CommentWindow string }
       type TextRank struct { SymbolID string; Score float64 }
       type GraphRank struct { SymbolID string; Score float64 }
       func New(path string) (*Engine, error)                       { panic("not implemented (RED)") }
       func Open(path string) (*Engine, error)                      { panic("not implemented (RED)") }
       func (e *Engine) Close() error                               { panic("not implemented (RED)") }
       func (e *Engine) UpsertBatch(ctx context.Context, docs []SymbolDoc) error { panic("not implemented (RED)") }
       func (e *Engine) QueryBleve(task string, anchors []string) ([]TextRank, error) { panic("not implemented (RED)") }
       func (e *Engine) GetMeta(key string) ([]byte, error)         { panic("not implemented (RED)") }
       func (e *Engine) SetMeta(key string, val []byte) error       { panic("not implemented (RED)") }

    4) Create STUB internal/semantic/retrieval/corpus.go:
       package retrieval
       import "github.com/agenthands/helix/internal/semantic/store"
       func MapSymbolToDoc(row store.SymbolRow, fileSource []byte) SymbolDoc {
         panic("not implemented (RED gate — Task 2 GREEN fills this in)")
       }

    5) Create rrf_test.go and bleve_test.go per the behavior block above. They MUST compile (so they exercise the stub signatures) but MUST fail at runtime due to the panics.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/semantic/retrieval/... && ! go test ./internal/semantic/retrieval/ -run 'TestRRF_|TestBleve_' -count=1 -timeout=60s</automated>
  </verify>
  <acceptance_criteria>
    - rrf_test.go contains all 5 named TestRRF_ tests.
    - bleve_test.go contains all 3 named TestBleve_ tests.
    - rrf.go, bleve.go, corpus.go all exist as compilable stubs (panic-bodied).
    - go vet ./internal/semantic/retrieval/... exits 0.
    - go test ./internal/semantic/retrieval/ -run 'TestRRF_|TestBleve_' -count=1 exits NON-ZERO (RED gate).
  </acceptance_criteria>
  <done>RED gate met for retrieval primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 GREEN: Implement retrieval primitives (rrf.go + bleve.go + corpus.go) — turns RED tests green</name>
  <files>internal/semantic/retrieval/rrf.go, internal/semantic/retrieval/bleve.go, internal/semantic/retrieval/corpus.go</files>
  <read_first>
    - internal/semantic/retrieval/rrf_test.go (RED tests drive impl)
    - internal/semantic/retrieval/bleve_test.go
    - internal/semantic/retrieval/config.go
    - .planning/phases/64-new-mcp-tools/64-PATTERNS.md (lines 421-515 — bleve/corpus/rrf pattern excerpts)
    - .planning/phases/64-new-mcp-tools/64-RESEARCH.md (lines 481-538 — RRF formula + recovery)
  </read_first>
  <action>
    1) Implement internal/semantic/retrieval/corpus.go (transform: store row -> bleve doc):
       func MapSymbolToDoc(row store.SymbolRow, fileSource []byte) SymbolDoc {
         // tokenize ident: split camel/snake into space-separated words for bleve analyzer
         // tokenize path: split on / and -
         // extract +/-5-line comment window around row.LineStart
       }
       Helpers: tokenizeIdent(s) []string, tokenizePath(p) []string, extractCommentWindow(src []byte, line int, window int) string.

       Note: the function takes `store.SymbolRow` from internal/semantic/store (declared by 64-02) — NOT a locally-redefined type.

    2) Implement internal/semantic/retrieval/bleve.go:
       package retrieval
       import (
         "context"
         bleve "github.com/blevesearch/bleve/v2"
         "github.com/blevesearch/bleve/v2/mapping"
         "github.com/blevesearch/bleve/v2/search/query"
       )
       type Engine struct {
         idx     bleve.Index
         path    string
         logger  *slog.Logger
       }
       func New(path string) (*Engine, error)
       func Open(path string) (*Engine, error)
       func (e *Engine) Close() error
       func (e *Engine) UpsertBatch(ctx, docs []SymbolDoc) error
       func (e *Engine) QueryBleve(task string, anchors []string) ([]TextRank, error)
       func (e *Engine) GetMeta(key string) ([]byte, error)
       func (e *Engine) SetMeta(key string, val []byte) error
       func buildMapping() *mapping.IndexMappingImpl

       Mapping: index `name`, `path`, `doc`, `comment_window` as text-analyzed; index `id` as keyword (exact). Anchors bias: build a BooleanQuery combining a MatchQuery on the task across all four text fields with a DisjunctionQuery boosting docs whose `id` is in anchors (BoostingQuery v2 API).

       TextRank type lives locally in this package (declared in the Task 1 RED stub). To avoid an import cycle with internal/skill/semantic, the retrieval package keeps its own TextRank/GraphRank types; the daemon-side adapter (P64-08) translates `[]retrieval.TextRank` → `[]semantic.TextRank` when implementing the skill-side `RetrievalAccessor`.

    3) Implement internal/semantic/retrieval/rrf.go:
       func Fuse(text []TextRank, graph []GraphRank, cfg RRFConfig, gvLookup func(symbolID string) uint64) []FusedCandidate {
         scores := make(map[string]float64)
         textRankByID := make(map[string]int)
         graphRankByID := make(map[string]int)
         for rank, t := range text {
           textRankByID[t.SymbolID] = rank + 1
           scores[t.SymbolID] += cfg.WText / float64(cfg.K + rank + 1)
         }
         for rank, g := range graph {
           graphRankByID[g.SymbolID] = rank + 1
           scores[g.SymbolID] += cfg.WGraph / float64(cfg.K + rank + 1)
         }
         ids := make([]string, 0, len(scores))
         for id := range scores { ids = append(ids, id) }
         sort.Strings(ids)
         out := make([]FusedCandidate, 0, len(ids))
         for _, id := range ids {
           out = append(out, FusedCandidate{
             SymbolID: id, Score: scores[id], GraphVersion: gvLookup(id),
             TextRank: textRankByID[id], GraphRank: graphRankByID[id],
           })
         }
         sort.SliceStable(out, func(i, j int) bool {
           if out[i].Score != out[j].Score { return out[i].Score > out[j].Score }
           if out[i].GraphVersion != out[j].GraphVersion { return out[i].GraphVersion > out[j].GraphVersion }
           return out[i].SymbolID < out[j].SymbolID
         })
         return out
       }
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/semantic/retrieval/... && go test ./internal/semantic/retrieval/ -run 'TestRRF_|TestBleve_' -count=1 -timeout=60s && go test ./internal/semantic/retrieval/ -run TestRRF_Determinism_TieScore -count=10 -timeout=30s && gofmt -l internal/semantic/retrieval/ | tr -d ' \n' | grep -q '^$'</automated>
  </verify>
  <acceptance_criteria>
    - internal/semantic/retrieval/{bleve,corpus,rrf,config}.go all exist with real implementations (no panics remaining for tested code paths).
    - bleve.go contains "bleve.Index" (substring match).
    - rrf.go contains "func Fuse(" and "sort.SliceStable" (substring matches).
    - rrf.go tiebreak uses (score desc, graph_version desc, symbol_id asc).
    - corpus.go imports/uses store.SymbolRow (the type declared in 64-02's effective_graph.go).
    - go test ./internal/semantic/retrieval/ -run 'TestRRF_|TestBleve_' -count=1 exits 0.
    - go test -run TestRRF_Determinism_TieScore -count=10 exits 0.
    - go vet passes.
    - gofmt clean.
    - internal/semantic/store/effective_graph.go is NOT in this plan's diff.
  </acceptance_criteria>
  <done>GREEN gate met. Retrieval primitives ship; RRF determinism proven across 10 runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 RED: Failing tests for recovery procedure (StoreReader seam + 6 mismatch cases)</name>
  <files>internal/semantic/retrieval/recovery_test.go, internal/semantic/retrieval/recovery.go</files>
  <read_first>
    - internal/semantic/retrieval/bleve.go (Engine.GetMeta/SetMeta from Task 1 GREEN)
    - internal/semantic/store/effective_graph.go (READ-ONLY: IterateCommittedSymbols + LatestCommittedSnapshot signatures from 64-02 — recovery.go consumes via the StoreReader seam)
    - .planning/phases/64-new-mcp-tools/64-RESEARCH.md (lines 522-544 — recovery procedure)
    - .planning/phases/64-new-mcp-tools/64-PATTERNS.md (lines 519-538 — recovery pattern)
  </read_first>
  <behavior>
    recovery_test.go tests (use a fake StoreReader implementation in tests; no real *Store dependency):
    - TestRecovery_NoSnapshot_Noop: store.LatestCommittedSnapshot returns 0. Probe returns nil; engine.GetMeta(last_indexed) is unset.
    - TestRecovery_BleveMatchesSnapshot_Noop: pre-populate engine.SetMeta(last_indexed, "42"); store returns 42. Probe returns nil; no rebuild status created.
    - TestRecovery_BleveMissing_TriggersRebuild: store returns 42 with 5 stub symbols (fake StoreReader.IterateCommittedSymbols invokes fn with 5 deterministic rows). Engine empty. Probe returns nil immediately; goroutine populates engine; after wait <2s, RetrievalPending(ws)=false and engine.GetMeta(last_indexed)="42".
    - TestRecovery_BleveStale_TriggersRebuild: engine.SetMeta(last_indexed, "10"); store returns 42 with 5 symbols. Probe -> goroutine -> RetrievalPending true initially, false after rebuild.
    - TestRecovery_BleveAhead_LogsWarnAndRebuilds: engine.SetMeta(last_indexed, "100"); store returns 42 (post-rollback). Probe rebuilds anyway (treats as missing).
    - TestRecovery_StoreReaderInterface_AbortOnFalse: fake StoreReader's IterateCommittedSymbols: confirm Recoverer's fn-callback returns true normally (does not abort iteration); aborting is reserved for callers wanting partial walks (not used by recovery).
  </behavior>
  <action>
    1) Create STUB internal/semantic/retrieval/recovery.go (compiles; methods panic):
       package retrieval

       import (
         "context"
         "log/slog"
         "sync"
         "sync/atomic"
         "time"

         "github.com/agenthands/helix/internal/semantic/store"
         "github.com/agenthands/helix/internal/workspace"
       )

       const metaKeyLastIndexed = "last_indexed_snapshot_id"

       // StoreReader is a consumer-defined interface seam. Production wiring (P64-08)
       // passes a *Store value (the implementations live in 64-02's effective_graph.go).
       type StoreReader interface {
         LatestCommittedSnapshot(ctx context.Context, repoID string) (uint64, error)
         IterateCommittedSymbols(ctx context.Context, snapshotID uint64, fn func(store.SymbolRow) bool) error
       }

       type RecoveryStatus struct {
         InProgress bool
         StartedAt  time.Time
         Total      int64
         Indexed    atomic.Int64
       }

       type Recoverer struct {
         engine    *Engine
         store     StoreReader
         logger    *slog.Logger
         mu        sync.Mutex
         statuses  map[string]*RecoveryStatus  // key=repoID
       }

       func NewRecoverer(engine *Engine, store StoreReader, logger *slog.Logger) *Recoverer { panic("not implemented (RED)") }
       func (r *Recoverer) Probe(ctx context.Context, ws workspace.WorkspaceKey) error      { panic("not implemented (RED)") }
       func (r *Recoverer) RetrievalPending(ws workspace.WorkspaceKey) bool                  { panic("not implemented (RED)") }

    2) Create recovery_test.go covering all 6 behaviors. Tests must compile (exercising the stub signatures) and fail due to panics.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/semantic/retrieval/... && ! go test ./internal/semantic/retrieval/ -run TestRecovery_ -count=1 -timeout=30s</automated>
  </verify>
  <acceptance_criteria>
    - recovery.go contains "type StoreReader interface" with both `LatestCommittedSnapshot` and `IterateCommittedSymbols` methods.
    - recovery_test.go contains all 6 named tests.
    - go vet ./internal/semantic/retrieval/... exits 0.
    - go test ./internal/semantic/retrieval/ -run TestRecovery_ -count=1 exits NON-ZERO (RED gate).
  </acceptance_criteria>
  <done>RED gate met for recovery procedure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement recovery.go (bleve dual-store reconciliation) — turns RED tests green</name>
  <files>internal/semantic/retrieval/recovery.go</files>
  <read_first>
    - internal/semantic/retrieval/recovery_test.go (RED tests drive impl)
    - internal/semantic/retrieval/bleve.go (Engine.GetMeta/SetMeta + UpsertBatch from Task 1 GREEN)
    - internal/skill/repomap/skill.go (lines 100-106, 362-471 — lazy walk + cache populate analog)
    - .planning/phases/64-new-mcp-tools/64-RESEARCH.md (lines 522-544 — recovery procedure)
  </read_first>
  <action>
    Implement Recoverer per the recovery procedure:

    func NewRecoverer(engine, store, logger) *Recoverer { return &Recoverer{...} }

    func (r *Recoverer) Probe(ctx, ws) error:
      // 1. latest := r.store.LatestCommittedSnapshot(ctx, ws.RepoID()).
      // 2. if latest == 0 -> nothing to do; return nil.
      // 3. raw := r.engine.GetMeta(metaKeyLastIndexed); parse uint64 (treat missing/parse-fail as 0).
      // 4. if raw == latest -> no-op; return nil.
      // 5. if raw > latest -> log WARN (post-rollback case); fall through to rebuild.
      // 6. Spawn goroutine: r.statuses[ws.RepoID()] = &RecoveryStatus{InProgress: true, StartedAt: now}.
      //    Iterate via r.store.IterateCommittedSymbols(ctx, latest, fn): batch up to 1000 SymbolDocs at a time;
      //    inside fn, call corpus.MapSymbolToDoc(row, nil) (file content not loaded for this seam — see note below)
      //    and append to a per-batch slice. When the batch reaches 1000 (or fn is called for the last time),
      //    call r.engine.UpsertBatch and update status.Indexed. Return true from fn to continue iteration; ctx
      //    cancellation handled by the caller of IterateCommittedSymbols (which honors ctx via QueryContext).
      //    On done: r.engine.SetMeta(metaKeyLastIndexed, []byte(strconv.FormatUint(latest, 10))); delete status entry; clear InProgress.

    func (r *Recoverer) RetrievalPending(ws) bool:
      // returns true if r.statuses[ws.RepoID()].InProgress.

    Note on file content for comment-window extraction: `IterateCommittedSymbols` returns `SymbolRow` without file source bytes. For recovery, we pass `nil` for fileSource to `MapSymbolToDoc` and accept that the comment-window field is empty for symbols indexed via recovery (rather than at the original commit time). Bleve queries against the other text fields (name, path, docstring) still work. If a future phase wants comment-window on recovery, 64-02 can extend `SymbolRow` to include file bytes (or a separate accessor returning content) — that's deferred.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/semantic/retrieval/... && go test ./internal/semantic/retrieval/ -run TestRecovery_ -count=1 -timeout=30s && gofmt -l internal/semantic/retrieval/recovery.go | tr -d ' \n' | grep -q '^$'</automated>
  </verify>
  <acceptance_criteria>
    - internal/semantic/retrieval/recovery.go contains "func (r *Recoverer) Probe(" with real implementation (no panics on tested paths).
    - File imports "github.com/agenthands/helix/internal/semantic/store" but does NOT contain `func (s *store.Store)` or any other modification of the store package (verify via `grep -c 'func (s \*store.Store)' internal/semantic/retrieval/recovery.go` returns 0).
    - go test ./internal/semantic/retrieval/ -run TestRecovery_ -count=1 exits 0.
    - go vet passes.
    - internal/semantic/store/effective_graph.go is NOT in this plan's diff (verify via git status — that file is owned by 64-02).
  </acceptance_criteria>
  <done>GREEN gate met. Bleve recovery shipped, consumes Store via interface only. B2 closure proven: IterateCommittedSymbols is read via the local StoreReader seam; effective_graph.go is never edited by 64-07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 RED: Failing tests for tools_context handler (9 behaviors including determinism harness)</name>
  <files>internal/skill/semantic/tools_context_test.go, internal/skill/semantic/tools_context.go, internal/skill/semantic/skill.go</files>
  <read_first>
    - internal/skill/semantic/skill.go (current state — has stub `contextHelp` in stub-var block; THIS task DELETES that line + adds a stub `tools_context.go` with panic-bodied handler)
    - internal/skill/semantic/envelope.go (ContextResult shape)
    - internal/skill/semantic/accessors.go (RetrievalAccessor interface — including TopEdgesFor declared by 64-03)
    - internal/skill/semantic/tools_index.go (typed-args + handler analog from P64-04)
    - internal/skill/semantic/handler_helpers.go (errorResult, jsonResult, validatePaths from P64-04)
    - internal/skill/repomap/skill.go (lines 289-351 — execGetContext analog; lines 516-538 — extractTokenBudget)
    - internal/semantic/retrieval/rrf.go (Fuse signature from Task 1 GREEN)
  </read_first>
  <behavior>
    tools_context_test.go tests (must compile + fail at runtime due to handler stub panic):
    - TestContextHandler_HappyPath_RankedEvidence: mock retrieval returns textRanks=[A,B,C], graphRanks=[C,B,A]; mock retrieval.TopEdgesFor returns [edge1, edge2]. Result.Candidates non-empty with per-candidate Evidence populated (TopEdges captured from accessor).
    - TestContextHandler_RetrievalPending_ReturnsStale: mock retrieval.RetrievalPending=true. Result has RetrievalPending=true, Freshness=stale, Candidates=nil.
    - TestContextHandler_TokenBudgetClamped_Min: args.MaxTokens=0 -> uses default 2048.
    - TestContextHandler_TokenBudgetClamped_Max: args.MaxTokens=99999 -> clamped to 32768.
    - TestContextHandler_PathTraversalRejected: args.Files=["../etc"] -> InvalidArgs.
    - TestContextHandler_Determinism_10Runs: same task/anchors/budget; run 10 times; assert byte-identical JSON marshal of Result.Candidates across all 10 runs (CONTEXT.md acceptance test #4 / #8).
    - TestContextHandler_FreshnessMode_AllowStale_Default: empty FreshnessMode -> defaults to allow_stale.
    - TestContextHandler_ModeReadAccepted: session.Mode="read" -> handler proceeds.
    - TestContextHandler_TopEdgesFor_RespectsCap: mock TopEdgesFor returns 10 edges; assert handler caps at 5 in Evidence.TopEdges.
  </behavior>
  <action>
    1) Create STUB internal/skill/semantic/tools_context.go (compiles; handler panics so tests run RED):
       package semantic

       import (
         "context"
         // imports for mcpsdk, kernel, etc. — verify exact paths from internal/skill/semantic/tools_index.go
       )

       const contextHelp = "get_semantic_context: stub help (RED gate; full multi-line text lands in GREEN)"

       type GetSemanticContextArgs struct {
         Task          string   `json:"task,omitempty"`
         Files         []string `json:"files,omitempty"`
         Symbols       []string `json:"symbols,omitempty"`
         MaxTokens     int      `json:"max_tokens,omitempty"`
         FreshnessMode string   `json:"freshness_mode,omitempty" jsonschema:"allow_stale | require_current | validate_live"`
       }

       func registerGetSemanticContext(server *mcp.SerenaMCPServer, s *SemanticSkill, tracer trace.Tracer) {
         panic("not implemented (RED gate — Task 3 GREEN fills this in)")
       }

       // Handler symbol that the test harness invokes directly — also panics.
       func (s *SemanticSkill) handleGetSemanticContext(ctx context.Context, args GetSemanticContextArgs) (*mcpsdk.CallToolResult, any, error) {
         panic("not implemented (RED gate)")
       }

    2) Update internal/skill/semantic/skill.go: SINGLE-LINE DELETE of `contextHelp = "get_semantic_context: stub help (replaced by P64-07)"` from the stub-var block. The `Tools()` reference to `contextHelp` now resolves to the const declared in tools_context.go (RED stub form first; GREEN replaces with the real verbose const).

       NOTE: This plan does NOT add any methods to skill.go. workspaceKey + sessionSnapshot are owned by 64-03 and consumed by this plan's GREEN handler via direct calls (`s.workspaceKey(ctx)`, `s.sessionSnapshot(ctx)`).

    3) Create tools_context_test.go covering all 9 behaviors. Tests must compile (exercising the typed-args + handler signatures) and fail at runtime due to handler panic.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/... && ! go test ./internal/skill/semantic/ -run TestContextHandler_ -count=1 -timeout=30s && ! grep -q 'contextHelp = "get_semantic_context: stub help (replaced' internal/skill/semantic/skill.go && grep -q 'const contextHelp' internal/skill/semantic/tools_context.go</automated>
  </verify>
  <acceptance_criteria>
    - tools_context.go exists as compilable stub (panics in handler body).
    - tools_context.go declares `const contextHelp = ` (substring match) — even if stub-text initially.
    - skill.go does NOT contain `contextHelp = "get_semantic_context: stub help (replaced by P64-07)"` (the W0 stub line is gone).
    - tools_context_test.go contains all 9 named tests (including TestContextHandler_TopEdgesFor_RespectsCap and TestContextHandler_Determinism_10Runs).
    - go vet ./internal/skill/semantic/... exits 0.
    - go test -run TestContextHandler_ exits NON-ZERO (RED gate).
  </acceptance_criteria>
  <done>RED gate met for tools_context handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 GREEN: Implement tools_context.go handler (consumes 64-03's workspaceKey/sessionSnapshot) — turns RED tests green</name>
  <files>internal/skill/semantic/tools_context.go</files>
  <read_first>
    - internal/skill/semantic/tools_context_test.go (RED tests drive impl)
    - internal/skill/semantic/skill.go (consumes s.workspaceKey + s.sessionSnapshot — both authored by 64-03)
    - internal/skill/semantic/tools_index.go (typed-args + handler analog from P64-04)
    - internal/skill/semantic/handler_helpers.go (errorResult, jsonResult, validatePaths from P64-04)
    - internal/semantic/retrieval/rrf.go (Fuse signature)
    - .planning/phases/64-new-mcp-tools/64-PATTERNS.md (lines 233-275 — tools_context.go pattern excerpt)
  </read_first>
  <action>
    1) Replace the stub `const contextHelp` in tools_context.go with the verbose multi-line help text (parameter docs for task/files/symbols/max_tokens/freshness_mode + return shape). Single substitution; no edit to skill.go needed.

    2) Add greedy token-budget packing helper inside tools_context.go (or a new context_helpers.go OWNED by 64-07; do not touch handler_helpers.go which is owned by 64-04):
       func greedyPack(candidates []retrieval.FusedCandidate, budget int, tokensPerCandidate func(retrieval.FusedCandidate) int) []retrieval.FusedCandidate {
         out := make([]retrieval.FusedCandidate, 0, len(candidates))
         used := 0
         for _, c := range candidates {
           cost := tokensPerCandidate(c)
           if used + cost > budget { continue }  // skip; do not break — allows packing smaller followers
           out = append(out, c)
           used += cost
         }
         return out
       }

    3) Implement the real registerGetSemanticContext + handler body, replacing the panic stubs:
       - registerGetSemanticContext(server, s, tracer): mirrors tools_index.go shape; Name="get_semantic_context".
       - Handler body:
           1. snap := s.sessionSnapshot(ctx); if err := checkMode(snap, modeTierRead); err != nil { return errorResult(err.Error()), nil, nil }.
           2. ws := s.workspaceKey(ctx)         // method authored by 64-03 — consume directly
           3. if err := validatePaths(args.Files, ws.RepoRoot()); err != nil { return errorResult(err.Error()), nil, nil }
              if err := validateSymbolIDs(args.Symbols); err != nil { return errorResult(err.Error()), nil, nil }
           4. budget := clamp(args.MaxTokens, retrieval.MinTokenBudget, retrieval.MaxTokenBudget); if zero -> retrieval.DefaultContextBudget.
           5. fmod := args.FreshnessMode -> closed-enum FreshnessMode value (default allow_stale).
           6. retrievalPending := s.retrieval.RetrievalPending(ws).
           7. gv, _ := s.store.CurrentGraphVersion(ctx, ws.RepoID())
              overlayActive := s.store.OverlayHasPendingRows(ws.RepoID())
              pendingLSP := s.queue.DepthAll(ws)
           8. If retrievalPending:
                return jsonResult(ContextResult{
                  CommonEnvelope:    CommonEnvelope{Freshness: FreshnessStale, GraphVersion: gv, OverlayActive: overlayActive},
                  FreshnessMode:    fmod,
                  PendingLSPFiles:  pendingLSP,
                  RetrievalPending: true,
                  Candidates:       nil,
                }), nil, nil
           9. anchors := append(args.Files, args.Symbols...)
           10. textRanksRaw, _ := s.retrieval.QueryBleve(args.Task, anchors)
               graphRanksRaw, _ := s.retrieval.PersonalizedPageRank(ctx, ws.RepoID(), anchors)
               // Convert to retrieval.TextRank/GraphRank if accessor returns the skill-package types.
           11. fused := rrf.Fuse(textRanks, graphRanks, retrieval.DefaultRRFConfig(), gvLookupFn).
           12. packed := greedyPack(fused, budget, tokensPerCandidate).
           13. Build candidates with evidence shape:
                 for each FusedCandidate -> ContextCandidate{
                   SymbolID: c.SymbolID,
                   Confidence: clampToUnit(c.Score),
                   Evidence: ContextEvidence{
                     TextRank: c.TextRank, GraphRank: c.GraphRank,
                     MatchedTerms: c.MatchedTerms,
                     TopEdges: topEdgesFromAccessor(ctx, s, ws.RepoID(), c.SymbolID),
                   },
                 }
                 // topEdgesFromAccessor calls s.retrieval.TopEdgesFor(ctx, repoID, symbolID) (declared by 64-03).
                 // Caps result at 5 entries via TopEdgesPerCandidate const.
           14. freshness := computeFreshness(overlayActive, pendingLSP > 0, retrievalPending)
           15. Return ContextResult.

       gvLookupFn: trivially `func(symbolID string) uint64 { return gv }` for now (graph_version is per-snapshot, not per-symbol; using the global gv for tiebreak satisfies sort-before-iterate determinism).
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/... && go test ./internal/skill/semantic/ -run TestContextHandler_ -count=1 -timeout=30s && go test ./internal/skill/semantic/ -run TestContextHandler_Determinism_10Runs -count=1 -timeout=30s && gofmt -l internal/skill/semantic/tools_context.go internal/skill/semantic/skill.go | tr -d ' \n' | grep -q '^$'</automated>
  </verify>
  <acceptance_criteria>
    - internal/skill/semantic/tools_context.go contains "func registerGetSemanticContext(" with real implementation (no panic).
    - File invokes s.retrieval.QueryBleve, s.retrieval.PersonalizedPageRank, s.retrieval.TopEdgesFor, rrf.Fuse (grep returns each).
    - File invokes `s.workspaceKey(` and `s.sessionSnapshot(` (consumes 64-03's helpers; substring matches).
    - File contains the closed-enum FreshnessMode handling (substring match for "allow_stale").
    - File declares `const contextHelp = ` with verbose body (length > 200 chars; not the RED stub).
    - go test ./internal/skill/semantic/ -run TestContextHandler_ -count=1 exits 0.
    - go test -run TestContextHandler_Determinism_10Runs -count=1 exits 0 (10 byte-identical results).
    - skill.go does NOT contain `contextHelp = "get_semantic_context: stub help`.
    - accessors.go is NOT in this plan's diff.
    - skill.go diff in this plan is EXCLUSIVELY the single-line stub deletion from RED Task 3 (no method additions in either RED or GREEN — closes revision-W1 in sibling plan).
  </acceptance_criteria>
  <done>GREEN gate met. TOOL-04 ships with determinism harness green. CONTEXT.md acceptance test #4 + #8 closed. B2 + B4 closures proven: this plan never edits accessors.go nor effective_graph.go. revision-W2 closed: 6 atomic tasks with strict RED→GREEN sequencing, test files listed before impl files in each <files> block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP request -> retrieval | task + files + symbols + max_tokens are untrusted |
| bleve segment <-> snapshot | Two stores not transactionally bound; daemon-restart drift possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-07-01 | Tampering | Path traversal in args.Files | mitigate | validatePaths called on every Files/Symbols slice. |
| T-64-07-02 | DoS | Unbounded max_tokens | mitigate | Clamp to MaxTokenBudget=32768. |
| T-64-07-03 | DoS | Unbounded task string driving bleve scan | mitigate | bleve query has internal limits; TelemetryMiddleware BudgetFunc bounds total time. |
| T-64-07-04 | Information Disclosure | Symbol IDs leak project structure | accept | Same disclosure as existing repomap tools; agents already see this surface. |
| T-64-07-05 | Tampering | bleve segment corruption -> incorrect results | mitigate | Recovery procedure rebuilds from snapshot when last_indexed_snapshot_id mismatches. |
| T-64-07-06 | DoS | Recovery rebuild blocks retrieval indefinitely | mitigate | Probe spawns goroutine; tools_context returns retrieval_pending=true immediately; agents poll status. |
| T-64-07-07 | Tampering | Concurrent rebuild + UpsertBatch race | mitigate | Recoverer.mu guards statuses map; bleve batch ops are internally goroutine-safe per bleve docs. |
| T-64-07-08 | Information Disclosure | Raw bleve error in MCP envelope | mitigate | errorResult helper returns closed-enum reason; raw err logged via slog. |
</threat_model>

<verification>
- go vet ./internal/semantic/retrieval/... ./internal/skill/semantic/... passes
- All RRF/bleve/recovery/context tests pass GREEN
- go test -count=10 on determinism test exits 0
- gofmt clean
- go run ./cmd/vet-noduckdb ./... passes (retrieval pkg uses no duckdb-go directly — goes through StoreReader seam)
- internal/skill/semantic/accessors.go NOT edited (B4 closure)
- internal/semantic/store/effective_graph.go NOT edited (B2 closure)
- internal/skill/semantic/skill.go diff exclusively the single-line contextHelp stub deletion (no method additions)
- Each task pair (RED→GREEN) ran independently — RED commits demonstrate failing tests; GREEN commits demonstrate passing tests
</verification>

<success_criteria>
1. Retrieval package complete (5 files in new package), built RED-first.
2. Determinism harness asserts 10 byte-identical runs.
3. Recovery procedure handles all 4 mismatch cases via consumer-defined StoreReader interface, built RED-first.
4. TOOL-04 handler ships with foreground-budget read path (no recompute on hot path), built RED-first.
5. CONTEXT.md acceptance test #4, #6 (recovery), #8 (determinism) closed.
6. File ownership preserved: this plan adds tools_context.go (new) + 5 retrieval files (new) and deletes one stub line from skill.go. accessors.go and effective_graph.go are NOT in the diff (B2 + B4 closed).
7. revision-W2 closure: 6 tasks structured as RED→GREEN pairs with `tdd="true"` on every task, test files preceding implementation files in each `<files>` block.
</success_criteria>

<output>
Create .planning/phases/64-new-mcp-tools/64-07-SUMMARY.md.
</output>
</content>
</invoke>