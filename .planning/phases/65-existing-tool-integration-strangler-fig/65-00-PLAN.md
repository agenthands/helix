---
phase: 65
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/lint/nokernel2semantic/analyzer.go
  - internal/lint/nokernel2semantic/analyzer_test.go
  - internal/lint/nokernel2semantic/testdata/src/github.com/agenthands/helix/internal/kernel/integimport/integimport.go
autonomous: true
requirements:
  - INTEG-01
  - INTEG-03
  - INTEG-04
tags:
  - vet
  - architecture
  - allowlist

must_haves:
  truths:
    - "Kernel packages can import internal/semantic/integ without violating nokernel2semantic (research finding #2; M-vet)"
    - "Kernel packages still cannot import internal/semantic/store, internal/semantic/retrieval, internal/semantic/graph (M-vet boundary preserved)"
    - "analysistest fixture confirms allowlisted import passes; existing fixtures still fail as expected"
  artifacts:
    - path: internal/lint/nokernel2semantic/analyzer.go
      provides: "Allowlist constant + slash-boundary aware predicate (Pitfall §1; A6)"
      contains: "allowedSemanticIntegPath"
    - path: internal/lint/nokernel2semantic/analyzer_test.go
      provides: "TestAnalyzer_AllowsKernelImportingSemanticInteg"
      contains: "AllowsKernelImportingSemanticInteg"
    - path: internal/lint/nokernel2semantic/testdata/src/github.com/agenthands/helix/internal/kernel/integimport/integimport.go
      provides: "analysistest fixture: a kernel-prefixed package importing the allowlisted integ stub"
      contains: "internal/semantic/integ"
  key_links:
    - from: internal/lint/nokernel2semantic/analyzer.go
      to: TestAnalyzer_AllowsKernelImportingSemanticInteg
      via: "analysistest.Run on kernel/integimport fixture"
      pattern: "AllowsKernelImportingSemanticInteg"
    - from: 65-03 (consumer)
      to: internal/semantic/integ (kernel-side import)
      via: "go vet passes once allowlist amendment lands"
      pattern: "make vet"
---

<objective>
Amend the `nokernel2semantic` analyzer with an explicit allowlist for `internal/semantic/integ/` so that subsequent waves (65-03, 65-06) can import the new types-only `integ` package from kernel packages without tripping `make vet`. Without this Wave-0 unblock, the kernel-side `analyze_blast_radius` skill adapter will fail to compile under `make vet` (RESEARCH.md Common Pitfalls §1).

Purpose: Closes M-vet for Phase 65. Preserves the architectural intent ("kernel does not import internal/semantic/* concretions") while opening a single, named, types-only seam.

Output: Amended analyzer + analysistest fixture proving both the allowed and forbidden paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-CONTEXT.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-RESEARCH.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-PATTERNS.md
@internal/lint/nokernel2semantic/analyzer.go
@internal/lint/nokernel2semantic/analyzer_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Amend nokernel2semantic with integ allowlist (RED → GREEN)</name>
  <files>
    internal/lint/nokernel2semantic/analyzer.go
    internal/lint/nokernel2semantic/analyzer_test.go
    internal/lint/nokernel2semantic/testdata/src/github.com/agenthands/helix/internal/kernel/integimport/integimport.go
    internal/lint/nokernel2semantic/testdata/src/github.com/agenthands/helix/internal/semantic/integ/stub.go
  </files>
  <read_first>
    - internal/lint/nokernel2semantic/analyzer.go (target file; full 57 LOC)
    - internal/lint/nokernel2semantic/analyzer_test.go (analog: existing analysistest pattern)
    - .planning/phases/65-existing-tool-integration-strangler-fig/65-RESEARCH.md (Common Pitfalls §1 — exact-match-or-slash-boundary recommendation per A6)
    - .planning/phases/65-existing-tool-integration-strangler-fig/65-PATTERNS.md ("internal/lint/nokernel2semantic/analyzer.go (MODIFIED — allowlist)")
  </read_first>
  <behavior>
    - Test 1 (RED→GREEN): TestAnalyzer_AllowsKernelImportingSemanticInteg — analysistest.Run on `internal/kernel/integimport` fixture (which imports `internal/semantic/integ`) reports ZERO diagnostics.
    - Test 2 (still RED→GREEN): TestAnalyzer_RejectsKernelImportingSemantic — existing kernel/badpkg fixture (which imports `internal/semantic/store`) still reports the existing diagnostic.
    - Test 3 (regression guard): TestAnalyzer_RejectsKernelImportingSemanticIntegLookalike — a fixture importing `internal/semantic/integ_evil` (NOT the allowed exact path) STILL reports the diagnostic. Validates A6 (exact-or-slash-boundary, not bare HasPrefix).
  </behavior>
  <action>
    1. RED: Write the three test functions in `analyzer_test.go` BEFORE editing `analyzer.go`. Run `go test ./internal/lint/nokernel2semantic/... -count=1` and confirm Test 1 fails (current analyzer rejects integ import).
    2. Create stub at `internal/lint/nokernel2semantic/testdata/src/github.com/agenthands/helix/internal/semantic/integ/stub.go` containing `package integ` and a single exported sentinel symbol (e.g. `var Sentinel struct{}`) so the fixture has something to import.
    3. Create fixture at `internal/lint/nokernel2semantic/testdata/src/github.com/agenthands/helix/internal/kernel/integimport/integimport.go` containing `package integimport` with `import _ "github.com/agenthands/helix/internal/semantic/integ"` and NO `// want` comment (zero-diagnostic expected).
    4. Create regression fixture at `internal/lint/nokernel2semantic/testdata/src/github.com/agenthands/helix/internal/kernel/integlookalike/integlookalike.go` importing `github.com/agenthands/helix/internal/semantic/integ_evil` with a `// want "..."` comment matching the existing diagnostic format.
    5. GREEN: Edit `analyzer.go` lines 33-57 — add a constant `allowedSemanticIntegPath = "github.com/agenthands/helix/internal/semantic/integ"` next to `forbiddenImportPrefix`. Replace the predicate `if strings.HasPrefix(path, forbiddenImportPrefix)` with a helper `isForbidden(path)` that returns true ONLY when (a) `strings.HasPrefix(path, forbiddenImportPrefix)` AND (b) NOT (`path == allowedSemanticIntegPath` OR `strings.HasPrefix(path, allowedSemanticIntegPath+"/")`). The slash-boundary check enforces A6 — `internal/semantic/integ_evil` (no slash boundary) is still forbidden; `internal/semantic/integ/sub` (slash boundary) is allowed.
    6. Update the package doc-comment (lines 1-25) and the `Analyzer.Doc` string to note the integ allowlist.
    7. Run `go test ./internal/lint/nokernel2semantic/... -count=1 -race` and confirm all three tests pass.
  </action>
  <verify>
    <automated>go test ./internal/lint/nokernel2semantic/... -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/lint/nokernel2semantic/... -count=1 -race` exits 0 with PASS for TestAnalyzer_AllowsKernelImportingSemanticInteg, TestAnalyzer_RejectsKernelImportingSemantic, and TestAnalyzer_RejectsKernelImportingSemanticIntegLookalike.
    - `grep -c "allowedSemanticIntegPath" internal/lint/nokernel2semantic/analyzer.go` returns at least 2 (constant + use site).
    - `grep -c "AllowsKernelImportingSemanticInteg" internal/lint/nokernel2semantic/analyzer_test.go` returns at least 1.
    - `go vet ./internal/lint/nokernel2semantic/...` exits 0.
    - `go build ./...` exits 0 (no compilation regression).
  </acceptance_criteria>
  <done>Analyzer amendment lands; integ allowlist proven by passing test; lookalike regression guard rejects bare-prefix bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → vet | Static analysis enforces architecture invariants pre-merge |
| developer → analyzer | Analyzer authors a contract; consumers (Phase 65 wave 1) rely on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-00-01 | T (tampering with architecture) | nokernel2semantic predicate | mitigate (M-vet) | Slash-boundary check (not bare HasPrefix); regression test `TestAnalyzer_RejectsKernelImportingSemanticIntegLookalike` enforces A6 |
| T-65-00-02 | E (elevation of privilege) | future kernel package | mitigate (M-vet) | Allowlist is a single named path; any expansion requires deliberate code edit + new analysistest fixture |
| T-65-00-03 | I (information disclosure) | analyzer error text | accept | Analyzer reports import path only; no secrets in source |
</threat_model>

<verification>
- `go test ./internal/lint/nokernel2semantic/... -count=1 -race` PASSES.
- `make vet` (if invoked locally) reports zero new diagnostics.
- Fixture tree under `testdata/` follows existing analysistest convention.
</verification>

<success_criteria>
- Allowlist constant present + use site reads it (grep canary).
- Three tests pass: allow integ; still reject store; reject lookalike.
- M-vet boundary unbroken: any kernel→semantic/store import still flagged.
</success_criteria>

<output>
After completion, create `.planning/phases/65-existing-tool-integration-strangler-fig/65-00-SUMMARY.md`.
</output>
