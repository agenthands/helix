---
phase: 65
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - internal/daemon/semantic_wiring.go
  - internal/daemon/semantic_wiring_test.go
autonomous: true
requirements:
  - INTEG-01
  - INTEG-02
tags:
  - buildfn
  - extract
  - snapshot

must_haves:
  truths:
    - "makeProductionBuildFn writes real per-language Facts to the snapshot (D-09 carryover; replaces empty-Facts placeholder)"
    - "Per-file extraction errors are non-fatal (research §Pattern 4); the build commits whatever extracted successfully"
    - "TestE2E_IndexThenContext_SymbolCount in internal/skill/semantic asserts a non-zero committed symbol count after this plan ships"
    - "Pipeline invokes extract.Provider.Extract → extract.ToStoreFacts → store.WriteSnapshotFacts in that order"
    - "Classifier walk uses live.ClassifyPathChange (Phase 60); deleted/skipped files bypass extraction"
  artifacts:
    - path: internal/daemon/semantic_wiring.go
      provides: "Production buildFn pipeline (replaces empty-Facts at lines 687-742)"
      contains: "ToStoreFacts"
    - path: internal/daemon/semantic_wiring_test.go
      provides: "TestProductionBuildFn_WritesNonEmptyFacts integration harness"
      contains: "TestProductionBuildFn"
  key_links:
    - from: internal/daemon/semantic_wiring.go (makeProductionBuildFn)
      to: internal/semantic/extract/to_store.go:58 (ToStoreFacts)
      via: "facts := extract.ToStoreFacts(extracted)"
      pattern: "ToStoreFacts"
    - from: internal/daemon/semantic_wiring.go (makeProductionBuildFn)
      to: internal/semantic/live/classifier.go (ClassifyPathChange)
      via: "live.ClassifyPathChange(ctx, repoID, path, b.store, nil, live.ChangeSourceWatcher)"
      pattern: "ClassifyPathChange"
    - from: internal/daemon/semantic_wiring.go (makeProductionBuildFn)
      to: internal/semantic/store (WriteSnapshotFacts)
      via: "b.store.WriteSnapshotFacts(ctx, snap, facts)"
      pattern: "WriteSnapshotFacts"
---

<objective>
Replace the empty-Facts placeholder in `makeProductionBuildFn` at `internal/daemon/semantic_wiring.go:687-742` with the real production pipeline documented inline at lines 621-686 and verified in RESEARCH.md §Pattern 4. The pipeline walks (full mode) or drains overlay (incremental mode), classifies each path via `live.ClassifyPathChange`, resolves the per-language extractor through the registry (Phase 59 EXTRACT-01..05), converts to wire-format `Facts` via the locked `extract.ToStoreFacts` adapter, and commits via the existing snapshot lifecycle.

Purpose: Closes Phase 64 carryover D-09 #1. Without this, the strangler-fig integration in Wave 1+ has zero ranking signal — every `semantic`-source envelope renders against an empty graph.

Output: Production buildFn pipeline + integration test asserting non-empty Facts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-CONTEXT.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-RESEARCH.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-PATTERNS.md
@.planning/phases/64-new-mcp-tools/VERIFICATION.md
@internal/daemon/semantic_wiring.go
@internal/semantic/extract/to_store.go
@internal/semantic/live/classifier.go

<interfaces>
<!-- Key contracts the executor needs. Do NOT explore — use these directly. -->

From internal/semantic/extract/to_store.go:
```go
// ToStoreFacts converts per-language extracted files to wire-format Facts.
// Defensive nil-skip; preserves Partial flags.
func ToStoreFacts(files []*ExtractedFile) semanticstore.Facts
```

From internal/semantic/extract/provider.go (Phase 59):
```go
type Provider interface {
    Extract(ctx context.Context, source []byte, file SourceFile) (*ExtractedFile, error)
}
type Registry interface {
    Provider(language string) (Provider, bool)
}
```

From internal/semantic/live/classifier.go:
```go
func ClassifyPathChange(
    ctx context.Context,
    repoID semantic.RepoID,
    path string,
    store *semanticstore.Store,
    hasher Hasher, // nil OK — falls back to default
    source ChangeSource, // live.ChangeSourceWatcher
) (kind ChangeKind, ok bool, err error)
```

From internal/semantic/store (snapshot lifecycle, contract documented at semantic_wiring.go:668-674):
```go
BeginSnapshot(ctx, SnapshotMeta{RepoID, BaseSnapshotID}) (*Snapshot, error)
WriteSnapshotFacts(ctx, snap, Facts) error
CommitSnapshot(ctx, snap, SnapshotSummary) error
AbortSnapshot(ctx, snap, reason) error
```
</interfaces>
</context>

<feature>
  <name>Production buildFn pipeline (D-09 carryover #1)</name>
  <files>
    internal/daemon/semantic_wiring.go (replace lines 687-742; keep structure)
    internal/daemon/semantic_wiring_test.go (NEW or extend existing)
  </files>
  <behavior>
    Mode=full: walk repo root via filepath.WalkDir; classify each path; for first-class language (Provider returns ok), read file source, call provider.Extract, accumulate ExtractedFile slice. Mode=incremental: read base snapshot, drain overlay-pending paths via the live overlay surface; same classify+extract loop on the drained paths. Skipped/deleted/unknown-language files are dropped. Total extraction failures still commit an empty snapshot (matches Phase 64 behavior). Snapshot lifecycle: BeginSnapshot → WriteSnapshotFacts(ctx, snap, ToStoreFacts(extracted)) → CommitSnapshot. Defer-Abort on non-commit path.

    RED: TestProductionBuildFn_WritesNonEmptyFacts asserts that running the production buildFn against a tempdir containing 3 minimal Go files produces FilesIndexed >= 3 and the resulting snapshot contains > 0 SymbolRows when iterated via store.IterateCommittedSymbols.

    GREEN: Replace the placeholder; run the test until it passes. Per-file errors are logged at debug; the loop continues.

    REFACTOR: Extract `walkOrDrain(b, ws, mode)` helper if line count exceeds 70; keep loop body readable.
  </behavior>
  <implementation>
    Use the verbatim pipeline shape from RESEARCH §Pattern 4 (research lines 396-490). Concrete signatures verified at:
    - extract.Provider.Extract: internal/semantic/extract/provider.go:48
    - extract.ToStoreFacts: internal/semantic/extract/to_store.go:58
    - live.ClassifyPathChange: internal/semantic/live/classifier.go:51
    - *Store.LatestCommittedSnapshot: internal/semantic/store/effective_graph.go:208
    - Snapshot lifecycle contract: internal/daemon/semantic_wiring.go:668-674

    Existing imports already present (verify at top of semantic_wiring.go); add `extract` + `live` if not present.

    Reuse existing `b.store`, `b.extractRegistry`, `b.live` fields on `*semanticBundle`. If `extractRegistry` field is missing on `semanticBundle`, add it as part of this plan with construction in `newSemanticBundle` (consume the daemon's `extract.Registry` instance — the daemon constructs one per Phase 59 wiring).

    Language detection helper `langFromExt(path)` — reuse `internal/repomap/lang.go` if available; otherwise add a tiny in-file helper mapping `.go`→"go", `.ts`→"typescript", `.tsx`→"typescript", `.js`→"javascript", `.py`→"python".
  </implementation>
</feature>

<tdd_gates>
**RED gate:** `go test ./internal/daemon/... -run TestProductionBuildFn_WritesNonEmptyFacts -count=1` MUST FAIL on the empty-Facts placeholder. Commit message: `test(65-01): add failing test for production buildFn pipeline`.

**GREEN gate:** Same command passes after pipeline lands. Commit message: `feat(65-01): implement production buildFn (D-09 carryover #1)`.

**REFACTOR gate:** Extract helpers if needed; same test still passes. Commit message: `refactor(65-01): extract walkOrDrain helper`.
</tdd_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → extract | per-language extractors consume bytes; classifier mediates symlink + size policy |
| extract → store | ToStoreFacts is the locked wire boundary (Phase 65 D-08 / extract.to_store.go) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01-01 | D (DoS via huge file) | filepath.WalkDir | mitigate | live.ClassifyPathChange already enforces size + symlink rules (T-60-04-03); reuse, do not bypass |
| T-65-01-02 | T (tampering) | per-file extractor errors | mitigate | non-fatal; per-file failures logged at debug; loop continues; commit proceeds with whatever extracted (research §Pattern 4 acceptable error behavior) |
| T-65-01-03 | I (info disclosure) | error text | accept | errors stay in slog at debug level inside the daemon; no MCP envelope at this layer |
| T-65-01-04 | M-readtier | snapshot lifecycle | accept | this plan WRITES (BeginSnapshot/CommitSnapshot) — by design, this is the build path, not a lookup. Lookup tier (read+) is enforced in 65-03. |
</threat_model>

<verification>
- `go test ./internal/daemon/... -count=1 -race` PASSES (new test included).
- `go test ./internal/skill/semantic/... -run TestE2E_IndexThenContext_SymbolCount -count=1` PASSES with non-zero count (was already locked to 15 in Phase 64 fixture; will continue passing).
- `grep -c "ToStoreFacts" internal/daemon/semantic_wiring.go` returns at least 1.
- `grep -c "ClassifyPathChange" internal/daemon/semantic_wiring.go` returns at least 1.
- `grep -c "semanticstore.Facts{}" internal/daemon/semantic_wiring.go` returns 0 (placeholder removed; or only present in zero-fallback paths).
- `go build ./...` exits 0; `go vet ./...` exits 0.
</verification>

<success_criteria>
- Empty-Facts placeholder is replaced with the real pipeline.
- Three commits land in TDD order (RED → GREEN → REFACTOR if needed).
- Phase 64 VERIFICATION.md TODO(phase-65) anchor at `internal/daemon/semantic_wiring.go:687-742` is removed in the GREEN commit.
- Per-file extraction errors do not abort the build.
</success_criteria>

<output>
After completion, create `.planning/phases/65-existing-tool-integration-strangler-fig/65-01-SUMMARY.md`.
</output>
