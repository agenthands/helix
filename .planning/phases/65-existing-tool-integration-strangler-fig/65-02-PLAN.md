---
phase: 65
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - internal/daemon/semantic_wiring.go
  - internal/daemon/semantic_wiring_test.go
autonomous: true
requirements:
  - INTEG-02
  - INTEG-04
tags:
  - workspace
  - session
  - adapter

must_haves:
  truths:
    - "semSessionAdapter.Workspace returns the daemon's active workspace key, never workspace.WorkspaceKey{} (D-09 carryover #2)"
    - "Retrieval queries via the session adapter are workspace-scoped (not against repoRoot=\"\"); fixes Phase 65 strangler-fig zero-signal regression"
    - "TestSemSessionAdapter_WorkspaceResolved confirms non-zero key after activation"
  artifacts:
    - path: internal/daemon/semantic_wiring.go
      provides: "semSessionAdapter.Workspace returns wsKeyFn() (closure pass-through, RESEARCH §Pattern 3 option a)"
      contains: "wsKeyFn"
    - path: internal/daemon/semantic_wiring_test.go
      provides: "TestSemSessionAdapter_WorkspaceResolved unit test"
      contains: "TestSemSessionAdapter_WorkspaceResolved"
  key_links:
    - from: internal/daemon/semantic_wiring.go (semSessionAdapter)
      to: internal/daemon/daemon.go:521 (activeWSKey closure)
      via: "wsKeyFn func() workspace.WorkspaceKey threaded via newSemanticBundle"
      pattern: "wsKeyFn"
    - from: semSessionAdapter.Workspace
      to: retrieval (workspace-scoped engine handle)
      via: "ws := a.wsKeyFn() // returns activeWSKey"
      pattern: "wsKeyFn()"
---

<objective>
Replace the zero-value `workspace.WorkspaceKey{}` return at `internal/daemon/semantic_wiring.go:509-525` with a closure-based registry lookup. The daemon already maintains `activeWSKey` at `internal/daemon/daemon.go:521-523` and exposes it through a `wsKeyFn func() workspace.WorkspaceKey` closure (already passed to `symbols.RegisterTools` and `edit.RegisterTools`). Phase 65 threads the same closure through `newSemanticBundle` into the `semSessionAdapter` so retrieval queries are workspace-scoped.

Purpose: Closes Phase 64 carryover D-09 #2. Without this, `semRetrievalAdapter` queries against `repoRoot=""` and the strangler-fig integration inherits zero ranking signal.

Output: `wsKeyFn` field on `semSessionAdapter` populated from the daemon's active workspace closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-CONTEXT.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-RESEARCH.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-PATTERNS.md
@.planning/phases/64-new-mcp-tools/VERIFICATION.md
@internal/daemon/semantic_wiring.go
</context>

<feature>
  <name>WorkspaceKey adapter via closure pass-through (D-09 carryover #2)</name>
  <files>
    internal/daemon/semantic_wiring.go (modify semSessionAdapter struct lines ~498-525; modify newSemanticBundle signature/call sites)
    internal/daemon/daemon.go (thread the existing wsKeyFn closure into newSemanticBundle)
    internal/daemon/semantic_wiring_test.go (NEW or extend)
  </files>
  <behavior>
    RED: TestSemSessionAdapter_WorkspaceResolved constructs a semSessionAdapter with a stubbed `wsKeyFn` returning a known non-zero `workspace.WorkspaceKey{RepoRoot: "/tmp/test-ws", ...}`. Asserts `adapter.Workspace(ctx) == knownKey`. Initially fails because the field does not exist.

    GREEN: Add `wsKeyFn func() workspace.WorkspaceKey` field to `semSessionAdapter`. Replace the body of `Workspace(ctx)` with `if a == nil || a.wsKeyFn == nil { return workspace.WorkspaceKey{} }; return a.wsKeyFn()`. Update `newSemanticBundle` to accept a `wsKeyFn` parameter; update the call site in `daemon.go` to pass the existing `wsKeyFn` closure.

    REFACTOR: None expected — change is a single field + plumb-through.
  </behavior>
  <implementation>
    See RESEARCH §Pattern 3 option (a). The existing closure is at `internal/daemon/daemon.go:521-523` and is already passed to `symbols.RegisterTools(...)` (`daemon.go:526`) and `edit.RegisterTools(...)` (line 527). Phase 65 plumbs the same closure into `newSemanticBundle`. This is a single-workspace daemon today; multi-workspace expansion (deferred per `semantic_wiring.go:559`) will swap the closure for a real registry without changing the field shape.

    Reject the SessionInfo-reverse-hash approach (option b in RESEARCH §Pattern 3) — `*mcp.SessionInfo.WorkspaceKey` is a sha256-truncated string and reversing it requires a registry lookup anyway; the closure pass-through is strictly simpler.
  </implementation>
</feature>

<tdd_gates>
**RED gate:** `go test ./internal/daemon/... -run TestSemSessionAdapter_WorkspaceResolved -count=1` FAILS (field does not exist; compiles only after stub field added in RED commit, then test asserts the wired path). Commit: `test(65-02): add failing test for workspace-key adapter`.

**GREEN gate:** Same command PASSES. Commit: `feat(65-02): wire wsKeyFn into semSessionAdapter (D-09 carryover #2)`.
</tdd_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon → semantic adapter | wsKeyFn closure crosses the daemon-boundary into the bundle; daemon owns the source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-02-01 | T (tampering) | wsKeyFn nil after construction | mitigate | nil-guard on a.wsKeyFn returns zero WorkspaceKey; preserves fail-safe behavior |
| T-65-02-02 | I (info disclosure) | RepoRoot path | accept | RepoRoot is already known to the daemon; not a secret in this layer |
| T-65-02-03 | M-key | zero WorkspaceKey from session adapter | mitigate | dedicated test asserts non-zero return after wiring |
</threat_model>

<verification>
- `go test ./internal/daemon/... -count=1 -race` PASSES.
- `grep -n "a.wsKeyFn" internal/daemon/semantic_wiring.go` returns at least one match.
- `grep -n "return workspace.WorkspaceKey{}" internal/daemon/semantic_wiring.go` returns AT MOST nil-guard hits (no unconditional zero return remains in the Workspace() method body).
- The Phase 64 VERIFICATION.md `TODO(phase-65)` anchor at lines 509-525 is removed in the GREEN commit.
- `go build ./...` exits 0.
</verification>

<success_criteria>
- `Workspace(ctx)` returns the daemon's `activeWSKey` via the closure.
- `wsKeyFn` field nil-guarded against pre-init use.
- Phase 64 carryover anchor removed.
</success_criteria>

<output>
After completion, create `.planning/phases/65-existing-tool-integration-strangler-fig/65-02-SUMMARY.md`.
</output>
