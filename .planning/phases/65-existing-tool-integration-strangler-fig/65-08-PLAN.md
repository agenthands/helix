---
phase: 65
plan: 08
type: execute
wave: 3
depends_on: [05, 06, 07]
files_modified:
  - internal/skill/semantic/integration_test.go
  - internal/skill/repomap/skill_integration_test.go
autonomous: true
requirements:
  - INTEG-01
  - INTEG-02
  - INTEG-03
  - INTEG-04
  - INTEG-05
tags:
  - integration
  - matrix
  - acceptance

must_haves:
  truths:
    - "Index-disabled goldens are byte-identical (modulo JSON envelope wrap from 65-05); env.Tree text matches pre-Phase-65 output verbatim"
    - "Strangler-fig source × fallback_reason matrix covers every meaningful cell across the four MCP tools"
    - "TestE2E_StranglerFig_SourceMatrix table-driven exercises: (semantic, ''), (tree_sitter, ''), (fallback, no_snapshot_yet), (fallback, index_building), (fallback, index_error), (fallback, bleve_rebuilding)"
    - "Existing TestE2E_IndexThenContext_SymbolCount 15-symbol fixture extended for the matrix; non-zero count after 65-01 ships"
    - "Phase 65 acceptance closure: every D-NN decision and every INTEG requirement exercised by automated test"
  artifacts:
    - path: internal/skill/semantic/integration_test.go
      provides: "TestE2E_StranglerFig_SourceMatrix table-driven across {source × fallback_reason × tool}"
      contains: "TestE2E_StranglerFig_SourceMatrix"
    - path: internal/skill/repomap/skill_integration_test.go
      provides: "TestEnvelope_IndexDisabledIsTreeSitter (D-04 / Pitfall §3) + tree-text-byte-identical fixture lock"
      contains: "TestEnvelope_IndexDisabledIsTreeSitter"
  key_links:
    - from: matrix table
      to: integ.Source × integ.FallbackReason
      via: "table-driven: 6 rows × 4 tools = 24 cells (subset where meaningful)"
      pattern: "SourceMatrix"
    - from: index-disabled golden
      to: env.Tree byte-identical to pre-65 output
      via: "captured fixture file + assert.Equal(t, expected, env.Tree)"
      pattern: "env.Tree"
---

<objective>
Lock the Phase 65 acceptance contract with a single integration test matrix that exercises every meaningful `{source × fallback_reason}` combination across the four strangler-fig tools (`get_repo_map`, `get_context`, `analyze_blast_radius`, `get_health`). Confirm that:

1. Index-disabled goldens preserve the v1.9 tree text byte-identically (D-04; INTEG-01 contract; Pitfall §3 — source==tree_sitter, NOT source==fallback+reason==index_disabled in steady state).
2. Every closed-enum (`source`, `fallback_reason`) value emitted by any of the four tools is exercised by at least one row of the matrix.
3. The 15-symbol fixture from `TestE2E_IndexThenContext_SymbolCount` (Phase 64 P64-08) is extended for the strangler-fig matrix and now sees a non-zero committed-symbol count thanks to 65-01's production buildFn.

Purpose: Closes Phase 65 verification gate — every INTEG requirement has automated test coverage.

Output: Matrix integration test + golden-text-byte-identical assertion on the index-disabled path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-existing-tool-integration-strangler-fig/65-CONTEXT.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-RESEARCH.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-PATTERNS.md
@.planning/phases/65-existing-tool-integration-strangler-fig/65-VALIDATION.md
@internal/skill/semantic/integration_test.go
@internal/skill/repomap/skill_integration_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Index-disabled goldens preserved (byte-identical tree text)</name>
  <files>
    internal/skill/repomap/skill_integration_test.go
    internal/skill/repomap/testdata/goldens/index_disabled_repo_map.txt (NEW — captured tree text)
    internal/skill/repomap/testdata/goldens/index_disabled_context.txt (NEW — captured tree text)
  </files>
  <read_first>
    - internal/skill/repomap/skill_integration_test.go (lines 141-170 — existing index-disabled goldens, modified by 65-05 to JSON-parse)
    - .planning/phases/65-existing-tool-integration-strangler-fig/65-RESEARCH.md (Pitfall §2 — JSON-wrap preserves tree-text under env.Tree byte-identical)
    - .planning/phases/65-existing-tool-integration-strangler-fig/65-PATTERNS.md ("internal/skill/repomap/skill_integration_test.go (MODIFIED — golden mechanical update)")
  </read_first>
  <action>
    1. Run the v1.9 path with cfg.SemanticIndex.Enabled=false against the existing test fixture. Capture the resulting `env.Tree` string into `internal/skill/repomap/testdata/goldens/index_disabled_repo_map.txt` (commit the bytes verbatim).
    2. Repeat for get_context against the same fixture; save to `index_disabled_context.txt`.
    3. Add `TestEnvelope_IndexDisabledIsTreeSitter` to `skill_integration_test.go`:
       - call execGetRepoMap with cfg.Enabled=false
       - parse the JSON envelope
       - assert env.Source == "tree_sitter"
       - assert env.FallbackReason == ""
       - assert env.Tree == captured-golden-text (byte-identical via os.ReadFile + assert.Equal)
       - mirror for execGetContext
    4. Add a fail-loud comment block above the goldens explaining: "Phase 65 INTEG-01 contract — internal/repomap engine source MUST NOT change. If this test fails after engine modification, the offending change violates INTEG-01."
  </action>
  <verify>
    <automated>go test ./internal/skill/repomap/... -run TestEnvelope_IndexDisabledIsTreeSitter -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/skill/repomap/... -run TestEnvelope_IndexDisabledIsTreeSitter -count=1 -race` exits 0.
    - `internal/skill/repomap/testdata/goldens/index_disabled_repo_map.txt` exists with non-empty content.
    - `internal/skill/repomap/testdata/goldens/index_disabled_context.txt` exists with non-empty content.
    - `grep -c "TestEnvelope_IndexDisabledIsTreeSitter" internal/skill/repomap/skill_integration_test.go` returns at least 1.
    - `grep -c "INTEG-01" internal/skill/repomap/skill_integration_test.go` returns at least 1 (the fail-loud comment block).
  </acceptance_criteria>
  <done>Index-disabled goldens locked; tree text byte-identical; D-04 + INTEG-01 contract under regression guard.</done>
</task>

<task type="auto">
  <name>Task 2: Strangler-fig source × fallback_reason matrix integration test</name>
  <files>
    internal/skill/semantic/integration_test.go (extend; existing TestE2E_IndexThenContext_SymbolCount serves as foundation)
  </files>
  <read_first>
    - internal/skill/semantic/integration_test.go (lines 420-480 — existing 15-symbol harness)
    - .planning/phases/65-existing-tool-integration-strangler-fig/65-RESEARCH.md (Validation Architecture — full matrix list at lines 976-994)
    - .planning/phases/65-existing-tool-integration-strangler-fig/65-PATTERNS.md ("internal/skill/semantic/integration_test.go (MODIFIED — extend 15-symbol fixture)")
    - internal/semantic/integ/source_select.go (ChooseSource — matrix arbiter under test)
  </read_first>
  <action>
    1. Add `TestE2E_StranglerFig_SourceMatrix` table-driven integration test to `internal/skill/semantic/integration_test.go`. Reuse the existing 15-symbol harness `newE2EHarness(t, "review", makeFixtureFacts(15))` as a base; for each row, mutate harness state to produce the target source/fallback_reason cell.

    2. Matrix rows (each row drives ALL FOUR tools where applicable):
       - Row A: cfg.Enabled=false → expect every envelope source=="tree_sitter", fallback_reason=="" (depends on 65-06 routing source-selection through `integ.ChooseSource(cfgGate, lookup, nil)` per D-04 + Pitfall §3 — if a future edit reverts 65-06's handler to the bare `lookup == nil || !lookup.Available()` short-circuit, this row will fail because analyze_blast_radius will emit `source==fallback,fallback_reason==index_disabled` instead of `source==tree_sitter`).
       - Row B: cfg.Enabled=true, snapshot committed (15 symbols), no overlay → expect source=="semantic", graph_version > 0; (analyze_blast_radius) confidence ladder applied; (get_health) latest_snapshot_status=="ready", graph_version matching status.
       - Row C: cfg.Enabled=true, NO snapshot yet → expect source=="fallback", fallback_reason=="no_snapshot_yet"; v1.9 path runs; (analyze_blast_radius) confidence cap ≤ 0.6.
       - Row D: cfg.Enabled=true, build in progress (singleflight in-flight) → expect source=="fallback", fallback_reason=="index_building".
       - Row E: cfg.Enabled=true, store error injected → expect source=="fallback", fallback_reason=="index_error".
       - Row F: cfg.Enabled=true, bleve segment in recovery (RetrievalPending=true) → expect source=="fallback", fallback_reason=="bleve_rebuilding".

    3. For each row, assert:
       - get_repo_map / get_context envelope source field equals expected
       - get_repo_map / get_context envelope fallback_reason field equals expected (or empty)
       - analyze_blast_radius envelope source field equals expected
       - analyze_blast_radius confidence cap (Row C/D/E/F → all PerNode.Confidence ≤ 0.6)
       - get_health envelope: when row is A → no semantic_index block; when row B-F → semantic_index block present with the appropriate latest_snapshot_status

    4. Provide row-id labels in `t.Run(fmt.Sprintf("Row_%s_%s_%s", row.Source, row.FallbackReason, row.Tool), ...)` so failure output names the failing cell.

    5. Confirm the existing `TestE2E_IndexThenContext_SymbolCount` (15-symbol fixture) still asserts non-zero count after 65-01 ships (it is locked to expectedSymbolCount=15; that assertion now passes with real data, not the empty-Facts placeholder).
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/... -run "TestE2E_StranglerFig_SourceMatrix|TestE2E_IndexThenContext_SymbolCount" -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/skill/semantic/... -run "TestE2E_StranglerFig_SourceMatrix" -count=1 -race -v` exits 0; output contains at least 6 distinct subtest names matching `Row_*`.
    - `go test ./internal/skill/semantic/... -run "TestE2E_IndexThenContext_SymbolCount" -count=1 -race` exits 0 (continued passage with real Facts from 65-01).
    - `grep -c "TestE2E_StranglerFig_SourceMatrix" internal/skill/semantic/integration_test.go` returns 1.
    - `grep -E "(no_snapshot_yet|index_building|index_error|bleve_rebuilding)" internal/skill/semantic/integration_test.go` returns at least 4 matches (each closed-enum value referenced).
    - Phase-level command `go test ./internal/skill/repomap/... ./internal/skill/semantic/... ./internal/kernel/symbols/... ./internal/kernel/health/... ./internal/semantic/integ/... ./internal/daemon/... ./internal/lint/nokernel2semantic/... -count=1 -race` exits 0.
  </acceptance_criteria>
  <done>Matrix exercised; every closed-enum source/fallback_reason value covered; existing 15-symbol fixture continues passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| acceptance test → integration env | tests own the harness; production code unchanged in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-08-01 | T (tampering) | engine source change slipping past INTEG-01 | mitigate | byte-identical golden text assertion + fail-loud comment block |
| T-65-08-02 | M7 path-drift | source mis-classification leaks into production | mitigate | matrix exercises every closed-enum cell; failure surfaces the row id |
| T-65-08-03 | M-confcap | confidence cap regression on fallback | mitigate | Row C/D/E/F assert all PerNode.Confidence ≤ 0.6 |
| T-65-08-04 | M-additive | get_health regression on SC-1 | mitigate | Row A asserts NO semantic_index block (omitempty); SC-1 envelope unchanged |
| T-65-08-05 | M-cold | matrix triggers background indexing | accept | matrix harness is test-only; cold-start unmodified by this plan |
</threat_model>

<verification>
- `go test ./internal/skill/repomap/... ./internal/skill/semantic/... -count=1 -race` PASSES.
- `go test ./internal/skill/semantic/... -run TestE2E_StranglerFig_SourceMatrix -v -count=1` shows ≥6 subtests in output.
- Index-disabled goldens are byte-identical to captured fixture (regression guard).
- All five INTEG requirements exercised by at least one row of the matrix.
- `go build ./...` exits 0; `go vet ./...` exits 0.
</verification>

<success_criteria>
- Phase 65 acceptance gate: every requirement covered by automated test.
- D-04 + Pitfall §3 enforced: cfg.Enabled=false → source=="tree_sitter".
- D-08 + ROADMAP SC #2 enforced: every fallback row caps confidence at ≤ 0.6.
- M-additive enforced: Row A leaves the semantic_index block omitted.
- Tree text byte-identical lock prevents engine drift (INTEG-01 regression guard).
</success_criteria>

<output>
After completion, create `.planning/phases/65-existing-tool-integration-strangler-fig/65-08-SUMMARY.md`.
</output>
