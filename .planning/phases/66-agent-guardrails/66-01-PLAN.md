---
phase: 66
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/guardrails/doc.go
  - internal/guardrails/receipt.go
  - internal/guardrails/receipt_id.go
  - internal/guardrails/receipt_id_test.go
  - internal/guardrails/store.go
  - internal/guardrails/store_test.go
  - internal/guardrails/validate.go
  - internal/guardrails/validate_test.go
  - internal/guardrails/enforcement.go
  - internal/guardrails/enforcement_test.go
  - internal/guardrails/issue_sink.go
  - internal/errors/kinds.go
  - internal/errors/kinds_test.go
  - internal/obs/metrics.go
  - .planning/phases/66-agent-guardrails/66-VALIDATION.md
autonomous: true
requirements: [GUARD-03, GUARD-04, GUARD-05, GUARD-07]
threat_model_severity: high
tags: [guardrails, receipts, mcp]
must_haves:
  truths:
    - "A read-tool success can issue a receipt that survives until TTL or graph_version advance."
    - "A destructive operation with a stale (graph_drift), expired (TTL), wrong-class, wrong-workspace, or wrong-scope receipt is rejected with a typed sentinel error."
    - "Receipts are stored in-memory only (sync.Map per workspace, 10k LRU cap, 30s janitor) — never on disk."
    - "Enforcement level for (profile, tool, rule) resolves via 5-layer precedence (CLI > per-tool > per-rule > profile > global default), highest-precedence-set-wins (NOT most-restrictive)."
    - "guardrail_warned and guardrail_blocked are present in the telemetry outcome enum."
    - "helix_receipt_issued_total{class}, helix_receipt_expired_total{reason}, helix_receipt_lookup_total{outcome}, helix_guardrail_eval_timeout_total exist with closed-enum bounded labels (drop-unknown discipline; the eval_timeout counter is unlabeled per T-66-21 fail-open mitigation)."
  artifacts:
    - path: "internal/guardrails/receipt.go"
      provides: "Receipt struct + closed ReceiptClass enum + 5 typed scope structs (ReferencesCheckedScope, ImpactCheckedScope, ContextGatheredScope, StructuralOverviewScope, DiagnosticsCleanScope) with marker method"
    - path: "internal/guardrails/receipt_id.go"
      provides: "ReceiptID type, NewReceiptID() (rcpt_<base32-26>), ParseReceiptID(string) error"
    - path: "internal/guardrails/store.go"
      provides: "Store with Issue/Get/InvalidateOnGraphVersionAdvance, sync.Map per workspace, 30s janitor, LRU 10k"
    - path: "internal/guardrails/validate.go"
      provides: "ValidateReceiptForOperation + 6 typed sentinels (ErrWrongWorkspace, ErrGraphVersionMismatch, ErrReceiptExpired, ErrReceiptTooStale, ErrWrongReceiptClass, ErrReceiptScopeMismatch)"
    - path: "internal/guardrails/enforcement.go"
      provides: "EnforcementLevel closed enum (off|warn|enforce|require_force) + ResolveGuardrailEnforcement(cli, cfg, profile, tool, rule) function"
    - path: "internal/guardrails/issue_sink.go"
      provides: "atomic.Pointer sink (SetReceiptIssueSink, IssueReceiptOnSuccess) mirroring editOutcomeSink"
    - path: "internal/errors/kinds.go"
      provides: "GuardrailViolation Kind + ErrGuardrailViolation sentinel + NewGuardrailViolation(rule, message, requiredReceipts, suggestedTools, seeAlso) constructor"
    - path: "internal/obs/metrics.go"
      provides: "ReceiptIssuedInc, ReceiptExpiredInc, ReceiptLookupInc with drop-unknown closed-enum gating; GuardrailEvalTimeoutInc unlabeled counter (T-66-21 fail-open audit trail)"
  key_links:
    - from: "internal/guardrails/store.go"
      to: "internal/obs/metrics.go ReceiptIssuedInc/ReceiptExpiredInc/ReceiptLookupInc"
      via: "MetricsSink interface or direct *obs.Metrics handle"
      pattern: "ReceiptIssuedInc|ReceiptExpiredInc|ReceiptLookupInc|GuardrailEvalTimeoutInc"
    - from: "internal/guardrails/validate.go"
      to: "internal/errors typed sentinels"
      via: "errors.Is on returned sentinels"
      pattern: "ErrWrongWorkspace|ErrGraphVersionMismatch|ErrReceiptExpired|ErrReceiptTooStale|ErrWrongReceiptClass|ErrReceiptScopeMismatch"
---

<objective>
Land the foundational guardrails subsystem: receipt model + closed-enum classes + typed scope unions, the in-memory per-workspace receipt store with TTL/janitor/LRU/graph_version invalidation, the validation predicate with 6 typed sentinels, the 5-layer enforcement-precedence resolver, the issue-sink closure, the new `serr.GuardrailViolation` Kind, and bounded-label metric counters.

Purpose: Without this plan, no later wave has the storage, validation, or telemetry primitives to build on. This is pure business logic suitable for RED→GREEN→REFACTOR.

Output: `internal/guardrails/` package skeleton with full receipt + store + validate + enforcement + issue_sink machinery, `internal/errors/kinds.go` extended with GuardrailViolation, `internal/obs/metrics.go` extended with three new bounded-label counters. Compiles standalone (no middleware or rule wiring yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-agent-guardrails/66-CONTEXT.md
@.planning/phases/66-agent-guardrails/66-RESEARCH.md
@.planning/phases/66-agent-guardrails/66-PATTERNS.md
@.planning/phases/66-agent-guardrails/66-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Verbatim anchors required by the executor; do NOT rediscover. -->

From internal/errors/kinds.go (lines 12-22, verified):
```go
type Kind string
const (
    NotFound         Kind = "not_found"
    InvalidArgs      Kind = "invalid_args"
    NoWorkspace      Kind = "no_workspace"
    Unsupported      Kind = "unsupported"
    Internal         Kind = "internal"
    CircuitOpen      Kind = "circuit_open"
    Timeout          Kind = "timeout"
    PermissionDenied Kind = "permission_denied"
)
var (
    ErrNotFound         = &Error{Kind: NotFound}
    ErrInvalidArgs      = &Error{Kind: InvalidArgs}
    /* ... */
)
```
ADD: `GuardrailViolation Kind = "guardrail_violation"` and `ErrGuardrailViolation = &Error{Kind: GuardrailViolation}`. Inspect existing `Error` struct (same file) for fields; add a typed extension struct for guardrail-specific fields if not embeddable, or marshal via Error.Detail map if it exists. Read the file to confirm shape before editing.

From internal/mcp/middleware.go (editOutcomeSink pattern, lines 50-97 verbatim):
```go
var editOutcomeSink atomic.Pointer[func(ctx context.Context, toolName, outcome, strategy string)]

func setEditOutcomeSink(fn func(ctx context.Context, toolName, outcome, strategy string)) {
    editOutcomeSink.Store(&fn)
}

func RecordEditOutcome(ctx context.Context, toolName, outcome, strategy string) {
    p := editOutcomeSink.Load()
    if p == nil || *p == nil {
        return
    }
    (*p)(ctx, toolName, outcome, strategy)
}
```
MIRROR for `internal/guardrails/issue_sink.go` with signature `func(ctx context.Context, class ReceiptClass, scope ReceiptScope, tool string) (ReceiptID, error)`.

From internal/obs/metrics.go (EditOutcomeInc, lines 550-568 verbatim — use this as the drop-unknown analog):
```go
func (m *Metrics) EditOutcomeInc(toolName, outcome, strategy string) {
    switch outcome {
    case "success", "no_match", "ambiguous_match", "validation_failed", "ls_error", "internal":
    default:
        return
    }
    switch strategy {
    case "exact", "whitespace_normalized", "indentation_flexible", "none":
    default:
        return
    }
    m.EditOutcome.WithLabelValues(toolName, outcome, strategy).Inc()
}
```

From internal/semantic/integ/lookup.go (workspace key shape, used in store key):
```go
import "github.com/agenthands/helix/internal/workspace"
// type workspace.WorkspaceKey { RepoRoot string }
// type integ.SymbolID string
```

From RESEARCH §"Receipt struct + closed-enum class" (D-02, D-03 — VERBATIM — locked):
```go
type ReceiptClass string
const (
    ClassReferencesChecked  ReceiptClass = "references_checked"
    ClassImpactChecked      ReceiptClass = "impact_checked"
    ClassContextGathered    ReceiptClass = "context_gathered"
    ClassStructuralOverview ReceiptClass = "structural_overview"
    ClassDiagnosticsClean   ReceiptClass = "diagnostics_clean"
)

type Receipt struct {
    ID              ReceiptID
    Class           ReceiptClass
    SchemaVersion   uint32
    WorkspaceKey    workspace.WorkspaceKey
    SnapshotID      uint64
    GraphVersion    uint64
    Freshness       string  // closed-enum mirror of integ.Freshness values
    ScoreStatus     string
    ClusterStatus   string
    PendingLSPFiles int
    LSPCoverage     float64
    IssuedAt        time.Time
    ExpiresAt       time.Time
    IssuingTool     string
    IssuingCallID   string
    TraceID         string
    Scope           ReceiptScope
}

type ReceiptScope interface{ isReceiptScope() }
type ReferencesCheckedScope struct {
    SymbolID     integ.SymbolID
    RefCount     int
    FilePath     string
    IncludeTests bool
}
func (ReferencesCheckedScope) isReceiptScope() {}
// Plus ImpactCheckedScope, ContextGatheredScope, StructuralOverviewScope, DiagnosticsCleanScope per D-01.
```

From RESEARCH §"Validation Architecture" — receipt ID format (D-07 LOCKED): `rcpt_<UUIDv7-base32-26char>`.
- 26-char body uses base32 with NO padding ("23456789abcdefghijkmnpqrstuvwxyz" or stdlib StdEncoding without padding — picker's discretion within base32 RFC-4648 alphabet).
- UUIDv7 source: `github.com/google/uuid`. Verify presence first: `grep "google/uuid" go.mod`. If absent: `go get github.com/google/uuid@v1.6.0`. The Receipt body is opaque to the agent — only the prefix + length + alphabet are validated by `ParseReceiptID`.

From CONTEXT.md D-20 (LOCKED): 5-layer precedence is `CLI > per-tool > per-rule > profile > global default` with **highest-precedence-set-wins** (NOT most-restrictive). Treat each layer as `(EnforcementLevel, set bool)`; first set layer wins.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→MCP `tools/call` | Agent-controlled receipt IDs and arguments cross into the daemon |
| read-tool handler→Receipt store | Server-issued receipts originate here; bodies must never leak to the agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Spoofing | ReceiptID forgery via brute force | mitigate | UUIDv7 base32-26 = 130 bits entropy (`crypto/rand`); ParseReceiptID validates prefix + length + alphabet; opaque body-store lookup so unknown IDs are rejected with `miss` outcome |
| T-66-02 | Tampering | Receipt replay across graph_version advances | mitigate | `Store.InvalidateOnGraphVersionAdvance(ws, newGV)` deletes all receipts with `GraphVersion < newGV`; `ValidateReceiptForOperation` returns `ErrGraphVersionMismatch` on stale GV |
| T-66-03 | Repudiation | Issuance source unclear in audit | accept | `IssuingTool` + `IssuingCallID` + `TraceID` recorded in receipt; metric counter records issuance class |
| T-66-04 | Information Disclosure | Receipt store error leaks scope contents | mitigate | Sentinels carry no scope payload; `helix_receipt_lookup_total{outcome}` uses closed-enum labels with no source content (drop-unknown gate) |
| T-66-05 | Denial of Service | Unbounded receipt store via issuance flood | mitigate | LRU 10k cap per workspace + 30s janitor + opportunistic-on-lookup expiry (D-06) |
| T-66-06 | Elevation of Privilege | Cross-workspace receipt forgery in multi-workspace daemon | mitigate | `WorkspaceKey` field on receipt; `Get` returns `ErrWrongWorkspace` if `receipt.WorkspaceKey != current` |
| T-66-07 | Tampering | Persistence of receipts to disk would bypass graph_version invalidation | mitigate | In-memory `sync.Map` only; REQUIREMENTS.md line 167 forbids persistence; no DB schema, no file write paths in this plan |
| T-66-08 | TOCTOU | Time-of-check / time-of-use gap between receipt issuance (read-tool success) and consumption (destructive call) | mitigate | Issuance→consumption window is bounded by two independent invariants enforced by Plan 01: (a) graph_version monotonic-advance invalidation via `Store.InvalidateOnGraphVersionAdvance` (umbrella mitigation T-66-02 — any code or workspace state mutation between issuance and consumption advances graph_version, deleting all in-flight receipts with `GraphVersion < newGV`); (b) hard 5-minute TTL via `ExpiresAt` checked opportunistically on every `Get` and swept by the 30s janitor. Window cannot extend beyond `min(graph_version_advance_time, 5m)`. The narrower scope-mismatch sentinels (ErrReceiptScopeMismatch on `validateScope`) prevent a stale-but-still-fresh receipt from being reused on a different symbol/file/path target. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Populate 66-VALIDATION.md per-task verification table</name>
  <files>.planning/phases/66-agent-guardrails/66-VALIDATION.md</files>
  <read_first>
    - .planning/phases/66-agent-guardrails/66-01-PLAN.md (this plan — source of 66-01-T1..T3 task IDs and their <automated> commands)
    - .planning/phases/66-agent-guardrails/66-02-PLAN.md (66-02-T1..T2)
    - .planning/phases/66-agent-guardrails/66-03-PLAN.md (66-03-T1..T3)
    - .planning/phases/66-agent-guardrails/66-04-PLAN.md (66-04-T1..T2)
    - .planning/phases/66-agent-guardrails/66-05-PLAN.md (66-05-T1..T2)
    - .planning/phases/66-agent-guardrails/66-06-PLAN.md (66-06-T1..T3)
    - .planning/phases/66-agent-guardrails/66-VALIDATION.md (current TBD-01 placeholder row to be REPLACED)
  </read_first>
  <action>
    1. Open `.planning/phases/66-agent-guardrails/66-VALIDATION.md`.
    2. REPLACE the single TBD-01 placeholder row in the "Per-Task Verification Map" table with one row per concrete task across all six plans. Source the `<automated>` command for each row by reading each plan's `<verify><automated>...</automated></verify>` block. Required rows (extract task IDs and commands verbatim):
       - 66-01-T1, Plan 01, Wave 1, GUARD-03/04/05/07, T-66-01..08, "Receipt ID + closed-enum + scope union", unit, `go test ./internal/guardrails/ -run "TestParseReceiptID|TestNewReceiptID|TestReceiptClassExhaustive" -count=1 -race && go vet ./internal/guardrails/...`
       - 66-01-T2, Plan 01, Wave 1, GUARD-03/04/05, T-66-02/04/05/06, "Receipt store TTL/janitor/LRU/graph_version", unit, `go test ./internal/guardrails/ -run TestStore -count=1 -race && go vet ./internal/guardrails/... ./internal/obs/...`
       - 66-01-T3, Plan 01, Wave 1, GUARD-04/05/07, T-66-01..06, "Validation predicate + 5-layer enforcement + issue sink + GuardrailViolation Kind", unit, `go test ./internal/guardrails/ -run "TestValidate|TestResolveGuardrailEnforcement|TestParseEnforcementLevel|TestValidateScope_AllClasses" -count=1 -race && go test ./internal/errors/ -count=1 -race && go vet ./internal/guardrails/... ./internal/errors/...`
       - 66-02-T1, Plan 02, Wave 1, (per Plan 02 frontmatter), (per Plan 02 STRIDE), (per Plan 02 task name), (per Plan 02 verify type), `<verbatim Plan 02 Task 1 automated>`
       - 66-02-T2, Plan 02, Wave 1, (per Plan 02), (per Plan 02), (per Plan 02), (per Plan 02), `<verbatim Plan 02 Task 2 automated>`
       - 66-03-T1..T3, Plan 03, Wave 2, (per Plan 03), (per Plan 03), (per Plan 03), (per Plan 03), `<verbatim Plan 03 Task 1..3 automated>`
       - 66-04-T1, Plan 04, Wave 3, GUARD-01/03/04/05/07, T-66-18..23, "GuardrailMiddleware shell + production deps + skill", integration, `go build ./... && go test ./internal/mcp/ -run TestGuardrailMiddleware -count=1 -race && go test ./internal/guardrails/... -count=1 -race && go vet ./...`
       - 66-04-T2, Plan 04, Wave 3, GUARD-01, T-66-18/19/22, "Telemetry outcome enum + LIFO regression + daemon step 14b.5", integration, `go build ./... && go test ./internal/mcp/ -run "TestMiddleware_LIFO|TestGuardrailMiddleware|TestTelemetry_GuardrailOutcome" -count=1 -race && go test ./internal/daemon/ -count=1 -race && go vet ./...`
       - 66-05-T1, Plan 05, Wave 4, GUARD-02/03/05, T-66-24..28, "Read-tool issuance (5 tools)", unit, `go build ./... && go test ./internal/kernel/symbols/ ./internal/kernel/edit/ ./internal/kernel/diag/ -count=1 -race && go vet ./...`
       - 66-05-T2, Plan 05, Wave 4, GUARD-02/03/05, T-66-24..28, "Skill-side issuance + destructive args extension", unit, `go build ./... && go test ./internal/skill/... ./internal/kernel/edit/ ./internal/kernel/fileops/ -count=1 -race && go vet ./...`
       - 66-05-T3 (NEW per revision), Plan 05, Wave 4, GUARD-02/03/05, T-66-24, "Issuance smoke test (8 tools each increment helix_receipt_issued_total)", integration, `go test ./internal/mcp/integ/ -run TestReceiptIssuanceSmoke -count=1 -race`
       - 66-06-T1, Plan 06, Wave 4, GUARD-02/06, T-66-29..32, "GUARDRAILS.md + DoD.md + 6 embedded topic docs", structural, `test -f GUARDRAILS.md && test -f DoD.md && test -d internal/kernel/help/docs && test "$(ls internal/kernel/help/docs/*.md | wc -l | tr -d ' ')" = "6" && grep -q "G-001" GUARDRAILS.md && grep -q "G-005" GUARDRAILS.md && grep -q "Definition of Done" DoD.md && go build ./internal/kernel/help/...`
       - 66-06-T2, Plan 06, Wave 4, GUARD-06, T-66-31, "TopicRegistry refactor on get_tool_help", unit, `go test ./internal/kernel/help/ -count=1 -race && go vet ./internal/kernel/help/...`
       - 66-06-T3, Plan 06, Wave 4, GUARD-02, T-66-30/32, "Final VALIDATION.md consistency check + context-truncation E2E", integration, `go test -tags=integration ./test/harness/ -run TestContextTruncationE2E_GuardrailEnforcedWithoutReceipt -count=1 -timeout 120s`
       For each row, mark `File Exists` as ❌ W0 (not yet built) and `Status` as ⬜ pending. Where `(per Plan 0X)` placeholders appear above, OPEN the referenced PLAN file at execution time and substitute verbatim values.
    3. DELETE the TBD-01 placeholder row.
    4. EDIT frontmatter: change `wave_0_complete: false` → `wave_0_complete: true`. Leave `nyquist_compliant: false` as-is — Plan 06 Task 3 flips it after Wave 1 sampling proves table accuracy.
    5. Verify markdown table renders (no broken pipes; column count consistent across all rows).
  </action>
  <verify>
    <automated>grep -q "wave_0_complete: true" .planning/phases/66-agent-guardrails/66-VALIDATION.md && ! grep -q "TBD-01" .planning/phases/66-agent-guardrails/66-VALIDATION.md && [ "$(grep -cE '^\| 66-0[1-6]-T' .planning/phases/66-agent-guardrails/66-VALIDATION.md)" -ge 14 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TBD-01" .planning/phases/66-agent-guardrails/66-VALIDATION.md` returns 0.
    - `grep -cE "^\| 66-0[1-6]-T" .planning/phases/66-agent-guardrails/66-VALIDATION.md` returns >= 14 (8+ task IDs across the six plans, including 66-05-T3 added in revision).
    - `grep -q "wave_0_complete: true" .planning/phases/66-agent-guardrails/66-VALIDATION.md` matches.
    - `grep -q "nyquist_compliant: false" .planning/phases/66-agent-guardrails/66-VALIDATION.md` still matches (Plan 06 Task 3 flips this).
  </acceptance_criteria>
  <done>VALIDATION.md per-task table populated with concrete task IDs + automated commands; wave_0_complete flipped; TBD-01 placeholder removed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 1: Receipt ID + closed-enum ReceiptClass + typed scope union (RED→GREEN)</name>
  <files>internal/guardrails/doc.go, internal/guardrails/receipt.go, internal/guardrails/receipt_id.go, internal/guardrails/receipt_id_test.go</files>
  <read_first>
    - internal/errors/kinds.go (full file — confirm Error struct shape)
    - internal/semantic/integ/lookup.go lines 95-105 (workspace.WorkspaceKey + integ.SymbolID types)
    - .planning/phases/66-agent-guardrails/66-PATTERNS.md "Pattern C: Closed-Enum + Sentinel Error" + "Pattern H: Closed-Type-Union via Marker Interface"
    - .planning/phases/66-agent-guardrails/66-RESEARCH.md §"Code Examples → Receipt struct + closed-enum class"
    - go.mod (verify whether github.com/google/uuid is already present)
  </read_first>
  <behavior>
    - ParseReceiptID rejects: "" (empty), "rcpt" (no underscore), "rcpt_" (no body), "rcpt_xxx" (too short, not 26 body chars), strings of length ≠ 31 total ("rcpt_" + 26), non-base32 alphabet chars, and prefix other than "rcpt_".
    - ParseReceiptID accepts a known-good 31-char `rcpt_<26 base32 chars>` value.
    - NewReceiptID() returns a value that ParseReceiptID accepts and uses crypto/rand-backed UUIDv7 source.
    - Two consecutive NewReceiptID() calls produce monotonically non-decreasing values when sorted lexicographically (UUIDv7 timestamp prefix property).
    - Each ReceiptScope concrete type (ReferencesCheckedScope, ImpactCheckedScope, ContextGatheredScope, StructuralOverviewScope, DiagnosticsCleanScope) implements isReceiptScope() via a method receiver, and a `var _ ReceiptScope = (*<Scope>)(nil)` compile-time assertion holds.
    - receiptClassEnum has exactly the 5 D-01 values, in declaration order; an exhaustiveness test iterates the enum and confirms each value maps to a concrete scope type.
  </behavior>
  <action>
    1. Verify `github.com/google/uuid` presence: run `grep -q "github.com/google/uuid" go.mod`. If absent, run `go get github.com/google/uuid@v1.6.0` and confirm `go list -m github.com/google/uuid` reports >= v1.6.0.
    2. Create `internal/guardrails/doc.go` with the package comment: "Package guardrails ships server-side capability receipts (Phase 66, GUARD-01..GUARD-07). Middleware install order MUST remain LIFO with LazyInit installed last (executes first); see internal/mcp/lazy_init.go:106-112."
    3. Create `internal/guardrails/receipt.go` per D-02/D-03 (LOCKED) with: ReceiptClass string type + 5 const values verbatim from interfaces block; receiptClassEnum slice; Receipt struct with all 17 fields verbatim; ReceiptScope marker interface; 5 concrete scope structs (ReferencesCheckedScope, ImpactCheckedScope, ContextGatheredScope, StructuralOverviewScope, DiagnosticsCleanScope) using fields per D-01 verbatim. Use `internal/semantic/integ.SymbolID` for symbol fields and `internal/workspace.WorkspaceKey` for WorkspaceKey. ImpactCheckedScope fields: SymbolID, RefCount, PublicAPI bool, BlastNodes int, MaxDepth int, IncludedCallers bool, IncludedTypes bool. ContextGatheredScope fields: FileSet []string, TargetSymbols []integ.SymbolID, TaskHash string, TokenBudgetUsed int, MaxTokens int. StructuralOverviewScope fields: RootPath string, Depth int, FileCount int, MaxTokens int. DiagnosticsCleanScope fields: FileSet []string, DiagnosticCount int, ErrorCount int, WarningCount int, Tool string. Each gets a `func (X) isReceiptScope() {}` line.
    4. Create `internal/guardrails/receipt_id.go`: type `ReceiptID string`; `const receiptIDPrefix = "rcpt_"`; `const receiptIDBodyLen = 26`; `func NewReceiptID() (ReceiptID, error)` using `uuid.NewV7()` then base32-encoding the 16 raw bytes (use `base32.StdEncoding.WithPadding(base32.NoPadding)` — produces 26 chars from 16 bytes); `func ParseReceiptID(s string) (ReceiptID, error)` returning typed errors `ErrInvalidReceiptIDPrefix`, `ErrInvalidReceiptIDLength`, `ErrInvalidReceiptIDAlphabet`. Total ID length is 31 chars (5 prefix + 26 body).
    5. Create table-driven `internal/guardrails/receipt_id_test.go` covering every behavior bullet. Use Go subtests (`t.Run`).
    6. Confirm with `go vet ./internal/guardrails/...` clean.
  </action>
  <verify>
    <automated>go test ./internal/guardrails/ -run "TestParseReceiptID|TestNewReceiptID|TestReceiptClassExhaustive" -count=1 -race && go vet ./internal/guardrails/...</automated>
  </verify>
  <acceptance_criteria>
    - File internal/guardrails/receipt.go exists.
    - File internal/guardrails/receipt_id.go exists.
    - `grep -c "isReceiptScope()" internal/guardrails/receipt.go` returns >= 5 (one method per scope type).
    - `grep -c -E "Class(ReferencesChecked|ImpactChecked|ContextGathered|StructuralOverview|DiagnosticsClean)" internal/guardrails/receipt.go` returns >= 5.
    - `grep -E "^const receiptIDPrefix = \"rcpt_\"" internal/guardrails/receipt_id.go` matches.
    - `go test ./internal/guardrails/ -run "TestParseReceiptID|TestNewReceiptID|TestReceiptClassExhaustive" -count=1` exits 0.
    - `go vet ./internal/guardrails/...` exits 0.
  </acceptance_criteria>
  <done>Receipt model + ID parser are GREEN under -race; closed-enum exhaustiveness test passes.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Receipt store (TTL + janitor + LRU + graph_version invalidation) (RED→GREEN→REFACTOR)</name>
  <files>internal/guardrails/store.go, internal/guardrails/store_test.go, internal/obs/metrics.go</files>
  <read_first>
    - internal/guardrails/receipt.go (just created — confirm Receipt + ReceiptScope shape)
    - internal/obs/metrics.go lines 540-580 (EditOutcomeInc drop-unknown pattern from PATTERNS.md Pattern B)
    - internal/mcp/lazy_init.go lines 38-50 (sync.Map + sync.Mutex shape)
    - .planning/phases/66-agent-guardrails/66-RESEARCH.md §"Receipt store with janitor (D-06)" — full code block
    - .planning/phases/66-agent-guardrails/66-CONTEXT.md D-06 (10k LRU cap, 30s janitor, opportunistic-on-lookup expiry)
  </read_first>
  <behavior>
    - Issue(ws, receipt) stores the receipt and emits ReceiptIssuedInc(class).
    - Get(ws, id) returns (receipt, true) for a fresh entry; (nil, false) + ReceiptLookupInc("miss") for unknown ID; (nil, false) + ReceiptLookupInc("expired") + ReceiptExpiredInc("ttl") for an entry whose ExpiresAt has passed (opportunistic check, even before janitor runs); (nil, false) + ReceiptLookupInc("workspace_mismatch") when ws differs from receipt.WorkspaceKey.
    - InvalidateOnGraphVersionAdvance(ws, newGV) deletes every receipt where receipt.WorkspaceKey == ws AND receipt.GraphVersion < newGV; emits ReceiptExpiredInc("graph_drift") per deletion.
    - LRU eviction: storing the 10001st receipt for a workspace evicts the least-recently-issued; emits ReceiptExpiredInc("lru_evicted").
    - Janitor: a configurable-tick variant (NewStoreWithTicker) sweeps expired entries; under a fake/short ticker the test observes deletion within one tick.
    - Concurrency: Issue and Get from N=100 goroutines is race-clean under `go test -race`; sync.Map plus per-workspace RWMutex for the LRU bookkeeping.
    - Close() stops the janitor and is idempotent.
  </behavior>
  <action>
    1. Add four counters to `internal/obs/metrics.go` mirroring `EditOutcomeInc` (lines 550-568) — drop-unknown switch:
       - `ReceiptIssuedInc(class string)` — closed enum: `references_checked, impact_checked, context_gathered, structural_overview, diagnostics_clean`
       - `ReceiptExpiredInc(reason string)` — closed enum: `ttl, graph_drift, lru_evicted`
       - `ReceiptLookupInc(outcome string)` — closed enum: `hit, miss, expired, scope_mismatch, graph_drift, workspace_mismatch, freshness_rejected, wrong_class`
       - `GuardrailEvalTimeoutInc()` — UNLABELED (no closed enum — just a count) per T-66-21 mitigation; use `prometheus.Counter` (not CounterVec) named `helix_guardrail_eval_timeout_total`. Incremented from Plan 04 `deps_production.Evaluate` timeout fail-open branch.
       The first three methods register a `prometheus.CounterVec` field on `*Metrics` (mirror existing `EditOutcome` field shape; check the existing `Metrics` struct to confirm naming convention) named `helix_receipt_issued_total` / `helix_receipt_expired_total` / `helix_receipt_lookup_total`. The fourth registers a plain `prometheus.Counter` field named `helix_guardrail_eval_timeout_total`. Counter creation goes in the `Metrics` constructor (find existing constructor by `grep -n "func New" internal/obs/metrics.go`).
    2. Create `internal/guardrails/store.go` with `MetricsSink` interface (3 methods: IncIssued, IncExpired, IncLookup) so tests can pass a fake. Production wiring later (Plan 04) will pass an adapter around `*obs.Metrics`. Implement Store per RESEARCH §"Receipt store with janitor" verbatim, plus per-workspace LRU using a `container/list` doubly-linked list keyed off the issuance index (cap=10000, configurable via NewStoreWithCap).
    3. Public API: `NewStore(opts StoreOptions) *Store` + `NewStoreWithTicker(opts StoreOptions, tick *time.Ticker) *Store` (test-only entry; production uses NewStore which constructs a 30s ticker); `Issue(ws workspace.WorkspaceKey, class ReceiptClass, scope ReceiptScope, fields IssueFields) (ReceiptID, error)` where `IssueFields` carries SnapshotID/GraphVersion/Freshness/etc.; `Get(ws, id)`; `InvalidateOnGraphVersionAdvance(ws, newGV)`; `Close()`.
    4. Create `internal/guardrails/store_test.go` with subtests: TestStore_TTLExpiry (advance fake clock past ExpiresAt → Get returns false + expired counter); TestStore_OpportunisticExpiry (set ExpiresAt in the past, Get returns false WITHOUT janitor running); TestStore_WorkspaceMismatch; TestStore_GraphVersionAdvance (issue at GV=42, invalidate to GV=43, Get returns false); TestStore_LRU (cap=4 fixture, 5th issuance evicts oldest); TestStore_Janitor (use a manual ticker channel; send a tick, observe deletion); TestStore_Concurrent (100 goroutines × 100 iterations issue+get).
    5. Use a `func() time.Time` clock injection for testability; default to `time.Now`.
  </action>
  <verify>
    <automated>go test ./internal/guardrails/ -run TestStore -count=1 -race && go vet ./internal/guardrails/... ./internal/obs/...</automated>
  </verify>
  <acceptance_criteria>
    - File internal/guardrails/store.go exists.
    - `grep -c "func (s \*Store)" internal/guardrails/store.go` returns >= 4 (Issue, Get, InvalidateOnGraphVersionAdvance, Close).
    - `grep -E "ReceiptIssuedInc|ReceiptExpiredInc|ReceiptLookupInc|GuardrailEvalTimeoutInc" internal/obs/metrics.go` matches all four.
    - `grep -E "helix_receipt_issued_total|helix_receipt_expired_total|helix_receipt_lookup_total|helix_guardrail_eval_timeout_total" internal/obs/metrics.go` matches all four (prom counter names).
    - `go test ./internal/guardrails/ -run TestStore -count=1 -race` exits 0.
    - `go test ./internal/guardrails/ -run TestStore -count=1 -race -v 2>&1 | grep -c -E "PASS:.*TestStore_"` returns >= 7 (TTLExpiry, OpportunisticExpiry, WorkspaceMismatch, GraphVersionAdvance, LRU, Janitor, Concurrent).
  </acceptance_criteria>
  <done>Store passes all sub-tests under -race; obs metrics drop-unknown gates verified by direct enum-coverage tests.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Validation predicate, 5-layer enforcement resolver, issue sink, GuardrailViolation Kind (RED→GREEN)</name>
  <files>internal/guardrails/validate.go, internal/guardrails/validate_test.go, internal/guardrails/enforcement.go, internal/guardrails/enforcement_test.go, internal/guardrails/issue_sink.go, internal/errors/kinds.go, internal/errors/kinds_test.go</files>
  <read_first>
    - internal/errors/kinds.go (full file — confirm sentinel pattern and Error struct shape)
    - internal/mcp/middleware.go lines 50-97 (editOutcomeSink VERBATIM — issue_sink.go MIRRORS this)
    - internal/mcp/middleware.go lines 240-258 (classifyOutcome ordered-sentinel pattern)
    - internal/kernel/edit/tools.go lines 52-82 (ClassifyEditError ordered-sentinel pattern)
    - internal/guardrails/receipt.go (just created)
    - .planning/phases/66-agent-guardrails/66-CONTEXT.md D-05, D-09, D-20, D-21, D-22 (all LOCKED)
    - .planning/phases/66-agent-guardrails/66-RESEARCH.md §"ValidateReceiptForOperation predicate"
  </read_first>
  <behavior>
    - ValidateReceiptForOperation returns sentinels in this priority order (first error wins): ErrWrongWorkspace > ErrGraphVersionMismatch > ErrReceiptExpired > ErrReceiptTooStale > ErrWrongReceiptClass > ErrReceiptScopeMismatch.
    - For ClassReferencesChecked / ClassImpactChecked: scope.SymbolID != target.SymbolID returns ErrReceiptScopeMismatch.
    - For ClassContextGathered: !FileSetContainsAll(scope.FileSet, target.TouchedFiles) returns ErrReceiptScopeMismatch.
    - For ClassStructuralOverview: !PathIsWithin(target.Path, scope.RootPath) returns ErrReceiptScopeMismatch.
    - For ClassDiagnosticsClean: !FileSetContainsAll(scope.FileSet, target.SecuritySensitiveTouchedFiles) OR scope.ErrorCount != 0 returns ErrReceiptScopeMismatch.
    - 5-class exhaustiveness: a TestValidateScope_AllClasses test iterates receiptClassEnum and confirms validateScope handles each (no default-fallthrough).
    - ResolveGuardrailEnforcement: highest-precedence-set-wins per D-20. Layer order CLI > per-tool > per-rule > profile > global. Each layer is `(EnforcementLevel, set bool)`. Test fixture: `global=enforce`, `rule.G-001=warn`, `tool.fuzzy_edit=unset`, `profile.edit=warn`, `cli=unset` → returns `warn` (per-rule wins because per-tool not set, then per-rule is the next-highest set layer; per-rule G-001=warn wins). Another fixture: `global=warn`, `cli.set=enforce` → returns `enforce`. Another: all unset → returns global.
    - EnforcementLevel closed enum: `LevelOff="off", LevelWarn="warn", LevelEnforce="enforce", LevelRequireForce="require_force"`. ParseEnforcementLevel rejects unknown strings with a typed error.
    - serr.NewGuardrailViolation(rule, message, requiredReceipts, suggestedTools, seeAlso) returns an error with Kind == GuardrailViolation that errors.Is(err, ErrGuardrailViolation) matches; the typed value is recoverable via errors.As to a *GuardrailViolation struct carrying the 5 fields.
    - issue_sink: SetReceiptIssueSink(fn) stores; IssueReceiptOnSuccess(ctx, class, scope, tool) returns "" + nil error if sink unset (test mode); otherwise calls the sink and returns its result. Mirror editOutcomeSink atomic.Pointer pattern verbatim.
  </behavior>
  <action>
    1. Extend `internal/errors/kinds.go`: add `GuardrailViolation Kind = "guardrail_violation"` to const block; add `ErrGuardrailViolation = &Error{Kind: GuardrailViolation}` to var block. Add a typed extension struct `GuardrailViolation` in a new section of the same file with fields `Rule string, Message string, RequiredReceipts []ReceiptClassRef, SuggestedTools []string, SeeAlso []SeeAlsoRef`. Define `ReceiptClassRef = string` (avoid cyclic import; the guardrails package will define a typed alias) and `SeeAlsoRef struct { Tool string; Args map[string]string }`. Provide `func NewGuardrailViolation(rule, message string, required []ReceiptClassRef, suggested []string, seeAlso []SeeAlsoRef) error` returning a `*Error` whose Kind == GuardrailViolation and embedding the GuardrailViolation struct via the existing Error.Detail mechanism (read kinds.go to confirm the embedding path; if Error has no Detail, add a typed field `Guardrail *GuardrailViolation` on Error or define a new error type that wraps Error and matches `errors.Is(err, ErrGuardrailViolation)` via Error.Is logic). Add unit test `internal/errors/kinds_test.go` confirming `errors.Is(NewGuardrailViolation(...), ErrGuardrailViolation)` is true and `errors.As` recovers the typed payload.
    2. Create `internal/guardrails/enforcement.go`: `type EnforcementLevel string` with 4 constants per D-21; `func ParseEnforcementLevel(s string) (EnforcementLevel, error)`; `type EnforcementLayer struct { Level EnforcementLevel; Set bool }`; `func ResolveGuardrailEnforcement(cli, perTool, perRule, profile, global EnforcementLayer) EnforcementLevel` returning the first `Set==true` layer's Level (else global.Level even if Set==false, defaulting to LevelWarn if global not set).
    3. Create `internal/guardrails/enforcement_test.go` with table-driven tests covering all 5 layers and a `TestResolveGuardrailEnforcement_HighestPrecedenceWinsNotMostRestrictive` test that explicitly fails if the implementation uses min/max comparison.
    4. Create `internal/guardrails/validate.go`: `type Target struct { SymbolID integ.SymbolID; Path string; TouchedFiles []string; SecuritySensitiveTouchedFiles []string; RequiredClass ReceiptClass; MinFreshness string }`; six exported sentinels per D-05; `ValidateReceiptForOperation(rcpt *Receipt, target Target, currentGV uint64, currentWS workspace.WorkspaceKey, now time.Time) error` implementing the priority order from RESEARCH §"ValidateReceiptForOperation predicate"; helper `validateScope(class, scope, target) error` with explicit switch and a default-case `panic("unhandled receipt class — closed enum violation")` so tests catch missing classes; helpers `FileSetContainsAll` and `PathIsWithin` (filepath.Rel-based; reject results starting with "..").
    5. Create `internal/guardrails/validate_test.go`: per-class STRICT scope-mismatch test (symbol A receipt rejected for symbol B target), priority-order test that constructs a receipt failing on multiple criteria and confirms ErrWrongWorkspace fires before ErrGraphVersionMismatch fires before ErrReceiptExpired etc., TestValidateScope_AllClasses iterating receiptClassEnum.
    6. Create `internal/guardrails/issue_sink.go` MIRRORING editOutcomeSink verbatim with signature `func(ctx context.Context, class ReceiptClass, scope ReceiptScope, tool string) (ReceiptID, error)`. Provide `SetReceiptIssueSink`, `IssueReceiptOnSuccess` (returns "" + nil if sink unset). Add a paired `SetReceiptIssueSinkForTest(fn)` + a t.Cleanup-friendly nil-restoring helper (mirror SetEditOutcomeSinkForTest discipline noted at middleware.go:88-93).
  </action>
  <verify>
    <automated>go test ./internal/guardrails/ -run "TestValidate|TestResolveGuardrailEnforcement|TestParseEnforcementLevel|TestValidateScope_AllClasses" -count=1 -race && go test ./internal/errors/ -count=1 -race && go vet ./internal/guardrails/... ./internal/errors/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "GuardrailViolation +Kind = \"guardrail_violation\"" internal/errors/kinds.go` matches.
    - `grep -E "ErrGuardrailViolation +=" internal/errors/kinds.go` matches.
    - `grep -E "func NewGuardrailViolation\(" internal/errors/kinds.go` matches.
    - `grep -c "func (s \*Store)" internal/guardrails/store.go` still >= 4 (no regression from Task 2).
    - `grep -E "ErrWrongWorkspace|ErrGraphVersionMismatch|ErrReceiptExpired|ErrReceiptTooStale|ErrWrongReceiptClass|ErrReceiptScopeMismatch" internal/guardrails/validate.go` matches all six.
    - `grep -E "LevelOff|LevelWarn|LevelEnforce|LevelRequireForce" internal/guardrails/enforcement.go` matches all four.
    - `grep -E "var receiptIssueSink atomic.Pointer" internal/guardrails/issue_sink.go` matches.
    - `go test ./internal/guardrails/ -run "TestValidate|TestResolveGuardrailEnforcement|TestParseEnforcementLevel|TestValidateScope_AllClasses" -count=1 -race` exits 0.
    - `go test ./internal/errors/ -count=1 -race` exits 0.
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>Validation, enforcement, issue-sink, and serr.GuardrailViolation are GREEN under -race with full sentinel-priority coverage.</done>
</task>

</tasks>

<verification>
- `go test ./internal/guardrails/... ./internal/errors/... ./internal/obs/... -count=1 -race` is green.
- `go vet ./...` is clean.
- No file outside `files_modified` is changed.
- `go test ./internal/guardrails/ -run "Test" -count=1 -race -v 2>&1 | grep -c "PASS"` returns the expected count (sub-tests across all three tasks).
</verification>

<success_criteria>
- Foundation primitives compile and pass under `-race`.
- Closed-enum + bounded-label discipline preserved (PROM counter names verified by grep).
- 5-layer precedence resolver returns highest-precedence-set, NOT most-restrictive.
- All 6 typed sentinels are reachable via `errors.Is` from a constructed `ValidateReceiptForOperation` failure.
- Receipt store survives 100×100 concurrent issue+get under `-race` without data races.
</success_criteria>

<output>
After completion, create `.planning/phases/66-agent-guardrails/66-01-SUMMARY.md` summarizing receipt model, store, validate, enforcement, sink, and metric counters with file paths and test names.
</output>
