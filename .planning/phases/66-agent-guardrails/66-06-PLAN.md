---
phase: 66
plan: 06
type: execute
wave: 4
depends_on: [66-04]
files_modified:
  - GUARDRAILS.md
  - DoD.md
  - internal/kernel/help/topics.go
  - internal/kernel/help/topics_test.go
  - internal/kernel/help/tools.go
  - internal/kernel/help/embed.go
  - internal/kernel/help/docs/guardrails.md
  - internal/kernel/help/docs/dod.md
  - internal/kernel/help/docs/workflow_rename.md
  - internal/kernel/help/docs/workflow_delete.md
  - internal/kernel/help/docs/workflow_large_edit.md
  - internal/kernel/help/docs/workflow_security_sensitive_edit.md
  - test/harness/context_truncation_test.go
autonomous: true
requirements: [GUARD-02, GUARD-06]
threat_model_severity: medium
tags: [guardrails, docs, get_tool_help, e2e-test]
must_haves:
  truths:
    - "GUARDRAILS.md exists at repo root with sections for: rule semantics (one section per G-001..G-005), config reference (full D-22 keys with defaults), enforcement-precedence runbook (highest-precedence-set-wins, NOT most-restrictive), telemetry interpretation, profile-defaults table, known limitations (require_force == enforce until override transport, Java/Rust/C# G-005 catalogs deferred)."
    - "DoD.md exists at repo root with per-task-class checklists for rename, delete, public-API-change, large-edit, security-sensitive-edit; each lists the canonical tool sequence (e.g., find_references → receipt issued → rename_symbol{receipts:[...]} → verify_edit)."
    - "get_tool_help accepts an optional `topic` argument (in addition to the existing `tool_name` arg) and dispatches to a TopicRegistry for topics: guardrails, dod, workflow:rename, workflow:delete, workflow:large-edit, workflow:security-sensitive-edit. Exact-match dispatch only (D-24 + RESEARCH OI-04 — prefix dispatch deferred)."
    - "TopicRegistry loads markdown content via embed.FS at daemon start; 6 registered topics."
    - "An end-to-end test (test/harness/context_truncation_test.go) drives forwarder→daemon, calls find_references, deletes the read-tool response from the agent-side message log, and invokes a destructive tool with a fabricated receipts:[] — the daemon refuses (in enforce mode) without a server-side receipt match. This satisfies GUARD-02 success criterion #2."
  artifacts:
    - path: "GUARDRAILS.md"
      provides: "Repo-root operator/user docs for guardrail semantics, config, runbook"
    - path: "DoD.md"
      provides: "Repo-root agent-consumable Definition-of-Done checklists"
    - path: "internal/kernel/help/topics.go"
      provides: "TopicRegistry (Get(name) (*Topic, bool)) + Register helper + 6 default topic registrations from embed.FS"
    - path: "internal/kernel/help/embed.go"
      provides: "//go:embed docs/*.md content for topic dispatch"
    - path: "test/harness/context_truncation_test.go"
      provides: "GUARD-02 SC-2 E2E test driving forwarder→daemon with simulated context wipe"
  key_links:
    - from: "internal/kernel/help/tools.go get_tool_help handler"
      to: "internal/kernel/help/topics.go TopicRegistry"
      via: "args.Topic dispatch when args.ToolName is empty"
      pattern: "topics.Get\\(args.Topic\\)"
    - from: "test/harness/context_truncation_test.go"
      to: "internal/mcp/guardrail_middleware.go enforcement path"
      via: "stdio forwarder → daemon → blocked tool call"
      pattern: "guardrail_violation|guardrail_blocked"
---

<objective>
Ship the operator-facing GUARDRAILS.md and agent-facing DoD.md, plug 6 doc topics into get_tool_help, and prove ID-only forwarding survives context compaction via an end-to-end test driven through the stdio forwarder + daemon (NOT in-process — per RESEARCH Pitfall 8 + memory feedback `feedback_uat_no_manual_mcp.md`).

Purpose: GUARD-06 demands the docs are committed and reachable; GUARD-02 success criterion #2 demands the context-truncation E2E test exists and passes; the test is the single most important integration check in this phase because it proves the threat model holds.

Output: 2 root-level markdown files, 6 embedded topic snippets, a get_tool_help topic-registry refactor (additive — existing tool_name dispatch preserved), and one E2E test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-agent-guardrails/66-CONTEXT.md
@.planning/phases/66-agent-guardrails/66-RESEARCH.md
@.planning/phases/66-agent-guardrails/66-PATTERNS.md
@.planning/phases/66-agent-guardrails/66-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- D-23 LOCKED dual-doc audience split: -->
- GUARDRAILS.md is OPERATOR-facing: per-rule semantics, config keys, enforcement precedence, runbook, telemetry, profile defaults, known limitations.
- DoD.md is AGENT-consumable: per-task-class checklists with canonical tool sequences.

<!-- D-24 LOCKED 6 topics for get_tool_help: -->
1. `guardrails` → full GUARDRAILS.md
2. `dod` → full DoD.md
3. `workflow:rename` → DoD.md "Rename" section
4. `workflow:delete` → DoD.md "Delete" section
5. `workflow:large-edit` → DoD.md "Large Edit" section
6. `workflow:security-sensitive-edit` → DoD.md "Security-Sensitive Edit" section

<!-- OI-04 RESOLUTION (per RESEARCH §OI-04 + Pattern 5): exact-match-only registry; prefix dispatch deferred. Add an optional `topic` field to the get_tool_help args; if set, dispatch to TopicRegistry; if `tool_name` is set, dispatch to existing ToolRegistry path. Existing args + tools/list shape preserved. -->

<!-- D-12 envelope shape for guardrail_violation (mirrors Phase 22 typed-error envelope): -->
```json
{
  "ok": false,
  "error": {
    "kind": "guardrail_violation",
    "rule": "G-003",
    "message": "...",
    "required_receipts": [{"class":"impact_checked","scope_hint":"analyze_blast_radius on AuthMiddleware"}],
    "suggested_tools": ["analyze_blast_radius"],
    "see_also": [{"tool":"get_tool_help","args":{"topic":"workflow:rename"}}]
  }
}
```

<!-- E2E test pattern (per RESEARCH Pitfall 8): drive through stdio forwarder + gRPC daemon. Locate existing test/harness/Runner via `grep -rn "type Runner\|test/harness" test/`. If the harness exists, mirror an existing forwarder-driven test; if not, the test ships as a single self-contained binary-driven test launching `helix daemon` as subprocess and the forwarder as a child of the test process. Memory feedback: NEVER ask the user to run `helix daemon` or MCP tool calls manually — the test must drive everything. -->

<!-- Profile defaults table for GUARDRAILS.md (D-11 LOCKED): -->
| Profile | Default Enforcement |
|---------|---------------------|
| claude-code | warn |
| codex | warn |
| ide-assistant | warn |
| full | warn |
| ci-bot | enforce |

<!-- D-22 config keys for GUARDRAILS.md "Configuration Reference": full key tree with defaults verbatim from Plan 02 internal/config/defaults.go. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedded markdown → agent context | Operator-trusted documentation surfaces to agents at runtime |
| E2E test harness → daemon subprocess | Test driver controls a real daemon process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-29 | Information Disclosure | GUARDRAILS.md leaks security-sensitive package patterns to public docs | accept | The catalog is operator-visible by D-22 design; no security-by-obscurity |
| T-66-30 | Tampering | E2E test passes locally but in-process — does not actually test forwarder→daemon hop | mitigate | Test launches `helix daemon` as subprocess and connects via stdio forwarder; assertion is on the JSON-RPC response from the daemon, not an in-process function call |
| T-66-31 | Spoofing | Topic registry serves arbitrary on-disk markdown via path traversal in `topic` arg | mitigate | Exact-match registry — no filesystem path concatenation; topic strings are looked up in a fixed map |
| T-66-32 | Repudiation | Test harness leaves orphan daemon processes | mitigate | t.Cleanup terminates the subprocess; SIGTERM with 2s grace then SIGKILL; test prints process ID for forensics on failure |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author GUARDRAILS.md + DoD.md + 6 embedded topic snippets</name>
  <files>GUARDRAILS.md, DoD.md, internal/kernel/help/docs/guardrails.md, internal/kernel/help/docs/dod.md, internal/kernel/help/docs/workflow_rename.md, internal/kernel/help/docs/workflow_delete.md, internal/kernel/help/docs/workflow_large_edit.md, internal/kernel/help/docs/workflow_security_sensitive_edit.md, internal/kernel/help/embed.go</files>
  <read_first>
    - .planning/phases/66-agent-guardrails/66-CONTEXT.md D-13..D-17, D-22, D-23, D-11 (LOCKED rule triggers, config shape, doc audience split, profile defaults)
    - .planning/phases/66-agent-guardrails/66-RESEARCH.md §"Pitfall 6" (require_force == enforce known limitation)
    - .planning/phases/66-agent-guardrails/66-RESEARCH.md §"Per-Rule Detection" table
    - internal/profile/embed.go (verbatim //go:embed pattern to mirror in internal/kernel/help/embed.go)
    - internal/profile/profiles/ci-bot.yaml (the only YAML carrying enforcement=enforce)
    - .planning/phases/66-agent-guardrails/66-PATTERNS.md "GUARDRAILS.md / DoD.md content shape" (No Analog Found row — use D-23 outline)
  </read_first>
  <action>
    1. Create `GUARDRAILS.md` at repo root with these top-level sections (use H1/H2 markdown):
       - **Overview** — One-paragraph charter: server-side capability receipts that survive context compaction; pointer to v1.10-ROADMAP.md Phase 66 entry.
       - **The Five Rules** — One H2 per rule (G-001..G-005). Each section has subsections: *Trigger*, *Required Receipt*, *Default Enforcement*, *Example Valid Sequence* (e.g., "find_references on Foo → rename_symbol{receipts:[rcpt_...]} → verify_edit"), *Example Rejected Sequence* (e.g., "rename_symbol with no receipts").
       - **Receipts** — Lifecycle: issued by 7 read/diagnostics tools on success; ID-only forwarding; 5-min TTL; auto-invalidated on graph_version advance; LRU 10k per workspace; lost on daemon restart (acceptable — re-gather is cheap).
       - **Configuration Reference** — Verbatim D-22 yaml shape with EVERY default value from internal/config/defaults.go: `guardrails.enforcement`, `guardrails.receipt_ttl`, `guardrails.rules.G-001..G-005.enforcement`, `guardrails.tools.<5>.enforcement`, `guardrails.G-004.{max_changed_lines, max_files, max_file_change_ratio, enable_ratio_trigger}`, `guardrails.G-005.{path_globs, identifier_patterns, import_patterns.<lang>}`. Include the 4-layer config precedence reminder (CLI > project > user > profile defaults).
       - **Enforcement Precedence** — Explicit example showing `global=enforce, rules.G-001=warn, profile.edit=warn` resolves G-001 to **warn** because per-rule is the highest-precedence layer that is set, NOT the most restrictive. Cross-link to D-20.
       - **Profile Defaults Table** — verbatim from interfaces block.
       - **Telemetry** — `outcome` enum extension (`guardrail_warned`, `guardrail_blocked`); 3 new counters (`helix_receipt_issued_total{class}`, `helix_receipt_expired_total{reason}`, `helix_receipt_lookup_total{outcome}`) with bounded-label values.
       - **Runbook** — "My agent is being blocked, what now?" with the canonical fix sequences ("call analyze_blast_radius first; pass the receipt id"; "use rename_symbol instead of fuzzy_edit for renames"; "lower enforcement via profile or per-rule override").
       - **Known Limitations** — `require_force` currently behaves identically to `enforce` (override transport deferred); Java/Rust/C# G-005 catalogs deferred (eval-corpus signal will determine v1.10.x inclusion); receipts lost on daemon restart by design.
       - **Forbidden Patterns** — Persistent receipts (REQUIREMENTS.md line 167); class-only receipt match (D-09); client-stored receipts.
    2. Create `DoD.md` at repo root with these H1 sections (each is the agent's task-class checklist):
       - **Definition of Done — Rename** — checklist: (1) call `find_references` on the symbol; (2) confirm the response carries `_receipt_id`; (3) call `rename_symbol{path, line, column, new_name, receipts:[<id>]}`; (4) call `verify_edit`. Include canonical example JSON-RPC turn-by-turn snippet.
       - **Definition of Done — Delete** — checklist: (1) for `safe_delete_symbol`: call `find_references` AND/OR `analyze_blast_radius`; (2) for `delete_file` with exported symbols: call `analyze_blast_radius` for each exported symbol in the file; (3) call delete with `receipts:[...]`; (4) `verify_edit`.
       - **Definition of Done — Public API Change** — checklist: (1) call `analyze_blast_radius` (NOT just `find_references`); (2) issue `replace_symbol_body` or `rename_symbol` with `receipts:[<impact_checked_id>]`; (3) `verify_edit`.
       - **Definition of Done — Large Edit** — checklist: (1) call `get_context` for single-file or `get_repo_map` for multi-file; (2) issue the destructive call with `receipts:[<context_or_overview_id>]`; (3) `verify_edit`.
       - **Definition of Done — Security-Sensitive Edit** — checklist: (1) call `get_context` covering all touched files; (2) issue the destructive call with `receipts:[<context_id>]`; (3) call `verify_edit` to obtain `diagnostics_clean` receipt; (4) report ErrorCount==0 in handoff.
    3. Create `internal/kernel/help/docs/` directory with 6 markdown files:
       - `guardrails.md` — symlink-equivalent: COPY of GUARDRAILS.md (the embed cannot reach repo root from internal/; copy and add a one-line note "Synced from /GUARDRAILS.md; do not edit directly — edit the root copy and re-run `make sync-docs`" — Plan 06 ships the copies; future automation in Phase 66.x can add a Makefile target).
       - `dod.md` — COPY of DoD.md.
       - `workflow_rename.md` — JUST the "Definition of Done — Rename" section, extracted standalone with intro line "Canonical Helix workflow for symbol rename."
       - `workflow_delete.md` — JUST the "Definition of Done — Delete" section.
       - `workflow_large_edit.md` — JUST the "Definition of Done — Large Edit" section.
       - `workflow_security_sensitive_edit.md` — JUST the "Definition of Done — Security-Sensitive Edit" section.
    4. Create `internal/kernel/help/embed.go`:
       ```go
       package help
       import "embed"
       //go:embed docs/*.md
       var EmbeddedTopicDocs embed.FS
       ```
       (Place adjacent to existing help package files; do NOT collide with any existing embed.go in the package.)
  </action>
  <verify>
    <automated>test -f GUARDRAILS.md && test -f DoD.md && test -d internal/kernel/help/docs && test "$(ls internal/kernel/help/docs/*.md | wc -l | tr -d ' ')" = "6" && grep -q "G-001" GUARDRAILS.md && grep -q "G-005" GUARDRAILS.md && grep -q "Definition of Done" DoD.md && go build ./internal/kernel/help/...</automated>
  </verify>
  <acceptance_criteria>
    - File `GUARDRAILS.md` exists at repo root.
    - File `DoD.md` exists at repo root.
    - `grep -c "^## " GUARDRAILS.md` returns >= 7 (sections: Overview is H1; Five Rules has 5 H2 + Receipts/Config/Precedence/Profile Defaults/Telemetry/Runbook/Known Limitations/Forbidden = at least 7 H2 headings).
    - `grep -E "G-001|G-002|G-003|G-004|G-005" GUARDRAILS.md | wc -l` returns >= 5.
    - `grep -q "highest-precedence" GUARDRAILS.md` returns 0 (case-insensitive search confirms precedence wording).
    - `grep -q "require_force.*enforce\|require_force.*identical" GUARDRAILS.md` matches (Known Limitations).
    - `grep -c "Definition of Done" DoD.md` returns >= 5.
    - `grep -q "find_references" DoD.md && grep -q "rename_symbol" DoD.md && grep -q "analyze_blast_radius" DoD.md && grep -q "verify_edit" DoD.md` matches (canonical tool names appear in checklists).
    - `ls internal/kernel/help/docs/*.md | wc -l` returns 6.
    - `grep -E "^//go:embed docs/\*\.md" internal/kernel/help/embed.go` matches.
    - `go build ./internal/kernel/help/...` exits 0.
  </acceptance_criteria>
  <done>Two repo-root docs + 6 embedded topic markdowns committed; embed.FS declaration ships.</done>
</task>

<task type="auto">
  <name>Task 2: TopicRegistry refactor on get_tool_help (additive — preserve tool_name dispatch)</name>
  <files>internal/kernel/help/topics.go, internal/kernel/help/topics_test.go, internal/kernel/help/tools.go</files>
  <read_first>
    - internal/kernel/help/tools.go (full file — current dispatch on args.ToolName via server.Registry())
    - internal/kernel/help/skill_adapter.go (full file)
    - internal/kernel/help/embed.go (just created)
    - .planning/phases/66-agent-guardrails/66-RESEARCH.md §"Pattern 5: Topic registry refactor for get_tool_help (OI-04 resolution)"
    - .planning/phases/66-agent-guardrails/66-PATTERNS.md "internal/kernel/help/tools.go" row
  </read_first>
  <action>
    1. Create `internal/kernel/help/topics.go`:
       ```go
       package help

       import (
           "embed"
           "fmt"
           "io/fs"
           "sync"
       )

       type Topic struct {
           Name    string
           Content string
       }

       type TopicRegistry struct {
           mu     sync.RWMutex
           topics map[string]*Topic
       }

       func NewTopicRegistry() *TopicRegistry {
           return &TopicRegistry{topics: make(map[string]*Topic)}
       }

       func (r *TopicRegistry) Register(name string, content string) {
           r.mu.Lock()
           defer r.mu.Unlock()
           r.topics[name] = &Topic{Name: name, Content: content}
       }

       func (r *TopicRegistry) Get(name string) (*Topic, bool) {
           r.mu.RLock()
           defer r.mu.RUnlock()
           t, ok := r.topics[name]
           return t, ok
       }

       func (r *TopicRegistry) Names() []string { /* sorted */ }

       // LoadDefaults populates the 6 Phase 66 D-24 topics from the embedded docs FS.
       func (r *TopicRegistry) LoadDefaults(fsys embed.FS) error {
           mapping := map[string]string{
               "guardrails":                       "docs/guardrails.md",
               "dod":                              "docs/dod.md",
               "workflow:rename":                  "docs/workflow_rename.md",
               "workflow:delete":                  "docs/workflow_delete.md",
               "workflow:large-edit":              "docs/workflow_large_edit.md",
               "workflow:security-sensitive-edit": "docs/workflow_security_sensitive_edit.md",
           }
           for topic, path := range mapping {
               b, err := fs.ReadFile(fsys, path)
               if err != nil {
                   return fmt.Errorf("topic %q load %q: %w", topic, path, err)
               }
               r.Register(topic, string(b))
           }
           return nil
       }
       ```
    2. Create `internal/kernel/help/topics_test.go`:
       - TestTopicRegistry_Register_GetExactMatch
       - TestTopicRegistry_LoadDefaults_All6Present (iterate the 6 topic names; assert each Get returns non-empty Content with len > 100)
       - TestTopicRegistry_Get_UnknownReturnsFalse
    3. EDIT `internal/kernel/help/tools.go` get_tool_help args + handler:
       - ADD an optional `Topic string \`json:"topic,omitempty" jsonschema:"Documentation topic name (alternative to tool_name): guardrails, dod, workflow:rename, workflow:delete, workflow:large-edit, workflow:security-sensitive-edit"\`` field to the args struct.
       - In the handler: if `args.Topic != ""`, look up via TopicRegistry; on hit, return the Content as the tool result text; on miss, return a typed `serr.NotFound` error with a list of available topics. If `args.Topic == ""` and `args.ToolName == ""`, return InvalidArgs ("specify tool_name or topic"). If both are set, prefer Topic with a warning in the result.
       - Inject a `*TopicRegistry` into the help skill — the easiest path: add a package-level singleton `var defaultTopics = NewTopicRegistry()` initialized via `func init() { _ = defaultTopics.LoadDefaults(EmbeddedTopicDocs) }`. The handler calls `defaultTopics.Get(args.Topic)`.
    4. Verify the existing `tool_name` dispatch path is untouched — run the existing help tests with `go test ./internal/kernel/help/...`.
  </action>
  <verify>
    <automated>go test ./internal/kernel/help/ -count=1 -race && go vet ./internal/kernel/help/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "type TopicRegistry struct" internal/kernel/help/topics.go` matches.
    - `grep -E "LoadDefaults\(fsys embed.FS\) error" internal/kernel/help/topics.go` matches.
    - `grep -c -E "\"guardrails\"|\"dod\"|\"workflow:rename\"|\"workflow:delete\"|\"workflow:large-edit\"|\"workflow:security-sensitive-edit\"" internal/kernel/help/topics.go` returns >= 6.
    - `grep -E "Topic\s+string.*topic,omitempty" internal/kernel/help/tools.go` matches.
    - `go test ./internal/kernel/help/ -run "TestTopicRegistry_Register_GetExactMatch|TestTopicRegistry_LoadDefaults_All6Present|TestTopicRegistry_Get_UnknownReturnsFalse" -count=1 -race` exits 0.
    - `go test ./internal/kernel/help/... -count=1 -race` exits 0 (no regression in the existing tool_name dispatch tests).
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <done>get_tool_help additive Topic field works; 6 default topics loaded at init; existing tool_name dispatch preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Context-truncation E2E test (forwarder → daemon, simulated context wipe)</name>
  <files>test/harness/context_truncation_test.go</files>
  <read_first>
    - test/harness/ directory listing: `ls test/harness/`
    - Any existing test/harness/Runner: `grep -rn "type Runner\|func.*Runner" test/harness/`
    - cmd/helix/main.go (entry point; daemon subprocess invocation pattern)
    - internal/forwarder/ files for the stdio→gRPC dial pattern (the test reuses this transport)
    - .planning/phases/66-agent-guardrails/66-RESEARCH.md §"Pitfall 8: Context-truncation E2E test harness drift" (drives the test design)
    - .planning/phases/66-agent-guardrails/66-CONTEXT.md "Specifics" — `test/harness/Runner` pattern hint
    - The user-memory feedback notes (in the system context block): "UAT must automate daemon/MCP checks" — the test MUST drive everything; no manual MCP invocation by the user.
  </read_first>
  <action>
    1. Locate the existing test harness pattern via `find test -name '*.go' | head -20` and `grep -rn "exec.Command.*helix\|forwarder" test/`. If there is a reusable Runner/Driver, build on it; otherwise, the test self-contains the bootstrap.
    2. Create `test/harness/context_truncation_test.go` with build tag `//go:build integration` (so the standard `go test ./...` does not run it; CI runs `go test -tags=integration`). Test name: `TestContextTruncationE2E_GuardrailEnforcedWithoutReceipt`.
    3. Test flow (HIGH LEVEL — implementer adapts to the existing harness shape):
       a. Build `helix daemon` binary if not present (check `bin/helix` or use `go run ./cmd/helix daemon` in a subprocess); use `t.TempDir()` for an isolated workspace.
       b. Initialize a tiny Go fixture file in the temp dir: a single file `auth.go` with a `func AuthMiddleware(...)` symbol so the rules have something to fire on.
       c. Set the daemon config to `enforce` for G-001..G-005 (write a `.helix/project.yml` in the temp dir with `semantic_index.guardrails.enforcement: enforce`).
       d. Launch `helix daemon` as a subprocess; t.Cleanup terminates with SIGTERM (2s grace) then SIGKILL.
       e. Connect via stdio forwarder: launch `helix` (forwarder mode) as a child process; pipe its stdin/stdout to the test as a bidirectional JSON-RPC channel.
       f. Send `initialize` per MCP; assert success.
       g. Send `tools/call` for `find_references` on `AuthMiddleware`; capture the response and the `_receipt_id` field from the result (per Plan 05 OI-05 Item 3).
       h. **Simulate context compaction:** discard the response. Do NOT pass the receipt ID.
       i. Send `tools/call` for `rename_symbol` on `AuthMiddleware` with `receipts: []` (fabricated empty list, simulating an agent that lost the receipt).
       j. Assert the response is an MCP error with content matching `kind: "guardrail_violation"` AND `rule: "G-001"` (or "G-003", whichever fires first in the evaluator's deterministic order — accept either via OR-match) AND that the suggestedTools array contains a hint to call `find_references` or `analyze_blast_radius`.
       k. Send the same `rename_symbol` again, this time with the previously-captured receipt ID. Assert success (the operation completes — the receipt was server-side and matches by ID).
    4. The test MUST automate every daemon/MCP interaction; no manual MCP invocations. Failures dump the daemon's stderr to the test log.
    5. Document at the top of the file (file-level comment) that this is the GUARD-02 SC-2 satisfier and references the user-memory directive `feedback_uat_no_manual_mcp.md`.
  </action>
  <verify>
    <automated>go test -tags=integration ./test/harness/ -run TestContextTruncationE2E_GuardrailEnforcedWithoutReceipt -count=1 -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - File `test/harness/context_truncation_test.go` exists.
    - `head -1 test/harness/context_truncation_test.go` matches `//go:build integration` (the test is gated so CI must opt in).
    - `grep -E "TestContextTruncationE2E_GuardrailEnforcedWithoutReceipt" test/harness/context_truncation_test.go` matches.
    - `grep -E "guardrail_violation" test/harness/context_truncation_test.go` matches (assertion).
    - `grep -E "exec\.Command\|exec\.CommandContext\|os/exec" test/harness/context_truncation_test.go` matches (subprocess driving — proves it is NOT in-process).
    - `grep -q "feedback_uat_no_manual_mcp" test/harness/context_truncation_test.go` matches (file-level reference to the user-memory feedback).
    - `go test -tags=integration ./test/harness/ -run TestContextTruncationE2E_GuardrailEnforcedWithoutReceipt -count=1 -timeout 120s` exits 0 (the integration test passes when run with the build tag).
    - `go vet ./test/harness/...` exits 0.
  </acceptance_criteria>
  <done>GUARD-02 SC-2 E2E test passes with the simulated context-wipe + receipt-replay sequence; subprocess-driven (forwarder + daemon), not in-process.</done>
</task>

</tasks>

<verification>
- `test -f GUARDRAILS.md && test -f DoD.md` returns true.
- `go test ./internal/kernel/help/... -count=1 -race` is green (TopicRegistry tests + existing tool_name tests).
- `go test -tags=integration ./test/harness/ -run TestContextTruncationE2E -count=1 -timeout 120s` is green.
- `go vet ./...` is clean.
- The 6 topic names register at daemon init; `get_tool_help{topic:"workflow:rename"}` returns canonical rename DoD.
</verification>

<success_criteria>
- GUARD-06 satisfied: GUARDRAILS.md and DoD.md are committed and explain rule semantics + completion criteria.
- GUARD-02 SC-2 satisfied: ID-only forwarding survives compaction (E2E test enforces this).
- get_tool_help carries 6 new topics, exact-match dispatch.
- Existing get_tool_help tool_name dispatch preserved (zero regression).
- E2E test drives forwarder→daemon end-to-end without any manual user steps.
</success_criteria>

<output>
After completion, create `.planning/phases/66-agent-guardrails/66-06-SUMMARY.md` summarizing the docs structure, topic registry refactor, and E2E test design with verbatim test names + grep anchors. Also update `.planning/phases/66-agent-guardrails/66-VALIDATION.md` to mark all task IDs `66-01-T1..66-06-T3` as wired and set `nyquist_compliant: true`.
</output>
