---
phase: 68-precise-filefactdiff-populator
plan: 05
type: tdd
wave: 3
depends_on: [68-04]
files_modified:
  - internal/semantic/live/handler/handler_diff_e2e_test.go
  - .planning/deferred-items.md
autonomous: true
requirements: [DIFF-03]
nyquist_compliant: true
tags: [e2e, live-handler, integration, race-clean]
must_haves:
  truths:
    - "End-to-end test seeds a real *Store + real extract.Registry, dispatches a single live edit, and asserts RecordSymbolChanged fires exactly once"
    - "ApplyRepair is called with non-empty GraphRepair (DirtyNodes non-empty)"
    - "Outcome metric records tier=\"full\""
    - "Test runs race-clean under go test -race -count=1"
    - "DEF-67-F01-FULL-DIFF marked resolved in .planning/deferred-items.md"
  artifacts:
    - path: internal/semantic/live/handler/handler_diff_e2e_test.go
      provides: "End-to-end test proving DIFF-03 contract"
      contains: "TestE2E_LiveEditFiresPreciseDiff"
    - path: .planning/deferred-items.md
      provides: "DEF-67-F01-FULL-DIFF flipped to resolved with Phase 68 backlink"
      contains: "DEF-67-F01-FULL-DIFF"
  key_links:
    - from: internal/semantic/live/handler/handler_diff_e2e_test.go
      to: internal/semantic/live/handler/recorder_test.go
      via: "Reuses recordingRankApplier + slogLoggerAdapter scaffolding"
      pattern: "recordingRankApplier"
    - from: internal/semantic/live/handler/handler_diff_e2e_test.go
      to: internal/semantic/store/filefact_accessor.go
      via: "Real *Store on tmpdir DuckDB; populator hits the real GetLatestFileFact"
      pattern: "GetLatestFileFact"
---

<objective>
Ship the end-to-end test that proves DIFF-03 against the real production path:
real `*Store` (DuckDB on tmpdir), real `extract.Registry` (Go provider), real
`Handler.populateRecorderForFile` → `tryFullDiff` → `diffSymbols`. NO mocked
populator, NO LSP, NO fsnotify (Pitfall 5).

Also flips `DEF-67-F01-FULL-DIFF` to resolved in `.planning/deferred-items.md`
because Phase 68 closes the Tier-1/Tier-2 scaffolding-only debt left by the
62-09 close-out (D-11).

Purpose: closes DIFF-03 (graph_version advances + ApplyRepair receives
non-empty per-symbol deltas) and closes the carryover deferred-item from
Phase 67.

Output: one new E2E test file, one updated deferred-items.md row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-precise-filefactdiff-populator/68-CONTEXT.md
@.planning/phases/68-precise-filefactdiff-populator/68-RESEARCH.md
@.planning/phases/68-precise-filefactdiff-populator/68-PATTERNS.md
@.planning/phases/68-precise-filefactdiff-populator/68-VALIDATION.md
@.planning/phases/68-precise-filefactdiff-populator/68-01-PLAN.md
@.planning/phases/68-precise-filefactdiff-populator/68-02-PLAN.md
@.planning/phases/68-precise-filefactdiff-populator/68-03-PLAN.md
@.planning/phases/68-precise-filefactdiff-populator/68-04-PLAN.md
@.planning/deferred-items.md
@internal/semantic/live/handler/recorder_test.go
@internal/semantic/live/handler/handler.go
@internal/semantic/store/snapshot.go

<interfaces>
Test scaffolding from recorder_test.go:20-87 (reusable verbatim — same `package handler_test`):
```go
type recordingRankApplier struct{ calls []recordedApplyRepairCall }
type recordedApplyRepairCall struct{ repoID string; repair graphpkg.GraphRepair }
func (r *recordingRankApplier) ApplyRepair(_ context.Context, repoID string, repair graphpkg.GraphRepair) (uint64, bool, error)
```

Test seam from Plan 68-04:
```go
func LastRecorderSnapshotForTest(h *Handler) graphpkg.FileFactDiff
```

Handler setters from Plan 68-04:
```go
func (h *Handler) SetFileFactStore(s FileFactStore)
func (h *Handler) SetExtractRegistry(r ExtractRegistry)
```

Dispatch entry (handler.go existing):
```go
func (h *Handler) Dispatch(ctx context.Context, ev live.SourceChangeEvent) error
```
(Confirm exact signature against current handler.go before using.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Author E2E test, expect failure on stale-handler scaffolds</name>
  <files>internal/semantic/live/handler/handler_diff_e2e_test.go</files>
  <read_first>
    - internal/semantic/live/handler/recorder_test.go (lines 20-87 reuse: recordingRankApplier, recordingSlogHandler, slogLoggerAdapter, newRecorderTestHandler)
    - internal/semantic/live/handler/handler.go (handler.New signature + Dispatch entry point + live.SourceChangeEvent / live.ChangeHelixEdit kind values)
    - internal/semantic/store/snapshot_test.go (real *Store on tmpdir DuckDB bootstrap pattern — TestMain or per-test helper)
    - internal/semantic/extract/golang/provider.go (NewProvider signature for building the real registry in tests)
    - 68-PATTERNS.md lines 355-389 (handler_diff_e2e_test.go analog: REUSE recordingRankApplier)
    - 68-RESEARCH.md Example 4 (E2E test skeleton)
    - 68-RESEARCH.md Pitfall 5 (do NOT import lspclient/fsnotify)
    - 68-CONTEXT.md D-09 (E2E shape: edit one Go function, assert RecordSymbolChanged fires exactly once, ApplyRepair gets non-empty repair, tier="full")
    - 68-CONTEXT.md D-10 (use REAL populator path — no SetPopulateRecorderForTest)
  </read_first>
  <behavior>
    File: `internal/semantic/live/handler/handler_diff_e2e_test.go`, `package handler_test`.

    Required test: `TestE2E_LiveEditFiresPreciseDiff`.

    Sequence:
    1. `t.TempDir()` for the workspace AND a separate dir for the DuckDB file (or use in-tempdir DuckDB; mirror snapshot_test.go).
    2. Bootstrap real `*storepkg.Store` on tmpdir DuckDB; run migrations.
    3. Build the real `extract.Registry` with a Go provider (mirror existing test bootstraps in extract/golang).
    4. Seed prior state: write `pkg/hello.go` with `package pkg\nfunc Hello() string { return "v1" }\n`. Drive a first ingestion path so the snapshot tables get populated (option A: invoke the scheduler batch flow directly; option B: directly INSERT into semantic_snapshots+semantic_files+semantic_symbols with the SymbolFact for Hello-v1). Option B is simpler and avoids dragging in the scheduler — recommended.
    5. Construct Handler via `handler.New(...)` (mirror newRecorderTestHandler from recorder_test.go). Inject `SetRankApplier(applier)`, `SetFileFactStore(store)`, `SetExtractRegistry(registry)`. Do NOT call SetPopulateRecorderForTest (D-10).
    6. Modify the on-disk file: rewrite to `func Hello() int { return 42 }\n` (signature change: return type `string`→`int`).
    7. Dispatch the live event: `h.Dispatch(ctx, live.SourceChangeEvent{Kind: live.ChangeHelixEdit, RepoID: ..., Path: ...})`. (Confirm exact field set against current SourceChangeEvent.)
    8. Assertions (post-dispatch):
       - `len(applier.calls) == 1` — ApplyRepair fired exactly once.
       - `len(applier.calls[0].repair.DirtyNodes) > 0` — non-empty repair (the DIFF-03 success crit).
       - Recorder snapshot via `handler.LastRecorderSnapshotForTest(h)` has `len(.ChangedSymbols) == 1` and that entry has `SignatureChanged == true` (asserts precise per-symbol delta, not synthetic marker).
       - Metric: outcome counter `helix_live_filefactdiff_total{tier="full", repo=...}` incremented to 1 (read via prometheus testutil — same pattern as obs/metrics_test.go).
       - NO synthetic_reason metric incremented.

    Additional guards:
    - Test must use `t.Parallel()` and run race-clean by default.
    - DO NOT import `internal/kernel/lspclient` (verify via build), `github.com/fsnotify`, or anything else outside the semantic+obs subtree (Pitfall 5).
    - If `testing.Short()`, t.Skip("e2e") at top.

    At this RED step, the test file may compile (all setters exist from Plan 68-04). If it compiles, run it — the test SHOULD already pass since plans 68-01..04 landed Tier-1. If it passes immediately, that's acceptable: this Task 1 is effectively a verification-as-author task. The RED→GREEN cycle for E2E plays out at the integration boundary.

    If the test FAILS unexpectedly (e.g., metric assertion off, recorder snapshot empty), iterate on the test until assertions match the contract of Plans 68-01..04. DO NOT modify production code from this task — file a follow-up note in 68-05-SUMMARY.md instead.

    Commit: `test(68-05): add end-to-end test proving precise FileFactDiff under -race`
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix &amp;&amp; go test -race -run 'TestE2E_LiveEditFiresPreciseDiff' ./internal/semantic/live/handler/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/semantic/live/handler/handler_diff_e2e_test.go` exists.
    - `grep -c 'TestE2E_LiveEditFiresPreciseDiff' internal/semantic/live/handler/handler_diff_e2e_test.go` == 1.
    - `grep -c 'SetPopulateRecorderForTest' internal/semantic/live/handler/handler_diff_e2e_test.go` == 0 (D-10: real populator path, no mocked populator).
    - `grep -c 'lspclient\|fsnotify' internal/semantic/live/handler/handler_diff_e2e_test.go` == 0 (Pitfall 5 negative invariant).
    - `grep -c 'SetFileFactStore\|SetExtractRegistry' internal/semantic/live/handler/handler_diff_e2e_test.go` ≥ 2 (wires both new Handler deps).
    - `grep -c 'SignatureChanged' internal/semantic/live/handler/handler_diff_e2e_test.go` ≥ 1 (asserts precise flag — not synthetic marker).
    - `grep -c 'tier.*full\|"full"' internal/semantic/live/handler/handler_diff_e2e_test.go` ≥ 1 (asserts outcome metric tier).
    - `go test -race -run 'TestE2E_LiveEditFiresPreciseDiff' ./internal/semantic/live/handler/... -count=1` exits 0.
  </acceptance_criteria>
  <done>E2E test green under -race, asserts the full DIFF-03 chain (recorder → ApplyRepair → non-empty repair → outcome metric tier="full"); no LSP, no fsnotify, no mocked populator.</done>
</task>

<task type="auto">
  <name>Task 2: Close DEF-67-F01-FULL-DIFF in deferred-items.md</name>
  <files>.planning/deferred-items.md</files>
  <read_first>
    - .planning/deferred-items.md (locate the DEF-67-F01-FULL-DIFF row and its current status)
    - 68-CONTEXT.md D-11 (closure obligation)
  </read_first>
  <action>
    Edit `.planning/deferred-items.md`:
    - Locate the row for `DEF-67-F01-FULL-DIFF`.
    - Flip its status to `resolved` (matching the file's existing status taxonomy — use the same column values other resolved items use).
    - Add a backlink to Phase 68 in the resolution column or as an inline note: "Resolved by Phase 68 (precise FileFactDiff populator) — DIFF-01..04 closed."
    - Add the resolution date if the file has a date column: `2026-05-13`.

    If multiple rows reference 62-09 / FULL-DIFF, only flip the one named `DEF-67-F01-FULL-DIFF`. Leave others untouched.

    Commit: `docs(68-05): close DEF-67-F01-FULL-DIFF; resolved by Phase 68`
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix &amp;&amp; grep -A2 'DEF-67-F01-FULL-DIFF' .planning/deferred-items.md | grep -E 'resolved|Phase 68'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DEF-67-F01-FULL-DIFF' .planning/deferred-items.md` ≥ 1.
    - Within 3 lines after the DEF-67-F01-FULL-DIFF anchor, file contains both `Phase 68` and a status token indicating resolution (file's existing taxonomy — `resolved`, `[x]`, `closed`, or equivalent).
    - No other deferred-items rows modified (verify via `git diff --stat .planning/deferred-items.md` → 1 file changed, modest line delta).
  </acceptance_criteria>
  <done>DEF-67-F01-FULL-DIFF flipped to resolved with Phase 68 backlink; deferred-items.md committed.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide regression sweep</name>
  <files></files>
  <read_first>
    - 68-VALIDATION.md "Sampling Rate" → phase-gate row ("Full suite command")
  </read_first>
  <action>
    Phase-gate verification before /gsd-verify-work:
    1. `go test ./... -race -count=1` — full suite, race-clean.
    2. `make vet` — vet-nokernel2semantic invariant intact.
    3. Spot-check the canary tests for adjacent subsystems:
       - `go test -race ./internal/semantic/graph/... -count=1` (ApplyRepair / ComputeGraphRepair regression).
       - `go test -race ./internal/semantic/live/... -count=1` (live pipeline broad regression).
       - `go test -race ./internal/obs/... -count=1`.

    No files modified.

    If `go test ./...` exposes a flake, note it in 68-05-SUMMARY.md and re-run; do not paper over flakes by modifying production code from this verification task — file a follow-up.

    Commit: none (verification only).
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix &amp;&amp; go test ./... -race -count=1 &amp;&amp; make vet</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -race -count=1` exits 0.
    - `make vet` exits 0.
    - No new package failures introduced by Phase 68.
  </acceptance_criteria>
  <done>Full repo -race-green; vet-nokernel2semantic green; Phase 68 ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → real Store on tmpdir | Test-owned filesystem; trusted; isolated per test |
| test → real extract.Registry | Same trust as production wiring |
| test → recorder snapshot via LastRecorderSnapshotForTest | Test-only export; visibility-restricted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-16 | T (flaky test → wrong diff result on CI) | LSP / fsnotify in test path | mitigate | Pitfall 5 negative invariant: acceptance criteria forbid lspclient/fsnotify imports in handler_diff_e2e_test.go. Test drives the handler directly via Dispatch. |
| T-68-17 | R (race on Handler shared state under -race) | recorder + lastRecorderSnapshot | mitigate | Single-goroutine tx ownership invariant (62-09); test runs under `-race` default; acceptance asserts -count=1 exit 0. |
| T-68-18 | I (test seam expanding production attack surface) | LastRecorderSnapshotForTest export | accept | Exported via `export_test.go` only — Go's `_test.go` packaging means the symbol is not part of the production binary. |
</threat_model>

<verification>
- `go test -race -run 'TestE2E_LiveEditFiresPreciseDiff' ./internal/semantic/live/handler/... -count=1` exits 0.
- `go test ./... -race -count=1` exits 0 (phase-gate full suite).
- `make vet` exits 0.
- `grep 'DEF-67-F01-FULL-DIFF' .planning/deferred-items.md` shows resolved status with Phase 68 backlink.
- E2E test does not import lspclient or fsnotify.
</verification>

<success_criteria>
- DIFF-03 closed: end-to-end live edit produces exactly one RecordSymbolChanged + non-empty ApplyRepair + outcome metric tier="full"; race-clean.
- DEF-67-F01-FULL-DIFF closed in deferred-items.md (D-11 obligation).
- Full repo `go test ./... -race -count=1` green.
- `make vet` (vet-nokernel2semantic) green.
- Phase 68 ready for `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/68-precise-filefactdiff-populator/68-05-SUMMARY.md` documenting:
- Final test name + the assertions it carries.
- Whether seeding used option A (scheduler batch flow) or option B (direct INSERT into snapshot tables).
- Recorder snapshot inspection — confirm ChangedSymbols carries SignatureChanged=true (per the live signature `string→int` edit).
- DEF-67-F01-FULL-DIFF closure entry as it now reads in deferred-items.md.
- Full-suite test pass count + duration; any flakes observed.
- Phase 68 closure statement covering DIFF-01..04 + success criteria 1–5.
</output>
