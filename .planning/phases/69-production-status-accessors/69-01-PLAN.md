---
phase: 69
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/semantic/store/effective_graph.go
  - internal/semantic/store/effective_graph_test.go
autonomous: true
requirements: [STATUS-01]
tags: [semantic-graph, status, cluster, read-accessor, race-clean]
must_haves:
  truths:
    - "*Store.ClusterStatusForGraphVersion returns {ComputedAt unix-ms, MemberCount, ClusterCount, GraphVersion} for a seeded (repoID, gv) pair"
    - "Accessor returns zero-value struct + nil error when no cluster rows exist"
    - "Accessor is lock-free under concurrent BeginOverlayTx writers (D-09 invariant)"
    - "MemberCount derived from COUNT(*) over semantic_cluster_members, NOT from the semantic_clusters.score column"
    - "ClusterStatusRow type exported from internal/semantic/store for downstream adapter consumption"
  artifacts:
    - path: "internal/semantic/store/effective_graph.go"
      provides: "ClusterStatusForGraphVersion method + ClusterStatusRow type"
      contains: "func (s *Store) ClusterStatusForGraphVersion"
    - path: "internal/semantic/store/effective_graph_test.go"
      provides: "TestClusterStatusForGraphVersion basic + LockFree_RaceSafe variant + seedClusterRows helper"
      contains: "TestClusterStatusForGraphVersion_LockFree_RaceSafe"
  key_links:
    - from: "internal/semantic/store/effective_graph.go"
      to: "semantic_clusters + semantic_cluster_members tables (migrations.go:265-289)"
      via: "s.db.QueryRowContext SELECT JOIN COUNT"
      pattern: "QueryRowContext.*semantic_clusters.*semantic_cluster_members"
---

<objective>
Land `*Store.ClusterStatusForGraphVersion` as a pure DuckDB read accessor that exposes the persisted Phase 62 cluster state to downstream consumers (`semSchedulerAdapter.ClusterStatus` in Plan 69-05).

Purpose: STATUS-01 requires a single source-of-truth read path that survives daemon restart and obeys D-09 (no Begin/Commit/Abort/Write on the read path). Phase 62 already persists `semantic_clusters` + `semantic_cluster_members` rows — this plan opens the read seam without schema change.

Output: One new method on `*Store`, one new exported struct `ClusterStatusRow`, two new tests (basic table + race-safety stress), and a `seedClusterRows` test helper for Plan 69-06 reuse.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-production-status-accessors/69-CONTEXT.md
@.planning/phases/69-production-status-accessors/69-RESEARCH.md
@.planning/phases/69-production-status-accessors/69-PATTERNS.md

@internal/semantic/store/effective_graph.go
@internal/semantic/store/migrations.go
@internal/semantic/store/overlay.go

<interfaces>
Existing canonical analog — copy this shape (effective_graph.go:155-176):

```go
func (s *Store) CountStaleScoreRows(ctx context.Context, repoID, projection string) (stale, total int, err error) {
    if s == nil || s.db == nil {
        return 0, 0, errors.New("CountStaleScoreRows: nil store")
    }
    const q = `SELECT COALESCE(SUM(CASE WHEN status='stale' THEN 1 ELSE 0 END),0), COUNT(*)
               FROM semantic_graph_scores WHERE repo_id=? AND score_name=?`
    if err := s.db.QueryRowContext(ctx, q, repoID, projection).Scan(&stale, &total); err != nil {
        if errors.Is(err, sql.ErrNoRows) { return 0, 0, nil }
        return 0, 0, fmt.Errorf("CountStaleScoreRows(%q,%q): %w", repoID, projection, err)
    }
    return stale, total, nil
}
```

Phase 62 schema (migrations.go:265-289):
- `semantic_clusters(repo_id, snapshot_id, graph_version, cluster_id, algorithm, label, summary, score, status, computed_at TIMESTAMP)` — PK (repo_id, graph_version, cluster_id)
- `semantic_cluster_members(repo_id, graph_version, cluster_id, node_id, weight, role)` — PK (repo_id, graph_version, cluster_id, node_id)
- `computed_at` written via `now()` in `UpsertClusters` (overlay.go:829)
- `semantic_clusters.score` overloaded with `float64(c.MemberCount)` (overlay.go:835) — **DO NOT TRUST**; use COUNT(*) on members instead (RESEARCH Q8 / PATTERNS deviation note)
</interfaces>
</context>

<deferred>
- `"building"` state is DEFERRED — no daemon-resident cluster scheduler exists today (RESEARCH Q1 + PATTERNS §Adapter Replacement). The accessor only needs to surface enough data for the Plan 69-05 adapter to emit `current` / `stale` / `unknown`. CONTEXT.md D1's `"building"` clause is explicitly relaxed for Phase 69.
</deferred>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write failing tests for ClusterStatusForGraphVersion (basic + lock-free race-safe)</name>
  <read_first>
    - `internal/semantic/store/effective_graph_test.go:242-293` (analog — `TestCountStaleScoreRows_LockFree_RaceSafe`)
    - `internal/semantic/store/effective_graph_test.go` — locate `openStoreForOverlayTest` helper + any existing `seed*` helpers
    - `internal/semantic/store/overlay.go:777-902` — `BeginOverlayTx`, `UpsertClusters`, `UpsertClusterMembers`, `ClusterSummary` / `ClusterMemberRow` shapes
    - PATTERNS.md §Read Accessor on *Store + §Race-Clean Read-Path Test
  </read_first>
  <behavior>
    - Test 1 (basic, populated): seed 2 clusters with 3+2 members at gv=1 → `ClusterStatusForGraphVersion(ctx, repoID, 1)` returns ClusterCount=2, MemberCount=5, ComputedAt > 0
    - Test 2 (basic, empty): no rows for gv=99 → returns zero-value struct, nil error (mirror sql.ErrNoRows → 0,0,nil arm of CountStaleScoreRows)
    - Test 3 (basic, wrong gv): rows exist for gv=1 but query gv=2 → returns ClusterCount=0, MemberCount=0, nil error
    - Test 4 (LockFree_RaceSafe): 100 concurrent reader probes alongside a background goroutine spinning `BeginOverlayTx → Commit`; each reader wrapped in a 5-second timeout select; must not deadlock and must not return error
    - Test 5 (MemberCount derivation): seed cluster with `ClusterSummary.MemberCount=99` (which writes 99.0 into score column) BUT only 3 actual rows in semantic_cluster_members → accessor returns MemberCount=3 (proves we count members, not score)
  </behavior>
  <action>
    Add `TestClusterStatusForGraphVersion_Basic` (table-driven covering populated / empty / wrong-gv / score-vs-members-divergence cases) and `TestClusterStatusForGraphVersion_LockFree_RaceSafe` (mirror lines 242-293 verbatim, replacing `seedScoreRow` with new `seedClusterRows(t, ctx, s, repoID, gv, []clusterSeed{{id:1, memberCount:3}, {id:2, memberCount:2}})` helper and `s.CountStaleScoreRows` call with `s.ClusterStatusForGraphVersion`). The `seedClusterRows` helper opens a `BeginOverlayTx`, calls `UpsertClusters` with `ClusterSummary{ID, MemberCount}` slice and `UpsertClusterMembers` with synthetic `ClusterMemberRow{ClusterID, NodeID}` rows, then `Commit`s. Use `testing.Short` skip on the race test matching the analog. Per PATTERNS deviation: signature MUST return a struct (`ClusterStatusRow`) not multi-return — anticipate this in test scaffolds. Tests MUST fail at compile time (method does not exist) per RED phase. Commit message: `test(69-01): RED ClusterStatusForGraphVersion tests + seedClusterRows helper`.
  </action>
  <verify>
    <automated>go test ./internal/semantic/store/ -run "TestClusterStatusForGraphVersion" -count=1 2>&1 | grep -E "undefined|no field|cannot|undeclared"</automated>
  </verify>
  <done>Tests compile-fail with "undefined: ClusterStatusForGraphVersion" or "undefined: ClusterStatusRow"; commit landed; no other tests broken.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement ClusterStatusForGraphVersion + ClusterStatusRow</name>
  <read_first>
    - `internal/semantic/store/effective_graph.go:155-176` (`CountStaleScoreRows` shape to copy)
    - `internal/semantic/store/migrations.go:265-289` (table column types — `computed_at TIMESTAMP`)
    - RESEARCH §Code Examples §New *Store.ClusterStatusForGraphVersion (target SQL)
    - PATTERNS.md §Read Accessor on *Store deviation notes (DuckDB EPOCH cast for unix-ms)
  </read_first>
  <action>
    Add to `internal/semantic/store/effective_graph.go` (place beside `CountStaleScoreRows`):

    Define exported `ClusterStatusRow` struct with fields `GraphVersion uint64`, `ComputedAt int64` (unix ms; 0 when no rows), `MemberCount int`, `ClusterCount int`.

    Define `func (s *Store) ClusterStatusForGraphVersion(ctx context.Context, repoID string, graphVersion uint64) (ClusterStatusRow, error)`. Receiver MUST be `*Store` (not `*OverlayTx`) — D-09 compile-time guard. Body uses a single `s.db.QueryRowContext` SELECT against `semantic_clusters c` aliased; computed_at_ms via `COALESCE(MAX(EXTRACT(EPOCH FROM c.computed_at) * 1000), 0)::BIGINT`; cluster_count via `COUNT(DISTINCT c.cluster_id)`; member_count via a correlated subquery `(SELECT COUNT(*) FROM semantic_cluster_members m WHERE m.repo_id=? AND m.graph_version=?)`. WHERE `c.repo_id=? AND c.graph_version=?`. Bind order: member-subquery repoID, gv, outer repoID, gv. On `sql.ErrNoRows` return zero-value + nil. On other error wrap with `fmt.Errorf("ClusterStatusForGraphVersion(%q, %d): %w", repoID, graphVersion, err)`. Set `row.GraphVersion = graphVersion` before return on success path.

    NO `s.mu` lock, NO `BeginOverlayTx`, NO `Begin*/Commit*/Abort*/Write*` calls — D-09 invariant.
  </action>
  <verify>
    <automated>go test ./internal/semantic/store/ -run "TestClusterStatusForGraphVersion" -count=1 -race -timeout 30s</automated>
  </verify>
  <done>All 5 test cases PASS under -race. `grep -nE 'BeginSnapshot|BeginOverlayTx|CommitSnapshot|AbortSnapshot|WriteSnapshotFacts' internal/semantic/store/effective_graph.go | grep -i "ClusterStatusForGraphVersion"` returns no matches (D-09 grep audit). `go vet ./internal/semantic/store/...` clean. Commit: `feat(69-01): implement ClusterStatusForGraphVersion read accessor`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill → store | Status handler invokes read accessor; no caller-supplied data reaches SQL beyond repoID (workspace-scoped) and graphVersion (uint64). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | I (Information Disclosure) | ClusterStatusForGraphVersion result | mitigate | Accessor returns counts + computed_at only — no node_ids, no symbol paths, no cluster labels (SELECT does not include c.label / m.node_id) |
| T-69-03 | T (Tampering) / D (DoS) | D-09 lock contention | mitigate | Method receiver is `*Store` not `*OverlayTx`; race-safe test asserts 100 concurrent readers + writer do not deadlock (5s timeout); grep audit confirms no Begin/Commit/Abort/Write on read path |
</threat_model>

<verification>
- `go test ./internal/semantic/store/... -count=1 -race -timeout 30s` PASS
- `go vet ./internal/semantic/store/...` clean
- `grep -nE 'Begin(Snapshot|OverlayTx)|Commit(Snapshot)?|Abort(Snapshot)?|WriteSnapshotFacts' internal/semantic/store/effective_graph.go` shows zero new matches inside ClusterStatusForGraphVersion body
- `seedClusterRows` helper is exported test-only (lowercase, in `_test.go`) so Plan 69-06 can reuse it via the same package
</verification>

<success_criteria>
- ClusterStatusForGraphVersion + ClusterStatusRow exist on `*Store` and pass all 5 tests under -race
- LockFree_RaceSafe test completes in < 30s with zero deadlock errors
- D-09 grep audit clean
- seedClusterRows helper usable from Plan 69-06 integration test (same package)
</success_criteria>

<output>
After completion, write `.planning/phases/69-production-status-accessors/69-01-SUMMARY.md` capturing: SQL final form, any DuckDB EPOCH-cast quirks encountered, exact test counts, and whether `seedClusterRows` shape required adjustment from the planned signature.
</output>
