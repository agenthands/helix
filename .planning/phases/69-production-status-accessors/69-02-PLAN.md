---
phase: 69
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/semantic/retrieval/bleve.go
  - internal/semantic/retrieval/recovery.go
  - internal/semantic/retrieval/recovery_test.go
autonomous: true
requirements: [STATUS-02]
tags: [semantic-graph, status, retrieval, bleve, single-writer]
must_haves:
  truths:
    - "Recoverer.rebuildBlocking writes corpus_version meta (uint64 string-encoded) after successful UpsertBatch loop"
    - "Recoverer.rebuildBlocking writes indexed_files meta (int64 string-encoded, count of distinct file_ids seen during walk)"
    - "Engine exposes DocCount() (uint64, error) wrapping idx.DocCount for indexed_symbols read"
    - "corpus_version / indexed_files SetMeta failures are logged via r.logger.Warn (non-fatal) — meta lapse must NOT undo the rebuild"
    - "metaKeyLastIndexed write retains existing fatal-on-error policy (single-writer site preserved)"
  artifacts:
    - path: "internal/semantic/retrieval/bleve.go"
      provides: "DocCount() method on *Engine + metaKeyCorpusVersion / metaKeyIndexedFiles constants"
      contains: "func (e *Engine) DocCount()"
    - path: "internal/semantic/retrieval/recovery.go"
      provides: "Two new SetMeta calls beside metaKeyLastIndexed; distinctFileCount accumulator inside walk loop; StoreReader seam extended if needed"
      contains: "metaKeyCorpusVersion"
    - path: "internal/semantic/retrieval/recovery_test.go"
      provides: "TestRebuild_WritesCorpusVersionAndFileCount + TestEngine_DocCount"
      contains: "TestRebuild_WritesCorpusVersionAndFileCount"
  key_links:
    - from: "recovery.go rebuildBlocking"
      to: "bleve internal meta (corpus_version, indexed_files)"
      via: "r.engine.SetMeta after flush()"
      pattern: "SetMeta.*corpus_version"
    - from: "recovery.go rebuildBlocking"
      to: "store.CurrentGraphVersion"
      via: "r.store.CurrentGraphVersion(ctx, repoID)"
      pattern: "CurrentGraphVersion"
---

<objective>
Add three bleve corpus-state meta surfaces consumed by `semRetrievalAdapter.RetrievalStatus` (Plan 69-05): `corpus_version` and `indexed_files` written by the Recoverer's single-writer commit point, plus a `DocCount()` wrapper on `*Engine` exposing `bleve.Index.DocCount()` for `indexed_symbols`.

Purpose: STATUS-02 needs production bleve meta. Today the production buildFn does NOT call `UpsertBatch` (RESEARCH Q4) — the only bleve writer is `Recoverer.rebuildBlocking` (recovery.go:217-260). All three new meta writers/readers therefore live in the retrieval package.

Output: One new method on `*Engine`, two new meta-key constants, two new `SetMeta` calls in the existing single-writer site, optional `StoreReader` seam extension for `CurrentGraphVersion`, two new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-production-status-accessors/69-CONTEXT.md
@.planning/phases/69-production-status-accessors/69-RESEARCH.md
@.planning/phases/69-production-status-accessors/69-PATTERNS.md

@internal/semantic/retrieval/bleve.go
@internal/semantic/retrieval/recovery.go
@internal/semantic/retrieval/corpus.go

<interfaces>
Existing single-writer commit point (recovery.go:255-261) — extend AT THIS SITE:

```go
if err := flush(); err != nil { return err }
if err := r.engine.SetMeta(metaKeyLastIndexed, []byte(strconv.FormatUint(snapshotID, 10))); err != nil {
    return fmt.Errorf("SetMeta(last_indexed=%d): %w", snapshotID, err)
}
return nil
```

Existing bleve meta API (bleve.go:178-202) — `GetMeta` / `SetMeta` already exposed. `*Engine` wraps `idx bleve.Index`; bleve's `Index.DocCount() (uint64, error)` is available natively.

`MapSymbolToDoc` (corpus.go) — one bleve doc per symbol per `SymbolRow`. Walk loop at recovery.go:235-243 receives `store.SymbolRow` rows; `row.FileID` is the field to accumulate for distinct-file counting (per RESEARCH Q2 — option (b)).

StoreReader seam (recovery.go) — RESEARCH A4 flags MEDIUM risk that this local interface may not expose `CurrentGraphVersion`. The implementer MUST grep the StoreReader interface declaration in recovery.go and extend it if absent (add `CurrentGraphVersion(ctx context.Context, repoID string) (uint64, error)` — `*Store` already provides this).

Error policy divergence (PATTERNS §Bleve Single-Writer Meta — Recoverer Extension): existing metaKeyLastIndexed write is fatal-on-error. New corpus_version + indexed_files writes are `r.logger.Warn` non-fatal per RESEARCH §Bleve Meta Write — a meta lapse must not undo a completed rebuild.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing tests for DocCount + Recoverer meta writes</name>
  <read_first>
    - `internal/semantic/retrieval/recovery_test.go` (existing test setup — `newTestRecoverer` or similar harness)
    - `internal/semantic/retrieval/bleve.go:178-202` (GetMeta/SetMeta signatures)
    - `internal/semantic/retrieval/corpus.go` (MapSymbolToDoc + SymbolDoc fields)
    - PATTERNS.md §Bleve `GetMeta` / `SetMeta` Surface
  </read_first>
  <behavior>
    - Test 1: `(*Engine).DocCount()` returns 0 for empty index; returns N after UpsertBatch of N distinct SymbolDocs
    - Test 2: `rebuildBlocking` with snapshot containing symbols across 3 distinct file_ids → after success, `engine.GetMeta("indexed_files")` returns "3" (string-encoded int64)
    - Test 3: `rebuildBlocking` with non-zero CurrentGraphVersion=7 → after success, `engine.GetMeta("corpus_version")` returns "7" (string-encoded uint64)
    - Test 4: `rebuildBlocking` when CurrentGraphVersion returns gv=0 → corpus_version meta is NOT written (no key present after rebuild)
    - Test 5 (non-fatal policy): inject a SetMeta failure for corpus_version via a wrapping engine fake → rebuild still returns nil (logger.Warn observed but error not propagated). Use a `bytes.Buffer`-backed `*slog.Logger` or capture logger sink to assert the Warn message.
    - Test 6: metaKeyLastIndexed write failure remains fatal (regression guard — existing behavior preserved)
  </behavior>
  <action>
    Append to `internal/semantic/retrieval/recovery_test.go`:
    - `TestEngine_DocCount` — open in-memory Engine, assert DocCount==0, UpsertBatch 5 docs, assert DocCount==5.
    - `TestRebuild_WritesCorpusVersionAndFileCount` — table-driven over the 6 behavior cases. For the StoreReader stub, extend the existing test fake to implement `CurrentGraphVersion(ctx, repoID) (uint64, error)` returning a configurable value. For the non-fatal-Warn test, wrap `*Engine` in a small `engineFailingSetMeta` shim that returns error on `SetMeta("corpus_version", ...)` — note this requires recovery.go to call SetMeta via an interface seam OR the test injects via a fake `bleveWriter` field; implementer must decide whether to add a narrow `bleveMetaWriter interface { SetMeta(string,[]byte) error }` on Recoverer (preferred — keeps tests honest) or use a build tag.

    Tests MUST fail: `DocCount` method does not exist; `corpus_version` / `indexed_files` keys not written.

    Commit: `test(69-02): RED bleve meta writes + DocCount tests`.
  </action>
  <verify>
    <automated>go test ./internal/semantic/retrieval/ -run "TestEngine_DocCount|TestRebuild_WritesCorpusVersionAndFileCount" -count=1 2>&1 | grep -E "undefined|cannot|undeclared|FAIL"</automated>
  </verify>
  <done>Tests compile-fail OR fail with assertion errors (no `corpus_version` key found, DocCount method undefined). Commit landed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement DocCount + Recoverer meta writes</name>
  <read_first>
    - `internal/semantic/retrieval/bleve.go:178-202` (Engine + idx field)
    - `internal/semantic/retrieval/recovery.go:217-260` (full rebuildBlocking body)
    - Locate StoreReader interface declaration in recovery.go (grep `type StoreReader`); extend with `CurrentGraphVersion` if absent
    - RESEARCH §Code Examples §Bleve Meta Write in Recoverer
  </read_first>
  <action>
    1) `bleve.go`: Add exported constants beside existing meta keys: `const metaKeyCorpusVersion = "corpus_version"`, `const metaKeyIndexedFiles = "indexed_files"`. Add method `func (e *Engine) DocCount() (uint64, error)` — nil-guards mirror `GetMeta`; delegates to `e.idx.DocCount()`; wraps error `idx.DocCount: %w`.

    2) `recovery.go`: Inside `rebuildBlocking`, BEFORE the existing walk loop, declare `distinctFiles := make(map[uint64]struct{})`. Inside the iterate callback (next to `batch = append(batch, MapSymbolToDoc(row, nil))`), record `distinctFiles[row.FileID] = struct{}{}`.

    AFTER the existing successful `SetMeta(metaKeyLastIndexed, ...)` line and BEFORE the function's final `return nil`:
    - Resolve `repoID := ws.Hash()` (or whatever the existing signature exposes — verify via grep on rebuildBlocking signature).
    - Call `r.store.CurrentGraphVersion(ctx, repoID)`. If err non-nil → `r.logger.Warn("retrieval.rebuild: CurrentGraphVersion failed", "err", err)` and skip corpus_version write. If gv == 0 → skip corpus_version write entirely. Otherwise call `r.engine.SetMeta(metaKeyCorpusVersion, []byte(strconv.FormatUint(gv, 10)))`; on err → `r.logger.Warn("retrieval.rebuild: SetMeta(corpus_version) failed", "err", err)` (NON-FATAL).
    - Always write indexed_files: `r.engine.SetMeta(metaKeyIndexedFiles, []byte(strconv.FormatInt(int64(len(distinctFiles)), 10)))`; on err → `r.logger.Warn(...)` (NON-FATAL).

    If `StoreReader` does not expose `CurrentGraphVersion`, extend the local interface (add the method signature `CurrentGraphVersion(ctx context.Context, repoID string) (uint64, error)`) — `*Store` already implements it; existing tests pass an `*Store` or a fake that must grow the stub.

    Commit: `feat(69-02): write corpus_version/indexed_files meta + DocCount wrapper`.
  </action>
  <verify>
    <automated>go test ./internal/semantic/retrieval/ -count=1 -race -timeout 60s</automated>
  </verify>
  <done>All retrieval tests PASS under -race. `grep -n 'SetMeta(metaKeyCorpusVersion' internal/semantic/retrieval/recovery.go` shows exactly one match. `grep -n 'SetMeta(metaKeyIndexedFiles' internal/semantic/retrieval/recovery.go` shows exactly one match (single-writer invariant). `go vet ./internal/semantic/retrieval/...` clean. Commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recoverer → bleve internal meta | New meta keys; only the Recoverer (and Plan 69-03's compactor for last_compact_at) ever writes them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-02 | I (Information Disclosure) | corpus_version exposure | mitigate | `corpus_version` is a monotonic uint64 with no embedded identity; per-workspace scope inherited from the engine's per-repo file path |
| T-69-03 | T / D | Single-writer invariant | mitigate | Grep audit asserts exactly one SetMeta site per new key in the retrieval package; non-fatal Warn policy prevents a flaky meta write from undoing a successful rebuild |
</threat_model>

<verification>
- `go test ./internal/semantic/retrieval/... -count=1 -race -timeout 60s` PASS
- `grep -rn 'SetMeta(metaKeyCorpusVersion\|SetMeta("corpus_version"' internal/` returns exactly the recovery.go site (no other writers)
- `grep -rn 'SetMeta(metaKeyIndexedFiles\|SetMeta("indexed_files"' internal/` returns exactly the recovery.go site
- `go vet ./internal/semantic/retrieval/...` clean
</verification>

<success_criteria>
- DocCount method usable from Plan 69-05 adapter
- corpus_version + indexed_files meta keys readable via existing GetMeta API
- StoreReader seam exposes CurrentGraphVersion (or the recovery seam was already wide enough)
- All 6 tests PASS under -race
</success_criteria>

<output>
After completion, write `.planning/phases/69-production-status-accessors/69-02-SUMMARY.md` capturing: whether StoreReader required extension, exact non-fatal Warn message format, and the bleveMetaWriter seam decision (interface vs direct *Engine call).
</output>
