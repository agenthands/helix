---
phase: 69
plan: 03
type: tdd
wave: 2
depends_on: [02]
files_modified:
  - internal/semantic/compact/accessors.go
  - internal/semantic/compact/compactor.go
  - internal/semantic/compact/compactor_test.go
  - internal/daemon/compact_wiring.go
autonomous: true
requirements: [STATUS-02]
tags: [semantic-graph, status, compactor, bleve, dep-injection]
must_haves:
  truths:
    - "compact.Deps gains optional BleveMeta field (nil-safe — existing test literals do not break)"
    - "runCompaction stamps last_compact_at (unix ms via c.now().UnixMilli()) immediately after c.lastBaseID = snap.ID and before maybeVacuum"
    - "BleveMeta SetMeta failure is non-fatal (`_ = c.deps.BleveMeta.SetMeta(...)`) — compaction success outcome unchanged"
    - "compact_wiring.go ensureCompactor threads a per-workspace BleveMeta resolver from semanticBundle.engines[ws.RepoRoot]"
    - "Plan 69-05 wires the resolver; this plan extends compactBundle so the wiring point is ready"
    - "SetMeta call uses the EXPORTED retrieval.MetaKeyLastCompactAt constant (NOT a string literal) — introduces a deliberate compact→retrieval import that is verified acyclic"
  artifacts:
    - path: "internal/semantic/compact/accessors.go"
      provides: "BleveMeta interface { SetMeta(key string, val []byte) error }"
      contains: "type BleveMeta interface"
    - path: "internal/semantic/compact/compactor.go"
      provides: "Deps.BleveMeta field + nil-safe SetMeta call in runCompaction success path; imports internal/semantic/retrieval for MetaKeyLastCompactAt"
      contains: "retrieval.MetaKeyLastCompactAt"
    - path: "internal/daemon/compact_wiring.go"
      provides: "compactBundle gains bleveMetaFn or equivalent resolver field; ensureCompactor passes BleveMeta into compact.Deps literal"
      contains: "BleveMeta:"
  key_links:
    - from: "internal/semantic/compact/compactor.go runCompaction"
      to: "bleve internal meta last_compact_at"
      via: "c.deps.BleveMeta.SetMeta(retrieval.MetaKeyLastCompactAt, ...) after c.lastBaseID = snap.ID"
      pattern: "SetMeta\\(retrieval\\.MetaKeyLastCompactAt"
    - from: "internal/daemon/compact_wiring.go ensureCompactor"
      to: "semanticBundle.engines[repoRoot]"
      via: "bleveMetaFn closure or direct field"
      pattern: "BleveMeta:"
---

<objective>
Inject a `BleveMeta` writer dep into the compactor and write `last_compact_at` (unix ms) on the success path of `runCompaction`, then thread the per-workspace resolver through `compactBundle` so Plan 69-05 can wire it to the real `*retrieval.Engine`.

Purpose: STATUS-02 needs `last_compact_at` to be a real bleve meta key. Today the compactor has no bleve dep (RESEARCH Q3); the cleanest seam is a narrow `BleveMeta` interface in `compact.Deps`, nil-safe to preserve existing test literals (RESEARCH Pitfall 5). The SetMeta key MUST be the exported `retrieval.MetaKeyLastCompactAt` constant introduced in Plan 69-02 — single token across writer + reader, no drift.

Output: One new interface, one new optional Deps field, one new SetMeta call at compactor.go:332 referencing `retrieval.MetaKeyLastCompactAt`, one wiring update in compact_wiring.go (a placeholder/nil bleveMetaFn is acceptable in this plan — Plan 69-05 binds it to the real engine resolver).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-production-status-accessors/69-CONTEXT.md
@.planning/phases/69-production-status-accessors/69-RESEARCH.md
@.planning/phases/69-production-status-accessors/69-PATTERNS.md
@.planning/phases/69-production-status-accessors/69-02-PLAN.md

@internal/semantic/compact/accessors.go
@internal/semantic/compact/compactor.go
@internal/daemon/compact_wiring.go

<interfaces>
Insertion point (compactor.go:332) — append after `c.lastBaseID = snap.ID` and BEFORE `c.maybeVacuum(ctx)`:

```go
c.lastBaseID = snap.ID
// (Phase 69) new SetMeta call goes here — nil-safe
c.maybeVacuum(ctx)
```

Nil-safe analog (compactor.go:223 / 233 metrics guard):

```go
if c.deps.Metrics != nil { c.deps.Metrics.SemanticCompactionObserve(...) }
```

Wiring analog (compact_wiring.go:172-178 ensureCompactor):

```go
c := compact.NewCompactor(ws, repoID, b.cfg, compact.Deps{
    Gate: gate, Store: b.store, OverlayOps: b.store, Metrics: b.metrics, Logger: b.logger,
})
```

`compactBundle` field shape — current bundle holds `b.live`, `b.rankBundle`, `b.lspQueue` etc. Add `b.bleveMetaFn func(ws workspace.WorkspaceKey) compact.BleveMeta` (a closure resolving the per-workspace `*retrieval.Engine`). Default to nil-returning closure in this plan; Plan 69-05 binds it via a new `SetBleveMetaFn` setter or direct field assignment on the bundle from semantic_wiring.go.

Compactor `c.now` clock source — `c.now().UnixMilli()` is the canonical timestamp (RESEARCH A3, compactor.go:221).

**Cross-package import (NEW — addresses constant-naming asymmetry warning):**

This plan introduces `import "github.com/agenthands/helix/internal/semantic/retrieval"` into `internal/semantic/compact/compactor.go` solely to reference `retrieval.MetaKeyLastCompactAt`. Acyclicity verified (Plan 69-02 interfaces section): `retrieval` does NOT import `compact`, today or in Plan 69-02's deltas. Implementer MUST re-verify after Plan 69-02 lands with: `grep -rn '"github.com/agenthands/helix/internal/semantic/compact"' internal/semantic/retrieval/`. If any match — STOP and surface as an unexpected coupling.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing test for runCompaction writes last_compact_at + nil-safety</name>
  <read_first>
    - `internal/semantic/compact/compactor_test.go` (existing test harness — locate the test that runs a full compaction cycle; identify how `compact.Deps` literals are constructed)
    - `internal/semantic/compact/compactor.go:204-339` (full runCompaction body)
    - PATTERNS.md §Compactor Dep Injection
    - 69-02-PLAN.md (for the exact `retrieval.MetaKeyLastCompactAt` constant the test must read against)
  </read_first>
  <behavior>
    - Test 1 (writes): construct a `fakeBleveMeta` (in-memory `map[string][]byte` satisfying `BleveMeta`); run a successful compaction; assert `fakeBleveMeta[retrieval.MetaKeyLastCompactAt]` is a valid int64 unix-ms string ≥ test start time and ≤ test end time
    - Test 2 (nil-safe): construct Compactor with `Deps{... BleveMeta: nil ...}`; run a successful compaction; assert no panic, outcome unchanged
    - Test 3 (non-fatal): `fakeBleveMeta` that returns error from SetMeta; run successful compaction; assert outcome is still "success" (compactor.go's `outcome` variable), error is NOT propagated
    - Test 4 (failure path not stamped): force CommitSnapshot to error before c.lastBaseID assignment; assert `fakeBleveMeta` was never called (write only on success path)
  </behavior>
  <action>
    Append to `internal/semantic/compact/compactor_test.go`:
    - Add `type fakeBleveMeta struct { mu sync.Mutex; data map[string][]byte; failKeys map[string]error }` with `SetMeta(k string, v []byte) error` honoring failKeys.
    - `TestRunCompaction_WritesLastCompactAt` covering all 4 behavior cases. Use the existing harness's full-success path; assert via `fakeBleveMeta.data[retrieval.MetaKeyLastCompactAt]` (import the retrieval package in the test file — `_test.go` files are allowed to add this import).
    - For the failure-path test, use the existing mechanism (likely a `*fakeStore` with CommitSnapshot error injection — verify in compactor_test.go).

    Tests MUST fail: `BleveMeta` type undefined, `Deps.BleveMeta` field undefined.

    Commit: `test(69-03): RED last_compact_at write + nil-safe BleveMeta dep`.
  </action>
  <verify>
    <automated>go test ./internal/semantic/compact/ -run "TestRunCompaction_WritesLastCompactAt" -count=1 2>&1 | grep -E "undefined|cannot|undeclared"</automated>
  </verify>
  <done>Tests compile-fail with "undefined: BleveMeta" or "unknown field BleveMeta in struct literal Deps". Commit landed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — add BleveMeta interface + Deps field + SetMeta call referencing retrieval.MetaKeyLastCompactAt</name>
  <read_first>
    - `internal/semantic/compact/accessors.go:28-74` (analog interface declarations like CoalescerAccessor)
    - `internal/semantic/compact/compactor.go:320-340` (success-path commit + lastBaseID assignment)
    - PATTERNS.md §Compactor Dep Injection + §Nil-Safe Optional Dep
    - 69-02-PLAN.md (confirms `retrieval.MetaKeyLastCompactAt` is exported and reachable)
  </read_first>
  <action>
    0) Pre-check: `grep -rn '"github.com/agenthands/helix/internal/semantic/compact"' internal/semantic/retrieval/`. MUST return empty — Plan 69-02 must not have introduced a retrieval→compact import. If non-empty, STOP and report (this would create a cycle when 69-03 adds the reverse edge).

    1) `internal/semantic/compact/accessors.go`: add interface:

    `type BleveMeta interface { SetMeta(key string, val []byte) error }`

    Doc comment names it the single-writer seam for `last_compact_at`; states nil is a valid no-op so test deps may omit it.

    2) `internal/semantic/compact/compactor.go`:
    - Add `import "github.com/agenthands/helix/internal/semantic/retrieval"` (group with other `internal/semantic/*` imports). This introduces the deliberate compact→retrieval dependency — coupling is one constant, no behavior surface.
    - Add `BleveMeta BleveMeta` field to the `Deps` struct definition.
    - At the success path AFTER `c.lastBaseID = snap.ID` and BEFORE `c.maybeVacuum(ctx)` (compactor.go:332-336), insert:
      ```go
      if c.deps.BleveMeta != nil {
          _ = c.deps.BleveMeta.SetMeta(retrieval.MetaKeyLastCompactAt, []byte(strconv.FormatInt(c.now().UnixMilli(), 10)))
      }
      ```
    - Use the exported constant `retrieval.MetaKeyLastCompactAt` — NOT a string literal. This keeps writer (69-03) and reader (69-05) on a single token.
    - Add `strconv` import if absent.

    Commit: `feat(69-03): write last_compact_at meta on successful compaction (using retrieval.MetaKeyLastCompactAt)`.
  </action>
  <verify>
    <automated>go test ./internal/semantic/compact/ -count=1 -race -timeout 60s && grep -rn '"github.com/agenthands/helix/internal/semantic/compact"' internal/semantic/retrieval/ | grep -v '^$'</automated>
  </verify>
  <done>All 4 new tests + all existing compactor tests PASS under -race. `grep -n 'SetMeta(retrieval.MetaKeyLastCompactAt' internal/semantic/compact/compactor.go` returns exactly one match. `grep -rn 'SetMeta("last_compact_at"' internal/` returns ZERO matches (string literal eradicated). Reverse-import grep against `internal/semantic/retrieval/` returns empty (cycle absent). `go vet ./internal/semantic/compact/...` clean. Commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Wire compactBundle to accept a BleveMeta resolver — placeholder closure</name>
  <read_first>
    - `internal/daemon/compact_wiring.go` (full file — identify `compactBundle` struct definition + `ensureCompactor`)
    - PATTERNS.md §Compactor Dep Injection (File 3 — daemon/compact_wiring.go threading)
    - Existing analog of post-init wiring closure (RESEARCH §Architecture-Diagram + STATE.md mentions `SetEnrichFn` pattern)
  </read_first>
  <action>
    1) In `internal/daemon/compact_wiring.go`, extend `compactBundle` struct with `bleveMetaFn func(ws workspace.WorkspaceKey) compact.BleveMeta` (default zero-value nil). Document inline: "Resolves the per-workspace bleve engine as a compact.BleveMeta writer; bound by Plan 69-05 from semantic_wiring.go."

    2) Add a setter method `func (b *compactBundle) SetBleveMetaFn(fn func(workspace.WorkspaceKey) compact.BleveMeta)` with the same nil-safe shape used by Phase 64's `SetEnrichFn` pattern.

    3) In `ensureCompactor` at the `compact.NewCompactor(..., compact.Deps{...})` literal (compact_wiring.go:172-178), add a new field:
       ```
       BleveMeta: func() compact.BleveMeta {
           if b.bleveMetaFn == nil { return nil }
           return b.bleveMetaFn(ws)
       }(),
       ```
       (Resolve once at construction time; the per-workspace engine handle is stable for the compactor's lifetime — same pattern as `b.live`, `b.store`.)

    4) NO binding to the real `semanticBundle.engines[...]` happens here — that lives in Plan 69-05 (which owns semantic_wiring.go). This plan only opens the wiring slot.

    Commit: `feat(69-03): thread bleveMetaFn through compactBundle (binding deferred to 69-05)`.
  </action>
  <verify>
    <automated>go build ./internal/daemon/... && go test ./internal/daemon/... -run "TestCompact" -count=1</automated>
  </verify>
  <done>`compactBundle` has `bleveMetaFn` field + `SetBleveMetaFn` setter. `ensureCompactor` passes `BleveMeta:` into the Deps literal. Existing daemon tests still PASS. With bleveMetaFn nil (the only state in this plan), compactor's nil-safe guard means `last_compact_at` is NOT yet written from the daemon path — that is expected and closes when Plan 69-05 binds the resolver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compactor → bleve meta | Compactor stamps a single timestamp; no caller data, no symbol identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-02 | I | last_compact_at exposure | mitigate | unix-ms scalar; no embedded identity |
| T-69-03 | T / D | dep-injection nil-safety regression | mitigate | nil-safe guard at SetMeta call site; existing compactor_test.go Deps literals unchanged (Pitfall 5 guard) |
</threat_model>

<verification>
- `go test ./internal/semantic/compact/... ./internal/daemon/... -count=1 -race -timeout 60s` PASS
- `grep -rn 'SetMeta(retrieval.MetaKeyLastCompactAt' internal/` returns exactly one match (compactor.go)
- `grep -rn 'SetMeta("last_compact_at"' internal/` returns ZERO matches (literal eradicated)
- `grep -rn '"github.com/agenthands/helix/internal/semantic/compact"' internal/semantic/retrieval/` returns empty (cycle guard)
- `go vet ./internal/semantic/compact/... ./internal/daemon/...` clean
- compactBundle.bleveMetaFn binding slot exists; binding itself lives in Plan 69-05
</verification>

<success_criteria>
- BleveMeta interface lands on compact package
- Compactor writes last_compact_at on success path (nil-safe) using `retrieval.MetaKeyLastCompactAt` — no string literal anywhere
- New compact→retrieval import verified acyclic
- compactBundle exposes a bleveMetaFn setter ready for Plan 69-05 to bind
- All existing compactor + daemon tests still pass
</success_criteria>

<output>
After completion, write `.planning/phases/69-production-status-accessors/69-03-SUMMARY.md` capturing: whether the per-workspace engine handle stability assumption held (re-resolve per call vs once-at-construction decision), any change to compactor_test.go fixture authors, and confirmation that the cycle-guard grep ran clean.
</output>
