---
phase: 69
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/skill/semantic/envelope.go
  - internal/skill/semantic/accessors.go
  - internal/skill/semantic/envelope_test.go
autonomous: true
requirements: [STATUS-01, STATUS-02]
tags: [semantic-graph, status, envelope, closed-enum]
must_haves:
  truths:
    - "ClusterStatus struct grows two additive fields (ComputedAt int64, MemberCount int) — both omitempty — without breaking existing JSON keys (state, reason)"
    - "RetrievalStatus struct exists with five fields: CorpusVersion uint64, IndexedFiles int64, IndexedSymbols int64, LastCompactAt int64, Reason string omitempty"
    - "StatusResult gains nested RetrievalStatus field with JSON tag retrieval_status — RetrievalPending bool remains at top level (Phase 64 contract preserved)"
    - "RetrievalAccessor interface grows RetrievalStatus(ws workspace.WorkspaceKey) RetrievalStatus method"
    - "Closed-enum Reason values documented: '' | bleve-unavailable | corpus_version-uninitialized | corpus_version-lag | compactor-never-ran (priority order documented)"
    - "JSON round-trip preserves all fields with deterministic key order"
  artifacts:
    - path: "internal/skill/semantic/envelope.go"
      provides: "Extended ClusterStatus; new RetrievalStatus type; StatusResult.RetrievalStatus field"
      contains: "type RetrievalStatus struct"
    - path: "internal/skill/semantic/accessors.go"
      provides: "RetrievalAccessor.RetrievalStatus method signature"
      contains: "RetrievalStatus(ws workspace.WorkspaceKey)"
    - path: "internal/skill/semantic/envelope_test.go"
      provides: "Round-trip JSON tests for new shape + closed-enum reason coverage"
      contains: "TestRetrievalStatus_JSONShape"
  key_links:
    - from: "envelope.go ClusterStatus"
      to: "tools_status.go handler composition"
      via: "additive fields surface in handler return"
      pattern: "ClusterStatus{State:.*ComputedAt:"
    - from: "accessors.go RetrievalAccessor"
      to: "semantic_wiring.go semRetrievalAdapter (Plan 69-05)"
      via: "interface method that adapter implements"
      pattern: "RetrievalStatus.*workspace.WorkspaceKey"
---

<objective>
Extend the SPEC §23.3 envelope additively to carry real cluster + retrieval state, and grow the `RetrievalAccessor` interface with a `RetrievalStatus(ws)` method that Plan 69-05's adapter implements.

Purpose: STATUS-01 + STATUS-02 require additive struct extensions (CONTEXT.md D2 + D4). Closed-enum stability is mandatory — existing JSON keys must not change, and the `Reason` field on the new `RetrievalStatus` must use a deterministic priority-ordered closed set (RESEARCH Q6 + Open-Q2).

Output: Two additive fields on `ClusterStatus`, one new `RetrievalStatus` struct, one new field on `StatusResult`, one new method on `RetrievalAccessor`, plus round-trip JSON tests + closed-enum reason coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-production-status-accessors/69-CONTEXT.md
@.planning/phases/69-production-status-accessors/69-RESEARCH.md
@.planning/phases/69-production-status-accessors/69-PATTERNS.md

@internal/skill/semantic/envelope.go
@internal/skill/semantic/accessors.go

<interfaces>
Existing closed-enum struct (envelope.go:67-74):

```go
type ClusterStatus struct {
    State  string `json:"state"`            // "unknown" | "current" | "stale" | "building" (building DEFERRED in Phase 69)
    Reason string `json:"reason,omitempty"`
}
```

Existing StatusResult (envelope.go:104-112):

```go
type StatusResult struct {
    CommonEnvelope
    LatestSnapshotID uint64            `json:"latest_snapshot_id"`
    ScoreStatus      map[string]string `json:"score_status"`
    ClusterStatus    ClusterStatus     `json:"cluster_status"`
    LastLiveUpdateMs int64             `json:"last_live_update_ms"`
    PendingLSPFiles  int               `json:"pending_lsp_files"`
    RetrievalPending bool              `json:"retrieval_pending"`     // PHASE 64 CONTRACT — KEEP AT TOP LEVEL
}
```

Existing RetrievalAccessor (accessors.go:97-111) — has QueryBleve, PersonalizedPageRank, RetrievalPending, TopEdgesFor. Add ONE method.

Closed-enum RetrievalStatus.Reason set (RESEARCH Q6) with priority order (RESEARCH Open-Q2):
- bleve-unavailable        (highest priority — engine missing entirely)
- corpus_version-uninitialized   (Recoverer never ran successfully)
- corpus_version-lag       (bleve.corpus_version < store.CurrentGraphVersion)
- compactor-never-ran      (last_compact_at meta absent)
- ""                       (lowest — all fields consistent / no degradation)

Pitfall 4 (RESEARCH): omitempty on int64 zero — tests must assert non-zero rather than presence-only.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — envelope shape + closed-enum reason coverage tests</name>
  <read_first>
    - Existing `internal/skill/semantic/envelope_test.go` (if absent, create it; check via `ls` first)
    - PATTERNS.md §Envelope Extension + §Closed-Enum Reason Strings
  </read_first>
  <behavior>
    - Test 1 (ClusterStatus additive JSON): marshal `ClusterStatus{State:"current", ComputedAt:1715000000000, MemberCount:5}` → JSON contains `"state":"current"`, `"computed_at":1715000000000`, `"member_count":5`, no `"reason"` key (omitempty)
    - Test 2 (ClusterStatus backward compat): marshal `ClusterStatus{State:"unknown", Reason:"no-graph-version"}` → produces same JSON as today (no `computed_at` / `member_count` keys present due to omitempty zero values)
    - Test 3 (RetrievalStatus full): marshal `RetrievalStatus{CorpusVersion:7, IndexedFiles:42, IndexedSymbols:123, LastCompactAt:1715000000000}` → all four fields present, reason absent (omitempty empty string)
    - Test 4 (RetrievalStatus reason set, table-driven): each of the 5 closed-enum reason values round-trips correctly via Marshal/Unmarshal
    - Test 5 (StatusResult shape): marshal a fully populated StatusResult; assert `retrieval_status` is a nested object AND `retrieval_pending` is at top level (NOT inside retrieval_status block) — Phase 64 contract regression guard
    - Test 6 (RetrievalAccessor interface satisfaction): compile-time assertion via `var _ RetrievalAccessor = (*fakeRetrievalForTest)(nil)` where fakeRetrievalForTest stubs all 5 methods including the new RetrievalStatus(ws) one
  </behavior>
  <action>
    Create or append to `internal/skill/semantic/envelope_test.go`. Use `testify/require` already in dep tree. Tests MUST fail compilation: `ComputedAt`, `MemberCount` fields don't exist on ClusterStatus; `RetrievalStatus` type doesn't exist; `StatusResult.RetrievalStatus` field doesn't exist; `RetrievalAccessor.RetrievalStatus` method missing from interface.

    Commit: `test(69-04): RED envelope additive fields + RetrievalStatus closed-enum`.
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/ -run "TestClusterStatus|TestRetrievalStatus|TestStatusResult" -count=1 2>&1 | grep -E "undefined|no field|cannot|undeclared"</automated>
  </verify>
  <done>All 6 tests compile-fail. Commit landed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — additive envelope fields + RetrievalAccessor.RetrievalStatus</name>
  <read_first>
    - `internal/skill/semantic/envelope.go:67-112` (current struct definitions to extend)
    - `internal/skill/semantic/accessors.go:97-111` (current RetrievalAccessor)
    - PATTERNS.md §Envelope Extension + §Accessor Interface Extension
  </read_first>
  <action>
    1) `envelope.go`:

    Extend `ClusterStatus` with two omitempty fields preserving existing JSON keys:
    - `ComputedAt int64 json:"computed_at,omitempty"`   (unix ms from semantic_clusters.computed_at; per CONTEXT.md D4)
    - `MemberCount int json:"member_count,omitempty"`    (COUNT(*) over semantic_cluster_members)

    Add doc-comment line on State noting `"building"` is reserved for a future phase (Phase 69 emits only `unknown` / `current` / `stale`).

    Declare new `RetrievalStatus` struct (CONTEXT.md D2 exact shape):
    ```
    type RetrievalStatus struct {
        CorpusVersion  uint64 `json:"corpus_version"`
        IndexedFiles   int64  `json:"indexed_files"`
        IndexedSymbols int64  `json:"indexed_symbols"`
        LastCompactAt  int64  `json:"last_compact_at"`
        Reason         string `json:"reason,omitempty"`
    }
    ```
    Doc-comment enumerates the closed-enum Reason values with priority order: `bleve-unavailable` > `corpus_version-uninitialized` > `corpus_version-lag` > `compactor-never-ran` > `""`.

    Extend `StatusResult` with `RetrievalStatus RetrievalStatus json:"retrieval_status"` (NEW field). KEEP `RetrievalPending bool` at top level unchanged (Phase 64 consumer contract — RESEARCH STATE.md note Plan 06 + 07 chain depend on it).

    2) `accessors.go`:

    Add to `RetrievalAccessor` interface:
    ```
    // RetrievalStatus returns the bleve-corpus state for the given workspace.
    // Closed-enum Reason set (priority order): bleve-unavailable > corpus_version-uninitialized > corpus_version-lag > compactor-never-ran > "".
    RetrievalStatus(ws workspace.WorkspaceKey) RetrievalStatus
    ```

    Be aware: this is an interface-breaking change. ALL existing implementations of `RetrievalAccessor` will fail to compile (including `e2eRetrievalAcc` in integration_test.go:275-289 and `semRetrievalAdapter` in semantic_wiring.go). Plan 69-05 closes both. This plan accepts the broken build between Task 2 GREEN and Plan 69-05's wiring — note this explicitly in the commit message.

    Commit: `feat(69-04): additive envelope fields + RetrievalAccessor.RetrievalStatus (breaks adapter compile until 69-05)`.
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/ -run "TestClusterStatus|TestRetrievalStatus|TestStatusResult" -count=1</automated>
  </verify>
  <done>The 6 envelope-only tests in `internal/skill/semantic/` PASS. Note the package-wide build (and `internal/daemon/...` build) will be RED until Plan 69-05 lands the adapter — this is expected and intentional within Wave 1 isolation; do not "fix" by adding stubs in tools_status.go (that file lives in Plan 69-05's purview). Commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP response → client | Envelope carries counts + versions only; no symbol-level identifiers (T-69-01 mitigation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | I | RetrievalStatus.IndexedSymbols / IndexedFiles counts | mitigate | Pure cardinalities — no file paths, no symbol names, no labels |
| T-69-02 | I | corpus_version exposed across MCP | mitigate | Closed-enum struct is per-workspace scope (driven by ws.WorkspaceKey downstream); no cross-workspace leakage path |
</threat_model>

<verification>
- `go test ./internal/skill/semantic/ -run "TestClusterStatus|TestRetrievalStatus|TestStatusResult" -count=1` PASS
- Package-wide compile is intentionally RED between this plan and 69-05 (interface-breaking change to RetrievalAccessor) — documented in commit
- `gofmt -l internal/skill/semantic/envelope.go internal/skill/semantic/accessors.go` empty
</verification>

<success_criteria>
- ClusterStatus carries ComputedAt + MemberCount with omitempty preserving backward compat
- RetrievalStatus struct exists with the five exact fields + closed-enum Reason doc
- StatusResult nests retrieval_status, retains top-level retrieval_pending
- RetrievalAccessor interface has 5 methods; existing implementers known-broken pending Plan 69-05
- JSON round-trip + closed-enum table tests all PASS
</success_criteria>

<output>
After completion, write `.planning/phases/69-production-status-accessors/69-04-SUMMARY.md` capturing: the priority-order doc comment final text, and explicit confirmation that integration_test.go + semantic_wiring.go are now known-broken (Plan 69-05 work).
</output>
