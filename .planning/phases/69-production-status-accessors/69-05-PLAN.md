---
phase: 69
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - internal/daemon/semantic_wiring.go
  - internal/daemon/semantic_accessor_factories.go
  - internal/skill/semantic/integration_test.go
autonomous: true
requirements: [STATUS-01, STATUS-02]
tags: [semantic-graph, status, adapter-wiring, placeholder-removal, constructor-extraction]
must_haves:
  truths:
    - "semSchedulerAdapter.ClusterStatus reads via a.store.CurrentGraphVersion + ClusterStatusForGraphVersion and emits {current | stale | unknown} per CONTEXT D1 (stale derived from row.IsCurrent==false && ClusterCount > 0)"
    - "ClusterStatus returns {State:current, ComputedAt, MemberCount} for current-gv rows; {State:stale, ComputedAt, MemberCount, Reason:graph_version-lag} when accessor returns IsCurrent=false with rows; {State:unknown, Reason:<scoped>} on no-store / no-graph-version / accessor-error / no-cluster-rows"
    - "semRetrievalAdapter.RetrievalStatus uses retrieval.MetaKeyCorpusVersion / MetaKeyIndexedFiles / MetaKeyLastCompactAt (exported by 69-02) — no string literals, no re-export hedge"
    - "RetrievalStatus reason priority order respected: bleve-unavailable > corpus_version-uninitialized > corpus_version-lag > compactor-never-ran"
    - "compactBundle.bleveMetaFn bound to a closure that resolves semanticBundle.engines[ws.RepoRoot] as compact.BleveMeta"
    - "All three placeholder comment blocks at semantic_wiring.go:407-409 / 433-443 / 450-455 removed"
    - "Exported constructors NewSchedulerAccessorForStore + NewRetrievalAccessorForStore land in a new internal/daemon/semantic_accessor_factories.go so 69-06 E2E test can instantiate the REAL adapter code path without test-local re-implementation (revision Warning 3 — preferred approach)"
    - "Daemon's own semSchedulerAdapter / semRetrievalAdapter delegate to these factories — single implementation, two call sites"
    - "e2eRetrievalAcc test fake grows RetrievalStatus stub method so package compiles"
    - "Grep audit: zero occurrences of 'phase-62-clustering-no-status-accessor' across the repo"
  artifacts:
    - path: "internal/daemon/semantic_wiring.go"
      provides: "Real ClusterStatus + RetrievalStatus implementations (thin delegations to the factories); placeholder removal; compactBundle binding"
      contains: "ClusterStatusForGraphVersion"
    - path: "internal/daemon/semantic_accessor_factories.go"
      provides: "NewSchedulerAccessorForStore(store *semanticstore.Store) semantic.SchedulerAccessor + NewRetrievalAccessorForStore(store *semanticstore.Store, eng *retrieval.Engine) semantic.RetrievalAccessor — single source-of-truth for cluster/retrieval status logic, exercised by both daemon adapters AND Plan 69-06 E2E test"
      contains: "func NewSchedulerAccessorForStore"
    - path: "internal/skill/semantic/integration_test.go"
      provides: "e2eRetrievalAcc gains RetrievalStatus stub method for compile"
      contains: "func (a *e2eRetrievalAcc) RetrievalStatus"
  key_links:
    - from: "semSchedulerAdapter.ClusterStatus"
      to: "*Store.ClusterStatusForGraphVersion"
      via: "factory-produced accessor"
      pattern: "ClusterStatusForGraphVersion"
    - from: "semRetrievalAdapter.RetrievalStatus"
      to: "*retrieval.Engine.GetMeta + DocCount"
      via: "factory-produced accessor; reads via retrieval.MetaKey* exported constants"
      pattern: "retrieval\\.MetaKeyCorpusVersion"
    - from: "compactBundle.bleveMetaFn"
      to: "semanticBundle.engines[ws.RepoRoot]"
      via: "post-init wiring closure"
      pattern: "SetBleveMetaFn"
---

<objective>
Replace the three Phase 64 W1 placeholders in `semantic_wiring.go` (lines 407-409, 433-443, 450-455) with real `*Store`- and `*Engine`-backed accessor implementations; bind `compactBundle.bleveMetaFn` to the production engine resolver; extract the cluster/retrieval status logic into two exported factory constructors so Plan 69-06's E2E test exercises the SAME code path the daemon does; and extend the `e2eRetrievalAcc` test fake so the package compiles after Plan 69-04's interface change.

Purpose: This is the wiring plan that closes both Phase 64 W1 production placeholders. It depends on Plans 69-01..69-04 having landed their underlying surfaces. It also restores the package build that 69-04 intentionally broke. Per revision feedback (Warning 3), the cluster/retrieval status logic is factored into exported constructors — the daemon's own adapters and the 69-06 E2E test both call these constructors, so the test covers the real adapter code (not a re-implemented shim).

Output: Updated `semSchedulerAdapter.ClusterStatus` body (thin delegation); new `semRetrievalAdapter.RetrievalStatus` method (thin delegation); new file `semantic_accessor_factories.go` with the two constructors; new `bleveMetaResolver` closure construction; placeholder comment removal; stub method on `e2eRetrievalAcc`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-production-status-accessors/69-CONTEXT.md
@.planning/phases/69-production-status-accessors/69-RESEARCH.md
@.planning/phases/69-production-status-accessors/69-PATTERNS.md
@.planning/phases/69-production-status-accessors/69-01-PLAN.md
@.planning/phases/69-production-status-accessors/69-02-PLAN.md
@.planning/phases/69-production-status-accessors/69-03-PLAN.md
@.planning/phases/69-production-status-accessors/69-04-PLAN.md

@internal/daemon/semantic_wiring.go
@internal/skill/semantic/integration_test.go

<interfaces>
Three placeholder sites in semantic_wiring.go:
- Lines 407-409: doc comment on `semSchedulerAdapter` referencing W1 placeholders — REWRITE
- Lines 433-443: `ScoreStatus` "deliberate companion" comment — REWRITE to drop the W1 framing (ScoreStatus stays graph.ScoreStatusMissing per RESEARCH Q9; only the comment needs the W1 narrative removed)
- Lines 450-462: `ClusterStatus` method body — REPLACE the placeholder body with a delegation to the factory

**Cluster state derivation (CONTEXT D1 — three reachable states for Phase 69):**
- `a == nil || a.store == nil` → `{State:"unknown", Reason:"no-store"}`
- `gv, err := a.store.CurrentGraphVersion(ctx, repoID)`; err != nil OR gv == 0 → `{State:"unknown", Reason:"no-graph-version"}`
- `row, err := a.store.ClusterStatusForGraphVersion(ctx, repoID, gv)`; err != nil → `{State:"unknown", Reason:"accessor-error"}`
- `row.ClusterCount == 0` → `{State:"unknown", Reason:"no-cluster-rows"}`
- `row.ClusterCount > 0 && row.IsCurrent` → `{State:"current", ComputedAt:row.ComputedAt, MemberCount:row.MemberCount}`
- `row.ClusterCount > 0 && !row.IsCurrent` → `{State:"stale", ComputedAt:row.ComputedAt, MemberCount:row.MemberCount, Reason:"graph_version-lag"}` (revision Blocker 2 — Approach A)
- `"building"` state remains DEFERRED (no signal source today; see Plan 69-01 deferred block)

**RetrievalStatus reason priority order (RESEARCH Open-Q2):**
1. engine == nil → `{Reason:"bleve-unavailable"}` (all other fields zero)
2. `engine.GetMeta(retrieval.MetaKeyCorpusVersion)` returns nil/empty bytes → `{Reason:"corpus_version-uninitialized"}`
3. Parse corpus_version uint64; if `store.CurrentGraphVersion(ctx, repoID)` returns gv with parsed < gv → fields populated AND `Reason:"corpus_version-lag"`
4. `engine.GetMeta(retrieval.MetaKeyLastCompactAt)` returns nil/empty bytes → `Reason:"compactor-never-ran"`
5. Else `Reason:""`

All meta reads use the exported constants `retrieval.MetaKeyCorpusVersion` / `retrieval.MetaKeyIndexedFiles` / `retrieval.MetaKeyLastCompactAt` (landed by Plan 69-02). NO string literals; NO re-export hedge.

**Exported factory constructors (revision Warning 3 preferred approach):**

New file `internal/daemon/semantic_accessor_factories.go` exports:

```go
// Returns a production semantic.SchedulerAccessor backed by *Store.
// Used by the daemon's semSchedulerAdapter AND the Plan 69-06 E2E test —
// single implementation, two call sites.
func NewSchedulerAccessorForStore(store *semanticstore.Store) semantic.SchedulerAccessor

// Returns a status-focused semantic.RetrievalAccessor backed by *Store + *Engine.
// QueryBleve / PersonalizedPageRank / RetrievalPending / TopEdgesFor are intentionally
// minimal — this constructor is for the STATUS surface only; daemon production query
// paths continue to use the full semRetrievalAdapter. Documented carve-out.
func NewRetrievalAccessorForStore(store *semanticstore.Store, eng *retrieval.Engine) semantic.RetrievalAccessor
```

Daemon adapter delegation pattern:

```go
func (a *semSchedulerAdapter) ClusterStatus(repoID string) semantic.ClusterStatus {
    return NewSchedulerAccessorForStore(a.store).ClusterStatus(repoID)
}

func (a *semRetrievalAdapter) RetrievalStatus(ws workspace.WorkspaceKey) semantic.RetrievalStatus {
    return NewRetrievalAccessorForStore(a.bundle.store, a.engineFor(ws)).RetrievalStatus(ws)
}
```

Constructing a new accessor per call is acceptable — struct is two pointers, escape-analysis-friendly. Plan 69-06 instantiates ONCE; the daemon instantiates per-call. Both exercise the identical underlying logic.

compactBundle binding closure (place near `SetEnrichFn` / `SetActivateCallback`):

```go
b.compactBundle.SetBleveMetaFn(func(ws workspace.WorkspaceKey) compact.BleveMeta {
    b.semanticBundle.mu.Lock()
    defer b.semanticBundle.mu.Unlock()
    eng, ok := b.semanticBundle.engines[ws.RepoRoot]
    if !ok || eng == nil { return nil }
    return eng
})
```

`*retrieval.Engine` satisfies `compact.BleveMeta` via existing `SetMeta(string, []byte) error` — confirm via `var _ compact.BleveMeta = (*retrieval.Engine)(nil)` (place in semantic_accessor_factories.go).

Test fake extension (integration_test.go:275-289) — `e2eRetrievalAcc`:

```go
func (a *e2eRetrievalAcc) RetrievalStatus(_ workspace.WorkspaceKey) RetrievalStatus { return RetrievalStatus{} }
```

(Compile-stub only; Plan 69-06 instantiates the real factory and does not exercise this stub.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land semantic_accessor_factories.go and migrate daemon adapters to delegate</name>
  <read_first>
    - `internal/daemon/semantic_wiring.go:395-465` AND :580-640 (full adapter regions — confirm exact line numbers)
    - `.planning/phases/69-production-status-accessors/69-01-PLAN.md` — final `ClusterStatusForGraphVersion` signature including `IsCurrent` / `ActualGraphVersion` fields (stale-fallback)
    - `.planning/phases/69-production-status-accessors/69-02-PLAN.md` — final exported `retrieval.MetaKey*` constant names
    - PATTERNS.md §Adapter Replacement
    - RESEARCH §Code Examples §State Derivation + Open-Q2 priority order
  </read_first>
  <action>
    1) Create new file `internal/daemon/semantic_accessor_factories.go`:
       - Package `daemon`.
       - Imports: `context`, `strconv`, `internal/semantic/retrieval`, `internal/semantic/store` aliased `semanticstore`, `internal/semantic/compact`, `internal/skill/semantic`, `internal/workspace`, `internal/graph`.
       - `type schedulerAccessorImpl struct { store *semanticstore.Store }` with three methods:
         - `IsQuiescent(_ string) bool { return true }`
         - `ScoreStatus(_, _ string) graph.ScoreStatus { return graph.ScoreStatusMissing }`  (RESEARCH Q9 — pending future phase)
         - `ClusterStatus(repoID string) semantic.ClusterStatus` — full derivation per the interfaces block (including the **stale** branch via `row.IsCurrent==false`).
       - `type retrievalAccessorImpl struct { store *semanticstore.Store; engine *retrieval.Engine }` — STATUS-ONLY accessor. Documented carve-out at file level: "Non-status methods (QueryBleve / PersonalizedPageRank / RetrievalPending / TopEdgesFor) return zero values and MUST NOT be used for query paths; the daemon's semRetrievalAdapter retains the production implementations. This impl exists so daemon AND Plan 69-06 E2E share a single ClusterStatus/RetrievalStatus code path." Stub the four non-status methods returning zero values.
       - Implement `(*retrievalAccessorImpl).RetrievalStatus(ws workspace.WorkspaceKey) semantic.RetrievalStatus` using `retrieval.MetaKeyCorpusVersion` / `MetaKeyIndexedFiles` / `MetaKeyLastCompactAt` constants directly. Apply the priority-order reason logic.
       - Export `NewSchedulerAccessorForStore(store *semanticstore.Store) semantic.SchedulerAccessor` and `NewRetrievalAccessorForStore(store *semanticstore.Store, eng *retrieval.Engine) semantic.RetrievalAccessor`.
       - Add compile-time assertion: `var _ compact.BleveMeta = (*retrieval.Engine)(nil)`.

    2) `semantic_wiring.go`:
       - `semSchedulerAdapter` struct: add `store *semanticstore.Store` field (wire at construction; grep `semSchedulerAdapter{` in this file).
       - Replace `ClusterStatus` body with: `return NewSchedulerAccessorForStore(a.store).ClusterStatus(repoID)`.
       - Add `semRetrievalAdapter.RetrievalStatus` method: `return NewRetrievalAccessorForStore(a.bundle.store, a.engineFor(ws)).RetrievalStatus(ws)`. Verify `a.bundle.store` reachable; thread in if needed.
       - Remove the W1 doc comments at lines 407-409 / 433-443 / 450-455 — replace with short factual comments referencing the new factory constructors.
       - ScoreStatus body stays `return graph.ScoreStatusMissing`. The comment loses the "deliberate companion to the W1 ClusterStatus placeholder" framing — say only "ScoreStatus pending Phase 65/67 per-projection accessor (RESEARCH Q9)."

    Commit: `feat(69-05): extract status-accessor factories + migrate daemon adapters + remove W1 placeholders`.
  </action>
  <verify>
    <automated>go build ./internal/daemon/... && grep -nE 'phase-62-clustering-no-status-accessor|W1 placeholder|W1 production-layer placeholder|deliberate companion' internal/daemon/semantic_wiring.go</automated>
  </verify>
  <done>Build succeeds. Grep returns ZERO matches for the four forbidden tokens. `go vet ./internal/daemon/...` clean. Factories file exports both constructors and contains the `compact.BleveMeta` compile assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Bind compactBundle.bleveMetaFn + extend e2eRetrievalAcc + close repo-wide build break</name>
  <read_first>
    - Daemon bootstrap site (grep `SetEnrichFn|SetActivateCallback` for the canonical post-init pattern)
    - `internal/skill/semantic/integration_test.go:275-289` (e2eRetrievalAcc — extend with stub)
    - PATTERNS.md §Compactor Dep Injection (File 3)
  </read_first>
  <action>
    1) Bind `compactBundle.bleveMetaFn` — AFTER both bundles exist, in the same post-init region as `SetEnrichFn`:

    ```go
    b.compactBundle.SetBleveMetaFn(func(ws workspace.WorkspaceKey) compact.BleveMeta {
        b.semanticBundle.mu.Lock()
        defer b.semanticBundle.mu.Unlock()
        eng, ok := b.semanticBundle.engines[ws.RepoRoot]
        if !ok || eng == nil { return nil }
        return eng
    })
    ```

    2) `internal/skill/semantic/integration_test.go` — append to `e2eRetrievalAcc`:

    ```go
    func (a *e2eRetrievalAcc) RetrievalStatus(_ workspace.WorkspaceKey) RetrievalStatus { return RetrievalStatus{} }
    ```

    This is a COMPILE STUB ONLY. Plan 69-06 will instantiate `daemon.NewRetrievalAccessorForStore` directly (bypassing this stub) so the test exercises the real factory code path.

    Commit: `feat(69-05): bind bleveMetaFn + restore test compile via e2eRetrievalAcc stub`.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/skill/semantic/ ./internal/daemon/ -count=1 -race -timeout 90s</automated>
  </verify>
  <done>Whole-repo `go build ./...` succeeds (closes the 69-04 intentional break). Existing daemon + semantic skill tests PASS. `grep -rn 'phase-62-clustering-no-status-accessor' internal/` returns ZERO matches across the repo. Commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon adapter → MCP response | Adapter only forwards counts + versions; no caller-supplied data, no PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | I | Response composition | mitigate | Adapter passes through accessor outputs verbatim (no identifiers) |
| T-69-02 | I | Cross-workspace fingerprinting via corpus_version | mitigate | `engineFor(ws)` scopes by ws.RepoRoot; factory takes a per-workspace engine |
| T-69-03 | T / D | D-09 read-path on adapter | mitigate | Adapter calls only the new `*Store` read methods + bleve GetMeta/DocCount; grep audit `grep -nE 'Begin(Snapshot\|OverlayTx)\|Commit(Snapshot)?\|WriteSnapshotFacts' internal/daemon/semantic_wiring.go internal/daemon/semantic_accessor_factories.go` shows no additions |
</threat_model>

<verification>
- `go build ./...` clean across the entire repo
- `go test ./internal/skill/semantic/ ./internal/daemon/ -count=1 -race -timeout 90s` PASS
- `grep -rn 'phase-62-clustering-no-status-accessor' internal/` returns ZERO matches
- `grep -nE 'W1 placeholder|W1 production-layer placeholder|deliberate companion' internal/daemon/semantic_wiring.go` returns ZERO matches
- `grep -n 'NewSchedulerAccessorForStore\|NewRetrievalAccessorForStore' internal/daemon/semantic_accessor_factories.go` returns both definitions
- `grep -rn 'GetMeta("corpus_version"\|GetMeta("indexed_files"\|GetMeta("last_compact_at"' internal/daemon/` returns ZERO matches (constants only)
- `go vet ./...` clean
</verification>

<success_criteria>
- All three placeholder comment blocks removed (success criterion #5 from ROADMAP Phase 69)
- ClusterStatus returns `current` / `stale` / `unknown` per CONTEXT D1 (stale via row.IsCurrent==false branch)
- RetrievalStatus reads via exported `retrieval.MetaKey*` constants only — no hedge
- Exported factory constructors land — daemon AND 69-06 E2E share the same code path
- bleveMetaFn binding closes the Plan 69-03 wiring slot
- Repo-wide build green; no regression in existing tests
</success_criteria>

<output>
After completion, write `.planning/phases/69-production-status-accessors/69-05-SUMMARY.md` capturing: the final factory file layout, the exact bootstrap insertion point for SetBleveMetaFn, the carve-out comment text for the retrievalAccessorImpl non-status methods, and any deviation from the planned RetrievalStatus reason priority logic.
</output>
