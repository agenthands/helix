---
phase: 69
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - internal/daemon/semantic_wiring.go
  - internal/skill/semantic/integration_test.go
autonomous: true
requirements: [STATUS-01, STATUS-02]
tags: [semantic-graph, status, adapter-wiring, placeholder-removal]
must_haves:
  truths:
    - "semSchedulerAdapter.ClusterStatus reads via a.bundle.store.CurrentGraphVersion + ClusterStatusForGraphVersion and translates to envelope shape"
    - "ClusterStatus returns {State:current, ComputedAt, MemberCount} when rows exist for current gv; {State:unknown, Reason:no-graph-version|no-cluster-rows|accessor-error|no-store} otherwise"
    - "semRetrievalAdapter has RetrievalStatus(ws) method reading corpus_version + indexed_files + last_compact_at via engine.GetMeta + DocCount for indexed_symbols"
    - "RetrievalStatus reason priority order respected: bleve-unavailable > corpus_version-uninitialized > corpus_version-lag > compactor-never-ran"
    - "compactBundle.bleveMetaFn bound to a closure that resolves semanticBundle.engines[ws.RepoRoot] as compact.BleveMeta"
    - "All three placeholder comment blocks at semantic_wiring.go:407-409 / 433-443 / 450-455 removed"
    - "e2eRetrievalAcc test fake grows RetrievalStatus stub method so package compiles"
    - "Grep audit: zero occurrences of 'phase-62-clustering-no-status-accessor' across the repo"
  artifacts:
    - path: "internal/daemon/semantic_wiring.go"
      provides: "Real ClusterStatus + RetrievalStatus implementations; placeholder removal; compactBundle binding"
      contains: "ClusterStatusForGraphVersion"
    - path: "internal/skill/semantic/integration_test.go"
      provides: "e2eRetrievalAcc gains RetrievalStatus stub method for compile"
      contains: "func (a *e2eRetrievalAcc) RetrievalStatus"
  key_links:
    - from: "semSchedulerAdapter.ClusterStatus"
      to: "*Store.ClusterStatusForGraphVersion"
      via: "a.bundle.store accessor call"
      pattern: "ClusterStatusForGraphVersion"
    - from: "semRetrievalAdapter.RetrievalStatus"
      to: "*retrieval.Engine.GetMeta + DocCount"
      via: "a.engineFor(ws) lookup"
      pattern: "engine.GetMeta.*corpus_version"
    - from: "compactBundle.bleveMetaFn"
      to: "semanticBundle.engines[ws.RepoRoot]"
      via: "post-init wiring closure"
      pattern: "SetBleveMetaFn"
---

<objective>
Replace the three Phase 64 W1 placeholders in `semantic_wiring.go` (lines 407-409, 433-443, 450-455) with real `*Store`- and `*Engine`-backed accessor implementations; bind `compactBundle.bleveMetaFn` to the production engine resolver; and extend the `e2eRetrievalAcc` test fake so the package compiles after Plan 69-04's interface change.

Purpose: This is the wiring plan that closes both Phase 64 W1 production placeholders. It depends on Plans 69-01..69-04 having landed their underlying surfaces. It also restores the package build that 69-04 intentionally broke.

Output: Updated `semSchedulerAdapter.ClusterStatus` body; new `semRetrievalAdapter.RetrievalStatus` method; new `bleveMetaResolver` closure construction; placeholder comment removal; stub method on `e2eRetrievalAcc`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-production-status-accessors/69-CONTEXT.md
@.planning/phases/69-production-status-accessors/69-RESEARCH.md
@.planning/phases/69-production-status-accessors/69-PATTERNS.md
@.planning/phases/69-production-status-accessors/69-01-PLAN.md
@.planning/phases/69-production-status-accessors/69-02-PLAN.md
@.planning/phases/69-production-status-accessors/69-03-PLAN.md
@.planning/phases/69-production-status-accessors/69-04-PLAN.md

@internal/daemon/semantic_wiring.go
@internal/skill/semantic/integration_test.go

<interfaces>
Three placeholder sites in semantic_wiring.go:
- Lines 407-409: doc comment on `semSchedulerAdapter` referencing W1 placeholders — REWRITE to describe real wiring
- Lines 433-443: `ScoreStatus` "deliberate companion" comment — REWRITE to drop the W1 framing (ScoreStatus stays graph.ScoreStatusMissing per RESEARCH Q9; only the comment needs the W1 narrative removed)
- Lines 450-462: `ClusterStatus` method body — REPLACE the placeholder body

`semSchedulerAdapter` currently holds `rb *rankBundle` (line 411). It needs read access to `*Store`. Add a `store *semanticstore.Store` field (or thread via a `*semanticBundle` reference if cleaner; the canonical pattern in this file is `semStoreAdapter.store` at line 398-405).

`semRetrievalAdapter.engineFor(ws)` already exists (semantic_wiring.go:588-595). The new `RetrievalStatus(ws)` method uses it to grab `*retrieval.Engine`.

Reason priority order (RESEARCH Open-Q2) — implement in this exact order:
1. engine == nil → `{Reason:"bleve-unavailable"}` (all other fields zero)
2. `corpus_version` GetMeta returns nil bytes → `{Reason:"corpus_version-uninitialized"}`
3. Read corpus_version parsed uint64; compare to `store.CurrentGraphVersion`; if parsed < currentGv → fields populated AND `Reason:"corpus_version-lag"`
4. `last_compact_at` GetMeta returns nil bytes → `Reason:"compactor-never-ran"` (only if priorities 1-3 did not fire)
5. Else `Reason:""` (omitted by JSON)

compactBundle binding: In the daemon bootstrap path that constructs the semantic + compact bundles (locate via grep `SetEnrichFn\|SetActivateCallback` for the canonical post-init pattern), add a call:
```
b.compactBundle.SetBleveMetaFn(func(ws workspace.WorkspaceKey) compact.BleveMeta {
    b.semanticBundle.mu.Lock()
    defer b.semanticBundle.mu.Unlock()
    eng, ok := b.semanticBundle.engines[ws.RepoRoot]
    if !ok || eng == nil { return nil }
    return eng  // *retrieval.Engine satisfies compact.BleveMeta via existing SetMeta(string, []byte) error
})
```

The `*retrieval.Engine` already has `SetMeta(key string, val []byte) error` (bleve.go:189-202) — structural-typing the `compact.BleveMeta` interface. Verify via `var _ compact.BleveMeta = (*retrieval.Engine)(nil)` compile-time assertion (place in semantic_wiring.go).

Test fake extension (integration_test.go:275-289) — `e2eRetrievalAcc` needs:
```
func (a *e2eRetrievalAcc) RetrievalStatus(_ workspace.WorkspaceKey) RetrievalStatus {
    return RetrievalStatus{} // empty; Plan 69-06 swaps this for a real-engine-backed variant
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite semSchedulerAdapter.ClusterStatus + remove first two placeholder comment blocks</name>
  <read_first>
    - `internal/daemon/semantic_wiring.go:395-465` (full adapter region — confirm exact line numbers; they may have drifted since RESEARCH date)
    - `.planning/phases/69-production-status-accessors/69-01-PLAN.md` — final `ClusterStatusForGraphVersion` signature
    - PATTERNS.md §Adapter Replacement — semSchedulerAdapter.ClusterStatus
    - RESEARCH §Code Examples §State Derivation
  </read_first>
  <action>
    1) `semSchedulerAdapter` struct: add a `store *semanticstore.Store` field (or `bundle *semanticBundle` for symmetry with `semRetrievalAdapter`; pick whichever is shorter to thread — bundle pattern wins if the rest of the file already holds it). Wire the value at the adapter's construction site (grep `semSchedulerAdapter{` in this file to find the literal — typically in the same factory function that builds the rest of the bundle adapters; same factory that holds `rb`).

    2) Replace the body of `func (a *semSchedulerAdapter) ClusterStatus(repoID string) semantic.ClusterStatus`:
       - If `a == nil` or `a.store == nil` → return `{State:"unknown", Reason:"no-store"}`
       - `ctx := context.Background()` (status reads are short, isolated DB queries)
       - `gv, err := a.store.CurrentGraphVersion(ctx, repoID)` — on err non-nil OR gv == 0 → return `{State:"unknown", Reason:"no-graph-version"}`
       - `row, err := a.store.ClusterStatusForGraphVersion(ctx, repoID, gv)` — on err non-nil → `{State:"unknown", Reason:"accessor-error"}`
       - If `row.ClusterCount == 0` → `{State:"unknown", Reason:"no-cluster-rows"}` (Phase 69 omits `"stale"` and `"building"` per CONTEXT.md D1 relaxation + RESEARCH Q1 — emit only `current` / `unknown` for this version; future phase may add `stale` by checking older gv)
       - Else return `{State:"current", ComputedAt:row.ComputedAt, MemberCount:row.MemberCount}` (Reason omitted via omitempty)

    3) Remove the W1 doc comment at lines 407-409 (`semSchedulerAdapter wraps the rank bundle. ScoreStatus + ClusterStatus production accessors are W1 placeholders today — Phase 65/67 wires real sources.`) — replace with a short factual comment naming the new dependencies (`*rankBundle` for IsQuiescent/ScoreStatus, `*Store` for ClusterStatus).

    4) Rewrite the `ScoreStatus` comment at lines 433-443 to drop the "deliberate companion to the W1 ClusterStatus placeholder" framing. ScoreStatus body stays `return graph.ScoreStatusMissing` (out of scope; RESEARCH Q9 confirms). New comment: ScoreStatus returns the closed-enum graph.ScoreStatusMissing pending a Phase 65/67 per-projection accessor.

    5) Remove the `ClusterStatus` doc comment at lines 450-455 ("ClusterStatus is the W1 production-layer placeholder... Closes checker W1") — replace with a one-line factual doc describing the new logic.

    Commit: `feat(69-05): real ClusterStatus via *Store.ClusterStatusForGraphVersion + remove W1 comments`.
  </action>
  <verify>
    <automated>go build ./internal/daemon/... && grep -nE 'phase-62-clustering-no-status-accessor|W1 placeholder|W1 production-layer placeholder' internal/daemon/semantic_wiring.go</automated>
  </verify>
  <done>Build succeeds. Grep returns ZERO matches for the three forbidden tokens (placeholder removal success criterion #5 from ROADMAP). `go vet ./internal/daemon/...` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add semRetrievalAdapter.RetrievalStatus + bind compactBundle.bleveMetaFn + extend e2eRetrievalAcc</name>
  <read_first>
    - `internal/daemon/semantic_wiring.go:580-640` (semRetrievalAdapter region — engineFor helper)
    - `internal/semantic/retrieval/bleve.go` — confirm `metaKeyCorpusVersion` / `metaKeyIndexedFiles` exported (from Plan 69-02) and DocCount signature
    - `internal/skill/semantic/integration_test.go:275-289` (e2eRetrievalAcc — extend with stub)
    - PATTERNS.md §Adapter Addition — semRetrievalAdapter.RetrievalStatus
    - RESEARCH Open-Q2 priority order
  </read_first>
  <action>
    1) `semantic_wiring.go` — add method to `semRetrievalAdapter`:

    `func (a *semRetrievalAdapter) RetrievalStatus(ws workspace.WorkspaceKey) semantic.RetrievalStatus`

    Body:
    - `engine := a.engineFor(ws)`; if nil → return `semantic.RetrievalStatus{Reason:"bleve-unavailable"}`
    - `corpusBytes, _ := engine.GetMeta(retrieval.MetaKeyCorpusVersion)` (the exported constant name — verify against Plan 69-02 final naming; may be `metaKeyCorpusVersion` package-private requiring an exported re-export or a public getter)
    - If `corpusBytes == nil || len(corpusBytes) == 0` → return `semantic.RetrievalStatus{Reason:"corpus_version-uninitialized"}`
    - Parse `corpusVersion, parseErr := strconv.ParseUint(string(corpusBytes), 10, 64)`; on parseErr → `{Reason:"corpus_version-uninitialized"}` (treat malformed as uninitialized)
    - `filesBytes, _ := engine.GetMeta(retrieval.MetaKeyIndexedFiles)`; parse to int64 (0 on missing or parse error)
    - `lastCompactBytes, _ := engine.GetMeta("last_compact_at")` (the compactor wrote with the string literal in Plan 69-03 — match exactly)
    - `indexedSymbols, _ := engine.DocCount()`; cast to int64 (RESEARCH Pitfall 2)
    - Build `out := semantic.RetrievalStatus{CorpusVersion: corpusVersion, IndexedFiles: filesParsed, IndexedSymbols: int64(indexedSymbols), LastCompactAt: lastCompactParsed}`
    - Reason determination (priority order):
      - If we have a store reference: `currentGv, gvErr := a.bundle.store.CurrentGraphVersion(context.Background(), ws.RepoRoot)`; if gvErr == nil && corpusVersion < currentGv → `out.Reason = "corpus_version-lag"`
      - Else if `lastCompactBytes == nil || lastCompactParsed == 0` → `out.Reason = "compactor-never-ran"`
      - Else `out.Reason = ""` (omitted)
    - Return out

    Confirm `semRetrievalAdapter.bundle` exposes the `*Store` (grep around line 580 — if not, thread it in; the bundle pattern likely already carries it).

    Add compile-time assertion near the type declarations: `var _ compact.BleveMeta = (*retrieval.Engine)(nil)` (proves structural typing).

    2) Bind `compactBundle.bleveMetaFn` — locate the daemon bootstrap site where both `semanticBundle` and `compactBundle` are constructed (single function, likely in `daemon.go` or `semantic_wiring.go` post-init region). AFTER both bundles exist:

    ```
    b.compactBundle.SetBleveMetaFn(func(ws workspace.WorkspaceKey) compact.BleveMeta {
        b.semanticBundle.mu.Lock()
        defer b.semanticBundle.mu.Unlock()
        eng, ok := b.semanticBundle.engines[ws.RepoRoot]
        if !ok || eng == nil { return nil }
        return eng
    })
    ```

    Match the existing post-init wiring pattern (`SetEnrichFn`, `SetActivateCallback`) — same indent, same nil-guard idiom.

    3) `internal/skill/semantic/integration_test.go` — append to `e2eRetrievalAcc`:

    `func (a *e2eRetrievalAcc) RetrievalStatus(_ workspace.WorkspaceKey) RetrievalStatus { return RetrievalStatus{} }`

    Place after the existing methods at lines 275-289 region. This restores package compile after Plan 69-04's interface-breaking change. Plan 69-06 will replace this stub with a real-engine-backed implementation for the E2E test.

    Commit: `feat(69-05): wire RetrievalStatus adapter + bind bleveMetaFn + restore test compile`.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/skill/semantic/ ./internal/daemon/ -count=1 -race -timeout 90s</automated>
  </verify>
  <done>Whole-repo `go build ./...` succeeds (closes the 69-04 intentional break). Existing daemon + semantic skill tests PASS. `grep -rn 'phase-62-clustering-no-status-accessor' internal/` returns ZERO matches across the repo. `var _ compact.BleveMeta = (*retrieval.Engine)(nil)` compile-time assertion satisfied. Commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon adapter → MCP response | Adapter only forwards counts + versions; no caller-supplied data, no PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | I | Response composition | mitigate | Adapter passes through accessor outputs verbatim (which by Plan 69-01/02 design contain no identifiers) |
| T-69-02 | I | Cross-workspace fingerprinting via corpus_version | mitigate | `engineFor(ws)` scopes by ws.RepoRoot; no cross-workspace engine access path |
| T-69-03 | T / D | D-09 read-path on adapter | mitigate | Adapter calls only the new `*Store` read methods + bleve GetMeta/DocCount — no Begin/Commit/Abort/Write tokens; grep audit `grep -nE 'Begin(Snapshot\|OverlayTx)\|Commit(Snapshot)?\|WriteSnapshotFacts' internal/daemon/semantic_wiring.go` shows no additions inside ClusterStatus or RetrievalStatus method bodies |
</threat_model>

<verification>
- `go build ./...` clean across the entire repo
- `go test ./internal/skill/semantic/ ./internal/daemon/ -count=1 -race -timeout 90s` PASS
- `grep -rn 'phase-62-clustering-no-status-accessor' internal/` returns ZERO matches
- `grep -nE 'W1 placeholder|W1 production-layer placeholder|deliberate companion' internal/daemon/semantic_wiring.go` returns ZERO matches
- `go vet ./...` clean
- compactBundle.bleveMetaFn bound to a closure that resolves to a non-nil engine for activated workspaces
</verification>

<success_criteria>
- All three placeholder comment blocks removed (success criterion #5 from ROADMAP Phase 69)
- ClusterStatus and RetrievalStatus return real envelope shapes against a populated workspace
- bleveMetaFn binding closes the Plan 69-03 wiring slot
- Repo-wide build green; no regression in existing tests
</success_criteria>

<output>
After completion, write `.planning/phases/69-production-status-accessors/69-05-SUMMARY.md` capturing: the final adapter struct field shape (store vs bundle), the exact bootstrap insertion point for SetBleveMetaFn, and any deviation from the planned RetrievalStatus reason priority logic.
</output>
