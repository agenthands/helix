---
phase: 69
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - internal/skill/semantic/integration_test.go
autonomous: true
requirements: [STATUS-03]
tags: [semantic-graph, status, e2e, integration]
must_haves:
  truths:
    - "TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval seeds a 15-symbol workspace, primes bleve corpus, seeds 2-3 cluster_summary + cluster_members rows, calls get_semantic_graph_status, and asserts non-placeholder values"
    - "ClusterStatus assertions: State == 'current', MemberCount > 0 (== sum of seeded members), ComputedAt > 0"
    - "RetrievalStatus assertions: CorpusVersion == current graph_version, IndexedSymbols > 0, IndexedFiles > 0, Reason in {'', 'compactor-never-ran'} (no compaction has occurred in fixture)"
    - "Existing TestE2E_IndexThenContext_SymbolCount continues to PASS (regression guard)"
    - "Test uses the REAL daemon factory constructors daemon.NewSchedulerAccessorForStore + daemon.NewRetrievalAccessorForStore (per Plan 69-05) — NOT a test-local re-implementation. This guarantees the test exercises the actual production code path the daemon uses."
  artifacts:
    - path: "internal/skill/semantic/integration_test.go"
      provides: "TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval + adapter swap using daemon factory constructors"
      contains: "daemon.NewSchedulerAccessorForStore"
  key_links:
    - from: "TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval"
      to: "handleGetSemanticGraphStatus through daemon.NewSchedulerAccessorForStore + daemon.NewRetrievalAccessorForStore"
      via: "harness adapter swap using exported factory constructors"
      pattern: "daemon\\.NewSchedulerAccessorForStore"
---

<objective>
Author a single E2E integration test that proves the full STATUS-01 + STATUS-02 chain end-to-end on a populated workspace, satisfying STATUS-03 and success criterion #4 from ROADMAP Phase 69.

Purpose: The Phase 64 P07 fixture (`TestE2E_IndexThenContext_SymbolCount` at integration_test.go:445) already builds a 15-symbol indexed workspace with real `*Store` + bleve in tempdirs. Phase 69 reuses that harness, swaps the placeholder `e2eSchedAcc` / `e2eRetrievalAcc` for instances produced by `daemon.NewSchedulerAccessorForStore` and `daemon.NewRetrievalAccessorForStore` (the same factory constructors the daemon itself uses, per Plan 69-05), seeds cluster rows, and asserts the envelope carries real values across both `cluster_status` and `retrieval_status`.

This addresses revision Warning 3 (E2E coverage gap): the test now exercises THE SAME code path the daemon's `semSchedulerAdapter.ClusterStatus` and `semRetrievalAdapter.RetrievalStatus` invoke — not a parallel test-local re-implementation.

Output: One new test function in `integration_test.go` (or a new file under `package semantic_test` if cycle resolution requires it). Existing tests untouched (regression-clean).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-production-status-accessors/69-CONTEXT.md
@.planning/phases/69-production-status-accessors/69-RESEARCH.md
@.planning/phases/69-production-status-accessors/69-PATTERNS.md
@.planning/phases/69-production-status-accessors/69-01-PLAN.md
@.planning/phases/69-production-status-accessors/69-05-PLAN.md

@internal/skill/semantic/integration_test.go

<interfaces>
Existing fixture entry points (integration_test.go):
- `newE2EHarness(t, mode, facts)` — builds *Store + bleve + recoverer in tempdirs
- `makeFixtureFacts(N)` — generates N synthetic SymbolFact rows
- `e2eSchedAcc` (lines 241-247) — placeholder adapter; this test SWAPS it for `daemon.NewSchedulerAccessorForStore(h.store)`
- `e2eRetrievalAcc` (lines 275-289 + Plan 69-05's compile stub) — this test SWAPS for `daemon.NewRetrievalAccessorForStore(h.store, h.engine)`
- `primeBleveCorpus` helper from `internal/daemon/integ_lookup_e2e_helpers.go:185-206` — populates bleve docs + meta

Seed pattern for cluster rows (PATTERNS §E2E Fixture Extension, RESEARCH §Code Examples):
```
tx, err := h.store.BeginOverlayTx(ctx, repoID)
require.NoError(t, err)
require.NoError(t, tx.UpsertClusters(ctx, "weak_components", gv, []semanticstore.ClusterSummary{
    {ID: 1, MemberCount: 3}, {ID: 2, MemberCount: 2},
}))
require.NoError(t, tx.UpsertClusterMembers(ctx, "weak_components", gv, []semanticstore.ClusterMemberRow{
    {ClusterID: 1, NodeID: 100}, {ClusterID: 1, NodeID: 101}, {ClusterID: 1, NodeID: 102},
    {ClusterID: 2, NodeID: 200}, {ClusterID: 2, NodeID: 201},
}))
require.NoError(t, tx.Commit())
```

**Adapter swap using daemon factories (revision Warning 3 — preferred approach):**

```go
import daemonpkg "github.com/agenthands/helix/internal/daemon"

schedAcc := daemonpkg.NewSchedulerAccessorForStore(h.store)
retrAcc  := daemonpkg.NewRetrievalAccessorForStore(h.store, h.engine)

h.skill.SetSchedulerAccessor(schedAcc)
h.skill.SetRetrievalAccessor(retrAcc)
```

This exercises EXACTLY the same `(*schedulerAccessorImpl).ClusterStatus` and `(*retrievalAccessorImpl).RetrievalStatus` methods the daemon uses — no test-local re-implementation, no parallel logic to drift. The carve-out documented in Plan 69-05 (the retrieval accessor's non-status methods return zero values) is acceptable here because this test only invokes `RetrievalStatus` — the four query-path methods are not called via the status handler path.

**Import-cycle check:** `internal/skill/semantic` importing `internal/daemon` may create a cycle (daemon already imports skill/semantic for interface types). Resolution: the new test must live in a file with `package semantic_test` (external test package), which is legal to coexist with internal `_test.go` files. If the existing integration_test.go is internal (`package semantic`), put the new test in a NEW file `integration_status_e2e_test.go` declared `package semantic_test` and access skill APIs through the package's exported surface.

Decoder helper — likely need `decodeStatusResult(t, res)` analogous to `decodeIndexResult` (check via grep; add if absent — `json.Unmarshal` of res content into a `semantic.StatusResult`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval using daemon factory constructors</name>
  <read_first>
    - `internal/skill/semantic/integration_test.go:1-470` (full file — confirm harness API, locate decodeIndexResult, e2eSchedAcc/e2eRetrievalAcc, makeFixtureFacts, newE2EHarness, package declaration)
    - `internal/daemon/integ_lookup_e2e_helpers.go:185-206` (primeBleveCorpus signature)
    - `internal/daemon/semantic_accessor_factories.go` (Plan 69-05 — exported constructors)
    - PATTERNS.md §E2E Fixture Extension
    - 69-01-SUMMARY.md (final ClusterStatusForGraphVersion signature; seedClusterRows helper lives in `internal/semantic/store` test package so this test calls UpsertClusters / UpsertClusterMembers directly)
    - 69-05-SUMMARY.md (factory constructor names, the carve-out for retrievalAccessorImpl non-status methods)
  </read_first>
  <action>
    0) **Import-cycle check:** `grep '^package' internal/skill/semantic/integration_test.go`. If `package semantic`, the new test MUST live in a NEW file `internal/skill/semantic/integration_status_e2e_test.go` with `package semantic_test` to allow importing `internal/daemon`. If already `package semantic_test`, append in place. Document the decision in the SUMMARY.

    1) Build harness: `h := newE2EHarness(t, "review", makeFixtureFacts(15))`. (If newE2EHarness is internal-only and the test moved to external package, either re-export a `NewE2EHarnessForTest` shim or move the harness builder into a `_test_helpers.go` file with `// +build testtools` — pick whichever is shorter.)

    2) Index: call `h.skill.handleIndexSemanticGraph(ctx, IndexSemanticGraphArgs{Mode: "full"})`. Assert `!res.IsError` and `decodeIndexResult(t, res).SnapshotID != 0`.

    3) Prime bleve: ensure bleve docs exist AND `MetaKeyCorpusVersion` + `MetaKeyIndexedFiles` are populated. Prefer calling the real `recoverer.RebuildBlocking(...)` path — this exercises the production single-writer site from Plan 69-02. If `primeBleveCorpus` does not trigger the recoverer, extend the harness to call it directly.

    4) Resolve `repoID := h.ws.RepoRoot` (or `h.ws.Hash()` — match the field used elsewhere). Read `gv, err := h.store.CurrentGraphVersion(ctx, repoID)`; assert gv > 0.

    5) Seed clusters via `BeginOverlayTx → UpsertClusters → UpsertClusterMembers → Commit` (2 clusters, 3 + 2 members, totalMembers = 5).

    6) **Swap in the REAL daemon factory constructors:**
       ```go
       import daemonpkg "github.com/agenthands/helix/internal/daemon"

       schedAcc := daemonpkg.NewSchedulerAccessorForStore(h.store)
       retrAcc  := daemonpkg.NewRetrievalAccessorForStore(h.store, h.engine)

       h.skill.SetSchedulerAccessor(schedAcc)
       h.skill.SetRetrievalAccessor(retrAcc)
       ```
       Heart of the revision Warning 3 fix — the test hits exactly the same `(*schedulerAccessorImpl).ClusterStatus` / `(*retrievalAccessorImpl).RetrievalStatus` code paths the daemon does. Verify `h.skill.SetSchedulerAccessor` / `SetRetrievalAccessor` exist (STATE.md mentions 8 setters; both should be present).

    7) Call `sres := h.skill.handleGetSemanticGraphStatus(ctx, GetSemanticGraphStatusArgs{})`. Assert `!sres.IsError`. Decode via `decodeStatusResult(t, sres)` (add helper near `decodeIndexResult` if absent).

    8) Assertions (testify require for fail-fast):
       - `require.Equal(t, "current", sr.ClusterStatus.State)`
       - `require.Equal(t, 5, sr.ClusterStatus.MemberCount)`
       - `require.Greater(t, sr.ClusterStatus.ComputedAt, int64(0))`
       - `require.Equal(t, gv, sr.RetrievalStatus.CorpusVersion)`
       - `require.Greater(t, sr.RetrievalStatus.IndexedSymbols, int64(0))`
       - `require.GreaterOrEqual(t, sr.RetrievalStatus.IndexedFiles, int64(1))`
       - `require.Contains(t, []string{"", "compactor-never-ran"}, sr.RetrievalStatus.Reason)` — fixture does not run the compactor, so `compactor-never-ran` is expected; empty is acceptable if a prior code path seeded last_compact_at.

    9) Regression guard — keep `TestE2E_IndexThenContext_SymbolCount` untouched. Isolate any harness changes behind a new option (e.g., `newE2EHarness(t, mode, facts, useProdFactories bool)`).

    Commit: `test(69-06): E2E TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval via daemon factory constructors`.
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/ -run "TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval|TestE2E_IndexThenContext_SymbolCount" -count=1 -race -timeout 120s</automated>
  </verify>
  <done>New E2E PASSES under -race. Existing `TestE2E_IndexThenContext_SymbolCount` still PASSES (regression-clean). `go test ./... -count=1` PASSES across the repo. `grep -rn 'daemon.NewSchedulerAccessorForStore\|daemon.NewRetrievalAccessorForStore' internal/skill/semantic/` returns the new test's call sites (proves factory usage, not re-implementation). Commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → production code paths | The test exercises the real *Store accessor + bleve GetMeta/DocCount/SetMeta paths end-to-end via the daemon factory constructors; assertions only inspect counts/versions (no identifiers leaked through test logs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | I | Test logs | mitigate | Assertions check structural fields; failure messages reproduce only counts + versions, not member ids |
| T-69-03 | T / D | Race-safety of the E2E flow | mitigate | Test runs under -race; harness reuses Phase 64 P07's race-clean tempdir isolation |
</threat_model>

<verification>
- `go test ./internal/skill/semantic/ -run "TestE2E_IndexThen" -count=1 -race -timeout 120s` PASS
- `go test ./... -count=1` PASS repo-wide
- `gofmt -l internal/skill/semantic/integration_test.go internal/skill/semantic/integration_status_e2e_test.go` empty (whichever file landed)
- No new placeholder strings introduced (grep `phase-62-clustering-no-status-accessor` still zero)
- Test imports `internal/daemon` for the factory constructors — proves the production code path is exercised, not a test-local re-implementation
</verification>

<success_criteria>
- STATUS-03 closed: a single integration test exercises the full status surface on a populated workspace and asserts non-placeholder values across both cluster_status and retrieval_status
- The test instantiates `daemon.NewSchedulerAccessorForStore` and `daemon.NewRetrievalAccessorForStore` directly — exercises the SAME factory the daemon adapter uses (revision Warning 3 closed)
- Phase 64 regression test still green
</success_criteria>

<output>
After completion, write `.planning/phases/69-production-status-accessors/69-06-SUMMARY.md` capturing: exact assertion list, whether primeBleveCorpus required extension or recoverer.RebuildBlocking was used directly, the chosen package-cycle resolution (external test package vs internal), and any harness API changes.
</output>
