---
phase: 69
plan: 06
type: execute
wave: 3
depends_on: [05]
files_modified:
  - internal/skill/semantic/integration_test.go
autonomous: true
requirements: [STATUS-03]
tags: [semantic-graph, status, e2e, integration]
must_haves:
  truths:
    - "TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval seeds a 15-symbol workspace, primes bleve corpus, seeds 2-3 cluster_summary + cluster_members rows, calls get_semantic_graph_status, and asserts non-placeholder values"
    - "ClusterStatus assertions: State == 'current', MemberCount > 0 (== sum of seeded members), ComputedAt > 0"
    - "RetrievalStatus assertions: CorpusVersion == current graph_version, IndexedSymbols > 0, IndexedFiles > 0, Reason in {'', 'compactor-never-ran'} (no compaction has occurred in fixture)"
    - "Existing TestE2E_IndexThenContext_SymbolCount continues to PASS (regression guard)"
    - "Test uses production semSchedulerAdapter shape (not the legacy e2eSchedAcc placeholder) — swaps in a *Store-backed adapter for the assertion path"
  artifacts:
    - path: "internal/skill/semantic/integration_test.go"
      provides: "TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval + production-shape adapter swap"
      contains: "TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval"
  key_links:
    - from: "TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval"
      to: "handleGetSemanticGraphStatus through real semSchedulerAdapter + semRetrievalAdapter"
      via: "harness adapter swap"
      pattern: "ClusterStatusForGraphVersion"
---

<objective>
Author a single E2E integration test that proves the full STATUS-01 + STATUS-02 chain end-to-end on a populated workspace, satisfying STATUS-03 and success criterion #4 from ROADMAP Phase 69.

Purpose: The Phase 64 P07 fixture (`TestE2E_IndexThenContext_SymbolCount` at integration_test.go:445) already builds a 15-symbol indexed workspace with real `*Store` + bleve in tempdirs. Phase 69 reuses that harness, swaps the placeholder `e2eSchedAcc` for the production `*Store`-backed adapter (or a thin shim around `ClusterStatusForGraphVersion`), seeds cluster rows, and asserts the envelope carries real values across both `cluster_status` and `retrieval_status`.

Output: One new test function in `integration_test.go`. Existing tests untouched (regression-clean).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-production-status-accessors/69-CONTEXT.md
@.planning/phases/69-production-status-accessors/69-RESEARCH.md
@.planning/phases/69-production-status-accessors/69-PATTERNS.md
@.planning/phases/69-production-status-accessors/69-01-PLAN.md
@.planning/phases/69-production-status-accessors/69-05-PLAN.md

@internal/skill/semantic/integration_test.go

<interfaces>
Existing fixture entry points (integration_test.go):
- `newE2EHarness(t, mode, facts)` — builds *Store + bleve + recoverer in tempdirs
- `makeFixtureFacts(N)` — generates N synthetic SymbolFact rows
- `e2eSchedAcc` (lines 241-247) — the placeholder adapter that today returns `{State:"unknown", Reason:"phase-62-clustering-no-status-accessor"}`
- `e2eRetrievalAcc` (lines 275-289 + Plan 69-05's appended `RetrievalStatus` stub)
- `primeBleveCorpus` helper from `internal/daemon/integ_lookup_e2e_helpers.go:185-206` — populates bleve docs + meta

Seed pattern for cluster rows (PATTERNS §E2E Fixture Extension, RESEARCH §Code Examples):
```
tx, err := h.store.BeginOverlayTx(ctx, repoID)
require.NoError(t, err)
require.NoError(t, tx.UpsertClusters(ctx, "weak_components", gv, []semanticstore.ClusterSummary{
    {ID: 1, MemberCount: 3}, {ID: 2, MemberCount: 2},
}))
require.NoError(t, tx.UpsertClusterMembers(ctx, "weak_components", gv, []semanticstore.ClusterMemberRow{
    {ClusterID: 1, NodeID: 100}, {ClusterID: 1, NodeID: 101}, {ClusterID: 1, NodeID: 102},
    {ClusterID: 2, NodeID: 200}, {ClusterID: 2, NodeID: 201},
}))
require.NoError(t, tx.Commit())
```

Adapter swap — define a test-local `prodSchedAcc` that wraps `h.store` and implements `SchedulerAccessor` exactly as `semSchedulerAdapter` does (IsQuiescent=true, ScoreStatus=Missing, ClusterStatus calls store.CurrentGraphVersion + ClusterStatusForGraphVersion + emits envelope). Similarly `prodRetrievalAcc` wraps `h.engine` and implements `RetrievalStatus` using the same priority order as Plan 69-05's adapter. (Test-local re-implementation is acceptable to avoid coupling the test to daemon internals; the production code under test is exercised at the *Store accessor and *Engine.GetMeta layer regardless.)

Alternative if cleaner: extract a small package-private helper in `internal/daemon/semantic_wiring.go` like `NewSchedulerAccessorForWorkspace(store *semanticstore.Store) semantic.SchedulerAccessor` and use it from the test — but that grows the daemon's exported surface. Default to test-local adapter and document the choice.

Decoder helper — likely need `decodeStatusResult(t, res)` analogous to `decodeIndexResult` (already in this file). Check via grep — if absent, add it (jsonUnmarshal of res content into a `StatusResult`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval</name>
  <read_first>
    - `internal/skill/semantic/integration_test.go:1-470` (full file — confirm harness API, locate decodeIndexResult, e2eSchedAcc, makeFixtureFacts, newE2EHarness)
    - `internal/daemon/integ_lookup_e2e_helpers.go:185-206` (primeBleveCorpus signature)
    - PATTERNS.md §E2E Fixture Extension
    - 69-01-SUMMARY.md (final ClusterStatusForGraphVersion signature, seedClusterRows helper — note that helper lives in `internal/semantic/store` test package; you may need to call UpsertClusters / UpsertClusterMembers directly from this test since cross-package _test.go helpers are not importable)
    - 69-05-SUMMARY.md (adapter binding details, reason priority implementation)
  </read_first>
  <action>
    Append `TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval` after the existing `TestE2E_IndexThenContext_SymbolCount` (~line 470). Steps:

    1) Build harness: `h := newE2EHarness(t, "review", makeFixtureFacts(15))`.

    2) Index: `res := h.skill.handleIndexSemanticGraph(ctx, IndexSemanticGraphArgs{Mode: "full"})`. Assert `!res.IsError` and `decodeIndexResult(t, res).SnapshotID != 0`.

    3) Prime bleve: call `primeBleveCorpus(ctx, h.store, h.engine, ir.SnapshotID)` to ensure bleve docs exist AND meta keys `corpus_version` + `indexed_files` are populated. NOTE: `primeBleveCorpus` may not yet write the new Phase 69 meta keys — if it does not, EITHER extend it (one-line additions for corpus_version/indexed_files writes, matching Plan 69-02 patterns) OR call the real `recoverer.RebuildBlocking(...)` path to trigger the production single-writer site. Prefer the latter (real path) — extend the harness to call the recoverer rather than the test helper.

    4) Resolve `repoID := h.ws.RepoRoot` (or `h.ws.Hash()` — match the field used elsewhere in the file). Read `gv, err := h.store.CurrentGraphVersion(ctx, repoID)` — assert gv > 0.

    5) Seed clusters via the BeginOverlayTx → UpsertClusters → UpsertClusterMembers → Commit sequence shown in the interfaces block. Seed 2 clusters with 3 + 2 members each (totalMembers = 5).

    6) Construct production-shape adapters:
       - `prodSchedAcc` — struct holding `*Store`; ClusterStatus calls CurrentGraphVersion + ClusterStatusForGraphVersion and assembles the envelope per Plan 69-05's logic (current/unknown only).
       - `prodRetrievalAcc` — struct holding `*retrieval.Engine` + `*Store`; RetrievalStatus implements the priority-order logic per Plan 69-05.
       - Wire into the harness: there is likely a `h.skill.SetSchedulerAccessor` / `SetRetrievalAccessor` (grep for `SemanticSkill` setters; from STATE.md the 8 setters include both). Replace the e2e* placeholders.

    7) Call `sres := h.skill.handleGetSemanticGraphStatus(ctx, GetSemanticGraphStatusArgs{})`. Assert `!sres.IsError`. Decode via `decodeStatusResult(t, sres)` (add this helper near `decodeIndexResult` if absent — it should match the StatusResult JSON shape from Plan 69-04).

    8) Assertions (use testify require for fail-fast):
       - `require.Equal(t, "current", sr.ClusterStatus.State)`
       - `require.Equal(t, 5, sr.ClusterStatus.MemberCount)`
       - `require.Greater(t, sr.ClusterStatus.ComputedAt, int64(0))`
       - `require.Equal(t, gv, sr.RetrievalStatus.CorpusVersion)`
       - `require.Greater(t, sr.RetrievalStatus.IndexedSymbols, int64(0))`
       - `require.GreaterOrEqual(t, sr.RetrievalStatus.IndexedFiles, int64(1))`
       - `require.Contains(t, []string{"", "compactor-never-ran"}, sr.RetrievalStatus.Reason)` — fixture does not run the compactor, so `compactor-never-ran` is the expected closed-enum value; empty is acceptable if a prior test seeded last_compact_at. Document the closed expectation explicitly.

    9) Regression guard — keep `TestE2E_IndexThenContext_SymbolCount` untouched; do NOT modify the existing harness primitives in ways that would alter its behavior. If the production adapter swap requires harness changes, isolate behind a new option on `newE2EHarness` (e.g., `newE2EHarness(t, mode, facts, useProdAdapters bool)`) so existing call sites stay correct.

    Commit: `test(69-06): E2E TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval`.
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/ -run "TestE2E_IndexThenStatus_NonPlaceholderClusterAndRetrieval|TestE2E_IndexThenContext_SymbolCount" -count=1 -race -timeout 120s</automated>
  </verify>
  <done>New E2E PASSES under -race. Existing TestE2E_IndexThenContext_SymbolCount still PASSES (regression-clean). `go test ./... -count=1` PASSES across the repo. Commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → production code paths | The test exercises the real *Store accessor + bleve GetMeta/DocCount/SetMeta paths end-to-end; assertions only inspect counts/versions (no identifiers leaked through test logs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | I | Test logs | mitigate | Assertions check structural fields; failure messages reproduce only counts + versions, not member ids |
| T-69-03 | T / D | Race-safety of the E2E flow | mitigate | Test runs under -race; harness reuses Phase 64 P07's race-clean tempdir isolation |
</threat_model>

<verification>
- `go test ./internal/skill/semantic/ -run "TestE2E_IndexThen" -count=1 -race -timeout 120s` PASS
- `go test ./... -count=1` PASS repo-wide
- `gofmt -l internal/skill/semantic/integration_test.go` empty
- No new placeholder strings introduced (grep `phase-62-clustering-no-status-accessor` still zero)
</verification>

<success_criteria>
- STATUS-03 closed: a single integration test exercises the full status surface on a populated workspace and asserts non-placeholder values across both cluster_status and retrieval_status
- Phase 64 regression test still green
- The test runs production *Store + *Engine code paths (not test fakes) for the new accessors — fakes only stand in for unrelated surfaces (LiveAccessor, etc.)
</success_criteria>

<output>
After completion, write `.planning/phases/69-production-status-accessors/69-06-SUMMARY.md` capturing: exact assertion list, whether primeBleveCorpus required extension or recoverer.RebuildBlocking was used directly, and any harness API changes.
</output>
