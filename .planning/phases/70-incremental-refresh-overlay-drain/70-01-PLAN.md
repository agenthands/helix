---
phase: 70
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/semantic/store/overlay.go
  - internal/semantic/store/overlay_test.go
autonomous: true
requirements: [REFRESH-01]
tags: [duckdb, semantic-store, overlay, accessor]
must_haves:
  truths:
    - "OverlayChangedPathsSince returns paths whose write_epoch > baseEpoch for the given repo_id"
    - "OverlayChangedPathsSince returns the current overlay epoch alongside the path set"
    - "baseEpoch == 0 returns ALL current overlay paths (cold-start signal)"
    - "Empty paths with non-zero baseEpoch is contract-preserving (caller falls back to full-walk)"
    - "Read path uses zero Begin/Commit/Abort/Write/Tx tokens (D-09 / D-07 invariant)"
  artifacts:
    - path: "internal/semantic/store/overlay.go"
      provides: "OverlayChangedPathsSince accessor"
      contains: "func (s *Store) OverlayChangedPathsSince"
    - path: "internal/semantic/store/overlay_test.go"
      provides: "TestOverlayChangedPathsSince with 5 sub-tests"
      contains: "TestOverlayChangedPathsSince"
  key_links:
    - from: "OverlayChangedPathsSince"
      to: "semantic_live_overlay_files index idx_overlay_files_write_epoch"
      via: "SELECT DISTINCT path WHERE repo_id=? AND write_epoch>?"
      pattern: "write_epoch > \\?"
    - from: "OverlayChangedPathsSince"
      to: "semantic_live_overlay_meta.current_epoch"
      via: "SELECT current_epoch FROM semantic_live_overlay_meta"
      pattern: "current_epoch FROM semantic_live_overlay_meta"
---

<objective>
Land the `*Store.OverlayChangedPathsSince` read accessor that returns the set of distinct overlay paths whose `write_epoch > baseEpoch` plus the current overlay epoch. This is the new seam that both `index_semantic_graph(mode=incremental)` and `refresh_semantic_graph` will consume (per CONTEXT.md D1 + D2).

Purpose: Closes REFRESH-01 at the data layer. Single source of truth for "what changed since" enumeration. Lock-free, D-09 compliant, idempotent.

Output: One sibling accessor in `overlay.go` (~50 LOC) + table-driven test with 5 sub-tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-CONTEXT.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-RESEARCH.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-PATTERNS.md
@internal/semantic/store/overlay.go
@internal/semantic/store/filefact_accessor.go

<interfaces>
<!-- Pattern source: internal/semantic/store/overlay.go:1004-1038 (CurrentOverlayEpoch) -->
<!-- Multi-row scan source: internal/semantic/store/filefact_accessor.go:117-144 -->

Signature (CONTEXT.md D2, locked):
```go
func (s *Store) OverlayChangedPathsSince(
    ctx context.Context,
    repoID string,
    baseEpoch uint64,
) (paths []string, currentEpoch uint64, err error)
```

Underlying queries (RESEARCH.md Pattern 1):
```sql
SELECT current_epoch FROM semantic_live_overlay_meta WHERE repo_id = ?
SELECT DISTINCT path FROM semantic_live_overlay_files WHERE repo_id = ? AND write_epoch > ?
```

Existing index (no new index needed) — `migrations.go:527`:
```
idx_overlay_files_write_epoch ON semantic_live_overlay_files(repo_id, write_epoch)
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — TestOverlayChangedPathsSince with 5 sub-tests</name>
  <files>internal/semantic/store/overlay_test.go</files>
  <read_first>
    - internal/semantic/store/overlay.go (sibling TestCurrentOverlayEpoch shape)
    - internal/semantic/store/filefact_accessor_test.go (4-arm sub-test pattern)
    - internal/semantic/store/overlay_test.go (full file — locate insertion point near existing overlay tests)
  </read_first>
  <behavior>
    - Sub-test "cold_start": baseEpoch=0 against a non-empty overlay → returns ALL paths in overlay and currentEpoch == latest write_epoch
    - Sub-test "single_change": commit one overlay tx that touches file "a.go" with write_epoch=5; OverlayChangedPathsSince(repoID, baseEpoch=3) → paths=["a.go"], currentEpoch=5
    - Sub-test "multi_epoch": commit two overlay txs (epochs 7 then 9, distinct paths "b.go" and "c.go"); OverlayChangedPathsSince(repoID, baseEpoch=6) → paths={"b.go","c.go"} (order-insensitive), currentEpoch=9
    - Sub-test "empty_since": commit overlay at epoch=4; OverlayChangedPathsSince(repoID, baseEpoch=4) → paths=[], currentEpoch=4 (workspace quiet since baseline)
    - Sub-test "no_meta_row": fresh store, no overlay activity → OverlayChangedPathsSince(repoID, baseEpoch=0) → paths=nil, currentEpoch=0, err=nil (mirrors CurrentOverlayEpoch ErrNoRows handling)
    - Negative cases (separate assertions in test body): nil store → error; empty repoID → error
  </behavior>
  <action>
    Add `TestOverlayChangedPathsSince` to `internal/semantic/store/overlay_test.go` using the existing `newTestStore(t)` / `BeginOverlayTx` / `UpsertOverlayFile` helpers (mirror `TestCurrentOverlayEpoch` setup if present, otherwise mirror the helper pattern used by neighboring overlay tests). Each sub-test runs in `t.Run(...)`; use `repoID := "test-repo-70-01-<n>"` per sub-test (distinct repo IDs so sub-tests do not share state). Use `BeginOverlayTx` + `UpsertOverlayFile(repoID, path, ...)` + `tx.Commit()` to write overlay rows; the `write_epoch` is assigned by the tx itself per Phase 60 D-04, so capture it via `tx.WriteEpoch()` or `CurrentOverlayEpoch` after commit. Run the test FIRST and confirm RED: compile failure ("OverlayChangedPathsSince undeclared name").
  </action>
  <verify>
    <automated>go test ./internal/semantic/store/ -race -run TestOverlayChangedPathsSince -count=1 2>&1 | grep -E "undeclared name|undefined: .*OverlayChangedPathsSince|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `func TestOverlayChangedPathsSince(t *testing.T)` with five `t.Run` sub-tests covering cold_start, single_change, multi_epoch, empty_since, no_meta_row
    - Build fails with the exact symbol-missing error (proves RED)
    - No `Begin/Commit/Abort/Write` tokens appear ON the accessor under test (the test may use BeginOverlayTx for fixture setup — that is the producer side, not the accessor)
  </acceptance_criteria>
  <done>Test compiles only against fixture helpers; `OverlayChangedPathsSince` call sites produce a compiler error proving the symbol does not yet exist. Test is committed as `test(70-01): add failing test for OverlayChangedPathsSince`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement OverlayChangedPathsSince</name>
  <files>internal/semantic/store/overlay.go</files>
  <read_first>
    - internal/semantic/store/overlay.go (lines 1004-1054 — CurrentOverlayEpoch pattern; locate insertion point immediately after)
    - internal/semantic/store/filefact_accessor.go (multi-row Scan loop, lines 117-144)
  </read_first>
  <action>
    Append `func (s *Store) OverlayChangedPathsSince(ctx context.Context, repoID string, baseEpoch uint64) (paths []string, currentEpoch uint64, err error)` to `internal/semantic/store/overlay.go` immediately after `CurrentOverlayEpoch`. Body order (locked by RESEARCH.md Pattern 1):
    1. Nil-guard: `if s == nil || s.db == nil { return nil, 0, fmt.Errorf("OverlayChangedPathsSince: nil store") }`
    2. Empty-repoID guard: `if repoID == "" { return nil, 0, fmt.Errorf("OverlayChangedPathsSince: empty repoID") }`
    3. Read `current_epoch` first via `s.db.QueryRowContext(ctx, "SELECT current_epoch FROM semantic_live_overlay_meta WHERE repo_id = ?", repoID).Scan(&currentEpoch)`. On `errors.Is(err, sql.ErrNoRows)` return `(nil, 0, nil)`. On other err return wrapped `fmt.Errorf("OverlayChangedPathsSince(%q): epoch read: %w", repoID, err)`.
    4. Query paths via `s.db.QueryContext(ctx, "SELECT DISTINCT path FROM semantic_live_overlay_files WHERE repo_id = ? AND write_epoch > ?", repoID, baseEpoch)`. `defer rows.Close()`. Loop `rows.Next()`, `rows.Scan(&p)`, append to `paths`. Check `rows.Err()`. Wrap any error as `fmt.Errorf("OverlayChangedPathsSince(%q): path query: %w", repoID, err)` (or `path scan` / `path rows` to match the wrap-string discipline in `filefact_accessor.go`).
    5. Return `paths, currentEpoch, nil`.
    Reuse existing imports (`context`, `database/sql`, `errors`, `fmt`) — no new imports.
  </action>
  <verify>
    <automated>go test ./internal/semantic/store/ -race -run TestOverlayChangedPathsSince -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/semantic/store/ -race -run TestOverlayChangedPathsSince -count=1` exits 0 with all sub-tests PASS
    - `grep -nE '\b(BeginOverlayTx|CommitOverlayTx|AbortOverlayTx|tx\.Exec|Write)\b' internal/semantic/store/overlay.go | grep -c "func (s \*Store) OverlayChangedPathsSince"` returns 0 (the function body uses no tx/write tokens — D-09)
    - `go vet ./internal/semantic/store/...` exits 0
    - `make vet-nokernel2semantic` exits 0 (boundary preserved)
  </acceptance_criteria>
  <done>All 5 sub-tests green. Commit as `feat(70-01): implement OverlayChangedPathsSince accessor`.</done>
</task>

</tasks>

<verification>
- `go test ./internal/semantic/store/ -race -run 'OverlayChangedPathsSince|TestCurrentOverlayEpoch' -count=1` exits 0
- `go vet ./internal/semantic/store/...` exits 0
- `make vet-nokernel2semantic` exits 0
- D-09 grep: `grep -nE 'Begin|Commit|Abort|Write' internal/semantic/store/overlay.go | grep -E 'OverlayChangedPathsSince'` returns no match (function body is pure read)
</verification>

<success_criteria>
- `OverlayChangedPathsSince` is callable, race-clean, idempotent
- baseEpoch=0 returns all current overlay paths; non-zero baseEpoch returns strict-superset filter
- Empty result is distinguishable: `(nil, 0, nil)` = no meta row, `([], N, nil)` = quiet workspace since baseline
- D-09 invariant preserved (no tx/write tokens)
</success_criteria>

<output>
After completion, create `.planning/phases/70-incremental-refresh-overlay-drain/70-01-SUMMARY.md`
</output>
