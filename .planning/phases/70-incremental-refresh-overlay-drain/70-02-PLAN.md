---
phase: 70
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/semantic/store/migrations.go
  - internal/semantic/store/migrations_registry.go
  - internal/semantic/store/migrations_types.go
  - internal/semantic/store/migrations_test.go
  - internal/semantic/store/snapshot.go
  - internal/semantic/store/snapshot_test.go
autonomous: true
requirements: [REFRESH-01]
tags: [duckdb, migrations, schema, snapshot]
must_haves:
  truths:
    - "Schema migrates 5 → 6 by adding base_overlay_epoch UBIGINT DEFAULT 0 column to semantic_snapshots"
    - "Migration 006 stamps semantic_schema_version with (6, now())"
    - "SnapshotMeta carries BaseOverlayEpoch uint64 field"
    - "CommitSnapshot persists BaseOverlayEpoch on the snapshot row in the same tx as the status flip"
    - "Existing snapshots (pre-migration) default to base_overlay_epoch=0 (cold-start signal)"
    - "LatestCommittedSnapshotBaseEpoch(ctx, repoID) accessor returns (epoch, true, nil) on hit, (0, false, nil) on no committed snapshot"
  artifacts:
    - path: "internal/semantic/store/migrations.go"
      provides: "applyMigration006 + schema6Statements"
      contains: "applyMigration006"
    - path: "internal/semantic/store/migrations_registry.go"
      provides: "registry entry From:5 To:6"
      contains: "From: 5, To: 6"
    - path: "internal/semantic/store/migrations_types.go"
      provides: "CurrentSchemaVersion = 6"
      contains: "CurrentSchemaVersion = 6"
    - path: "internal/semantic/store/snapshot.go"
      provides: "BaseOverlayEpoch field on SnapshotMeta + CommitSnapshot UPDATE column + LatestCommittedSnapshotBaseEpoch accessor"
      contains: "BaseOverlayEpoch"
    - path: "internal/semantic/store/snapshot.go"
      provides: "LatestCommittedSnapshotBaseEpoch read accessor (consumed by Plan 04 buildFn)"
      contains: "func (s *Store) LatestCommittedSnapshotBaseEpoch"
  key_links:
    - from: "applyMigration006"
      to: "semantic_snapshots.base_overlay_epoch"
      via: "ALTER TABLE ... ADD COLUMN base_overlay_epoch UBIGINT DEFAULT 0"
      pattern: "ADD COLUMN base_overlay_epoch UBIGINT"
    - from: "CommitSnapshot"
      to: "semantic_snapshots row"
      via: "UPDATE semantic_snapshots SET ... base_overlay_epoch=? WHERE snapshot_id=?"
      pattern: "base_overlay_epoch=\\?"
    - from: "LatestCommittedSnapshotBaseEpoch"
      to: "semantic_snapshots row (most recent committed for repo)"
      via: "SELECT base_overlay_epoch FROM semantic_snapshots WHERE repo_id=? AND status='committed' ORDER BY snapshot_id DESC LIMIT 1"
      pattern: "ORDER BY snapshot_id DESC LIMIT 1"
---

<objective>
Land schema migration 5 → 6 adding `base_overlay_epoch UBIGINT DEFAULT 0` to `semantic_snapshots`, extend `SnapshotMeta` + `CommitSnapshot` to capture/persist the baseline overlay epoch (CONTEXT.md D3; locked), and add the `LatestCommittedSnapshotBaseEpoch` read accessor that Plan 04's buildFn consumes.

Purpose: Provides the persistent baseline that the next incremental build queries against the seam (`OverlayChangedPathsSince(repoID, baseEpoch)`). Restart-safe per the Phase 68 D-01 precedent.

Output: One ALTER migration + registry append + version bump + struct extension + CommitSnapshot UPDATE column + LatestCommittedSnapshotBaseEpoch accessor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-incremental-refresh-overlay-drain/70-CONTEXT.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-RESEARCH.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-PATTERNS.md
@internal/semantic/store/migrations.go
@internal/semantic/store/migrations_registry.go
@internal/semantic/store/migrations_types.go
@internal/semantic/store/snapshot.go

<interfaces>
<!-- Pattern source: migrations.go:495-535 (applyMigration003) -->
<!-- Constraint: DuckDB rejects NOT NULL DEFAULT <expr> on ALTER ADD COLUMN — use bare DEFAULT 0 (migrations.go:482-487) -->
<!-- Registry append point: migrations_registry.go:27-33 -->
<!-- Final statement MUST stamp semantic_schema_version (registry.go:75-81 re-reads and fails if absent) -->

SnapshotMeta extension (snapshot.go:86-98):
```go
type SnapshotMeta struct {
    RepoID           string
    BaseSnapshotID   uint64
    CapturedEpoch    uint64
    BaseOverlayEpoch uint64 // NEW Phase 70
}
```

CommitSnapshot UPDATE site (snapshot.go:~426):
```sql
UPDATE semantic_snapshots
   SET status='committed', committed_at=now(), base_overlay_epoch=?
 WHERE snapshot_id=?
```

LatestCommittedSnapshotBaseEpoch (NEW Plan 04 consumer):
```go
func (s *Store) LatestCommittedSnapshotBaseEpoch(
    ctx context.Context, repoID string,
) (epoch uint64, ok bool, err error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration 5 → 6 (applyMigration006 + registry + version bump + migration test)</name>
  <files>internal/semantic/store/migrations.go, internal/semantic/store/migrations_registry.go, internal/semantic/store/migrations_types.go, internal/semantic/store/migrations_test.go</files>
  <read_first>
    - internal/semantic/store/migrations.go (lines 472-535 — schema3 ALTER+INDEX pattern; lines 482-487 — DuckDB ALTER constraint)
    - internal/semantic/store/migrations_registry.go (lines 27-33 — registry tail)
    - internal/semantic/store/migrations_types.go (line 21 — CurrentSchemaVersion constant)
    - internal/semantic/store/migrations_test.go (existing TestMigrationNNN shape for the assertion pattern)
  </read_first>
  <behavior>
    - After running migrations on a fresh store, schema_version reports 6
    - `PRAGMA table_info('semantic_snapshots')` (or equivalent DuckDB introspection used by sibling tests) reports a `base_overlay_epoch` column with type `UBIGINT` (or `HUGEINT`/`BIGINT` depending on the existing convention used by `current_epoch` — match the existing column type verbatim)
    - Existing snapshot rows (if any pre-existed at v5) carry `base_overlay_epoch = 0` after migration
    - Inserting a row without explicitly setting `base_overlay_epoch` succeeds (DEFAULT 0 honored)
  </behavior>
  <action>
    1. In `internal/semantic/store/migrations.go` append `applyMigration006(ctx context.Context, db *sql.DB) error` mirroring `applyMigration003` body (loop over `schema6Statements()`, wrap errors as `fmt.Errorf("applyMigration006: stmt %d (%s): %w", i+1, firstLine(stmt), err)`).
    2. Append `schema6Statements() []string` returning exactly two statements in this order:
       - `ALTER TABLE semantic_snapshots ADD COLUMN base_overlay_epoch UBIGINT DEFAULT 0`
       - `INSERT INTO semantic_schema_version (version, applied_at) VALUES (6, now())`
       Use bare `DEFAULT 0` (no NOT NULL — DuckDB constraint per migrations.go:482-487).
    3. In `internal/semantic/store/migrations_registry.go` append `{From: 5, To: 6, Kind: MigrationInPlace, Apply: applyMigration006},` to the `migrations` slice tail.
    4. In `internal/semantic/store/migrations_types.go` change `const CurrentSchemaVersion = 5` to `const CurrentSchemaVersion = 6`.
    5. In `internal/semantic/store/migrations_test.go` add `TestMigration006_BaseOverlayEpochColumn` that: opens a fresh test store (which runs ALL migrations); queries the column existence via the same introspection helper sibling tests use (DuckDB `information_schema.columns` or `PRAGMA table_info`); asserts column present with type UBIGINT; INSERTs a stub snapshot row without setting `base_overlay_epoch` and asserts SELECT returns 0.
  </action>
  <verify>
    <automated>go test ./internal/semantic/store/ -race -run 'TestMigration006|TestMigrationsRegistry|TestCurrentSchemaVersion' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `TestMigration006_BaseOverlayEpochColumn` PASS
    - `grep -nE 'CurrentSchemaVersion = 6' internal/semantic/store/migrations_types.go` matches exactly one line
    - `grep -nE 'From:\s*5,\s*To:\s*6' internal/semantic/store/migrations_registry.go` matches exactly one line
    - `grep -nE 'ADD COLUMN base_overlay_epoch UBIGINT DEFAULT 0' internal/semantic/store/migrations.go` matches exactly one line
    - `grep -nE 'VALUES \(6, now\(\)\)' internal/semantic/store/migrations.go` matches exactly one line (version-stamp present)
    - `go vet ./internal/semantic/store/...` exits 0
  </acceptance_criteria>
  <done>Migration applies cleanly on a fresh store; existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SnapshotMeta.BaseOverlayEpoch + CommitSnapshot persistence + LatestCommittedSnapshotBaseEpoch accessor</name>
  <files>internal/semantic/store/snapshot.go, internal/semantic/store/snapshot_test.go</files>
  <read_first>
    - internal/semantic/store/snapshot.go (lines 86-98 SnapshotMeta; 242-308 BeginSnapshot; 408-460 CommitSnapshot full body)
    - internal/semantic/store/snapshot_test.go (existing CommitSnapshot test shape)
  </read_first>
  <behavior>
    - `SnapshotMeta` exposes a `BaseOverlayEpoch uint64` field
    - `CommitSnapshot` accepts (via its existing argument set — extend `SnapshotSummary` OR add `BaseOverlayEpoch` to the `*Snapshot` struct that's already in scope; pick whichever requires the smallest surface change) the baseline epoch value and writes it to `semantic_snapshots.base_overlay_epoch` in the SAME tx as the `status='committed'` flip
    - A `SELECT base_overlay_epoch FROM semantic_snapshots WHERE snapshot_id=?` after CommitSnapshot returns the value supplied at commit time
    - When the caller does NOT supply a BaseOverlayEpoch (zero value), the column persists 0 (cold-start / no overlay activity)
    - `LatestCommittedSnapshotBaseEpoch(ctx, repoID)` returns `(epoch, true, nil)` for the most recent committed snapshot of `repoID`; `(0, false, nil)` when no committed snapshot exists (cold-start); wrapped error on any other DB failure
  </behavior>
  <action>
    1. Extend `SnapshotMeta` in `snapshot.go` (around line 86-98) with `BaseOverlayEpoch uint64 // Phase 70: overlay epoch captured at CommitSnapshot time; baseline for the next incremental build's OverlayChangedPathsSince query`.
    2. Decide and implement the plumbing path (whichever is smaller). Two acceptable options:
       a. Add `BaseOverlayEpoch uint64` to the in-memory `*Snapshot` handle (the struct that `BeginSnapshot` returns and `CommitSnapshot` consumes), with a `(snap *Snapshot) SetBaseOverlayEpoch(uint64)` setter. CommitSnapshot reads `snap.BaseOverlayEpoch` and writes it. Caller (Plan 04) calls `snap.SetBaseOverlayEpoch(epoch)` before `CommitSnapshot`.
       b. Add a `BaseOverlayEpoch uint64` field to `SnapshotSummary` (if SnapshotSummary is the existing CommitSnapshot argument type). CommitSnapshot reads `summary.BaseOverlayEpoch` and writes it.
       Pick option (a) if Snapshot is the only argument; pick (b) if Summary is already an argument. Document the choice in the task commit message.
    3. Modify the `UPDATE semantic_snapshots SET status='committed', committed_at=now() WHERE snapshot_id=?` statement (snapshot.go:~426) to ALSO set `base_overlay_epoch=?`. The new query becomes `UPDATE semantic_snapshots SET status='committed', committed_at=now(), base_overlay_epoch=? WHERE snapshot_id=?`. Pass the captured `BaseOverlayEpoch` as the additional bind argument before the snapshot ID.
    4. Add `func (s *Store) LatestCommittedSnapshotBaseEpoch(ctx context.Context, repoID string) (epoch uint64, ok bool, err error)` to `snapshot.go` (or sibling file) that runs `SELECT base_overlay_epoch FROM semantic_snapshots WHERE repo_id=? AND status='committed' ORDER BY snapshot_id DESC LIMIT 1`. Returns `(epoch, true, nil)` on hit, `(0, false, nil)` on `errors.Is(err, sql.ErrNoRows)`, and `(0, false, fmt.Errorf("LatestCommittedSnapshotBaseEpoch(%q): %w", repoID, err))` on any other error. Nil-guard the receiver and empty `repoID` (mirror the discipline used by `OverlayChangedPathsSince` in Plan 01). **This is the seam Plan 04's buildFn calls** — it MUST be on `*Store` (not on `*Snapshot`) so the daemon's StoreAccessor can wrap it.
    5. Extend an existing snapshot test (or add `TestCommitSnapshot_PersistsBaseOverlayEpoch`) that: BeginSnapshot, SetBaseOverlayEpoch(42) (or pass via summary), CommitSnapshot, then SELECT — assert column equals 42. Add a second sub-test that omits the setter and asserts column == 0. Add a third test `TestLatestCommittedSnapshotBaseEpoch_RoundTrip` that commits two snapshots with epochs 7 and 11 (in order) and asserts the accessor returns `(11, true, nil)`; a fourth sub-test `TestLatestCommittedSnapshotBaseEpoch_NoSnapshot` that calls the accessor against a fresh store and asserts `(0, false, nil)`.
  </action>
  <verify>
    <automated>go test ./internal/semantic/store/ -race -run 'CommitSnapshot|LatestCommittedSnapshotBaseEpoch' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'BaseOverlayEpoch\s+uint64' internal/semantic/store/snapshot.go` matches at least once
    - `grep -nE 'base_overlay_epoch=\?' internal/semantic/store/snapshot.go` matches at least once
    - `grep -nE 'func \(s \*Store\) LatestCommittedSnapshotBaseEpoch' internal/semantic/store/snapshot.go` matches exactly once
    - `TestCommitSnapshot_PersistsBaseOverlayEpoch` and `TestLatestCommittedSnapshotBaseEpoch_*` (or the named subtests) PASS
    - `go vet ./internal/semantic/store/...` exits 0
    - `make vet-nokernel2semantic` exits 0
  </acceptance_criteria>
  <done>Baseline epoch round-trips through Begin/SetBaseOverlayEpoch/CommitSnapshot/LatestCommittedSnapshotBaseEpoch.</done>
</task>

</tasks>

<verification>
- `go test ./internal/semantic/store/ -race -count=1` exits 0 (full package)
- `go vet ./internal/semantic/store/...` exits 0
- `make vet-nokernel2semantic` exits 0
</verification>

<success_criteria>
- Schema is at v6; `base_overlay_epoch` column exists with DEFAULT 0
- CommitSnapshot persists the baseline epoch atomically with the status flip
- `LatestCommittedSnapshotBaseEpoch` accessor is callable for Plan 04's buildFn
</success_criteria>

<output>
After completion, create `.planning/phases/70-incremental-refresh-overlay-drain/70-02-SUMMARY.md`
</output>
