---
phase: 70
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/semantic/live/coalescer/coalescer.go
  - internal/semantic/live/coalescer/coalescer_test.go
  - internal/semantic/live/service/service.go
  - internal/obs/metrics.go
  - internal/obs/metrics_labels_test.go
autonomous: true
requirements: [REFRESH-01, REFRESH-03]
tags: [coalescer, live-pipeline, metrics, observability]
must_haves:
  truths:
    - "Coalescer.FlushNow(ctx) synchronously drains the active batch and returns after the on-flush hook has fired"
    - "Service.FlushNow(ctx, ws) delegates to the per-workspace coalescer.FlushNow"
    - "FlushNow is re-entrant safe under concurrent Enqueue (no deadlock)"
    - "helix_incremental_refresh_fallback_total CounterVec registered with labels {reason, repo}"
    - "IncrementalRefreshFallbackInc drops unknown reason values (closed enum: cold_start|overlay_rotated|empty_overlay|error)"
    - "metrics_labels_test.go carves out the reason label for the new metric family"
  artifacts:
    - path: "internal/semantic/live/coalescer/coalescer.go"
      provides: "FlushNow synchronous flush API"
      contains: "func (c *Coalescer) FlushNow"
    - path: "internal/semantic/live/service/service.go"
      provides: "Service.FlushNow workspace delegator"
      contains: "func (s *Service) FlushNow"
    - path: "internal/obs/metrics.go"
      provides: "IncrementalRefreshFallback CounterVec + Inc helper"
      contains: "helix_incremental_refresh_fallback_total"
    - path: "internal/obs/metrics_labels_test.go"
      provides: "reason label carve-out for the new metric"
      contains: "helix_incremental_refresh_fallback_total"
  key_links:
    - from: "Service.FlushNow"
      to: "Coalescer.FlushNow"
      via: "per-workspace coalescer dispatch"
      pattern: "FlushNow"
    - from: "obs.Metrics.IncrementalRefreshFallbackInc"
      to: "prometheus CounterVec"
      via: "closed-enum WithLabelValues(reason, repo)"
      pattern: "WithLabelValues\\(reason, repo\\)"
---

<objective>
Land the synchronous-flush plumbing the refresh tool needs to avoid the fire-and-forget race (RESEARCH.md Pitfall 1; CONTEXT.md "Implementation Notes" flush-timing), and register the bounded-label fallback metric the new `collectCandidatePaths` will emit (CONTEXT.md D4; locked).

Purpose: Two prerequisite seams for Plan 04 and Plan 05. Coalescer FlushNow is the Wave 0 explore that RESEARCH.md Assumption A2 flagged — this plan does the work in Wave 1 so Wave 2 can wire without surprises.

Output: One synchronous flush method on the coalescer + one delegator on the live Service + one bounded-label CounterVec + one label allowlist carve-out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-incremental-refresh-overlay-drain/70-CONTEXT.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-RESEARCH.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-PATTERNS.md
@internal/semantic/live/coalescer/coalescer.go
@internal/semantic/live/service/service.go
@internal/obs/metrics.go

<interfaces>
<!-- Pattern source for metric: internal/obs/metrics.go:346-355 (LiveFileFactDiff NewCounterVec), 768-778 (LiveFileFactDiffInc helper) -->
<!-- MustRegister append point: internal/obs/metrics.go:~526 -->
<!-- Carve-out pattern: internal/obs/metrics_labels_test.go ~line 108-112 -->

Coalescer flush hook (existing): coalescer.go:124 `SetOnFlush(fn func())`
Service entry: internal/semantic/live/service/service.go:184 `OnWorkspaceChanged` (fire-and-forget)

Closed-enum reason values (declared as Go const in obs package):
```go
const (
    IncrementalRefreshFallbackReasonColdStart      = "cold_start"
    IncrementalRefreshFallbackReasonOverlayRotated = "overlay_rotated"
    IncrementalRefreshFallbackReasonEmptyOverlay   = "empty_overlay"
    IncrementalRefreshFallbackReasonError          = "error"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coalescer.FlushNow + Service.FlushNow synchronous flush</name>
  <files>internal/semantic/live/coalescer/coalescer.go, internal/semantic/live/coalescer/coalescer_test.go, internal/semantic/live/service/service.go</files>
  <read_first>
    - internal/semantic/live/coalescer/coalescer.go (full file — locate timer field, SetOnFlush at line ~124, makeFlush implementation, Enqueue, lock/mutex usage)
    - internal/semantic/live/coalescer/coalescer_test.go (existing flush-timing tests for the test harness)
    - internal/semantic/live/service/service.go (lines 170-232 — OnWorkspaceChanged + per-workspace coalescer dispatch shape)
  </read_first>
  <behavior>
    - `Coalescer.FlushNow(ctx context.Context) error` blocks until any in-flight batch (timer-triggered or pending) has flushed and the registered `OnFlush` hook has returned. Returns nil on success, ctx.Err() on cancellation.
    - Calling `FlushNow` when no batch is pending returns nil immediately (no-op for empty queue).
    - Concurrent `Enqueue` calls during `FlushNow` do not deadlock; they may be flushed as part of the current call or queued for the next flush — both behaviors are acceptable as long as the test "Enqueue→FlushNow→assert flushed" passes.
    - `Service.FlushNow(ctx context.Context, ws workspace.WorkspaceKey) error` looks up the per-workspace coalescer and calls its `FlushNow`. Returns nil if no coalescer is registered for `ws` (no-op).
  </behavior>
  <action>
    1. Read `coalescer.go` end-to-end first. Identify: (a) the timer field and its Stop()/Reset() pattern, (b) the mutex protecting the pending-batch state, (c) the makeFlush/doFlush function that the timer fires.
    2. Add `func (c *Coalescer) FlushNow(ctx context.Context) error` to `coalescer.go`. Implementation strategy (locked):
       - Acquire the coalescer mutex briefly to: (a) snapshot whether a pending batch exists, (b) cancel the active timer so it does not double-fire, (c) detach the pending batch into a local variable, (d) reset state to "no pending".
       - Release the mutex BEFORE invoking the flush body to avoid re-entry deadlock with `OnFlush` callbacks that might call back into the coalescer.
       - Invoke the existing flush body (the same function the timer calls) with the detached batch.
       - If the implementation already has a `flushLocked()` / `doFlush()` private method, reuse it; do NOT duplicate flush logic.
       - Respect `ctx`: if `ctx.Err() != nil` before the flush body runs, return `ctx.Err()` without running flush (avoid wasting work on a cancelled refresh call).
    3. Add `func (s *Service) FlushNow(ctx context.Context, ws workspace.WorkspaceKey) error` to `internal/semantic/live/service/service.go`. Body: look up `s.coalescers[ws]` (or whatever the per-workspace map field is — read the file and match the exact field name); if absent return nil; otherwise call `coalescer.FlushNow(ctx)` and propagate the error.
    4. Add `TestCoalescer_FlushNow_DrainsPendingBatch` to `coalescer_test.go`: construct a coalescer with a slow timer (e.g., 10s `max_batch_delay`), `Enqueue` a fake event, call `FlushNow(ctx.Background())`, assert the OnFlush hook fired exactly once. Sub-test `TestCoalescer_FlushNow_EmptyIsNoop`: brand-new coalescer with no Enqueue → FlushNow returns nil immediately and OnFlush never fires. Sub-test `TestCoalescer_FlushNow_ConcurrentEnqueueSafe`: spawn 8 goroutines Enqueuing concurrently with a single FlushNow call; assert no deadlock (test completes under a 2s timeout) and that all enqueued items eventually flush (either in this call or a subsequent flush).
  </action>
  <verify>
    <automated>go test ./internal/semantic/live/coalescer/ ./internal/semantic/live/service/ -race -run 'FlushNow' -count=1 -timeout 30s</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'func \(c \*Coalescer\) FlushNow' internal/semantic/live/coalescer/coalescer.go` matches exactly once
    - `grep -nE 'func \(s \*Service\) FlushNow' internal/semantic/live/service/service.go` matches exactly once
    - All three new tests PASS under `-race` within a 30s timeout (no deadlock)
    - `go vet ./internal/semantic/live/...` exits 0
  </acceptance_criteria>
  <done>Synchronous flush path is callable from refresh tool (Plan 05) without coalescer-internal changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IncrementalRefreshFallback metric + labels allowlist carve-out</name>
  <files>internal/obs/metrics.go, internal/obs/metrics_labels_test.go</files>
  <read_first>
    - internal/obs/metrics.go (lines 1-30 doc header on AllowedLabels discipline; 340-410 LiveFileFactDiff field declaration; 520-540 MustRegister block; 760-790 LiveFileFactDiffInc helper)
    - internal/obs/metrics_labels_test.go (full file — locate AllowedLabels constant and Phase 68 carve-out entries ~lines 100-115)
  </read_first>
  <behavior>
    - `obs.Metrics` struct exposes `IncrementalRefreshFallback *prometheus.CounterVec`
    - The vector is registered with `m.registry.MustRegister(...)` in `newMetrics()`
    - `IncrementalRefreshFallbackInc(reason, repo string)` increments the vec only when `reason` is in {"cold_start","overlay_rotated","empty_overlay","error"}; unknown values are dropped silently
    - `IncrementalRefreshFallbackInc` is nil-safe (`m == nil || m.IncrementalRefreshFallback == nil` → return)
    - `metrics_labels_test.go` carve-out permits `reason` (and `repo`) labels on `helix_incremental_refresh_fallback_total` — `TestMetricsLabelsAllowlist` (or its sibling existing test) still passes
    - Reason values are declared as package-level constants in `internal/obs/` (one source of truth)
  </behavior>
  <action>
    1. In `internal/obs/metrics.go` add four package-level constants:
       - `IncrementalRefreshFallbackReasonColdStart = "cold_start"`
       - `IncrementalRefreshFallbackReasonOverlayRotated = "overlay_rotated"`
       - `IncrementalRefreshFallbackReasonEmptyOverlay = "empty_overlay"`
       - `IncrementalRefreshFallbackReasonError = "error"`
       Place near the existing reason-token constants (mirror Phase 68 D-08 placement).
    2. Add the field `IncrementalRefreshFallback *prometheus.CounterVec` to the `Metrics` struct, alongside `LiveFileFactDiff`.
    3. In `newMetrics()` initialize the field exactly like `LiveFileFactDiff` (around metrics.go:346-355):
       - Name `helix_incremental_refresh_fallback_total`
       - Help `"Incremental refresh fell back to full-walk; reason is one of cold_start|overlay_rotated|empty_overlay|error. Phase 70."`
       - Labels `[]string{"reason", "repo"}`
    4. Append `m.IncrementalRefreshFallback,` to the `m.registry.MustRegister(...)` block (around metrics.go:526).
    5. Add `func (m *Metrics) IncrementalRefreshFallbackInc(reason, repo string)`:
       - Nil-guard `if m == nil || m.IncrementalRefreshFallback == nil { return }`
       - `switch reason { case IncrementalRefreshFallbackReasonColdStart, IncrementalRefreshFallbackReasonOverlayRotated, IncrementalRefreshFallbackReasonEmptyOverlay, IncrementalRefreshFallbackReasonError: default: return }`
       - `m.IncrementalRefreshFallback.WithLabelValues(reason, repo).Inc()`
    6. In `internal/obs/metrics_labels_test.go` add the carve-out entry to the same map that holds `"helix_live_filefactdiff_total": {"tier": true, "repo": true}`: `"helix_incremental_refresh_fallback_total": {"reason": true, "repo": true},`. Add a code comment above it: `// Phase 70 D-04: closed-enum "reason" ∈ {cold_start, overlay_rotated, empty_overlay, error} + bounded "repo" identifier on the incremental-refresh fallback counter. Neither label is in AllowedLabels; both are carved out here. Helper IncrementalRefreshFallbackInc is the single emission site and drops unknown reason values.`
    7. Add `TestMetrics_IncrementalRefreshFallbackInc_DropsUnknownReason` to `internal/obs/metrics.go`'s sibling test (or `metrics_test.go`): construct a `*Metrics`, call `Inc("bogus", "r")`, gather metric families, assert no sample emitted; call `Inc("cold_start", "r")`, gather, assert sample with `reason="cold_start"` and value 1.
  </action>
  <verify>
    <automated>go test ./internal/obs/ -race -run 'IncrementalRefreshFallback|MetricsLabelsAllowlist' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'helix_incremental_refresh_fallback_total' internal/obs/metrics.go` matches at least once
    - `grep -nE 'IncrementalRefreshFallback\s+\*prometheus\.CounterVec' internal/obs/metrics.go` matches at least once
    - `grep -nE 'func \(m \*Metrics\) IncrementalRefreshFallbackInc' internal/obs/metrics.go` matches exactly once
    - `grep -nE '"helix_incremental_refresh_fallback_total"' internal/obs/metrics_labels_test.go` matches exactly once
    - `TestMetricsLabelsAllowlist` PASS (existing test still green with new carve-out)
    - `TestMetrics_IncrementalRefreshFallbackInc_DropsUnknownReason` PASS
    - `go vet ./internal/obs/...` exits 0
  </acceptance_criteria>
  <done>Metric is registered, carve-out is honored, helper drops unknowns. Ready for Plan 04 to call IncrementalRefreshFallbackInc.</done>
</task>

</tasks>

<verification>
- `go test ./internal/obs/ ./internal/semantic/live/coalescer/ ./internal/semantic/live/service/ -race -count=1` exits 0
- `go vet ./internal/obs/... ./internal/semantic/live/...` exits 0
- `make vet-nokernel2semantic` exits 0
</verification>

<success_criteria>
- `Coalescer.FlushNow` is race-clean and re-entrant safe
- `Service.FlushNow` is a thin delegator
- New metric is bounded-label and drops unknowns at the emission site
- Allowlist test stays green with the explicit carve-out comment in place
</success_criteria>

<output>
After completion, create `.planning/phases/70-incremental-refresh-overlay-drain/70-03-SUMMARY.md`
</output>
