---
phase: 70
plan: 04
type: execute
wave: 2
depends_on: [70-01, 70-02, 70-03]
files_modified:
  - internal/daemon/semantic_wiring.go
  - internal/daemon/semantic_wiring_test.go
  - internal/skill/semantic/accessors.go
autonomous: true
requirements: [REFRESH-01, REFRESH-03]
tags: [daemon, wiring, incremental-refresh, semantic-store]
must_haves:
  truths:
    - "collectCandidatePaths(mode='incremental') calls *Store.OverlayChangedPathsSince with the baseline from LatestCommittedSnapshotBaseEpoch"
    - "Empty seam result classifies the reason as cold_start (baseEpoch==0) | overlay_rotated (currentEpoch>baseEpoch) | empty_overlay (else)"
    - "Each fallback to full-walk increments helix_incremental_refresh_fallback_total with the matching reason label and emits a slog.Warn line with the same reason token"
    - "buildFn captures CurrentOverlayEpoch immediately before CommitSnapshot and persists it via SnapshotMeta.BaseOverlayEpoch (Pitfall 3 mitigation)"
    - "The refresh-degraded multi-paragraph annotation at semantic_wiring.go:1466-1480 is replaced by a 3-line note pointing at the seam"
    - "StoreAccessor interface in internal/skill/semantic/accessors.go exposes CurrentOverlayEpoch, OverlayChangedPathsSince, and LatestCommittedSnapshotBaseEpoch; LiveAccessor exposes FlushNow"
    - "mode='full' path is byte-identical behavior to today (full-walk preserved verbatim, extracted into fullWalkPaths helper)"
    - "Overlay paths returned by the seam are ABSOLUTE (resolved via A1 in RESEARCH.md §Open Questions RESOLVED); no filepath.Join translation is needed before returning from collectCandidatePaths"
  artifacts:
    - path: "internal/daemon/semantic_wiring.go"
      provides: "Rewritten collectCandidatePaths with seam dispatch + fallback classification + metric emission; buildFn baseline-epoch capture"
      contains: "OverlayChangedPathsSince"
    - path: "internal/skill/semantic/accessors.go"
      provides: "Extended StoreAccessor (adds CurrentOverlayEpoch, OverlayChangedPathsSince, LatestCommittedSnapshotBaseEpoch) and LiveAccessor (adds FlushNow)"
      contains: "OverlayChangedPathsSince"
  key_links:
    - from: "collectCandidatePaths(mode='incremental')"
      to: "*Store.OverlayChangedPathsSince"
      via: "b.store.OverlayChangedPathsSince(ctx, repoID, baseEpoch)"
      pattern: "OverlayChangedPathsSince\\(ctx"
    - from: "buildFn pre-CommitSnapshot"
      to: "*Store.CurrentOverlayEpoch"
      via: "captured at build-end, persisted on snapshot row"
      pattern: "CurrentOverlayEpoch\\(ctx"
    - from: "empty seam result"
      to: "obs.Metrics.IncrementalRefreshFallbackInc"
      via: "closed-enum reason label"
      pattern: "IncrementalRefreshFallbackInc\\("
---

<objective>
Rewrite `collectCandidatePaths` to actually use the `mode` parameter (today it's `_ string` per RESEARCH.md). For `mode=="incremental"`, query the new seam (`*Store.OverlayChangedPathsSince`) using the baseline persisted on the latest committed snapshot. On empty seam result, classify the reason, emit the bounded-label metric (Plan 03), log the bounded reason via slog, and fall back to the existing full-walk. Extend buildFn to capture and persist `BaseOverlayEpoch` at CommitSnapshot time (Pitfall 3). Remove the "refresh-degraded" annotation per CONTEXT.md D6.

Purpose: Direct closure of REFRESH-01 + REFRESH-03 at the daemon wiring layer. The single biggest behavior change of the phase.

Output: Modified `semantic_wiring.go` with a real dispatching `collectCandidatePaths`, an extracted `fullWalkPaths` helper, a baseline-epoch capture in buildFn, an extended StoreAccessor/LiveAccessor interface, and the daemon-side adapters honoring those interfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-incremental-refresh-overlay-drain/70-CONTEXT.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-RESEARCH.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-PATTERNS.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-01-PLAN.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-02-PLAN.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-03-PLAN.md
@internal/daemon/semantic_wiring.go
@internal/skill/semantic/accessors.go

<interfaces>
<!-- Pre-existing call site (semantic_wiring.go:1397): paths := b.collectCandidatePaths(ws, mode) -->
<!-- Current sig: func (b *semanticBundle) collectCandidatePaths(ws workspace.WorkspaceKey, _ string) []string -->
<!-- New sig (Plan 04): func (b *semanticBundle) collectCandidatePaths(ctx context.Context, ws workspace.WorkspaceKey, mode string, baseEpoch uint64) []string -->

<!-- buildFn baseline-epoch capture (Pitfall 3, locked): -->
<!--   1. before CommitSnapshot, baseEpoch := b.store.CurrentOverlayEpoch(ctx, repoID) -->
<!--   2. snap.SetBaseOverlayEpoch(baseEpoch)  // OR pass via SnapshotSummary, matching Plan 02's choice -->
<!--   3. b.store.CommitSnapshot(ctx, snap, summary) — persists the value atomically -->

<!-- For incremental input: baseline pulled via b.store.LatestCommittedSnapshotBaseEpoch(ctx, repoID); (0, false, nil) means cold-start -->

<!-- StoreAccessor extension (internal/skill/semantic/accessors.go:18-33): -->
<!--   CurrentOverlayEpoch(ctx context.Context, repoID string) (uint64, error) -->
<!--   OverlayChangedPathsSince(ctx context.Context, repoID string, baseEpoch uint64) (paths []string, currentEpoch uint64, err error) -->
<!--   LatestCommittedSnapshotBaseEpoch(ctx context.Context, repoID string) (epoch uint64, ok bool, err error) -->

<!-- LiveAccessor extension (internal/skill/semantic/accessors.go:64-72): -->
<!--   FlushNow(ctx context.Context, ws workspace.WorkspaceKey) error -->

<!-- A1 RESOLUTION (RESEARCH.md §Open Questions RESOLVED 2026-05-15): -->
<!--   Overlay rows store ABSOLUTE paths (handler.go:417 writes the verbatim path -->
<!--   produced by fsnotify, which is absolute because watcher.Add receives absRoot). -->
<!--   collectCandidatePaths's full-walk branch also returns absolute paths via -->
<!--   filepath.WalkDir(ws.RepoRoot, …). Both branches therefore agree on path shape; -->
<!--   no filepath.Join(ws.RepoRoot, p) translation is required on the incremental -->
<!--   branch. Plan 04 Task 2 documents this inline and the implementer MUST read -->
<!--   handler.go:392-417 to confirm before committing. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend StoreAccessor + LiveAccessor interfaces; daemon adapters implement them</name>
  <files>internal/skill/semantic/accessors.go, internal/daemon/semantic_wiring.go</files>
  <read_first>
    - internal/skill/semantic/accessors.go (full file — locate StoreAccessor and LiveAccessor interface definitions plus the comment block at 127-131 about D-09 enforcement)
    - internal/daemon/semantic_wiring.go (locate the existing StoreAccessor implementation — likely a daemon-side struct that satisfies it, with methods like LatestCommittedSnapshot and CurrentGraphVersion delegating to b.store)
  </read_first>
  <behavior>
    - `StoreAccessor` interface gains `CurrentOverlayEpoch(ctx, repoID) (uint64, error)`, `OverlayChangedPathsSince(ctx, repoID, baseEpoch) (paths []string, currentEpoch uint64, err error)`, and `LatestCommittedSnapshotBaseEpoch(ctx, repoID) (epoch uint64, ok bool, err error)`
    - `LiveAccessor` interface gains `FlushNow(ctx, ws workspace.WorkspaceKey) error`
    - The daemon-side adapter structs in `semantic_wiring.go` implement the new methods by delegating to `b.store.OverlayChangedPathsSince`, `b.store.CurrentOverlayEpoch`, `b.store.LatestCommittedSnapshotBaseEpoch`, and `b.liveService.FlushNow` respectively
    - All existing call sites of StoreAccessor and LiveAccessor still compile (no removed methods)
  </behavior>
  <action>
    1. In `internal/skill/semantic/accessors.go`:
       - Add `CurrentOverlayEpoch(ctx context.Context, repoID string) (uint64, error)` to the `StoreAccessor` interface block
       - Add `OverlayChangedPathsSince(ctx context.Context, repoID string, baseEpoch uint64) (paths []string, currentEpoch uint64, err error)` to the `StoreAccessor` interface block
       - Add `LatestCommittedSnapshotBaseEpoch(ctx context.Context, repoID string) (epoch uint64, ok bool, err error)` to the `StoreAccessor` interface block (Plan 02 ships this method on *Store; Plan 04 wires the interface seam)
       - Add `FlushNow(ctx context.Context, ws workspace.WorkspaceKey) error` to the `LiveAccessor` interface block
       - Update the D-09 comment block (accessors.go:~127-131) to note that the new Store methods are pure read accessors and that LiveAccessor.FlushNow is a synchronous flush (NOT a snapshot write)
    2. In `internal/daemon/semantic_wiring.go` locate the existing StoreAccessor implementation (search for `func (.*) LatestCommittedSnapshot\(` — the receiver type is the adapter). Add adapter methods:
       - `func (a *<adapterType>) CurrentOverlayEpoch(ctx context.Context, repoID string) (uint64, error) { return a.store.CurrentOverlayEpoch(ctx, repoID) }`
       - `func (a *<adapterType>) OverlayChangedPathsSince(ctx context.Context, repoID string, baseEpoch uint64) (paths []string, currentEpoch uint64, err error) { return a.store.OverlayChangedPathsSince(ctx, repoID, baseEpoch) }`
       - `func (a *<adapterType>) LatestCommittedSnapshotBaseEpoch(ctx context.Context, repoID string) (epoch uint64, ok bool, err error) { return a.store.LatestCommittedSnapshotBaseEpoch(ctx, repoID) }`
    3. Locate the existing LiveAccessor implementation (search for `func (.*) OnWorkspaceChanged\(`). Add `func (a *<liveAdapterType>) FlushNow(ctx context.Context, ws workspace.WorkspaceKey) error { return a.svc.FlushNow(ctx, ws) }` (use the same field name the adapter uses to reference the live service — likely `svc` or `service`; read the file to confirm).
    4. Run `go build ./...` to confirm the interface extension compiles against all existing implementations.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/skill/semantic/... ./internal/daemon/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'OverlayChangedPathsSince\(ctx context\.Context' internal/skill/semantic/accessors.go` matches exactly once (interface declaration)
    - `grep -nE 'CurrentOverlayEpoch\(ctx context\.Context' internal/skill/semantic/accessors.go` matches exactly once (interface declaration)
    - `grep -nE 'LatestCommittedSnapshotBaseEpoch\(ctx context\.Context' internal/skill/semantic/accessors.go` matches exactly once (interface declaration)
    - `grep -nE 'FlushNow\(ctx context\.Context' internal/skill/semantic/accessors.go` matches exactly once (interface declaration)
    - `go build ./...` exits 0
    - `make vet-nokernel2semantic` exits 0
  </acceptance_criteria>
  <done>Interfaces extended; daemon adapters delegate to *Store and *Service. No interface satisfaction errors at build time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: collectCandidatePaths rewrite + buildFn baseline-epoch capture + refresh-degraded annotation removal</name>
  <files>internal/daemon/semantic_wiring.go, internal/daemon/semantic_wiring_test.go</files>
  <read_first>
    - internal/daemon/semantic_wiring.go (lines 1369-1463 — makeProductionBuildFn including the LatestCommittedSnapshot call near 1385 and the CommitSnapshot call near 1446-1450; lines 1466-1520 — current collectCandidatePaths and its annotation block)
    - internal/daemon/semantic_wiring_test.go (existing test patterns for the bundle/buildFn — locate a sibling test that constructs a semanticBundle with a fake store so the new tests can mirror it)
    - internal/obs/metrics.go (IncrementalRefreshFallbackReason* constants from Plan 03 — to import the right symbols)
    - internal/semantic/live/handler/handler.go (lines 392-417 — source of truth for A1: confirm overlay rows store absolute paths verbatim from the caller; this is the seam that must agree with collectCandidatePaths's full-walk branch on path shape)
  </read_first>
  <behavior>
    - `collectCandidatePaths` accepts `ctx context.Context, ws workspace.WorkspaceKey, mode string, baseEpoch uint64` and returns `[]string`
    - `mode="full"` path delegates to `fullWalkPaths(ws)` (the existing filepath.WalkDir body extracted verbatim — no behavior change)
    - `mode="incremental"`:
        - calls `b.store.OverlayChangedPathsSince(ctx, repoID, baseEpoch)` where `repoID := ws.Hash()`
        - on err: logs `b.logger.Warn("collectCandidatePaths: overlay seam error; falling back to full-walk", "repo", repoID, "err", err)`, emits `b.metrics.IncrementalRefreshFallbackInc(obs.IncrementalRefreshFallbackReasonError, repoID)`, returns `fullWalkPaths(ws)`
        - on `len(paths) > 0`: returns `paths` directly (no full-walk). **Per A1 (RESOLVED in RESEARCH.md):** overlay rows store ABSOLUTE paths (handler.go:417 writes the verbatim caller-supplied path; producers — fsnotify watcher, kernel-edit OnEdit — all pass absolute paths). The full-walk branch also returns absolute paths (`filepath.WalkDir(ws.RepoRoot, …)`). Both branches therefore agree on shape; **no `filepath.Join(ws.RepoRoot, p)` translation is required**. If a future producer is added that writes relative paths, this assumption MUST be revisited — flag with an inline `// A1: paths assumed absolute per RESEARCH.md §Open Questions RESOLVED 2026-05-15` comment so the next reader sees the invariant.
        - on empty paths: classifies reason as `cold_start` if `baseEpoch == 0`, else `overlay_rotated` if `currentEpoch > baseEpoch`, else `empty_overlay`; logs `b.logger.Warn("collectCandidatePaths: incremental fell back to full-walk", "repo", repoID, "reason", reason, "base_epoch", baseEpoch, "current_epoch", currentEpoch)`; emits `b.metrics.IncrementalRefreshFallbackInc(reason, repoID)`; returns `fullWalkPaths(ws)`
    - buildFn captures `baseOverlayEpoch, err := b.store.CurrentOverlayEpoch(ctx, repoID)` IMMEDIATELY before `b.store.CommitSnapshot(...)` and threads it into the snapshot (via `snap.SetBaseOverlayEpoch(baseOverlayEpoch)` or the SnapshotSummary field — whichever Plan 02 chose). On err: log slog.Warn with `"event":"capture_base_overlay_epoch_failed"` and pass 0 (cold-start signal for the next build).
    - buildFn for `mode="incremental"` reads the baseline by calling `baseEpoch, _, _ := b.store.LatestCommittedSnapshotBaseEpoch(ctx, repoID)` (introduced by Plan 02) BEFORE calling `collectCandidatePaths`, and passes that value
    - The refresh-degraded multi-paragraph comment block at the old `collectCandidatePaths` declaration is replaced with the 3-line block specified in CONTEXT.md D6 verbatim
  </behavior>
  <action>
    1. Read `semantic_wiring.go` lines 1369-1520. Locate the existing call `paths := b.collectCandidatePaths(ws, mode)` (around line 1397) and update it to `paths := b.collectCandidatePaths(ctx, ws, mode, baseEpoch)` where `baseEpoch` is obtained earlier in the function (see step 3). **Before editing, also read `internal/semantic/live/handler/handler.go:392-417` to confirm A1: overlay rows store absolute paths.** This read is mandatory — it is the source of truth for the "no filepath.Join translation" invariant the new code relies on. If the read reveals path normalization that was added between research time and execution time (unlikely but possible), DO NOT proceed; surface the gap.
    2. Replace the entire `collectCandidatePaths` function (lines ~1466-1520) with a new dispatcher that matches the behavior block above. Extract the existing `filepath.WalkDir` body into a new private method `func (b *semanticBundle) fullWalkPaths(ws workspace.WorkspaceKey) []string` — copy the body verbatim (including the `.helix`, `.git`, and dot-dir exclusions) so `mode="full"` behavior is byte-identical. In the `len(paths) > 0` branch, add the inline comment `// A1: paths are absolute per RESEARCH.md §Open Questions RESOLVED — overlay producer (handler.go:417) writes the verbatim caller-supplied absolute path; no filepath.Join translation needed.`
    3. In `makeProductionBuildFn` (around line 1378-1463): immediately after the existing `LatestCommittedSnapshot` call (line ~1385), add a `LatestCommittedSnapshotBaseEpoch` call to derive `baseEpoch` for the incremental path. For `mode != "incremental"` set `baseEpoch = 0` (full mode ignores it). Pass `baseEpoch` to `collectCandidatePaths`.
    4. In the same function, IMMEDIATELY before `b.store.CommitSnapshot(...)` (around line 1446), insert:
       ```go
       baseOverlayEpoch, err := b.store.CurrentOverlayEpoch(ctx, repoID)
       if err != nil {
           b.logger.Warn("buildFn: capture base_overlay_epoch failed; persisting 0", "repo", repoID, "err", err)
           baseOverlayEpoch = 0
       }
       snap.SetBaseOverlayEpoch(baseOverlayEpoch)  // or pass via SnapshotSummary depending on Plan 02 choice
       ```
       Confirm the exact method/field name against Plan 02's chosen option by reading `snapshot.go` for the setter or summary field; emit whichever matches.
    5. Replace the comment block at the top of `collectCandidatePaths` (the multi-paragraph "refresh-degraded" annotation, lines 1466-1480) with EXACTLY this text (CONTEXT.md D6 verbatim):
       ```go
       // collectCandidatePaths builds the candidate path set the production buildFn
       // classifies + extracts.
       //   - mode=full: walk ws.RepoRoot via filepath.WalkDir (.helix, .git, dot-dirs excluded).
       //   - mode=incremental: query OverlayChangedPathsSince(baseEpoch); on empty
       //     result, fall back to full-walk and emit the bounded-label fallback metric.
       ```
    6. Add five integration tests to `internal/daemon/semantic_wiring_test.go`:
       - `TestCollectCandidatePaths_Incremental_HitReturnsSeamPaths`: fake store returns `(paths=["/abs/a.go"], currentEpoch=10, nil)` for `OverlayChangedPathsSince`; assert result is `["/abs/a.go"]` AND `IncrementalRefreshFallbackInc` was NOT called (verify via a `*Metrics` snapshot or a recorder around the metrics struct). Use absolute paths in the fixture to mirror A1.
       - `TestCollectCandidatePaths_Incremental_EmptySeamFallback_ColdStart`: fake store returns `(nil, 0, nil)`, `baseEpoch=0`; assert result equals `fullWalkPaths(ws)` AND fallback metric incremented with reason="cold_start".
       - `TestCollectCandidatePaths_Incremental_EmptySeamFallback_OverlayRotated`: fake store returns `([], 20, nil)`, `baseEpoch=10`; assert result equals `fullWalkPaths(ws)` AND fallback metric incremented with reason="overlay_rotated".
       - `TestCollectCandidatePaths_Incremental_EmptySeamFallback_EmptyOverlay`: fake store returns `([], 10, nil)`, `baseEpoch=10`; assert result equals `fullWalkPaths(ws)` AND fallback metric incremented with reason="empty_overlay".
       - `TestCollectCandidatePaths_Full_UsesWalker`: `mode="full"` with a tempdir workspace containing one fixture file; assert seam is NOT called and the file is in the returned slice (absolute path).
       Use a fake `*Store` (or interface stub) that records calls; reuse any sibling fake/mock pattern already present in `semantic_wiring_test.go` if available.
  </action>
  <verify>
    <automated>go test ./internal/daemon/ -race -run 'TestCollectCandidatePaths' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All 5 sub-tests PASS under -race
    - `grep -nE 'func \(b \*semanticBundle\) fullWalkPaths' internal/daemon/semantic_wiring.go` matches exactly once
    - `grep -nE 'OverlayChangedPathsSince\(ctx' internal/daemon/semantic_wiring.go` matches at least once
    - `grep -nE 'IncrementalRefreshFallbackInc' internal/daemon/semantic_wiring.go` matches at least once
    - `grep -nE 'SetBaseOverlayEpoch|BaseOverlayEpoch:' internal/daemon/semantic_wiring.go` matches at least once (baseline capture wired)
    - `grep -nE 'refresh-degraded' internal/daemon/semantic_wiring.go` returns no matches (annotation removed)
    - `grep -c '^// collectCandidatePaths builds the candidate path set' internal/daemon/semantic_wiring.go` returns 1 (new 3-line note present)
    - `grep -nE 'A1:.*absolute' internal/daemon/semantic_wiring.go` matches at least once (A1 invariant documented inline)
    - `go vet ./internal/daemon/...` exits 0
    - `make vet-nokernel2semantic` exits 0
  </acceptance_criteria>
  <done>Incremental path actively uses the seam; fallback path is observable via metric + log; baseline epoch is persisted at commit time; old annotation is gone; A1 path-form invariant is documented inline so future readers do not have to re-derive it.</done>
</task>

</tasks>

<verification>
- `go test ./internal/daemon/ ./internal/skill/semantic/ ./internal/semantic/store/ -race -count=1` exits 0
- `go vet ./...` exits 0
- `make vet-nokernel2semantic` exits 0
- `grep -nE 'refresh-degraded' internal/daemon/semantic_wiring.go` returns no matches
</verification>

<success_criteria>
- `mode="incremental"` exits through the seam (hot path) OR through full-walk with a classified reason (fallback)
- `mode="full"` behavior is byte-identical to today
- buildFn captures and persists the baseline at commit time (Pitfall 3 mitigation)
- StoreAccessor + LiveAccessor interfaces are ready for Plan 05 to consume
- A1 (absolute-path invariant) is verified at execution time and documented inline
</success_criteria>

<output>
After completion, create `.planning/phases/70-incremental-refresh-overlay-drain/70-04-SUMMARY.md`
</output>
