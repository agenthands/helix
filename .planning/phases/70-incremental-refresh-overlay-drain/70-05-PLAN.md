---
phase: 70
plan: 05
type: tdd
wave: 3
depends_on: [70-01, 70-03, 70-04]
files_modified:
  - internal/skill/semantic/tools_refresh.go
  - internal/skill/semantic/tools_refresh_test.go
autonomous: true
requirements: [REFRESH-01]
tags: [mcp-tool, refresh, semantic-skill]
must_haves:
  truths:
    - "tools_refresh.go captures CurrentOverlayEpoch (preEpoch) BEFORE calling LiveAccessor.OnWorkspaceChanged"
    - "After OnWorkspaceChanged, the handler calls LiveAccessor.FlushNow(ctx, ws) and tolerates non-fatal errors (logged, continues)"
    - "files_updated = len(paths) where paths = StoreAccessor.OverlayChangedPathsSince(ctx, ws.Hash(), preEpoch)"
    - "When args.Paths is non-empty (strict-subset request), files_updated still reflects the seam result intersected against args.Paths (or remains as seam count — see action for the exact policy)"
    - "tools_refresh.go remains free of Begin/Commit/Abort/Write snapshot tokens (D-09/D-13 grep gate stays green)"
  artifacts:
    - path: "internal/skill/semantic/tools_refresh.go"
      provides: "Honest files_updated derivation from the seam"
      contains: "OverlayChangedPathsSince"
  key_links:
    - from: "handleRefreshSemanticGraph"
      to: "StoreAccessor.OverlayChangedPathsSince"
      via: "post-FlushNow seam read against preEpoch baseline"
      pattern: "OverlayChangedPathsSince\\(ctx"
    - from: "handleRefreshSemanticGraph"
      to: "LiveAccessor.FlushNow"
      via: "synchronous drain after OnWorkspaceChanged"
      pattern: "FlushNow\\(ctx"
---

<objective>
Replace the `files_updated := len(args.Paths)` wart at `internal/skill/semantic/tools_refresh.go:170-176` with an honest count derived from the seam. The handler captures the pre-drain overlay epoch, fires `OnWorkspaceChanged`, synchronously drains via `FlushNow`, and then queries `OverlayChangedPathsSince(preEpoch)` for the path set that actually landed (CONTEXT.md D1, locked).

Purpose: Closes the second consumer of the seam (CONTEXT.md D1 — single seam, two consumers). Preserves D-09/D-13 invariants: no snapshot tokens added to this file.

Output: Modified handler with three new operations (preEpoch capture, FlushNow call, seam read) and one removed line (`len(args.Paths)`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-incremental-refresh-overlay-drain/70-CONTEXT.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-RESEARCH.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-PATTERNS.md
@internal/skill/semantic/tools_refresh.go
@internal/skill/semantic/accessors.go

<interfaces>
<!-- Pre-existing call site (tools_refresh.go:156-160): -->
<!--   if s.live != nil { if err := s.live.OnWorkspaceChanged(ws, args.Paths); err != nil { ... } } -->

<!-- Replacement shape (RESEARCH.md §Code Examples): -->
<!--   1. preEpoch, _ := s.store.CurrentOverlayEpoch(ctx, ws.Hash()) -->
<!--   2. live.OnWorkspaceChanged(ws, args.Paths) -->
<!--   3. live.FlushNow(ctx, ws) — non-fatal on error -->
<!--   4. changed, _, _ := s.store.OverlayChangedPathsSince(ctx, ws.Hash(), preEpoch) -->
<!--   5. filesUpdated := len(changed) -->

<!-- D-09 / D-13 invariant (tools_refresh.go:14-23, 124-129): NO Begin/Commit/Abort/Write tokens may be added to this file. -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — TestRefresh_FilesUpdated_FromSeam</name>
  <files>internal/skill/semantic/tools_refresh_test.go</files>
  <read_first>
    - internal/skill/semantic/tools_refresh.go (lines 130-237 — full handler)
    - internal/skill/semantic/tools_refresh_test.go (full file — locate the existing recorder-mock pattern that flags D-09/D-13 violations and the fixture-construction helpers for handleRefreshSemanticGraph)
    - internal/skill/semantic/accessors.go (full StoreAccessor / LiveAccessor — to know what the recorder mock implements)
  </read_first>
  <behavior>
    - `TestRefresh_FilesUpdated_FromSeam_UnfilteredDrain`: fake StoreAccessor returns `preEpoch=5` from `CurrentOverlayEpoch`; fake LiveAccessor records `OnWorkspaceChanged` + `FlushNow` calls; fake StoreAccessor returns `(paths=["a.go","b.go"], currentEpoch=7, nil)` from `OverlayChangedPathsSince(_, _, 5)`; handler invoked with `args.Paths == nil`; assert response envelope's `files_updated == 2` (NOT `len(args.Paths) == 0`).
    - `TestRefresh_FilesUpdated_FromSeam_FilteredSubset`: same fake setup, but `args.Paths == ["a.go"]`; the seam returns the same `["a.go","b.go"]`. Assert `files_updated == 2` (seam-truth) unless this plan adopts an intersect-with-args policy — pick ONE policy in the action step and document it.
    - `TestRefresh_FilesUpdated_Seam_FlushNowErrorIsNonFatal`: fake `FlushNow` returns an error; handler still proceeds to call `OverlayChangedPathsSince`; response is success; the error is logged via slog (verify via a captured logger if the harness exposes one — otherwise omit the log assertion).
    - `TestRefresh_FilesUpdated_NoLive_StillReads`: handler with `s.live == nil`; preEpoch read still happens; seam still read; result reflects whatever the seam returns (likely 0 since no drain).
    - Recorder-mock assertion: existing snapshot-write rejection (the fake fails the test loudly if `BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshot` are reached through) must remain green.
  </behavior>
  <action>
    Add the four sub-tests to `internal/skill/semantic/tools_refresh_test.go`. Use the existing fake/mock structs (read the file first to confirm naming — likely `fakeStoreAccessor`, `fakeLiveAccessor` or similar). Extend the fakes with the new methods (`CurrentOverlayEpoch`, `OverlayChangedPathsSince`, `FlushNow`) recording calls and returning canned values per sub-test. **Policy decision for the FilteredSubset case (LOCK IN THE TEST):** the seam returns the authoritative count; `files_updated` reflects what landed since `preEpoch`, NOT the strict subset. Comment this policy choice next to the test assertion: "files_updated reflects seam truth (overlay landings since preEpoch); the args.Paths filter governs WHICH paths are driven into the live service, not the response count. The seam can legitimately surface fewer or more paths than args.Paths depending on coalescer behavior." Run the test FIRST and confirm RED: assertion failures pointing at `files_updated == len(args.Paths)`.
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/ -race -run 'TestRefresh_FilesUpdated_FromSeam|TestRefresh_FilesUpdated_Seam_FlushNowErrorIsNonFatal|TestRefresh_FilesUpdated_NoLive_StillReads' -count=1 2>&1 | grep -E "FAIL|files_updated"</automated>
  </verify>
  <acceptance_criteria>
    - At least one of the four new tests fails with an assertion mentioning `files_updated`
    - Build succeeds (fakes implement the extended interface from Plan 04)
    - Recorder-mock snapshot-write rejection still green (no false-positive trigger from the new test setup)
  </acceptance_criteria>
  <done>Tests committed RED as `test(70-05): add failing tests for seam-derived files_updated`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — rewrite handleRefreshSemanticGraph files_updated derivation</name>
  <files>internal/skill/semantic/tools_refresh.go</files>
  <read_first>
    - internal/skill/semantic/tools_refresh.go (lines 14-23 + 124-129 — D-09/D-13 invariant comments; lines 130-237 — full handler; lines 156-176 — current OnWorkspaceChanged + filesUpdated derivation)
  </read_first>
  <action>
    Modify `handleRefreshSemanticGraph` in `internal/skill/semantic/tools_refresh.go`:
    1. Immediately BEFORE the existing `if s.live != nil {` block (around line 156), insert:
       ```go
       // Phase 70 D1: capture pre-drain overlay epoch so we can ask the seam
       // "what landed since I started this call?" after FlushNow returns.
       var preEpoch uint64
       if s.store != nil {
           preEpoch, _ = s.store.CurrentOverlayEpoch(ctx, ws.Hash())
       }
       ```
    2. Inside the `if s.live != nil {` block, AFTER `OnWorkspaceChanged` returns successfully, BEFORE leaving the block, add:
       ```go
       // Phase 70 Pitfall-1 mitigation: synchronously drain the coalescer so
       // the subsequent seam read sees the post-drain state. Non-fatal: if
       // flush errors, we still surface whatever the seam reports, which is
       // bounded by max_batch_delay_ms in the worst case.
       if err := s.live.FlushNow(ctx, ws); err != nil {
           slog.Default().Warn("refresh_semantic_graph: live flush error; files_updated may undercount",
               "ws", ws.Hash(), "err", err)
       }
       ```
       Confirm whether `slog` is already imported in this file; if not, add the `log/slog` import.
    3. Replace the lines previously occupied by:
       ```go
       // (the long files_updated wart comment block + filesUpdated := len(args.Paths))
       ```
       (around tools_refresh.go:170-176) with:
       ```go
       // Phase 70 D1: derive files_updated from the seam — the actual set of
       // paths whose overlay rows landed since preEpoch. Honest envelope:
       // empty preEpoch read or empty seam result both report 0; non-zero
       // means real changes hit the overlay during this call.
       var filesUpdated int
       if s.store != nil {
           changed, _, _ := s.store.OverlayChangedPathsSince(ctx, ws.Hash(), preEpoch)
           filesUpdated = len(changed)
       }
       ```
    4. Do NOT add any `Begin/Commit/Abort/Write` snapshot tokens. The new code only calls `CurrentOverlayEpoch`, `OverlayChangedPathsSince`, and `FlushNow` — all read/flush APIs.
    5. Run the test suite from Task 1 — all four new tests must now PASS.
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/ -race -run 'TestRefresh' -count=1 && bash -c 'set -e; grep -nE "\b(BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshot)\b" internal/skill/semantic/tools_refresh.go && exit 1; exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - All four new tests PASS under -race
    - Existing `tools_refresh_test.go` tests still pass (no regressions)
    - `grep -nE '\b(BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshot)\b' internal/skill/semantic/tools_refresh.go` returns 0 matches (D-09/D-13 grep gate green)
    - `grep -nE 'files_updated semantics: when args\.Paths is supplied' internal/skill/semantic/tools_refresh.go` returns 0 matches (old wart comment removed)
    - `grep -nE 'OverlayChangedPathsSince\(ctx' internal/skill/semantic/tools_refresh.go` matches exactly once
    - `grep -nE 'FlushNow\(ctx' internal/skill/semantic/tools_refresh.go` matches exactly once
    - `go vet ./internal/skill/semantic/...` exits 0
    - `make vet-nokernel2semantic` exits 0
  </acceptance_criteria>
  <done>files_updated is honest. D-09/D-13 grep gate stays green. Commit as `feat(70-05): refresh files_updated derives from overlay seam`.</done>
</task>

</tasks>

<verification>
- `go test ./internal/skill/semantic/ -race -count=1` exits 0
- `go vet ./internal/skill/semantic/...` exits 0
- `make vet-nokernel2semantic` exits 0
- D-09/D-13 grep gate: `grep -nE '\b(BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshot)\b' internal/skill/semantic/tools_refresh.go` returns no matches
</verification>

<success_criteria>
- `refresh_semantic_graph.files_updated` reflects what actually landed during the call, not `len(args.Paths)`
- FlushNow synchronously drains the coalescer so the seam read sees the post-drain state
- D-09/D-13 read-only invariant on `tools_refresh.go` preserved
</success_criteria>

<output>
After completion, create `.planning/phases/70-incremental-refresh-overlay-drain/70-05-SUMMARY.md`
</output>
