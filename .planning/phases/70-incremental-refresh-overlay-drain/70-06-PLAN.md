---
phase: 70
plan: 06
type: execute
wave: 4
depends_on: [70-04, 70-05]
files_modified:
  - internal/eval/runner/refresh_incremental_test.go
  - internal/eval/runner/refresh_harness_test.go
autonomous: true
requirements: [REFRESH-03]
tags: [e2e, eval-runner, incremental-refresh, fallback]
must_haves:
  truths:
    - "TestRefreshIncremental_SingleFileChanged_OnlyThatFileTouched proves the seam hot path: 1 file edited → exactly 1 candidate path returned AND IncrementalRefreshFallback metric did NOT increment"
    - "TestRefreshIncremental_Fallback_ColdStart proves fallback with reason=cold_start (no prior committed snapshot → baseEpoch=0 → seam returns empty or paths but reason classifier picks cold_start)"
    - "TestRefreshIncremental_Fallback_OverlayRotated proves fallback with reason=overlay_rotated (baseEpoch>0, currentEpoch>baseEpoch, len(paths)==0 after forced compaction)"
    - "TestRefreshIncremental_Fallback_EmptyOverlay proves fallback with reason=empty_overlay (baseEpoch>0, currentEpoch==baseEpoch, no edits since)"
    - "Each fallback sub-test verifies BOTH the metric increment AND the slog.Warn line emission with the matching reason token"
    - "All four sub-tests are race-clean (default -race)"
  artifacts:
    - path: "internal/eval/runner/refresh_incremental_test.go"
      provides: "REFRESH-03 correctness suite (4 sub-tests)"
      contains: "TestRefreshIncremental"
    - path: "internal/eval/runner/refresh_harness_test.go"
      provides: "Shared test harness (newRefreshHarness + makeFixtureFactsNFiles)"
      contains: "newRefreshHarness"
  key_links:
    - from: "TestRefreshIncremental_SingleFileChanged_OnlyThatFileTouched"
      to: "semanticBundle.collectCandidatePaths"
      via: "SetCollectCandidatePathsHook test seam"
      pattern: "SetCollectCandidatePathsHook"
    - from: "fallback sub-tests"
      to: "obs.Metrics.IncrementalRefreshFallback"
      via: "CounterVec sampling with reason label"
      pattern: "IncrementalRefreshFallback"
---

<objective>
Land the REFRESH-03 correctness test surface in `internal/eval/runner/` per ROADMAP success criterion #4 (which names the file path verbatim). Four sub-tests prove the hot path (seam) AND all three fallback reasons (cold_start, overlay_rotated, empty_overlay). CONTEXT.md D5 locks this surface.

Purpose: Functional proof that the seam wiring works end-to-end against a real `*Store` + buildFn + coalescer + handler stack (not just unit tests against mocks).

Output: One harness helper file + one correctness test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-incremental-refresh-overlay-drain/70-CONTEXT.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-RESEARCH.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-PATTERNS.md
@internal/eval/runner/inprocess.go
@internal/eval/runner/inprocess_fixtures_test.go
@internal/skill/semantic/integration_test.go

<interfaces>
<!-- Fixture pattern source: internal/skill/semantic/integration_test.go:332-360 makeFixtureFacts (1-file, N-symbol) -->
<!-- RESEARCH.md instruction: extract SIBLING helper makeFixtureFactsNFiles(numFiles, symbolsPerFile int) — do NOT modify makeFixtureFacts (used by 6+ tests) -->
<!-- Test seam: semanticBundle.SetCollectCandidatePathsHook(func([]string)) — added in Plan 04 if not already there; if absent, Task 1 here adds a minimal hook -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refresh harness helpers (newRefreshHarness + makeFixtureFactsNFiles + editFile + indexFull + refresh)</name>
  <files>internal/eval/runner/refresh_harness_test.go</files>
  <read_first>
    - internal/skill/semantic/integration_test.go (lines 325-481 — makeFixtureFacts, newE2EHarness, the existing E2E pattern that constructs *Store + bleve + bundle in a tempdir)
    - internal/eval/runner/inprocess.go (full file — the eval-runner's in-process bundle construction; identify the seams for store / live service / metrics)
    - internal/eval/runner/inprocess_fixtures_test.go (manual-sampling pattern at lines 18-65)
    - internal/daemon/semantic_wiring.go (the semanticBundle struct + collectCandidatePaths signature — to identify whether a test hook is already exposed or needs adding)
  </read_first>
  <action>
    1. Read `internal/skill/semantic/integration_test.go` to confirm `makeFixtureFacts(symbolCount int)` signature. Per RESEARCH.md, DO NOT modify it. Add a sibling helper `makeFixtureFactsNFiles(numFiles, symbolsPerFile int) semanticstore.Facts` to `refresh_harness_test.go` (the new test file) that produces `numFiles` distinct `FileFact{Path: fmt.Sprintf("src/file_%d.go", i), ...}` rows and `numFiles * symbolsPerFile` deterministic `SymbolFact` rows.
    2. Create `newRefreshHarness(t *testing.T, numFiles, symbolsPerFile int) *refreshHarness` constructor. The harness struct exposes:
       - `bundle *semanticBundle` (or the daemon-adapter equivalent — read inprocess.go for the actual type)
       - `store *semanticstore.Store`
       - `live live.Service` (the real coalescer-backed service so FlushNow works)
       - `metrics *obs.Metrics`
       - `ws workspace.WorkspaceKey`
       - `tempdir string` (cleaned up via `t.Cleanup`)
       - Helper methods: `indexFull(t)` (writes the fixture as a committed snapshot via WriteSnapshotFacts + CommitSnapshot, capturing BaseOverlayEpoch=current_epoch), `editFile(t, idx int, content string)` (writes file content to tempdir AND fires `live.OnWorkspaceChanged` for the path so a real overlay row lands), `refresh(t, paths []string)` (calls `handleRefreshSemanticGraph` against the bundle and returns the envelope), `forceCompactPastBaseline(t)` (calls the existing compaction path with a snapshot's captured epoch ≥ baseline so all baseline-era overlay rows are swept — used by overlay_rotated sub-test)
    3. If `semanticBundle` does NOT already expose a way to record `collectCandidatePaths` invocations for testing, add a minimal in-package hook (in `internal/daemon/semantic_wiring.go`):
       ```go
       // Test-only hook for Phase 70 REFRESH-03 verification.
       func (b *semanticBundle) SetCollectCandidatePathsHook(fn func(paths []string)) { b.collectCandidatePathsHook = fn }
       ```
       Wire `b.collectCandidatePathsHook` to fire once at the end of `collectCandidatePaths` with the returned slice. This is the only `semantic_wiring.go` edit Task 1 makes; if Plan 04 already exposed the hook, skip this step entirely. Either way, the harness's `refresh()` method registers a hook that captures the slice into `h.lastCandidates`.
    4. The harness uses a real coalescer (from Plan 03) so `FlushNow` works end-to-end. Wire `s.live` on the constructed semantic-skill state to the harness's live service so `tools_refresh.go` calls FlushNow against a real coalescer.
  </action>
  <verify>
    <automated>go test ./internal/eval/runner/ -race -run 'TestRefreshHarness_Smoke' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/eval/runner/refresh_harness_test.go` exists
    - `grep -nE 'func newRefreshHarness' internal/eval/runner/refresh_harness_test.go` matches exactly once
    - `grep -nE 'func makeFixtureFactsNFiles' internal/eval/runner/refresh_harness_test.go` matches exactly once
    - Trivial smoke test `TestRefreshHarness_Smoke` constructs the harness with `(3, 5)`, calls `indexFull`, and asserts no error — PASSES
    - `go vet ./internal/eval/runner/...` exits 0
  </acceptance_criteria>
  <done>Harness is reusable by Task 2 (correctness) and Plan 07 (bench).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TestRefreshIncremental — 4 sub-tests (hot path + 3 fallback reasons)</name>
  <files>internal/eval/runner/refresh_incremental_test.go</files>
  <read_first>
    - internal/eval/runner/refresh_harness_test.go (the helpers from Task 1)
    - internal/daemon/semantic_wiring.go (the collectCandidatePaths implementation from Plan 04 — to confirm the exact reason classification + log line shape)
    - internal/obs/metrics.go (the IncrementalRefreshFallback CounterVec + reason constants from Plan 03)
  </read_first>
  <behavior>
    - All four sub-tests use the harness from Task 1
    - Sub-test 1 (hot path): build a 3-file workspace, `indexFull` (commits a snapshot with `BaseOverlayEpoch = currentEpoch_after_index`), `editFile(1, "// new\n")` which fires `OnWorkspaceChanged` and lands an overlay row at write_epoch > BaseOverlayEpoch, then `refresh(nil)`. Assert: `h.lastCandidates` contains exactly one element, that element's path ends with `file_1.go`, AND the `IncrementalRefreshFallback` counter for this workspace is 0 (verify by sampling the prom metric with `dto.Metric` collection).
    - Sub-test 2 (cold_start): fresh harness, NO `indexFull` (so `LatestCommittedSnapshotBaseEpoch` returns `(0, false, nil)` → `baseEpoch=0`); call `refresh(nil)`. Assert: fallback metric incremented exactly once with `reason="cold_start"`. (Note: cold_start can also fire when the seam returns paths with `baseEpoch=0` — confirm whether the Plan 04 classifier picks cold_start in BOTH the empty AND non-empty cases when baseEpoch=0. RESEARCH.md says cold_start = baseEpoch=0; reread Plan 04 task 2 action step 2 — the hot-path branch returns paths early WITHOUT classifying when `len(paths) > 0`. So in this sub-test the seam must return EMPTY for the cold_start label to fire. Force this by NOT writing any overlay rows before calling refresh. If the seam returns paths anyway, this sub-test instead asserts the hot-path early-return — adapt the assertion to match Plan 04's actual behavior, which Task 2 of Plan 04 documents as: cold_start fires when baseEpoch==0 AND len(paths)==0.) For sub-test 2 specifically: ensure no overlay activity → seam returns `(nil, 0, nil)` → fallback fires with reason="cold_start".
    - Sub-test 3 (overlay_rotated): `indexFull` (captures baseEpoch=N), `editFile(0)` (overlay write at epoch N+1), then force compaction past N+1 by calling whatever Phase 63 compaction surface the harness exposes (the harness's `forceCompactPastBaseline(t)` method from Task 1). After compaction, `currentEpoch > baseEpoch` AND the post-N+1 overlay rows are all swept → seam returns `([], currentEpoch>baseEpoch, nil)`. Call `refresh(nil)`. Assert: fallback metric incremented with `reason="overlay_rotated"`.
    - Sub-test 4 (empty_overlay): `indexFull` (baseEpoch=N), NO edits, call `refresh(nil)`. Seam returns `([], N, nil)` (currentEpoch unchanged from baseEpoch). Assert: fallback metric incremented with `reason="empty_overlay"`.
    - Each fallback sub-test ALSO verifies the slog.Warn line was emitted by capturing the logger output via a `slog.Handler` that records messages into a buffer (set on the harness during construction).
    - All sub-tests race-clean: `-race -count=1`.
  </behavior>
  <action>
    Create `internal/eval/runner/refresh_incremental_test.go` with one parent `TestRefreshIncremental` function using `t.Run` for each of the four sub-tests. Each sub-test:
    1. Constructs a fresh harness via `newRefreshHarness(t, 3, 5)`.
    2. Captures the metric counter sample BEFORE invoking refresh (so the test can assert the delta, not absolute value — important if other tests in the package register on the same metric).
    3. Performs the sub-test-specific setup (indexFull / editFile / forceCompactPastBaseline as appropriate).
    4. Calls `h.refresh(t, nil)`.
    5. Samples the metric counter AFTER and computes the delta. For the hot-path sub-test, assert delta == 0. For each fallback sub-test, assert delta == 1 AND the label set matches the expected `reason`.
    6. For fallback sub-tests, also assert the captured log buffer contains a line with `level=WARN` AND `reason=<expected>`.
    Use `dto.Metric` style sampling (the standard prometheus client_golang gather-and-walk pattern) to read CounterVec values by label.
  </action>
  <verify>
    <automated>go test ./internal/eval/runner/ -race -run TestRefreshIncremental -count=1 -timeout 60s</automated>
  </verify>
  <acceptance_criteria>
    - All four sub-tests PASS under `-race -count=1` within 60s
    - `grep -nE 'TestRefreshIncremental_SingleFileChanged_OnlyThatFileTouched|TestRefreshIncremental_Fallback_ColdStart|TestRefreshIncremental_Fallback_OverlayRotated|TestRefreshIncremental_Fallback_EmptyOverlay' internal/eval/runner/refresh_incremental_test.go` matches at least 4 lines (one per sub-test name, whether implemented as separate Test functions or t.Run sub-tests — match either)
    - `grep -nE 'reason.*cold_start' internal/eval/runner/refresh_incremental_test.go` matches at least once
    - `grep -nE 'reason.*overlay_rotated' internal/eval/runner/refresh_incremental_test.go` matches at least once
    - `grep -nE 'reason.*empty_overlay' internal/eval/runner/refresh_incremental_test.go` matches at least once
    - `go vet ./internal/eval/runner/...` exits 0
    - `make vet-nokernel2semantic` exits 0
  </acceptance_criteria>
  <done>REFRESH-03 closed: hot path proven via seam; all three fallback reasons proven by direct sub-tests.</done>
</task>

</tasks>

<verification>
- `go test ./internal/eval/runner/ -race -count=1 -timeout 90s` exits 0
- `go vet ./...` exits 0
- `make vet-nokernel2semantic` exits 0
</verification>

<success_criteria>
- ROADMAP success criterion #4 satisfied at the named file path
- REFRESH-03 requirement closed
- All three closed-enum fallback reasons have direct test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/70-incremental-refresh-overlay-drain/70-06-SUMMARY.md`
</output>
