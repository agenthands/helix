---
phase: 70
plan: 07
type: execute
wave: 4
depends_on: [70-04, 70-05, 70-06]
files_modified:
  - internal/eval/runner/bench_refresh_incremental_test.go
autonomous: true
requirements: [REFRESH-02]
tags: [bench, eval-runner, performance, local-only]
must_haves:
  truths:
    - "TestBench_RefreshIncremental_10kSymbols_P95Under200ms runs locally with 200 files × 50 symbols = 10k symbols"
    - "Bench samples 50 iterations of single-file-edit + incremental refresh, computes p95, asserts p95 ≤ 200ms"
    - "Bench is t.Skip'd when os.Getenv(\"CI\") != \"\" (project rule feedback_no_ci_benchmarks)"
    - "Bench is t.Skip'd under testing.Short() (mirrors TestRunQuickFullFixtureSetWallTime convention)"
    - "Fixture is built ONCE (via sync.Once or TestMain) so per-iteration cost reflects refresh latency, not snapshot construction (Pitfall 5 mitigation)"
    - "Bench logs p50, p95, p99 for visibility even when the assertion passes"
  artifacts:
    - path: "internal/eval/runner/bench_refresh_incremental_test.go"
      provides: "REFRESH-02 local-only bench harness"
      contains: "TestBench_RefreshIncremental_10kSymbols_P95Under200ms"
  key_links:
    - from: "bench iteration loop"
      to: "harness.refresh / harness.editFile"
      via: "single-file-change scenario per iteration"
      pattern: "h\\.editFile\\(t, i"
    - from: "bench skip gate"
      to: "os.Getenv(\"CI\")"
      via: "early t.Skip"
      pattern: "os\\.Getenv\\(\"CI\"\\)"
---

<objective>
Land the REFRESH-02 local-only bench harness that proves p95 ≤ 200ms for the single-file-changed incremental refresh against a 10k-symbol workspace. The bench `t.Skip`s on CI per project rule (no CI benchmarks).

Purpose: Performance evidence that the seam delivers the speedup the ROADMAP and CONTEXT promise. Direct closure of REFRESH-02.

Output: One new test file reusing the Plan 06 harness, scaled to 200 files × 50 symbols.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-incremental-refresh-overlay-drain/70-CONTEXT.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-RESEARCH.md
@.planning/phases/70-incremental-refresh-overlay-drain/70-PATTERNS.md
@internal/eval/runner/inprocess_fixtures_test.go
@internal/eval/runner/refresh_harness_test.go

<interfaces>
<!-- Skip-on-CI pattern (project rule): -->
<!--   if os.Getenv("CI") != "" { t.Skip("bench is local-only per project rule feedback_no_ci_benchmarks") } -->
<!--   if testing.Short() { t.Skip("bench skipped under -short") } -->

<!-- Manual p95 sampling pattern (RESEARCH.md §Code Examples, inprocess_fixtures_test.go:18-65 analog): -->
<!--   sort.Slice(samples, ...); p95 := samples[int(0.95*float64(len(samples)))] -->

<!-- Pitfall 5 mitigation: build fixture ONCE; reset only overlay state between iterations -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TestBench_RefreshIncremental_10kSymbols_P95Under200ms</name>
  <files>internal/eval/runner/bench_refresh_incremental_test.go</files>
  <read_first>
    - internal/eval/runner/inprocess_fixtures_test.go (lines 18-65 — TestRunQuickFullFixtureSetWallTime manual-sampling + skip pattern)
    - internal/eval/runner/refresh_harness_test.go (the harness from Plan 06 — to know how to call indexFull and editFile)
    - .planning/phases/70-incremental-refresh-overlay-drain/70-CONTEXT.md (D5 — fixture scale 10k symbols ≈ 200 files × 50 symbols)
  </read_first>
  <behavior>
    - Test name: `TestBench_RefreshIncremental_10kSymbols_P95Under200ms`
    - First action: skip gates. If `os.Getenv("CI") != ""` → `t.Skip("bench is local-only per project rule feedback_no_ci_benchmarks")`. If `testing.Short()` → `t.Skip("bench skipped under -short")`. Both gates must be present.
    - Build fixture ONCE: `h := newRefreshHarness(t, 200 /*files*/, 50 /*symbolsPerFile*/)` then `h.indexFull(t)`. The harness is reused across all iterations of the loop below — do NOT call newRefreshHarness inside the loop.
    - Loop: for `i := 0; i < 50; i++` { `h.editFile(t, i%200, fmt.Sprintf("// iter %d\n", i))`; `start := time.Now()`; `h.refresh(t, nil)`; `samples = append(samples, time.Since(start))` }
    - After loop: `sort.Slice(samples, func(i, j int) bool { return samples[i] < samples[j] })`. Compute `p50 := samples[len(samples)/2]`, `p95 := samples[int(0.95*float64(len(samples)))]`, `p99 := samples[len(samples)-1]`. Log all three via `t.Logf("p50=%v p95=%v p99=%v", p50, p95, p99)` so local runs surface the numbers even when the assertion passes.
    - Assertion: `if p95 > 200*time.Millisecond { t.Errorf("p95 %v exceeds 200ms budget", p95) }`.
    - Race-clean is NOT required for the bench (the test does not run under `-race` in normal CI because of the skip gate; local runs can opt in but the assertion budget is for the non-race build).
  </behavior>
  <action>
    Create `internal/eval/runner/bench_refresh_incremental_test.go` with exactly one test function `TestBench_RefreshIncremental_10kSymbols_P95Under200ms(t *testing.T)`. Required imports: `os`, `sort`, `testing`, `time`, `fmt`. Body matches the behavior block above. Use the harness from Plan 06 (`newRefreshHarness`) verbatim — no harness modifications in this plan. If the harness lacks one of the required methods, fail the task and surface the gap rather than reimplementing inline.
  </action>
  <verify>
    <automated>CI= go test ./internal/eval/runner/ -run TestBench_RefreshIncremental_10kSymbols_P95Under200ms -count=1 -timeout 5m -v 2>&1 | grep -E "p50=|p95=|p99=|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/eval/runner/bench_refresh_incremental_test.go` exists with exactly one test function named `TestBench_RefreshIncremental_10kSymbols_P95Under200ms`
    - `grep -nE 'os\.Getenv\("CI"\) != ""' internal/eval/runner/bench_refresh_incremental_test.go` matches exactly once
    - `grep -nE 'testing\.Short\(\)' internal/eval/runner/bench_refresh_incremental_test.go` matches exactly once
    - `grep -nE '200\*time\.Millisecond' internal/eval/runner/bench_refresh_incremental_test.go` matches exactly once
    - `grep -nE 'newRefreshHarness\(t, 200, 50\)' internal/eval/runner/bench_refresh_incremental_test.go` matches exactly once (10k symbols = 200 × 50)
    - Verify the SKIP path works on CI: `CI=true go test ./internal/eval/runner/ -run TestBench_RefreshIncremental_10kSymbols_P95Under200ms -count=1 -v 2>&1 | grep -E "SKIP"` matches the skip output
    - Local non-skip run logs `p50=`, `p95=`, `p99=` values and PASSES (p95 ≤ 200ms) on the developer's workstation
    - `go vet ./internal/eval/runner/...` exits 0
  </acceptance_criteria>
  <done>REFRESH-02 closed by local bench evidence; bench is invisible to CI by construction.</done>
</task>

</tasks>

<verification>
- `CI=true go test ./internal/eval/runner/ -run TestBench_RefreshIncremental_10kSymbols_P95Under200ms -count=1 -v` shows `--- SKIP` (no real bench run on CI)
- `CI= go test ./internal/eval/runner/ -run TestBench_RefreshIncremental_10kSymbols_P95Under200ms -count=1 -v -timeout 5m` (locally) shows `--- PASS` with p95 ≤ 200ms logged
- `go vet ./internal/eval/runner/...` exits 0
- No GitHub workflow file references `TestBench_RefreshIncremental` (CI gate stays clean): `grep -rE 'TestBench_RefreshIncremental' .github/ || echo "clean"` returns `clean`
</verification>

<success_criteria>
- p95 ≤ 200ms locally on a single-file-changed incremental refresh against 10k symbols
- Bench skips silently on CI
- p50/p95/p99 visible in test output for local performance regression tracking
</success_criteria>

<output>
After completion, create `.planning/phases/70-incremental-refresh-overlay-drain/70-07-SUMMARY.md`
</output>
