---
phase: 71-p1-single-symbol-read-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/semantic/store/effective_graph.go
  - internal/semantic/store/effective_graph_test.go
  - internal/skill/semantic/accessors.go
  - internal/skill/semantic/skill.go
  - internal/skill/semantic/seed_resolve.go
  - internal/skill/semantic/seed_resolve_test.go
  - internal/skill/semantic/populated_graph_fixture_test.go
autonomous: true
requirements:
  - P1TOOL-01
  - P1TOOL-02
  - P1TOOL-06
tags:
  - semantic
  - store-accessor
  - mcp-skill
  - seed-resolution

must_haves:
  truths:
    - "*Store exposes QuerySymbolByName(ctx, repoID, path, name) returning up to 6 matching SymbolIDs ordered deterministically"
    - "*Store exposes LatestExtractorRunID(ctx, repoID) returning a monotonic run identifier"
    - "internal/skill/semantic/accessors.go declares SymbolByNameAccessor, ExtractorRunAccessor, ClusterMembershipAccessor narrow interfaces"
    - "SemanticSkill.SetSymbolByName / SetExtractorRun / SetClusterMembership setters compile and are guarded by s.mu"
    - "seed_resolve.go.resolveSeed returns ResolvedSeed with closed-enum Resolution ∈ {exact, ambiguous, not_found}; ambiguous list capped at 5"
    - "populated_graph_fixture_test.go builds a fixture with ≥1 Go + ≥1 TypeScript + ≥1 Java symbol and a handful of CALLS / RESOLVES_TO / USES_TYPE edges, callable by other plans' tests"
  artifacts:
    - path: "internal/semantic/store/effective_graph.go"
      provides: "QuerySymbolByName, LatestExtractorRunID read accessors"
      contains: "func (s *Store) QuerySymbolByName"
    - path: "internal/skill/semantic/accessors.go"
      provides: "SymbolByNameAccessor / ExtractorRunAccessor / ClusterMembershipAccessor"
      contains: "type SymbolByNameAccessor interface"
    - path: "internal/skill/semantic/skill.go"
      provides: "SetSymbolByName / SetExtractorRun / SetClusterMembership"
      contains: "func (s *SemanticSkill) SetSymbolByName"
    - path: "internal/skill/semantic/seed_resolve.go"
      provides: "resolveSeed shared helper + Resolution closed enum + ResolvedSeed struct"
      contains: "type Resolution string"
    - path: "internal/skill/semantic/populated_graph_fixture_test.go"
      provides: "multi-language Go/TS/Java populated-graph fixture builder"
      contains: "func buildPopulatedGraphFixture"
  key_links:
    - from: "internal/skill/semantic/seed_resolve.go"
      to: "internal/skill/semantic/accessors.go::SymbolByNameAccessor"
      via: "s.symbolByName.QuerySymbolByName"
      pattern: "s\\.symbolByName\\.QuerySymbolByName"
    - from: "internal/skill/semantic/accessors.go"
      to: "internal/semantic/store/effective_graph.go::QuerySymbolByName"
      via: "narrow interface implemented by *Store wrapper"
      pattern: "QuerySymbolByName"
---

<objective>
Land the four read-only seams Phase 71 handlers need before they can be written: a name-keyed symbol lookup on *Store, a latest-extractor-run identifier on *Store, three narrow accessor interfaces in the semantic skill, a shared seed-resolve helper, and a multi-language populated-graph fixture builder consumed by every plan-71 handler test.

Purpose: Phase 71's three handlers (71-03/04/05) all need (file_path, symbol_name) → SymbolID resolution (D1) and the FreshnessV2 envelope (D5). Currently neither exists. This plan establishes the seams so wave-2 handler work parallelizes cleanly and shares a single fixture for the multi-language success criterion.

Output: Two new *Store SQL accessors, three new narrow interfaces + setters, one shared seed-resolve helper with its tests, and the populated_graph_fixture_test.go builder used by every Phase 71 handler test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-p1-single-symbol-read-tools/71-CONTEXT.md
@.planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md
@.planning/phases/71-p1-single-symbol-read-tools/71-PATTERNS.md
@.planning/phases/71-p1-single-symbol-read-tools/71-VALIDATION.md
@CLAUDE.md

@internal/semantic/store/effective_graph.go
@internal/skill/semantic/accessors.go
@internal/skill/semantic/skill.go
@internal/skill/semantic/handler_helpers.go

<interfaces>
<!-- Key shapes downstream Phase 71 plans depend on. Embedded here so 71-02/03/04/05 do not need to re-grep. -->

From internal/semantic/store/effective_graph.go (existing, study before adding):
- func (s *Store) QuerySymbolByLocation(ctx context.Context, repoID, path string, line, col uint32) (string, bool, error)
- func (s *Store) LatestCommittedSnapshot(ctx context.Context, repoID string) (uint64, error)
- func (s *Store) CurrentGraphVersion(ctx context.Context, repoID string) (uint64, error)
- func (s *Store) OverlayHasPendingRows(repoID string) bool

From internal/skill/semantic/accessors.go (existing narrow interfaces to mirror):
- type StoreAccessor interface { ... }
- type QueueAccessor interface { DepthAll(ws workspace.WorkspaceKey) int; LastEnqueueAt(ws workspace.WorkspaceKey) int64 }
- type RetrievalAccessor interface { QueryBleve(...) ; PersonalizedPageRank(...) }
- type SchedulerAccessor interface { ClusterStatus(repoID string) ... }

From internal/semantic/integ:
- type SymbolID string
- type SemanticLookup interface { SymbolID(file string, line, col uint32) (SymbolID, error); ExpandFrom(...) }

Closed-enum convention (envelope.go:14-32):
- type Freshness string + const block; downstream plans copy this shape for Resolution and FreshnessStatus.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QuerySymbolByName + LatestExtractorRunID on *Store with tests</name>
  <read_first>
    - internal/semantic/store/effective_graph.go (read in full; QuerySymbolByLocation lines 537-582 and LatestCommittedSnapshot near line 385 are the exact analogs)
    - internal/semantic/store/effective_graph_test.go (study TestQuerySymbolByLocation* tables to mirror table shape)
    - .planning/phases/71-p1-single-symbol-read-tools/71-PATTERNS.md § "Pattern H: Store accessor SQL shape"
    - .planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md § Pitfall 1 (race with overlay writes) + Open Question / A4 (extractor_run_id may not have a backing column)
  </read_first>
  <action>
Add two new read-only accessors on `*Store` in `internal/semantic/store/effective_graph.go`:

1. `QuerySymbolByName(ctx context.Context, repoID, path, name string) ([]string, error)` — returns up to 6 `stable_key` (SymbolID) matches in deterministic order (ORDER BY stable_key ASC) for the given (path, name) at the latest committed snapshot. Filter columns: `f.path = ? AND sym.name = ?` (planner: if the column is `qualified_name` instead of `name`, use that — confirm by SMTC `goto_definition` on the existing symbol-insertion sites in the same file). Use `LIMIT 6` so the caller can detect `>5` and truncate ambiguous candidates per D1's cap of 5. Follow the existing `QuerySymbolByLocation` shape exactly: nil-guard (`if s == nil || s.db == nil { return nil, errors.New(...) }`), `LatestCommittedSnapshot` lookup, `sql.ErrNoRows` is a clean miss (`return nil, nil`), wrap real errors with `fmt.Errorf("QuerySymbolByName(%q,%q): %w", path, name, err)`.

2. `LatestExtractorRunID(ctx context.Context, repoID string) (string, error)` — returns a monotonic identifier (string form) for the most recent extractor pass against `repoID`. Per A4, a backing column may not exist. Resolution policy:
   - First, probe whether `semantic_files.extractor_run_id` (or a `semantic_extractor_runs` table) exists. If yes, `SELECT MAX(extractor_run_id) FROM semantic_files WHERE repo_id = ? AND snapshot_id = ?` against `LatestCommittedSnapshot`.
   - If no such column exists, derive the run id as `fmt.Sprintf("snap-%d", latestSnapshotID)` and document inline: this gives downstream consumers a monotonically-advancing id that changes whenever the snapshot advances. This is the cheapest correct behavior; a dedicated column can replace it later without changing the accessor signature.
   - Empty repo / snapshot==0 → `return "", nil`.

Add tests to `internal/semantic/store/effective_graph_test.go`:
- `TestQuerySymbolByName_Exact` — single match returns 1 row.
- `TestQuerySymbolByName_Ambiguous` — multiple matches return all rows (≤6), ordered.
- `TestQuerySymbolByName_NotFound` — unknown name returns nil slice, nil error.
- `TestQuerySymbolByName_NilStore` — nil receiver returns error.
- `TestLatestExtractorRunID_PopulatedSnapshot` — returns non-empty id.
- `TestLatestExtractorRunID_EmptyRepo` — returns "", nil.

All tests run under `-race`. Mirror the existing TestQuerySymbolByLocation* table style.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/semantic/store/ && go test ./internal/semantic/store/ -run 'TestQuerySymbolByName|TestLatestExtractorRunID' -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func (s \*Store) QuerySymbolByName" internal/semantic/store/effective_graph.go` returns one hit
    - `grep -n "func (s \*Store) LatestExtractorRunID" internal/semantic/store/effective_graph.go` returns one hit
    - Both new accessors call `LatestCommittedSnapshot` (read-only path) and never reference `Begin/Commit/Abort/Write`
    - All six new test cases pass under `-race -count=1`
    - `go vet ./internal/semantic/store/...` is clean
  </acceptance_criteria>
  <done>Two new lock-free read accessors land on *Store with race-clean tests covering exact, ambiguous, not-found, and nil-receiver paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add three narrow accessor interfaces + setters + locate CI grep-gate</name>
  <read_first>
    - internal/skill/semantic/accessors.go (read in full; QueueAccessor lines 71-79 is the narrow-interface template)
    - internal/skill/semantic/skill.go (read in full; SetStore/SetScheduler block ~lines 60-115 is the post-init-setter template)
    - .planning/phases/71-p1-single-symbol-read-tools/71-PATTERNS.md § Pattern F + Pattern G
    - .planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md Open Question 1 (grep-gate runner location)
    - internal/skill/semantic/tools_refresh.go (header comment, lines 15-23)
  </read_first>
  <action>
Extend `internal/skill/semantic/accessors.go` with three new narrow interfaces (each declared independently — do not extend existing interfaces, per Pattern F):

- `SymbolByNameAccessor` — single method `QuerySymbolByName(ctx context.Context, repoID, path, name string) ([]integ.SymbolID, error)`. Inside the accessor wrapper that satisfies this interface (place adjacent to the existing `*Store` adapter in the daemon-wiring layer — Phase 71-03/04/05 plans wire the production binding), translate `[]string` returned by `*Store.QuerySymbolByName` into `[]integ.SymbolID` via a simple cast loop.
- `ExtractorRunAccessor` — single method `LatestExtractorRunID(ctx context.Context, repoID string) (string, error)`.
- `ClusterMembershipAccessor` — single method `ClusterIDOf(ctx context.Context, repoID string, symbolID integ.SymbolID) (clusterID uint64, size int, err error)`. Per RESEARCH Open Question 2 / A3: implementation may be a 1-row SELECT against `semantic_cluster_members` (read accessor on `*Store` lands later in 71-04 if needed); the interface itself MUST land in this plan so wave-2 plans can declare dependencies. If 71-04 chooses to disable the cluster boost, the production binding returns `(0, 0, nil)` and `find_related_symbols` emits `fallback_reason: "cluster_boost_unavailable"`.

Extend `internal/skill/semantic/skill.go`:
- Add three private fields to `SemanticSkill`: `symbolByName SymbolByNameAccessor`, `extractorRun ExtractorRunAccessor`, `clusterMembership ClusterMembershipAccessor`.
- Add three setters following the existing `SetStore` pattern (mu.Lock / defer mu.Unlock / assign). Names: `SetSymbolByName`, `SetExtractorRun`, `SetClusterMembership`.

CI grep-gate location task: search for the grep gate runner. Probe (in this order):
- `grep -rn 'BeginSnapshot' .github/workflows/ scripts/ Makefile 2>/dev/null`
- `grep -rn 'tools_refresh' .github/workflows/ scripts/ Makefile 2>/dev/null`
- `grep -rn 'read-only' .github/workflows/ 2>/dev/null`

If a runner is found, record its path in this task's SUMMARY (plan 71-05 extends it). If no runner is found, document the absence in the SUMMARY — plan 71-05 will implement a Go-side gate test mirroring `tools_refresh_test.go:116-135` recorder pattern instead.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/ && go build ./internal/skill/semantic/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type SymbolByNameAccessor interface" internal/skill/semantic/accessors.go` returns 1
    - `grep -c "type ExtractorRunAccessor interface" internal/skill/semantic/accessors.go` returns 1
    - `grep -c "type ClusterMembershipAccessor interface" internal/skill/semantic/accessors.go` returns 1
    - `grep -c "func (s \*SemanticSkill) SetSymbolByName" internal/skill/semantic/skill.go` returns 1
    - `grep -c "func (s \*SemanticSkill) SetExtractorRun" internal/skill/semantic/skill.go` returns 1
    - `grep -c "func (s \*SemanticSkill) SetClusterMembership" internal/skill/semantic/skill.go` returns 1
    - SUMMARY records grep-gate runner location (found-path or "absent — Go-side gate planned in 71-05")
    - Package builds and vets cleanly
  </acceptance_criteria>
  <done>Three narrow interfaces and three setters compile; CI grep-gate location resolved (file path or documented absence).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement resolveSeed helper + multi-language populated-graph fixture builder</name>
  <read_first>
    - internal/skill/semantic/handler_helpers.go (validatePaths is the analog for shared input-validation helpers)
    - internal/skill/semantic/tools_refresh_test.go lines 38-50 + 113-135 (recorder-style mock pattern with t.Fatal canaries; test imports header)
    - internal/skill/semantic/tools_context.go lines 31-104 + 151-163 (handler entry shape — confirms the SeedInput JSON contract)
    - .planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md lines 256-303 (drafted resolveSeed pseudocode)
    - .planning/phases/71-p1-single-symbol-read-tools/71-PATTERNS.md § "No Analog Found" (fixture must be built fresh)
    - internal/semantic/store/effective_graph_test.go (mine for SQL insertion helpers used to build a populated *Store in tests)
  </read_first>
  <behavior>
    - Test SeedResolve_SymbolID: SeedInput{SymbolID:"sym_abc"} returns ResolvedSeed{SymbolID:"sym_abc", Resolution:ResolutionExact, AmbiguousCandidates:nil}.
    - Test SeedResolve_FileAndName_Exact: (file_path, symbol_name) matching exactly one candidate returns Resolution:ResolutionExact.
    - Test SeedResolve_FileAndName_Ambiguous: 3 candidates returned by accessor → Resolution:ResolutionAmbiguous, AmbiguousCandidates=[3 entries], primary symbol_id = candidates[0].
    - Test SeedResolve_FileAndName_AmbiguousCappedAtFive: 6 candidates → AmbiguousCandidates length is exactly 5.
    - Test SeedResolve_FileAndName_NotFound: 0 candidates → Resolution:ResolutionNotFound, SymbolID is zero value.
    - Test SeedResolve_MissingBoth: SeedInput{} → error via serr.InvalidArgs with message mentioning "symbol_id OR (file_path AND symbol_name)".
    - Test SeedResolve_FilePathOnly: SeedInput{FilePath:"f.go"} (missing symbol_name) → error.
    - Test SeedResolve_AccessorError: accessor returns error → resolveSeed propagates wrapped error.
    - Test PopulatedGraphFixture_MultiLanguage: buildPopulatedGraphFixture(t) returns a fixture with ≥1 Go symbol, ≥1 TypeScript symbol, ≥1 Java symbol; at least one CALLS edge, one RESOLVES_TO edge, and one USES_TYPE edge across the three languages; the fixture exposes accessors that satisfy SymbolByNameAccessor, ExtractorRunAccessor, ClusterMembershipAccessor for use by other plans' tests.
  </behavior>
  <action>
RED → GREEN → REFACTOR.

**RED:** Create `internal/skill/semantic/seed_resolve_test.go` and `internal/skill/semantic/populated_graph_fixture_test.go` with the failing tests above. The fixture test asserts that `buildPopulatedGraphFixture(t)` returns a `*PopulatedGraphFixture` struct whose accessors return non-empty results for at least one Go, one TS, and one Java symbol. Run tests and confirm RED. Commit: `test(71-01): add failing tests for resolveSeed + multi-language fixture`.

**GREEN:** Implement:

1. `internal/skill/semantic/seed_resolve.go`:
   - `type Resolution string` with closed-enum const block (mirror envelope.go:14-32 style): `ResolutionExact`, `ResolutionAmbiguous`, `ResolutionNotFound`.
   - `type SeedInput struct { SymbolID string; FilePath string; SymbolName string }` with `json:"…,omitempty"` and `jsonschema:` tags per RESEARCH lines 261-268.
   - `type ResolvedSeed struct { SymbolID integ.SymbolID; Resolution Resolution; AmbiguousCandidates []integ.SymbolID }`.
   - `func (s *SemanticSkill) resolveSeed(ctx context.Context, ws workspace.WorkspaceKey, in SeedInput) (ResolvedSeed, error)`:
     - If `in.SymbolID != ""` → return Exact, no accessor call.
     - If `in.FilePath == "" || in.SymbolName == ""` → return error via `serr.New(serr.InvalidArgs, "seed requires symbol_id OR (file_path AND symbol_name)")`.
     - Else call `s.symbolByName.QuerySymbolByName(ctx, ws.Hash(), in.FilePath, in.SymbolName)`.
     - Switch on `len(candidates)`: 0 → NotFound; 1 → Exact with candidates[0]; >1 → Ambiguous with `candidates[:min(5, len(candidates))]` as ambiguous list and `candidates[0]` as primary SymbolID.

2. `internal/skill/semantic/populated_graph_fixture_test.go`:
   - `PopulatedGraphFixture` struct holds: a backing `*store.Store` (built via the existing test helpers in `internal/semantic/store/effective_graph_test.go`) populated with Go/TS/Java symbols and edges; pre-built accessor implementations (`SymbolByNameAccessor`, `ExtractorRunAccessor`, `ClusterMembershipAccessor`); known seed identifiers exported as fields (e.g., `GoSeedSymbolID integ.SymbolID`, `TSSeedSymbolID`, `JavaSeedSymbolID`).
   - `func buildPopulatedGraphFixture(t *testing.T) *PopulatedGraphFixture` — t.Helper(); commits one snapshot with at least three files (one per language), each carrying ≥2 symbols and the cross-symbol edges (CALLS Go→Go, RESOLVES_TO TS symbol→type, USES_TYPE Java symbol→type). Use minimal but realistic stable_key values.
   - The fixture is EXPORTED at package level so 71-03/04/05 tests can import and reuse it. Document the fixture in a header comment as "shared multi-language graph fixture — DO NOT inline-copy in per-tool test files; reuse via buildPopulatedGraphFixture(t)".

Run tests; all RED tests turn GREEN. Commit: `feat(71-01): implement resolveSeed + populated multi-language fixture`.

**REFACTOR:** Extract any duplicated SQL-insert plumbing into local helper funcs inside the fixture file. Re-run tests under `-race -count=1`. If clean, commit: `refactor(71-01): tidy fixture builder helpers`.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/ && go test ./internal/skill/semantic/ -run 'TestSeedResolve|TestPopulatedGraphFixture' -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type Resolution string" internal/skill/semantic/seed_resolve.go` returns 1
    - `grep -c "func (s \*SemanticSkill) resolveSeed" internal/skill/semantic/seed_resolve.go` returns 1
    - `grep -c "func buildPopulatedGraphFixture" internal/skill/semantic/populated_graph_fixture_test.go` returns 1
    - All eight resolveSeed test cases + the multi-language fixture test pass under `-race`
    - Fixture exposes ≥1 Go, ≥1 TS, ≥1 Java symbol verified by grep on the fixture field names
    - `go vet ./internal/skill/semantic/...` clean
    - Three commits land in git log: `test(71-01)`, `feat(71-01)`, optionally `refactor(71-01)`
  </acceptance_criteria>
  <done>resolveSeed helper passes all eight behavior tests; buildPopulatedGraphFixture returns a Go/TS/Java fixture used by every Phase 71 handler plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → semantic skill | Untrusted (file_path, symbol_name) inputs cross here; resolveSeed validates and `validatePaths` (Phase 64) reused downstream |
| Semantic skill → *Store | Trusted internal seam; read-only by D-09 invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01-01 | Tampering | QuerySymbolByName SQL builder | mitigate | Parameterized SQL only (`?` placeholders) — no string concat of path/name into the query |
| T-71-01-02 | Information Disclosure | LatestExtractorRunID | accept | run_id is opaque monotonic — no PII; surface to read+ tier only |
| T-71-01-03 | Denial of Service | QuerySymbolByName unbounded result | mitigate | `LIMIT 6` cap in SQL; D1 ambiguous cap of 5 applied in resolveSeed |
| T-71-01-04 | Tampering | resolveSeed accepting both SymbolID and file/name | mitigate | If SymbolID present, accessor is NOT called — short-circuit prevents lookup amplification |
</threat_model>

<verification>
- `go vet ./internal/semantic/store/... ./internal/skill/semantic/...` clean
- `go test ./internal/semantic/store/ ./internal/skill/semantic/ -race -count=1` green
- New accessors compile without breaking existing `*Store` API consumers
- Fixture is import-reusable by 71-03/04/05 tests (verified at plan-merge time)
- No `Begin/Commit/Abort/Write` tokens in new code paths
</verification>

<success_criteria>
- All three tasks pass their automated verify
- Package vet clean; race-clean test runs
- All six must_haves truths observable in the repo
- Phase 71 wave-2 plans (71-03/04) can import `buildPopulatedGraphFixture` and the three accessor interfaces with no remaining seam work
</success_criteria>

<output>
Create `.planning/phases/71-p1-single-symbol-read-tools/71-01-SUMMARY.md` when done. Include the CI grep-gate runner location (or documented absence) for plan 71-05 to consume.
</output>
