---
phase: 71-p1-single-symbol-read-tools
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/skill/semantic/edge_kind_surface.go
  - internal/skill/semantic/edge_kind_surface_test.go
  - internal/skill/semantic/envelope.go
  - internal/skill/semantic/envelope_test.go
autonomous: true
requirements:
  - P1TOOL-01
  - P1TOOL-02
  - P1TOOL-06
tags:
  - semantic
  - edge-kind
  - envelope
  - closed-enum

must_haves:
  truths:
    - "edge_kind_surface.go declares a closed lowercase enum EdgeKindSurface with values calls/references/implements/extends/has_type/uses_type/contains/other"
    - "MapInternalKind(\"RESOLVES_TO\") returns EdgeKindHasType (D3 Pitfall 2 fix)"
    - "MapInternalKind on any unmapped internal string returns EdgeKindOther without panic"
    - "FreshnessV2 struct exposes graph_version, snapshot_id, extractor_run_id, as_of_unix_ms, status fields"
    - "FreshnessStatus closed enum has exactly three values: current, stale, unknown (D5, Phase 69 D1)"
    - "FreshnessSource closed enum has exactly three values: graph, type_resolver_ladder, ast_fallback (D5)"
    - "Existing Freshness enum (Phase 64) and CommonEnvelope are unchanged — additive only"
  artifacts:
    - path: "internal/skill/semantic/edge_kind_surface.go"
      provides: "EdgeKindSurface closed enum + MapInternalKind function"
      contains: "func MapInternalKind"
    - path: "internal/skill/semantic/envelope.go"
      provides: "FreshnessV2 struct + FreshnessStatus closed enum + FreshnessSource closed enum"
      contains: "type FreshnessV2 struct"
  key_links:
    - from: "internal/skill/semantic/edge_kind_surface.go"
      to: "internal/semantic/graph/repair.go internal kind strings (CALLS / RESOLVES_TO / USES_TYPE / etc.)"
      via: "one-way switch table"
      pattern: "case \"RESOLVES_TO\""
---

<objective>
Land the two pure-data primitives Phase 71 handlers consume: the closed edge-kind surface enum (D3) plus its internal→surface mapper, and the FreshnessV2 envelope (D5) carrying graph_version + snapshot_id + extractor_run_id + as_of_unix_ms + closed-enum status. Both are additive — the existing Freshness/CommonEnvelope contract used by Phase 64 tools is unchanged.

Purpose: All three Phase 71 handlers emit edges classified by surface enum (D3) and stamp responses with the V2 freshness envelope (D5). Implementing these primitives in wave 1 lets 71-03/04/05 land in parallel in wave 2 without contention.

Output: Two new closed-enum / mapper files and one extended envelope file with their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-p1-single-symbol-read-tools/71-CONTEXT.md
@.planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md
@.planning/phases/71-p1-single-symbol-read-tools/71-PATTERNS.md
@.planning/phases/71-p1-single-symbol-read-tools/71-VALIDATION.md

@internal/skill/semantic/envelope.go
@internal/skill/semantic/envelope_test.go

<interfaces>
<!-- Closed-enum convention (envelope.go:14-32, existing Phase 64 pattern): -->
<!--
type Freshness string
const (
    FreshnessFresh Freshness = "fresh"
    FreshnessStale Freshness = "stale"
    FreshnessStructurallyFreshSemanticallyPending Freshness = "structurally_fresh_semantically_pending"
    FreshnessOverlayActive Freshness = "overlay_active"
)
-->
<!-- Internal kind strings observed in-tree (RESEARCH §"Edge kind surface mapping"): -->
<!-- CALLS, REFERENCES, IMPLEMENTS, EXTENDS, RESOLVES_TO, USES_TYPE, CONTAINS, DEFINED_IN, IMPORTS -->
<!-- RESOLVES_TO maps to has_type (NOT uses_type) — Pitfall 2 in 71-RESEARCH.md. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Edge-kind surface enum + MapInternalKind (RED → GREEN → REFACTOR)</name>
  <read_first>
    - internal/skill/semantic/envelope.go (closed-enum const block convention, lines 14-32)
    - internal/skill/semantic/envelope_test.go (table-driven test style)
    - .planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md lines 319-351 (drafted enum + mapper code) and § Pitfall 2 (RESOLVES_TO → has_type)
    - .planning/phases/71-p1-single-symbol-read-tools/71-PATTERNS.md § Pattern I (closed-enum const block)
    - internal/semantic/graph/repair.go (line 13 — internal EdgeKind doc-comment for current internal vocabulary)
  </read_first>
  <behavior>
    - TestEdgeKindSurface_RoundTrip table:
      - "CALLS"       → EdgeKindCalls       ("calls")
      - "REFERENCES"  → EdgeKindReferences  ("references")
      - "IMPLEMENTS"  → EdgeKindImplements  ("implements")
      - "EXTENDS"     → EdgeKindExtends     ("extends")
      - "RESOLVES_TO" → EdgeKindHasType     ("has_type")     [Pitfall 2]
      - "USES_TYPE"   → EdgeKindUsesType    ("uses_type")
      - "CONTAINS"    → EdgeKindContains    ("contains")
      - "DEFINED_IN"  → EdgeKindContains    ("contains")     [synonym, per RESEARCH lines 346-347]
      - "IMPORTS"     → EdgeKindReferences  ("references")   [per RESEARCH line 347]
    - TestEdgeKindSurface_Unmapped:
      - "WHATEVER_NEW_KIND" → EdgeKindOther ("other")
      - ""                  → EdgeKindOther ("other")
    - TestEdgeKindSurface_AllConstantsLowercase: every const value in the enum is all-lowercase, matching the closed-enum convention in envelope.go.
    - TestEdgeKindSurface_ClosedSet: a hand-maintained list of exactly the 8 surface values equals the set of declared constants (catches accidental additions/removals).
  </behavior>
  <action>
RED → GREEN → REFACTOR.

**RED:** Create `internal/skill/semantic/edge_kind_surface_test.go` with the four tests above. Reference `EdgeKindSurface`, `MapInternalKind`, and `EdgeKind*` constants that do not yet exist. Run `go test ./internal/skill/semantic/ -run TestEdgeKindSurface -race`; expect compile errors / failures. Commit: `test(71-02): add failing edge-kind surface round-trip tests`.

**GREEN:** Create `internal/skill/semantic/edge_kind_surface.go`:
- Package `semantic`.
- File header comment explaining: surface enum is closed and lowercase per D3; mapping is one-way (internal → surface); unmapped internal kinds → `EdgeKindOther` plus a bounded-label metric record (Pitfall 4 — emit `mcp_edge_kind_surface_other_total{internal_kind=…}` via the existing obs.Metrics seam IF it is available in this package; if it is not yet wired, leave a TODO comment referencing Pitfall 4 — metric instrumentation is non-blocking for this plan).
- `type EdgeKindSurface string`.
- Const block with the 8 values listed in <behavior>.
- `func MapInternalKind(internal string) EdgeKindSurface` — single switch statement with one case per documented internal kind plus `default: return EdgeKindOther`.

Run tests; all four turn GREEN. Commit: `feat(71-02): add EdgeKindSurface closed enum + MapInternalKind`.

**REFACTOR:** Inline-document each non-obvious mapping (e.g., `RESOLVES_TO → has_type` cites Pitfall 2; `DEFINED_IN → contains` cites synonym-in-some-extractors). Re-run tests. Commit if changed: `refactor(71-02): document non-obvious edge-kind mappings`.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/ && go test ./internal/skill/semantic/ -run TestEdgeKindSurface -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type EdgeKindSurface string" internal/skill/semantic/edge_kind_surface.go` returns 1
    - `grep -c "func MapInternalKind" internal/skill/semantic/edge_kind_surface.go` returns 1
    - `grep -c "EdgeKindHasType" internal/skill/semantic/edge_kind_surface.go` returns at least 2 (declaration + RESOLVES_TO mapping)
    - All four TestEdgeKindSurface_* test cases pass
    - At least two commits in git log: `test(71-02)` then `feat(71-02)`
    - File header carries a comment referencing Pitfall 2 + Pitfall 4 (in the 71-RESEARCH.md sense)
  </acceptance_criteria>
  <done>Surface enum + mapper land with race-clean round-trip tests proving every documented internal kind has a coverage row and unmapped kinds collapse to `other`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FreshnessV2 envelope + FreshnessStatus closed enum (RED → GREEN → REFACTOR)</name>
  <read_first>
    - internal/skill/semantic/envelope.go (full file — particularly the existing Freshness, ClusterStatus, and CommonEnvelope structs to confirm where additive fields live)
    - internal/skill/semantic/envelope_test.go (table-driven shape, marshaling assertions)
    - .planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md lines 355-384 (drafted FreshnessV2 + assembleFreshness)
    - .planning/phases/71-p1-single-symbol-read-tools/71-CONTEXT.md § D5 (envelope contract)
    - .planning/phases/69-production-status-accessors/69-CONTEXT.md (three-state status enum precedent: current / stale / unknown)
  </read_first>
  <behavior>
    - TestFreshnessStatus_ClosedEnum: exactly three constants — FreshnessStatusCurrent ("current"), FreshnessStatusStale ("stale"), FreshnessStatusUnknown ("unknown"). All lowercase.
    - TestFreshnessSource_ClosedEnum: exactly three constants — FreshnessSourceGraph ("graph"), FreshnessSourceTypeResolverLadder ("type_resolver_ladder"), FreshnessSourceASTFallback ("ast_fallback"). All lowercase; mirrors the FreshnessStatus closed-enum pattern; envelope serialization round-trips (Source field populated → re-marshalled JSON yields identical key/value).
    - TestFreshnessV2_ShapeMarshal: marshalling a populated FreshnessV2{GraphVersion:7, SnapshotID:42, ExtractorRunID:"snap-42", AsOfUnixMs:1715760000000, Status:FreshnessStatusCurrent} yields JSON with exactly the keys graph_version / snapshot_id / extractor_run_id / as_of_unix_ms / status and the expected values.
    - TestFreshnessV2_OmitEmpty: an empty FreshnessV2{} marshals with omit-empty semantics on optional fields (per Phase 64 envelope convention — non-zero numeric and non-empty string fields appear; zero-value fields omitted as documented).
    - TestFreshnessV2_AdditiveToExisting: existing Freshness enum (Phase 64) const block is byte-identical to the pre-plan state (regression guard against accidentally renaming the existing 4-state Freshness type). Concrete check: the test enumerates `FreshnessFresh, FreshnessStale, FreshnessStructurallyFreshSemanticallyPending, FreshnessOverlayActive` and asserts their string values are `"fresh"`, `"stale"`, `"structurally_fresh_semantically_pending"`, `"overlay_active"`.
    - TestFreshnessStatus_Independent: `FreshnessStatus` is a NEW distinct type — does NOT shadow or replace `Freshness`. Test asserts the two are different named types (compile-time guard via separate const block referencing the new type only).
  </behavior>
  <action>
RED → GREEN → REFACTOR.

**RED:** Extend `internal/skill/semantic/envelope_test.go` with five test funcs (`TestFreshnessStatus_ClosedEnum`, `TestFreshnessV2_ShapeMarshal`, `TestFreshnessV2_OmitEmpty`, `TestFreshnessV2_AdditiveToExisting`, `TestFreshnessStatus_Independent`) referencing `FreshnessV2`, `FreshnessStatus`, and the three `FreshnessStatus*` constants that do not yet exist. Run tests; expect compile failures. Commit: `test(71-02): add failing FreshnessV2 envelope tests`.

**GREEN:** Extend `internal/skill/semantic/envelope.go`:
- Add `type FreshnessStatus string` block (new, distinct from existing `Freshness` type) with const `FreshnessStatusCurrent = "current"`, `FreshnessStatusStale = "stale"`, `FreshnessStatusUnknown = "unknown"`.
- Add `type FreshnessSource string` block (new) with const `FreshnessSourceGraph = "graph"`, `FreshnessSourceTypeResolverLadder = "type_resolver_ladder"`, `FreshnessSourceASTFallback = "ast_fallback"` (D5 closed enum: `source ∈ {graph, type_resolver_ladder, ast_fallback}`).
- Add `type FreshnessV2 struct` with fields (use `omitempty` on the four numeric/string fields that may be zero in degraded paths, per Pattern A in 71-PATTERNS.md):
  - `GraphVersion uint64 \`json:"graph_version,omitempty"\``
  - `SnapshotID uint64 \`json:"snapshot_id,omitempty"\``
  - `ExtractorRunID string \`json:"extractor_run_id,omitempty"\``
  - `AsOfUnixMs int64 \`json:"as_of_unix_ms,omitempty"\``
  - `Status FreshnessStatus \`json:"status,omitempty"\``
  - `Source FreshnessSource \`json:"source,omitempty"\``
- DO NOT touch the existing `Freshness` const block, `ClusterStatus`, `RetrievalStatus`, or `CommonEnvelope`. This is strictly additive (D5: V2 augments, does not replace).

Run tests; all five turn GREEN. Commit: `feat(71-02): add FreshnessV2 envelope + FreshnessStatus closed enum`.

**REFACTOR:** Add a doc-comment block above the new types citing D5 + Phase 69 D1 status enum. Re-run tests. Commit if changed: `refactor(71-02): document FreshnessV2 lineage`.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/ && go test ./internal/skill/semantic/ -run 'TestFreshness' -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type FreshnessV2 struct" internal/skill/semantic/envelope.go` returns 1
    - `grep -c "type FreshnessStatus string" internal/skill/semantic/envelope.go` returns 1
    - `grep -E "FreshnessStatusCurrent|FreshnessStatusStale|FreshnessStatusUnknown" internal/skill/semantic/envelope.go | wc -l` returns 3
    - `grep -c "type FreshnessSource string" internal/skill/semantic/envelope.go` returns 1
    - `grep -E "FreshnessSourceGraph|FreshnessSourceTypeResolverLadder|FreshnessSourceASTFallback" internal/skill/semantic/envelope.go | wc -l` returns 3
    - All five new TestFreshness* test cases pass
    - Existing TestFreshness (if any in envelope_test.go) still passes — no regression on Phase 64 envelope shape
    - At least two commits land: `test(71-02)`, `feat(71-02)`
  </acceptance_criteria>
  <done>FreshnessV2 + FreshnessStatus land additively; Phase 64 envelope shape unchanged; all five behavior tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal kind strings → MCP surface enum | Internal vocabulary may evolve; surface enum is a hard contract with agents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02-01 | Tampering | EdgeKindSurface | mitigate | Closed enum; default case returns `EdgeKindOther`; no freeform strings reach agents |
| T-71-02-02 | Information Disclosure | FreshnessV2 ExtractorRunID | accept | Opaque monotonic identifier — no PII or path content |
| T-71-02-03 | Repudiation | Unmapped edge kinds silently collapsing to `other` | mitigate | Bounded-label metric (Pitfall 4) records unmapped kinds; TODO carved if metric seam unavailable in plan 71-02 |
</threat_model>

<verification>
- `go vet ./internal/skill/semantic/...` clean
- `go test ./internal/skill/semantic/ -race -count=1` green
- Phase 64 existing tests unchanged
- No new package dependencies introduced
</verification>

<success_criteria>
- Both tasks pass their automated verify
- All four must_haves truths observable
- 71-03 / 71-04 / 71-05 plans can reference `EdgeKindSurface`, `MapInternalKind`, `FreshnessV2`, and `FreshnessStatus*` constants directly with no further scaffolding
</success_criteria>

<output>
Create `.planning/phases/71-p1-single-symbol-read-tools/71-02-SUMMARY.md` when done.
</output>
