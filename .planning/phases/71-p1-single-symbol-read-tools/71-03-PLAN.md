---
phase: 71-p1-single-symbol-read-tools
plan: 03
type: tdd
wave: 2
depends_on:
  - 71-01
  - 71-02
files_modified:
  - internal/skill/semantic/tools_explain_symbol.go
  - internal/skill/semantic/tools_explain_symbol_test.go
  - internal/skill/semantic/register.go
autonomous: true
requirements:
  - P1TOOL-01
tags:
  - semantic
  - mcp-tool
  - explain-symbol-deep
  - read-only

must_haves:
  truths:
    - "explain_symbol_deep is registered through RegisterAll and visible to MCP clients"
    - "Handler enforces read+ mode tier at entry via checkMode(snap, modeTierRead)"
    - "Response includes type chain, callers (≤50), incoming edges (≤100), outgoing edges (≤100), cluster reference, and FreshnessV2 envelope"
    - "Edges in response are classified via MapInternalKind; internal_kind field always present"
    - "Truncation surfaces via *_count_total vs *_count_returned envelope fields (D2)"
    - "Degraded type-resolver path caps top-level confidence at 0.6 via types.CapCommentConfidence (TYPES-04)"
    - "Handler is read-only — no Begin/Commit/Abort/Write tokens in source"
  artifacts:
    - path: "internal/skill/semantic/tools_explain_symbol.go"
      provides: "ExplainSymbolDeepArgs, registerExplainSymbolDeep, handleExplainSymbolDeep, explainSymbolDeepHelp const, ExplainSymbolDeepResult struct"
      contains: "func (s \\*SemanticSkill) handleExplainSymbolDeep"
    - path: "internal/skill/semantic/register.go"
      provides: "registerExplainSymbolDeep wired into RegisterAll"
      contains: "registerExplainSymbolDeep(server, s, tracer)"
  key_links:
    - from: "internal/skill/semantic/tools_explain_symbol.go"
      to: "internal/skill/semantic/seed_resolve.go::resolveSeed"
      via: "s.resolveSeed(ctx, ws, args.Seed)"
      pattern: "resolveSeed\\("
    - from: "internal/skill/semantic/tools_explain_symbol.go"
      to: "internal/skill/semantic/edge_kind_surface.go::MapInternalKind"
      via: "edge classification"
      pattern: "MapInternalKind\\("
    - from: "internal/skill/semantic/tools_explain_symbol.go"
      to: "internal/skill/semantic/envelope.go::FreshnessV2"
      via: "assembleFreshness helper from 71-01 / 71-02"
      pattern: "FreshnessV2"
---

<objective>
Ship `explain_symbol_deep` — the first of the three Phase 71 P1 MCP tools. Given one seed symbol (via symbol_id OR (file_path, symbol_name)), return type chain, callers, incoming + outgoing edges (classified by surface enum), cluster membership reference, and the FreshnessV2 envelope. Mode tier read+ enforced at handler entry; read-only with respect to *Store committed state.

Purpose: Closes P1TOOL-01. Agents asking "tell me everything about this symbol" get one MCP call instead of a multi-step recon through goto_definition + find_references + get_context.

Output: One new handler file + tests + one-line register.go extension. RegisterAll exposes the new tool.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-p1-single-symbol-read-tools/71-CONTEXT.md
@.planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md
@.planning/phases/71-p1-single-symbol-read-tools/71-PATTERNS.md
@.planning/phases/71-p1-single-symbol-read-tools/71-VALIDATION.md
@.planning/phases/71-p1-single-symbol-read-tools/71-01-PLAN.md
@.planning/phases/71-p1-single-symbol-read-tools/71-02-PLAN.md

@internal/skill/semantic/tools_context.go
@internal/skill/semantic/tools_refresh.go
@internal/skill/semantic/tools_refresh_test.go
@internal/skill/semantic/register.go
@internal/skill/semantic/mode_check.go

<interfaces>
<!-- From 71-01: -->
<!-- SeedInput { SymbolID string; FilePath string; SymbolName string } -->
<!-- Resolution closed enum: ResolutionExact | ResolutionAmbiguous | ResolutionNotFound -->
<!-- (s *SemanticSkill).resolveSeed(ctx, ws, in SeedInput) (ResolvedSeed, error) -->
<!-- buildPopulatedGraphFixture(t) *PopulatedGraphFixture — multi-language test fixture -->

<!-- From 71-02: -->
<!-- type EdgeKindSurface string + EdgeKindCalls / EdgeKindReferences / EdgeKindImplements / EdgeKindExtends / EdgeKindHasType / EdgeKindUsesType / EdgeKindContains / EdgeKindOther -->
<!-- func MapInternalKind(internal string) EdgeKindSurface -->
<!-- type FreshnessV2 struct { GraphVersion uint64; SnapshotID uint64; ExtractorRunID string; AsOfUnixMs int64; Status FreshnessStatus } -->
<!-- type FreshnessStatus string + FreshnessStatusCurrent | FreshnessStatusStale | FreshnessStatusUnknown -->

<!-- Existing seams (study tools_context.go: 31-104 for help const + args struct; 110-124 for register; 151-163 for handler entry; 194-210 for accessor fan-out; 313-326 for receipt issuance): -->
<!-- checkMode(snap, modeTierRead) error -->
<!-- s.sessionSnapshot(ctx), s.workspaceKey(ctx), ws.Hash() returns repoID -->
<!-- integ.SemanticLookup.ExpandFrom(ctx, repoID, symbolID) — returns type-chain ancestor / impact rows -->
<!-- StoreAccessor.QueryEffectiveAdjacency(ctx, repoID, symbolID, projection) — (outgoing, incoming) edges -->
<!-- types.CapCommentConfidence(c float64, kind string) float64 — TYPES-04 cap helper -->
<!-- guardrails.IssueReceiptOnSuccess(ctx, ClassContextGathered, scope, "tool_name") -->
<!-- kernel.WrapToolSpan(tracer, "tool_name", handler) -->
<!-- mcpsdk.AddTool / server.Registry().Register(&mcp.ToolDef{...}) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: explain_symbol_deep handler + register + tests (RED → GREEN → REFACTOR)</name>
  <read_first>
    - internal/skill/semantic/tools_context.go (full file — exact analog for handler shape, help const, register, args struct, fan-out, receipt issuance)
    - internal/skill/semantic/tools_refresh.go lines 15-23 (D-09 read-only invariant header to copy verbatim with tool-name swap)
    - internal/skill/semantic/tools_refresh_test.go lines 38-50 + 113-135 (recorderStoreAccessor pattern with t.Fatal canaries — MUST install this in the new test file)
    - internal/skill/semantic/handler_helpers.go (jsonResult, errorResult, validatePaths)
    - .planning/phases/71-p1-single-symbol-read-tools/71-CONTEXT.md § D2 (caps: max_callers=50, max_edges_per_direction=100) and § D6 (TYPES-04 cap on degraded)
    - .planning/phases/71-p1-single-symbol-read-tools/71-VALIDATION.md (per-task test rows for explain_symbol_deep)
    - internal/semantic/integ/lookup.go (SemanticLookup contract — confirms ExpandFrom returns Impact rows usable for type chain)
    - internal/semantic/types/ladder.go (CapCommentConfidence + ConfidenceForEvidence signatures)
  </read_first>
  <behavior>
    - TestExplainSymbolDeep_HappyPath: seed = a Go symbol from buildPopulatedGraphFixture. Response contains:
      * `type_chain` array (non-empty, each entry has `tier`, `confidence`, `evidence_kind`)
      * `callers` array length ≤ 50; `callers_count_total` ≥ length; `callers_count_returned` == length
      * `edges_incoming` length ≤ 100; each entry has `edge_kind` ∈ closed surface enum AND `internal_kind` populated
      * `edges_outgoing` length ≤ 100; same shape
      * `cluster` field with `{cluster_id, size}` (size may be 0 if isolated — not an error per RESEARCH anti-patterns)
      * `freshness` field marshalls as FreshnessV2 with non-zero graph_version, snapshot_id, and status="current" against the populated fixture
      * `seed` envelope carries `resolution: "exact"`
    - TestExplainSymbolDeep_ResolutionAmbiguous: (file, name) tuple matching 3 fixture symbols → response carries `resolution: "ambiguous"` + `ambiguous_candidates` array of 3.
    - TestExplainSymbolDeep_ResolutionNotFound: unknown (file, name) → response carries `resolution: "not_found"` and `fallback_reason: "symbol_not_found"`; no panics.
    - TestExplainSymbolDeep_TruncationCallers: a "hot" fixture symbol with > 50 callers → `callers_count_returned == 50`, `callers_count_total > 50`.
    - TestExplainSymbolDeep_TruncationEdges: a symbol with > 100 incoming edges → `edges_incoming_count_returned == 100`, `edges_incoming_count_total > 100`. Same shape for outgoing.
    - TestExplainSymbolDeep_DegradedPath: when the type-resolver returns tier6_heuristic on the fixture → top-level `confidence` ≤ 0.6 AND `fallback_reason` populated with a closed-enum reason (e.g., `"type_resolver_degraded"`). Per-source contributions in `type_chain` MAY be unclamped (D4 documented asymmetry).
    - TestExplainSymbolDeep_ReadOnly: the recorderStoreAccessor installed in this test panics on Begin/Commit/Abort/WriteSnapshotFacts. Calling handleExplainSymbolDeep MUST NOT trigger any of those panics — full happy-path traversal stays on read accessors only.
    - TestExplainSymbolDeep_ModeRejected: seed is valid, but `s.sessionSnapshot(ctx)` returns a mode below modeTierRead → handler returns an error envelope; no accessor calls.
    - TestExplainSymbolDeep_EdgeClassification: response edges include at least one entry with `internal_kind="RESOLVES_TO"` and `edge_kind="has_type"` (Pitfall 2 regression guard).
  </behavior>
  <action>
RED → GREEN → REFACTOR.

**RED:** Create `internal/skill/semantic/tools_explain_symbol_test.go` containing all nine behavior tests above. Mirror the import header and recorder mock construction from `tools_refresh_test.go:1-16, 38-50, 113-135`. Reuse `buildPopulatedGraphFixture(t)` from plan 71-01. Reference `ExplainSymbolDeepArgs`, `ExplainSymbolDeepResult`, `s.handleExplainSymbolDeep`, `explainSymbolDeepHelp` — none of which exist yet. Run tests; expect compile errors. Commit: `test(71-03): add failing explain_symbol_deep tests`.

**GREEN:** Create `internal/skill/semantic/tools_explain_symbol.go`:

Header (copy from tools_refresh.go:15-23, adjust tool name): `// INVARIANT (D-09 / D-13): explain_symbol_deep MUST NOT touch the snapshot-write surface of *Store. Specifically: no Begin/Commit/Abort/Write methods on snapshots, and no compactor flush trigger. The grep gate in CI enforces the absence of those identifier tokens in this file; the recorder mocks in tools_explain_symbol_test.go enforce it under unit test.`

Structure:

1. `const explainSymbolDeepHelp` — multi-paragraph Markdown documenting Usage Examples, Mode Tier (read+), Inputs (Seed: symbol_id OR file_path+symbol_name), Outputs (type chain, callers, edges incoming/outgoing classified by surface enum, cluster reference, FreshnessV2 envelope), Caps (D2: callers≤50, edges≤100 per direction), Confidence (TYPES-04 cap on degraded paths), Determinism note. Mirror tools_context.go:31-104 prose style.

2. `type ExplainSymbolDeepArgs struct { Seed SeedInput \`json:"seed" jsonschema:"seed symbol — symbol_id OR (file_path AND symbol_name)"\` }` — single field; SeedInput type comes from 71-01.

3. `type ExplainSymbolDeepResult struct { CommonEnvelope; Seed SeedEnvelope; TypeChain []TypeChainEntry; Callers []CallerRef; CallersCountTotal int; CallersCountReturned int; EdgesIncoming []EdgeRef; EdgesIncomingCountTotal int; EdgesIncomingCountReturned int; EdgesOutgoing []EdgeRef; EdgesOutgoingCountTotal int; EdgesOutgoingCountReturned int; Cluster ClusterRef; Freshness FreshnessV2; Confidence float64; FallbackReason string \`json:"fallback_reason,omitempty"\` }` — declare nested types (TypeChainEntry, CallerRef, EdgeRef, ClusterRef, SeedEnvelope) inline; each carries the minimum fields required by the behavior tests (e.g., EdgeRef has EdgeKind EdgeKindSurface + InternalKind string + To string + From string).

4. `func registerExplainSymbolDeep(server *mcp.SerenaMCPServer, s *SemanticSkill, tracer trace.Tracer)` — copy `registerGetSemanticContext` from tools_context.go:110-124 verbatim, swap names/descriptions. BriefDescription: "Deep symbol explanation". HelpText: explainSymbolDeepHelp.

5. `func (s *SemanticSkill) handleExplainSymbolDeep(ctx context.Context, args ExplainSymbolDeepArgs) *mcpsdk.CallToolResult` — handler body. Steps in order, mirroring tools_context.go:151-326:
   a. `snap := s.sessionSnapshot(ctx); if err := checkMode(snap, modeTierRead); err != nil { return errorResult(err.Error()) }`
   b. `ws := s.workspaceKey(ctx); repoID := ws.Hash()`
   c. `resolved, err := s.resolveSeed(ctx, ws, args.Seed)` — propagate error envelope on err. If `resolved.Resolution == ResolutionNotFound`, marshal a NotFound envelope with `fallback_reason: "symbol_not_found"` and return.
   d. Read type chain via `s.lookup.ExpandFrom(ctx, repoID, resolved.SymbolID)` (or the equivalent existing SemanticLookup method). Translate each row into `TypeChainEntry`. Per-entry confidence comes from `types.ConfidenceForEvidence`. If any tier is `tier6_heuristic` or `tier7_unknown`, set a local `degraded := true` flag.
   e. Read adjacency via `s.store.QueryEffectiveAdjacency(ctx, repoID, resolved.SymbolID, "call_graph")` (or the projection name from accessors.go — confirm with SMTC). Map outgoing/incoming into `[]EdgeRef`, applying `MapInternalKind` to each. Compute totals BEFORE truncation, then truncate to 100 each direction. Same for callers from `QueryEffectiveAdjacency(..., "call_graph")` reversed; cap at 50.
   f. Read cluster reference: `clusterID, size, err := s.clusterMembership.ClusterIDOf(ctx, repoID, resolved.SymbolID)`. If err or nil accessor → `ClusterRef{ClusterID: 0, Size: 0}`. Empty cluster is NOT an error.
   g. Assemble FreshnessV2 via the helper drafted in 71-RESEARCH.md lines 365-383 (`assembleFreshness` — implement it here if 71-02 did not place it; place it in this file or in a small shared helper at the package level — planner discretion). Compute Status: `current` if `!overlay_active && snapshot_id != 0 && extractor_run_id != ""`, `stale` if overlay active, `unknown` if snapshot_id == 0.
   h. Compute final `Confidence`: raw sum from contributions; if `degraded` is true, clamp via `types.CapCommentConfidence(raw, "tier6_heuristic")` and set `FallbackReason = "type_resolver_degraded"`.
   i. `guardrails.IssueReceiptOnSuccess(ctx, guardrails.ClassContextGathered, …, "explain_symbol_deep")` on success.
   j. `return jsonResult(result)`.

6. Extend `internal/skill/semantic/register.go`'s `RegisterAll`:
   - Add one line after the existing four `register*` calls (research's drafted shape, lines 165-170 of 71-PATTERNS.md): `registerExplainSymbolDeep(server, s, tracer)`.

Run tests; all nine RED tests turn GREEN. Commit: `feat(71-03): implement explain_symbol_deep handler + register`.

**REFACTOR:** Extract any duplicated edge-shaping logic into a small unexported helper (e.g., `shapeEdges(rows []…, cap int) (refs []EdgeRef, total int)`). Re-run tests under `-race -count=1`. If clean, commit: `refactor(71-03): extract shapeEdges helper`.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/ && go test ./internal/skill/semantic/ -run TestExplainSymbolDeep -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func (s \*SemanticSkill) handleExplainSymbolDeep" internal/skill/semantic/tools_explain_symbol.go` returns 1
    - `grep -c "func registerExplainSymbolDeep" internal/skill/semantic/tools_explain_symbol.go` returns 1
    - `grep -c "registerExplainSymbolDeep(server, s, tracer)" internal/skill/semantic/register.go` returns 1
    - `grep -E "BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshotFacts" internal/skill/semantic/tools_explain_symbol.go | grep -v '^#'` returns no matches (D-09 / D6 read-only invariant)
    - File header (first 25 lines of tools_explain_symbol.go) contains the string `INVARIANT (D-09 / D-13)` and names the tool
    - All nine TestExplainSymbolDeep_* test cases pass under `-race`
    - recorderStoreAccessor canary methods (Begin/Commit/Abort/WriteSnapshotFacts) are NEVER invoked across the full test run (zero `t.Fatal` triggers)
    - `MapInternalKind` is referenced from the handler (verified via grep)
    - `FreshnessV2` is the type of the response's Freshness field
    - At least two commits: `test(71-03)`, `feat(71-03)`
  </acceptance_criteria>
  <done>explain_symbol_deep ships: registered, mode-gated, read-only, returns type chain + callers + classified edges + cluster ref + V2 envelope; all nine behaviors verified including the Pitfall 2 RESOLVES_TO regression guard and TYPES-04 cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → handler | Untrusted seed inputs; validated via resolveSeed + serr.InvalidArgs |
| Handler → *Store | Trusted; read-only by D-09 invariant enforced via recorder mocks + header + CI grep-gate (71-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03-01 | Tampering | Handler invoking snapshot writes | mitigate | D-09 header + recorderStoreAccessor canaries in tests + CI grep-gate landing in 71-05 |
| T-71-03-02 | Elevation of Privilege | Read+ tool accessible below read+ tier | mitigate | checkMode(snap, modeTierRead) at handler entry; TestExplainSymbolDeep_ModeRejected covers |
| T-71-03-03 | Information Disclosure | Unbounded caller list leak | mitigate | D2 cap of 50 callers + count totals so agent sees truncation |
| T-71-03-04 | Tampering | Freeform edge_kind strings to client | mitigate | MapInternalKind closed-enum classification |
| T-71-03-05 | Repudiation | Stale results without notice | mitigate | FreshnessV2.Status closed enum + fallback_reason field |
</threat_model>

<verification>
- `go vet ./internal/skill/semantic/...` clean
- `go test ./internal/skill/semantic/ -race -count=1` green (full package, not just TestExplainSymbolDeep)
- recorderStoreAccessor canaries never tripped
- `tools/list` would expose `explain_symbol_deep` (verified by inspecting register.go body)
</verification>

<success_criteria>
- All must_haves truths observable in code + tests
- P1TOOL-01 closed
- Handler is the template 71-04 and 71-05 mirror (shared shape: header, mode-check, seed-resolve, accessor reads, envelope assembly, receipt, jsonResult)
</success_criteria>

<output>
Create `.planning/phases/71-p1-single-symbol-read-tools/71-03-SUMMARY.md` when done.
</output>
