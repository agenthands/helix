---
phase: 71-p1-single-symbol-read-tools
plan: 05
type: tdd
wave: 3
depends_on:
  - 71-03
  - 71-04
files_modified:
  - internal/skill/semantic/tools_validate_edge.go
  - internal/skill/semantic/tools_validate_edge_test.go
  - internal/skill/semantic/integration_test.go
  - internal/skill/semantic/register.go
autonomous: true
requirements:
  - P1TOOL-06
tags:
  - semantic
  - mcp-tool
  - validate-graph-edge
  - ci-gate
  - integration-test
  - read-only

must_haves:
  truths:
    - "validate_graph_edge is registered through RegisterAll and visible to MCP clients"
    - "Handler enforces read+ mode tier at entry via checkMode(snap, modeTierRead)"
    - "Accepts two seeds (from, to) each via symbol_id OR (file_path, symbol_name); accepts edge_kind (closed surface enum) input"
    - "Returns top-level confidence ∈ [0,1] + ordered evidence array (cap 10) sorted by confidence_contribution desc"
    - "evidence source closed enum ∈ {lsp, ast, type_resolver} typed as EvidenceSource (not untyped string); tier field present iff source==type_resolver"
    - "evidence_status closed enum ∈ {complete, partial, none} typed as EvidenceStatus (not untyped string); non-zero confidence with empty evidence is allowed and reports status:none with capped confidence (D4 lenient)"
    - "TYPES-04 cap: top-level confidence ≤ 0.6 when any type_resolver tier is tier6_heuristic / tier7_unknown OR evidence_status != complete (D4 + Pitfall 5)"
    - "Edge absent → low confidence + fallback_reason:'edge_not_found' (no error)"
    - "CI grep-gate (or in-tree Go gate) extended to cover tools_explain_symbol.go, tools_find_related.go, tools_validate_edge.go for Begin/Commit/Abort/Write absence"
    - "Integration suite asserts cross-tool consistency, concurrent race-cleanliness, envelope shape parity, and read+ mode enforcement across the three tools"
  artifacts:
    - path: "internal/skill/semantic/tools_validate_edge.go"
      provides: "ValidateGraphEdgeArgs, registerValidateGraphEdge, handleValidateGraphEdge, validateGraphEdgeHelp, ValidateGraphEdgeResult, EvidenceCitation"
      contains: "func (s \\*SemanticSkill) handleValidateGraphEdge"
    - path: "internal/skill/semantic/integration_test.go"
      provides: "TestThreeTools_CrossConsistency, TestThreeTools_Concurrent, TestThreeTools_EnvelopeShape, TestThreeTools_ModeEnforcement"
      contains: "func TestThreeTools_"
    - path: "internal/skill/semantic/register.go"
      provides: "registerValidateGraphEdge wired into RegisterAll"
      contains: "registerValidateGraphEdge(server, s, tracer)"
  key_links:
    - from: "internal/skill/semantic/tools_validate_edge.go"
      to: "internal/semantic/types.CapCommentConfidence"
      via: "TYPES-04 cap on degraded confidence"
      pattern: "types\\.CapCommentConfidence"
    - from: "CI gate (workflow / scripts / Makefile / in-tree test)"
      to: "tools_explain_symbol.go, tools_find_related.go, tools_validate_edge.go"
      via: "grep -v '^#' | grep -E 'BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshotFacts'"
      pattern: "BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshotFacts"
---

<objective>
Ship the third Phase 71 tool — `validate_graph_edge` — that answers (from, to, edge_kind) claims with a confidence score + ordered evidence array (LSP / AST / type_resolver citations), applying the D4 lenient stance and TYPES-04 cap. Land the cross-tool integration suite covering all three Phase 71 tools (cross-consistency, concurrent race-cleanliness, envelope shape parity, mode enforcement). Extend the CI grep-gate (or land an equivalent in-tree Go gate) to enforce D-09 read-only invariants across all three new handler files.

Purpose: Closes P1TOOL-06 and provides the phase-level "all three tools together" gate that Phase 71 success criteria #1, #4, #5, and D7 #4-5 demand.

Output: One new handler + tests, integration-test extensions, register.go extension, and a CI gate (file path + line if external runner exists; otherwise an in-tree Go test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-p1-single-symbol-read-tools/71-CONTEXT.md
@.planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md
@.planning/phases/71-p1-single-symbol-read-tools/71-PATTERNS.md
@.planning/phases/71-p1-single-symbol-read-tools/71-VALIDATION.md
@.planning/phases/71-p1-single-symbol-read-tools/71-01-PLAN.md
@.planning/phases/71-p1-single-symbol-read-tools/71-02-PLAN.md
@.planning/phases/71-p1-single-symbol-read-tools/71-03-PLAN.md
@.planning/phases/71-p1-single-symbol-read-tools/71-04-PLAN.md

@internal/skill/semantic/tools_status.go
@internal/skill/semantic/tools_refresh.go
@internal/skill/semantic/integration_test.go
@internal/skill/semantic/register.go

<interfaces>
<!-- From 71-01: resolveSeed, ResolvedSeed, ClusterMembershipAccessor -->
<!-- From 71-02: MapInternalKind, EdgeKindSurface, FreshnessV2 -->
<!-- From 71-03: handleExplainSymbolDeep (integration tests cross-reference) -->
<!-- From 71-04: handleFindRelatedSymbols (integration tests cross-reference) -->

<!-- Internal type-resolver tier vocabulary (from internal/semantic/types/ladder.go): -->
<!-- tier1_lsp, tier2_annotation, tier3_constructor, tier4_assignment, tier5_godoc, tier6_heuristic, tier7_unknown -->
<!-- Helper: types.CapCommentConfidence(c float64, kind string) float64 -->

<!-- Internal edge.Source format observed (RESEARCH Open Q3 + types/golang/resolver.go:60): -->
<!-- "lsp.go.text_document_definition", "lsp.ts.text_document_references", etc. — prefix lsp.{lang}.{lsp_method} -->
<!-- AST citation metadata may not exist for all edges (RESEARCH Open Q3) — fallback: source:"ast", tree_sitter_kind:"", evidence_status:"partial" -->

<!-- Confidence formula (D4 + Pitfall 5): -->
<!-- raw = sum(contributions) where LSP=0.4, AST=0.3, type_resolver=0.3 (scaled by tier) -->
<!-- final = (evidence_status != complete OR any tier ∈ {tier6, tier7}) ? min(raw, 0.6) : raw -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validate_graph_edge handler + register + tests (RED → GREEN → REFACTOR)</name>
  <read_first>
    - internal/skill/semantic/tools_status.go (full file — closest analog for envelope-rich read tool; freshness selection logic at lines 120-232)
    - internal/skill/semantic/tools_refresh.go lines 15-23 (D-09 invariant header)
    - internal/skill/semantic/tools_refresh_test.go lines 38-50 + 113-135 (recorder pattern)
    - .planning/phases/71-p1-single-symbol-read-tools/71-CONTEXT.md § D4 (evidence shape; flat ordered list; cap 10; lenient stance)
    - .planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md § Pitfall 5 (TYPES-04 cap vs additive sum asymmetry) + Open Question 3 (AST citation metadata)
    - internal/semantic/types/ladder.go (7-tier evidence enum; CapCommentConfidence)
    - internal/semantic/types/golang/resolver.go lines 45-80 (RESOLVES_TO emission + LSP source format)
  </read_first>
  <behavior>
    - TestEvidenceSource_ClosedSet: declared constants of `EvidenceSource` are exactly `{"lsp", "ast", "type_resolver"}` — hand-maintained list equals the declared const set; mirrors `TestEdgeKindSurface_ClosedSet` pattern in 71-02.
    - TestEvidenceStatus_ClosedSet: declared constants of `EvidenceStatus` are exactly `{"complete", "partial", "none"}` — hand-maintained list equals the declared const set; mirrors `TestEdgeKindSurface_ClosedSet` pattern in 71-02.
    - TestValidateGraphEdge_HappyPath: fixture has a known CALLS edge with LSP + AST + type_resolver(tier1_lsp) evidence → response: confidence == 1.0 (0.4+0.3+0.3), evidence array length == 3 sorted by confidence_contribution desc, evidence_status:"complete", each citation carries expected source/range/file fields. fallback_reason empty.
    - TestValidateGraphEdge_LenientEmptyEvidence: edge present in `semantic_edges` but no citations available → response: confidence > 0 but ≤ 0.6 (capped via TYPES-04), evidence:[], evidence_status:"none", fallback_reason populated with a closed-enum reason such as "evidence_lookup_lagging".
    - TestValidateGraphEdge_TYPES04Cap: edge present with only type_resolver tier6_heuristic evidence → raw additive sum may exceed 0.6, but top-level confidence is clamped to ≤ 0.6 via types.CapCommentConfidence. Per-source confidence_contribution in the evidence array IS NOT clamped (D4 asymmetry).
    - TestValidateGraphEdge_EdgeAbsent: fixture (from, to, edge_kind) tuple that has no row in semantic_edges → response: confidence == 0 (or near-zero per accessor's signal), evidence:[], evidence_status:"none", fallback_reason:"edge_not_found"; NO error.
    - TestValidateGraphEdge_EdgeKindClosedEnum: input edge_kind must accept only the closed surface enum values (`calls`, `references`, `implements`, `extends`, `has_type`, `uses_type`, `contains`, `other`). Input "FREEFORM_KIND" → error envelope via serr.InvalidArgs.
    - TestValidateGraphEdge_EvidenceCapAt10: synthesize a fixture edge with > 10 citations → evidence array length == 10; evidence_count_total > 10; evidence_count_returned == 10.
    - TestValidateGraphEdge_BothSeedsResolution: from=symbol_id, to=(file,name) ambiguous → response carries per-seed resolution envelope (`from_resolution: "exact"`, `to_resolution: "ambiguous"`).
    - TestValidateGraphEdge_FromNotFound: from seed resolves to NotFound → response: confidence=0, evidence:[], evidence_status:"none", fallback_reason:"symbol_not_found"; NO error.
    - TestValidateGraphEdge_ASTCitationFallback (Pitfall handling for RESEARCH Open Q3): when an edge has graph-attested AST contribution but no extractor metadata (no tree_sitter_kind, no range), the citation emits `source:"ast"`, `tree_sitter_kind:""`, and the overall envelope evidence_status drops to "partial".
    - TestValidateGraphEdge_ReadOnly: recorderStoreAccessor canaries never tripped through full happy-path traversal.
    - TestValidateGraphEdge_ModeRejected: sub-read tier → error envelope; no accessor calls.
  </behavior>
  <action>
RED → GREEN → REFACTOR.

**RED:** Create `internal/skill/semantic/tools_validate_edge_test.go` with all eleven tests above. Install recorderStoreAccessor canaries. Reuse `buildPopulatedGraphFixture`. Reference `ValidateGraphEdgeArgs`, `ValidateGraphEdgeResult`, `EvidenceCitation`, `s.handleValidateGraphEdge`, `validateGraphEdgeHelp` — none exist yet. Run; expect compile errors. Commit: `test(71-05): add failing validate_graph_edge tests`.

**GREEN:** Investigate AST citation metadata first (RESEARCH Open Q3): use SMTC (`find_references`/`goto_definition`) on `Edge.Source` and the extractor pass that writes `CALLS` edges to determine whether `tree_sitter_kind` and `range` metadata is attached. Record finding in this plan's SUMMARY:
- If metadata exists → handler populates `tree_sitter_kind` and `range` on AST citations.
- If absent → AST contributes via internal-kind presence only (`tree_sitter_kind:""`, `range:nil`, `evidence_status` of envelope drops to `"partial"` whenever AST contribution is present without metadata).

Create `internal/skill/semantic/tools_validate_edge.go`:

Header (copy from tools_refresh.go:15-23, swap names): `// INVARIANT (D-09 / D-13): validate_graph_edge MUST NOT touch the snapshot-write surface of *Store. …`

Structure:

1. `const validateGraphEdgeHelp` — Markdown documenting Usage (from + to seeds, edge_kind), Mode Tier (read+), Output schema (confidence + evidence + evidence_status + freshness), TYPES-04 cap behavior, lenient stance documentation (non-zero confidence + empty evidence is allowed per D4), edge_kind closed enum (calls/references/implements/extends/has_type/uses_type/contains/other).

2. `type ValidateGraphEdgeArgs struct { From SeedInput \`json:"from"\`; To SeedInput \`json:"to"\`; EdgeKind string \`json:"edge_kind" jsonschema:"closed surface enum: calls/references/implements/extends/has_type/uses_type/contains/other"\` }`.

2.5. Add closed-enum type declarations (in `tools_validate_edge.go` or a co-located `enums.go` in `internal/skill/semantic/`):
   - `type EvidenceSource string` with const block `EvidenceSourceLSP = "lsp"`, `EvidenceSourceAST = "ast"`, `EvidenceSourceTypeResolver = "type_resolver"` (D4 closed enum: `source ∈ {lsp, ast, type_resolver}`).
   - `type EvidenceStatus string` with const block `EvidenceStatusComplete = "complete"`, `EvidenceStatusPartial = "partial"`, `EvidenceStatusNone = "none"` (D4 closed enum: `evidence_status ∈ {complete, partial, none}`).
   - Use these named types on EvidenceCitation.Source and ValidateGraphEdgeResult.EvidenceStatus (declared next).

3. `type EvidenceCitation struct { Source EvidenceSource \`json:"source"\`; LSPMethod string \`json:"lsp_method,omitempty"\`; TreeSitterKind string \`json:"tree_sitter_kind,omitempty"\`; Tier string \`json:"tier,omitempty"\`; EvidenceKind string \`json:"evidence_kind,omitempty"\`; File string \`json:"file,omitempty"\`; Range *graph.Range \`json:"range,omitempty"\`; ConfidenceContribution float64 \`json:"confidence_contribution"\` }`.

4. `type ValidateGraphEdgeResult struct { CommonEnvelope; FromResolution Resolution; ToResolution Resolution; Confidence float64; Evidence []EvidenceCitation; EvidenceCountTotal int; EvidenceCountReturned int; EvidenceStatus EvidenceStatus \`json:"evidence_status"\`; Freshness FreshnessV2; FallbackReason string \`json:"fallback_reason,omitempty"\` }`.

5. `func registerValidateGraphEdge(...)` — mirror existing register pattern.

6. `func (s *SemanticSkill) handleValidateGraphEdge(ctx, args) *mcpsdk.CallToolResult`:
   a. Mode check (modeTierRead).
   b. Validate `args.EdgeKind` is a known surface enum value; reject freeform via serr.InvalidArgs.
   c. Resolve from + to seeds via `s.resolveSeed`. If either is NotFound → short-circuit response with confidence=0, evidence=[], evidence_status="none", fallback_reason="symbol_not_found".
   d. Map `args.EdgeKind` (surface) → query the edge in the effective graph. Since mapping is one-way (D3), use the candidate set of internal kinds that could project to this surface kind (e.g., `has_type` → match internal_kind ∈ {RESOLVES_TO}; `references` → {REFERENCES, IMPORTS}). Inline-document this reverse-projection table; it lives in this handler only (no surface-to-internal API on `MapInternalKind` per D3 + Deferred Ideas).
   e. Probe `semantic_edges` for any (from, to, internal_kind ∈ projected_set) row at LatestCommittedSnapshot. If no row → confidence=0, evidence=[], evidence_status="none", fallback_reason="edge_not_found". Return.
   f. Assemble evidence:
      - LSP citation: if Edge.Source has prefix `lsp.` → emit `{source:"lsp", lsp_method: parsed-from-source, file, range, confidence_contribution: 0.4}`.
      - AST citation: per Open Q3 investigation. If extractor metadata is available → emit full citation with tree_sitter_kind + range, contribution 0.3. If absent → emit `{source:"ast", tree_sitter_kind:"", confidence_contribution: 0.3}` AND mark `astMetadataPartial = true` (the envelope's evidence_status drops to "partial" later).
      - type_resolver citation: if internal_kind == RESOLVES_TO, look up the ladder tier (via existing type-resolver accessors); emit `{source:"type_resolver", tier: <tierN_name>, evidence_kind: <enum>, file, range, confidence_contribution: scaled-by-tier}`. Tier scaling: tier1_lsp=0.3, tier2_annotation=0.3, tier3_constructor=0.25, tier4_assignment=0.2, tier5_godoc=0.15, tier6_heuristic=0.1, tier7_unknown=0.05.
   g. Sort evidence by ConfidenceContribution desc; cap at 10; record total vs returned.
   h. Compute raw confidence = sum of unclamped contributions.
   i. Compute `evidence_status`: "complete" if all three sources present AND no AST metadata partial AND no tier ≥ 6; "partial" if any contribution is partial-metadata or tier ≥ 6 but at least one citation exists; "none" if evidence array is empty.
   j. Compute final Confidence: if `evidence_status != "complete"` OR any type_resolver tier is tier6/7 → `final := types.CapCommentConfidence(raw, "tier6_heuristic")` (clamps to ≤ 0.6). Else `final := raw` (clamped to [0,1]).
   k. Per-source `confidence_contribution` in the evidence array is the UNCLAMPED value (D4 asymmetry — document inline).
   l. Assemble FreshnessV2.
   m. `guardrails.IssueReceiptOnSuccess(ctx, ..., "validate_graph_edge")`.
   n. Return jsonResult.

7. Extend `RegisterAll` in register.go: append `registerValidateGraphEdge(server, s, tracer)`.

Run tests; all eleven RED tests turn GREEN. Commit: `feat(71-05): implement validate_graph_edge handler + register`.

**REFACTOR:** Extract evidence-shaping into small helpers (`buildLSPCitation`, `buildASTCitation`, `buildTypeResolverCitation`). Re-run tests under `-race`. Commit if changed: `refactor(71-05): extract evidence builders`.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go vet ./internal/skill/semantic/ && go test ./internal/skill/semantic/ -run TestValidateGraphEdge -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func (s \*SemanticSkill) handleValidateGraphEdge" internal/skill/semantic/tools_validate_edge.go` returns 1
    - `grep -c "type EvidenceSource string" internal/skill/semantic/` (recursive across `tools_validate_edge.go` or `enums.go`) returns 1
    - `grep -c "type EvidenceStatus string" internal/skill/semantic/` (recursive across `tools_validate_edge.go` or `enums.go`) returns 1
    - `grep -E "EvidenceSourceLSP|EvidenceSourceAST|EvidenceSourceTypeResolver" internal/skill/semantic/ -r | wc -l` returns ≥ 3
    - `grep -E "EvidenceStatusComplete|EvidenceStatusPartial|EvidenceStatusNone" internal/skill/semantic/ -r | wc -l` returns ≥ 3
    - `grep -c "func registerValidateGraphEdge" internal/skill/semantic/tools_validate_edge.go` returns 1
    - `grep -c "registerValidateGraphEdge(server, s, tracer)" internal/skill/semantic/register.go` returns 1
    - `grep -E "BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshotFacts" internal/skill/semantic/tools_validate_edge.go | grep -v '^#'` returns no matches
    - `grep -c "types.CapCommentConfidence" internal/skill/semantic/tools_validate_edge.go` returns ≥ 1
    - All eleven TestValidateGraphEdge_* tests pass under `-race -count=1`
    - recorder canaries never tripped
    - SUMMARY records the AST citation metadata investigation outcome (Open Q3 resolution)
    - At least two commits: `test(71-05)`, `feat(71-05)`
  </acceptance_criteria>
  <done>validate_graph_edge ships with D4 lenient evidence + TYPES-04 cap + edge-absent graceful path; all eleven behavior tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-tool integration tests (Three-Tools suite)</name>
  <read_first>
    - internal/skill/semantic/integration_test.go (full file — existing structure to extend)
    - internal/skill/semantic/tools_refresh_test.go (mode-rejection pattern for read+ enforcement)
    - .planning/phases/71-p1-single-symbol-read-tools/71-CONTEXT.md § D7 (test surface — items 4 and 5)
    - .planning/phases/71-p1-single-symbol-read-tools/71-VALIDATION.md (per-task verification map for cross-consistency / concurrent / envelope shape / mode enforcement)
    - 71-01-PLAN.md (populated-graph fixture export shape)
  </read_first>
  <action>
Extend `internal/skill/semantic/integration_test.go` with four new test functions sharing `buildPopulatedGraphFixture` from 71-01. NO new fixture — reuse the multi-language Go/TS/Java one.

1. `TestThreeTools_CrossConsistency`:
   - Pick a known seed (a Go fixture symbol with known incoming edges).
   - Invoke `handleExplainSymbolDeep` to get the incoming-edges list.
   - For each edge in `EdgesIncoming` (up to 5 sampled), invoke `handleValidateGraphEdge` with that (from, to, edge_kind).
   - Assert: every sampled edge returns `confidence > 0` AND `fallback_reason != "edge_not_found"`. (The two tools must agree about edge existence.)
   - Also invoke `handleFindRelatedSymbols` and assert at least one result overlaps with `EdgesIncoming` symbols (PageRank should rank caller neighbors).

2. `TestThreeTools_Concurrent`:
   - Spin 8 goroutines × 3 tools = 24 concurrent invocations against the same seed.
   - Each goroutine collects responses.
   - Assert: per-tool responses are byte-identical across goroutines (idempotency).
   - Run via `go test -run TestThreeTools_Concurrent -race -count=10` — race detector must remain clean across all 10 runs.

3. `TestThreeTools_EnvelopeShape`:
   - For each of the three tools, invoke against a known seed.
   - Marshal response to JSON.
   - Assert every response carries: `freshness` object with `graph_version`, `snapshot_id`, `extractor_run_id`, `as_of_unix_ms`, `status`; `status` is one of {"current","stale","unknown"}; if any `fallback_reason` field is present, its value is from the closed-enum set listed in the per-tool help docs.

4. `TestThreeTools_ModeEnforcement`:
   - For each of the three tools, invoke with a session snapshot below `modeTierRead`.
   - Assert: each returns an error envelope citing mode tier; no accessor calls reach the recorder (canary methods on the recorderStoreAccessor must NOT be triggered).
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go test ./internal/skill/semantic/ -run TestThreeTools_ -race -count=10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func TestThreeTools_CrossConsistency" internal/skill/semantic/integration_test.go` returns 1
    - `grep -c "func TestThreeTools_Concurrent" internal/skill/semantic/integration_test.go` returns 1
    - `grep -c "func TestThreeTools_EnvelopeShape" internal/skill/semantic/integration_test.go` returns 1
    - `grep -c "func TestThreeTools_ModeEnforcement" internal/skill/semantic/integration_test.go` returns 1
    - All four tests pass under `-race -count=10`
    - Concurrent test asserts byte-identical responses across goroutines (idempotency proof)
    - Mode-enforcement test references all three handlers (handleExplainSymbolDeep, handleFindRelatedSymbols, handleValidateGraphEdge)
  </acceptance_criteria>
  <done>Cross-tool integration suite covers D7 #4 and #5; race-clean across 10 runs.</done>
</task>

<task type="auto">
  <name>Task 3: CI grep-gate extension or in-tree gate test</name>
  <read_first>
    - Output of the grep-gate location task from 71-01-SUMMARY.md (must be available before this task runs)
    - internal/skill/semantic/tools_refresh.go (existing gated file)
    - .planning/phases/71-p1-single-symbol-read-tools/71-CONTEXT.md § D6 (grep-gate mandate)
    - .planning/phases/71-p1-single-symbol-read-tools/71-RESEARCH.md Open Question 1
  </read_first>
  <action>
Branch on the outcome of 71-01's grep-gate location investigation (recorded in 71-01-SUMMARY.md):

**Branch A — External runner found (e.g., `.github/workflows/lint.yml`, `Makefile`, or `scripts/check-read-only.sh`):**
- Locate the existing line / pattern that gates `tools_refresh.go` against `Begin/Commit/Abort/Write` tokens.
- Extend the file list (or the regex) to include `tools_explain_symbol.go`, `tools_find_related.go`, `tools_validate_edge.go`.
- Use proper grep-gate hygiene: `grep -v '^#' <file> | grep -cE 'BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshotFacts'` must equal 0 (filter out comments first so the D-09 header itself does not self-invalidate the gate).
- Commit: `ci(71-05): extend read-only grep-gate to Phase 71 handlers`.

**Branch B — No external runner found:**
- Add a Go-side gate test in `internal/skill/semantic/readonly_gate_test.go`:
  - `TestReadOnlyGate_Phase71Handlers` reads each of the three handler files via `os.ReadFile`, strips comment lines (lines starting with `//` after trim), and asserts the remaining content does NOT contain any of `BeginSnapshot`, `CommitSnapshot`, `AbortSnapshot`, `WriteSnapshotFacts`.
  - This is a string-match test, not a parse-aware test — adequate for the CI gate role per CONTEXT § D6.
- Commit: `test(71-05): add in-tree read-only gate for Phase 71 handlers`.

In both branches, the SUMMARY records which branch was taken and the exact file path of the gate.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && bash -c 'for f in internal/skill/semantic/tools_explain_symbol.go internal/skill/semantic/tools_find_related.go internal/skill/semantic/tools_validate_edge.go; do HITS=$(grep -v "^//" "$f" | grep -v "^[[:space:]]*//" | grep -cE "BeginSnapshot|CommitSnapshot|AbortSnapshot|WriteSnapshotFacts" || true); if [ "$HITS" != "0" ]; then echo "GATE FAIL: $f has $HITS hits"; exit 1; fi; done; echo "GATE OK"' && (go test ./internal/skill/semantic/ -run TestReadOnlyGate -race -count=1 2>/dev/null || echo "external-gate branch (no in-tree test)")</automated>
  </verify>
  <acceptance_criteria>
    - Either: an external CI runner file references the three new handler filenames (`grep -l 'tools_explain_symbol\|tools_find_related\|tools_validate_edge' .github/ Makefile scripts/` returns at least one path), OR an in-tree `TestReadOnlyGate_Phase71Handlers` exists in `internal/skill/semantic/readonly_gate_test.go` and passes.
    - The verify-command bash loop above returns "GATE OK" (no Begin/Commit/Abort/Write tokens outside comments in any of the three handler files).
    - SUMMARY documents which branch was taken and the gate file path.
    - At least one commit lands referring to the gate (`ci(71-05)` or `test(71-05)`).
  </acceptance_criteria>
  <done>D-09 read-only invariant enforced for all three Phase 71 handlers via a verifiable gate; SUMMARY documents the gate location.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → handler | Untrusted (from, to, edge_kind) inputs validated |
| Handler → effective graph | Trusted; read-only by D-09 invariant |
| CI / build pipeline | Enforces read-only invariant across the three Phase 71 handler files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-05-01 | Tampering | Freeform edge_kind input | mitigate | Closed surface enum validation; serr.InvalidArgs on unknown values |
| T-71-05-02 | Tampering | TYPES-04 cap bypass via additive sum | mitigate | Pitfall 5: cap applied to top-level confidence; per-source unclamped explicitly documented |
| T-71-05-03 | Information Disclosure | Misleading high confidence on degraded evidence | mitigate | D4 lenient + TYPES-04 cap caps degraded paths to ≤ 0.6 |
| T-71-05-04 | Tampering | Future regression introducing snapshot writes in Phase 71 handlers | mitigate | CI grep-gate (or in-tree gate test) blocks Begin/Commit/Abort/Write tokens in all three handler files |
| T-71-05-05 | Repudiation | Cross-tool disagreement (explain reports edge, validate denies) | mitigate | TestThreeTools_CrossConsistency asserts agreement on sampled edges |
| T-71-05-06 | Tampering | Race in concurrent invocation | mitigate | TestThreeTools_Concurrent under `-race -count=10` |
</threat_model>

<verification>
- `go vet ./internal/skill/semantic/...` clean
- `go test ./internal/skill/semantic/ -race -count=1` green (full package)
- `go test ./internal/skill/semantic/ -run TestThreeTools_ -race -count=10` green (concurrent test runs 10 times under `-race`)
- Gate verification command (Task 3 automated check) returns "GATE OK"
- Full repo: `go vet ./...` clean; `go test ./... -race -count=1` green
</verification>

<success_criteria>
- P1TOOL-06 closed
- All ten must_haves truths observable
- Phase 71 success criteria #1, #4, #5 verified by the Three-Tools integration suite
- D7 #4 (cross-consistency) and D7 #5 (race-clean concurrent) verified
- All three Phase 71 handlers are gated against snapshot writes by either an external CI runner or an in-tree Go test
</success_criteria>

<output>
Create `.planning/phases/71-p1-single-symbol-read-tools/71-05-SUMMARY.md` when done. Include: (a) which gate branch was taken in Task 3 and the gate's file path; (b) the AST citation metadata investigation outcome from Task 1 (resolves RESEARCH Open Question 3); (c) phase-completion checklist confirming all 5 ROADMAP success criteria are met across plans 71-01..05.
</output>
