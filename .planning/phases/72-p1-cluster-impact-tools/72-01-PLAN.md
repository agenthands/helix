---
phase: 72-p1-cluster-impact-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/skill/semantic/cluster_id.go
  - internal/skill/semantic/accessors.go
  - internal/skill/semantic/skill.go
  - internal/semantic/store/effective_graph.go
autonomous: true
requirements: [P1TOOL-03, P1TOOL-04]

must_haves:
  truths:
    - "encodeClusterID / decodeClusterID round-trips are correct for the composite token format"
    - "Four new narrow accessor interfaces (ClusterMapAccessor, ClusterMemberAccessor, ClusterPageRankAccessor, ImpactLookupAccessor) are declared in accessors.go and readable by wave-2 handler plans"
    - "SemanticSkill has four new fields and four SetXxx / getXxx pairs following the Phase 71 lock-guarded pattern"
    - "Store.QueryClusterSummaries, QueryClusterMembers, QueryNodePageRanks are callable read methods on *Store with race-clean tests"
  artifacts:
    - path: "internal/skill/semantic/cluster_id.go"
      provides: "encodeClusterID, decodeClusterID, decodedClusterID, checkClusterIDFresh"
      exports: []
    - path: "internal/skill/semantic/accessors.go"
      provides: "ClusterMapAccessor, ClusterMemberAccessor, ClusterPageRankAccessor, ImpactLookupAccessor + ClusterSummaryRow, ClusterMemberRow row types"
      contains: "Phase 72-01 additions"
    - path: "internal/skill/semantic/skill.go"
      provides: "clusterMap, clusterMember, clusterPageRank, impactLookup fields + setters + getters"
      contains: "Phase 72 additions"
    - path: "internal/semantic/store/effective_graph.go"
      provides: "QueryClusterSummaries, QueryClusterMembers, QueryNodePageRanks methods on *Store"
      exports: ["QueryClusterSummaries", "QueryClusterMembers", "QueryNodePageRanks"]
  key_links:
    - from: "internal/skill/semantic/tools_explain_cluster.go (wave 2)"
      to: "internal/skill/semantic/cluster_id.go"
      via: "decodeClusterID + checkClusterIDFresh"
      pattern: "decodeClusterID\\|checkClusterIDFresh"
    - from: "internal/skill/semantic/tools_cluster_map.go (wave 2)"
      to: "internal/skill/semantic/skill.go"
      via: "s.getClusterMap()"
      pattern: "getClusterMap"
    - from: "internal/skill/semantic/accessors.go"
      to: "internal/semantic/store/effective_graph.go"
      via: "ClusterMapAccessor.QueryClusterSummaries"
      pattern: "QueryClusterSummaries"
---

<objective>
Lay the wave-1 foundation for Phase 72: the opaque cluster_id codec and four new narrow accessor interfaces that wave-2 handler plans compile against.

Purpose: Wave-2 plans (PLAN-02, PLAN-03, PLAN-04) depend on three things existing before they execute: (1) the cluster_id encode/decode helpers, (2) the four accessor interfaces declared in accessors.go, and (3) the three new *Store read methods that the production accessor adapters will implement. Without this foundation both compilation and test harnesses fail.

Output: cluster_id.go (codec), accessors.go additions (4 interfaces + 2 row types), skill.go additions (4 fields + 4 setters + 4 getters), effective_graph.go additions (3 read methods + unit tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/72-p1-cluster-impact-tools/72-CONTEXT.md
@.planning/phases/72-p1-cluster-impact-tools/72-RESEARCH.md
@.planning/phases/72-p1-cluster-impact-tools/72-PATTERNS.md
@.planning/phases/71-p1-single-symbol-read-tools/71-VERIFICATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From internal/skill/semantic/accessors.go (Phase 71 section pattern to mirror):
```
// ----- Phase 71-01 additions: read-only seams for the P1 single-symbol tools. -----
type SymbolByNameAccessor interface { ... }
type ExtractorRunAccessor interface { ... }
type ClusterMembershipAccessor interface { ... }
```

From internal/skill/semantic/skill.go (lock-guarded field + setter + getter pattern):
```
// Phase 71-01 additions: read-only seams for the P1 single-symbol tools
symbolByName    SymbolByNameAccessor
extractorRun    ExtractorRunAccessor
clusterMembership ClusterMembershipAccessor

func (s *SemanticSkill) SetSymbolByName(a SymbolByNameAccessor) {
    s.mu.Lock(); defer s.mu.Unlock(); s.symbolByName = a
}
func (s *SemanticSkill) getSymbolByName() SymbolByNameAccessor {
    s.mu.Lock(); defer s.mu.Unlock(); return s.symbolByName
}
```

From internal/semantic/store/effective_graph.go (QueryRankedFiles SQL read pattern):
```
func (s *Store) QueryRankedFiles(ctx context.Context, repoID, projection string, graphVersion uint64, limit int) ([]RankedFile, error) {
    if s == nil || s.db == nil { return nil, nil }
    latest, err := s.LatestCommittedSnapshot(ctx, repoID)
    // ... SQL SELECT ... rows.Close() ... rows.Err() ...
}
```

From internal/semantic/store/effective_graph.go (ClusterStatusForGraphVersion SQL pattern, lines 238-294):
```
func (s *Store) ClusterStatusForGraphVersion(ctx context.Context, repoID string, graphVersion uint64) (ClusterStatusRow, error) {
    if s == nil || s.db == nil { return ClusterStatusRow{}, nil }
    // ... SELECT FROM semantic_clusters WHERE repo_id=? AND graph_version=? ...
}
```

From internal/semantic/store/overlay.go (ClusterSummary / ClusterMemberRow types already exist):
```
// ClusterSummary{ID uint64, MemberCount int}  — overlay.go:787
// ClusterMemberRow{ClusterID uint64, NodeID uint64} — overlay.go:794
// semantic_clusters schema: (repo_id, graph_version, cluster_id, algorithm, label, summary, score, status, computed_at)
//   score column = MemberCount at write time (overlay.go:835)
// semantic_cluster_members schema: (repo_id, graph_version, cluster_id, node_id, weight, role)
// semantic_graph_scores schema: (repo_id, graph_version, node_id, score_name, score)
```

From internal/skill/semantic/seed_resolve.go (serr.New pattern for decode errors):
```
import serr "github.com/agenthands/helix/internal/errors"
serr.New(serr.InvalidArgs, fmt.Sprintf("..."))
```

From internal/semantic/integ/lookup.go (SemanticLookup.ExpandFrom signature):
```
type SemanticLookup interface {
    ExpandFrom(ctx context.Context, ws workspace.WorkspaceKey, sym SymbolID, depth int) ([]Impact, error)
    Status(ctx context.Context, ws workspace.WorkspaceKey) (SemanticStatus, error)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cluster_id.go codec + accessors.go + skill.go wave-1 seam</name>
  <files>
    internal/skill/semantic/cluster_id.go,
    internal/skill/semantic/cluster_id_test.go,
    internal/skill/semantic/accessors.go,
    internal/skill/semantic/skill.go
  </files>
  <read_first>
    - internal/skill/semantic/seed_resolve.go — decode error shape using serr.New(serr.InvalidArgs, ...) + strings.SplitN pattern; this is the exact analog for cluster_id.go
    - internal/skill/semantic/accessors.go — Phase 71-01 section (lines 182-287), interface declaration pattern (one method per interface), row type declaration pattern
    - internal/skill/semantic/skill.go — Phase 71-01 struct fields (lines 37-57), SetXxx setter pattern (lines 139-163), getXxx getter pattern (lines 407-419)
    - internal/semantic/integ/lookup.go — SemanticLookup interface to understand what ImpactLookupAccessor wraps
  </read_first>
  <behavior>
    - encodeClusterID("weak_components", 42, 7) returns "weak_components:42:7"
    - decodeClusterID("weak_components:42:7") returns {Projection:"weak_components", GraphVersion:42, ClusterIntID:7}, nil
    - decodeClusterID("bad") returns serr.InvalidArgs error (not enough parts)
    - decodeClusterID("proj:notanint:7") returns serr.InvalidArgs error (graph_version not uint64)
    - decodeClusterID("proj:42:notanint") returns serr.InvalidArgs error (cluster_int_id not uint64)
    - checkClusterIDFresh({_, 42, _}, 42, freshness) returns nil (fresh)
    - checkClusterIDFresh({_, 41, _}, 42, freshness) returns non-nil jsonResult with FallbackReason=="stale_cluster_id"
    - Round-trip: decodeClusterID(encodeClusterID(proj, gv, id)) returns the exact same values
  </behavior>
  <action>
    Create internal/skill/semantic/cluster_id.go in package semantic. Import "fmt", "strconv", "strings", serr "github.com/agenthands/helix/internal/errors", and mcpsdk "github.com/modelcontextprotocol/go-sdk/mcp".

    Declare decodedClusterID struct with fields Projection string, GraphVersion uint64, ClusterIntID uint64.

    Implement encodeClusterID(projection string, graphVersion, clusterIntID uint64) string using fmt.Sprintf("%s:%d:%d", ...).

    Implement decodeClusterID(token string) (decodedClusterID, error) using strings.SplitN(token, ":", 3); return serr.New(serr.InvalidArgs, ...) when len(parts) != 3 or ParseUint fails for parts[1] or parts[2].

    Implement checkClusterIDFresh(decoded decodedClusterID, currentGraphVersion uint64, freshness FreshnessV2) *mcpsdk.CallToolResult: if decoded.GraphVersion != currentGraphVersion, return jsonResult(ExplainClusterResult{FallbackReason: "stale_cluster_id", Freshness: freshness}); otherwise return nil. Note: ExplainClusterResult is declared in wave-2 plan (tools_explain_cluster.go), so the function must reference it — create a stub ExplainClusterResult in cluster_id.go with just ClusterID string, FallbackReason string, Freshness FreshnessV2, OR defer checkClusterIDFresh to tools_explain_cluster.go. Preferred: declare checkClusterIDFresh inline in tools_explain_cluster.go (wave 2) and keep cluster_id.go limited to encode/decode + decodedClusterID. This avoids a circular type dependency.

    In accessors.go, add a new section comment "// ----- Phase 72-01 additions: read-only seams for the P1 cluster & impact tools. -----" after the existing Phase 71-05 block. Declare four interfaces:
    - ClusterMapAccessor with method QueryClusterSummaries(ctx context.Context, repoID, projection string, graphVersion uint64, topN int) ([]ClusterSummaryRow, error)
    - ClusterMemberAccessor with method QueryClusterMembers(ctx context.Context, repoID, projection string, graphVersion, clusterIntID uint64, limit int) ([]ClusterMemberRow, error)
    - ClusterPageRankAccessor with method QueryNodePageRanks(ctx context.Context, repoID, projection string, graphVersion uint64, nodeIDs []uint64) (map[uint64]float64, error)
    - ImpactLookupAccessor with methods ExpandFrom(ctx context.Context, ws workspace.WorkspaceKey, sym integ.SymbolID, depth int) ([]integ.Impact, error) and Status(ctx context.Context, ws workspace.WorkspaceKey) (integ.SemanticStatus, error)

    Also declare row types in the same section:
    - ClusterSummaryRow struct { ClusterIntID uint64; MemberCount int }
    - ClusterMemberRow struct { NodeID uint64; SymbolID string }

    Note: the existing store/overlay.go ClusterSummary and ClusterMemberRow types are store-internal; the accessors.go row types are skill-layer types used by handler plans.

    In skill.go, add a "// Phase 72 additions" comment block after the Phase 71-03/05 additions and add four new fields: clusterMap ClusterMapAccessor, clusterMember ClusterMemberAccessor, clusterPageRank ClusterPageRankAccessor, impactLookup ImpactLookupAccessor.

    Add four SetXxx setters (SetClusterMap, SetClusterMember, SetClusterPageRank, SetImpactLookup) following the exact lock-guarded pattern from SetSymbolByName (lines 139-145 of skill.go). Add four getXxx private getters (getClusterMap, getClusterMember, getClusterPageRank, getImpactLookup) following getSymbolByName pattern (lines 407-412).

    Create cluster_id_test.go with table-driven tests for encode/decode round-trips and error cases per the behavior block.

    Run go vet ./internal/skill/semantic/ to confirm no compilation errors before finishing.
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/ -run TestClusterID -race -count=1</automated>
  </verify>
  <done>cluster_id_test.go tests pass under -race; accessors.go has the four new interfaces; skill.go has four new fields + setters + getters; go vet clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store.QueryClusterSummaries + QueryClusterMembers + QueryNodePageRanks + tests</name>
  <files>
    internal/semantic/store/effective_graph.go,
    internal/semantic/store/effective_graph_test.go
  </files>
  <read_first>
    - internal/semantic/store/effective_graph.go — QueryRankedFiles (lines 428-484) for the exact nil-guard, LatestCommittedSnapshot, SQL SELECT, defer rows.Close(), rows.Err() pattern to follow
    - internal/semantic/store/effective_graph.go — ClusterStatusForGraphVersion (lines 238-294) for the semantic_clusters SELECT shape including the score=MemberCount overload note
    - internal/semantic/store/migrations.go lines 266-289 — schema for semantic_clusters, semantic_cluster_members, semantic_graph_scores
    - internal/semantic/store/overlay.go — ClusterSummary{ID, MemberCount} and ClusterMemberRow{ClusterID, NodeID} write-side types; note: the accessor row types (ClusterSummaryRow, ClusterMemberRow) are declared in accessors.go (skill layer); the store methods return store-internal slices whose fields map to those types
  </read_first>
  <behavior>
    - QueryClusterSummaries returns top-N rows ordered by score DESC from semantic_clusters for the given (repoID, graphVersion); score column = MemberCount per overlay.go:835
    - QueryClusterSummaries on a nil store returns nil, nil (no panic)
    - QueryClusterMembers returns rows from semantic_cluster_members for the given (repoID, graphVersion, clusterID) with a JOIN to semantic_symbols to surface stable_key as SymbolID
    - QueryClusterMembers on a nil store returns nil, nil
    - QueryNodePageRanks returns map[nodeID]score from semantic_graph_scores for the given nodeIDs where score_name = "pagerank" (or the actual score name used in Phase 62 inserts — confirm by reading the write side)
    - QueryNodePageRanks on a nil store returns nil, nil
    - All three methods wrap errors with fmt.Errorf("QueryXxx(%q, %q): %w", ...)
  </behavior>
  <action>
    Add three methods to *Store in internal/semantic/store/effective_graph.go, after the existing ClusterStatusForGraphVersion block. Follow the QueryRankedFiles pattern exactly: nil-guard at function top (if s == nil || s.db == nil { return nil, nil }), use LatestCommittedSnapshot to anchor the snapshot (or skip for methods keyed by explicit graphVersion — use graphVersion directly as passed), then run a named-const SQL query via s.db.QueryContext, defer rows.Close(), scan in a rows.Next() loop, check rows.Err().

    QueryClusterSummaries(ctx context.Context, repoID, projection string, graphVersion uint64, topN int) ([]store.ClusterSummaryRow, error): SELECT cluster_id, CAST(score AS INTEGER) AS member_count FROM semantic_clusters WHERE repo_id=? AND graph_version=? ORDER BY score DESC LIMIT ?. Return []ClusterSummaryRow with ClusterIntID=cluster_id, MemberCount=member_count. Note: ClusterSummaryRow is declared in accessors.go (skill layer) — define a parallel store-internal struct (e.g., ClusterSummaryStoreRow) here if there is a package-circular issue, OR have the method return a locally-defined type and let the accessor adapter convert. Preferred per research: return a fresh type in the store package (e.g., type ClusterSummaryResult struct { ClusterIntID uint64; MemberCount int }) and let the accessor adapter convert, OR accept that accessors.go is already in a separate package and returning the store-layer type is fine since the accessor interface bridges the gap. Use store-internal types here.

    QueryClusterMembers(ctx context.Context, repoID, projection string, graphVersion, clusterIntID uint64, limit int) ([]ClusterMemberResult, error): SELECT scm.node_id, ss.stable_key AS symbol_id FROM semantic_cluster_members scm JOIN semantic_symbols ss ON ss.node_id = scm.node_id AND ss.repo_id = scm.repo_id WHERE scm.repo_id=? AND scm.graph_version=? AND scm.cluster_id=? ORDER BY scm.node_id ASC LIMIT ?. Define ClusterMemberResult { NodeID uint64; SymbolID string } in the store package.

    QueryNodePageRanks(ctx context.Context, repoID, projection string, graphVersion uint64, nodeIDs []uint64) (map[uint64]float64, error): Build the IN clause with positional parameters (one ? per nodeID), SELECT node_id, score FROM semantic_graph_scores WHERE repo_id=? AND score_name=? AND graph_version=? AND node_id IN (...). Use score_name="pagerank" — confirm this matches what Phase 62 writes by reading the graph scores write path in internal/semantic/graph/ranking.go or similar before implementing. Return map[uint64]float64.

    Add test coverage in effective_graph_test.go (or create it if it doesn't cover these methods). Tests should seed an in-memory SQLite DB (follow the existing store test pattern), insert rows into semantic_clusters / semantic_cluster_members / semantic_symbols / semantic_graph_scores, then assert the read methods return the expected rows.

    Run go vet ./internal/semantic/store/ and go test ./internal/semantic/store/ -race -count=1 to confirm clean.
  </action>
  <verify>
    <automated>go test ./internal/semantic/store/ -run TestQueryCluster -race -count=1</automated>
  </verify>
  <done>Three new store methods exist, all three test cases pass under -race, go vet clean on the store package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler→store | cluster_id codec decode is the only input-trust boundary in this plan; all reads are from trusted local SQLite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01-01 | Tampering | decodeClusterID token parsing | mitigate | serr.InvalidArgs returned on malformed token; token fields are uint64-validated; no SQL injection path since values are bound positional args |
| T-72-01-02 | Information Disclosure | QueryNodePageRanks IN clause | mitigate | use positional bound parameters, not string concatenation, for the IN clause |
| T-72-01-SC | Tampering | npm/pip/cargo installs | accept | No new external packages; N/A |
</threat_model>

<verification>
1. go vet ./internal/skill/semantic/ — clean
2. go vet ./internal/semantic/store/ — clean
3. go test ./internal/skill/semantic/ -run TestClusterID -race -count=1 — PASS
4. go test ./internal/semantic/store/ -run TestQueryCluster -race -count=1 — PASS
5. grep "ClusterMapAccessor\|ClusterMemberAccessor\|ClusterPageRankAccessor\|ImpactLookupAccessor" internal/skill/semantic/accessors.go | wc -l returns 4
6. grep "SetClusterMap\|SetClusterMember\|SetClusterPageRank\|SetImpactLookup" internal/skill/semantic/skill.go | wc -l returns 4
7. grep "encodeClusterID\|decodeClusterID" internal/skill/semantic/cluster_id.go | wc -l returns at least 2
</verification>

<success_criteria>
- cluster_id.go exists with encodeClusterID, decodeClusterID, decodedClusterID (round-trip verified by tests)
- accessors.go has 4 new interface declarations in a Phase 72-01 section
- accessors.go has ClusterSummaryRow and ClusterMemberRow row types
- skill.go has 4 new fields + 4 SetXxx setters + 4 getXxx getters in Phase 72 section
- effective_graph.go has QueryClusterSummaries, QueryClusterMembers, QueryNodePageRanks methods
- go vet and go test -race -count=1 clean on both packages
</success_criteria>

<output>
Create `.planning/phases/72-p1-cluster-impact-tools/72-01-SUMMARY.md` when done.
</output>
