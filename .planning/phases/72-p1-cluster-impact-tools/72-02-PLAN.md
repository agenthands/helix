---
phase: 72-p1-cluster-impact-tools
plan: 02
type: tdd
wave: 2
depends_on: [72-01]
files_modified:
  - internal/skill/semantic/tools_cluster_map.go
  - internal/skill/semantic/tools_cluster_map_test.go
  - internal/skill/semantic/register.go
autonomous: true
requirements: [P1TOOL-03]

must_haves:
  truths:
    - "get_cluster_map returns workspace-level cluster overview with count, top-N clusters, member counts, representative symbols, dominant edge kinds, and FreshnessV2 envelope"
    - "cluster_id tokens in the response decode correctly via decodeClusterID"
    - "read+ mode tier is enforced at handler entry"
    - "accessor-nil graceful degrade returns empty clusters list, not a panic"
    - "registerGetClusterMap is wired in RegisterAll"
  artifacts:
    - path: "internal/skill/semantic/tools_cluster_map.go"
      provides: "handleGetClusterMap, registerGetClusterMap, GetClusterMapArgs, ClusterSummaryEntry, GetClusterMapResult"
      contains: "INVARIANT (D-09 / D-13)"
    - path: "internal/skill/semantic/tools_cluster_map_test.go"
      provides: "TestGetClusterMap_HappyPath, TestGetClusterMap_ModeTier, TestGetClusterMap_AccessorNil, TestGetClusterMap_FreshnessV2Present, TestGetClusterMap_TopNClamping, TestGetClusterMap_ReadOnlyCanary"
    - path: "internal/skill/semantic/register.go"
      provides: "registerGetClusterMap call added to RegisterAll"
  key_links:
    - from: "tools_cluster_map.go:handleGetClusterMap"
      to: "mode_check.go:checkMode"
      via: "checkMode(snap, modeTierRead) first call"
      pattern: "checkMode.*modeTierRead"
    - from: "tools_cluster_map.go:handleGetClusterMap"
      to: "skill.go:getClusterMap"
      via: "s.getClusterMap().QueryClusterSummaries(...)"
      pattern: "getClusterMap"
    - from: "tools_cluster_map.go:handleGetClusterMap"
      to: "cluster_id.go:encodeClusterID"
      via: "encodeClusterID(projection, gv, row.ClusterIntID)"
      pattern: "encodeClusterID"
    - from: "tools_cluster_map.go:handleGetClusterMap"
      to: "tools_explain_symbol.go:assembleFreshness"
      via: "s.assembleFreshness(ctx, repoID)"
      pattern: "assembleFreshness"
---

<objective>
Ship the get_cluster_map handler using TDD: RED (failing tests that express all P1TOOL-03 behavior), GREEN (minimal handler that makes tests pass), REFACTOR (clean up + extend register.go).

Purpose: Agents need a workspace-level cluster overview to understand repository structure at a glance. This tool provides count, size distribution, top-N clusters with members, representative symbols, and dominant edge kinds — all built on the Phase 62 cluster engine read path established in PLAN-01.

Output: tools_cluster_map.go (handler + register + types), tools_cluster_map_test.go (unit tests), register.go extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/72-p1-cluster-impact-tools/72-CONTEXT.md
@.planning/phases/72-p1-cluster-impact-tools/72-RESEARCH.md
@.planning/phases/72-p1-cluster-impact-tools/72-PATTERNS.md
@.planning/phases/72-p1-cluster-impact-tools/72-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From internal/skill/semantic/tools_explain_symbol.go (exact handler skeleton to follow):
```
// INVARIANT (D-09 / D-13): get_cluster_map MUST NOT touch the snapshot-write surface.
// Specifically: no Begin/Commit/Abort/Write methods on snapshots, and no compactor flush trigger.
// The grep gate in CI enforces the absence of those identifier tokens in this file.

const (
    clusterMapDefaultTopN    = 20
    clusterMapMaxTopN        = 100
    clusterMapMembersPreview = 5
)

func (s *SemanticSkill) handleGetClusterMap(ctx context.Context, args GetClusterMapArgs) *mcpsdk.CallToolResult {
    // 1. checkMode(snap, modeTierRead) — MUST be first
    // 2. workspaceKey + repoID
    // 3. clamp top_n to [1, 100], default 20
    // 4. read graphVersion via s.getStore().LatestCommittedSnapshot(ctx, repoID)
    //    or via StoreAccessor — use whatever accessor the test mock exposes
    // 5. s.getClusterMap().QueryClusterSummaries(ctx, repoID, projection, gv, topN) — nil-guarded
    // 6. For each cluster: s.getClusterPageRank().QueryNodePageRanks(...) for rep symbols + preview
    // 7. encodeClusterID(projection, gv, row.ClusterIntID) for each cluster token
    // 8. s.assembleFreshness(ctx, repoID)
    // 9. guardrails.IssueReceiptOnSuccess(...)
    // 10. return jsonResult(GetClusterMapResult{...})
}
```

From internal/skill/semantic/accessors.go (Phase 72-01 additions — new row types + interfaces):
```
type ClusterSummaryRow struct { ClusterIntID uint64; MemberCount int }
type ClusterMemberRow struct { NodeID uint64; SymbolID string }

type ClusterMapAccessor interface {
    QueryClusterSummaries(ctx, repoID, projection string, graphVersion uint64, topN int) ([]ClusterSummaryRow, error)
}
type ClusterPageRankAccessor interface {
    QueryNodePageRanks(ctx, repoID, projection string, graphVersion uint64, nodeIDs []uint64) (map[uint64]float64, error)
}
```

From internal/skill/semantic/edge_kind_surface.go:
```
type EdgeKindSurface string
MapInternalKind(internal string) EdgeKindSurface  // e.g., "CALLS" → "calls"
```

From internal/skill/semantic/handler_helpers.go:
```
func errorResult(msg string) *mcpsdk.CallToolResult  // IsError=true
func jsonResult(v any) *mcpsdk.CallToolResult         // marshals v defensively
```

From internal/skill/semantic/register.go (pattern to extend):
```
func RegisterAll(server *mcp.SerenaMCPServer, s *SemanticSkill, tracer trace.Tracer) {
    // ... existing calls ...
    registerGetClusterMap(server, s, tracer)   // ADD THIS
}
```

From integration_test.go (threeToolsStoreRec read-only canary pattern at ~lines 1370-1374):
```
type threeToolsStoreRec struct {
    t             *testing.T
    graphVersion  uint64
    latestSnapshot uint64
    // ... write methods call t.Fatalf("write called: %s", methodName) ...
}
```
</interfaces>
</context>

<feature>
  <name>get_cluster_map handler</name>
  <files>internal/skill/semantic/tools_cluster_map.go, internal/skill/semantic/tools_cluster_map_test.go</files>
  <behavior>
    Requirement P1TOOL-03 behaviors:

    Happy path: fixture with 3 clusters, topN=2 returns exactly 2 ClusterSummaryEntry items, each with cluster_id token, member_count, members_preview (up to 5 symbol_ids by PageRank), representative_symbols (top-3 by PageRank), dominant_edge_kinds (top-3 by count), and non-zero total_clusters=3.

    ClusterID token encodes correctly: decodeClusterID(entry.ClusterID) returns the exact projection/graphVersion/clusterIntID used to build the fixture.

    read+ mode tier: checkMode(snap, modeTierRead) is called first; since modeTierRead passes every session, the test confirms the path by wiring a read-mode session and asserting a non-error result with correct fields.

    accessor-nil graceful degrade: when clusterMap accessor is nil, handler returns GetClusterMapResult with empty Clusters slice, FallbackReason="cluster_map_unavailable", and valid FreshnessV2 — not a panic and not IsError.

    FreshnessV2 present: result.Freshness.GraphVersion > 0 when fixture has a committed graph version.

    top_n clamping: topN=0 defaults to 20; topN=200 is clamped to 100.

    Read-only canary: the store recorder used in the test panics/calls t.Fatalf if any write method (Begin/Commit/Abort/Write) is invoked during handleGetClusterMap.

    dominant_edge_kinds: result uses MapInternalKind to map raw edge kind strings to EdgeKindSurface values; top-3 by count among intra-cluster edges. For the fixture: provide canned intra-edge kinds and assert the correct top-3 appear.

    Note: dominant edge kinds require an accessor or computed data for intra-cluster edges. Per OQ-3 (locked: compute on demand), the handler may need a new StoreAccessor.QueryEffectiveAdjacency call or a dedicated ClusterEdgesAccessor. Per research: for Phase 72 compute on demand. If the accessor gap is discovered, use the existing StoreAccessor.QueryEffectiveAdjacency (already accessible in accessors.go) and filter by member node IDs to count intra-cluster edge kinds.
  </behavior>
  <implementation>
    RED phase: Write tools_cluster_map_test.go first. Declare mock types (clusterMapStoreRec with write-canary, fixClusterMapAccessor implementing ClusterMapAccessor, fixClusterPageRankAccessor implementing ClusterPageRankAccessor). Write the test functions from the behavior list. Run tests — they MUST fail to compile or MUST fail assertions.

    GREEN phase: Create tools_cluster_map.go. Copy INVARIANT header from tools_explain_symbol.go:17-23, adapt for get_cluster_map. Declare GetClusterMapArgs{Projection string, TopN int}, ClusterSummaryEntry{ClusterID string, MemberCount int, MembersPreview []string, RepresentativeSymbols []string, DominantEdgeKinds []EdgeKindSurface}, GetClusterMapResult{TotalClusters int, TopN int, Clusters []ClusterSummaryEntry, Freshness FreshnessV2, FallbackReason string}. Implement handleGetClusterMap following the 10-step handler order. Include registerGetClusterMap. Run tests — they MUST pass.

    REFACTOR: Add registerGetClusterMap call to register.go RegisterAll after registerValidateGraphEdge. Ensure go vet clean.

    Concrete identifiers required in action: SemanticSkill, GetClusterMapArgs, ClusterSummaryEntry, GetClusterMapResult, ClusterMapAccessor, ClusterPageRankAccessor, QueryClusterSummaries, QueryNodePageRanks, assembleFreshness, checkMode, modeTierRead, encodeClusterID, MapInternalKind, FreshnessV2, EdgeKindSurface, jsonResult, errorResult, guardrails.IssueReceiptOnSuccess, registerGetClusterMap.

    Default projection string when args.Projection == "": use "weak_components".

    Per OQ-3 (locked: compute on demand), dominant edge kinds for get_cluster_map: use StoreAccessor.QueryEffectiveAdjacency(ctx, repoID, "weak_components") to get the full adjacency, filter to edges where both src and dst are in the cluster's node set (from member NodeIDs), count by internal_kind, sort top-3, map via MapInternalKind. Add nil-guard. This is the research-recommended approach; note the O(E) cost in a TODO comment.

    getHelp text constant: define var getClusterMapHelp = "Returns workspace-level weak-component cluster overview..." for registry registration.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler→accessor | top_n and projection inputs are agent-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02-01 | Tampering | top_n input | mitigate | clamp to [1, 100] before SQL LIMIT; never passes raw user value to query |
| T-72-02-02 | Tampering | projection input | mitigate | use as string bind parameter (not interpolated into SQL); passthrough to accessor as-is |
| T-72-02-SC | Tampering | npm/pip/cargo installs | accept | no new packages |
</threat_model>

<verification>
1. RED gate: go test ./internal/skill/semantic/ -run TestGetClusterMap -race -count=1 FAILS before GREEN implementation
2. GREEN gate: go test ./internal/skill/semantic/ -run TestGetClusterMap -race -count=1 PASSES
3. grep "registerGetClusterMap" internal/skill/semantic/register.go returns 1 line
4. grep -v '^//' internal/skill/semantic/tools_cluster_map.go | grep -c "BeginSnapshot\|CommitSnapshot\|AbortSnapshot\|WriteSnapshotFacts" == 0
5. go vet ./internal/skill/semantic/ — clean
</verification>

<success_criteria>
- tools_cluster_map.go exists with INVARIANT header, handleGetClusterMap, registerGetClusterMap
- All TestGetClusterMap_* tests pass under go test -race -count=1
- register.go has registerGetClusterMap call
- D-09 forbidden tokens absent from tools_cluster_map.go non-comment lines
- go vet clean
</success_criteria>

<output>
Create `.planning/phases/72-p1-cluster-impact-tools/72-02-SUMMARY.md` when done.
</output>
