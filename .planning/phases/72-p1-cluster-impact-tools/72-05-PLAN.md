---
phase: 72-p1-cluster-impact-tools
plan: 05
type: execute
wave: 3
depends_on: [72-02, 72-03, 72-04]
files_modified:
  - internal/skill/semantic/integration_test.go
  - internal/skill/semantic/populated_graph_fixture_test.go
  - internal/skill/semantic/readonly_gate_test.go
autonomous: true
requirements: [P1TOOL-03, P1TOOL-04, P1TOOL-05]

must_haves:
  truths:
    - "TestFourTools_ClusterToImpact chain test passes: get_cluster_map → explain_cluster → get_change_impact_graph; all three FreshnessV2.GraphVersion values are identical"
    - "TestReadOnlyGate_Phase71Handlers (renamed to TestReadOnlyGate_Phase7xHandlers or extended) covers all six handler files including the three Phase 72 additions"
    - "Full package go test -race -count=1 on internal/skill/semantic/ passes clean"
    - "go vet ./... passes clean"
    - "go test ./... -race -count=1 passes clean (full project)"
  artifacts:
    - path: "internal/skill/semantic/integration_test.go"
      provides: "TestFourTools_ClusterToImpact (cross-tool chain) + newFourToolsHarness or extension of newThreeToolsHarness"
      contains: "TestFourTools_ClusterToImpact"
    - path: "internal/skill/semantic/populated_graph_fixture_test.go"
      provides: "fixClusterMapAccessor, fixClusterMemberAccessor, fixClusterPageRankAccessor, fixImpactLookupAccessor mocks for shared use across per-tool test files"
    - path: "internal/skill/semantic/readonly_gate_test.go"
      provides: "gatedHandlerFiles extended with tools_cluster_map.go, tools_explain_cluster.go, tools_change_impact.go"
  key_links:
    - from: "integration_test.go:TestFourTools_ClusterToImpact"
      to: "tools_cluster_map.go:handleGetClusterMap"
      via: "h.skill.handleGetClusterMap step 1"
      pattern: "handleGetClusterMap"
    - from: "integration_test.go:TestFourTools_ClusterToImpact"
      to: "tools_explain_cluster.go:handleExplainCluster"
      via: "h.skill.handleExplainCluster step 2"
      pattern: "handleExplainCluster"
    - from: "integration_test.go:TestFourTools_ClusterToImpact"
      to: "tools_change_impact.go:handleGetChangeImpactGraph"
      via: "h.skill.handleGetChangeImpactGraph step 3"
      pattern: "handleGetChangeImpactGraph"
    - from: "readonly_gate_test.go:gatedHandlerFiles"
      to: "tools_cluster_map.go, tools_explain_cluster.go, tools_change_impact.go"
      via: "gatedHandlerFiles slice extension"
      pattern: "tools_cluster_map.go\|tools_explain_cluster.go\|tools_change_impact.go"
---

<objective>
Seal Phase 72 with the cross-tool integration test (D7), the read-only gate extension (D-09), and the final full-project test gate.

Purpose: The three wave-2 handler plans each have per-handler unit tests, but the D7 requirement (Phase 71 inherited) mandates a cross-tool chain test that proves envelope consistency across all three tools. The read-only gate (D-09) must be extended to include all six handler files (Phase 71 + Phase 72). The final go vet + go test -race -count=1 is the phase ship gate.

Output: integration_test.go extended with TestFourTools_ClusterToImpact, populated_graph_fixture_test.go extended with Phase 72 mock accessors, readonly_gate_test.go gatedHandlerFiles extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/72-p1-cluster-impact-tools/72-CONTEXT.md
@.planning/phases/72-p1-cluster-impact-tools/72-RESEARCH.md
@.planning/phases/72-p1-cluster-impact-tools/72-PATTERNS.md
@.planning/phases/72-p1-cluster-impact-tools/72-01-SUMMARY.md
@.planning/phases/72-p1-cluster-impact-tools/72-02-SUMMARY.md
@.planning/phases/72-p1-cluster-impact-tools/72-03-SUMMARY.md
@.planning/phases/72-p1-cluster-impact-tools/72-04-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From internal/skill/semantic/integration_test.go (TestThreeTools_CrossConsistency at ~line 1448 — exact pattern to follow):
```
// TestThreeTools_CrossConsistency runs explain_symbol_deep, find_related_symbols,
// validate_graph_edge against the same fixture and asserts FreshnessV2.GraphVersion
// agreement across all three results.
func TestThreeTools_CrossConsistency(t *testing.T) {
    h := newThreeToolsHarness(t)
    ctx := context.Background()
    // Step 1: explain...
    // Step 2: related...
    // Step 3: validate...
    // Assert all three Freshness.GraphVersion are equal
}
```

From internal/skill/semantic/integration_test.go (newThreeToolsHarness at ~lines 1353-1443):
```
func newThreeToolsHarness(t *testing.T) *threeToolsHarness {
    t.Helper()
    s := &SemanticSkill{}
    _ = s.Init(skill.SkillDeps{})
    sess := &mcp.SessionInfo{SessionID: "...", Mode: "review"}  // "review" for cross-tool (change_impact needs review+)
    ws := workspace.WorkspaceKey{RepoRoot: "/tmp/..."}
    s.SetSessionAccessor(&mockSessionAccessor{ws: ws, sess: sess})
    store := &threeToolsStoreRec{t: t, graphVersion: 42, latestSnapshot: 7}
    s.SetStore(store)
    // set all accessor mocks...
    return &threeToolsHarness{skill: s, store: store}
}
```

From internal/skill/semantic/readonly_gate_test.go (current gatedHandlerFiles — extend this):
```
var gatedHandlerFiles = []string{
    "tools_explain_symbol.go",
    "tools_find_related.go",
    "tools_validate_edge.go",
    // Phase 72 additions below:
    "tools_cluster_map.go",
    "tools_explain_cluster.go",
    "tools_change_impact.go",
}
```

From integration_test.go TestThreeTools_Concurrent (~line 1518 — concurrent test pattern):
```
func TestThreeTools_Concurrent(t *testing.T) {
    h := newThreeToolsHarness(t)
    var wg sync.WaitGroup
    for g := 0; g < 8; g++ {
        wg.Add(1)
        go func() { defer wg.Done(); // call each tool ... }()
    }
    wg.Wait()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend populated_graph_fixture_test.go + readonly_gate_test.go gatedHandlerFiles</name>
  <files>
    internal/skill/semantic/populated_graph_fixture_test.go,
    internal/skill/semantic/readonly_gate_test.go
  </files>
  <read_first>
    - internal/skill/semantic/populated_graph_fixture_test.go — existing mock accessor declarations (fixSymbolByNameAccessor, fixExtractorRunAccessor, etc.) to understand naming and structural pattern; add Phase 72 mocks at the bottom in a "Phase 72 mock accessors" section comment
    - internal/skill/semantic/readonly_gate_test.go — lines 43-90 for the full current gatedHandlerFiles declaration and the scan loop; the extension is a simple slice append
    - internal/skill/semantic/tools_cluster_map_test.go (from PLAN-02) — if it declares local mock types that could be promoted to the shared fixture file instead, move them here; otherwise leave per-file mocks and only add to the shared fixture what integration_test.go needs
  </read_first>
  <action>
    In populated_graph_fixture_test.go, add a Phase 72 section at the bottom. Declare shared mock types that integration_test.go will use for newFourToolsHarness: fixClusterMapAccessorShared (implements ClusterMapAccessor, returns a static []ClusterSummaryRow with 3 entries), fixClusterMemberAccessorShared (implements ClusterMemberAccessor, returns 4 ClusterMemberRow for the first cluster), fixClusterPageRankAccessorShared (implements ClusterPageRankAccessor, returns a static map[uint64]float64 with known node IDs → PageRank values), fixImpactLookupAccessorShared (implements ImpactLookupAccessor, ExpandFrom returns 3 integ.Impact with Confidence=0.9 so confidence_cap is NOT triggered; Status returns a zero-value integ.SemanticStatus). All mock types are _test.go only (testing package).

    In readonly_gate_test.go, extend the gatedHandlerFiles var to include "tools_cluster_map.go", "tools_explain_cluster.go", "tools_change_impact.go" per the Phase 72-PATTERNS.md exact modification. The test function name stays TestReadOnlyGate_Phase71Handlers unless there is a reason to rename it — keep existing name to avoid breaking CI references; the comment can note "extended for Phase 72" in the var declaration comment.

    Run go test ./internal/skill/semantic/ -run TestReadOnlyGate -race -count=1 to confirm the extended gate passes.
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/ -run TestReadOnlyGate -race -count=1</automated>
  </verify>
  <done>
    gatedHandlerFiles has 6 entries; TestReadOnlyGate_Phase71Handlers passes scanning all six files for forbidden tokens; populated_graph_fixture_test.go has four new Phase 72 mock accessor types.
  </done>
</task>

<task type="auto">
  <name>Task 2: TestFourTools_ClusterToImpact integration + full phase gate</name>
  <files>
    internal/skill/semantic/integration_test.go
  </files>
  <read_first>
    - internal/skill/semantic/integration_test.go — lines 1353-1590 for the full newThreeToolsHarness + TestThreeTools_CrossConsistency + TestThreeTools_Concurrent pattern; the new TestFourTools_ClusterToImpact function is added after TestThreeTools_Concurrent
    - internal/skill/semantic/tools_cluster_map.go (PLAN-02 output) — GetClusterMapResult JSON structure to unmarshal
    - internal/skill/semantic/tools_explain_cluster.go (PLAN-03 output) — ExplainClusterResult JSON structure to unmarshal
    - internal/skill/semantic/tools_change_impact.go (PLAN-04 output) — GetChangeImpactGraphResult JSON structure to unmarshal
  </read_first>
  <action>
    Add function newFourToolsHarness(t *testing.T) (or extend the existing newThreeToolsHarness to wire the four new Phase 72 accessors) at the top of the new TestFourTools_* section. The harness must: set session mode="review" (get_change_impact_graph requires review+), use a store rec that has graphVersion=42 and exposes the read methods, wire SetClusterMap / SetClusterMember / SetClusterPageRank / SetImpactLookup with the shared fixture mocks from populated_graph_fixture_test.go Phase 72 section.

    Implement TestFourTools_ClusterToImpact following the 5-step pattern from PATTERNS.md:
    Step 1: h.skill.handleGetClusterMap(ctx, GetClusterMapArgs{TopN:1}) — unmarshal from CallToolResult.Content[0].Text into GetClusterMapResult, assert IsError==false, assert len(Clusters)>=1, capture Clusters[0].ClusterID as clusterID and Clusters[0].Freshness.GraphVersion.
    Step 2: h.skill.handleExplainCluster(ctx, ExplainClusterArgs{ClusterID: clusterID}) — unmarshal into ExplainClusterResult, assert IsError==false, assert len(Members)>=1, capture Members[0].SymbolID as repSymbol.
    Step 3: h.skill.handleGetChangeImpactGraph(ctx, GetChangeImpactGraphArgs{Seed: SeedInput{SymbolID: repSymbol}}) — unmarshal into GetChangeImpactGraphResult, assert IsError==false.
    Step 4: Assert cm.Freshness.GraphVersion == ec.Freshness.GraphVersion == ig.Freshness.GraphVersion (all three must be 42 from fixture).
    Step 5: Assert ec.MemberCount >= 1 (sanity: the cluster has members).

    After implementing, run the full package gate: go vet ./... and go test ./internal/skill/semantic/ -race -count=1. Fix any failures before completing.

    Then run the full project gate: go vet ./... and go test ./... -race -count=1. Fix any failures. This is the phase ship gate.
  </action>
  <verify>
    <automated>go test ./internal/skill/semantic/ -run TestFourTools_ClusterToImpact -race -count=1 && go test ./internal/skill/semantic/ -race -count=1 && go vet ./... && go test ./... -race -count=1</automated>
  </verify>
  <done>
    TestFourTools_ClusterToImpact passes; all three FreshnessV2.GraphVersion values are equal; full package go test -race -count=1 passes; go vet ./... clean; go test ./... -race -count=1 clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-only | This plan only modifies test files; no production trust surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05-01 | Tampering | gatedHandlerFiles scan (D-09 gate) | mitigate | extending the list enforces the snapshot-write-token absence check on the three new Phase 72 files |
| T-72-05-SC | Tampering | npm/pip/cargo installs | accept | no new packages |
</threat_model>

<verification>
1. go test ./internal/skill/semantic/ -run TestReadOnlyGate -race -count=1 — PASS (6 files scanned, 0 forbidden tokens)
2. go test ./internal/skill/semantic/ -run TestFourTools_ClusterToImpact -race -count=1 — PASS
3. go test ./internal/skill/semantic/ -race -count=1 — PASS (full package, all tools)
4. go vet ./... — clean
5. go test ./... -race -count=1 — clean (full project gate per CLAUDE.md)
6. grep -c "tools_cluster_map.go\|tools_explain_cluster.go\|tools_change_impact.go" internal/skill/semantic/readonly_gate_test.go == 3
</verification>

<success_criteria>
- integration_test.go has TestFourTools_ClusterToImpact that chains all three Phase 72 tools
- Integration test asserts FreshnessV2.GraphVersion identical across all three results
- readonly_gate_test.go gatedHandlerFiles has 6 entries (3 Phase 71 + 3 Phase 72)
- Full package go test -race -count=1 passes
- Full project go vet ./... and go test ./... -race -count=1 pass (CLAUDE.md gate)
</success_criteria>

<output>
Create `.planning/phases/72-p1-cluster-impact-tools/72-05-SUMMARY.md` when done.
</output>
