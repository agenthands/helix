---
phase: 73-p1-tools-integration-e2e-verification
plan: 02
type: tdd
wave: 2
depends_on: ["73-01"]
files_modified:
  - internal/skill/semantic/profile_filter_test.go
autonomous: true
requirements: [P1TOOL-07]
must_haves:
  truths:
    - "Profile-filter golden tests cover all 6 P1 tools across the full 5 profiles x 4 modes matrix"
    - "The 5 read+ P1 tools surface in all 4 modes for every profile"
    - "get_change_impact_graph (review+) surfaces in review/admin and is absent from read/edit for every profile"
  artifacts:
    - path: "internal/skill/semantic/profile_filter_test.go"
      provides: "p1ToolNames + p1ReadPlusOnlyTools + p1ReviewPlusOnlyTools slices and matrix tests"
      contains: "p1ToolNames"
  key_links:
    - from: "profile_filter_test.go"
      to: "skill.ResolveTools via resolveForProfileMode"
      via: "table-driven 5x4 matrix assertion"
      pattern: "resolveForProfileMode"
---

<objective>
Add profile-filter golden tests covering all 6 P1 MCP tools across the full
5 profiles × 4 modes matrix, asserting `tools/list` filters per active profile
with no leakage across modes (SC#1).

Purpose: extend the existing inline table-driven golden test
(`profile_filter_test.go`) per D-01 — no `testdata/` snapshots, no committed
golden JSON. The test must prove the 5 read+ P1 tools are visible in all 4
modes for all 5 profiles, and that `get_change_impact_graph` (review+) is
visible in review/admin but absent from read/edit — mirroring the existing
`index_semantic_graph` exclusion tests.

Output: new `p1ToolNames` / `p1ReviewPlusOnlyTools` slices and matrix test
functions in `profile_filter_test.go`, reusing the existing
`resolveForProfileMode` / `hasAll` / `hasNone` helpers verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/73-p1-tools-integration-e2e-verification/73-CONTEXT.md
@.planning/phases/73-p1-tools-integration-e2e-verification/73-PATTERNS.md

<interfaces>
<!-- Reuse these existing helpers VERBATIM — do not reimplement. -->
profile_filter_test.go exports (package semantic, same file):
  - resolveForProfileMode(t, store *profile.ProfileStore, profileName, modeName string) []string
  - hasAll(t, where string, got, want []string)
  - hasNone(t, where string, got, unwanted []string)
  - semanticToolNames / readPlusOnlyTools  (P0 slices — pattern to mirror)

profile.LoadEmbedded() (*profile.ProfileStore, error) — used in every test func.

The 5 profiles: full, claude-code, codex, ide-assistant, ci-bot.
The 4 modes: read, edit, review, admin.

Tier facts (locked by Plan 73-01 + handler reads):
  read+  (visible in ALL 4 modes): explain_symbol_deep, find_related_symbols,
         validate_graph_edge, get_cluster_map, explain_cluster
  review+ (visible in review/admin ONLY): get_change_impact_graph
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add P1 tool-name slices and 5x4 matrix golden tests</name>
  <files>internal/skill/semantic/profile_filter_test.go</files>
  <read_first>
    - internal/skill/semantic/profile_filter_test.go (the file being modified —
      mirror the semanticToolNames/readPlusOnlyTools slice pattern at lines
      28-43 and the TestProfileFilter_* function structure at lines 117-192)
    - internal/profile/modes/read.yaml (confirm get_change_impact_graph now in
      exclude_tools — landed by Plan 73-01)
    - internal/profile/modes/review.yaml (confirm get_change_impact_graph NOT
      excluded — review+ visible)
  </read_first>
  <behavior>
    - For every profile in {full, claude-code, codex, ide-assistant, ci-bot}
      and every mode in {read, edit, review, admin}: the 5 read+ P1 tools are
      all present in resolveForProfileMode output.
    - For every profile, in read and edit modes: get_change_impact_graph is
      absent (hasNone).
    - For every profile, in review and admin modes: get_change_impact_graph is
      present (hasAll).
    - No mode/profile cell surfaces a P1 tool that the tier table forbids
      (no leakage).
  </behavior>
  <action>
    In `profile_filter_test.go` (package semantic) add three package-level
    slices mirroring `semanticToolNames`: `p1ToolNames` (all 6 P1 tool names),
    `p1ReadPlusOnlyTools` (the 5 read+ names), and `p1ReviewPlusOnlyTools`
    (just `get_change_impact_graph`). Then add table-driven test functions
    iterating the 5-profile × 4-mode matrix and driving `resolveForProfileMode`
    for each cell:
    - `TestProfileFilter_P1ReadPlusTools_AllProfilesAllModes` — for every
      (profile, mode) cell, `hasAll` p1ReadPlusOnlyTools.
    - `TestProfileFilter_P1ChangeImpact_ReviewPlusGating` — for every profile:
      `hasNone` get_change_impact_graph in read and edit; `hasAll`
      get_change_impact_graph in review and admin. Mirror the structure of the
      existing `TestProfileFilter_ReadMode_IndexExcluded` (lines 164-175).
    Reuse `resolveForProfileMode`, `hasAll`, `hasNone`, `profile.LoadEmbedded`
    verbatim — do NOT reimplement the resolution path. Use `t.Run` subtests
    keyed on `profile+"/"+mode` for granular failure reporting.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go test -race -count=1 ./internal/skill/semantic/ -run TestProfileFilter</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/skill/semantic/ -run TestProfileFilter` exits 0.
    - `grep -c p1ToolNames internal/skill/semantic/profile_filter_test.go` returns >= 1.
    - profile_filter_test.go contains test funcs named
      TestProfileFilter_P1ReadPlusTools_AllProfilesAllModes and
      TestProfileFilter_P1ChangeImpact_ReviewPlusGating.
    - The new tests exercise all 5 profiles and all 4 modes (20 cells).
    - `go vet ./internal/skill/semantic/` exits 0.
  </acceptance_criteria>
  <done>5x4 P1 golden matrix tests pass race-clean; no-leakage assertions green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Test-only file addition; no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-02-01 | Information Disclosure | profile-filter coverage | mitigate | the golden test itself IS the mitigation — it makes any future regression that leaks a review+ tool into read/edit `tools/list` a CI failure |
| T-73-02-02 | Tampering | test file | accept | pure test addition; no production code path touched |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/skill/semantic/ -run TestProfileFilter` green.
- `go vet ./...` exits 0.
- vet-nokernel2semantic / vet-noduckdb unaffected (test file only).
</verification>

<success_criteria>
- All 6 P1 tools covered across the 5×4 matrix.
- read+ P1 tools visible in all 4 modes; get_change_impact_graph gated to
  review/admin with no read/edit leakage — for every profile.
</success_criteria>

<output>
Create `.planning/phases/73-p1-tools-integration-e2e-verification/73-02-SUMMARY.md` when done
</output>
