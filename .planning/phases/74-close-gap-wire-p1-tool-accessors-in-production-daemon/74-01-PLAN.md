---
phase: 74-close-gap-wire-p1-tool-accessors-in-production-daemon
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/skill/semantic/export_p1_test.go
autonomous: true
requirements: [AUDIT-R-03]
must_haves:
  truths:
    - "WiredAccessorsForTest(skill) returns a typed WiredAccessorsBoolMap without reflection"
    - "Daemon-package tests can call semantic.WiredAccessorsForTest across package boundaries"
    - "The 10 bool fields of WiredAccessorsBoolMap map 1-to-1 to the 10 SemanticSkill P1 accessor fields"
  artifacts:
    - path: "internal/skill/semantic/export_p1_test.go"
      provides: "WiredAccessorsForTest export and WiredAccessorsBoolMap type — test seam for A-01/A-02 bootstrap assertions"
      contains: "WiredAccessorsForTest"
  key_links:
    - from: "internal/daemon/p1_accessor_bootstrap_test.go"
      to: "internal/skill/semantic/export_p1_test.go"
      via: "semantic.WiredAccessorsForTest(skill)"
      pattern: "WiredAccessorsForTest"
---

<objective>
Add WiredAccessorsForTest and WiredAccessorsBoolMap to the existing export_p1_test.go file.
This is the Wave-0 prerequisite enabling the D-03 runtime bootstrap test (Plan 74-05) to
assert P1 accessor wiring state without reflection.

Purpose: The Phase 73 D-04a pattern requires test-only exports live in `package semantic`
(not `package semantic_test`) so cross-package daemon tests can call them. Plan 74-05's
TestSemanticBundleWiresP1Accessors calls semantic.WiredAccessorsForTest(skill) after
newSemanticBundle completes. That call compiles only if WiredAccessorsForTest is declared
in a `_test.go` file inside `package semantic` — exactly the pattern already used by
HandleExplainSymbolDeepForTest in this file.

Output: Extended export_p1_test.go with WiredAccessorsBoolMap struct and WiredAccessorsForTest
function appended after line 60.
</objective>

<execution_context>
@/Users/Janis_Vizulis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Janis_Vizulis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-CONTEXT.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append WiredAccessorsBoolMap and WiredAccessorsForTest to export_p1_test.go</name>
  <files>internal/skill/semantic/export_p1_test.go</files>
  <read_first>
    - internal/skill/semantic/export_p1_test.go — full file (60 lines); understand the package
      declaration (`package semantic`), existing Handle*ForTest export shape, and imports.
      The new code must follow the same pattern: file stays `package semantic`, new exports
      are appended after the last Handle*ForTest function.
    - internal/skill/semantic/skill.go lines 27-68 — SemanticSkill struct field names. The
      WiredAccessorsBoolMap field names MUST match these private field names exactly:
      `s.symbolByName`, `s.extractorRun`, `s.clusterMembership`, `s.typeChain`,
      `s.symbolEdges`, `s.edgeEvidence`, `s.clusterMap`, `s.clusterMember`,
      `s.clusterPageRank`, `s.impactLookup`.
    - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
      Pattern: "export_p1_test.go must be `package semantic`" and the full WiredAccessorsForTest
      code excerpt with field names.
  </read_first>
  <action>
    Append two declarations to internal/skill/semantic/export_p1_test.go after the last
    existing function (HandleGetChangeImpactGraphForTest).

    Append the WiredAccessorsBoolMap struct with 10 bool fields in this exact order:
    SymbolByName, ExtractorRun, ClusterMembership, TypeChain, SymbolEdges, EdgeEvidence,
    ClusterMap, ClusterMember, ClusterPageRank, ImpactLookup. Include a doc comment:
    "WiredAccessorsBoolMap is a snapshot of which P1 accessor fields are non-nil on a
    SemanticSkill. Used by the D-03 runtime bootstrap test to assert production wiring
    without reflection."

    Append the WiredAccessorsForTest function with signature:
      func WiredAccessorsForTest(s *SemanticSkill) WiredAccessorsBoolMap
    Body: acquire s.mu.Lock() / defer s.mu.Unlock(), return WiredAccessorsBoolMap{...}
    with each field set to the corresponding private field nil-check:
      SymbolByName: s.symbolByName != nil,
      ExtractorRun: s.extractorRun != nil,
      ClusterMembership: s.clusterMembership != nil,
      TypeChain: s.typeChain != nil,
      SymbolEdges: s.symbolEdges != nil,
      EdgeEvidence: s.edgeEvidence != nil,
      ClusterMap: s.clusterMap != nil,
      ClusterMember: s.clusterMember != nil,
      ClusterPageRank: s.clusterPageRank != nil,
      ImpactLookup: s.impactLookup != nil.

    No new imports are needed (s.mu is a sync.Mutex already accessible within `package semantic`).
    Do NOT add an import for "sync" — it is already imported transitively by skill.go in the
    same package build.

    The file must remain `package semantic` (not `package semantic_test`). This is the invariant
    that makes WiredAccessorsForTest callable from daemon-package tests as semantic.WiredAccessorsForTest.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go build ./internal/skill/semantic/... && go vet ./internal/skill/semantic/...</automated>
  </verify>
  <acceptance_criteria>
    - internal/skill/semantic/export_p1_test.go compiles cleanly under `go build ./internal/skill/semantic/...`
    - `grep -c "WiredAccessorsForTest" internal/skill/semantic/export_p1_test.go` returns >= 2 (struct + function)
    - `grep -c "WiredAccessorsBoolMap" internal/skill/semantic/export_p1_test.go` returns >= 2 (type decl + usage)
    - File first line is still `package semantic` (not `package semantic_test`)
    - `go vet ./internal/skill/semantic/...` exits 0
    - All 10 bool fields present: SymbolByName, ExtractorRun, ClusterMembership, TypeChain, SymbolEdges, EdgeEvidence, ClusterMap, ClusterMember, ClusterPageRank, ImpactLookup (ties to A-01/A-02)
  </acceptance_criteria>
  <done>
    export_p1_test.go extended with WiredAccessorsBoolMap and WiredAccessorsForTest; file compiles,
    go vet clean; all 10 accessor booleans present mapping to private skill.go field names.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-only export | _test.go file in package semantic; compiled only during tests, not leaked to production API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01-SC | Tampering | export_p1_test.go | accept | No new external packages; file is test-only; no production surface change |
</threat_model>

<verification>
go build ./internal/skill/semantic/...
go vet ./internal/skill/semantic/...
grep -c "WiredAccessorsForTest" internal/skill/semantic/export_p1_test.go
grep -c "WiredAccessorsBoolMap" internal/skill/semantic/export_p1_test.go
</verification>

<success_criteria>
export_p1_test.go in package semantic exports WiredAccessorsBoolMap and WiredAccessorsForTest,
covering all 10 P1 accessor fields as boolean nil-checks. Compiles and vets clean.
The A-01 / A-02 validation assertions (VALIDATION.md) are now implementable in Plan 74-05.
</success_criteria>

<output>
Create .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-01-SUMMARY.md when done
</output>
