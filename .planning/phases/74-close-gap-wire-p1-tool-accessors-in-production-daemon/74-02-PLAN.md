---
phase: 74-close-gap-wire-p1-tool-accessors-in-production-daemon
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/daemon/semantic_wiring.go
autonomous: true
requirements: [AUDIT-R-01]
must_haves:
  truths:
    - "Five thin adapter structs exist in semantic_wiring.go: semP1SymbolByNameAdapter, semP1ExtractorRunAdapter, semP1ClusterMapAdapter, semP1ClusterMemberAdapter, semP1ClusterPageRankAdapter"
    - "Each adapter has a nil-guard on both receiver and store field before delegating"
    - "Each adapter has a corresponding semanticBundle factory constructor method"
    - "Type conversions for QuerySymbolByName ([]string → []integ.SymbolID) and QueryClusterSummaries/QueryClusterMembers (store result types → skill row types) are correct"
  artifacts:
    - path: "internal/daemon/semantic_wiring.go"
      provides: "Five P1 store-backed adapter structs + five factory constructors (D-01 accessors)"
      contains: "semP1SymbolByNameAdapter"
  key_links:
    - from: "internal/daemon/semantic_wiring.go (factory constructors)"
      to: "internal/skill/semantic/accessors.go (accessor interfaces)"
      via: "adapter structs satisfying SymbolByNameAccessor, ExtractorRunAccessor, ClusterMapAccessor, ClusterMemberAccessor, ClusterPageRankAccessor"
      pattern: "semP1.*Adapter"
    - from: "semP1SymbolByNameAdapter.QuerySymbolByName"
      to: "*semanticstore.Store.QuerySymbolByName"
      via: "a.store.QuerySymbolByName(ctx, repoID, path, name) with []string → []integ.SymbolID conversion"
      pattern: "integ.SymbolID"
---

<objective>
Implement the five thin store-backed adapter structs for the D-01 accessors (excluding ImpactLookup
which reuses the existing integSemanticLookup) in internal/daemon/semantic_wiring.go.

These adapters are Wave-1 prerequisites for Plan 74-04 (which calls b.skill.Set* using these
factory constructors). They run in Wave 1 in parallel with Plan 74-03 (the two-hop adapters)
because they touch no overlapping files and have no inter-dependency.

Purpose: Close BLOCKER-1 for the 5 D-01 store-backed accessors by providing the production
adapter implementations. Each adapter wraps one *Store method, applies a nil-guard, and
performs the type conversion the accessor interface requires.

Output: Five new struct types + five factory constructors appended to semantic_wiring.go
in the adapter section (after line ~473, mirroring the existing semStoreAdapter block).
</objective>

<execution_context>
@/Users/Janis_Vizulis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Janis_Vizulis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-CONTEXT.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add five D-01 adapter structs and factory constructors to semantic_wiring.go</name>
  <files>internal/daemon/semantic_wiring.go</files>
  <read_first>
    - internal/daemon/semantic_wiring.go lines 383-473 — the existing accessor factory constructors
      (storeAccessor, schedulerAccessor, queueAccessor, liveAccessor, compactorAccessor,
      sessionAccessor) and the semStoreAdapter struct with its nil-guard + delegate methods.
      This is the EXACT pattern to replicate for all five new adapters.
    - internal/skill/semantic/accessors.go lines 189-412 — the five accessor interfaces:
      SymbolByNameAccessor (QuerySymbolByName), ExtractorRunAccessor (LatestExtractorRunID),
      ClusterMapAccessor (QueryClusterSummaries), ClusterMemberAccessor (QueryClusterMembers),
      ClusterPageRankAccessor (QueryNodePageRanks). Check the exact method signatures and
      return types (ClusterSummaryRow, ClusterMemberRow).
    - internal/skill/semantic/p1_e2e_external_test.go lines 317-376 — the test-local analogs
      (p1StoreSymbolByName, p1StoreExtractorRun, p1StoreClusterMap, p1StoreClusterMember,
      p1StoreClusterPageRank). These show the exact type conversion patterns needed.
    - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
      — "Store-backed accessor adapters" section with full code excerpts for all 5 adapter shapes.
  </read_first>
  <action>
    Append a new section "// ----- Phase 74 P1 store-backed accessor adapters (D-01) -----"
    to internal/daemon/semantic_wiring.go after the existing adapter implementations block
    (around line 473, after semSessionAdapter). Do NOT create a separate file — all adapters
    go in semantic_wiring.go per D-04.

    Add the following five adapter structs and factory constructors in order:

    1. semP1SymbolByNameAdapter wraps *semanticstore.Store to satisfy SymbolByNameAccessor.
       Struct fields: store *semanticstore.Store.
       Factory: func (b *semanticBundle) symbolByNameAccessor() semantic.SymbolByNameAccessor
         returns &semP1SymbolByNameAdapter{store: b.store}.
       Method QuerySymbolByName(ctx context.Context, repoID, path, name string) ([]integ.SymbolID, error):
         nil-guard on a==nil||a.store==nil returning nil,nil; call a.store.QuerySymbolByName(ctx,repoID,path,name);
         convert []string → []integ.SymbolID via a for-range loop using integ.SymbolID(s) cast.

    2. semP1ExtractorRunAdapter wraps *semanticstore.Store to satisfy ExtractorRunAccessor.
       Struct fields: store *semanticstore.Store.
       Factory: func (b *semanticBundle) extractorRunAccessor() semantic.ExtractorRunAccessor
         returns &semP1ExtractorRunAdapter{store: b.store}.
       Method LatestExtractorRunID(ctx context.Context, repoID string) (string, error):
         nil-guard returning "",nil; delegate to a.store.LatestExtractorRunID(ctx, repoID).

    3. semP1ClusterMapAdapter wraps *semanticstore.Store to satisfy ClusterMapAccessor.
       Struct fields: store *semanticstore.Store.
       Factory: func (b *semanticBundle) clusterMapAccessor() semantic.ClusterMapAccessor
         returns &semP1ClusterMapAdapter{store: b.store}.
       Method QueryClusterSummaries(ctx context.Context, repoID, projection string, graphVersion uint64, topN int) ([]semantic.ClusterSummaryRow, error):
         nil-guard returning nil,nil; call a.store.QueryClusterSummaries(...);
         convert []semanticstore.ClusterSummaryResult → []semantic.ClusterSummaryRow via
         ClusterSummaryRow{ClusterIntID: r.ClusterIntID, MemberCount: r.MemberCount}.

    4. semP1ClusterMemberAdapter wraps *semanticstore.Store to satisfy ClusterMemberAccessor.
       Struct fields: store *semanticstore.Store.
       Factory: func (b *semanticBundle) clusterMemberAccessor() semantic.ClusterMemberAccessor
         returns &semP1ClusterMemberAdapter{store: b.store}.
       Method QueryClusterMembers(ctx context.Context, repoID, projection string, graphVersion, clusterIntID uint64, limit int) ([]semantic.ClusterMemberRow, error):
         nil-guard returning nil,nil; call a.store.QueryClusterMembers(...);
         convert []semanticstore.ClusterMemberResult → []semantic.ClusterMemberRow via
         ClusterMemberRow{NodeID: r.NodeID, SymbolID: r.SymbolID}.

    5. semP1ClusterPageRankAdapter wraps *semanticstore.Store to satisfy ClusterPageRankAccessor.
       Struct fields: store *semanticstore.Store.
       Factory: func (b *semanticBundle) clusterPageRankAccessor() semantic.ClusterPageRankAccessor
         returns &semP1ClusterPageRankAdapter{store: b.store}.
       Method QueryNodePageRanks(ctx context.Context, repoID, projection string, graphVersion uint64, nodeIDs []uint64) (map[uint64]float64, error):
         nil-guard returning nil,nil; delegate to a.store.QueryNodePageRanks(ctx, repoID, projection, graphVersion, nodeIDs).
         NOTE: no type conversion needed — return type is map[uint64]float64 on both sides.

    All imports needed (semanticstore, semantic, integ, context) are already present in
    semantic_wiring.go. Do not add new imports.

    Add compile-time interface guards after each adapter struct definition:
      var _ semantic.SymbolByNameAccessor = (*semP1SymbolByNameAdapter)(nil)
      var _ semantic.ExtractorRunAccessor = (*semP1ExtractorRunAdapter)(nil)
      var _ semantic.ClusterMapAccessor = (*semP1ClusterMapAdapter)(nil)
      var _ semantic.ClusterMemberAccessor = (*semP1ClusterMemberAdapter)(nil)
      var _ semantic.ClusterPageRankAccessor = (*semP1ClusterPageRankAdapter)(nil)
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go build ./internal/daemon/... && go vet ./internal/daemon/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/daemon/...` exits 0 (compile-time interface guards prove satisfaction)
    - `go vet ./internal/daemon/...` exits 0
    - `grep -c "semP1SymbolByNameAdapter\|semP1ExtractorRunAdapter\|semP1ClusterMapAdapter\|semP1ClusterMemberAdapter\|semP1ClusterPageRankAdapter" internal/daemon/semantic_wiring.go` returns >= 10 (at least one struct def + one guard per adapter)
    - `grep -c "symbolByNameAccessor\|extractorRunAccessor\|clusterMapAccessor\|clusterMemberAccessor\|clusterPageRankAccessor" internal/daemon/semantic_wiring.go` returns >= 5 (factory constructors present)
    - `go vet -tags vet-nokernel2semantic ./...` exits 0 (no kernel→semantic import violation)
    - `go vet -tags vet-noduckdb ./...` exits 0 (no direct duckdb import in daemon; delegates only via *Store)
    - Ties to AUDIT-R-01 (BLOCKER-1 remediation: store-backed adapters exist for 5 D-01 accessors)
  </acceptance_criteria>
  <done>
    Five P1 adapter structs (semP1SymbolByNameAdapter, semP1ExtractorRunAdapter,
    semP1ClusterMapAdapter, semP1ClusterMemberAdapter, semP1ClusterPageRankAdapter) and five
    factory constructors appended to semantic_wiring.go; compile-time interface guards all pass;
    go vet clean; both vet-nokernel2semantic and vet-noduckdb clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon→store | repoID string received from accessor interface call (already validated upstream by handler); passed to *Store positional-parameter SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02-01 | Tampering | semP1SymbolByNameAdapter.QuerySymbolByName | mitigate | repoID/path/name passed as positional SQL parameters in *Store.QuerySymbolByName — no string interpolation |
| T-74-02-02 | Tampering | semP1ClusterMemberAdapter.QueryClusterMembers | mitigate | All parameters are positional in *Store.QueryClusterMembers — existing *Store pattern |
| T-74-02-SC | Tampering | npm/pip/cargo installs | accept | No new external packages — all code in-tree |
</threat_model>

<verification>
go build ./internal/daemon/...
go vet ./internal/daemon/...
go vet -tags vet-nokernel2semantic ./...
go vet -tags vet-noduckdb ./...
grep -c "semP1SymbolByNameAdapter" internal/daemon/semantic_wiring.go
</verification>

<success_criteria>
Five D-01 adapter structs compiled and interface-checked in semantic_wiring.go. Factory
constructors callable in Plan 74-04's setters block extension. All vet gates clean.
</success_criteria>

<output>
Create .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-02-SUMMARY.md when done
</output>
