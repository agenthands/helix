---
phase: 74-close-gap-wire-p1-tool-accessors-in-production-daemon
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/daemon/semantic_wiring.go
autonomous: true
requirements: [AUDIT-R-01]
must_haves:
  truths:
    - "semP1SymbolEdgesAdapter satisfies SymbolEdgesAccessor with all three methods: CallersOf (CALLS-filtered incoming), IncomingEdgesOf (all incoming), OutgoingEdgesOf (all outgoing)"
    - "semP1ClusterMembershipAdapter satisfies ClusterMembershipAccessor with ClusterIDOf resolving node→cluster via CurrentGraphVersion"
    - "SymbolEdgesAdapter joins semantic_symbols.symbol_id (not node_id) to semantic_edges.src_node_id/dst_node_id"
    - "CallersOf SQL adds AND edge_kind = 'CALLS'; IncomingEdgesOf and OutgoingEdgesOf use no edge_kind filter"
    - "ClusterMembershipAdapter calls CurrentGraphVersion internally before querying semantic_cluster_members"
  artifacts:
    - path: "internal/daemon/semantic_wiring.go"
      provides: "semP1SymbolEdgesAdapter + semP1ClusterMembershipAdapter structs and factory constructors (D-01a FOLD)"
      contains: "semP1SymbolEdgesAdapter"
  key_links:
    - from: "semP1SymbolEdgesAdapter.CallersOf"
      to: "semantic_edges (dst_node_id filter + edge_kind='CALLS')"
      via: "two-hop: stableKey → symbol_id via QueryNodeIDByStableKey, then JOIN semantic_symbols for From/To stable_keys"
      pattern: "QueryNodeIDByStableKey"
    - from: "semP1ClusterMembershipAdapter.ClusterIDOf"
      to: "semantic_cluster_members JOIN semantic_clusters"
      via: "CurrentGraphVersion(ctx,repoID) then node_id lookup"
      pattern: "CurrentGraphVersion"
---

<objective>
Implement the two folded two-hop adapter structs for the D-01a FOLD accessors:
semP1SymbolEdgesAdapter (SymbolEdgesAccessor) and semP1ClusterMembershipAdapter (ClusterMembershipAccessor).

These are Wave-1 parallel with Plan 74-02 (different adapter structs, no file-content conflict
because they append to the same file in a later section; Plan 74-02 and 74-03 MUST be executed
sequentially if run in the same process — the executor must merge them or run 74-02 first).

NOTE FOR EXECUTOR: Plans 74-02 and 74-03 both modify semantic_wiring.go. They are wave-1
parallel in intent but must be applied sequentially to avoid write conflicts. Apply 74-02 first
(5 simple adapters), then 74-03 (2 SQL adapters) in the same wave.

Purpose: Close BLOCKER-1 for the two D-01a FOLD accessors. Both use inline SQL against
existing schema (semantic_edges, semantic_cluster_members, semantic_clusters) with the existing
*Store helpers QueryNodeIDByStableKey and CurrentGraphVersion as building blocks.

Output: Two new adapter structs with inline SQL + two factory constructors appended to the
Phase 74 P1 adapter section in semantic_wiring.go.
</objective>

<execution_context>
@/Users/Janis_Vizulis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Janis_Vizulis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-CONTEXT.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add semP1SymbolEdgesAdapter (three-method, inline SQL) to semantic_wiring.go</name>
  <files>internal/daemon/semantic_wiring.go</files>
  <read_first>
    - internal/daemon/semantic_wiring.go — the section added in Plan 74-02 (semP1ClusterPageRankAdapter
      and the compile-time guards at the end of the Phase 74 P1 section). The new struct goes
      immediately after in the same section.
    - internal/skill/semantic/accessors.go lines 258-283 — SymbolEdgesAccessor interface with
      CallersOf, IncomingEdgesOf, OutgoingEdgesOf signatures and the SymbolEdgeRow struct
      (From integ.SymbolID, To integ.SymbolID, InternalKind string).
    - internal/semantic/store/effective_graph.go lines 671-703 — QueryNodeIDByStableKey and
      QueryStableKeyByNodeID signatures. These are the two building blocks for the two-hop
      translation.
    - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-RESEARCH.md
      — "SymbolEdgesAccessor SQL sketch" (lines 592-607) — the exact SQL for CallersOf
      (dst_node_id=? AND edge_kind='CALLS'), IncomingEdgesOf (dst_node_id=? no edge_kind filter),
      OutgoingEdgesOf (src_node_id=?). Also Pitfall 1 (join on symbol_id not node_id) and
      Pitfall 2 (CallersOf must add edge_kind='CALLS' filter).
    - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
      — "No Analog Found" section for SymbolEdgesAdapter SQL pattern.
  </read_first>
  <action>
    Append semP1SymbolEdgesAdapter to the Phase 74 P1 adapter section in semantic_wiring.go.

    Struct declaration: semP1SymbolEdgesAdapter with field store *semanticstore.Store.
    Factory constructor: func (b *semanticBundle) symbolEdgesAccessor() semantic.SymbolEdgesAccessor
      returns &semP1SymbolEdgesAdapter{store: b.store}.
    Compile-time guard: var _ semantic.SymbolEdgesAccessor = (*semP1SymbolEdgesAdapter)(nil)

    Method CallersOf(ctx context.Context, repoID string, sym integ.SymbolID) ([]semantic.SymbolEdgeRow, error):
      1. nil-guard: if a==nil || a.store==nil return nil, nil.
      2. Resolve latest snapshot: snapshotID, err := a.store.LatestCommittedSnapshot(ctx, repoID).
         On err return nil, err. If snapshotID==0 return nil, nil.
      3. Resolve nodeID: nodeID, ok, err := a.store.QueryNodeIDByStableKey(ctx, repoID, string(sym)).
         On err return nil, err. If !ok return nil, nil.
      4. Execute inline SQL against a.store.DB() (obtain via a store accessor or direct field —
         check semantic_wiring.go for how existing adapters access raw DB for inline queries,
         e.g., whether *Store exposes a QueryRaw or direct *sql.DB). IMPORTANT: if *Store does
         not expose a DB() method, use a.store.QueryNodePageRanks trick or implement via a
         dedicated *Store method call. Preferred: add a helper method on *Store called
         QuerySymbolEdgesIncoming(ctx, snapshotID, nodeID uint64, callsOnly bool) that returns
         raw rows; OR inline the SQL via a store.ExecRaw-style access. Check what *Store exposes
         for raw access. If no raw access exists, add a QuerySymbolEdges method to
         internal/semantic/store/effective_graph.go (see Task 2 guidance in this plan).
      5. For each row returned: resolve From stable_key (src_node_id → stable_key via
         QueryStableKeyByNodeID) and To stable_key (dst_node_id → stable_key).
         Assemble SymbolEdgeRow{From: integ.SymbolID(fromKey), To: integ.SymbolID(toKey), InternalKind: row.edge_kind}.

    Method IncomingEdgesOf: identical to CallersOf except NO edge_kind filter in SQL.
    Method OutgoingEdgesOf: identical to IncomingEdgesOf except filter on src_node_id=nodeID
      (not dst_node_id). Returns From=sym, To=each target.

    SQL correctness requirements:
    - JOIN on semantic_symbols.symbol_id = semantic_edges.src_node_id (NOT node_id — Pitfall 1)
    - CallersOf: WHERE snapshot_id=? AND dst_node_id=? AND edge_kind='CALLS'
    - IncomingEdgesOf: WHERE snapshot_id=? AND dst_node_id=?
    - OutgoingEdgesOf: WHERE snapshot_id=? AND src_node_id=?

    If *Store does not expose direct DB access for inline SQL, add three focused methods to
    internal/semantic/store/effective_graph.go following the existing QueryNodePageRanks pattern:
      QuerySymbolEdgesIncoming(ctx context.Context, snapshotID, dstNodeID uint64, callsOnly bool) ([]symbolEdgeRaw, error)
      QuerySymbolEdgesOutgoing(ctx context.Context, snapshotID, srcNodeID uint64) ([]symbolEdgeRaw, error)
    where symbolEdgeRaw is a local unexported struct{SrcNodeID, DstNodeID uint64; EdgeKind string}.
    The adapter then calls these methods and resolves stable_keys for each row.

    In either case, the adapter MUST use positional parameters for all SQL (no string format/concat).
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go build ./internal/daemon/... ./internal/semantic/store/... && go vet ./internal/daemon/... ./internal/semantic/store/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/daemon/... ./internal/semantic/store/...` exits 0
    - `grep -c "semP1SymbolEdgesAdapter" internal/daemon/semantic_wiring.go` >= 2 (struct + guard)
    - `grep -c "symbolEdgesAccessor" internal/daemon/semantic_wiring.go` >= 1 (factory)
    - CallersOf contains the string "CALLS" confirming the edge_kind filter (A-11: callers direction)
    - `go vet -tags vet-noduckdb ./...` exits 0 (inline SQL goes via *Store methods, not direct DuckDB import)
    - Ties to AUDIT-R-01 FOLD decision: SymbolEdgesAccessor wired, all 3 direction methods present
  </acceptance_criteria>
  <done>
    semP1SymbolEdgesAdapter compiled with CallersOf / IncomingEdgesOf / OutgoingEdgesOf methods;
    SQL correctness enforced by Pitfall 1+2 constraints; compile-time interface guard passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add semP1ClusterMembershipAdapter (two-hop, two-query) to semantic_wiring.go</name>
  <files>internal/daemon/semantic_wiring.go</files>
  <read_first>
    - internal/daemon/semantic_wiring.go — the section produced by Task 1 (semP1SymbolEdgesAdapter).
      Append immediately after.
    - internal/skill/semantic/accessors.go lines 210-221 — ClusterMembershipAccessor interface
      with ClusterIDOf(ctx, repoID string, symbolID integ.SymbolID) (clusterID uint64, size int, err error).
    - internal/semantic/store/effective_graph.go — QueryNodeIDByStableKey (line 671) and
      CurrentGraphVersion (search for method name in the file; it may be in overlay.go at line ~984).
    - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-RESEARCH.md
      — "ClusterMembershipAccessor SQL sketch" section and Pitfall 3 (adapter self-resolves
      graph_version internally via CurrentGraphVersion; does NOT rely on caller providing it).
    - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
      — "No Analog Found" table for ClusterMembershipAdapter.
  </read_first>
  <action>
    Append semP1ClusterMembershipAdapter to the Phase 74 P1 adapter section in semantic_wiring.go,
    after the SymbolEdges adapter.

    Struct declaration: semP1ClusterMembershipAdapter with field store *semanticstore.Store.
    Factory constructor: func (b *semanticBundle) clusterMembershipAccessor() semantic.ClusterMembershipAccessor
      returns &semP1ClusterMembershipAdapter{store: b.store}.
    Compile-time guard: var _ semantic.ClusterMembershipAccessor = (*semP1ClusterMembershipAdapter)(nil)

    Method ClusterIDOf(ctx context.Context, repoID string, symbolID integ.SymbolID) (uint64, int, error):
      1. nil-guard: if a==nil || a.store==nil return 0, 0, nil.
      2. Resolve graphVersion: gv, err := a.store.CurrentGraphVersion(ctx, repoID).
         On err return 0, 0, err. If gv==0 return 0, 0, nil (no committed graph yet).
      3. Resolve nodeID: nodeID, ok, err := a.store.QueryNodeIDByStableKey(ctx, repoID, string(symbolID)).
         On err return 0, 0, err. If !ok return 0, 0, nil.
      4. Execute cluster lookup SQL. If *Store exposes no direct DB access, add a method
         QueryClusterIDOfNode(ctx context.Context, repoID string, graphVersion, nodeID uint64) (clusterID uint64, memberCount int, found bool, err error)
         to internal/semantic/store/effective_graph.go that executes:
           SELECT cm.cluster_id, CAST(c.score AS INTEGER) AS member_count
             FROM semantic_cluster_members AS cm
             JOIN semantic_clusters AS c
               ON c.repo_id=cm.repo_id AND c.graph_version=cm.graph_version AND c.cluster_id=cm.cluster_id
            WHERE cm.repo_id=? AND cm.graph_version=? AND cm.node_id=?
            LIMIT 1
         Returns (0, 0, false, nil) when no row found.
      5. If found: return (clusterID, memberCount, nil).
         If not found: return (0, 0, nil) — handler emits fallback_reason="cluster_boost_unavailable".

    SQL correctness: all three WHERE parameters (repoID, graphVersion, nodeID) must be positional
    parameters, never string-formatted.
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go build ./internal/daemon/... ./internal/semantic/store/... && go vet -tags vet-noduckdb ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/daemon/... ./internal/semantic/store/...` exits 0
    - `grep -c "semP1ClusterMembershipAdapter" internal/daemon/semantic_wiring.go` >= 2
    - `grep -c "clusterMembershipAccessor" internal/daemon/semantic_wiring.go` >= 1
    - `go vet -tags vet-noduckdb ./...` exits 0
    - `go test -race -count=1 ./internal/daemon/... ./internal/skill/semantic/...` exits 0 (existing tests still pass)
    - Ties to AUDIT-R-01: ClusterMembershipAccessor FOLD complete; both D-01a FOLD adapters exist
  </acceptance_criteria>
  <done>
    semP1ClusterMembershipAdapter compiled with ClusterIDOf; graph_version self-resolved via
    CurrentGraphVersion; SQL uses positional parameters; compile-time interface guard passes;
    all existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon→store (edge query) | repoID and nodeID received from accessor call; both used as positional SQL parameters |
| daemon→store (cluster query) | repoID, graphVersion, nodeID — all positional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03-01 | Tampering | semP1SymbolEdgesAdapter SQL | mitigate | All WHERE parameters are positional (snapshot_id=?, node_id=?, edge_kind literal). No string interpolation. |
| T-74-03-02 | Tampering | semP1ClusterMembershipAdapter SQL | mitigate | repo_id, graph_version, node_id all positional parameters. LIMIT 1 prevents row explosion. |
| T-74-03-03 | Information Disclosure | node_id ↔ symbol_id aliasing (Pitfall 1) | mitigate | JOIN on semantic_symbols.symbol_id = semantic_edges.src_node_id explicitly; verified by compile-time and SQL comment. |
| T-74-03-SC | Tampering | npm/pip/cargo installs | accept | No new external packages. |
</threat_model>

<verification>
go build ./internal/daemon/... ./internal/semantic/store/...
go vet -tags vet-noduckdb ./...
go vet -tags vet-nokernel2semantic ./...
go test -race -count=1 ./internal/daemon/... ./internal/skill/semantic/...
grep -c "semP1SymbolEdgesAdapter" internal/daemon/semantic_wiring.go
grep -c "semP1ClusterMembershipAdapter" internal/daemon/semantic_wiring.go
</verification>

<success_criteria>
semP1SymbolEdgesAdapter (CallersOf/IncomingEdgesOf/OutgoingEdgesOf) and
semP1ClusterMembershipAdapter (ClusterIDOf) compiled with interface guards; SQL uses positional
parameters; both vet gates clean; existing test suite unbroken.
</success_criteria>

<output>
Create .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-03-SUMMARY.md when done
</output>
