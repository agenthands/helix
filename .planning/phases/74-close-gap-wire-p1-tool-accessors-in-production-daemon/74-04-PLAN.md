---
phase: 74-close-gap-wire-p1-tool-accessors-in-production-daemon
plan: "04"
type: execute
wave: 2
depends_on: ["74-02", "74-03"]
files_modified:
  - internal/daemon/semantic_wiring.go
autonomous: true
requirements: [AUDIT-R-01, AUDIT-R-02, AUDIT-R-04]
must_haves:
  truths:
    - "The setters block at semantic_wiring.go:252-268 calls all 8 new Set* methods after the existing 8 P0 calls"
    - "The log line emits 'setters', 14 (not 8)"
    - "SetImpactLookup is called using b.integLookupAccessor() — no new struct needed"
    - "SetSymbolByName, SetExtractorRun, SetClusterMap, SetClusterMember, SetClusterPageRank each use their Plan 74-02 factory constructors"
    - "SetSymbolEdges and SetClusterMembership use their Plan 74-03 factory constructors"
    - "TypeChain and EdgeEvidence setters are NOT called (deferred to Phase 75)"
  artifacts:
    - path: "internal/daemon/semantic_wiring.go"
      provides: "Extended setters block with 14 total Set* calls and updated log line"
      contains: '"setters", 14'
  key_links:
    - from: "semantic_wiring.go setters block"
      to: "SemanticSkill.Set* methods (skill.go:150-241)"
      via: "b.skill.SetSymbolByName(b.symbolByNameAccessor()) ... b.skill.SetImpactLookup(b.integLookupAccessor())"
      pattern: "b.skill.Set"
---

<objective>
Extend the existing setters block at semantic_wiring.go:252-268 with 6 new Set* calls from
D-01 (using Plan 74-02 factory constructors), the ImpactLookup call (using existing
integLookupAccessor), and 2 Set* calls from D-01a FOLD (using Plan 74-03 factory constructors).
Update the log line from "setters", 8 to "setters", 14.

This plan closes BLOCKER-1 and BLOCKER-2 in the v1.11 audit. After this plan, the production
daemon wires all 8 chosen P1 accessors on b.skill, and FreshnessV2 can reach status "current"
because SetExtractorRun is now called.

Purpose: The single-block invariant from D-04 requires ALL Set* calls to be in the existing
`if b.skill != nil` block. No new helper function is created.

Output: 8 new b.skill.Set*(...) lines added to the setters block; "setters", 8 replaced
with "setters", 14.
</objective>

<execution_context>
@/Users/Janis_Vizulis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Janis_Vizulis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-CONTEXT.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
@.planning/phases/73-p1-tools-integration-e2e-verification/73-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend setters block — add 8 P1 Set* calls and update log line to 14</name>
  <files>internal/daemon/semantic_wiring.go</files>
  <read_first>
    - internal/daemon/semantic_wiring.go lines 251-268 — the current setters block. This is the
      exact section to edit. Current content:
        b.skill = semantic.GetSemanticSkill()
        if b.skill != nil {
          b.skill.SetStore(storeAcc)
          b.skill.SetScheduler(b.schedulerAccessor())
          b.skill.SetQueue(b.queueAccessor())
          b.skill.SetLive(b.liveAccessor())
          b.skill.SetRunner(b.runner)
          b.skill.SetRetrieval(b.retrievalAdapter)
          b.skill.SetCompactor(b.compactorAccessor())
          b.skill.SetSessionAccessor(b.sessionAccessor())
          if logger != nil {
            logger.Info("semantic skill setters wired",
              "setters", 8,
              ...
          }
        }
    - internal/daemon/semantic_wiring.go — the factory constructors added in Plans 74-02 and
      74-03: symbolByNameAccessor(), extractorRunAccessor(), clusterMapAccessor(),
      clusterMemberAccessor(), clusterPageRankAccessor(), symbolEdgesAccessor(),
      clusterMembershipAccessor(). Confirm these exist before editing the setters block.
    - internal/daemon/semantic_wiring.go lines 1373-1389 — integLookupAccessor() factory.
      This returns integ.SemanticLookup (not semantic.ImpactLookupAccessor directly) — verify
      that *integSemanticLookup already implements ImpactLookupAccessor (ExpandFrom + Status
      methods). The CONTEXT.md confirms it does. The call is:
        b.skill.SetImpactLookup(b.integLookupAccessor())
      If the types don't match directly, cast may be needed — check the compile result.
    - internal/skill/semantic/skill.go lines 234-241 — SetImpactLookup signature:
        func (s *SemanticSkill) SetImpactLookup(a ImpactLookupAccessor)
      where ImpactLookupAccessor requires ExpandFrom and Status. Confirm integLookupAccessor()
      return satisfies this interface.
    - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
      — "Setters block to extend" and "Pattern 3: ImpactLookup Setter" sections.
  </read_first>
  <action>
    Edit internal/daemon/semantic_wiring.go to extend the setters block at lines 251-268.

    After the b.skill.SetSessionAccessor(b.sessionAccessor()) line, and BEFORE the
    `if logger != nil` block, insert these 8 new Set* calls in this order:
      b.skill.SetSymbolByName(b.symbolByNameAccessor())
      b.skill.SetExtractorRun(b.extractorRunAccessor())
      b.skill.SetClusterMap(b.clusterMapAccessor())
      b.skill.SetClusterMember(b.clusterMemberAccessor())
      b.skill.SetClusterPageRank(b.clusterPageRankAccessor())
      b.skill.SetImpactLookup(b.integLookupAccessor())
      b.skill.SetSymbolEdges(b.symbolEdgesAccessor())
      b.skill.SetClusterMembership(b.clusterMembershipAccessor())

    If integLookupAccessor() returns integ.SemanticLookup but SetImpactLookup takes
    semantic.ImpactLookupAccessor, you need a thin wrapper adapter. Per CONTEXT.md and PATTERNS.md
    the *integSemanticLookup type ALREADY has ExpandFrom and Status matching ImpactLookupAccessor.
    The adapter pattern from the E2E test (p1IntegLookupAcc) is the model — add a
    semP1ImpactLookupAdapter struct wrapping integ.SemanticLookup if a direct assignment
    does not compile. Then the call becomes:
      b.skill.SetImpactLookup(&semP1ImpactLookupAdapter{lookup: b.integLookupAccessor()})

    Change the log line from:
      "setters", 8,
    to:
      "setters", 14,

    Do NOT add any new function, split the block, or add a separate wireP1Accessors() helper
    (D-04 single-block invariant). All 8 new calls go inline in the existing if b.skill != nil
    block.

    TypeChain and EdgeEvidence setters are explicitly NOT added here (deferred to Phase 75 per
    research recommendation). Add a comment after the 8 new lines:
      // TypeChain (SetTypeChain) and EdgeEvidence (SetEdgeEvidence) deferred to Phase 75
      // — schema columns absent in Schema v6 (no tree_sitter_kind / tier / evidence_kind).
  </action>
  <verify>
    <automated>cd /Users/Janis_Vizulis/go/src/github.com/agenthands/helix && go build ./internal/daemon/... && go vet ./internal/daemon/... && grep -E '"setters", 14' internal/daemon/semantic_wiring.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/daemon/...` exits 0 (all 8 new Set* calls compile)
    - `grep -E '"setters", 14' internal/daemon/semantic_wiring.go` returns 1 match (A-03)
    - `grep -E '"setters", 8' internal/daemon/semantic_wiring.go` returns 0 matches (old line gone)
    - `grep -c "b.skill.SetSymbolByName\|b.skill.SetExtractorRun\|b.skill.SetClusterMap\|b.skill.SetClusterMember\|b.skill.SetClusterPageRank\|b.skill.SetImpactLookup\|b.skill.SetSymbolEdges\|b.skill.SetClusterMembership" internal/daemon/semantic_wiring.go` returns 8 (all 8 new calls present)
    - `grep "SetTypeChain\|SetEdgeEvidence" internal/daemon/semantic_wiring.go` returns 0 production calls (deferral respected)
    - `go vet -tags vet-nokernel2semantic ./...` exits 0
    - `go vet -tags vet-noduckdb ./...` exits 0
    - `go test -race -count=1 ./internal/daemon/...` exits 0 (existing bootstrap tests pass)
    - Ties to AUDIT-R-01 (BLOCKER-1 closed for 8 accessors), AUDIT-R-02 (BLOCKER-2 closed via SetExtractorRun), AUDIT-R-04 (wiring block is the single source of truth)
  </acceptance_criteria>
  <done>
    Setters block extended with 8 new P1 Set* calls; log line updated to "setters", 14;
    TypeChain/EdgeEvidence deferred comment present; all builds and vet gates clean;
    existing bootstrap tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setters block | Called once during newSemanticBundle construction; no user-controlled input at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-04-01 | Elevation of Privilege | integLookupAccessor() type compatibility | mitigate | Compile-time type check enforced by go build; if *integSemanticLookup does not satisfy ImpactLookupAccessor, a thin adapter wraps it (verified at compile time) |
| T-74-04-SC | Tampering | npm/pip/cargo installs | accept | No new external packages |
</threat_model>

<verification>
go build ./internal/daemon/...
go vet -tags vet-nokernel2semantic ./...
go vet -tags vet-noduckdb ./...
go test -race -count=1 ./internal/daemon/...
grep -E '"setters", 14' internal/daemon/semantic_wiring.go
grep -c "b.skill.SetSymbolByName" internal/daemon/semantic_wiring.go
</verification>

<success_criteria>
Setters block has 16 total Set* calls (8 P0 + 8 P1); log line is "setters", 14; TypeChain
and EdgeEvidence deferred; all builds, vets, and existing tests pass. BLOCKER-1 (8 accessors)
and BLOCKER-2 (SetExtractorRun) are closed in the production daemon.
</success_criteria>

<output>
Create .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-04-SUMMARY.md when done
</output>
