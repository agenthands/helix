---
phase: 74-close-gap-wire-p1-tool-accessors-in-production-daemon
plan: "05"
type: tdd
wave: 3
depends_on: ["74-01", "74-04"]
files_modified:
  - internal/daemon/p1_accessor_bootstrap_test.go
autonomous: true
requirements: [AUDIT-R-03]
must_haves:
  truths:
    - "TestSemanticBundleWiresP1Accessors asserts all 8 FOLD accessors are non-nil after daemon.New"
    - "TestSemanticBundleWiresP1Accessors asserts TypeChain and EdgeEvidence are nil (deferred)"
    - "Test lives in package daemon (same package as semantic_wiring.go) and calls semantic.WiredAccessorsForTest"
    - "Test is a runtime assertion (calls daemon.New with temp config), not a static source scan"
  artifacts:
    - path: "internal/daemon/p1_accessor_bootstrap_test.go"
      provides: "D-03 runtime bootstrap test asserting 8 wired + 2 deferred P1 accessors after production bootstrap"
      contains: "TestSemanticBundleWiresP1Accessors"
  key_links:
    - from: "internal/daemon/p1_accessor_bootstrap_test.go"
      to: "internal/skill/semantic/export_p1_test.go (WiredAccessorsForTest)"
      via: "semantic.WiredAccessorsForTest(skill) called after daemon.New"
      pattern: "WiredAccessorsForTest"
    - from: "p1_accessor_bootstrap_test.go"
      to: "semantic_wiring.go (setters block)"
      via: "daemon.New triggers newSemanticBundle which runs the setters block"
      pattern: "daemon.New"
---

<objective>
Create the D-03 runtime bootstrap test via TDD RED/GREEN/REFACTOR cycle.

The test file internal/daemon/p1_accessor_bootstrap_test.go is `package daemon` (same package
as semantic_wiring.go) and uses daemon.New + semantic.WiredAccessorsForTest to assert all 8
wired P1 accessor fields are non-nil and the 2 deferred fields are nil after production bootstrap.

Purpose: TDD here means writing TestSemanticBundleWiresP1Accessors BEFORE Plan 74-04 lands
(it will RED because the setters block only has 8 P0 calls). After Plan 74-04 runs, the test
goes GREEN. Since Plan 74-04 is a `depends_on` prerequisite (wave ordering), the TDD cycle
in this plan demonstrates correctness of 74-04 by verifying the observable production behavior.

In practice: write the test first (RED against pre-74-04 state by temporarily reverting or
running before 74-04 applies), commit the test, then confirm GREEN after 74-04 applies.

Output: p1_accessor_bootstrap_test.go with TestSemanticBundleWiresP1Accessors and
TestSemanticBundleSetterCountLog; test suite passes with all 10 accessor assertions.
</objective>

<execution_context>
@/Users/Janis_Vizulis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Janis_Vizulis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-CONTEXT.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-VALIDATION.md
</context>

<feature>
  <name>Runtime P1 accessor wiring bootstrap test (D-03)</name>
  <files>internal/daemon/p1_accessor_bootstrap_test.go, internal/skill/semantic/export_p1_test.go</files>
  <behavior>
    After daemon.New completes with a minimal temp-dir config:
    - semantic.WiredAccessorsForTest(skill).SymbolByName == true (D-01 wired)
    - semantic.WiredAccessorsForTest(skill).ExtractorRun == true (D-02 BLOCKER-2 fix)
    - semantic.WiredAccessorsForTest(skill).ClusterMap == true (D-01 wired)
    - semantic.WiredAccessorsForTest(skill).ClusterMember == true (D-01 wired)
    - semantic.WiredAccessorsForTest(skill).ClusterPageRank == true (D-01 wired)
    - semantic.WiredAccessorsForTest(skill).ImpactLookup == true (D-01 wired)
    - semantic.WiredAccessorsForTest(skill).SymbolEdges == true (D-01a FOLD)
    - semantic.WiredAccessorsForTest(skill).ClusterMembership == true (D-01a FOLD)
    - semantic.WiredAccessorsForTest(skill).TypeChain == false (DEFERRED to Phase 75)
    - semantic.WiredAccessorsForTest(skill).EdgeEvidence == false (DEFERRED to Phase 75)

    Additional assertion (A-03):
    - The setters log line in semantic_wiring.go contains "setters", 14 (static grep check)

    Cases:
    - daemon.New(cfg, logger) with "full" profile → 8 wired, 2 nil, no panic
    - skill is non-nil (semantic enabled with any tempdir DuckDB path)
  </behavior>
  <implementation>
    After the test RED-commits and confirms failure (all 8 wired-false before Plan 74-04),
    Plan 74-04 extends the setters block. The test then GREEN-passes with no code change.
    If running after Plan 74-04 has already landed (wave ordering ensures this), the RED
    phase is demonstrated by temporarily passing a nil-store config or by reading the git
    history showing the test was committed before 74-04's setter calls.

    Test file structure:
    - package daemon
    - Import: testing, require (testify), semantic (for WiredAccessorsForTest)
    - Reuse newTestConfig(t) and newTestLogger() from bootstrap_test.go (same package)
    - Call daemon.New(cfg, logger), require.NoError
    - Retrieve skill via semantic.GetSemanticSkill()
    - require.NotNil(t, skill, "SemanticSkill must be registered")
    - Call wired := semantic.WiredAccessorsForTest(skill)
    - Assert each of the 10 booleans with a descriptive message referencing the D-0x decision
  </implementation>
</feature>

<execution_flow>
## RED Phase

Write internal/daemon/p1_accessor_bootstrap_test.go with TestSemanticBundleWiresP1Accessors.
The test calls daemon.New, gets the skill via semantic.GetSemanticSkill(), calls
semantic.WiredAccessorsForTest(skill), and asserts all 8 wired fields are true.

Before Plan 74-04 applies (or to prove RED: run the test against the pre-74-04 codebase),
the test MUST fail because the setters block only wires 8 P0 accessors — all 8 P1
require.True assertions will fail.

Commit: test(74-05): add failing bootstrap test for P1 accessor wiring (D-03)

## GREEN Phase

Plan 74-04 (already in depends_on) adds the 8 new Set* calls to the setters block. After
74-04 applies, re-run the test. All 10 assertions must pass.

Commit: feat(74-05): P1 accessor wiring bootstrap test passes after 74-04 wiring

## REFACTOR Phase

No refactor expected — the test is straightforward assertions. If the test function exceeds
30 lines, extract a table-driven subtests pattern for readability. Commit only if changed.
</execution_flow>

<read_first_for_tdd>
  - internal/daemon/bootstrap_test.go — full file: newTestConfig(t), newTestLogger(), existing
    TestBootstrapRegistersAllTools pattern. Reuse newTestConfig and newTestLogger; DO NOT
    redefine them in the new file.
  - internal/skill/semantic/export_p1_test.go — the WiredAccessorsForTest export added in
    Plan 74-01. Confirm function signature: WiredAccessorsForTest(s *SemanticSkill) WiredAccessorsBoolMap
    and the 10 WiredAccessorsBoolMap fields.
  - internal/daemon/semantic_wiring.go lines 251-268 — the setters block state before and
    after Plan 74-04 applies. Understand what causes RED (no P1 Set* calls) and GREEN (after
    Plan 74-04 adds them).
  - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
    — "p1_accessor_bootstrap_test.go" section with the full assertion code excerpt.
</read_first_for_tdd>

<acceptance_criteria>
  - File internal/daemon/p1_accessor_bootstrap_test.go exists with package daemon declaration
  - TestSemanticBundleWiresP1Accessors function is present
  - `go test -race -count=1 ./internal/daemon/... -run TestSemanticBundleWiresP1Accessors` exits 0 (GREEN after 74-04)
  - All 10 require.True/require.False assertions present (A-01/A-02):
    require.True for SymbolByName, ExtractorRun, ClusterMap, ClusterMember, ClusterPageRank, ImpactLookup, SymbolEdges, ClusterMembership
    require.False for TypeChain, EdgeEvidence
  - A-03: `grep -v '^//' internal/daemon/semantic_wiring.go | grep -c '"setters", 14'` returns 1
  - `go test -race -count=1 ./internal/daemon/...` exits 0 (all daemon tests pass)
  - `go vet ./internal/daemon/...` exits 0
  - TDD gate compliance: test(74-05) commit precedes feat(74-05) commit in git log
</acceptance_criteria>

<verification>
go test -race -count=1 ./internal/daemon/... -run TestSemanticBundleWiresP1Accessors
go test -race -count=1 ./internal/daemon/...
go vet ./internal/daemon/...
grep -v '^//' internal/daemon/semantic_wiring.go | grep -c '"setters", 14'
</verification>

<success_criteria>
- Failing test written and committed (RED gate)
- Test passes after Plan 74-04 applies (GREEN gate)
- Refactor if needed (REFACTOR gate)
- All 10 accessor assertions correct per VALIDATION.md A-01/A-02
- A-03 verified by grep
- 2-3 atomic commits produced
</success_criteria>

<output>
Create .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-05-SUMMARY.md when done.
SUMMARY.md must include: RED (what test was written, why it failed), GREEN (what change made it pass),
REFACTOR (what cleanup was done if any), Commits (list of commits produced), TDD Gate Compliance section.
</output>
