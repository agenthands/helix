---
phase: 74-close-gap-wire-p1-tool-accessors-in-production-daemon
plan: "06"
type: tdd
wave: 3
depends_on: ["74-04"]
files_modified:
  - internal/skill/semantic/p1_production_wiring_e2e_test.go
  - internal/daemon/p1_adapter_export_test.go
autonomous: true
requirements: [AUDIT-R-03, AUDIT-R-04, AUDIT-R-05]
must_haves:
  truths:
    - "TestP1E2EProductionPath drives all 6 P1 handlers against the real production b.skill wired by Plan 74-04"
    - "explain_symbol_deep returns fallback_reason='' (SymbolByName + SymbolEdges wired) and type_chain=[] (TypeChain nil)"
    - "find_related_symbols returns fallback_reason='' (ClusterMembership wired)"
    - "get_cluster_map returns fallback_reason='' and total_clusters > 0 (ClusterMap + ClusterPageRank wired)"
    - "explain_cluster returns fallback_reason='' (ClusterMember + ClusterPageRank + ClusterMembership wired)"
    - "get_change_impact_graph returns fallback_reason != 'impact_lookup_unavailable' (ImpactLookup wired)"
    - "validate_graph_edge returns fallback_reason='edge_not_found' (EdgeEvidence nil — D-03b deferred contract)"
    - "All three SymbolEdgesAccessor direction partitions are exercised (callers/incoming/outgoing)"
    - "FreshnessV2 extractor_run_id is non-empty for at least one handler invocation (A-12)"
  artifacts:
    - path: "internal/skill/semantic/p1_production_wiring_e2e_test.go"
      provides: "D-03a production-path E2E test using buildP1E2EFixture data + production adapters (not test-only Set* fakes)"
      contains: "TestP1E2EProductionPath"
  key_links:
    - from: "p1_production_wiring_e2e_test.go"
      to: "internal/daemon/semantic_wiring.go (semP1SymbolEdgesAdapter, semP1ClusterMembershipAdapter)"
      via: "production adapters constructed directly from the fixture's *Store (not test-only inline fakes)"
      pattern: "semP1SymbolEdgesAdapter\|semP1ClusterMembershipAdapter"
    - from: "p1_production_wiring_e2e_test.go"
      to: "internal/skill/semantic/export_p1_test.go (Handle*ForTest)"
      via: "semantic.HandleExplainSymbolDeepForTest(s, ctx, args), etc."
      pattern: "Handle.*ForTest"
---

<objective>
Create the D-03a production-path E2E test via TDD RED/GREEN/REFACTOR cycle.

The test file internal/skill/semantic/p1_production_wiring_e2e_test.go is `package semantic_test`
(black-box, following Phase 73 D-04a convention) and reuses buildP1E2EFixture(t) for data
population. Unlike the Phase 73 p1_e2e_external_test.go which used test-only inline fakes for
SymbolEdges and ClusterMembership, this test wires those two accessors using the production
adapter structs from semantic_wiring.go (semP1SymbolEdgesAdapter, semP1ClusterMembershipAdapter),
leaving TypeChain and EdgeEvidence nil (D-03b: assert documented fallback_reason values).

Purpose: TDD ensures we specify the observable production behavior (empty fallback_reason for
wired handlers, documented fallback values for nil handlers) before implementing. RED confirms
the test fails when inline fakes are replaced with production adapters pointing at empty data
or when nil accessors are used. GREEN confirms after Plan 74-04 wires the production path.

Output: TestP1E2EProductionPath with 6 sub-assertions (one per P1 tool) + direction-partition
coverage for SymbolEdges (callers / incoming / outgoing) + A-12 FreshnessV2 assertion.
</objective>

<execution_context>
@/Users/Janis_Vizulis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Janis_Vizulis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-CONTEXT.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-PATTERNS.md
@.planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-VALIDATION.md
@.planning/phases/73-p1-tools-integration-e2e-verification/73-CONTEXT.md
</context>

<feature>
  <name>Production-path P1 E2E test (D-03a / D-03b)</name>
  <files>internal/skill/semantic/p1_production_wiring_e2e_test.go</files>
  <behavior>
    All assertions use the p1E2EFixture populated by buildP1E2EFixture(t) with production
    adapters (not inline fakes) for SymbolEdges and ClusterMembership. TypeChain and
    EdgeEvidence are nil.

    explain_symbol_deep (seed: p1SeedGoHandle):
    - fallback_reason == "" (SymbolByName + SymbolEdges wired)
    - type_chain array is empty or absent (TypeChain nil — nil-guard skips it, not an error)
    - callers field non-empty (CallersOf exercises callers direction — A-11)

    explain_symbol_deep (seed: p1SeedGoServeHTTP):
    - fallback_reason == ""
    - edges_outgoing non-empty (OutgoingEdgesOf exercises outgoing direction — A-11)

    find_related_symbols (seed: p1SeedGoServeHTTP):
    - fallback_reason == "" (ClusterMembership wired returns a valid cluster)
    - symbols field non-empty (related symbols found via cluster co-membership)

    get_cluster_map (no seed — workspace-level):
    - fallback_reason == "" (ClusterMap + ClusterPageRank wired)
    - total_clusters > 0 (fixture seeds 3 clusters — A-07)

    explain_cluster (seedClusterIDEncoded from fixture):
    - fallback_reason == "" (ClusterMember + ClusterPageRank + ClusterMembership wired)
    - cluster members field non-empty (A-08)

    get_change_impact_graph (seed: p1SeedGoServeHTTP):
    - fallback_reason != "impact_lookup_unavailable" (ImpactLookup wired — A-09)
    - nodes field non-empty (ServeHTTP → handle call chain traversal)

    validate_graph_edge (from: p1SeedGoServeHTTP, to: p1SeedGoHandle):
    - fallback_reason == "edge_not_found" (EdgeEvidence nil; handler's nil-guard fires — D-03b A-10)

    FreshnessV2 assertion (A-12):
    - For explain_symbol_deep result: extractor_run_id field is non-empty string
      (ExtractorRun wired + fixture has a committed snapshot)
    - freshness.status field is non-empty string (one of "current", "stale", "unknown")

    Cases for IncomingEdgesOf coverage (A-11):
    - explain_symbol_deep on p1SeedGoHandle with at least one assertion on edges_incoming field
      being non-empty (confirms IncomingEdgesOf is exercised)
  </behavior>
  <implementation>
    File: internal/skill/semantic/p1_production_wiring_e2e_test.go
    Package: package semantic_test (BLACK-BOX — must NOT be package semantic)

    The test reuses the buildP1E2EFixture(t) builder from p1_e2e_external_test.go. After calling
    buildP1E2EFixture(t), replace the inline fakes with production adapters:
    - Create a new *semantic.SemanticSkill (or reuse fixture.skill), call s.SetStore(storeAcc),
      s.SetSymbolByName(...), s.SetExtractorRun(...), s.SetClusterMap(...), s.SetClusterMember(...),
      s.SetClusterPageRank(...), s.SetImpactLookup(...) using the same store-backed adapter
      types from p1_e2e_external_test.go (p1StoreSymbolByName, etc.), AND:
    - s.SetSymbolEdges(newP1ProductionSymbolEdgesAdapter(fixture.store)) using the production
      semP1SymbolEdgesAdapter from internal/daemon/semantic_wiring.go.
    - s.SetClusterMembership(newP1ProductionClusterMembershipAdapter(fixture.store)) using the
      production semP1ClusterMembershipAdapter.

    Since the test is package semantic_test (black-box), it CANNOT directly instantiate
    daemon-package unexported types. Use one of two approaches:
    A. Add a constructor function in internal/daemon/ (e.g.,
       NewP1SymbolEdgesAdapterForTest(store *semanticstore.Store) semantic.SymbolEdgesAccessor)
       in a new file internal/daemon/p1_adapter_export_test.go (package daemon, _test.go suffix).
       This follows the same export pattern as integ_lookup_export.go / NewIntegSemanticLookupForTest.
    B. Alternatively, implement the two production adapters as unexported types in a separate
       file internal/daemon/p1_adapters.go and export them with a For-Test constructor in
       integ_lookup_export.go or a new export file.

    Prefer approach A (add NewP1SymbolEdgesAdapterForTest + NewP1ClusterMembershipAdapterForTest
    to internal/daemon/) since it follows the existing NewIntegSemanticLookupForTest pattern exactly.
    The test then imports "github.com/agenthands/helix/internal/daemon" (already in the E2E imports).

    TypeChain and EdgeEvidence are NOT wired (nil). The test asserts D-03b fallback_reason contracts
    for handlers that depend on them.

    JSON parsing: use the same extractText + json.Unmarshal pattern from p1_e2e_external_test.go.
    Define a minimal response struct per handler for assertion fields. Do not assert struct fields
    that are implementation-internal — only observable envelope fields (fallback_reason, total_clusters,
    nodes, members, freshness.extractor_run_id, freshness.status).
  </implementation>
</feature>

<execution_flow>
## RED Phase

Create internal/skill/semantic/p1_production_wiring_e2e_test.go with TestP1E2EProductionPath.
Wire SymbolEdges and ClusterMembership using production adapters from daemon package.
Leave TypeChain and EdgeEvidence nil.

If running before Plan 74-04 (to prove RED): the test fails because the fixture's store has
no committed edges matching the production SQL (the store is populated with the p1E2E fixture
data; the production SymbolEdgesAdapter would need to read actual semantic_edges rows that the
fixture seeds). The RED failure comes from: when production adapter's SQL runs against the
fixture *Store, it should return the edge data (the fixture does commit CALLS edges), so the
test SHOULD pass... unless the SQL join condition (Pitfall 1) is wrong.

ALTERNATIVE RED strategy: Write the test asserting fallback_reason=="" for explain_symbol_deep.
Before Plan 74-04 the production daemon path has nil SymbolByName → handler returns
fallback_reason="seed_resolution_failed". Run the test file against the pre-74-04 daemon
state (use `go test -run TestP1E2EProductionPath` with the skill wired from 74-04's factory
constructors but the setters block still having only 8 P0 calls) to confirm RED.

In the test file itself, use the factory constructors directly from the daemon package
(e.g., daemon.NewP1SymbolEdgesAdapterForTest(fixture.store)) — these constructors come from
Plan 74-02/74-03. This means the test file compiles only after 74-02/74-03 apply.

Commit: test(74-06): add failing production-path P1 E2E test (D-03a)

## GREEN Phase

After Plan 74-04 applies (setters block wires all 8 P1 accessors), and the test uses
production adapters directly (bypassing the fixture.skill which was wired with test fakes),
re-run TestP1E2EProductionPath. All 6 handler assertions + A-12 FreshnessV2 + A-11
direction-partition assertions must pass.

Commit: feat(74-06): production-path P1 E2E passes with production adapter wiring

## REFACTOR Phase

Extract JSON parsing helpers into a local helper function if the test body exceeds 80 lines.
Run tests after any extraction. Commit only if changed:
  refactor(74-06): extract response parsing helpers in production E2E
</execution_flow>

<read_first_for_tdd>
  - internal/skill/semantic/p1_e2e_external_test.go full file — the exact analog. Understand:
    buildP1E2EFixture(t) return type (p1E2EFixture), the imports block (lines 38-59), the
    Handle*ForTest call pattern, the JSON extractText + Unmarshal pattern. The new test reuses
    all of this.
  - internal/skill/semantic/export_p1_test.go — Handle*ForTest function signatures: all 6
    handlers take (s *SemanticSkill, ctx context.Context, args *Args) *mcpsdk.CallToolResult.
  - internal/daemon/integ_lookup_export.go — NewIntegSemanticLookupForTest pattern. The new
    NewP1SymbolEdgesAdapterForTest + NewP1ClusterMembershipAdapterForTest constructors follow
    the same shape.
  - internal/daemon/semantic_wiring.go — the adapter structs added in Plans 74-02/74-03
    (semP1SymbolEdgesAdapter, semP1ClusterMembershipAdapter). Confirm struct names and fields
    before writing the For-Test constructors.
  - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-VALIDATION.md
    — Per-accessor validation assertions A-04 through A-12. Each test assertion maps to one of
    these validation IDs. The test must demonstrate all A-04..A-12 are covered.
  - .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-RESEARCH.md
    — "E2E Fixture Reuse (D-03a)" section and "SymbolEdgesAccessor Direction Partitions" section
    explaining which seeds cover callers/incoming/outgoing.
</read_first_for_tdd>

<acceptance_criteria>
  - File internal/skill/semantic/p1_production_wiring_e2e_test.go exists with `package semantic_test`
  - TestP1E2EProductionPath is present
  - `go test -race -count=1 ./internal/skill/semantic/... -run TestP1E2EProductionPath` exits 0 (GREEN after 74-04)
  - A-04: explain_symbol_deep on p1SeedGoHandle: fallback_reason=="" assertion passes
  - A-05: find_related_symbols on a seed with a wired cluster: fallback_reason=="" assertion passes
  - A-06: validate_graph_edge: fallback_reason=="edge_not_found" assertion passes (D-03b, A-10)
  - A-07: get_cluster_map: fallback_reason=="" and total_clusters>0 assertion passes
  - A-08: explain_cluster: fallback_reason=="" assertion passes
  - A-09: get_change_impact_graph: fallback_reason!="impact_lookup_unavailable" assertion passes
  - A-11: at least one assertion on callers, incoming, and outgoing edge fields (direction partition coverage)
  - A-12: extractor_run_id non-empty in at least one FreshnessV2 envelope
  - `go test -race -count=1 ./internal/skill/semantic/... ./internal/daemon/...` exits 0
  - `go vet ./internal/skill/semantic/... ./internal/daemon/...` exits 0
  - TDD gate compliance: test(74-06) commit precedes feat(74-06) commit in git log
</acceptance_criteria>

<verification>
go test -race -count=1 ./internal/skill/semantic/... -run TestP1E2EProductionPath
go test -race -count=1 ./internal/skill/semantic/... ./internal/daemon/...
go vet -tags vet-nokernel2semantic ./...
go vet -tags vet-noduckdb ./...
</verification>

<success_criteria>
- Failing test written and committed (RED gate)
- All 6 handler assertions pass after 74-04 applies (GREEN gate)
- A-04..A-12 validation assertions all covered
- SymbolEdges direction partitions (callers/incoming/outgoing) all exercised
- FreshnessV2 non-empty extractor_run_id confirmed (BLOCKER-2 closed)
- TypeChain/EdgeEvidence nil-accessor fallback_reason contracts asserted (D-03b)
- 2-3 atomic commits produced
</success_criteria>

<output>
Create .planning/phases/74-close-gap-wire-p1-tool-accessors-in-production-daemon/74-06-SUMMARY.md when done.
SUMMARY.md must include: RED (what test was written, which assertions defined RED failure),
GREEN (what makes it pass), REFACTOR (if any), Commits (list), TDD Gate Compliance section,
per-assertion A-04..A-12 coverage map.
</output>
