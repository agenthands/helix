---
context: phase
phase: 75-schema-fairness-contract-tree-skeleton
workflow: /gsd-discuss-phase 75
stage: discuss-phase (Area 4 of 4 remaining)
status: paused
last_updated: 2026-06-14T09:53:17.857Z
---

# Resume Pointer

3 of 4 user-selected gray areas captured in `75-DISCUSS-CHECKPOINT.json`. One area
(Cost-table & TOS attestation format) is left, followed by `write_context`,
`git_commit`, and `update_state`.

**Re-invoke `/gsd-discuss-phase 75`.** The workflow's `check_existing` step detects
the checkpoint and offers Resume. Pick Resume → continue from Area 4.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Init resolver requires heading-form phase in ROADMAP.md | `getRoadmapPhaseInternal` only matches `##`–`####` `Phase N:` headings under the active milestone in `.planning/ROADMAP.md`. Bullets (`- [ ] Phase N: ...`) and headings inside `.planning/milestones/vX.Y-ROADMAP.md` are invisible to it, so `phase_found` returns false and the workflow refuses to run. | advisory | When a new milestone's first phase becomes active, promote it to a `### Phase N:` heading with Goal/Depends-on/Requirements lines in the main `ROADMAP.md` (mirrors what was done for v1.10 Phase 57 historically). The milestone roadmap file stays as the detail source. |

<current_state>
Discuss-phase 3/4 areas done. Phase 75 directory exists with one checkpoint file
(`75-DISCUSS-CHECKPOINT.json`). No CONTEXT.md / RESEARCH.md / PLAN.md yet — those
are downstream steps in the workflow. No code changes anywhere except
`.planning/ROADMAP.md` (Phase 75 heading promoted from bullet to heading).
</current_state>

<completed_work>

Completed in this session:

- Roadmap promotion: Added `### Phase 75: Schema, Fairness Contract & Tree Skeleton` section to `.planning/ROADMAP.md` so `init.phase-op` resolves `phase_found=true`. (Working tree, uncommitted.)
- Discuss Area 1: **Result schema version & path** — 4 sub-decisions captured.
- Discuss Area 2: **Existing `bench/` Phase 64 artifacts** — 4 sub-decisions captured.
- Discuss Area 3: **Fairness contract shape & waiver mechanism** — 4 sub-decisions captured.

All decisions persisted in `75-DISCUSS-CHECKPOINT.json` (canonical until `write_context` produces `75-CONTEXT.md`).
</completed_work>

<remaining_work>

- Discuss Area 4: **Cost-table & TOS attestation format** — ~4 single-question turns:
  1. `bench/datasets/cost-table.yaml` field shape — what columns ship at Phase 75 (provider, model_id, input_per_mtok, output_per_mtok, currency, valid_until, last_verified, deprecation_at)?
  2. `bench/PROVIDERS.md` format — structured YAML, markdown frontmatter, or free-form markdown that `make verify-tos` parses with grep?
  3. eval/ ↔ bench/ cost-table sharing — does `internal/eval/` adopt the same `cost-table.yaml`, or stay independent?
  4. Failure semantics for `make verify-tos` / `make validate-cost-table` — hard exit-non-zero (block CI) or warn-summary (informational)?
- `write_context` → `75-CONTEXT.md` from accumulated decisions + canonical refs + code context + deferred ideas + planner note on REQUIREMENTS.md BENCH-03 v1→v2 fix.
- `write_discussion_log` → `75-DISCUSSION-LOG.md` from the per-question accumulator.
- `git_commit` → `docs(75): capture phase context` covering CONTEXT.md + DISCUSSION-LOG.md; delete checkpoint file.
- `update_state` → STATE.md session record + commit.

Decisions to roll forward into `write_context`:

- result.v2.json (lineage from eval's implicit v1) — schema at `bench/schema/result.v2.schema.json` — strict additive-only bump policy — single `schema_version: "v2"` field
- git mv Phase 64 semantic microbench artifacts (`bench/_blevprobe/`, `bench/fixtures/`, `bench/semantic_bench_*.go`, `bench/semantic_bench_REPORT.md`) to `internal/semantic/bench/` in a single atomic commit
- BENCH-01 acceptance asserts six dirs at `bench/` (five runtime + `schema/`)
- INFRA-03 eval↔bench boundary = prose-only this phase (no vet analyzer until real leakage)
- Fairness contract = pure Go literal in `bench/runners/fairness_contract.go`
- `ModeOverride` struct with free-text `WaiverReason` + `ApprovedBy`; loader fatals on empty WaiverReason; overrides logged to `result.v2.json.fairness.overrides[]`
- FAIR-02 deprecation gate: `deprecation_at: YYYY-MM-DD` field on each cost-table row; gate fires < 30 days
- FAIR-03 per-task variance gate: across N≥3 runs of same `(task, mode)`, flag when `(max−min)/mean > 0.05` on `tokens_input`; cached-input tokens reported as separate columns
</remaining_work>

<decisions_made>
See `75-DISCUSS-CHECKPOINT.json` for the full structured set (3 areas × 4 sub-decisions = 12 captured decisions). The HANDOFF.json `decisions[]` array mirrors them in a compact form.

Planner notes (apply during `write_context` or downstream planning):

- REQUIREMENTS.md BENCH-03 currently says result schema is "versioned at v1"; needs a one-line v1→v2 fix to align with v1.12-ROADMAP and PROJECT.md.
- Phase 75 plan will need an early wave (Wave 0?) to land the `git mv` of Phase 64 semantic microbench artifacts before any new file lands at `bench/{datasets,runners,languages,evaluators,reports,schema}/`.
- `bench/schema/` is a sixth top-level dir beyond BENCH-01's literal five; BENCH.md and the `tree -d -L 2 bench/` acceptance test wording need to reflect that.
</decisions_made>

<blockers>
None. Cost-table & TOS area is independent of the three captured areas; cleanly resumable.
</blockers>

## Required Reading (in order)
1. `.planning/phases/75-schema-fairness-contract-tree-skeleton/75-DISCUSS-CHECKPOINT.json` — the canonical record of captured decisions; consume verbatim.
2. `.planning/HANDOFF.json` — compact decision summary + remaining work + uncommitted-files list.
3. `.planning/REQUIREMENTS.md` (lines 15–22, 24–28, 76–80, 106–112) — BENCH-*, FAIR-*, COST-*, INFRA-* REQs locked by this phase.
4. `.planning/milestones/v1.12-ROADMAP.md` (lines 43–54) — Phase 75 Goal + Success Criteria.
5. `.planning/PROJECT.md` (lines 147–187) — v1.12 milestone scoping.
6. `.planning/research/SUMMARY.md` — milestone-level architectural verdicts.

## Critical Anti-Patterns (do NOT repeat these)
- Assuming a phase listed only as a bullet in `.planning/ROADMAP.md` is visible to `init.phase-op` — it is not. Promote to a heading first.
- Do not duplicate decisions inline in `75-CONTEXT.md` if they are already in REQUIREMENTS.md / v1.12-ROADMAP.md — CONTEXT.md captures *implementation* decisions, not WHAT/WHY (those are locked upstream).

## Infrastructure State
- Working directory: `/home/john/go/src/github.com/agenthands/helix` (branch: `main`).
- Uncommitted files (this session only):
  - `.planning/ROADMAP.md` (Phase 75 heading section added)
  - `.planning/HANDOFF.json` (this pause)
  - `.planning/phases/75-schema-fairness-contract-tree-skeleton/75-DISCUSS-CHECKPOINT.json` (in-flight discussion state)
  - `.planning/phases/75-schema-fairness-contract-tree-skeleton/.continue-here.md` (this file)
- Pre-existing `git status` deletions (57-* / 58-* phase artifacts) are unrelated to this session — do not stage or commit them as part of resume work.
- No background processes / running daemons.

<context>
Phase 75 is the foundation phase of v1.12. Its decisions lock 14 downstream phases (76–89), so the gray areas chosen were the highest-leverage ones: schema version & path, existing-bench-tree coexistence, fairness contract shape & waiver mechanism, and cost-table & TOS attestation format. The first three are done; the last is independent of the others and cleanly resumable in a single `/gsd-discuss-phase 75` re-invocation.

The discuss-phase workflow is in default mode (4 single-question turns per area, then a "more or next" check). Each Area-N → "Next area" transition writes the checkpoint. After Area 4, `write_context` produces `75-CONTEXT.md`, `git_commit` writes `75-DISCUSSION-LOG.md` and deletes the checkpoint, and `update_state` records the session in STATE.md.
</context>

<next_action>
Re-invoke `/gsd-discuss-phase 75`. When the `check_existing` step detects `75-DISCUSS-CHECKPOINT.json` (3 of 4 areas complete) and offers Resume / Start fresh, pick **Resume**. The workflow will continue from Area 4 (Cost-table & TOS attestation format) and proceed through `write_context` → `git_commit` → `update_state`.
</next_action>
