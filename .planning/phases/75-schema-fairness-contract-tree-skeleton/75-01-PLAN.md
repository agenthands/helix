---
phase: 75-schema-fairness-contract-tree-skeleton
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/semantic/bench/semantic_bench_test.go
  - internal/semantic/bench/semantic_bench_fts_test.go
  - internal/semantic/bench/semantic_bench_REPORT.md
  - internal/semantic/bench/_blevprobe/probe.go
  - internal/semantic/bench/fixtures/synthetic_50k_go/README.md
  - internal/semantic/bench/fixtures/synthetic_50k_go/gen.go
  - internal/semantic/store/bench_fts_probe.go
autonomous: true
requirements: [INFRA-01, INFRA-03]
must_haves:
  truths:
    - "Top-level bench/ no longer contains any Phase 64 semantic-microbench artifact"
    - "internal/semantic/bench/ contains the relocated microbench, building under -tags=benchfts"
    - "git log --follow on a moved file shows pre-relocation history (rename preserved)"
    - "git grep for the old bench/semantic_bench paths returns no source/doc hits"
  artifacts:
    - path: "internal/semantic/bench/semantic_bench_test.go"
      provides: "Relocated Phase 64 microbench (package bench)"
      contains: "package bench"
    - path: "internal/semantic/store/bench_fts_probe.go"
      provides: "Cross-ref comment updated to new microbench path"
      contains: "internal/semantic/bench/semantic_bench_test.go"
  key_links:
    - from: "internal/semantic/store/bench_fts_probe.go"
      to: "internal/semantic/bench/semantic_bench_test.go"
      via: "doc comment cross-reference"
      pattern: "internal/semantic/bench/semantic_bench_test\\.go"
---

<objective>
Wave 0 of Phase 75: relocate the Phase 64 semantic-microbench artifacts out of the
top-level `bench/` directory into `internal/semantic/bench/`, in a single atomic
`git mv` commit, updating the two external cross-references in the same commit so
`git grep` is clean afterward. This frees the top-level `bench/` namespace for the
v1.12 bench stack and matches the kernel/semantic package boundary (D-05/D-06).

This plan MUST land before any other plan writes a new file under `bench/`. It is the
phase's hard ordering prerequisite (planner note #3, CONTEXT D-06).

Purpose: preserve `git log --follow` history continuity for the Phase 64 microbench
while clearing `bench/` for the foundation skeleton.
Output: relocated package under `internal/semantic/bench/`, updated cross-refs, clean grep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-schema-fairness-contract-tree-skeleton/75-CONTEXT.md
@.planning/phases/75-schema-fairness-contract-tree-skeleton/75-RESEARCH.md
@.planning/phases/75-schema-fairness-contract-tree-skeleton/75-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 contribution)

New file paths (created by relocation):
- `internal/semantic/bench/semantic_bench_test.go` (moved from `bench/semantic_bench_test.go`)
- `internal/semantic/bench/semantic_bench_fts_test.go` (moved from `bench/semantic_bench_fts_test.go`)
- `internal/semantic/bench/semantic_bench_REPORT.md` (moved from `bench/semantic_bench_REPORT.md`)
- `internal/semantic/bench/_blevprobe/` (moved from `bench/_blevprobe/`)
- `internal/semantic/bench/fixtures/synthetic_50k_go/` (moved from `bench/fixtures/synthetic_50k_go/`)

No new Go symbols, types, functions, CLI subcommands, flags, struct fields, Makefile
targets, or JSON Schema `$id` are introduced by this plan — it is a mechanical move.
The relocated files keep `package bench` and the `benchfts` build tag unchanged.
Invocation path changes from `./bench/...` to `./internal/semantic/bench/...`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: git mv Phase 64 microbench to internal/semantic/bench/ and update cross-refs (atomic)</name>
  <read_first>
    - bench/semantic_bench_test.go (the file being moved; note its internal prose refs at lines 5,6,11,109)
    - bench/semantic_bench_fts_test.go (the file being moved)
    - bench/semantic_bench_REPORT.md (moved; internal prose refs at lines 11,30,64)
    - bench/fixtures/synthetic_50k_go/README.md (moved; external-facing prose refs at lines 41,49)
    - internal/semantic/store/bench_fts_probe.go (NOT moved; line 4 cross-ref must be updated in this same commit)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-PATTERNS.md (§"Wave 0 Cross-Reference Edits", lines 268-280 — load-bearing path list)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-RESEARCH.md (§"Runtime State Inventory" lines 354-374 — confirms package bench kept, import is absolute module path, no go.mod change)
  </read_first>
  <action>
    Create the destination tree and move every Phase 64 artifact with `git mv` (one
    invocation per path so git records each as a rename): move `bench/semantic_bench_test.go`,
    `bench/semantic_bench_fts_test.go`, `bench/semantic_bench_REPORT.md`, the entire
    `bench/_blevprobe/` directory, and the entire `bench/fixtures/` directory (which holds
    `synthetic_50k_go/` with `gen.go`, `README.md`) to the same relative position under
    `internal/semantic/bench/`. After the move, `bench/` must contain none of these.

    Keep `package bench` unchanged in the moved `.go` files — RESEARCH confirms the only
    internal import is the absolute module path
    `github.com/agenthands/helix/internal/semantic/store`, which is unaffected by the
    directory move; the `//go:build benchfts` and `//go:build ignore` build tags are
    unaffected; no `go.mod`/`replace` edit is needed (single module).

    In the SAME commit, update the two EXTERNAL cross-references (PATTERNS §"Wave 0
    Cross-Reference Edits"): (1) `internal/semantic/store/bench_fts_probe.go` line 4 — change
    the doc-comment path `bench/semantic_bench_test.go` to
    `internal/semantic/bench/semantic_bench_test.go`. (2) the moved fixture README at
    `internal/semantic/bench/fixtures/synthetic_50k_go/README.md` lines 41,49 — change
    `bench/semantic_bench_test.go` to `internal/semantic/bench/semantic_bench_test.go` and
    `./bench/...` to `./internal/semantic/bench/...`.

    Also update the self-referential prose paths INSIDE the moved files for cleanliness so
    no `git grep` hit remains: `internal/semantic/bench/semantic_bench_test.go` lines 5,6,11,109
    and `internal/semantic/bench/semantic_bench_REPORT.md` lines 11,30,64 — rewrite any
    `bench/semantic_bench`, `bench/_blevprobe`, `bench/fixtures` literal to the
    `internal/semantic/bench/...` equivalent. These are comment/doc-only edits; do not change
    any code identifier.

    Do NOT touch the existing `make bench` Makefile target (Makefile line 84) — it runs
    `./test/bench/...`, not the moved `bench/` dir, so the move does not break it. The
    `make bench` NAME-collision with future Phase 77 BENCH-05 is documented in Plan 02's
    BENCH.md, not fixed here (RESEARCH Pitfall 1; planner note: don't fix unless trivially safe).

    Commit message form (CONTEXT specific idea): `refactor(bench): relocate Phase 64 semantic
    microbench to internal/semantic/bench/ for v1.12 INFRA-03 separation`.
  </action>
  <acceptance_criteria>
    - `find bench -maxdepth 2 -name 'semantic_bench*' -o -name '_blevprobe' -o -name 'fixtures'` (under top-level bench/) returns empty
    - `ls internal/semantic/bench/semantic_bench_test.go internal/semantic/bench/semantic_bench_fts_test.go internal/semantic/bench/semantic_bench_REPORT.md` all exist
    - `ls -d internal/semantic/bench/_blevprobe internal/semantic/bench/fixtures/synthetic_50k_go` both exist
    - `git log --follow internal/semantic/bench/semantic_bench_test.go` shows commit history predating this relocation commit (rename detected)
    - `git grep -n "bench/semantic_bench\|bench/_blevprobe\|bench/fixtures" -- ':!*.planning*' ':!legacy*'` returns ZERO matches (all rewritten to internal/semantic/bench/...)
    - `grep -c "internal/semantic/bench/semantic_bench_test.go" internal/semantic/store/bench_fts_probe.go` is >= 1
    - `go test -tags=benchfts ./internal/semantic/bench/...` exits 0 (short run; fixture may be regenerated per its README)
    - `go vet ./...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>git grep -n "bench/semantic_bench\|bench/_blevprobe\|bench/fixtures" -- ':!*.planning*' ':!legacy*'; test $(git grep -c "bench/semantic_bench\|bench/_blevprobe\|bench/fixtures" -- ':!*.planning*' ':!legacy*' | wc -l) -eq 0 && go vet ./... && go test ./...</automated>
  </verify>
  <done>
    Phase 64 microbench lives under internal/semantic/bench/ with rename history preserved;
    the two external cross-refs and the in-file prose refs all point at the new path; top-level
    bench/ holds none of the moved artifacts; `go vet ./...` and `go test ./...` are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a VCS-level file relocation. It introduces no new input parsing, no network surface, and no new executable code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Tampering | `git mv` continuity | mitigate | Single atomic commit with per-path `git mv` so rename detection holds; verified by `git log --follow` (Pitfall 4). |
| T-75-02 | Information disclosure | dangling cross-ref pointers | mitigate | Both external cross-refs + in-file prose refs rewritten in the same commit; verified by empty `git grep`. |
| T-75-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan; all deps already vendored (RESEARCH Package Legitimacy Audit: zero new packages). |

No high-severity threats: this is a no-new-code mechanical relocation. The only failure modes
are history loss and dangling references, both gated by hard acceptance checks.
</threat_model>

<verification>
- `go vet ./...` and `go test ./...` both exit 0 after the move.
- `go test -tags=benchfts ./internal/semantic/bench/...` exits 0.
- `git grep "bench/semantic_bench\|bench/_blevprobe\|bench/fixtures"` (excluding .planning, legacy) is empty.
- `git log --follow internal/semantic/bench/semantic_bench_test.go` reaches pre-relocation commits.
</verification>

<success_criteria>
Top-level `bench/` is cleared of all Phase 64 artifacts; the microbench builds and tests
under its new `internal/semantic/bench/` home with rename history preserved; no dangling
cross-references remain; the full Go suite is green. This unblocks every later Phase 75 plan
that writes under `bench/`.
</success_criteria>

<output>
Create `.planning/phases/75-schema-fairness-contract-tree-skeleton/75-01-SUMMARY.md` when done.
</output>
