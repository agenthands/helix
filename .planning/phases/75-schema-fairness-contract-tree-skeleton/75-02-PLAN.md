---
phase: 75-schema-fairness-contract-tree-skeleton
plan: 02
type: execute
wave: 1
depends_on: ["75-01"]
files_modified:
  - bench/datasets/.gitkeep
  - bench/runners/.gitkeep
  - bench/languages/.gitkeep
  - bench/evaluators/.gitkeep
  - bench/reports/.gitkeep
  - bench/schema/.gitkeep
  - bench/BENCH.md
  - bench/PROVIDERS.md
  - bench/LICENSES.md
  - eval/EVAL.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v1.12-ROADMAP.md
autonomous: false
requirements: [BENCH-01, BENCH-06, INFRA-01, INFRA-02, INFRA-03]
must_haves:
  truths:
    - "tree -d -L 2 bench/ lists exactly six dirs: datasets, runners, languages, evaluators, reports, schema"
    - "bench/BENCH.md documents the six-dir runtime-vs-contract distinction, operator prereqs, the INFRA-03 eval-bench separation note, and flags the make bench name collision"
    - "bench/PROVIDERS.md carries a per-provider YAML frontmatter block parseable by verify-tos (D-14 field set)"
    - "bench/LICENSES.md exists with a per-dataset license table scaffold"
    - "eval/EVAL.md has a reciprocal INFRA-03 pointer paragraph to bench/"
    - "REQUIREMENTS.md BENCH-03 says versioned at v2 (not v1)"
    - "REQUIREMENTS.md BENCH-01 and the ROADMAP Phase 75 Success Criterion #1 enumerate six bench dirs (schema/ marked contract-only) — reconciled with D-07"
    - "REQUIREMENTS.md BENCH-01/INFRA-03 and ROADMAP SC-1 narrow the eval/ byte-identical clause to permit the single INFRA-03 reciprocal paragraph in eval/EVAL.md — reconciled with D-08/INFRA-03"
  artifacts:
    - path: "bench/BENCH.md"
      provides: "Bench-stack operator doc + INFRA-03 note + make-bench collision flag"
      min_lines: 40
    - path: "bench/PROVIDERS.md"
      provides: "Per-provider TOS attestation with YAML frontmatter (D-14)"
      contains: "benchmarking_permitted"
    - path: "bench/LICENSES.md"
      provides: "Per-dataset license audit scaffold (INFRA-02)"
      min_lines: 10
  key_links:
    - from: "bench/BENCH.md"
      to: "eval/EVAL.md"
      via: "INFRA-03 reciprocal cross-link"
      pattern: "eval/EVAL\\.md"
    - from: "eval/EVAL.md"
      to: "bench/BENCH.md"
      via: "INFRA-03 reciprocal cross-link"
      pattern: "bench/BENCH\\.md"
---

<objective>
Wave 1 of Phase 75: stand up the six-directory `bench/` skeleton and author the
foundation prose docs — `bench/BENCH.md` (operator prereqs + runtime-vs-contract
distinction + INFRA-03 separation note + `make bench` collision flag), `bench/PROVIDERS.md`
(per-provider TOS attestation with machine-parseable YAML frontmatter), and
`bench/LICENSES.md` (per-dataset license audit scaffold). Add the reciprocal INFRA-03
cross-link to `eval/EVAL.md`, and apply the graded-criteria reconciliation doc edits
(BENCH-03 `v1`→`v2`; BENCH-01/INFRA-03/ROADMAP-SC-1 five-dir→six-dir + eval-byte-identical
narrowing) so the upstream REQUIREMENTS.md / ROADMAP wording matches the LOCKED decisions
D-07 (six dirs) and D-08/INFRA-03 (mandated eval/EVAL.md reciprocal cross-link).

Per D-08, INFRA-03 is enforced by PROSE ONLY this phase — DO NOT add a `vet-noeval2bench`
analyzer (deferred until a real leak appears; matches the `internal/lint/` precedent).

Purpose: establish the directory contract every downstream phase (76-89) writes into and the
TOS/license attestation surface the validators in Plan 05 enforce; reconcile the graded
acceptance text with the locked decisions so verify-phase grades against an achievable spec.
Output: six-dir tree, three bench docs, eval cross-link, REQUIREMENTS + ROADMAP reconciliation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-schema-fairness-contract-tree-skeleton/75-CONTEXT.md
@.planning/phases/75-schema-fairness-contract-tree-skeleton/75-RESEARCH.md
@.planning/phases/75-schema-fairness-contract-tree-skeleton/75-PATTERNS.md
@eval/EVAL.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 contribution)

New file paths:
- `bench/datasets/.gitkeep`, `bench/runners/.gitkeep`, `bench/languages/.gitkeep`,
  `bench/evaluators/.gitkeep`, `bench/reports/.gitkeep`, `bench/schema/.gitkeep`
  (six top-level bench dirs; `.gitkeep` so empty dirs are tracked)
- `bench/BENCH.md` — operator prereqs (Python 3.11+, Docker Engine, per-language toolchains),
  six-dir runtime-vs-contract map, INFRA-03 separation note, `make bench` collision flag
- `bench/PROVIDERS.md` — per-provider TOS attestation; YAML frontmatter fields:
  `provider, tos_url, attested_by, attested_on, benchmarking_permitted, publish_permitted`
- `bench/LICENSES.md` — per-dataset license audit table scaffold

Modified files:
- `eval/EVAL.md` — adds one INFRA-03 reciprocal pointer paragraph to `bench/BENCH.md`
- `.planning/REQUIREMENTS.md` — three reconciliation edits:
  (1) BENCH-03 line: `versioned at v1` → `versioned at v2` (planner note #1, D-01);
  (2) BENCH-01 line: five-dir layout → six-dir layout (schema/ contract-only, D-07) and the
      `eval/ byte-identical` clause narrowed to permit the one INFRA-03 reciprocal paragraph;
  (3) INFRA-03 line: clarify the reciprocal `eval/EVAL.md` pointer is the single permitted
      change to `eval/` under BENCH-01.
- `.planning/milestones/v1.12-ROADMAP.md` — Phase 75 Success Criterion #1: five-dir → six-dir
  layout (schema/ contract-only, D-07) and `eval/ byte-identical` narrowed to "source/runtime
  byte-identical; single permitted change is the INFRA-03 reciprocal pointer in eval/EVAL.md"
  (D-08/INFRA-03). These reconciliations align the graded criteria with the locked decisions.

No new Go symbols/functions/types/CLI subcommands/Makefile targets/JSON Schema `$id` in this
plan. `schema/` is the contract dir (empty here; the schema document lands in Plan 03).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create the six-dir bench/ skeleton and author BENCH.md / PROVIDERS.md / LICENSES.md + eval cross-link + REQUIREMENTS/ROADMAP reconciliation</name>
  <read_first>
    - eval/EVAL.md (the prose + attestation-table template; lines 1-19 show the "Verified at" + attestation table idiom; the INFRA-03 reciprocal paragraph is appended here)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-PATTERNS.md (§"bench/BENCH.md / bench/PROVIDERS.md / bench/LICENSES.md (docs) — role-match", lines 202-222 — analog is eval/EVAL.md; PROVIDERS.md frontmatter field set)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-CONTEXT.md (D-07 six dirs, D-08 prose-only INFRA-03, D-14 PROVIDERS frontmatter fields, D-16 hard-fail semantics the docs describe)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-RESEARCH.md (§"Pattern 5" YAML frontmatter shape; §"Common Pitfalls" Pitfall 1 make-bench collision + Pitfall 6 six-vs-five dirs)
    - .planning/REQUIREMENTS.md (BENCH-01 line ~17 for the six-dir + eval-byte-identical reconciliation; BENCH-03 line ~19 for the v1→v2 fix; BENCH-06 line ~22 for operator-prereq + verify-tos 90-day requirement; INFRA-03 line ~110 for the reciprocal-cross-link clarification)
    - .planning/milestones/v1.12-ROADMAP.md (Phase 75 Success Criterion #1, line ~48 — six-dir + eval-byte-identical narrowing reconciliation)
  </read_first>
  <action>
    Create the six top-level bench dirs each tracked with an empty `.gitkeep`:
    `bench/datasets/`, `bench/runners/`, `bench/languages/`, `bench/evaluators/`,
    `bench/reports/` (the five BENCH-01 runtime dirs) plus `bench/schema/` (the sixth,
    D-07 contract dir). Do NOT place runtime artifacts in any of them this task — Plan 03
    fills `schema/`, Plan 04 fills `runners/`, Plan 05 fills `datasets/`.

    Author `bench/BENCH.md` modeled on `eval/EVAL.md` prose style. It MUST include: (a) a
    six-directory map that explicitly marks the FIVE runtime dirs
    (datasets, runners, languages, evaluators, reports) vs the ONE contract dir (schema)
    per D-07/D-08; (b) operator-side prereqs per BENCH-06 — Python 3.11+, Docker Engine,
    per-language toolchains — labeled operator-side, NOT Go build deps; (c) an INFRA-03
    "eval ↔ bench separation" note stating eval/ and bench/ are independent siblings with no
    shared pricing file (D-15), prose-enforced (no analyzer this phase per D-08), with an
    explicit link to `eval/EVAL.md`; (d) a flagged note about the `make bench` NAME collision:
    the existing `make bench` (Makefile line 84) runs the Phase 64 microbench at `./test/bench/...`,
    while future Phase 77 BENCH-05 wants `make bench` for `cmd/helix-bench run` — Phase 77 must
    resolve this (rename one), and Phase 75 only documents it (RESEARCH Pitfall 1; settled as
    "deferred to Phase 77" in RESEARCH §"Open Questions (RESOLVED)" Q1).

    Author `bench/PROVIDERS.md` (INFRA-01) using a per-provider section, each opening with a
    `---`-delimited YAML frontmatter block carrying EXACTLY these keys (D-14):
    `provider`, `tos_url`, `attested_by`, `attested_on` (YYYY-MM-DD format `2006-01-02`),
    `benchmarking_permitted` (bool), `publish_permitted` (bool); followed by EVAL.md-style
    human-readable prose notes. Include at minimum the Anthropic provider row with a real
    `tos_url` (reuse the EVAL.md retention sources). Set `attested_on` to today (2026-06-14) so
    Plan 05's `verify-tos` 90-day gate passes on the committed file. State in prose that
    `benchmarking_permitted`/`publish_permitted`/free-text fields are DISPLAY-ONLY attestations,
    never executed (no injection sink). This file is the input `make verify-tos` parses in Plan 05.

    Author `bench/LICENSES.md` (INFRA-02) as a per-dataset license-audit table scaffold with
    columns (dataset, source_url, license, redistribution_clause, sha256_or_pin, verified_on)
    and a header paragraph explaining that each external dataset adapter (Phases 85-88) appends
    its row here; seed it with the internal-ToolBench-Go row marked "internal — repo license".

    Append a reciprocal INFRA-03 pointer PARAGRAPH to `eval/EVAL.md` (do not restructure the
    file): one short paragraph stating that the v1.12 bench stack lives independently under
    `bench/` (see `bench/BENCH.md`), shares no code or pricing file with eval/, and that the
    boundary is prose-enforced per D-08. This paragraph is the ONLY permitted edit to `eval/`
    (BENCH-01's "eval/ byte-identical" clause is narrowed to allow exactly this — see the
    REQUIREMENTS/ROADMAP reconciliation below).

    Apply the THREE graded-criteria reconciliation doc edits (treat these as the same class of
    doc edit as the BENCH-03 v1→v2 fix — they reconcile upstream wording to the LOCKED decisions,
    NOT the reverse):

    (1) REQUIREMENTS.md BENCH-03 (planner note #1): change `versioned at v1` → `versioned at v2`
        to align with D-01 / v1.12-ROADMAP / PROJECT.md. Change ONLY that token.

    (2) REQUIREMENTS.md BENCH-01: change the five-dir enumeration
        `bench/{datasets,runners,languages,evaluators,reports}/` → the six-dir form
        `bench/{datasets,runners,languages,evaluators,reports,schema}/`, marking `schema/` as
        the contract-only dir (D-07); and narrow the `eval/ is byte-identical to pre-milestone
        HEAD` clause to `eval/ source/runtime is byte-identical ... the single permitted change
        being the one-paragraph INFRA-03 reciprocal pointer in eval/EVAL.md (D-08/INFRA-03)`.
        Apply the matching narrowing to the INFRA-03 line (the reciprocal eval/EVAL.md pointer
        is the single permitted change to eval/ under BENCH-01).

    (3) v1.12-ROADMAP.md Phase 75 Success Criterion #1: change the dir enumeration from five to
        six (`datasets, runners, languages, evaluators, reports, schema`, schema/ contract-only,
        D-07); and narrow the `eval/ byte-identical` clause to `eval/ source/runtime code is
        byte-identical; the single permitted change is the one-paragraph INFRA-03 reciprocal
        pointer in eval/EVAL.md (D-08/INFRA-03)`.

    NOTE: edits (1), (2) — BENCH-03 v2, BENCH-01 six-dir + eval-narrowing, INFRA-03 clarification —
    and (3) the ROADMAP SC-1 reconciliation may already be present on disk if a prior planning
    pass applied them; in that case CONFIRM they are present and idempotent rather than
    duplicating. Verify with the acceptance checks below.

    Do NOT create a `vet-noeval2bench` analyzer (D-08 defers it). Do NOT modify the Makefile
    (Plan 05 owns the new targets). Do NOT touch `make bench` itself.
  </action>
  <acceptance_criteria>
    - `find bench -maxdepth 1 -type d | sort` lists exactly: bench, bench/datasets, bench/evaluators, bench/languages, bench/reports, bench/runners, bench/schema (six subdirs)
    - `tree -d -L 2 bench/` (or `find bench -maxdepth 2 -type d`) shows six subdirs: datasets, runners, languages, evaluators, reports, schema
    - `grep -c "schema" bench/BENCH.md` >= 1 AND BENCH.md prose distinguishes runtime (5) vs contract (1) dirs
    - `grep -q "make bench" bench/BENCH.md` (collision flagged) — exit 0
    - `grep -q "eval/EVAL.md" bench/BENCH.md` (INFRA-03 link) — exit 0
    - `grep -q "bench/BENCH.md" eval/EVAL.md` (reciprocal INFRA-03 link) — exit 0
    - `grep -E "^(provider|tos_url|attested_by|attested_on|benchmarking_permitted|publish_permitted):" bench/PROVIDERS.md` returns all six frontmatter keys at least once
    - `grep -c "attested_on" bench/PROVIDERS.md` >= 1 with a YYYY-MM-DD value of 2026-06-14
    - `bench/LICENSES.md` exists and contains a markdown table header row (`grep -q "| dataset" bench/LICENSES.md` or equivalent)
    - REQUIREMENTS BENCH-03 reads v2: `grep -v '^#' .planning/REQUIREMENTS.md | grep -c "versioned at \`v1\`"` == 0 AND `grep -c "versioned at \`v2\`" .planning/REQUIREMENTS.md` >= 1
    - REQUIREMENTS BENCH-01 six-dir: `grep -c "datasets,runners,languages,evaluators,reports,schema" .planning/REQUIREMENTS.md` >= 1
    - REQUIREMENTS BENCH-01/INFRA-03 eval narrowing: `grep -c "single permitted change" .planning/REQUIREMENTS.md` >= 1
    - ROADMAP SC-1 six-dir: `grep -c "datasets,runners,languages,evaluators,reports,schema" .planning/milestones/v1.12-ROADMAP.md` >= 1
    - ROADMAP SC-1 eval narrowing: `grep -c "single permitted change" .planning/milestones/v1.12-ROADMAP.md` >= 1
    - `go vet ./...` exits 0 (no Go added, but confirms tree did not break build)
  </acceptance_criteria>
  <verify>
    <automated>test "$(find bench -maxdepth 1 -mindepth 1 -type d | wc -l)" -eq 6 && grep -q "eval/EVAL.md" bench/BENCH.md && grep -q "bench/BENCH.md" eval/EVAL.md && grep -Eq "^benchmarking_permitted:" bench/PROVIDERS.md && grep -q "datasets,runners,languages,evaluators,reports,schema" .planning/REQUIREMENTS.md && grep -q "datasets,runners,languages,evaluators,reports,schema" .planning/milestones/v1.12-ROADMAP.md && grep -q "single permitted change" .planning/REQUIREMENTS.md && grep -q "single permitted change" .planning/milestones/v1.12-ROADMAP.md && go vet ./...</automated>
  </verify>
  <done>
    Six bench dirs exist (five runtime + schema contract); BENCH.md documents the distinction,
    operator prereqs, INFRA-03 note, and the make-bench collision; PROVIDERS.md has parseable
    per-provider frontmatter with all six D-14 keys and a fresh attested_on; LICENSES.md has the
    per-dataset scaffold; EVAL.md has the reciprocal link; REQUIREMENTS.md BENCH-03 reads v2,
    BENCH-01 enumerates six dirs with the eval-byte-identical clause narrowed, INFRA-03 clarifies
    the single permitted eval edit; ROADMAP SC-1 mirrors the six-dir + eval-narrowing reconciliation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    bench/PROVIDERS.md provider TOS attestation rows — specifically the `tos_url`,
    `benchmarking_permitted`, and `publish_permitted` values for each provider. These are
    legal attestations that a maintainer must confirm against the live provider Terms of
    Service (VALIDATION.md Manual-Only Verification row; this cannot be machine-verified —
    `verify-tos` only checks the date is fresh and the frontmatter parses, not that the flags
    are factually correct).
  </what-built>
  <how-to-verify>
    1. Open `bench/PROVIDERS.md`.
    2. For each provider's frontmatter block, visit the `tos_url` and confirm the live TOS
       actually permits benchmarking (`benchmarking_permitted`) and publishing results
       (`publish_permitted`) as stated.
    3. Confirm `attested_by` names the maintainer making the attestation and `attested_on` is
       today (2026-06-14).
    4. If any flag is wrong, correct it and re-confirm.
  </how-to-verify>
  <resume-signal>Type "approved" once the TOS flags are confirmed against live provider terms, or describe corrections needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed doc → CI validator | `bench/PROVIDERS.md` frontmatter is parsed by `make verify-tos` (Plan 05). The file is repo-committed (trusted authorship), but a malformed/expired attestation must hard-fail rather than crash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03 | Repudiation / compliance | stale or false TOS attestation | mitigate | Fresh `attested_on` (2026-06-14) so the 90-day gate passes; blocking human checkpoint confirms flags against live TOS (only humans can verify legal terms). |
| T-75-04 | Spoofing (display-only text) | `benchmarking_permitted`/free-text frontmatter | accept | These fields are display/attestation only and are NEVER `exec`'d or used as a shell/command sink (documented in PROVIDERS.md prose). No injection surface. |
| T-75-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; docs + dirs only. |

No high-severity threats: this plan creates directories and prose docs. The TOS-accuracy risk
(a correctness/compliance threat, not a code-execution threat) is mitigated by the fresh-date
machine gate (Plan 05) plus the human checkpoint here.
</threat_model>

<verification>
- `find bench -maxdepth 1 -mindepth 1 -type d | wc -l` equals 6.
- PROVIDERS.md frontmatter exposes all six D-14 keys with a fresh `attested_on`.
- Reciprocal INFRA-03 links present in both BENCH.md and EVAL.md.
- REQUIREMENTS.md BENCH-03 reads `v2`; BENCH-01 enumerates six dirs; BENCH-01/INFRA-03 narrow the eval-byte-identical clause.
- ROADMAP SC-1 enumerates six dirs and narrows the eval-byte-identical clause.
- `go vet ./...` exits 0.
- Human checkpoint: maintainer confirms TOS flags against live provider terms.
</verification>

<success_criteria>
The canonical six-dir `bench/` layout exists; BENCH.md, PROVIDERS.md, and LICENSES.md render
with the required content (runtime-vs-contract distinction, operator prereqs, INFRA-03 note,
make-bench collision flag, parseable per-provider frontmatter, license scaffold); eval/EVAL.md
carries the reciprocal INFRA-03 link; REQUIREMENTS.md BENCH-03 is corrected to v2 and BENCH-01/
INFRA-03 are reconciled to six dirs + the narrowed eval clause; ROADMAP SC-1 mirrors the same
reconciliation. This satisfies v1.12-ROADMAP Phase 75 success criteria #1 (layout) and #5 (docs
render) prose portions against an achievable, decision-aligned spec.
</success_criteria>

<output>
Create `.planning/phases/75-schema-fairness-contract-tree-skeleton/75-02-SUMMARY.md` when done.
</output>
