---
phase: 75-schema-fairness-contract-tree-skeleton
plan: 05
type: tdd
wave: 2
depends_on: ["75-01", "75-02"]
files_modified:
  - cmd/helix-bench/main.go
  - cmd/helix-bench/main_test.go
  - cmd/helix-bench/validate_cost_table.go
  - cmd/helix-bench/verify_tos.go
  - cmd/helix-bench/validate_test.go
  - bench/datasets/cost-table.yaml
  - bench/datasets/testdata/cost-table-stale.yaml
  - bench/PROVIDERS-stale.testdata.md
  - Makefile
autonomous: true
requirements: [BENCH-02, BENCH-06, COST-01, FAIR-02]
must_haves:
  truths:
    - "helix-bench --help lists exactly 5 subcommands: run, fetch-datasets, doctor, report, validate-cost-table"
    - "helix-bench doctor exits 0 on a clean Linux host"
    - "make validate-cost-table exits non-zero on a past valid_until, >90d last_verified, or unparseable row; exits 0 on the good committed cost-table"
    - "make verify-tos exits non-zero on a missing/expired (>90d) attestation or malformed frontmatter; exits 0 on the good committed PROVIDERS.md"
    - "cost-table.yaml carries the full pricing+staleness field set (D-13) with the cached_input column"
  artifacts:
    - path: "cmd/helix-bench/main.go"
      provides: "cobra root + 5 subcommands; newRootCmd exported for testing"
      contains: "newRootCmd"
    - path: "bench/datasets/cost-table.yaml"
      provides: "Pricing + staleness rows (D-13)"
      contains: "deprecation_at"
    - path: "cmd/helix-bench/validate_cost_table.go"
      provides: "Strict-decode hard-fail cost-table validator (injected clock)"
      exports: ["newValidateCostTableCmd"]
    - path: "cmd/helix-bench/verify_tos.go"
      provides: "PROVIDERS.md frontmatter hard-fail TOS staleness gate (injected clock)"
  key_links:
    - from: "Makefile"
      to: "cmd/helix-bench"
      via: "go run ./cmd/helix-bench validate-cost-table / verify-tos (no continue-on-error)"
      pattern: "go run ./cmd/helix-bench"
    - from: "cmd/helix-bench/verify_tos.go"
      to: "bench/PROVIDERS.md"
      via: "yaml.v3 strict frontmatter decode"
      pattern: "PROVIDERS\\.md"
---

<objective>
Wave 2 of Phase 75: build the `cmd/helix-bench` cobra CLI skeleton (BENCH-02), author the
`bench/datasets/cost-table.yaml` pricing+staleness contract (COST-01/D-13), and implement the
two HARD-FAIL validators wired into the Makefile — `make validate-cost-table` and
`make verify-tos` (D-16/BENCH-06). Both validators use strict `yaml.v3` decode
(`KnownFields(true)`) and an INJECTED clock, and both exit NON-ZERO on stale/expired/malformed
input (the deliberate divergence from the warn-only `eval-attestation-check` template).

This plan is Wave 2 because `verify-tos` parses `bench/PROVIDERS.md`, which Plan 02 authors;
it also follows Wave 0 (Plan 01) which cleared `bench/`. The FAIR-02 deprecation-calendar field
(`deprecation_at`) lives on each cost-table row here and is the data the Plan 04 gate consumes.

Purpose: the cost/TOS contract becomes self-enforcing in CI; the CLI entrypoint exists for
every downstream phase to hang subcommands on.
Output: CLI skeleton, cost-table data, two hard-fail validators, two Makefile targets, fixtures.

EXECUTOR NOTE (scope_sanity): this plan is 2 tasks across 9 files (within thresholds) but Task 2
is context-heavy — it bundles BOTH the cost-table validator and the TOS validator (two TDD
validators) plus the Makefile targets in one RED→GREEN→REFACTOR cycle. Watch the context budget
on Task 2; if it approaches ~50%, finish and commit the cost-table validator (GREEN) before
starting the TOS validator rather than carrying both unfinished. No split is required.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-schema-fairness-contract-tree-skeleton/75-CONTEXT.md
@.planning/phases/75-schema-fairness-contract-tree-skeleton/75-RESEARCH.md
@.planning/phases/75-schema-fairness-contract-tree-skeleton/75-PATTERNS.md
@cmd/helix-eval/main.go
@cmd/helix-eval/main_test.go
@cmd/eval-attestation-check/main.go
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 05 contribution)

New file paths + Go symbols (package `main` under `cmd/helix-bench/`):
- `cmd/helix-bench/main.go`: `func main()`, `func newRootCmd() *cobra.Command` (Use:
  "helix-bench"), and subcommand factories `newRunCmd`, `newFetchDatasetsCmd`, `newDoctorCmd`,
  `newReportCmd`, `newValidateCostTableCmd`. Skeleton subcommands (run/fetch-datasets/report)
  return a clear "not yet implemented (Phase NN)" error via RunE; `doctor` exits 0 on a clean host.
- `cmd/helix-bench/validate_cost_table.go`: `newValidateCostTableCmd`, `validateCostTable(today time.Time, path string) error`,
  cost-table row type `CostRow{Provider, ModelID, InputPerMtok, OutputPerMtok, CachedInputPerMtok, Currency, ValidUntil, LastVerified, DeprecationAt}` + `CostTable{Rows []CostRow}` (yaml tags), strict decode.
- `cmd/helix-bench/verify_tos.go`: `newVerifyTOSCmd` (or a verify-tos entry), `verifyTOS(today time.Time, path string) error`,
  `ProviderAttestation{Provider, TOSURL, AttestedBy, AttestedOn, BenchmarkingPermitted, PublishPermitted}` (yaml tags), strict frontmatter decode.
- `cmd/helix-bench/main_test.go`: `TestHelixBenchHelpListsFiveSubcommands`, `TestHelixBenchDoctorExitsZero`.
- `cmd/helix-bench/validate_test.go`: `TestValidateCostTableGoodPasses`, `TestValidateCostTablePastValidUntilFails`, `TestValidateCostTableStaleLastVerifiedFails`, `TestValidateCostTableMalformedRowFails`, `TestVerifyTOSGoodPasses`, `TestVerifyTOSStaleAttestationFails`, `TestVerifyTOSMalformedFrontmatterFails`.

New data/fixture files:
- `bench/datasets/cost-table.yaml` — COST-01/D-13 rows (the good, committed table)
- `bench/datasets/testdata/cost-table-stale.yaml` — fixture with a past valid_until (for the fail test)
- `bench/PROVIDERS-stale.testdata.md` — fixture with a >90d attested_on (for the fail test)

New Makefile targets: `validate-cost-table`, `verify-tos` (both shell `go run ./cmd/helix-bench ...`,
NO continue-on-error), added to the `.PHONY` line.

Note: `verify-tos` may be implemented either as a `helix-bench` subcommand or invoked via the
`validate-cost-table` sibling; BENCH-02's five-subcommand list is run/fetch-datasets/doctor/report/
validate-cost-table — `verify-tos` is a Makefile target, not necessarily a 6th subcommand. Keep
the subcommand count at exactly 5 for the --help acceptance.
</artifacts_produced>

<feature>
  <name>helix-bench CLI skeleton + cost-table.yaml + two hard-fail validators (validate-cost-table, verify-tos)</name>
  <files>cmd/helix-bench/main.go, cmd/helix-bench/main_test.go, cmd/helix-bench/validate_cost_table.go, cmd/helix-bench/verify_tos.go, cmd/helix-bench/validate_test.go, bench/datasets/cost-table.yaml, bench/datasets/testdata/cost-table-stale.yaml, bench/PROVIDERS-stale.testdata.md, Makefile</files>
  <read_first>
    - cmd/helix-eval/main.go (EXACT cobra skeleton analog: main() lines 31-36, newRootCmd lines 38-56, subcommand factory lines 59-96, RunE error-propagation lines 245-252)
    - cmd/helix-eval/main_test.go (EXACT --help in-process capture analog: SetOut/SetErr/SetArgs lines 16-31)
    - cmd/eval-attestation-check/main.go (date-staleness + testable run() idiom lines 26-89 — INVERT warn-only to HARD-FAIL; threshold 90 days here, not 180; per project rule the warn-only continue-on-error wiring must NOT be copied)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-RESEARCH.md (Pattern 4 cobra lines 275-298; Pattern 5 YAML frontmatter strict decode lines 300-319; Code Examples strict cost-table decode lines 467-489; Pitfall 3 warn-vs-hard-fail divergence)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-CONTEXT.md (D-13 cost-table field set, D-14 PROVIDERS frontmatter field set, D-16 hard-fail semantics, FAIR-02 deprecation_at)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-PATTERNS.md (helix-bench main.go/main_test.go exact analogs lines 32-94; verify-tos/validate INVERT-exit-policy lines 169-198; cost-table.yaml no-analog use RESEARCH struct lines 226-230)
    - bench/PROVIDERS.md (the real file authored by Plan 02 — verify-tos parses its frontmatter; must already exist before this plan runs)
    - Makefile (lines 1 .PHONY; lines 233-238 eval-attestation-check target shape — copy the `go run ./cmd/...` form WITHOUT continue-on-error)
  </read_first>
  <behavior>
    CLI (standard, glue): `TestHelixBenchHelpListsFiveSubcommands` — `newRootCmd()` with
    SetArgs(["--help"]) Execute() returns nil and output Contains each of run, fetch-datasets,
    doctor, report, validate-cost-table (and `len(root.Commands()) == 5`).
    `TestHelixBenchDoctorExitsZero` — running the `doctor` subcommand returns nil on a clean host.

    Validators (TDD, defined I/O):
    - `TestValidateCostTableGoodPasses`: `validateCostTable(fixedRecentToday, "bench/datasets/cost-table.yaml")` → nil.
    - `TestValidateCostTablePastValidUntilFails`: a row with `valid_until` before injected today → non-nil.
    - `TestValidateCostTableStaleLastVerifiedFails`: a row with `last_verified` > 90 days before injected today → non-nil.
    - `TestValidateCostTableMalformedRowFails`: an unknown/extra YAML key (strict KnownFields(true)) → non-nil.
    - `TestVerifyTOSGoodPasses`: `verifyTOS(fixedRecentToday, "bench/PROVIDERS.md")` → nil (Plan 02 set attested_on fresh).
    - `TestVerifyTOSStaleAttestationFails`: a frontmatter with `attested_on` > 90 days before injected today → non-nil.
    - `TestVerifyTOSMalformedFrontmatterFails`: an unknown key / unparseable frontmatter → non-nil.

    Both validators take an INJECTED `today time.Time` (never time.Now() inside) so tests are
    deterministic and runs reproduce from --run-id; the cobra RunE passes time.Now().UTC().
    Both return errors via RunE so the process exits non-zero (D-16) — NO os.Exit deep inside,
    NO continue-on-error.
  </behavior>
  <implementation>
    Mirror `cmd/helix-eval/main.go` for the cobra tree: `main()` is the only os.Exit site;
    every subcommand uses `RunE`. Skeleton subcommands run/fetch-datasets/report return a clear
    "not yet implemented (Phase NN)" error; `doctor` checks documented prereqs and returns nil
    on a clean host (success criterion #2).

    `validate_cost_table.go`: define the `CostRow`/`CostTable` types with yaml tags per D-13
    (provider, model_id, input_per_mtok, output_per_mtok, cached_input_per_mtok, currency,
    valid_until, last_verified, deprecation_at). Decode with `yaml.NewDecoder(f)` +
    `dec.KnownFields(true)` (strict — unknown key hard-fails, D-16). For each row parse the three
    dates with layout `2006-01-02`; error if `valid_until < today`, `(today - last_verified) > 90d`,
    or any date unparseable. `validateCostTable(today, path)` returns the first error.

    `verify_tos.go`: split each provider's `---`-delimited YAML frontmatter block from
    `bench/PROVIDERS.md`, decode with `yaml.v3` + `KnownFields(true)` into `ProviderAttestation`
    (D-14 fields). Error if any block is missing required keys, unparseable, or `attested_on` is
    > 90 days before injected today. `verifyTOS(today, path)` returns the first error.

    Author `bench/datasets/cost-table.yaml` (the committed good table, COST-01/D-13) with at least
    the Anthropic dated-snapshot row matching the Plan 04 ModelID, fresh `valid_until` (future),
    `last_verified` today (2026-06-14), `deprecation_at` well beyond 30 days, and the
    `cached_input_per_mtok` column. Author the two stale fixtures for the fail tests.

    Makefile: add `validate-cost-table` and `verify-tos` targets each shelling
    `go run ./cmd/helix-bench validate-cost-table` / the verify-tos entry, WITHOUT
    continue-on-error; add both names to the `.PHONY` line (Makefile line 1).
  </implementation>
</feature>

<tasks>

<task type="execute">
  <name>Task 1: cobra CLI skeleton (5 subcommands, doctor exits 0) + cost-table.yaml data</name>
  <read_first>
    - cmd/helix-eval/main.go (cobra tree lines 31-96; RunE exit contract lines 245-252)
    - cmd/helix-eval/main_test.go (--help in-process capture lines 16-31)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-RESEARCH.md (Pattern 4 lines 275-298; Code Examples strict cost-table decode struct lines 467-489)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-CONTEXT.md (D-13 cost-table field set, BENCH-02 subcommand list)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-PATTERNS.md (lines 32-94)
  </read_first>
  <action>
    Author `cmd/helix-bench/main.go` mirroring `cmd/helix-eval/main.go`: `main()` calls
    `newRootCmd().Execute()` and `os.Exit(1)` on error; `newRootCmd()` (exported for testing)
    builds `&cobra.Command{Use: "helix-bench", Short: ...}` and AddCommands the FIVE BENCH-02
    subcommands via factories `newRunCmd`, `newFetchDatasetsCmd`, `newDoctorCmd`, `newReportCmd`,
    `newValidateCostTableCmd`. run/fetch-datasets/report return a clear "not yet implemented
    (Phase NN)" error via RunE; `doctor` checks documented prereqs and returns nil on a clean
    Linux host (success criterion #2). All subcommands use `RunE` (never `Run` — errors must
    propagate to the single os.Exit). `newValidateCostTableCmd` may be a thin stub in this task
    and is wired to the real validator in Task 2 (same file or validate_cost_table.go).

    Author `cmd/helix-bench/main_test.go` mirroring helix-eval: `TestHelixBenchHelpListsFiveSubcommands`
    uses SetOut/SetErr/SetArgs(["--help"]) + Execute(), asserts Contains for each of the five
    subcommand names and `len(newRootCmd().Commands()) == 5`; `TestHelixBenchDoctorExitsZero` runs
    the doctor subcommand and asserts nil.

    Author `bench/datasets/cost-table.yaml` (COST-01/D-13) with a top-level `rows:` list; include
    at minimum the Anthropic dated-snapshot row whose `model_id` matches Plan 04's
    DefaultContract.ModelID, with fields: provider, model_id, input_per_mtok, output_per_mtok,
    cached_input_per_mtok, currency, valid_until (a FUTURE date), last_verified (2026-06-14),
    deprecation_at (a date > 30 days out). All dates in `2006-01-02` format.

    Confirm `go build ./cmd/helix-bench` succeeds (BENCH-02 build acceptance).
  </action>
  <acceptance_criteria>
    - `go build ./cmd/helix-bench` exits 0
    - `go test ./cmd/helix-bench/...` exits 0 (help + doctor tests pass)
    - `go run ./cmd/helix-bench --help` output contains all five: run, fetch-datasets, doctor, report, validate-cost-table
    - `grep -q "Use: *\"helix-bench\"" cmd/helix-bench/main.go` — exit 0
    - `grep -q "func newRootCmd()" cmd/helix-bench/main.go` — exit 0
    - `go run ./cmd/helix-bench doctor` exits 0
    - `grep -E "^(provider|model_id|input_per_mtok|output_per_mtok|cached_input_per_mtok|currency|valid_until|last_verified|deprecation_at):" bench/datasets/cost-table.yaml | sort -u | wc -l` == 9 (all D-13 fields present)
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go build ./cmd/helix-bench && go test ./cmd/helix-bench/... && go run ./cmd/helix-bench doctor && go run ./cmd/helix-bench --help 2>&1 | grep -q validate-cost-table && go vet ./...</automated>
  </verify>
  <done>
    helix-bench builds, --help lists exactly the five BENCH-02 subcommands, doctor exits 0, and
    bench/datasets/cost-table.yaml carries the full D-13 pricing+staleness field set including
    the cached_input column; go vet green.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED→GREEN→REFACTOR — hard-fail validate-cost-table + verify-tos (injected clock) + Makefile targets</name>
  <files>cmd/helix-bench/validate_cost_table.go, cmd/helix-bench/verify_tos.go, cmd/helix-bench/validate_test.go, bench/datasets/testdata/cost-table-stale.yaml, bench/PROVIDERS-stale.testdata.md, Makefile</files>
  <read_first>
    - cmd/eval-attestation-check/main.go (date-staleness testable run() idiom lines 26-89 — INVERT to hard error, 90d threshold; do NOT copy continue-on-error)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-RESEARCH.md (Pattern 5 frontmatter strict decode lines 300-319; Code Examples strict cost-table decode lines 467-489; Pitfall 3 warn-vs-hard-fail)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-CONTEXT.md (D-13, D-14, D-16 hard-fail, FAIR-02 deprecation_at)
    - .planning/phases/75-schema-fairness-contract-tree-skeleton/75-PATTERNS.md (INVERT-exit-policy lines 169-198; Makefile target shape lines 193-198)
    - bench/PROVIDERS.md (Plan 02 output — verify-tos parses its frontmatter; must exist)
    - bench/datasets/cost-table.yaml (Task 1 output — validate-cost-table parses it)
    - Makefile (line 1 .PHONY; lines 233-238 target shape)
  </read_first>
  <behavior>
    - TestValidateCostTableGoodPasses: validateCostTable(recent injected today, bench/datasets/cost-table.yaml) → nil
    - TestValidateCostTablePastValidUntilFails: stale fixture row valid_until < today → non-nil error
    - TestValidateCostTableStaleLastVerifiedFails: last_verified > 90d before today → non-nil error
    - TestValidateCostTableMalformedRowFails: unknown YAML key (KnownFields(true)) → non-nil error
    - TestVerifyTOSGoodPasses: verifyTOS(recent injected today, bench/PROVIDERS.md) → nil
    - TestVerifyTOSStaleAttestationFails: stale-fixture attested_on > 90d before today → non-nil error
    - TestVerifyTOSMalformedFrontmatterFails: unparseable/unknown-key frontmatter → non-nil error
  </behavior>
  <action>
    EXECUTOR CONTEXT-BUDGET NOTE (scope_sanity): this task bundles TWO TDD validators
    (cost-table + TOS) plus the Makefile targets. If context approaches ~50% before both
    validators are GREEN, commit the cost-table validator (feat) first, then proceed to the TOS
    validator as a second GREEN+commit, rather than carrying both half-implemented. No split of
    the plan is needed; this is purely an execution-pacing reminder.

    RED: write `cmd/helix-bench/validate_test.go` with the seven behaviors above plus the two
    stale fixtures (`bench/datasets/testdata/cost-table-stale.yaml` with a past valid_until and a
    >90d last_verified; `bench/PROVIDERS-stale.testdata.md` with a >90d attested_on). Run
    `go test ./cmd/helix-bench/...` and confirm it FAILS (validator funcs absent) — commit
    `test(75-05): add failing cost-table + TOS hard-fail validator tests`.

    GREEN: implement `cmd/helix-bench/validate_cost_table.go` — define `CostRow`/`CostTable`
    (D-13 yaml tags), decode with `yaml.NewDecoder(f)` + `dec.KnownFields(true)` (strict),
    `validateCostTable(today time.Time, path string) error` parsing each row's three dates
    (layout `2006-01-02`) and erroring on `valid_until < today`, `today - last_verified > 90d`,
    unparseable date, or strict-decode failure (D-16). Wire `newValidateCostTableCmd` RunE to
    call it with `time.Now().UTC()` and return the error (process exits non-zero — NO os.Exit
    deep inside). Implement `cmd/helix-bench/verify_tos.go` — split `---`-delimited frontmatter
    blocks from PROVIDERS.md, strict-decode each into `ProviderAttestation` (D-14 fields),
    `verifyTOS(today time.Time, path string) error` erroring on missing/unparseable frontmatter
    or `attested_on` > 90d before today. Run `go test ./cmd/helix-bench/...`; all seven pass —
    commit `feat(75-05): hard-fail cost-table and TOS validators`.

    Add Makefile targets (mirror the `go run ./cmd/...` form at Makefile:237-238 but WITHOUT
    continue-on-error): `validate-cost-table:` shells `go run ./cmd/helix-bench validate-cost-table`
    (defaulting to bench/datasets/cost-table.yaml); `verify-tos:` shells the verify-tos entry
    against bench/PROVIDERS.md. Append both target names to the `.PHONY` line (Makefile line 1).

    REFACTOR if changes made: extract a shared date-layout const and a 90-day window const; tidy
    error messages — keep all tests green (commit `refactor(75-05): share date-window constants`).
    End with `go vet ./...` and `go test ./...`.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/helix-bench/...` exits 0 with all seven validator tests passing
    - `make validate-cost-table` exits 0 against the committed good cost-table.yaml
    - `make verify-tos` exits 0 against the committed good PROVIDERS.md
    - `go run ./cmd/helix-bench validate-cost-table bench/datasets/testdata/cost-table-stale.yaml` exits NON-ZERO (past valid_until / stale last_verified)
    - `grep -q "KnownFields(true)" cmd/helix-bench/validate_cost_table.go` AND `grep -q "KnownFields(true)" cmd/helix-bench/verify_tos.go` (strict decode, D-16)
    - `grep -q "func validateCostTable(today time.Time" cmd/helix-bench/validate_cost_table.go` (injected clock)
    - `grep -q "func verifyTOS(today time.Time" cmd/helix-bench/verify_tos.go` (injected clock)
    - `grep -E "^(validate-cost-table|verify-tos):" Makefile | wc -l` == 2 (both targets exist)
    - `grep -c "continue-on-error" Makefile` for these two targets is 0 (hard-fail, not warn-only) — verify the two new targets carry no continue-on-error
    - `grep "^.PHONY:" Makefile | grep -q validate-cost-table` AND `grep "^.PHONY:" Makefile | grep -q verify-tos`
    - `git log --oneline cmd/helix-bench/` shows a test-first commit preceding the validator-implementation commit
    - `go vet ./...` exits 0 AND `go test ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/helix-bench/... && make validate-cost-table && make verify-tos && ! go run ./cmd/helix-bench validate-cost-table bench/datasets/testdata/cost-table-stale.yaml && grep -q "func validateCostTable(today time.Time" cmd/helix-bench/validate_cost_table.go && grep -q "func verifyTOS(today time.Time" cmd/helix-bench/verify_tos.go && go vet ./...</automated>
  </verify>
  <done>
    make validate-cost-table and make verify-tos both exit 0 on the good committed files and
    NON-ZERO on the stale fixtures (past valid_until, >90d last_verified/attested_on, malformed
    rows), using strict yaml.v3 decode and an injected clock; both Makefile targets carry no
    continue-on-error and are in .PHONY; RED preceded GREEN in git history; full Go suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed YAML/markdown → CI validator | `bench/datasets/cost-table.yaml` and `bench/PROVIDERS.md` frontmatter are parsed by the two validators. Files are repo-committed (trusted authorship), but malformed/expired input must hard-fail CI rather than crash or silently pass. |
| CLI args → validator | `helix-bench validate-cost-table <path>` accepts a path argument; only used as a file read, never as a shell sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-10 | DoS / Tampering | malformed/oversized cost-table or PROVIDERS frontmatter | mitigate | Strict `yaml.v3` decode (`KnownFields(true)`) returns an error on unknown/malformed keys rather than panicking (ASVS V5); files are repo-committed, not untrusted external input. |
| T-75-11 | Repudiation / compliance | stale TOS attestation or expired pricing slipping through | mitigate | D-16 HARD-FAIL gates: `verify-tos` (90d attested_on) and `validate-cost-table` (past valid_until / >90d last_verified) exit non-zero in CI; NO continue-on-error (Pitfall 3). |
| T-75-12 | Tampering | warn-only template copied verbatim (silent pass) | mitigate | Tests assert the stale fixture exits NON-ZERO; acceptance checks confirm no continue-on-error on the new targets. |
| T-75-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; `spf13/cobra` and `gopkg.in/yaml.v3` already vendored (RESEARCH Package Legitimacy Audit). |

No high-severity threats: this is committed-config validation with offline strict decode. The
correctness/compliance risk (a wrong price or stale TOS) is mitigated by the hard-fail gates and
their non-zero-exit tests. No auth/session/network surface is introduced.
</threat_model>

<verification>
- `go build ./cmd/helix-bench` exits 0; `go test ./cmd/helix-bench/...` exits 0.
- `helix-bench --help` lists exactly the five BENCH-02 subcommands; `helix-bench doctor` exits 0.
- `make validate-cost-table` and `make verify-tos` exit 0 on good files, non-zero on stale fixtures.
- Strict decode (`KnownFields(true)`) and injected clocks in both validators; no continue-on-error.
- RED-before-GREEN git ordering on the validator commits.
- `go vet ./...` and `go test ./...` exit 0.
</verification>

<success_criteria>
`helix-bench --help` lists the five subcommands and `helix-bench doctor` succeeds on a clean
Linux host; `make verify-tos` and `make validate-cost-table` exit non-zero when an attestation
is older than 90 days or `cost-table.yaml` `valid_until` is past, and exit 0 on the good
committed files (v1.12-ROADMAP Phase 75 success criteria #2 and #5). The cost-table carries the
full D-13 pricing+staleness set feeding the FAIR-02 deprecation gate.
</success_criteria>

<output>
Create `.planning/phases/75-schema-fairness-contract-tree-skeleton/75-05-SUMMARY.md` when done.
</output>
