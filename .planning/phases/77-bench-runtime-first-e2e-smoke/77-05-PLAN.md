---
phase: 77-bench-runtime-first-e2e-smoke
plan: 05
type: execute
wave: 5
depends_on: ["77-04"]
files_modified:
  - Makefile
  - bench/BENCH.md
autonomous: false
requirements: [BENCH-05]
must_haves:
  truths:
    - "The pre-existing `bench:` Go-microbenchmark target is reconciled (renamed to bench-micro) so `make bench` invokes cmd/helix-bench run (BENCH-05, Pitfall 1)"
    - "`make bench-quick` builds helix first, runs the hermetic scripted your_agent_full smoke on the seed task, exits 0 with >=1 task succeeding in <=90s (criterion #2)"
    - "`make bench` and `make bench-<suite>` invoke `cmd/helix-bench run --benchmarks=…` (BENCH-05)"
    - "The single-task smoke `helix-bench run --benchmarks=toolbench-go --modes=your_agent_full --tasks=<one>` completes <=30s and writes a schema-valid result.v2.json (criterion #1)"
    - "bench/BENCH.md documents the chosen result.v2 provenance key names (outcome, trace_ref, model_id) so Phase 79 does not rename them (Open Question 3)"
  artifacts:
    - path: "Makefile"
      provides: "bench (reconciled), bench-quick, bench-<suite> targets + updated .PHONY"
      contains: "bench-quick"
    - path: "bench/BENCH.md"
      provides: "result.v2 provenance key-name contract + make target docs"
      contains: "trace_ref"
  key_links:
    - from: "Makefile bench-quick target"
      to: "cmd/helix-bench run"
      via: "go run/built helix-bench invocation after go build ./cmd/helix"
      pattern: "helix-bench run"
    - from: "Makefile bench target"
      to: "cmd/helix-bench run --benchmarks"
      via: "full-run invocation (reconciled name)"
      pattern: "helix-bench run --benchmarks"
---

<objective>
Close the phase: reconcile the `make bench` target-name COLLISION (Pitfall 1 — an existing `bench:`
microbench target at Makefile:95 shadows BENCH-05's requirement), add the `bench`/`bench-quick`/
`bench-<suite>` targets that invoke `cmd/helix-bench run`, prove the two timed smoke gates
(single-task <=30s + `make bench-quick` <=90s), and document the chosen result.v2 provenance key
names in `bench/BENCH.md` so Phase 79 consumes a stable contract.

Purpose: This is the BENCH-05 deliverable and the phase gate. It is Makefile/process orchestration
+ a human-verified timed smoke, so standard `type: execute` plus a final human-verify checkpoint
(the timed CI-budget smoke is the one thing best confirmed by an operator running it). The plan is
`autonomous: false` because of that checkpoint.

Output: reconciled + extended root `Makefile` and the BENCH.md key-name contract.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 05 slice)
- **Modified root `Makefile`** — the existing `bench:` microbench target RENAMED to `bench-micro`
  (it is local/gitignored per Makefile:98 sibling `bench-baseline`); a NEW `bench:` target invoking
  `go run ./cmd/helix-bench run --benchmarks=...`; a NEW `bench-quick:` target that `go build
  ./cmd/helix` FIRST then runs the hermetic scripted `your_agent_full` smoke on the seed task
  (<=90s); a parameterized `bench-<suite>` pattern target; `.PHONY` updated (drop `bench` collision,
  add `bench-micro`/`bench-quick`).
- **Modified `bench/BENCH.md`** — documents the chosen result.v2 provenance key names (`outcome`,
  `trace_ref`, `model_id`) as the stable contract Phase 79 reads (Open Question 3), plus the new make
  target semantics and the `bench`->`bench-micro` rename note.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-bench-runtime-first-e2e-smoke/77-CONTEXT.md
@.planning/phases/77-bench-runtime-first-e2e-smoke/77-RESEARCH.md
@.planning/phases/77-bench-runtime-first-e2e-smoke/77-PATTERNS.md
@.planning/phases/77-bench-runtime-first-e2e-smoke/77-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile `make bench` collision + add bench-quick/bench-<suite> + BENCH.md key-names (BENCH-05, Pitfall 1, Open Q3)</name>
  <files>Makefile, bench/BENCH.md</files>
  <read_first>
    - `Makefile:1` `.PHONY` line (lists `bench bench-baseline`) and `:95` the existing `bench:` target (`go test -short -bench=. ... ./test/bench/...`) + `:98` `bench-baseline:` (gitignored/local sibling) — the collision source (Pitfall 1).
    - `Makefile:230` `eval-quick:` (`go run ./cmd/helix-eval run --quick ...`) and `:240` `eval:` — the wiring template for the new bench targets.
    - `cmd/helix-bench/main.go` (Plan 04) — the implemented `run` subcommand + flag set the targets invoke.
    - `77-RESEARCH.md` Pitfall 1 + Open Question 1 (recommended: rename microbench -> `bench-micro`; new full-run `bench:` invokes helix-bench) and Open Question 3 (document `outcome`/`trace_ref`/`model_id`); Environment Availability "Build sequencing note" (`bench-quick` must `go build ./cmd/helix` first or the forwarder-drive path SKIPs).
    - `bench/BENCH.md` (existing, Phase 75) — the operator-side contract file to extend (do not rewrite; append the key-name + make-target section).
  </read_first>
  <action>
    RECONCILE the collision (Pitfall 1 — do NOT silently clobber): RENAME the existing microbench
    target `bench:` -> `bench-micro:` (preserve its recipe `go test -short -bench=. -benchmem
    -count=10 -run=^$$ ./test/bench/...` and its doc comment), update `bench-baseline`'s sibling
    comment if it references `bench`, and update `.PHONY` (line 1): remove the old `bench` meaning,
    add `bench-micro`, `bench-quick` (and the suite pattern if not auto-PHONY). Add a NEW `bench:`
    target that runs `go run ./cmd/helix-bench run --benchmarks=toolbench-go` (the documented full-run
    semantics, BENCH-05). Add `bench-quick:` that FIRST runs `go build ./cmd/helix` (so `helix` is on
    PATH / locatable for the forwarder-drive path — RESEARCH build-sequencing note) THEN runs the
    hermetic scripted smoke: `go run ./cmd/helix-bench run --benchmarks=toolbench-go
    --modes=your_agent_full --agent=scripted --out bench/reports` on the single seed task, sized for
    <=90s (criterion #2). Add a `bench-<suite>` mechanism (a pattern rule like `bench-%:` or
    documented `make bench SUITE=...`) that parameterizes `--benchmarks=<suite>` and invokes the same
    `cmd/helix-bench run`. Mirror eval-quick's local-only / no-network discipline in comments. Then
    APPEND to `bench/BENCH.md` a short section documenting (1) the chosen stable result.v2 provenance
    key names `outcome`, `trace_ref`, `model_id` (Open Question 3 — so Phase 79 reads, never renames),
    and (2) the new make target semantics + the `bench`->`bench-micro` rename (so anyone calling the
    old microbench target is redirected). Do NOT change eval targets or eval/EVAL.md.
  </action>
  <verify>
    <automated>grep -q 'bench-micro:' Makefile && grep -q 'bench-quick:' Makefile && grep -Eq 'bench:.*helix-bench run|^bench:' Makefile && grep -q 'trace_ref' bench/BENCH.md && go vet ./... </automated>
  </verify>
  <acceptance_criteria>
    - `make bench-micro` runs the OLD microbench recipe (`go test -bench`); `make bench` invokes `cmd/helix-bench run --benchmarks=...` (NOT `go test -bench`) — the collision is reconciled, not clobbered.
    - `.PHONY` no longer maps `bench` to the microbench meaning and includes `bench-micro` + `bench-quick`.
    - `bench-quick` runs `go build ./cmd/helix` BEFORE the smoke (build sequencing).
    - A `bench-<suite>` mechanism parameterizes `--benchmarks=<suite>` and invokes `cmd/helix-bench run`.
    - `bench/BENCH.md` documents `outcome`/`trace_ref`/`model_id` and the make-target/rename semantics (grep `trace_ref` matches).
    - `go vet ./...` clean.
  </acceptance_criteria>
  <done>The bench target collision is reconciled (bench-micro); bench/bench-quick/bench-<suite> invoke helix-bench run; BENCH.md pins the result.v2 provenance key names for Phase 79.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Timed E2E smoke gate — single-task <=30s + make bench-quick <=90s (criteria #1, #2)</name>
  <files>(no source files; operator-run verification of Plan 01-05 artifacts)</files>
  <action>
    Operator-run verification gate (no code changes). The executor pauses here and the operator runs
    the timed smokes below to confirm criteria #1 (single-task <=30s, schema-valid result) and #2
    (`make bench-quick` <=90s, >=1 task succeeds), the 2-leg trace + zero PID cross-talk (criterion
    #4), and the `make bench`/`bench-micro` collision reconciliation. See `<how-to-verify>` for the
    exact commands and expected outcomes; resume on the `<resume-signal>`.
  </action>
  <what-built>
    The full bench runtime is wired end-to-end. `make bench-quick` builds `helix`, then runs the
    hermetic scripted `your_agent_full` smoke on the single `sum-doubler` ToolBench Go task: spawns
    one subprocess daemon over a per-cell Unix socket (HTTP off), drives a `replace_in_file` edit
    through the forwarder, kills+PID-gated-taps the daemon, synthesizes the CC leg, merges a 2-leg
    trace, runs `go test` as verify, builds + schema-validates `result.v2.json`, and writes durable
    artifacts under `bench/reports/<run_id>/sum-doubler/your_agent_full/`.
  </what-built>
  <how-to-verify>
    1. From the repo root run: `go build ./cmd/helix` (ensures the daemon binary exists on PATH/CWD).
    2. Time the single-task smoke (criterion #1, <=30s):
       `time go run ./cmd/helix-bench run --benchmarks=toolbench-go --modes=your_agent_full --tasks=sum-doubler --out bench/reports`
       — expect: exit 0 in <=30s; a `result.v2.json` written under
       `bench/reports/<run_id>/sum-doubler/your_agent_full/`.
    3. Confirm the result validates against the schema:
       `go test ./bench/runtime/ -run ResultV2Valid -count=1` (already green) AND open the emitted
       `result.v2.json` to confirm `schema_version:"v2"`, an `outcome` of success, a `fairness` block,
       and a `trace_ref`.
    4. Time the CI gate (criterion #2, <=90s, >=1 task succeeds):
       `time make bench-quick` — expect exit 0 in <=90s.
    5. Confirm the trace is 2-leg with no PID cross-talk and no orphans: open the emitted
       `trace.json` (merged) and confirm `ToolCallSummary.Total >= 1`, at least one `cc`-source event
       (agent-tap leg present), and zero foreign tool names; run
       `go test ./bench/runtime/ -run 'DaemonTap|CrossCell' -count=1` (with `helix` on PATH) to assert
       `RejectedForeignPid == 0`.
    6. Confirm the collision reconciliation: `make bench-micro` still runs the Go microbenchmark
       (`go test -bench`), and `make bench` invokes `cmd/helix-bench run` (NOT `go test -bench`).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms: single-task smoke exit 0 <=30s + schema-valid result; `make bench-quick` exit 0 <=90s; merged trace has Total>=1 + CC leg + RejectedForeignPid==0; `make bench` -> helix-bench run, `make bench-micro` -> go test -bench.</human-check>
  </verify>
  <done>Operator approves: both timed smokes pass within budget producing a schema-valid 2-leg result with zero PID cross-talk, and the make-target collision is reconciled.</done>
  <resume-signal>Type "approved" if both timed smokes pass within budget with a schema-valid 2-leg result, or describe the failure (timing, schema, trace legs, or target collision).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| make targets -> subprocess (go run helix-bench / go build helix) | build + run invocations executed by the operator/CI |
| (inherits all Plan 03/04 boundaries via the smoke run) | the gate exercises the full spawn->drive->tap->merge path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-13 | Tampering | `make bench` silently running the wrong recipe (collision) | mitigate | explicit rename to `bench-micro` + `.PHONY` update; verify recipe identity in acceptance (`make bench` -> helix-bench run, `make bench-micro` -> go test -bench) |
| T-77-14 | DoS | smoke exceeding CI budget masks a hang | mitigate | timed gates (<=30s single task, <=90s bench-quick); a context deadline in `RunCell`/budget watchdog bounds runaway cells |
| T-77-15 | Info disclosure | smoke leaking creds via env into the daemon | accept | scripted gate uses no API key (D-01); env allowlist inherited from StartDaemon (sandbox.go:247) — no `high` threat in the gating path |
| T-77-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new packages this phase (RESEARCH audit: none); Makefile adds no install steps |
</threat_model>

<verification>
- `grep` checks: `bench-micro:`, `bench-quick:` present; `bench:` invokes `cmd/helix-bench run`; `trace_ref` documented in BENCH.md.
- `go vet ./...` and `gofmt -w .` clean.
- Human-verified timed smokes: single-task <=30s (criterion #1) and `make bench-quick` <=90s (criterion #2), both exit 0 with a schema-valid 2-leg result.
</verification>

<success_criteria>
- `make bench` collision reconciled (microbench -> `bench-micro`); `bench`/`bench-quick`/`bench-<suite>` invoke `cmd/helix-bench run` (BENCH-05).
- `make bench-quick` exits 0 with >=1 task succeeding in <=90s (criterion #2); single-task smoke <=30s, schema-valid result (criterion #1).
- `bench/BENCH.md` pins `outcome`/`trace_ref`/`model_id` for Phase 79 (Open Question 3).
</success_criteria>

<output>
Create `.planning/phases/77-bench-runtime-first-e2e-smoke/77-05-SUMMARY.md` when done.
</output>
