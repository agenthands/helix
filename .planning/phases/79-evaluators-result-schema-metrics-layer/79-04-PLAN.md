---
phase: 79
plan: 04
type: tdd
wave: 3
depends_on: ["79-01", "79-02", "79-03"]
files_modified:
  - bench/evaluators/evaluators.go
  - bench/evaluators/evaluators_test.go
  - bench/evaluators/METRICS.md
  - bench/runtime/result.go
  - bench/runtime/result_test.go
  - bench/runtime/cell.go
  - bench/runtime/matrix.go
  - bench/runtime/validate.go
autonomous: true
requirements: [METRIC-01, METRIC-02, METRIC-03, METRIC-04, METRIC-05, METRIC-06]
must_haves:
  truths:
    - "The coordinator runs all 5 graders; a single grader failure nulls only that metric + appends a metric_errors entry; the (task, mode, run_index) row is still emitted and schema-valid (D-07)"
    - "A real cell run produces a metric-complete result.v2.json with all 17 metrics present (nullable, never omitted) (METRIC-01/02)"
    - "BuildResult assembles the Metrics + MetricErrors into result.v2.json and the validate-on-write gate accepts it (METRIC-01)"
    - "The durable path carries a <run_index> segment and RunIndex is no longer hard-coded to 0 (Pitfall 3 / per (task, mode, run_index))"
    - "A pre-patch RunTests snapshot is captured before driveScript so regression_checker has its pre-patch passing set (D-05 / Pitfall 6)"
    - "bench/evaluators/METRICS.md documents the edit_locality and regression_rate definitions (METRIC-04/05 acceptance)"
  artifacts:
    - path: "bench/evaluators/evaluators.go"
      provides: "Coordinator: Grade(ctx, GradeInput) (Metrics, []MetricError) with per-metric failure isolation"
      contains: "MetricError"
    - path: "bench/evaluators/METRICS.md"
      provides: "edit_locality + regression_rate + edit_distance_patch definitions (required output artifact)"
      contains: "edit_locality"
    - path: "bench/runtime/result.go"
      provides: "ResultInput.Metrics + .MetricErrors; resultDoc.Metrics/MetricErrors; assembled + validated in BuildResult"
      contains: "Metrics"
    - path: "bench/runtime/cell.go"
      provides: "<run_index> path segment + pre-patch RunTests snapshot wired into RunCell"
      contains: "RunIndex"
  key_links:
    - from: "bench/evaluators/evaluators.go coordinator"
      to: "the 5 grader packages (test_runner, patch_validator, token_meter, tool_trace_analyzer, regression_checker)"
      via: "fan-out with per-grader error capture → Metrics + []MetricError"
      pattern: "test_runner|patch_validator|token_meter|tool_trace_analyzer|regression_checker"
    - from: "bench/runtime/result.go BuildResult"
      to: "result.v2.schema.json metrics + metric_errors (validate-on-write)"
      via: "resultDoc.Metrics marshaled then Validate()"
      pattern: "Validate"
    - from: "bench/runtime/cell.go RunCell durable path"
      to: "validatePathSegment(run_index)"
      via: "V5 path-traversal guard before filepath.Join"
      pattern: "validatePathSegment"
---

<objective>
Wire the phase together: build the `evaluators.go` coordinator (D-07 per-metric failure isolation), extend `bench/runtime/result.go` to carry the `Metrics` + `MetricErrors` into a validate-on-write `result.v2.json`, thread the `<run_index>` path segment + a pre-patch `RunTests` snapshot through `RunCell`/`matrix.go`, and author `bench/evaluators/METRICS.md`. This is the integration plan: it consumes the contract (Plan 01) and all 5 graders (Plans 02/03) and produces the metric-complete `result.v2.json` the phase goal demands, proven end-to-end by `make bench-quick`.

Purpose: Deliver the phase's observable outcome — every per-task `result.v2.json` populated with all 17 metrics from real graders, with failure isolation (D-07) and the run_index path/pre-patch-snapshot runtime changes RESEARCH/PATTERNS flagged. Without this plan the graders exist but nothing assembles them into the durable artifact.

Output: the coordinator, the extended result builder + runtime wiring, `METRICS.md`, and an E2E-verified metric-complete `result.v2.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-CONTEXT.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-RESEARCH.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-PATTERNS.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-VALIDATION.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-01-SUMMARY.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-02-SUMMARY.md
@.planning/phases/79-evaluators-result-schema-metrics-layer/79-03-SUMMARY.md
@bench/evaluators/metrics.go
@bench/runtime/result.go
@bench/runtime/cell.go
@bench/runtime/matrix.go
</context>

<artifacts_this_phase_produces>
**New Go symbols (`bench/evaluators/evaluators.go`, package `evaluators`):**
- `GradeInput` struct — the artifacts a cell hands the coordinator: `TestOutcome languages.TestOutcome`, `PrePatchOutcome languages.TestOutcome`, `RepoDir string`, `Merged trace.MergedTrace`, `UsagePresent bool`, `Agent string`, `CompileErrorsBefore *int`.
- `Grade(ctx context.Context, in GradeInput) (Metrics, []MetricError)` — runs all 5 graders, captures each grader's `*MetricError`, leaves the failed metric nil, appends the annotation, and ALWAYS returns a populated `Metrics` (never errors out / never drops the row).

**Modified Go symbols (`bench/runtime/result.go`):**
- `ResultInput` gains `Metrics evaluators.Metrics` and `MetricErrors []evaluators.MetricError`.
- `resultDoc` gains `Metrics evaluators.Metrics json:"metrics"` and `MetricErrors []evaluators.MetricError json:"metric_errors,omitempty"`.
- `BuildResult` assembles them; the existing `Validate` gate now also validates the metrics object.

**Modified Go symbols (`bench/runtime/cell.go`, `matrix.go`, `validate.go`):**
- `RunCell` durable path: `<OutDir>/<task>/<mode>/<run_index>/result.v2.json` (and `trace.json`), with the run_index segment passed through `validatePathSegment` (V5).
- A pre-patch `RunTests` call before `driveScript`, captured into `res` and threaded into the grader `GradeInput.PrePatchOutcome`.
- `matrix.go runOneCell`: stop hard-coding `RunIndex: 0` (line 255) — thread the cell's actual run index.
- `validate.go`: `validatePathSegment` applied to the new run_index segment (reused, no new func unless a numeric-segment variant is cleaner).

**New file:** `bench/evaluators/METRICS.md` — documents `edit_locality` (D-04 git-tracked denominator, root-only/all-files behavior), `regression_rate` (D-05 pre/post double-run, cached passing set), and the chosen `edit_distance_patch` definition (`git diff --numstat` added+deleted lines).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Coordinator with per-metric failure isolation (RED→GREEN)</name>
  <files>bench/evaluators/evaluators.go, bench/evaluators/evaluators_test.go</files>
  <read_first>
    - bench/runtime/result.go (lines 24-114 — `ResultInput`/`resultDoc`/`BuildResult` pure-transform house style; the coordinator mirrors this no-IO transform role)
    - bench/evaluators/metrics.go (the `Metrics`/`MetricError` contract from Plan 01)
    - bench/evaluators/test_runner, patch_validator, token_meter, tool_trace_analyzer, regression_checker (the exported grader signatures from Plans 02/03 — read each package's SUMMARY-noted signature)
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-PATTERNS.md (§"evaluators.go" — per-metric failure isolation, D-07; recommended single-doc seam, no fragment files)
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-CONTEXT.md (D-07)
  </read_first>
  <behavior>
    - Test (all graders succeed): a `GradeInput` whose artifacts let every grader succeed → a `Metrics` with all 17 fields non-nil (for a real-LLM/usage-present input) and an empty `[]MetricError`.
    - Test (per-metric isolation, D-07): a `GradeInput` that forces ONE grader to fail (e.g. `UsagePresent=false` so token_meter nulls tokens, OR a zero-tracked repo so patch_validator nulls edit_locality) → only that metric is nil, ALL other metrics still populate, and `[]MetricError` contains exactly the failed grader's annotation. Assert the row (the `Metrics` value) is still fully returned — never an error/drop.
    - Test (multiple failures): two graders fail → two `metric_errors` entries, the rest populate.
  </behavior>
  <action>
    RED: write `bench/evaluators/evaluators_test.go` (`package evaluators`) with `TestPerMetricIsolation` (single-failure + multi-failure) and `TestAllSeventeenMetrics` (all-present on a usage-present input) covering the three behaviors. Construct `GradeInput` fixtures directly (reuse the grader-package test fixtures' shapes). Run `go test ./bench/evaluators/ -run 'TestPerMetricIsolation|TestAllSeventeenMetrics'` — MUST fail. Commit `test(79-04): add failing coordinator per-metric isolation tests`.
    GREEN: create `bench/evaluators/evaluators.go` (`package evaluators`) declaring `GradeInput` and `Grade(ctx, in)`. `Grade` calls each of the 5 grader packages in turn, assigns successful values into the `Metrics` struct, and on a grader's returned `*MetricError` leaves that metric's pointer nil and appends the annotation to the result slice. `Grade` NEVER returns an error and NEVER short-circuits — every grader runs and the full `Metrics` (with whatever nils) is always returned (D-07). Thread `token_meter`'s `usagePresent` from `in.UsagePresent`, `regression_checker` from `in.PrePatchOutcome`+`in.TestOutcome`, the trace metrics from `in.Merged`, the test/compile metrics from `in.TestOutcome`+`in.CompileErrorsBefore`, and the diff metrics from `in.RepoDir`. Pure transform + exec delegation; no result.v2 IO here (that is Task 2). Commit `feat(79-04): implement coordinator with D-07 per-metric isolation`.
  </action>
  <verify>
    <automated>go test ./bench/evaluators/ -run 'TestPerMetricIsolation|TestAllSeventeenMetrics' -count=1 -x</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/evaluators/ -count=1` passes (single + multi failure isolation, all-present).
    - In the single-failure test the returned `Metrics` is non-nil with all-but-one metric populated and exactly one `metric_errors` entry; `Grade` returns no Go error.
    - grep confirms the coordinator references all 5 grader packages: `grep -nE 'test_runner|patch_validator|token_meter|tool_trace_analyzer|regression_checker' bench/evaluators/evaluators.go` matches all five.
    - RED commit precedes GREEN commit.
  </acceptance_criteria>
  <done>Coordinator runs all 5 graders with D-07 per-metric isolation — one failure nulls one metric + records an annotation, the row is always returned; RED→GREEN commits present.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Wire Metrics into result.v2 + run_index path + pre-patch snapshot (RED→GREEN)</name>
  <files>bench/runtime/result.go, bench/runtime/result_test.go, bench/runtime/cell.go, bench/runtime/matrix.go, bench/runtime/validate.go</files>
  <read_first>
    - bench/runtime/result.go (lines 24-168 — `ResultInput`, `resultDoc`, `BuildResult`, the verbatim jsonschema/v6 `Validate` gate; extend these in place)
    - bench/runtime/cell.go (lines 180-188 — the durable path join with NO run_index segment + the IN-05 deferral comment; line 297 `driveScript`; lines 347-365 the post-drive `RunTests`; line 387 the `trace.Merge` site; lines 414-416 the `ResultInput` assembly)
    - bench/runtime/matrix.go (line 255 — `RunIndex: 0` hard-coded in `runOneCell`)
    - bench/runtime/validate.go (line 23 — `validatePathSegment`)
    - bench/evaluators/evaluators.go (the `Grade`/`GradeInput` from Task 1)
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-PATTERNS.md (§"bench/runtime/result.go" + §"bench/runtime/{cell.go,matrix.go}" — the two runtime changes; validate-on-write reuse)
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-RESEARCH.md (Pitfall 3 run_index path; Pitfall 6 pre-patch snapshot; §"Security Domain" V5 path-segment guard)
  </read_first>
  <behavior>
    - Test (metric-complete result, METRIC-01): `BuildResult` with a populated `ResultInput.Metrics` + `MetricErrors` produces bytes that (a) contain a `metrics` object with all 17 fields, (b) validate against `result.v2.schema.json` via the existing `Validate` gate, (c) a nil metric marshals to JSON `null` (not omitted).
    - Test (D-07 row still valid): a `ResultInput` with some nil metrics + a `metric_errors` entry still passes `Validate`.
    - Test (run_index path): the durable path for a cell with `RunIndex=2` ends in `/2/result.v2.json` (the `<run_index>` segment is present); for `RunIndex=0` it ends in `/0/result.v2.json`.
    - Test (path-segment guard, V5): a run_index value that fails `validatePathSegment` (e.g. containing `..` or a separator — if run_index is ever non-numeric/derived) is rejected before `filepath.Join`. (If run_index is strictly an int formatted via `strconv`, the test asserts the formatted segment is still passed through the guard.)
    - Test (pre-patch snapshot wired): `RunCell` captures a pre-patch `RunTests` outcome before `driveScript` and threads it into `GradeInput.PrePatchOutcome` (assert the pre-patch call happens before the drive — via a fixture LanguageRunner that records call order, or by asserting `regression_rate` is computed on the seed task).
  </behavior>
  <action>
    RED: extend `bench/runtime/result_test.go` with `TestResultMetricsRoundTrip` (metric-complete + D-07 valid) and a run_index path test (`TestRunIndexPathSegment`); add a `RunCell` ordering/pre-patch assertion where the existing cell test harness lives. Run `go test ./bench/runtime/ -run 'TestResultMetricsRoundTrip|TestRunIndexPathSegment'` — MUST fail. Commit `test(79-04): add failing result-metrics + run_index path tests`.
    GREEN — result.go: add `Metrics evaluators.Metrics` and `MetricErrors []evaluators.MetricError` to `ResultInput`; add matching `Metrics` (`json:"metrics"`) and `MetricErrors` (`json:"metric_errors,omitempty"`) to `resultDoc`; assemble them in `BuildResult`; the existing `Validate` gate (verbatim jsonschema/v6 compile) now also gates the metrics object — reuse it unchanged. GREEN — cell.go: (1) add the `<run_index>` path segment to the `result.v2.json`/`trace.json` joins (`filepath.Join(cfg.OutDir, cfg.Task, cfg.Mode, runIndexSegment, ...)`), passing the run_index segment through `validatePathSegment` first (V5 — format the int via `strconv.Itoa` and validate); (2) add a pre-patch `RunTests` call BEFORE `driveScript` (only when a `LanguageRunner` is registered for the cell), store the outcome, and pass it into a `evaluators.Grade(ctx, GradeInput{...})` call assembled after the post-patch outcome + merge, writing the returned `Metrics`+`MetricErrors` into the `ResultInput`. GREEN — matrix.go: stop hard-coding `RunIndex: 0` at line 255 — thread the cell's actual run index from the expanded matrix cell. GREEN — validate.go: apply `validatePathSegment` to the run_index segment (reuse the existing func; add a thin numeric-segment wrapper only if clearer). Commit `feat(79-04): wire metrics into result.v2 + run_index path + pre-patch snapshot`.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestResultMetricsRoundTrip|TestRunIndexPathSegment' -count=1 -x && go vet ./bench/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/runtime/ -count=1` passes (new + pre-existing tests green).
    - A built result.v2.json carries a `metrics` object with all 17 fields and validates against the schema; nil metrics appear as JSON `null`.
    - The durable path includes a `<run_index>` segment; `grep -n 'RunIndex: 0' bench/runtime/matrix.go` returns nothing (the hard-code is removed).
    - The run_index segment passes through `validatePathSegment`: `grep -n 'validatePathSegment' bench/runtime/cell.go` matches in the path-join region.
    - A pre-patch `RunTests` call precedes `driveScript` in `RunCell` (`grep -n 'RunTests' bench/runtime/cell.go` shows two call sites — one pre-drive, one post-drive).
    - RED commit precedes GREEN commit.
  </acceptance_criteria>
  <done>result.v2.json carries the full nullable metrics object + metric_errors and passes validate-on-write; the durable path has a guarded run_index segment; RunIndex:0 hard-code removed; a pre-patch RunTests snapshot feeds regression_checker; RED→GREEN commits present.</done>
</task>

<task type="auto">
  <name>Task 3: Author METRICS.md + E2E metric-complete result.v2.json gate</name>
  <files>bench/evaluators/METRICS.md</files>
  <read_first>
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-CONTEXT.md (D-04 edit_locality git-tracked denominator + root-only/all-files; D-05 regression_rate pre/post double-run + cached passing set)
    - .planning/phases/79-evaluators-result-schema-metrics-layer/79-02-SUMMARY.md (the chosen edit_distance_patch definition to document verbatim)
    - .planning/ROADMAP.md (Phase 79 success criterion 3 — both definitions documented in METRICS.md; criterion 4 — single merged trace per (task,mode,run_index), Jaeger continuity)
    - bench/evaluators/metrics.go (the canonical 17-metric list to enumerate)
    - Makefile (the `bench-quick` recipe — the ≤90s hermetic Go-ToolBench E2E smoke gate)
  </read_first>
  <action>
    Write `bench/evaluators/METRICS.md` documenting all 17 metrics with their definitions, and specifically (required by METRIC-04/05 acceptance): the `edit_locality` definition `1 − (modified_files / total_files_in_repo_subtree)` with the D-04 clarification that `total_files_in_repo_subtree` = git-tracked files (`git ls-files`, excluding generated/vendored/untracked) and the edge cases (root-only edit → ~1.0 per the formula limit / Assumption A3 interpretation, all-files edit → 0.0); the `regression_rate` definition `(failing_pre-existing_tests_post_patch / passing_pre-existing_tests_pre_patch)` with the D-05 pre/post double-run + cached-passing-set semantics and the "pre-existing failures do NOT count" rule; and the chosen `edit_distance_patch` definition (`git diff --numstat` added+deleted lines) with a note it is the deterministic in-house definition (Open Q4). Add a short "source-of-truth" note for tokens (provider `usage`, never the MCP counter — METRIC-03/D-02) and the D-01 scripted-null rule. After writing, run the full phase E2E: `go vet ./... && go test ./...` then `make bench-quick` and confirm it produces a metric-complete `result.v2.json` (all 17 metrics present as values-or-null) under `<task>/<mode>/<run_index>/`. Do NOT inline code; reference the grader packages and formulas in prose. Commit `docs(79-04): add bench/evaluators/METRICS.md + E2E metric-complete result gate`.
  </action>
  <verify>
    <automated>test -f bench/evaluators/METRICS.md && grep -q "edit_locality" bench/evaluators/METRICS.md && grep -q "regression_rate" bench/evaluators/METRICS.md && go vet ./... && go test ./bench/... -count=1 && make bench-quick</automated>
  </verify>
  <acceptance_criteria>
    - `bench/evaluators/METRICS.md` exists and documents both `edit_locality` (with the git-tracked denominator + root-only/all-files edge cases) and `regression_rate` (with the pre/post double-run + cached-passing-set semantics), plus `edit_distance_patch`.
    - `go vet ./... && go test ./... -count=1` green.
    - `make bench-quick` exits 0 (≤90s) and the produced `result.v2.json` is schema-valid with a `metrics` object containing all 17 metrics (each a value or explicit `null` — none omitted); the durable path includes a `<run_index>` segment.
  </acceptance_criteria>
  <done>METRICS.md documents the locality/regression/edit-distance definitions per METRIC-04/05 acceptance; the full suite is green; `make bench-quick` produces a metric-complete, schema-valid result.v2.json under the run_index path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cell metadata (task/mode/run_index) → durable path | The run_index (now part of the durable filesystem path) must not enable path traversal. |
| cell repo working tree → coordinator graders | The coordinator delegates to graders that exec `git`/`go` over the agent-mutated repo (inherits Plan 02's mitigations). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-04-01 | Tampering | run_index path segment in RunCell durable path | mitigate | Format run_index via `strconv.Itoa` and pass it through `validatePathSegment` (reused from `validate.go`, rejects `..`/separators/leading dots — T-77-08/T-77-10) BEFORE `filepath.Join`. Enforced by the `validatePathSegment` grep in Task 2 acceptance. |
| T-79-04-02 | Tampering | git/go exec via the coordinator | mitigate | Inherited from Plan 02: fixed argv, `cmd.Dir`, no shell. The coordinator adds no new exec surface beyond delegating to the already-mitigated graders. |
| T-79-04-03 | Information Disclosure | metric_errors free-text reason leaking repo paths | accept | `MetricError.Reason` is a short grader-authored diagnostic string (e.g. "no provider usage block"), not interpolated untrusted repo content; result.v2.json is a local bench artifact, no PII, low-value target. |
| T-79-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (RESEARCH Package Legitimacy Audit: zero new external packages). |

No `high`-severity threats.
</threat_model>

<verification>
- `go vet ./... ` clean; `go test ./... -count=1` green (full suite, not just bench/).
- `make bench-quick` exits 0 (≤90s) producing a schema-valid metric-complete result.v2.json under `<task>/<mode>/<run_index>/`.
- `grep -n 'RunIndex: 0' bench/runtime/matrix.go` returns nothing (hard-code removed).
- METRICS.md documents edit_locality + regression_rate + edit_distance_patch.
- (Manual, per VALIDATION.md) Jaeger import of the merged trace.json shows a single trace root→LSP leaves, no orphan spans — automated structural continuity is asserted by Plan 03's tool_trace_analyzer test (`tool_calls == ToolCallSummary.Total`, `RejectedForeignPid == 0`).
</verification>

<success_criteria>
- The coordinator delivers D-07 per-metric isolation: one grader failure nulls one metric + annotates, never drops the row (METRIC-01).
- result.v2.json carries the full nullable 17-metric `metrics` object + `metric_errors`, validated on write (METRIC-01/02).
- The durable path is `<task>/<mode>/<run_index>/`, guarded by `validatePathSegment`; RunIndex no longer hard-coded (per (task, mode, run_index); Pitfall 3).
- A pre-patch RunTests snapshot feeds regression_checker (D-05/Pitfall 6).
- METRICS.md documents edit_locality + regression_rate (METRIC-04/05 acceptance).
- `make bench-quick` produces a metric-complete result.v2.json end-to-end (phase goal).
</success_criteria>

<output>
Create `.planning/phases/79-evaluators-result-schema-metrics-layer/79-04-SUMMARY.md` when done. Per TDD task record RED/GREEN/REFACTOR + commits. Record the final durable-path shape, the `GradeInput` contract, and the `make bench-quick` E2E result (exit code, timing, metric-completeness of the produced result.v2.json) so the phase verifier can confirm the goal.
</output>
