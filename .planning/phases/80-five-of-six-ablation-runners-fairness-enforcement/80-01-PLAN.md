---
phase: 80-five-of-six-ablation-runners-fairness-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/runners/baseline_plain/MODE.md
  - bench/runners/no_lsp/MODE.md
  - bench/runners/no_structured_edit/MODE.md
  - bench/runners/your_agent_no_semantic/MODE.md
  - bench/runners/baseline_rag/MODE.md
  - bench/runners/mode_resolver_test.go
  - bench/BENCH.md
autonomous: true
requirements: [ABLATE-01, ABLATE-03]
must_haves:
  truths:
    - "Each of the 5 new modes resolves to its profile via bench/runners/<mode>/MODE.md (no Go resolver change)"
    - "baseline_plain resolves to profile baseline (not a new bench-* YAML)"
    - "your_agent_no_semantic MODE.md prose documents the Phase 81 deferred guarantee"
    - "baseline_rag MODE.md prose documents the Phase 83 RAG deferral"
    - "bench/BENCH.md documents baseline_plain's empty Helix inventory and the no_semantic deferred guarantee"
  artifacts:
    - path: "bench/runners/baseline_plain/MODE.md"
      provides: "baseline_plain mode definition (profile: baseline)"
      contains: "profile: baseline"
    - path: "bench/runners/no_lsp/MODE.md"
      provides: "no_lsp mode definition (profile: bench-no-lsp)"
      contains: "profile: bench-no-lsp"
    - path: "bench/runners/no_structured_edit/MODE.md"
      provides: "no_structured_edit mode definition (profile: bench-no-structured-edit)"
      contains: "profile: bench-no-structured-edit"
    - path: "bench/runners/your_agent_no_semantic/MODE.md"
      provides: "no_semantic mode definition (profile: bench-no-semantic) + deferral prose"
      contains: "profile: bench-no-semantic"
    - path: "bench/runners/baseline_rag/MODE.md"
      provides: "baseline_rag stub mode definition + deferral prose"
      contains: "baseline_rag"
  key_links:
    - from: "bench/runners/mode_resolver_test.go"
      to: "bench/runners/<mode>/MODE.md"
      via: "ResolveProfile(mode) reads MODE.md frontmatter"
      pattern: "ResolveProfile\\("
---

<objective>
Add the 5 new `bench/runners/<mode>/MODE.md` directories that grow Phase 77's
table-driven mode→profile resolver from 1 mode to 6 — `baseline_plain`,
`no_lsp`, `no_structured_edit`, `your_agent_no_semantic`, and `baseline_rag` —
keeping every file's frontmatter to exactly `mode` + `profile` so the resolver
needs ZERO Go change (the filesystem IS the table). Extend the resolver test to
assert all 5 new modes resolve to their profiles, and document baseline_plain's
empty Helix inventory plus the no_semantic deferred guarantee in `bench/BENCH.md`.

Purpose: ABLATE-01 requires each of 6 modes to be a `bench/runners/<mode>/MODE.md`
definition; ABLATE-03 requires baseline_plain to reuse `baseline.yaml` (no new
YAML). This plan delivers the mode definitions and the operator-side documentation
(criteria #1 and #2). The cell-side wiring (gate, fail-close, ablation_status) and
the delta pass are separate plans.
Output: 5 MODE.md files, extended resolver test, BENCH.md doc additions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-CONTEXT.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-RESEARCH.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This phase (across all 5 plans) creates the following symbols/paths. This plan owns
the ones marked [THIS PLAN]:
- [THIS PLAN] `bench/runners/baseline_plain/MODE.md` (profile: baseline)
- [THIS PLAN] `bench/runners/no_lsp/MODE.md` (profile: bench-no-lsp)
- [THIS PLAN] `bench/runners/no_structured_edit/MODE.md` (profile: bench-no-structured-edit)
- [THIS PLAN] `bench/runners/your_agent_no_semantic/MODE.md` (profile: bench-no-semantic)
- [THIS PLAN] `bench/runners/baseline_rag/MODE.md` (stub mode)
- [THIS PLAN] new resolver test functions in `bench/runners/mode_resolver_test.go`
- (Plan 02) `ResultInput.AblationStatus` + `resultDoc.AblationStatus` + `ablation_status` schema prop, enum value `guarantee_pending_phase_81`
- (Plan 03) `CellResult.Deferred` + `CellResult.DeferredReason` fields; fairness gate call site in `RunCell`; `ablationStatusFor`/mode-name helper
- (Plan 03) `CellOutcome.Deferred` matrix outcome (or equivalent distinct-outcome flag)
- (Plan 04) `bench/runners/contract_test.go` test functions (TestEffectiveConfigMatchesContract, TestDefaultContractValidates if not already present)
- (Plan 05) `bench/runtime/deltas.go` (or bench/runners/) 3-delta helper; `ablation_deltas` open property in the result row
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the 5 new MODE.md mode definitions</name>
  <files>bench/runners/baseline_plain/MODE.md, bench/runners/no_lsp/MODE.md, bench/runners/no_structured_edit/MODE.md, bench/runners/your_agent_no_semantic/MODE.md, bench/runners/baseline_rag/MODE.md</files>
  <read_first>
    - bench/runners/your_agent_full/MODE.md (the seed template — mirror its frontmatter shape and prose structure: `---` delimited `mode:`/`profile:` block, then an H1 == mode name and a paragraph naming the resolved profile + ablated subsystem)
    - bench/runners/mode_resolver.go (lines 35-38 `modeFrontmatter` declares EXACTLY `mode`+`profile`; line 119 `dec.KnownFields(true)` rejects any third key — so these files MUST carry only `mode` and `profile`; line 54 `validateModeName` rejects path-traversal dir names)
    - internal/profile/profiles/baseline.yaml (D-01: baseline_plain resolves here, NOT a new bench-* YAML — ABLATE-03)
    - internal/profile/profiles/bench-no-semantic.yaml (header comment confirms NO kernel flag this phase; the no_semantic prose must cite the Phase 81 deferral)
  </read_first>
  <action>
    Create 5 directories under `bench/runners/`, each with a `MODE.md` whose
    frontmatter is the `---`-delimited two-key block `mode: <name>` / `profile: <name>`
    matching `your_agent_full/MODE.md:1-4` EXACTLY in shape. Use ONLY the keys
    `mode` and `profile` (no third key — `KnownFields(true)` at mode_resolver.go:119
    hard-fails any extra key; the baseline_rag stub is detected by mode name in
    Plan 03's cell wiring, NOT by a frontmatter marker, to keep the resolver
    change-free). Per D-01/D-02 and the PATTERNS.md per-mode table:
    - `baseline_plain/MODE.md`: `mode: baseline_plain`, `profile: baseline` (D-01,
      ABLATE-03 — reuses internal/profile/profiles/baseline.yaml, NOT a bench-* YAML).
      Prose: the zero-Helix-tools control arm; the empty inventory is enforced by the
      profile filter, the daemon still spawns for trace symmetry.
    - `no_lsp/MODE.md`: `mode: no_lsp`, `profile: bench-no-lsp`. Prose: the
      LSP-disabled ablation arm (disable_lsp_subsystem, Phase 76 ABLATE-05).
    - `no_structured_edit/MODE.md`: `mode: no_structured_edit`,
      `profile: bench-no-structured-edit`. Prose: the structured-edit-disabled arm
      (disable_structured_edit_subsystem, Phase 76 ABLATE-07).
    - `your_agent_no_semantic/MODE.md`: `mode: your_agent_no_semantic`,
      `profile: bench-no-semantic` (prefer this exact dir name per CONTEXT
      Claude's-discretion for Phase 81 forward-compat). Prose MUST document the
      D-03 deferred guarantee: this arm runs and emits a REAL row but is marked
      partial via `ablation_status: guarantee_pending_phase_81` because the
      kernel-level `disable_semantic_subsystem` flag (ABLATE-06, zero-DuckDB
      guarantee) lands in Phase 81 — until then the daemon may still read the
      semantic store via back-channel paths, so the row is NOT yet a clean
      no_semantic measurement.
    - `baseline_rag/MODE.md`: `mode: baseline_rag`, `profile: baseline` (a
      placeholder profile so the frontmatter is well-formed and the resolver
      parses it; the runner is fail-closed in Plan 03 BEFORE any daemon spawn so
      the profile is never actually used). Prose MUST document that this is a
      registered fail-closed stub: the real RAG arm (standalone
      `cmd/helix-bench-rag` + chromem-go embedding index, ABLATE-04) is deferred
      to Phase 83, and this mode produces NO result row this phase.
    All 5 dir names pass `validateModeName` (no separators, no leading dot,
    `filepath.Clean(name)==name`).
  </action>
  <verify>
    <automated>test -f bench/runners/baseline_plain/MODE.md && test -f bench/runners/no_lsp/MODE.md && test -f bench/runners/no_structured_edit/MODE.md && test -f bench/runners/your_agent_no_semantic/MODE.md && test -f bench/runners/baseline_rag/MODE.md && grep -q 'profile: baseline' bench/runners/baseline_plain/MODE.md && grep -q 'profile: bench-no-lsp' bench/runners/no_lsp/MODE.md && grep -q 'profile: bench-no-structured-edit' bench/runners/no_structured_edit/MODE.md && grep -q 'profile: bench-no-semantic' bench/runners/your_agent_no_semantic/MODE.md && grep -q 'guarantee_pending_phase_81' bench/runners/your_agent_no_semantic/MODE.md</automated>
  </verify>
  <acceptance_criteria>
    - All 5 MODE.md files exist at the exact paths above.
    - Each frontmatter contains ONLY `mode:` and `profile:` keys (no third key).
    - baseline_plain → `profile: baseline`; no_lsp → `profile: bench-no-lsp`;
      no_structured_edit → `profile: bench-no-structured-edit`;
      your_agent_no_semantic → `profile: bench-no-semantic`.
    - `your_agent_no_semantic/MODE.md` prose contains the literal string
      `guarantee_pending_phase_81` and a Phase 81 deferral note.
    - `baseline_rag/MODE.md` prose names "Phase 83" and "deferred".
  </acceptance_criteria>
  <done>All 5 mode definitions exist with two-key frontmatter and the required deferral prose; the resolver can read each one.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the resolver test to cover the 5 new modes + document in BENCH.md</name>
  <files>bench/runners/mode_resolver_test.go, bench/BENCH.md</files>
  <read_first>
    - bench/runners/mode_resolver_test.go (mirror TestModeResolverYourAgentFull at lines 23-31 — it asserts ResolveProfile("your_agent_full")=="bench-full"; add one table-driven test asserting each new mode resolves to its profile)
    - bench/runners/your_agent_full/MODE.md (the existing seed that the existing test already covers)
    - bench/BENCH.md (operator-side contract doc; find the runners/ section and the directory-layout table to add the baseline_plain empty-inventory + no_semantic deferred-guarantee notes)
  </read_first>
  <action>
    In `bench/runners/mode_resolver_test.go`, add a new table-driven test
    (e.g. `TestModeResolverAblationModes`) that asserts `ResolveProfile` returns
    the expected profile for each new mode: `baseline_plain`→`baseline`,
    `no_lsp`→`bench-no-lsp`, `no_structured_edit`→`bench-no-structured-edit`,
    `your_agent_no_semantic`→`bench-no-semantic`, and `baseline_rag`→`baseline`.
    Mirror the existing `TestModeResolverYourAgentFull` style (call `ResolveProfile`,
    compare to the want string, `t.Fatalf` on mismatch). Do NOT duplicate or alter
    `TestModeResolverYourAgentFull`, `TestModeResolverUnknownMode`,
    `TestModeResolverRejectsTraversal`, or `TestModeResolverFromRootStrictFrontmatter`
    — they stay green.
    In `bench/BENCH.md`, add operator-facing prose (criteria #1/#2): document that
    `baseline_plain` reuses `baseline.yaml` and exposes an EMPTY Helix tool inventory
    (only the shell/grep/read/edit/test the agent runtime exposes natively — enforced
    by the profile filter, not by bench code), and that `your_agent_no_semantic` emits
    a row marked `ablation_status: guarantee_pending_phase_81` because the kernel-level
    zero-DuckDB guarantee (ABLATE-06) lands in Phase 81. Mention that `baseline_rag` is
    a registered fail-closed stub (no row) until Phase 83.
  </action>
  <verify>
    <automated>go test ./bench/runners/ -run 'TestModeResolver' -count=1 && grep -q 'baseline_plain' bench/BENCH.md && grep -q 'guarantee_pending_phase_81' bench/BENCH.md</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/runners/ -run TestModeResolver -count=1` exits 0 (the new
      ablation-modes test passes AND the 4 existing resolver tests stay green).
    - The new test asserts all 5 new modes resolve to the profiles listed in the action.
    - `bench/BENCH.md` contains the strings `baseline_plain` and
      `guarantee_pending_phase_81` and prose describing the empty inventory and the
      Phase 81 deferral.
  </acceptance_criteria>
  <done>Resolver test green across all 6 modes; BENCH.md documents the baseline_plain empty inventory and the no_semantic deferred guarantee.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → resolver FS join | A mode name becomes a path segment in `filepath.Join(root, mode, "MODE.md")` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | mode dir name → `filepath.Join` in `ResolveProfileFromRoot` | mitigate (existing control) | `validateModeName` (mode_resolver.go:54) rejects `..`, separators, leading dot, absolute paths BEFORE any FS join. All 5 new mode names (`baseline_plain`, `no_lsp`, `no_structured_edit`, `your_agent_no_semantic`, `baseline_rag`) pass `filepath.Clean(name)==name` — no new threat surface; inherits existing path-segment validation. |

No new threat surface beyond the inherited path-segment validation: this plan adds
static config files and a test. No external input, no network, no new code path.
</threat_model>

<verification>
- `go build ./cmd/helix && go vet ./... && go test ./...` all green.
- Targeted: `go test ./bench/runners/ -run TestModeResolver -count=1` exits 0.
- All 5 MODE.md files exist and resolve to their declared profiles.
</verification>

<success_criteria>
- 6 modes (1 existing + 5 new) each have a `bench/runners/<mode>/MODE.md` (criterion #1).
- baseline_plain resolves to `baseline` with no new YAML (criterion #2, ABLATE-03).
- BENCH.md documents the empty baseline_plain inventory + no_semantic deferral.
- Resolver test green for all 6 modes; existing tests unbroken.
</success_criteria>

<output>
Create `.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-01-SUMMARY.md` when done
</output>
