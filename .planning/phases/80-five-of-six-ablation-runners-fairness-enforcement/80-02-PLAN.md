---
phase: 80-five-of-six-ablation-runners-fairness-enforcement
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - bench/runtime/result.go
  - bench/runtime/result_test.go
  - bench/schema/result.v2.schema.json
autonomous: true
requirements: [ABLATE-01]
must_haves:
  truths:
    - "BuildResult projects a non-empty AblationStatus into the result.v2 row as ablation_status"
    - "A row with ablation_status=guarantee_pending_phase_81 still passes the validate-on-write gate"
    - "An honest mode (empty AblationStatus) omits ablation_status entirely (omitempty)"
  artifacts:
    - path: "bench/runtime/result.go"
      provides: "ResultInput.AblationStatus + resultDoc.AblationStatus (json: ablation_status,omitempty) + BuildResult wiring"
      contains: "AblationStatus"
    - path: "bench/schema/result.v2.schema.json"
      provides: "documented optional ablation_status top-level property (additive, no schema major bump)"
      contains: "ablation_status"
  key_links:
    - from: "bench/runtime/result.go BuildResult"
      to: "resultDoc.AblationStatus json tag ablation_status"
      via: "doc literal assignment AblationStatus: in.AblationStatus"
      pattern: "AblationStatus:\\s*in\\.AblationStatus"
---

<objective>
Add the additive optional `ablation_status` field to the result.v2 builder
(`ResultInput.AblationStatus` → `resultDoc.AblationStatus` json `ablation_status,omitempty`
→ `BuildResult` wiring) and document it as an optional top-level property in
`result.v2.schema.json`. The top-level `additionalProperties` is OPEN (schema
`$comment`), so this is additive-only — NO schema major bump (D-03). Honest modes
omit it (omitempty); the no_semantic row will carry `guarantee_pending_phase_81`
(set by the cell wiring in Plan 03).

Purpose: D-03 belt-and-suspenders machine-checkable deferral marker. The Phase 82
aggregator must be able to programmatically distinguish a partial no_semantic row
from a clean one. This plan delivers the field + schema doc; Plan 03 sets the value
in the cell.
Output: extended ResultInput/resultDoc/BuildResult, schema property, RED→GREEN test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-CONTEXT.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-RESEARCH.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-PATTERNS.md
</context>

<artifacts_this_phase_produces>
[THIS PLAN]:
- `ResultInput.AblationStatus string` (bench/runtime/result.go)
- `resultDoc.AblationStatus string` json tag `ablation_status,omitempty` (bench/runtime/result.go)
- `BuildResult` doc-literal line `AblationStatus: in.AblationStatus`
- `ablation_status` optional top-level property in bench/schema/result.v2.schema.json
- enum value (greppable) `guarantee_pending_phase_81` (documented in schema description; SET by Plan 03)
- new test function(s) in bench/runtime/result_test.go (e.g. TestAblationStatusProjected / TestAblationStatusOmittedWhenEmpty)
(Other plans' artifacts listed in 80-01-PLAN.md.)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Additive ablation_status field on the result builder (RED→GREEN)</name>
  <files>bench/runtime/result.go, bench/runtime/result_test.go, bench/schema/result.v2.schema.json</files>
  <read_first>
    - bench/runtime/result.go (ResultInput at lines 28-69; resultDoc at 90-114 — mirror the open-provenance fields Outcome/TraceRef/ModelID at 104-106; BuildResult doc literal at 141-155 — add one line `AblationStatus: in.AblationStatus`; the Validate() gate at 185-209 must still pass)
    - bench/runtime/result_test.go (existing builder tests — match the BuildResult→Validate assertion style)
    - bench/schema/result.v2.schema.json (the $comment at line 6 establishes additive-only=minor with top-level additionalProperties OPEN; mirror the `mode` property def at lines 18-21 to add an optional `ablation_status` string property for documentation)
  </read_first>
  <behavior>
    - Test 1 (RED): `BuildResult(ResultInput{... AblationStatus: "guarantee_pending_phase_81"})` produces JSON that, when unmarshaled, contains key `ablation_status` == `"guarantee_pending_phase_81"`, AND `Validate(bytes)` returns nil (schema still valid — open additionalProperties).
    - Test 2 (RED): `BuildResult(ResultInput{... AblationStatus: ""})` produces JSON whose marshaled bytes do NOT contain the key `ablation_status` (omitempty), AND `Validate(bytes)` returns nil.
    Both tests fail to compile / fail assertion before the field exists (RED), pass after (GREEN).
  </behavior>
  <action>
    RED: add `TestAblationStatusProjected` and `TestAblationStatusOmittedWhenEmpty`
    to `bench/runtime/result_test.go` encoding the two behaviors above
    (build → unmarshal → assert key presence/value → `Validate` nil). Run to confirm
    they fail (field absent).
    GREEN:
    1. Add `AblationStatus string` to `ResultInput` (result.go:28-69), documented:
       `"" for honest modes; "guarantee_pending_phase_81" for no_semantic (D-03)`.
    2. Add `AblationStatus string \`json:"ablation_status,omitempty"\`` to `resultDoc`
       (result.go:90-114), mirroring the open-provenance props but WITH omitempty so
       honest modes emit nothing.
    3. In the `BuildResult` doc literal (result.go:141-155) add
       `AblationStatus: in.AblationStatus`.
    4. Document the field in `result.v2.schema.json` as an OPTIONAL top-level
       property `ablation_status` of `{"type":"string"}` with a description naming
       the greppable enum values (`guarantee_pending_phase_81` for the deferred
       no_semantic arm; honest modes omit or set `enforced` once Phase 81 lands) and
       noting it is additive-only (no schema major bump; schema_version stays "v2").
    Do NOT bump `schema_version`. Do NOT add `additionalProperties: false` anywhere.
    Run the tests to confirm GREEN.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestAblationStatus' -count=1 && go test ./bench/schema/ -count=1 && grep -q 'ablation_status' bench/schema/result.v2.schema.json</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/runtime/ -run TestAblationStatus -count=1` exits 0 (both new tests pass).
    - `go test ./bench/schema/ -count=1` exits 0 (the schema golden/validation tests still pass with the additive property).
    - A built row with `AblationStatus: "guarantee_pending_phase_81"` round-trips that exact value under key `ablation_status` and passes `runtime.Validate`.
    - A built row with empty `AblationStatus` has NO `ablation_status` key and passes `runtime.Validate`.
    - `schema_version` const remains `"v2"`; no `additionalProperties: false` introduced.
  </acceptance_criteria>
  <done>The ablation_status field exists, projects through BuildResult, omits when empty, keeps the row schema-valid, and is documented in the schema as additive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder → durable result.v2.json | The ablation_status string is serialized into a durable artifact later read by the Phase 82 aggregator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-02 | Tampering (data integrity) | a deferred no_semantic row scored as a clean measurement | mitigate | The machine-checkable `ablation_status` marker (this plan) lets the aggregator distinguish a partial row (`guarantee_pending_phase_81`) from a clean one; the value is SET by Plan 03's cell wiring. The field is display/provenance-only — never exec'd, shelled, or used as a query (no injection sink). |

The `ablation_status` value is a fixed internal constant set by the harness, not
external input. No new external surface; no network; no crypto.
</threat_model>

<verification>
- `go build ./cmd/helix && go vet ./... && go test ./...` all green.
- Targeted: `go test ./bench/runtime/ -run TestAblationStatus -count=1` and
  `go test ./bench/schema/ -count=1` exit 0.
</verification>

<success_criteria>
- ablation_status is an additive optional field; honest modes omit it (D-03).
- The deferred-marker row stays schema-valid (no major bump).
- RED→GREEN cycle complete; both new builder tests green.
</success_criteria>

<output>
Create `.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-02-SUMMARY.md` when done
</output>
