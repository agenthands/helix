---
phase: 80-five-of-six-ablation-runners-fairness-enforcement
plan: 03
type: tdd
wave: 2
depends_on: ["80-01", "80-02"]
files_modified:
  - bench/runtime/cell.go
  - bench/runtime/cell_test.go
  - bench/runtime/matrix.go
  - bench/runtime/matrix_test.go
autonomous: true
requirements: [ABLATE-01]
must_haves:
  truths:
    - "RunCell calls runners.DefaultContract.Validate() at startup and treats a non-nil return as fatal (no daemon spawned)"
    - "RunCell short-circuits baseline_rag BEFORE sandbox/daemon, producing NO result.v2.json and a distinct Deferred outcome"
    - "RunCell sets ablation_status=guarantee_pending_phase_81 on the your_agent_no_semantic row only"
    - "A baseline_rag cell is counted as neither a success nor an infra error by the matrix"
  artifacts:
    - path: "bench/runtime/cell.go"
      provides: "fairness Validate() gate + baseline_rag fail-close + ablation_status wiring + CellResult.Deferred/DeferredReason fields"
      contains: "DefaultContract.Validate()"
    - path: "bench/runtime/matrix.go"
      provides: "distinct deferred outcome on CellOutcome so a fail-closed stub is not Success"
      contains: "Deferred"
  key_links:
    - from: "bench/runtime/cell.go RunCell"
      to: "runners.DefaultContract.Validate()"
      via: "startup fatal gate after profile resolution"
      pattern: "DefaultContract\\.Validate\\(\\)"
    - from: "bench/runtime/cell.go RunCell"
      to: "result row ablation_status"
      via: "BuildResult(ResultInput{AblationStatus: ablationStatusFor(cfg.Mode)})"
      pattern: "AblationStatus:"
---

<objective>
Wire the three cell-level Phase 80 behaviors into `RunCell` (the uniform per-cell
spine all 6 modes flow through):
1. **D-04 startup fairness gate:** call `runners.DefaultContract.Validate()`
   unconditionally right after profile resolution; a non-nil return is FATAL (return
   an infra error before any sandbox/daemon — refuse to run an unfair benchmark).
2. **D-02 baseline_rag fail-close:** short-circuit `cfg.Mode == "baseline_rag"`
   BEFORE `benchsandbox.New`, returning a `CellResult` with a new
   `Deferred`/`DeferredReason` signal and NO result.v2.json. Add a distinct matrix
   outcome so a deferred stub is neither `Success` nor an infra `Err`.
3. **D-03 ablation_status:** at the `BuildResult` step, set
   `AblationStatus: guarantee_pending_phase_81` only when
   `cfg.Mode == "your_agent_no_semantic"` (honest modes leave it empty).

Purpose: criterion #1 (asymmetric deferred modes — no_semantic real-but-partial,
baseline_rag fail-closed-no-row) and criterion #3 (same-model-same-budget enforced
at runner startup). This is pure growth on the existing spine — no fork of the cell
path (D-01).
Output: extended RunCell + CellResult + matrix outcome, RED→GREEN tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-CONTEXT.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-RESEARCH.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-PATTERNS.md
@bench/runtime/result.go
</context>

<artifacts_this_phase_produces>
[THIS PLAN]:
- `CellResult.Deferred bool` + `CellResult.DeferredReason string` (bench/runtime/cell.go)
- fairness gate call site `runners.DefaultContract.Validate()` in `RunCell` (after profile resolution)
- `baseline_rag` fail-close short-circuit in `RunCell` (before sandbox)
- `ablationStatusFor(mode string) string` helper (or inline) returning `guarantee_pending_phase_81` iff `your_agent_no_semantic`
- `CellOutcome.Deferred bool` (or equivalent distinct flag) in `bench/runtime/matrix.go` so `runOneCell` marks a deferred stub as neither Success nor Err
Depends on: Plan 02's `ResultInput.AblationStatus`; Plan 01's MODE.md files (so resolution + baseline_rag mode exist).
(Other plans' artifacts listed in 80-01-PLAN.md.)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Fairness startup gate + ablation_status wiring in RunCell (RED→GREEN)</name>
  <files>bench/runtime/cell.go, bench/runtime/cell_test.go</files>
  <read_first>
    - bench/runtime/cell.go (RunCell numbered steps; step (1) profile resolution at 247-256 — insert the gate immediately after; step (9) BuildResult at 517-537 where Fairness: runners.DefaultContract is already set at 527 — add AblationStatus there; `runners` is already imported at line 40)
    - bench/runners/fairness_contract.go (DefaultContract at 84-100; Validate() at 107-114 — returns non-nil iff an override lacks a WaiverReason; pure + CI-cheap; committed contract passes so the gate never fatals in CI under the current contract)
    - bench/runtime/result.go (ResultInput.AblationStatus added in Plan 02 — set it in the BuildResult call)
    - bench/runtime/cell_test.go (benchRunnersRootForTest helper at 20-29; existing table-test style; mirror it for the gate + ablation_status assertions)
    - bench/runners/fairness_contract_test.go (TestEmptyWaiverReasonFatal at 16-75 — the bad-override construction pattern to reuse for the fatal-gate test)
  </read_first>
  <behavior>
    - Test 1 (fairness gate fatal): construct a RunCell path where `DefaultContract` is
      replaced/shadowed by a contract carrying an override with an empty WaiverReason
      (or directly unit-test the gate predicate the way RunCell calls it) — assert the
      gate returns a non-nil error. Since DefaultContract is a package var, test the
      gate at the unit level the same way `TestEmptyWaiverReasonFatal` does (a local
      FairnessContract with a bad override fails Validate()), AND assert RunCell wires
      Validate() such that a non-nil return aborts before sandbox. The committed
      DefaultContract.Validate()==nil (so a normal cell is unaffected) — assert that too.
    - Test 2 (ablation_status set for no_semantic): after a RunCell-built row for mode
      `your_agent_no_semantic`, the emitted result.v2.json contains
      `ablation_status: guarantee_pending_phase_81`; for mode `your_agent_full` the row
      has no `ablation_status` key. (Use a unit-level assertion on the
      `ablationStatusFor` helper + the BuildResult projection if a full daemon run is
      too heavy for the unit test; the multi-mode smoke in Plan 05 exercises the
      full path.)
  </behavior>
  <action>
    RED: add the two behavior tests above to `bench/runtime/cell_test.go`. The fatal-gate
    test asserts the gate predicate (mirror TestEmptyWaiverReasonFatal: a local
    `runners.FairnessContract` with one empty-WaiverReason override → `.Validate()` !=
    nil) AND that the committed `runners.DefaultContract.Validate()` == nil. The
    ablation_status test asserts `ablationStatusFor("your_agent_no_semantic") ==
    "guarantee_pending_phase_81"` and `ablationStatusFor("your_agent_full") == ""`, plus
    a BuildResult round-trip confirming the no_semantic value lands under
    `ablation_status` and the full mode omits it.
    GREEN:
    1. In `RunCell`, immediately after profile resolution (cell.go ~256, before the
       sandbox at 258), call `runners.DefaultContract.Validate()`; on non-nil return
       `return res, fmt.Errorf("bench/runtime: fairness contract invalid: %w", verr)`.
       Call it UNCONDITIONALLY (Open Q1 scope A recommendation — it is pure and
       CI-cheap; the CI contract test in Plan 04 is the separate always-on guarantee).
    2. Add a small helper `ablationStatusFor(mode string) string` returning
       `"guarantee_pending_phase_81"` iff `mode == "your_agent_no_semantic"`, else `""`.
    3. In the `BuildResult(ResultInput{...})` call (cell.go:520-530) add
       `AblationStatus: ablationStatusFor(cfg.Mode)`.
    Run the tests to GREEN. Do NOT fork the cell path; do NOT touch the daemon/tap/merge
    steps.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestFairnessGate|TestAblationStatus' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/runtime/ -run 'TestFairnessGate|TestAblationStatus' -count=1` exits 0.
    - The fatal-gate test proves a contract with an empty-WaiverReason override fails the gate predicate, and the committed DefaultContract passes.
    - `ablationStatusFor("your_agent_no_semantic") == "guarantee_pending_phase_81"`; `ablationStatusFor("your_agent_full") == ""`.
    - A BuildResult round-trip for your_agent_no_semantic contains `ablation_status: guarantee_pending_phase_81`; for your_agent_full the key is absent.
  </acceptance_criteria>
  <done>RunCell enforces the fairness contract at startup (fatal on non-nil) and tags only the no_semantic row with the deferral marker.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: baseline_rag fail-close (no row) + distinct matrix outcome (RED→GREEN)</name>
  <files>bench/runtime/cell.go, bench/runtime/cell_test.go, bench/runtime/matrix.go, bench/runtime/matrix_test.go</files>
  <read_first>
    - bench/runtime/cell.go (CellResult struct at 114-141 — add Deferred/DeferredReason mirroring the bool/string signal fields; RunCell step (2) sandbox at 258 — insert the fail-close BEFORE benchsandbox.New, after the fairness gate from Task 1)
    - bench/runtime/matrix.go (CellOutcome at 65-74; runOneCell at 234-274 — Success at 272 is `err == nil && res.VerifyExitCode == 0 && res.ResultValid`; a deferred stub has ResultValid==false so it is not Success, but add a distinct Deferred flag so it is also not lumped with infra errors)
    - bench/runtime/matrix_test.go (dispatch test style; assert outcome classification)
    - bench/runtime/cell_test.go (existing RunCell test scaffolding; baseline_rag needs no daemon so the test can assert the early return without a helix binary)
  </read_first>
  <behavior>
    - Test 1 (no row): a RunCell with `cfg.Mode == "baseline_rag"` returns
      `res.Deferred == true`, `res.DeferredReason` naming Phase 83, a nil error, and
      writes NO result.v2.json at `res.ResultPath` (the path field is set for layout
      but no file exists on disk because the cell short-circuits before BuildResult).
    - Test 2 (matrix classification): a `CellOutcome` for a deferred cell has
      `Success == false` AND a distinct `Deferred == true` (or equivalent), so the
      smoke can assert "registered + deferred", neither succeeded nor infra-errored.
  </behavior>
  <action>
    RED: add the two behavior tests. The cell test drives `RunCell` with
    `cfg.Mode == "baseline_rag"` (use `RunnersRoot` pointing at bench/runners via
    `benchRunnersRootForTest`, a temp OutDir, and a dummy HelixBin — the cell must
    return BEFORE touching the binary) and asserts: nil error, `res.Deferred == true`,
    `res.DeferredReason` contains "Phase 83", and `os.Stat(res.ResultPath)` is
    `os.ErrNotExist`. The matrix test feeds `dispatch` a `run` that returns a deferred
    outcome and asserts it is classified `Success == false` AND `Deferred == true`.
    GREEN:
    1. Add `Deferred bool` and `DeferredReason string` to `CellResult` (cell.go:114-141).
    2. In `RunCell`, after the fairness gate (Task 1) and BEFORE `benchsandbox.New`
       (cell.go:258), if `cfg.Mode == "baseline_rag"` set `res.Deferred = true`,
       `res.DeferredReason = "baseline_rag: real RAG arm deferred to Phase 83 (ABLATE-04)"`,
       and `return res, nil` (no sandbox, no daemon, no BuildResult, no writeDurable).
       Path-segment validation (cell.go:218-232) still runs first.
    3. Add `Deferred bool` to `CellOutcome` (matrix.go:65-74); in `runOneCell`
       (matrix.go:270-273) set `oc.Deferred = res.Deferred` and keep
       `oc.Success = err == nil && res.VerifyExitCode == 0 && res.ResultValid`
       (a deferred stub has ResultValid==false → not Success; Err is nil → not an infra
       error; Deferred==true is the distinct third outcome).
    Run the tests to GREEN. Detect baseline_rag by mode name (not a frontmatter
    marker) to keep the resolver change-free (Plan 01 kept MODE.md to two keys).
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestBaselineRag|TestDeferred|TestDispatch' -count=1 && go test ./bench/runtime/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/runtime/ -run 'TestBaselineRag|TestDeferred|TestDispatch' -count=1` exits 0.
    - A baseline_rag RunCell returns nil error, `Deferred == true`, `DeferredReason` containing "Phase 83", and writes NO result.v2.json on disk.
    - A deferred `CellOutcome` is `Success == false` and `Deferred == true` (neither succeeded nor infra-errored).
    - `go test ./bench/runtime/ -count=1` stays green (no regression in existing cell/matrix tests).
  </acceptance_criteria>
  <done>baseline_rag is a registered fail-closed stub producing no row and a distinct deferred outcome the matrix and smoke can assert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → RunCell | mode/task/benchmark names cross into path joins and the daemon profile |
| fairness contract → benchmark run | the contract gates whether a run is allowed to proceed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | path segments in RunCell (`cfg.Mode`/`cfg.Task`) | mitigate (existing control) | `validateCellKey` (cell.go:218-232) runs BEFORE any join, including for `baseline_rag`; no new path surface — inherits existing validation. |
| T-80-02 | Tampering (data integrity) | a stub mode silently scored as a real arm | mitigate | The fail-closed `baseline_rag` short-circuit (this plan) emits NO row and a distinct `Deferred` outcome so `runOneCell` never counts it as `Success` (matrix.go:272); the smoke asserts "registered + no row". The fairness gate refuses to run if any override is unjustified, preventing an unfair-budget run from being scored. |

No new external input surface; the fairness gate is a pure predicate over a
compile-time contract. baseline_rag detection is an internal mode-name comparison.
</threat_model>

<verification>
- `go build ./cmd/helix && go vet ./... && go test ./...` all green.
- Targeted: `go test ./bench/runtime/ -count=1` and the per-behavior `-run` filters above exit 0.
</verification>

<success_criteria>
- Fairness contract is enforced at runner startup, fatal on non-nil (criterion #3, D-04 layer 1).
- baseline_rag is fail-closed: no row, distinct deferred outcome (criterion #1, D-02).
- Only the no_semantic row carries the deferral marker (criterion #1, D-03).
- No fork of the cell path (D-01); existing cell/matrix tests unbroken.
</success_criteria>

<output>
Create `.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-03-SUMMARY.md` when done
</output>
