---
phase: 80-five-of-six-ablation-runners-fairness-enforcement
plan: 04
type: tdd
wave: 2
depends_on: ["80-01"]
files_modified:
  - bench/runners/contract_test.go
autonomous: true
requirements: [ABLATE-01, ABLATE-03]
must_haves:
  truths:
    - "An unconditional CI test asserts every registered runner's projected effective config matches DefaultContract"
    - "The contract test asserts the projected model_id == DefaultContract.ModelID for every mode and that DefaultContract.Validate() is clean"
    - "The test requires no live model and no network (hermetic)"
  artifacts:
    - path: "bench/runners/contract_test.go"
      provides: "TestEffectiveConfigMatchesContract (unconditional, projected-config assertion) + documentation of the live-agent enforcement gap"
      contains: "TestEffectiveConfigMatchesContract"
  key_links:
    - from: "bench/runners/contract_test.go"
      to: "DefaultContract.ModelID"
      via: "per-mode projected model_id assertion"
      pattern: "DefaultContract\\.ModelID"
---

<objective>
Add the unconditional CI contract test (`bench/runners/contract_test.go`) that
asserts every registered runner's *effective* config equals `DefaultContract`
(D-04 layer 2). Per RESEARCH Open Q1 scope A (recommended, within the "no new
infra" Phase 80 boundary): assert the *projected* config — `model_id ==
DefaultContract.ModelID` for every mode (the value `BuildResult` projects into the
row at result.go:152) — and that `DefaultContract.Validate()` is clean. Document
in the test that live-agent enforcement of `temperature`/`max_tokens`/
`system_prompt_hash`/`retry_policy`/`cache_policy` is wired-not-enforced until the
claude argv threads them (a future scope) — because `internal/eval/agent/claude.go`
buildArgv (76-89) sets only `--max-turns` today, so the other 5 fields have no live
producer. The test is hermetic and always-on regardless of `--agent`.

Purpose: criterion #3 — the always-on guarantee that no runner silently drifts from
the fairness contract. This sits alongside the existing `TestSystemPromptHashMatches`
drift gate and the new runtime startup gate (Plan 03).
Output: bench/runners/contract_test.go with the unconditional contract assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-CONTEXT.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-RESEARCH.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-PATTERNS.md
</context>

<artifacts_this_phase_produces>
[THIS PLAN]:
- `bench/runners/contract_test.go` with `TestEffectiveConfigMatchesContract` (and any helper asserting per-mode projected model_id == DefaultContract.ModelID)
Depends on: Plan 01's MODE.md files (the set of registered modes to iterate over).
Note: the existing `TestSystemPromptHashMatches` and `TestDefaultContractValidates`
already live in `fairness_contract_test.go` — do NOT duplicate them; reference/reuse.
(Other plans' artifacts listed in 80-01-PLAN.md.)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Unconditional effective-config CI contract test (RED→GREEN)</name>
  <files>bench/runners/contract_test.go</files>
  <read_first>
    - bench/runners/fairness_contract.go (DefaultContract at 84-100 — ModelID, Temperature, MaxTokens, SystemPromptHash, Retry, Cache, Overrides; Validate() at 107-114; the exact D-04 field set is FairnessContract at 66-74)
    - bench/runners/fairness_contract_test.go (TestDefaultContractValidates at 152-156 and TestSystemPromptHashMatches at 138-148 — the existing gates this test sits alongside; do NOT duplicate them. Same package `runners`, same testing style)
    - bench/runtime/result.go (BuildResult at 121-162 — model_id is the ONLY contract field projected into the row today, sourced from in.Fairness.ModelID at line 152; this is the "effective config" scope A asserts)
    - bench/runners/mode_resolver.go (ResolveProfile reads the MODE.md set — the registered modes the test iterates: your_agent_full, baseline_plain, no_lsp, no_structured_edit, your_agent_no_semantic, baseline_rag)
    - .planning/phases/80-.../80-RESEARCH.md § "The Fairness Effective Config Gap" (Open Q1 scope A; Pitfall 5 — do NOT assert a live argv value that no producer sets)
  </read_first>
  <behavior>
    - Test 1 (RED): `TestEffectiveConfigMatchesContract` iterates the 6 registered
      bench modes; for each it resolves the profile (ResolveProfile must not error)
      and asserts the *projected* effective `model_id` equals
      `DefaultContract.ModelID` (the value BuildResult projects). Since model_id is
      sourced uniformly from `DefaultContract.ModelID` for every mode, the assertion
      is that the contract is the single source (no per-mode override of ModelID
      exists / `DefaultContract.Overrides` carry no ModelID drift), and
      `DefaultContract.Validate()` is nil. The test fails if a mode does not resolve
      or if the contract fails Validate().
    - The test is unconditional: no `--agent` gate, no live model, no network.
  </behavior>
  <action>
    RED: create `bench/runners/contract_test.go` in package `runners` with
    `TestEffectiveConfigMatchesContract`. Build a table of the 6 registered mode names
    (`your_agent_full`, `baseline_plain`, `no_lsp`, `no_structured_edit`,
    `your_agent_no_semantic`, `baseline_rag`). For each: call `ResolveProfile(mode)`
    and require no error (the mode is registered). Then assert the effective config
    every runner executes under is `DefaultContract`: specifically that
    `DefaultContract.ModelID` is the dated snapshot every row's `model_id` projects
    from (assert it is non-empty and equal to itself across modes — the single-source
    invariant), and `require DefaultContract.Validate() == nil`. Add a top-of-test
    doc comment recording that the other 5 contract fields (temperature, max_tokens,
    system_prompt_hash, retry_policy, cache_policy) are wired-not-enforced on the live
    claude argv until a future scope threads them (`internal/eval/agent/claude.go:76-89`
    sets only `--max-turns`); the SystemPromptHash drift gate
    (`TestSystemPromptHashMatches`, fairness_contract_test.go:138) and this projected
    `model_id` assertion are the always-on guarantees this phase ships.
    GREEN: the test passes against the committed `DefaultContract` and the 6 MODE.md
    dirs (created in Plan 01). Confirm green.
    Do NOT thread contract fields into the claude argv (Open Q1 scope B is out of the
    "no new infra" boundary). Do NOT duplicate `TestSystemPromptHashMatches` or
    `TestDefaultContractValidates`.
  </action>
  <verify>
    <automated>go test ./bench/runners/ -run 'TestEffectiveConfigMatchesContract' -count=1 && go test ./bench/runners/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/runners/ -run TestEffectiveConfigMatchesContract -count=1` exits 0.
    - The test iterates all 6 registered modes; each resolves via `ResolveProfile` without error.
    - The test asserts the projected `model_id == DefaultContract.ModelID` (single-source invariant) and `DefaultContract.Validate() == nil`, with no live-model dependency.
    - A doc comment records the live-agent enforcement gap for the other 5 fields (Pitfall 5 / Open Q1 scope A).
    - `go test ./bench/runners/ -count=1` stays green (existing gates unbroken; no duplication).
  </acceptance_criteria>
  <done>An unconditional, hermetic CI contract test asserts every runner's projected effective config matches DefaultContract; the live-agent enforcement gap is documented, not faked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fairness contract → every runner | the single compile-time config source all runners must match |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-02 | Tampering (data integrity) | a runner silently drifting from the fairness contract | mitigate | This unconditional CI test asserts every registered runner's projected effective config (`model_id`) equals `DefaultContract` and the contract `Validate()`s; combined with the existing `TestSystemPromptHashMatches` drift gate, contract drift becomes a hard CI failure. Pitfall 5 explicitly avoided: the test asserts only the projected field that has a real producer, documenting the rest as a known gap rather than a vacuous pass. |

This plan adds a test file only — no production code, no external surface, no network.
</threat_model>

<verification>
- `go build ./cmd/helix && go vet ./... && go test ./...` all green.
- Targeted: `go test ./bench/runners/ -run TestEffectiveConfigMatchesContract -count=1` exits 0.
</verification>

<success_criteria>
- An unconditional CI contract test guards effective-config drift (criterion #3, D-04 layer 2).
- The test is hermetic (no model, no network) and iterates all 6 modes.
- The live-agent enforcement gap is documented, not silently expanded into scope B.
</success_criteria>

<output>
Create `.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-04-SUMMARY.md` when done
</output>
