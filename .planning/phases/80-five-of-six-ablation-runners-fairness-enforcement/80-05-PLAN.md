---
phase: 80-five-of-six-ablation-runners-fairness-enforcement
plan: 05
type: tdd
wave: 3
depends_on: ["80-02", "80-03"]
files_modified:
  - bench/runtime/deltas.go
  - bench/runtime/deltas_test.go
  - bench/runtime/matrix.go
  - bench/runtime/matrix_test.go
  - cmd/helix-bench/main.go
  - cmd/helix-bench/run_cmd_test.go
autonomous: true
requirements: [ABLATE-01]
must_haves:
  truths:
    - "After a multi-mode run, exactly 3 deltas (full−baseline_plain, full−no_lsp, full−no_structured_edit) are computed per task"
    - "The 3 deltas surface in the per-mode result.v2.json rows (re-validated, still schema-valid)"
    - "A multi-mode scripted smoke produces 4 real rows + 1 partial (no_semantic) + 0 baseline_rag rows (registered + deferred)"
    - "Tasks missing any of the 4 real modes are skipped (logged), not computed against a nil baseline"
  artifacts:
    - path: "bench/runtime/deltas.go"
      provides: "minimal single-run 3-delta helper (NOT the Phase 82 aggregator)"
      contains: "func "
    - path: "cmd/helix-bench/main.go"
      provides: "post-RunMatrix delta-pass invocation in runBench"
      contains: "delta"
  key_links:
    - from: "cmd/helix-bench/main.go runBench"
      to: "bench/runtime delta pass"
      via: "post-RunMatrix call over Summary.Outcomes after wg.Wait()"
      pattern: "Outcomes"
    - from: "bench/runtime/deltas.go"
      to: "per-mode result.v2.json rows"
      via: "read rows by ResultPath, write ablation_deltas back, re-Validate"
      pattern: "ablation_deltas"
---

<objective>
Add the minimal in-phase post-run 3-delta pass (D-05) and the multi-mode scripted
smoke that proves the five-of-six matrix. The delta pass runs at the matrix tier
(the only layer that sees all modes for a task) AFTER `RunMatrix` returns: it groups
written rows by task, and for each task with all 4 real-mode rows present
(`your_agent_full`, `baseline_plain`, `no_lsp`, `no_structured_edit`) computes
EXACTLY 3 deltas — `full − baseline_plain`, `full − no_lsp`,
`full − no_structured_edit` — on the comparable metrics, and surfaces them in the
per-mode result rows under a new open `ablation_deltas` property (re-validated via
`runtime.Validate`). Single-run, three-fixed-deltas only — NOT the Phase 82
aggregator (no BCa/pass@k/variance/leaderboard).

Then add the multi-mode scripted smoke asserting the full five-of-six contract:
4 real rows + 1 partial (`your_agent_no_semantic`, `ablation_status:
guarantee_pending_phase_81`) + `baseline_rag` registered-and-fail-closed (NO row,
deferred outcome).

Purpose: criterion #4 (deltas compute correctly + surface in per-mode rows) and the
phase-level smoke assertion (criterion #1, "five-of-six").
Output: deltas helper + matrix/cmd wiring + multi-mode smoke, RED→GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-CONTEXT.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-RESEARCH.md
@.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-PATTERNS.md
@bench/runtime/result.go
@bench/runtime/matrix.go
</context>

<artifacts_this_phase_produces>
[THIS PLAN]:
- `bench/runtime/deltas.go` — the single-run 3-delta helper (compute + write-back) and its tests
- `ablation_deltas` open property written into each per-mode result.v2.json row
- post-`RunMatrix` delta-pass invocation in `runBench` (cmd/helix-bench/main.go)
- multi-mode scripted smoke test (4 real + 1 partial + 1 fail-closed) in bench/runtime/matrix_test.go or cmd/helix-bench/run_cmd_test.go
Depends on: Plan 03 (rows written with the gate/fail-close/ablation_status wiring; CellResult.Deferred + CellOutcome.Deferred), Plan 02 (ablation_status field + the additive-property pattern).
(Other plans' artifacts listed in 80-01-PLAN.md.)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: 3-delta compute + per-mode-row write-back helper (RED→GREEN)</name>
  <files>bench/runtime/deltas.go, bench/runtime/deltas_test.go</files>
  <read_first>
    - bench/runtime/result.go (resultDoc at 90-114 — the row shape to read/extend; the metrics object lives under `metrics` per the Phase 79 schema; BuildResult/Validate at 121-209 — re-Validate after write-back; mirror the additive open-property pattern used for ablation_status in Plan 02)
    - bench/runtime/matrix.go (Summary at 78-82; CellOutcome at 65-74 with Result.ResultPath set at cell.go:242 — group rows by oc.Cell.Task; read each row via encoding/json from ResultPath)
    - bench/schema/result.v2.schema.json (metrics object at 78-177 — the comparable numeric metrics the delta operates on, e.g. tokens_input/tokens_output/tool_calls/files_modified/edit_locality; ablation_deltas lands as a new open top-level property, additive, no schema bump)
    - .planning/phases/80-.../80-RESEARCH.md § "Minimal 3-delta pass" + Pitfall 3 (skip tasks missing a mode; run AFTER wg.Wait)
  </read_first>
  <behavior>
    - Test 1 (RED): given 4 synthetic in-memory rows (the 4 real modes) for one task
      with known numeric metrics, the helper computes exactly 3 deltas
      (full−baseline_plain, full−no_lsp, full−no_structured_edit) with correct
      arithmetic on the comparable metrics (e.g. tokens_input delta == full.tokens_input
      − baseline_plain.tokens_input). Assert the exact delta values.
    - Test 2 (RED): the helper writes the computed deltas into each of the 4 per-mode
      rows under `ablation_deltas`, and each written row still passes `runtime.Validate`.
    - Test 3 (RED): a task whose set is missing one of the 4 real modes is SKIPPED
      (no panic, no nil-baseline arithmetic) and reported as skipped.
  </behavior>
  <action>
    RED: add `bench/runtime/deltas_test.go` encoding the three behaviors above on
    synthetic rows (build rows via BuildResult or fixture JSON, write to a temp dir,
    run the helper, read back, assert delta arithmetic + schema validity + skip
    behavior).
    GREEN: create `bench/runtime/deltas.go` with a small helper (e.g.
    `ComputeAndWriteDeltas(outcomes []CellOutcome) error` or a pure
    `computeTaskDeltas(rows map[string]resultRow) (deltas, ok)` plus a thin writer):
    1. Group outcomes by `oc.Cell.Task`.
    2. For each task, gather the rows for the 4 real modes by reading their
       `ResultPath` (encoding/json). If any of the 4 is missing (no row on disk, e.g.
       baseline_rag has none and is NOT one of the 4 — it is excluded by design), SKIP
       the task and log it (Pitfall 3). The no_semantic partial row is also NOT one of
       the 3 deltas (the 3 deltas are full vs baseline_plain/no_lsp/no_structured_edit).
    3. Compute exactly the 3 deltas on the comparable numeric metrics
       (full − baseline_plain, full − no_lsp, full − no_structured_edit). Keep the
       metric set small and clearly scoped (e.g. tokens_input, tokens_output,
       tool_calls, files_modified, edit_locality — those present in the metrics object;
       skip metrics that are null in either operand).
    4. Write the 3 deltas back into EACH of the 4 per-mode rows under a new open
       top-level property `ablation_deltas` (re-marshal + `runtime.Validate` + atomic
       writeDurable), so criterion #4 ("surface in the per-mode result rows") is met
       literally. Optionally also emit a sibling `<OutDir>/<task>/deltas.json`.
    Keep it single-run, 3-fixed-deltas — do NOT add BCa/pass@k/variance/leaderboard
    (that is Phase 82, Anti-Pattern). Run tests to GREEN.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestAblationDeltas|TestDeltas' -count=1 && go test ./bench/runtime/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/runtime/ -run 'TestAblationDeltas|TestDeltas' -count=1` exits 0.
    - The helper computes EXACTLY 3 deltas with correct arithmetic on the comparable metrics.
    - Each per-mode row, after write-back, contains `ablation_deltas` and still passes `runtime.Validate`.
    - A task missing one of the 4 real modes is skipped (no panic, no nil-baseline arithmetic) and logged.
    - No BCa/pass@k/variance/leaderboard logic is introduced (scope stays single-run/3-delta).
  </acceptance_criteria>
  <done>The minimal 3-delta helper computes the fixed deltas and surfaces them in the per-mode rows, schema-valid, skipping incomplete tasks.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Wire the delta pass into runBench + multi-mode five-of-six smoke (RED→GREEN)</name>
  <files>cmd/helix-bench/main.go, cmd/helix-bench/run_cmd_test.go, bench/runtime/matrix.go, bench/runtime/matrix_test.go</files>
  <read_first>
    - cmd/helix-bench/main.go (runBench at 177-257 — RunMatrix returns Summary at 230; insert the delta pass AFTER that return and after the per-cell error surfacing loop at 242-247, before the final completion print; --modes flag default at 148)
    - bench/runtime/matrix.go (RunMatrix/dispatch at 160-221 — wg.Wait() at 212 is the barrier; the delta pass MUST run after RunMatrix returns; CellOutcome.Deferred added in Plan 03)
    - cmd/helix-bench/run_cmd_test.go (existing run-subcommand test scaffolding — the in-process command-execution + scripted-agent smoke style to extend for the multi-mode run)
    - bench/runtime/cell.go (CellResult.Deferred/DeferredReason from Plan 03; the result path layout <OutDir>/<task>/<mode>/<run_index>/result.v2.json from cellDurablePaths at 159-167)
    - bench/datasets/internal-toolbench/go/ (the seed task dirs; the smoke drives one task across all 6 modes with --agent=scripted)
  </read_first>
  <behavior>
    - Test 1 (delta wiring): a multi-mode `runBench` (or RunMatrix + delta pass) over
      the 4 real modes on one task writes rows that, after the pass, carry
      `ablation_deltas`. Assert the delta pass ran after the matrix completed.
    - Test 2 (five-of-six smoke): a scripted multi-mode run across all 6 modes on one
      seed task produces exactly 4 real result.v2.json rows (your_agent_full,
      baseline_plain, no_lsp, no_structured_edit), 1 partial row
      (your_agent_no_semantic with `ablation_status: guarantee_pending_phase_81`), and
      0 rows for baseline_rag (its cell outcome is Deferred==true, Success==false, no
      file at its ResultPath).
  </behavior>
  <action>
    RED: add the smoke test to `cmd/helix-bench/run_cmd_test.go` (or
    `bench/runtime/matrix_test.go` if a daemon-free harness is needed) driving the
    scripted agent across all 6 modes on one seed task. Assert: row count == 4 real +
    1 partial; the no_semantic row's `ablation_status == guarantee_pending_phase_81`;
    NO file at the baseline_rag ResultPath; the baseline_rag outcome is `Deferred`. Add
    a delta-wiring assertion that the 4 real rows carry `ablation_deltas` after the run.
    GREEN:
    1. In `runBench` (cmd/helix-bench/main.go), after `RunMatrix` returns its Summary
       and after the per-cell error loop (~line 247), invoke the Plan-05 delta pass
       over `summary.Outcomes` (Task 1's helper). Surface a clear log line. The delta
       pass runs strictly after the matrix barrier (Pitfall 3).
    2. Ensure the run subcommand can drive multiple `--modes` (the flag is already
       repeatable at main.go:148) so the smoke passes all 6 modes.
    3. If the smoke needs it, confirm the matrix surfaces deferred cells distinctly in
       the operator log (e.g. "deferred: baseline_rag") without counting them as
       failures.
    Run tests to GREEN. Keep the smoke hermetic (scripted agent, no model, no network);
    target it to stay within the bench-quick budget shape.
  </action>
  <verify>
    <automated>go test ./cmd/helix-bench/ -run 'TestRun|TestSmoke|TestFiveOfSix' -count=1 && go build ./cmd/helix ./cmd/helix-bench && go vet ./... && go test ./... </automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/helix-bench/ -count=1` exits 0 (the multi-mode smoke + delta wiring pass).
    - The five-of-six smoke produces exactly 4 real rows + 1 partial row + 0 baseline_rag rows.
    - The no_semantic row carries `ablation_status: guarantee_pending_phase_81`.
    - The baseline_rag cell is `Deferred==true`, `Success==false`, and has no file at its ResultPath.
    - The 4 real rows carry `ablation_deltas` after the run.
    - `go build ./cmd/helix && go vet ./... && go test ./...` all green.
  </acceptance_criteria>
  <done>The delta pass runs post-matrix in runBench and the five-of-six scripted smoke proves the full mode matrix end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable result rows → delta read-back | the delta pass re-reads + re-writes durable JSON rows |
| operator CLI → multi-mode run | --modes list crosses into the matrix expansion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-02 | Tampering (data integrity) | a deferred/partial row mixed into the delta arithmetic | mitigate | The delta pass computes only the 3 fixed deltas over the 4 REAL modes and SKIPS tasks missing any of them (Pitfall 3) — baseline_rag (no row) and the no_semantic partial are excluded from the delta operands by construction. Re-`runtime.Validate` after write-back ensures the row stays schema-valid; atomic `writeDurable` (cell.go:668) prevents torn writes. |
| T-80-01 | Tampering | --modes / task names → matrix path joins | mitigate (existing control) | `validateMatrixID`/`validateCellKey` already gate every segment before any join (matrix.go:92, cell.go:218) — no new path surface. |

The delta pass reads/writes only harness-produced durable artifacts under the
run-scoped OutDir; no external input, no network. The smoke is hermetic (scripted).
</threat_model>

<verification>
- `go build ./cmd/helix && go vet ./... && go test ./...` all green.
- Targeted: `go test ./bench/runtime/ -run 'TestAblationDeltas|TestDeltas' -count=1` and
  `go test ./cmd/helix-bench/ -count=1` exit 0.
- Phase gate: a multi-mode scripted smoke produces 4 real rows + 1 partial + 0 baseline_rag rows.
</verification>

<success_criteria>
- Exactly 3 deltas compute correctly and surface in the per-mode rows (criterion #4, D-05).
- The delta pass is single-run / 3-fixed-deltas only (no Phase 82 aggregator creep).
- The five-of-six scripted smoke proves 4 real + 1 partial + 1 fail-closed (criterion #1).
- Full build/vet/test green; bench smoke hermetic.
</success_criteria>

<output>
Create `.planning/phases/80-five-of-six-ablation-runners-fairness-enforcement/80-05-SUMMARY.md` when done
</output>
