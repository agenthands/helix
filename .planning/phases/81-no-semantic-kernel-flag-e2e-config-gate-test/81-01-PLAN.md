---
phase: 81-no-semantic-kernel-flag-e2e-config-gate-test
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/obs/metrics.go
  - internal/obs/metrics_labels_test.go
  - internal/semantic/store/effective.go
  - internal/semantic/store/effective_graph.go
  - internal/semantic/store/overlay.go
  - internal/semantic/store/reads_counter_test.go
autonomous: true
requirements: [ABLATE-06]
must_haves:
  truths:
    - "A semantic-store DuckDB read increments helix_semantic_store_reads_total"
    - "When no read occurs, helix_semantic_store_reads_total stays at 0 (the assertion target for criterion #2)"
  artifacts:
    - path: internal/obs/metrics.go
      provides: "helix_semantic_store_reads_total counter registration + SemanticStoreReadsInc helper"
      contains: "helix_semantic_store_reads_total"
    - path: internal/semantic/store/reads_counter_test.go
      provides: "Net-new read-counter increment/stays-0 test"
      exports: ["TestReadCounter"]
  key_links:
    - from: internal/semantic/store (DuckDB read chokepoint)
      to: internal/obs metrics SemanticStoreReadsInc
      via: "counter increment at every store read"
      pattern: "SemanticStoreReadsInc"
---

<objective>
Add the net-new `helix_semantic_store_reads_total` Prometheus counter (per D-05, this counter does NOT exist today — the `helix_semantic_*` family at metrics.go:334-504 covers only writes/maintenance) and instrument it at the single DuckDB read chokepoint inside `internal/semantic/store/` so every semantic-store read increments it. This counter is the verification target the Phase 81 bench cell asserts == 0 on the `no_semantic` arm (criterion #2). Without it, D-05's assertion would be vacuous (Pitfall 1).

Purpose: A faithful "a read happened" signal. The build-but-block gate (D-04, Plan 04) is the structural guarantee; this counter is the independent runtime verification.
Output: Registered counter + `SemanticStoreReadsInc` helper + chokepoint instrumentation + a test proving it increments on a real read and stays 0 when no read occurs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-RESEARCH.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Locate the single DuckDB read chokepoint</name>
  <files>internal/semantic/store/effective.go, internal/semantic/store/effective_graph.go, internal/semantic/store/overlay.go</files>
  <read_first>
    - internal/semantic/store/duckdb.go (only schema-version read at duckdb.go:310 — NOT the chokepoint)
    - internal/semantic/store/effective.go, effective_graph.go, overlay.go (the actual SELECT execution paths surfaced via rankStoreAdapter / retrievalAdapter / integSemanticLookup methods RankFiles / ExpandFrom)
    - 81-RESEARCH.md §"Open Questions" Q1 (chokepoint discovery guidance) and §"Code Examples" (new read counter shape)
  </read_first>
  <action>
    Wave-0 spike from RESEARCH Open Question 1. Use SMTC (`mcp__smtc__find_call_sites_matching` / `get_callers`) and grep `QueryContext|QueryRowContext|\.Query(` across internal/semantic/store/*.go (non-test) to identify the lowest-level read-query helper that ALL semantic reads funnel through — RankFiles, ExpandFrom, retrieval, graph-edge validation. Prefer ONE chokepoint (a shared `store.Query`/read helper) over per-method instrumentation so EVERY read increments (RESEARCH recommendation: single faithful "any read" signal). Document the exact file:line of the chokepoint(s) chosen in the SUMMARY. If reads do not funnel through a single helper, enumerate the minimal complete set (≤3) and instrument each — the goal is zero false negatives.
  </action>
  <verify>
    <automated>grep -rln "QueryContext\|QueryRowContext\|\.Query(" internal/semantic/store/*.go | grep -v _test</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY names the exact file:line(s) of the chosen read chokepoint(s)
    - The chosen site(s) provably cover RankFiles + ExpandFrom + retrieval read paths (named consumers in ABLATE-06)
  </acceptance_criteria>
  <done>The read chokepoint (single helper or minimal complete set) is identified with file:line and justified as covering all semantic read paths.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 1 (RED→GREEN→REFACTOR): helix_semantic_store_reads_total counter + chokepoint instrumentation</name>
  <files>internal/obs/metrics.go, internal/obs/metrics_labels_test.go, internal/semantic/store/reads_counter_test.go, internal/semantic/store/effective.go (or the chokepoint file from Task 0)</files>
  <read_first>
    - internal/obs/metrics.go:334-350 (helix_semantic_store_open_total counter registration — the exact register shape to mirror) and :701-722 (SemanticStoreQuarantineInc/SemanticStoreOpenInc helper shape)
    - internal/obs/metrics_labels_test.go:52 (the bounded-label allowlist entry for helix_semantic_store_open_total — a new metric name needs a sibling allowlist entry or the labels test fails)
    - the chokepoint file from Task 0
    - 81-PATTERNS.md §"internal/obs/metrics.go + internal/semantic/store/duckdb.go (NET-NEW read counter)" (analog: SemanticStoreOpen counter + SemanticStoreOpenInc helper)
  </read_first>
  <behavior>
    - Test 1: calling the instrumented store read path once increments helix_semantic_store_reads_total by exactly 1 (use prometheus testutil.ToFloat64 or CollectAndCount against the registered counter)
    - Test 2: with no read performed, the counter reads 0 (the bench-cell assertion's structural target)
    - Test 3 (labels guard): the new metric name is present in the metrics_labels_test allowlist so `go test ./internal/obs/...` stays green
  </behavior>
  <action>
    RED: write internal/semantic/store/reads_counter_test.go (TestReadCounter) asserting the increment-on-read and stays-0 behaviors above; run `go test ./internal/semantic/store/ -run TestReadCounter -count=1` and confirm it FAILS (counter does not exist). Commit `test(81-01): add failing read-counter test`.
    GREEN: register `helix_semantic_store_reads_total` in the same block as helix_semantic_store_open_total (metrics.go:334) — keep labels minimal (RESEARCH recommends a faithful "any read" signal; prefer a low-cardinality label set or labelless; mirror the open counter's workspace_label only if a workspace handle is available at the read site). Add the `SemanticStoreReadsInc(...)` helper (mirror SemanticStoreOpenInc at metrics.go:713). Add the new metric name to the metrics_labels_test.go:52 allowlist. Emit `SemanticStoreReadsInc` at the Task-0 chokepoint so EVERY read increments. Run the test — MUST pass. Commit `feat(81-01): add helix_semantic_store_reads_total + instrument DuckDB read chokepoint`.
    REFACTOR (if needed): if instrumentation duplicates across the minimal set, extract a single wrapped read helper. Tests stay green. Commit `refactor(81-01): consolidate semantic read counter increment` only if changed.
    Do NOT instrument write/extraction/maintenance paths — this counter is reads-only (asserting a write counter is a false-negative source per RESEARCH Anti-Patterns).
  </action>
  <verify>
    <automated>go test ./internal/semantic/store/ -run TestReadCounter -count=1 && go test ./internal/obs/... -count=1 && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/semantic/store/ -run TestReadCounter -count=1` passes
    - `grep -c "helix_semantic_store_reads_total" internal/obs/metrics.go` ≥ 1
    - `go test ./internal/obs/... -count=1` passes (labels allowlist updated)
    - git log shows a `test(81-01):` commit preceding the `feat(81-01):` commit (RED→GREEN gate)
    - `go vet ./...` clean
  </acceptance_criteria>
  <done>The read counter is registered, instrumented at the chokepoint, increments on a real read, reads 0 with no read, and the obs labels test plus go vet are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ablation contract → bench result | A semantic read leaking onto the no_semantic arm silently corrupts benchmark fairness (integrity threat, not external attacker) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01-01 | Information Disclosure (vs. ablation contract) | semantic-store read path not covered by the counter | mitigate | Task 0 spike enumerates the complete read chokepoint set so no read path bypasses instrumentation (zero false negatives) |
| T-81-01-02 | Tampering | asserting on the wrong counter (a write/extraction counter reads 0 trivially) | mitigate | Counter is reads-only by construction; instrumented only at SELECT read sites, never at write/maintenance paths |
| T-81-01-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages added this phase (RESEARCH Package Legitimacy Audit: not applicable); no install task exists |
</threat_model>

<verification>
- `go test ./internal/semantic/store/ -run TestReadCounter -count=1` green
- `go test ./internal/obs/... -count=1` green
- `go vet ./...` clean
- Read-counter increments on a real store read; stays 0 with no read
</verification>

<success_criteria>
- `helix_semantic_store_reads_total` registered with a `SemanticStoreReadsInc` helper
- Counter emitted at the DuckDB read chokepoint covering RankFiles/ExpandFrom/retrieval
- TestReadCounter proves increment-on-read and stays-0
- RED→GREEN commit sequence present; `go vet ./...` and `go test ./...` green
</success_criteria>

<output>
Create `.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-01-SUMMARY.md` when done. Record the exact chokepoint file:line(s) chosen in Task 0 (Plan 04 and Plan 05 depend on knowing the read path).
</output>
