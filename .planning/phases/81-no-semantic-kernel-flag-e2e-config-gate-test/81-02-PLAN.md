---
phase: 81-no-semantic-kernel-flag-e2e-config-gate-test
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/semantic/config.go
  - internal/profile/profile.go
  - internal/cli/root.go
  - internal/profile/profiles/bench-no-semantic.yaml
  - internal/profile/bench_profiles_test.go
autonomous: true
requirements: [ABLATE-06]
must_haves:
  truths:
    - "semantic_index.bench_disabled is a distinct koanf-bound bool, independent of SemanticIndex.Enabled (D-01)"
    - "bench-no-semantic.yaml carries disable_semantic_subsystem: true (D-03)"
    - "CLI --disable-semantic-subsystem sets semantic_index.bench_disabled with only-when-set semantics (D-03)"
    - "Precedence is CLI override > profile YAML field > default-off (D-03)"
  artifacts:
    - path: internal/semantic/config.go
      provides: "BenchDisabled bool koanf:\"bench_disabled\" — the distinct ablation gate (D-01)"
      contains: "BenchDisabled"
    - path: internal/profile/profile.go
      provides: "DisableSemanticSubsystem bool yaml:\"disable_semantic_subsystem\""
      contains: "DisableSemanticSubsystem"
    - path: internal/cli/root.go
      provides: "--disable-semantic-subsystem CLI override flag (only-when-set)"
      contains: "disable-semantic-subsystem"
    - path: internal/profile/profiles/bench-no-semantic.yaml
      provides: "disable_semantic_subsystem: true"
      contains: "disable_semantic_subsystem"
  key_links:
    - from: internal/cli/root.go
      to: semantic_index.bench_disabled koanf override
      via: "overrides[\"semantic_index.bench_disabled\"] = true when flag set"
      pattern: "semantic_index.bench_disabled"
    - from: internal/profile/profiles/bench-no-semantic.yaml
      to: internal/profile/profile.go DisableSemanticSubsystem
      via: "yaml round-trip"
      pattern: "disable_semantic_subsystem"
---

<objective>
Land the config surface for the `disable_semantic_subsystem` gate: a distinct `semantic_index.bench_disabled` koanf field (D-01 — NOT a reuse of `SemanticIndex.Enabled`), a `DisableSemanticSubsystem` profile field, a `--disable-semantic-subsystem` CLI override with only-when-set semantics, the `disable_semantic_subsystem: true` field on `bench-no-semantic.yaml` (D-03), and the FLIP of the Phase 76 deferral assertion in `bench_profiles_test.go:142`. This establishes the precedence chain (CLI > profile YAML > default-off, D-03) that Plan 04 resolves at the daemon composition root.

Purpose: A distinct, greppable gate symbol decoupled from the production feature flag (so criterion #2's zero-read test is non-vacuous — D-01) and a greppable anchor for the Plan 03 vet analyzer.
Output: Config field + profile field + CLI flag + YAML field + flipped assertion, mirroring Phase 76's `disable_lsp_subsystem` end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-RESEARCH.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BenchDisabled config field + DisableSemanticSubsystem profile field + CLI override flag</name>
  <files>internal/semantic/config.go, internal/profile/profile.go, internal/cli/root.go</files>
  <read_first>
    - internal/semantic/config.go:27-31 (Config.Enabled — the sibling field; BenchDisabled goes adjacent) and the file header comment (config.go:18-26) re: koanf key lockstep handling / SPEC §25 coverage tests
    - internal/profile/profile.go:46-51 (DisableLSPSubsystem — verbatim doc-comment shape to copy, swap LSP→Semantic) and :53-58 (DisableStructuredEditSubsystem sibling)
    - internal/cli/root.go:75-76 (flag decl), :151-152 (read in runDaemon), :172-181 (only-when-set override apply) — the three --disable-lsp-subsystem sites
    - 81-PATTERNS.md §"internal/semantic/config.go", §"internal/profile/profile.go", §"internal/cli/root.go" (the koanf-path asymmetry note: LSP uses top-level disable_lsp_subsystem; the semantic gate's koanf key is NESTED semantic_index.bench_disabled)
  </read_first>
  <action>
    Per D-01: add `BenchDisabled bool \`koanf:"bench_disabled"\`` to internal/semantic/config.go's Config struct, adjacent to Enabled, with a doc-comment stating it is a DISTINCT ablation gate (NOT a reuse of Enabled), that under it the store/bundle is STILL built (D-04) and reads are forced to Noop, default false = ENABLED, resolved at the daemon composition root OR'd with the profile field + CLI override (D-02/D-03). Per D-03: add `DisableSemanticSubsystem bool \`yaml:"disable_semantic_subsystem"\`` to internal/profile/profile.go type Profile, mirroring DisableLSPSubsystem's doc comment. Per D-03: in internal/cli/root.go add `rootCmd.Flags().Bool("disable-semantic-subsystem", false, "...")` (mirror line 75-76), read it in runDaemon (mirror 151-152), and apply ONLY when set: `overrides["semantic_index.bench_disabled"] = true` (mirror 172-181 — note the NESTED koanf path, distinct from the top-level disable_lsp_subsystem path). If a coverage/labels test asserts the koanf key set verbatim (per the config.go header comment), update it in lockstep.
  </action>
  <verify>
    <automated>go build ./cmd/helix && go test ./internal/semantic/... ./internal/profile/... ./internal/cli/... -count=1 && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BenchDisabled" internal/semantic/config.go` ≥ 1 with `koanf:"bench_disabled"` tag present
    - `grep -c "DisableSemanticSubsystem" internal/profile/profile.go` ≥ 1 with `yaml:"disable_semantic_subsystem"` tag present
    - `grep -c "disable-semantic-subsystem" internal/cli/root.go` ≥ 2 (decl + read)
    - `grep -c "semantic_index.bench_disabled" internal/cli/root.go` ≥ 1 (the nested override path)
    - `go build ./cmd/helix` succeeds; `go vet ./...` clean
  </acceptance_criteria>
  <done>The distinct config field, profile field, and only-when-set CLI override all exist with correct koanf/yaml tags and the project builds + vets clean.</done>
</task>

<task type="auto">
  <name>Task 2: Set disable_semantic_subsystem on bench-no-semantic.yaml + FLIP the deferral assertion</name>
  <files>internal/profile/profiles/bench-no-semantic.yaml, internal/profile/bench_profiles_test.go</files>
  <read_first>
    - internal/profile/profiles/bench-no-semantic.yaml (header lines 1-8 currently say "this arm sets NO kernel flag" — must be rewritten) and bench-no-lsp.yaml / bench-no-structured-edit.yaml (which carry their disable field as the template)
    - internal/profile/bench_profiles_test.go:134 (the "kernel guard is Phase 81" comment) and :141-143 (the two assert.False on DisableLSPSubsystem/DisableStructuredEditSubsystem with the D-11/D-12 message) and :159 (the sibling POSITIVE assert.True for bench-no-structured-edit — the shape to mirror)
    - 81-PATTERNS.md §"internal/profile/profiles/bench-no-semantic.yaml" and §"internal/profile/bench_profiles_test.go:142 (assertion FLIP)"
  </read_first>
  <action>
    Add `disable_semantic_subsystem: true` to internal/profile/profiles/bench-no-semantic.yaml and rewrite the header comment to state Phase 81 now lands the kernel flag (drop the Phase 76 D-11/D-12 "tool-filter-only" caveat). In internal/profile/bench_profiles_test.go: ADD `assert.True(t, p.DisableSemanticSubsystem, "bench-no-semantic must declare disable_semantic_subsystem: true")` mirroring the line-159 positive assert; this is the deliberate behavior change flagged in VALIDATION.md Wave-0 (the assertion FLIP). KEEP the existing `assert.False` on DisableLSPSubsystem and DisableStructuredEditSubsystem (this arm still ablates ONLY semantic). Update the line-134 comment to reflect that the kernel guard has now landed (no longer "Phase 81 pending"). Do NOT remove the tool-filter exclude_tools list — the 10 semantic tools stay excluded from the agent surface AND the kernel gate is added (defense-in-depth).
  </action>
  <verify>
    <automated>go test ./internal/profile/ -run TestBenchNoSemanticProfile -count=1 && go test ./internal/profile/... -count=1 && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "disable_semantic_subsystem: true" internal/profile/profiles/bench-no-semantic.yaml` == 1
    - `grep -c "disable_semantic_subsystem" internal/profile/bench_profiles_test.go` ≥ 1 (the new positive assert)
    - `go test ./internal/profile/ -run TestBenchNoSemanticProfile -count=1` passes with the FLIPPED (true) assertion
    - `go test ./internal/profile/... -count=1` fully green; `go vet ./...` clean
  </acceptance_criteria>
  <done>The profile YAML carries the gate field, the deferral assertion is flipped to assert true, LSP/structured-edit asserts remain false, and profile tests + go vet are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile/CLI config → daemon resolution | A misconfigured gate (silently off on the no_semantic arm) would let semantic reads leak — integrity threat to ablation fairness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-02-01 | Tampering | reusing SemanticIndex.Enabled instead of a distinct gate | mitigate | D-01: a DISTINCT BenchDisabled field; the assertion flip in bench_profiles_test.go locks the YAML to disable_semantic_subsystem: true so a future edit that drops it fails CI |
| T-81-02-02 | Tampering | blank daemon invocation wiping a profile gate value | mitigate | D-03 only-when-set override semantics (mirror root.go:172-181) — a blank invocation cannot clear the profile field |
| T-81-02-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages added (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `go test ./internal/profile/ -run TestBenchNoSemanticProfile -count=1` green with flipped assertion
- `go build ./cmd/helix` succeeds
- `go test ./internal/semantic/... ./internal/profile/... ./internal/cli/... -count=1` green
- `go vet ./...` clean
</verification>

<success_criteria>
- Distinct `BenchDisabled` (koanf:"bench_disabled") field on the semantic Config (D-01)
- `DisableSemanticSubsystem` (yaml) profile field
- `--disable-semantic-subsystem` CLI override with only-when-set semantics → `semantic_index.bench_disabled`
- `bench-no-semantic.yaml` carries `disable_semantic_subsystem: true`
- `bench_profiles_test.go:142` assertion flipped false→true (deliberate behavior change)
- `go vet ./...` and `go test ./...` green
</success_criteria>

<output>
Create `.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-02-SUMMARY.md` when done. Record the exact koanf key path (`semantic_index.bench_disabled`) and the profile field name — Plan 04 threads both into `effSemanticDisabled`.
</output>
