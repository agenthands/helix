---
phase: 81-no-semantic-kernel-flag-e2e-config-gate-test
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/lint/ablationleakage/analyzer.go
  - internal/lint/ablationleakage/analyzer_test.go
  - internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/badgate/imports.go
  - internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/goodgate/imports.go
autonomous: true
requirements: [ABLATE-06]
must_haves:
  truths:
    - "The analyzer flags a direct SemanticLookup read (.ExpandFrom/.RankFiles/.ValidateCriticalEdges) outside the allowlisted integ adapter + gated wiring (criterion #3)"
    - "The analyzer allows the same read when routed through integ.ChooseSource / a wired ConfigGate"
    - "make vet fails on the red testdata violation and passes on the green fixture"
  artifacts:
    - path: internal/lint/ablationleakage/analyzer.go
      provides: "call-site gate check extending the Phase 76 import-boundary analyzer (D-06)"
      contains: "ChooseSource"
    - path: internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/badgate/imports.go
      provides: "green→red fixture: direct semantic read with a // want comment"
      contains: "want"
    - path: internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/goodgate/imports.go
      provides: "green fixture: same read gated behind ChooseSource"
  key_links:
    - from: internal/lint/ablationleakage/analyzer.go
      to: make vet -vettool chain
      via: "vet-ablation-leakage singlechecker rebuilt by Makefile:60-61"
      pattern: "vet-ablation-leakage"
---

<objective>
Extend the Phase 76 `vet-ablation-leakage` analyzer (`internal/lint/ablationleakage/`) from an import-boundary check to a call-site gate check (D-06): assert that every semantic-store read site routes through `integ.ChooseSource` / the wired `ConfigGate` rather than calling a `SemanticLookup` interface method directly. This catches the in-package direct-lookup bypass that an import-boundary analyzer alone cannot see (criterion #3). Ship a deliberate green→red `testdata/` violation so a regression makes `make vet` fail with a clear diagnostic. This pays down Phase 76 D-08's deferred runtime precision ("until a real case appears" — Phase 81 is that case).

Purpose: A static, compile-time complement to Plan 05's runtime counter assertion — belt-and-braces against a gate-bypass leak.
Output: The analyzer call-site check + badgate (red) / goodgate (green) fixtures + their two test functions, wired into the existing `make vet` chain (no Makefile change — the vettool is rebuilt from source).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-RESEARCH.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED→GREEN→REFACTOR): call-site gate check + green→red fixtures</name>
  <files>internal/lint/ablationleakage/analyzer.go, internal/lint/ablationleakage/analyzer_test.go, internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/badgate/imports.go, internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/goodgate/imports.go</files>
  <read_first>
    - internal/lint/ablationleakage/analyzer.go (the existing import-boundary Analyzer — analyzer.go:48-69 skeleton, the checkedPkgPrefix gate, forbiddenImportPrefixes, and the slash-boundary discipline at analyzer.go:18-22 that any new prefix/allowlist match MUST preserve)
    - internal/lint/ablationleakage/analyzer_test.go:14-24 (the analysistest.Run driver shape for badrunner/goodrunner) and the existing testdata dirs badrunner (red, // want), goodrunner (green), siblingrunner (lookalike-silent)
    - internal/lint/noduckdb/analyzer.go + analyzer_test.go (the second house analyzer template)
    - internal/semantic/integ/source_select.go (ChooseSource — the routing the analyzer pins) and the SemanticLookup interface method names ExpandFrom / RankFiles / ValidateCriticalEdges
    - 81-RESEARCH.md §"Pitfall 6" (narrow-AST vs SSA — narrow-AST is the chosen v1 approach) and §"vet call-site analyzer test"
    - 81-PATTERNS.md §"internal/lint/ablationleakage/analyzer.go" and §"testdata/src/{badgate,goodgate}/"
  </read_first>
  <behavior>
    - Test 1 (red): analysistest.Run on the `badgate` package — a direct `.ExpandFrom(` (or `.RankFiles(` / `.ValidateCriticalEdges(`) call NOT preceded by a ChooseSource gate, carrying a `// want "must route through integ.ChooseSource"` comment, IS flagged by the analyzer
    - Test 2 (green): analysistest.Run on the `goodgate` package — the same read routed through `integ.ChooseSource` is NOT flagged (no // want comment → analysistest fails if a diagnostic is emitted)
    - Test 3 (allowlist): the integ adapter + the gated daemon wiring files are NOT flagged (the analyzer's allowlist excludes the legitimate construction sites; verify by the existing siblingrunner-style silence or an explicit allowlist fixture)
  </behavior>
  <action>
    RED: author internal/lint/ablationleakage/testdata/src/.../badgate/imports.go containing a direct SemanticLookup-method call outside the allowlist with the `// want "must route through integ.ChooseSource"` comment, and internal/lint/ablationleakage/testdata/src/.../goodgate/imports.go with the same call gated behind ChooseSource. Add the two analysistest.Run test funcs to analyzer_test.go (mirror the badrunner/goodrunner driver). Run `go test ./internal/lint/ablationleakage/ -count=1` — MUST FAIL (the analyzer has no call-site check yet, so badgate is not flagged → analysistest reports the missing diagnostic). Commit `test(81-03): add green→red call-site gate fixtures`.
    GREEN: extend the Analyzer Run func with a narrow-AST call-site check (RESEARCH Pitfall 6 / A3 — narrow-AST matches house style; SSA is documented future work, NOT this phase): walk call expressions, flag any selector call to a SemanticLookup interface method name (ExpandFrom / RankFiles / ValidateCriticalEdges — enumerate the exact set from the integ interface) when the enclosing file is OUTSIDE an allowlist (the integ adapter package + the gated daemon wiring files) AND not syntactically routed through ChooseSource. Preserve the existing slash-boundary import-prefix discipline; do NOT regress the import-boundary check. Use `pass.Reportf` with the message matching the `// want` comment. Run `go test ./internal/lint/ablationleakage/ -count=1` — MUST pass. Commit `feat(81-03): add semantic read call-site gate check to vet-ablation-leakage`.
    REFACTOR (if needed): extract the allowlist + method-name set to package vars for clarity; tests stay green. Commit `refactor(81-03): ...` only if changed.
  </action>
  <verify>
    <automated>go test ./internal/lint/ablationleakage/ -count=1 && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/lint/ablationleakage/ -count=1` passes (both badgate-flagged and goodgate-silent funcs)
    - `grep -c "ChooseSource" internal/lint/ablationleakage/analyzer.go` ≥ 1 (the routing invariant the analyzer references)
    - `grep -c "want" internal/lint/ablationleakage/testdata/src/github.com/agenthands/helix/badgate/imports.go` ≥ 1
    - the existing import-boundary tests (badrunner/goodrunner/siblingrunner) STILL pass (no regression)
    - git log shows `test(81-03):` preceding `feat(81-03):` (RED→GREEN gate)
    - `go vet ./...` clean
  </acceptance_criteria>
  <done>The analyzer flags a direct semantic read outside the gate and allows a ChooseSource-routed read; both red and green fixtures pass under analysistest; the import-boundary check is preserved; go vet is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm make vet enforces the new check (no Makefile change)</name>
  <files>internal/lint/ablationleakage/analyzer.go</files>
  <read_first>
    - Makefile:20-46 (the vet target + VETTOOL_ABLATION_LEAKAGE rebuild rule; the singlechecker is rebuilt from internal/lint/ablationleakage/*.go on every make vet — no stale-artifact risk, no Makefile edit needed)
    - 81-RESEARCH.md §"Runtime State Inventory" (Build artifacts row — make vet rebuilds the vettool automatically)
  </read_first>
  <action>
    Confirm the extended analyzer is enforced by the existing chain (Makefile:46 `go vet -vettool=$(VETTOOL_ABLATION_LEAKAGE) ./...`). Run `make vet` against the real tree (which must be CLEAN — the production code routes all reads through ChooseSource after Plan 04, but Plan 03 runs in Wave 1 before Plan 04, so the real tree's existing semantic reads must already be allowlisted or ChooseSource-routed; if `make vet` flags a legitimate production read, widen the allowlist to cover the integ adapter + production wiring so the real tree stays green while the badgate fixture stays red). Do NOT add a Makefile rule — the existing rebuild rule covers the extension. This task is the criterion-#3 `make vet` smoke (VALIDATION.md row).
  </action>
  <verify>
    <automated>make vet</automated>
  </verify>
  <acceptance_criteria>
    - `make vet` exits 0 against the real tree (no false positives on legitimate production semantic reads)
    - The badgate testdata fixture remains flagged under `go test ./internal/lint/ablationleakage/`
    - No new VETTOOL line added to the Makefile (the existing rebuild rule already covers the extension)
  </acceptance_criteria>
  <done>`make vet` is green on the real tree and the analyzer extension is enforced through the existing vettool chain without a Makefile edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code → ablation contract | A code path that reads the semantic store directly (skipping the gate) silently leaks onto the no_semantic arm — the integrity threat this static guard catches at compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-03-01 | Tampering (vs. ablation contract) | a direct in-package semantic read bypassing ChooseSource/ConfigGate | mitigate | D-06 call-site analyzer flags it at `make vet`; the green→red badgate fixture proves it is not a rubber-stamp |
| T-81-03-02 | Repudiation | analyzer fails open (never flags) due to a narrow method-name set | mitigate | Enumerate the exact SemanticLookup interface method set (ExpandFrom/RankFiles/ValidateCriticalEdges) from the integ interface; the badgate fixture proves the check fires |
| T-81-03-03 | Information Disclosure | narrow-AST cannot prove cross-package receiver types (RESEARCH Pitfall 6) | accept | Narrow-AST is acceptable house style for v1 (A3); SSA via buildssa documented as future precision upgrade; the runtime counter assertion (Plan 05) is the complementary dynamic guard |
| T-81-03-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages added; golang.org/x/tools already vendored (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `go test ./internal/lint/ablationleakage/ -count=1` green (badgate flagged, goodgate silent, import-boundary tests still pass)
- `make vet` exits 0 on the real tree
- `go vet ./...` clean
</verification>

<success_criteria>
- Analyzer extended with a narrow-AST call-site gate check (D-06)
- badgate (red, // want) + goodgate (green, ChooseSource-routed) fixtures + two analysistest funcs
- Import-boundary check preserved (no regression)
- `make vet` green on the real tree; the red fixture proves the check fires
- RED→GREEN commit sequence; `go vet ./...` and `go test ./...` green
</success_criteria>

<output>
Create `.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-03-SUMMARY.md` when done. Record the exact SemanticLookup method-name set the analyzer flags and the allowlist (integ adapter + gated wiring files) — Plan 04 must keep its production wiring inside that allowlist.
</output>
