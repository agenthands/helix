---
phase: 81-no-semantic-kernel-flag-e2e-config-gate-test
plan: 04
type: tdd
wave: 2
depends_on: ["81-02"]
files_modified:
  - internal/daemon/daemon.go
  - internal/daemon/semantic_wiring.go
  - internal/daemon/semantic_gate_test.go
autonomous: true
requirements: [ABLATE-06]
must_haves:
  truths:
    - "effSemanticDisabled is resolved ONCE at the daemon composition root, OR'ing the config field + profile field (D-02)"
    - "When gated: symbolsLookupFn returns integ.NoopLookup{} even though sBndl != nil (D-04 build-but-block)"
    - "When gated: every cfgGate reports disabled so ChooseSource yields source == tree_sitter, NOT fallback (Pitfall 4)"
    - "When gated: repomap SetSemanticLookup is explicitly cleared to nil (idempotent null-object, Pitfall 5), not just skipped"
    - "When gated: the SemanticSkill Set*Accessor block (semantic_wiring.go:252-269) is skipped so find_related_symbols/explain_symbol_deep/validate_graph_edge/cluster/impact + the ExpandFrom/RankFiles back-channels get nil/Noop (Pitfall 3)"
    - "The semantic bundle/store is STILL built under the gate (D-04 — do NOT gate the bundle-build guard)"
  artifacts:
    - path: internal/daemon/daemon.go
      provides: "effSemanticDisabled resolution + gating of the 3 integLookupAccessor hand-out sites + cfgGates + health"
      contains: "effSemanticDisabled"
    - path: internal/daemon/semantic_wiring.go
      provides: "newSemanticBundle gates the Set*Accessor block under effSemanticDisabled"
      contains: "effSemanticDisabled"
    - path: internal/daemon/semantic_gate_test.go
      provides: "composition-root resolution + Noop/disabled-gate forcing + accessor gating tests"
      exports: ["TestEffSemanticDisabled", "TestSemanticGateForcesNoop", "TestSemanticSkillAccessorsGated"]
  key_links:
    - from: internal/daemon/daemon.go effSemanticDisabled
      to: symbolsLookupFn / symbolsCfgGate / health / repomap SetSemanticLookup+SetConfigGate
      via: "single boolean threaded into all ChooseSource consumers"
      pattern: "effSemanticDisabled"
    - from: internal/daemon/semantic_wiring.go newSemanticBundle
      to: b.skill.Set*Accessor block
      via: "if b.skill != nil && !effSemanticDisabled"
      pattern: "effSemanticDisabled"
---

<objective>
The core of Phase 81: resolve `effSemanticDisabled` ONCE at the daemon composition root (mirroring Phase 76's `effDisableLSP` at daemon.go:287, D-02) and thread it into every back-channel semantic read consumer so the `no_semantic` arm sees `integ.NoopLookup{}` + a disabled `ConfigGate` — while the bundle/store stays BUILT (D-04 build-but-block, what makes criterion #2 non-vacuous). This un-wires Phase 65's `SetSemanticLookup` strangler-fig for the 8 enumerated consumers: `get_repo_map`, `get_context`, `find_related_symbols`, `explain_symbol_deep`, `validate_graph_edge`, `analyze_blast_radius`, `RankFiles`, `ExpandFrom` (criterion #1 + ABLATE-06).

Purpose: A single resolution point governs all consumers (no scattered per-callsite flag checks — RESEARCH Anti-Patterns). The null-object injection at wiring time is the structural guarantee Plan 05 independently verifies.
Output: `effSemanticDisabled` resolution + gated ChooseSource consumers (symbols/repomap/health) + gated SemanticSkill accessor block + a composition-root test proving Noop forcing, tree_sitter source, and accessor gating — all under build-but-block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-RESEARCH.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-PATTERNS.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-02-SUMMARY.md
@internal/daemon/daemon.go
@internal/daemon/semantic_wiring.go
@internal/semantic/integ/source_select.go
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED→GREEN→REFACTOR): resolve effSemanticDisabled + force Noop + disabled cfgGate on ChooseSource consumers</name>
  <files>internal/daemon/daemon.go, internal/daemon/semantic_gate_test.go</files>
  <read_first>
    - internal/daemon/daemon.go:287-302 (effDisableLSP resolution — the verbatim D-02 template) and :317 / :506 (the bundle-build guards — DO NOT add effSemanticDisabled here; D-04 forbids it)
    - internal/daemon/daemon.go:612-661 (symbolsLookupFn closure, symbolsCfgGate, symbols.RegisterTools, healthLookup, healthCfgGate) — gate points 1 & 2
    - internal/daemon/daemon.go:702-782 (repomap wiring: SetEnrichFn(nil) idempotent-null template at :706-711, and SetConfigGate/SetSemanticLookup at :777-782) — gate point 3
    - internal/semantic/integ/source_select.go:32-74 (ConfigGate + ChooseSource priority ladder: gate-disabled is FIRST priority → SourceTreeSitter; gate-enabled-but-Noop → SourceFallback+index_disabled — Pitfall 4) and noop.go:25 (NoopLookup)
    - internal/profile/profile.go DisableSemanticSubsystem + internal/semantic/config.go BenchDisabled (added by Plan 02 — read 81-02-SUMMARY for exact names)
    - 81-PATTERNS.md §"internal/daemon/daemon.go" (Pattern 1 + Pattern 2, the gate-point edits with the && !effSemanticDisabled discipline) and §"Shared Patterns" (idempotent null-object, ChooseSource-first)
  </read_first>
  <behavior>
    - Test 1 (TestEffSemanticDisabled): `effSemanticDisabled` is true when EITHER cfg.SemanticIndex.BenchDisabled OR activeProfile.DisableSemanticSubsystem is set; false when neither (precedence per D-03; default-off)
    - Test 2 (TestSemanticGateForcesNoop): with the gate on AND a non-nil bundle (sBndl != nil), symbolsLookupFn() returns integ.NoopLookup{} (build-but-block, D-04) and the cfgGate reports disabled
    - Test 3 (tree_sitter, not fallback): with the gate's disabled cfgGate, integ.ChooseSource(cfgGate, lookup, err) yields SourceTreeSitter (NOT SourceFallback/index_disabled — Pitfall 4); assert against the integ ladder so source == "tree_sitter"
  </behavior>
  <action>
    RED: write internal/daemon/semantic_gate_test.go with TestEffSemanticDisabled (resolution truth table), TestSemanticGateForcesNoop (Noop forcing under non-nil bundle + disabled cfgGate), and the tree_sitter-not-fallback assertion against the integ ladder. To make these testable without booting the full daemon, factor the resolution + the gated lookupFn/cfgGate construction into small unexported helpers (e.g. resolveSemanticDisabled / gatedSymbolsLookupFn / gatedCfgGate) the test can call directly — mirror how Phase 76's gating is structured. Run `go test ./internal/daemon/ -run 'TestEffSemanticDisabled|TestSemanticGateForcesNoop' -count=1` — MUST FAIL. Commit `test(81-04): add failing semantic-gate composition-root tests`.
    GREEN: per D-02 add `effSemanticDisabled := cfg.SemanticIndex.BenchDisabled || activeProfile.DisableSemanticSubsystem` adjacent to effDisableLSP (daemon.go:287) and add it to the "subsystem ablation flags resolved" log block. Gate point 1: make `if effSemanticDisabled { return integ.NoopLookup{} }` the FIRST line of the symbolsLookupFn closure, and set `symbolsCfgGate := &daemonCfgGate{enabled: cfg.SemanticIndex.Enabled && !effSemanticDisabled}` (disable the GATE, not just the lookup — Pitfall 4). Gate point 2 (health): apply `&& !effSemanticDisabled` to healthCfgGate; healthLookup inherits the gated symbolsLookupFn. Gate point 3 (repomap, :777-782): set `SetConfigGate(&daemonCfgGate{enabled: cfg.SemanticIndex.Enabled && !effSemanticDisabled})`, and under the gate explicitly call `rs.SetSemanticLookup(nil)` (idempotent null-object, Pitfall 5 — mirror SetEnrichFn(nil) at :706-711) rather than skipping the setter. CRITICAL D-04: do NOT add effSemanticDisabled to the bundle-build guard at daemon.go:317 or :506 — the store MUST still be built. Keep the production wiring files inside the Plan 03 analyzer allowlist (read 81-03-SUMMARY). Run the tests — MUST pass. Commit `feat(81-04): resolve effSemanticDisabled + force Noop/disabled-gate on semantic read consumers`.
    REFACTOR (if needed): consolidate the three gate points' shared `cfg.SemanticIndex.Enabled && !effSemanticDisabled` expression into one local. Tests stay green. Commit `refactor(81-04): ...` only if changed.
  </action>
  <verify>
    <automated>go test ./internal/daemon/ -run 'TestEffSemanticDisabled|TestSemanticGateForcesNoop' -count=1 && go test ./internal/daemon/... ./internal/semantic/integ/... -count=1 && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/daemon/ -run 'TestEffSemanticDisabled|TestSemanticGateForcesNoop' -count=1` passes
    - `grep -c "effSemanticDisabled" internal/daemon/daemon.go` ≥ 4 (resolution + 3 gate points)
    - `grep -n "SetSemanticLookup(nil)" internal/daemon/daemon.go` returns a line (idempotent null-object under the gate)
    - the bundle-build guards at daemon.go:317/:506 do NOT reference effSemanticDisabled (D-04): `sed -n '315,320p;504,508p' internal/daemon/daemon.go | grep -c effSemanticDisabled` == 0
    - git log shows `test(81-04):` preceding `feat(81-04):` (RED→GREEN gate)
    - `go vet ./...` clean
  </acceptance_criteria>
  <done>effSemanticDisabled is resolved once at the composition root, forces Noop + disabled cfgGate on symbols/health/repomap (yielding tree_sitter), explicitly nulls the repomap lookup, leaves the bundle built (D-04), and the daemon tests + go vet are green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (RED→GREEN): gate the SemanticSkill Set*Accessor block (the non-ChooseSource consumers)</name>
  <files>internal/daemon/semantic_wiring.go, internal/daemon/semantic_gate_test.go</files>
  <read_first>
    - internal/daemon/semantic_wiring.go:251-269 (newSemanticBundle Set*Accessor block — the ~14 setters incl. SetImpactLookup(b.integLookupAccessor()) which is the ExpandFrom back-channel) and :865/:1076 (RankFiles, ExpandFrom methods of integSemanticLookup) and the daemonCfgGate at :1357
    - internal/skill/semantic/skill.go (the "nil means the seam is unwired; handler MUST guard" contract) and accessors.go:401-412 (ImpactLookupAccessor / ExpandFrom)
    - 81-RESEARCH.md §"Pitfall 3" (SemanticSkill read tools bypass ChooseSource — must gate the accessor block) and §"A5" (the three integLookupAccessor() call sites: daemon.go:614, daemon.go:780, semantic_wiring.go:266 — gating all three closes every RankFiles/ExpandFrom hand-out path)
    - 81-PATTERNS.md §"internal/daemon/semantic_wiring.go (SemanticSkill accessor block — Pattern 3 / Pitfall 3)"
  </read_first>
  <behavior>
    - Test (TestSemanticSkillAccessorsGated): when effSemanticDisabled is true, newSemanticBundle leaves the SemanticSkill accessors nil (no SetStore / SetImpactLookup / etc. called) so the direct-DuckDB-read tools (find_related_symbols, explain_symbol_deep, validate_graph_edge, cluster/impact) have no read path; when false, the accessors ARE wired (regression guard)
  </behavior>
  <action>
    RED: extend internal/daemon/semantic_gate_test.go with TestSemanticSkillAccessorsGated asserting the accessor block is skipped under the gate (verify via a test seam — e.g. a recording/fake SemanticSkill, or assert the integLookupAccessor hand-out count is 0 under the gate). Run `go test ./internal/daemon/ -run TestSemanticSkillAccessorsGated -count=1` — MUST FAIL. Commit `test(81-04): add failing SemanticSkill accessor-gating test`.
    GREEN: thread `effSemanticDisabled` as a new param into newSemanticBundle and gate the whole Set*Accessor block: `if b.skill != nil && !effSemanticDisabled { ...all setters... }`. When gated, leave accessors nil (handlers already nil-guard). This closes the third integLookupAccessor() hand-out (semantic_wiring.go:266) — together with the two daemon.go sites gated in Task 1, every RankFiles/ExpandFrom path is closed (A5). Verify no OTHER call site constructs integSemanticLookup directly (grep `integLookupAccessor()` and `integSemanticLookup{` — confirm exactly the three accessor call sites + the one struct constructor inside integLookupAccessor). Run the test — MUST pass. Commit `feat(81-04): gate SemanticSkill accessor block under effSemanticDisabled`.
  </action>
  <verify>
    <automated>go test ./internal/daemon/ -run TestSemanticSkillAccessorsGated -count=1 && go test ./internal/daemon/... -count=1 && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/daemon/ -run TestSemanticSkillAccessorsGated -count=1` passes
    - `grep -c "effSemanticDisabled" internal/daemon/semantic_wiring.go` ≥ 1 (the gated accessor block)
    - `grep -c "integLookupAccessor()" internal/daemon/daemon.go internal/daemon/semantic_wiring.go` confirms exactly the 3 known hand-out sites are present and all gated (per A5)
    - SUMMARY confirms no additional integSemanticLookup construction site exists outside the gated paths
    - git log shows `test(81-04):` preceding the second `feat(81-04):` commit
    - `go vet ./...` clean
  </acceptance_criteria>
  <done>The SemanticSkill accessor block is skipped under the gate so the direct-read tools have no DuckDB path; all three integLookupAccessor hand-outs are gated; A5 verified; daemon tests + go vet green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon composition root → 8 read consumers | This is THE gate: any consumer not wired to Noop/disabled-gate leaks a semantic read onto the no_semantic arm (the central integrity threat of the phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-04-01 | Information Disclosure (vs. ablation contract) | a back-channel read consumer not gated (4th integLookupAccessor site) | mitigate | A5: gate all 3 known hand-out sites + verify no 4th construction site via grep; the Plan 05 counter assertion independently catches any survivor |
| T-81-04-02 | Tampering | gate "fails open" — cfgGate left enabled while lookup forced Noop → source=fallback not tree_sitter (Pitfall 4) | mitigate | Disable the GATE (enabled: ... && !effSemanticDisabled) so ChooseSource hits its FIRST priority → tree_sitter; Test 3 asserts source == tree_sitter |
| T-81-04-03 | Tampering | stale real lookup persists on the process-global RepoMapSkill singleton (skip-the-setter, Pitfall 5) | mitigate | Explicit `rs.SetSemanticLookup(nil)` under the gate (idempotent null-object, mirror SetEnrichFn(nil)) |
| T-81-04-04 | Tampering | gating the bundle-build guard makes the zero-read test vacuous (D-04 violation) | mitigate | Acceptance criterion asserts effSemanticDisabled is NOT referenced at daemon.go:317/:506; the store stays built |
| T-81-04-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages added (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `go test ./internal/daemon/ -run 'TestEffSemanticDisabled|TestSemanticGateForcesNoop|TestSemanticSkillAccessorsGated' -count=1` green
- `go test ./internal/daemon/... ./internal/semantic/integ/... -count=1` green
- `go vet ./...` clean; bundle-build guards untouched (D-04)
</verification>

<success_criteria>
- `effSemanticDisabled` resolved once at the composition root (D-02), OR'ing config + profile fields
- All 8 enumerated consumers see NoopLookup + disabled cfgGate; ChooseSource → source == tree_sitter (Pitfall 4)
- repomap SetSemanticLookup explicitly nil'd under the gate (Pitfall 5)
- SemanticSkill accessor block skipped under the gate (Pitfall 3); all 3 integLookupAccessor hand-outs gated (A5)
- Bundle/store STILL built under the gate (D-04 — guards at :317/:506 untouched)
- RED→GREEN commits; `go vet ./...` and `go test ./...` green
</success_criteria>

<output>
Create `.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-04-SUMMARY.md` when done. Record: the exact gate-point line numbers touched, confirmation that the bundle-build guard is untouched (D-04), and the A5 verification (no 4th integSemanticLookup construction site). Plan 05 depends on the gate being live to assert zero reads.
</output>
