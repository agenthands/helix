---
phase: 81-no-semantic-kernel-flag-e2e-config-gate-test
plan: 05
type: tdd
wave: 3
depends_on: ["81-01", "81-02", "81-04"]
files_modified:
  - bench/runtime/cell.go
  - bench/runtime/no_semantic_zero_reads_test.go
  - bench/runners/your_agent_no_semantic/MODE.md
autonomous: true
requirements: [ABLATE-06]
must_haves:
  truths:
    - "An E2E no_semantic run reports helix_semantic_store_reads_total == 0 (criterion #2)"
    - "On a non-zero read count the bench cell LOGS and FAILS (hard fail, not warn)"
    - "The guarantee_pending_phase_81 deferral marker is removed from cell.go (the guarantee now lands)"
    - "MODE.md documents the gate key semantic_index.bench_disabled + the 8-consumer enumeration (criterion #4)"
  artifacts:
    - path: bench/runtime/no_semantic_zero_reads_test.go
      provides: "E2E counter==0 assertion + fail-cell-on-violation test"
      exports: ["TestNoSemanticZeroReads"]
    - path: bench/runtime/cell.go
      provides: "counter-scrape + counter==0 assertion; guarantee_pending_phase_81 removed"
      contains: "helix_semantic_store_reads_total"
    - path: bench/runners/your_agent_no_semantic/MODE.md
      provides: "gate key + strangler-fig consumer enumeration (criterion #4)"
      contains: "disable_semantic_subsystem"
  key_links:
    - from: bench/runtime/cell.go
      to: helix_semantic_store_reads_total counter value
      via: "counter-scrape (daemon-log line or Prometheus, per Wave-0 spike) → assert == 0 → fail cell"
      pattern: "helix_semantic_store_reads_total"
    - from: bench/runners/your_agent_no_semantic/MODE.md
      to: the 8 enumerated consumers
      via: "documented strangler-fig consumer list"
      pattern: "get_repo_map|RankFiles|ExpandFrom"
---

<objective>
Land the independent runtime verification (D-05, criterion #2): the bench cell asserts `helix_semantic_store_reads_total == 0` after a `no_semantic` run and LOGS + FAILS the cell on violation. Remove the `guarantee_pending_phase_81` deferral marker (cell.go:53-59 — the guarantee now lands). Rewrite `bench/runners/your_agent_no_semantic/MODE.md` to document the `semantic_index.bench_disabled` gate key + the 8-consumer strangler-fig enumeration so a future contributor cannot accidentally rip out the bypass (criterion #4). The no_lsp "zero-span trace-tap" is NOT reusable code (RESEARCH Pitfall 2) — the counter-scrape + assert path is built fresh.

Purpose: The build-but-block gate (Plan 04) is the structural guarantee; this counter assertion is the independent verification that proves the gate BLOCKS a store that exists and could be queried.
Output: E2E counter==0 cell assertion + fail-cell test + deferral-marker removal + rewritten MODE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-RESEARCH.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-PATTERNS.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-01-SUMMARY.md
@.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-04-SUMMARY.md
@bench/runtime/cell.go
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Confirm the counter-exposure path in the bench Unix-socket sandbox</name>
  <files>bench/runtime/cell.go</files>
  <read_first>
    - bench/runtime/cell.go:132-146 (MergedTrace / MergedTracePath fields), :529 (tap.RejectedForeignPid), :265 (cellDurablePaths) — how the cell already obtains trace data
    - internal/eval/trace/tap.go:23-56 + schema.go:104-149 (MergedTrace is built from daemon-log line tapping on msg=="tool call"/"receipt issued", NOT OTel spans — A2)
    - bench/runtime/subprocess/daemon.go StartDaemon (the bench daemon runs over a Unix socket with --http-addr empty so the Prometheus /metrics HTTP scrape is likely unreachable; P77 D-06/D-07)
    - 81-RESEARCH.md §"Open Questions" Q2 (Prometheus scrape vs daemon-log emission) and §"Pitfall 2"
  </read_first>
  <action>
    Wave-0 spike from RESEARCH Open Question 2. Determine how the bench cell can read `helix_semantic_store_reads_total` given the daemon runs HTTP-disabled over a Unix socket. Evaluate the two candidate paths and pick one: (A) emit a single parseable daemon-log line at shutdown (e.g. `msg="semantic store reads total" count=N`) that the existing tap.go msg== pattern can capture into MergedTrace — RESEARCH's recommended path given HTTP-disabled; or (B) expose the counter value over the gRPC/admin path the cell already uses. Confirm whichever is reachable in the sandbox. Document the chosen mechanism + the exact log-line format or RPC field in the SUMMARY so Task 1's assertion parses the real signal (not a phantom). If path (A) requires the daemon to emit the line, that emission is a small addition to the daemon shutdown path — record it as part of this task's GREEN in Task 1.
  </action>
  <verify>
    <automated>grep -n "msg==\|msg ==" internal/eval/trace/tap.go</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY names the chosen exposure mechanism (daemon-log line OR gRPC/admin) and confirms it is reachable in the HTTP-disabled Unix-socket sandbox
    - If daemon-log: the exact `msg=` string + field name the cell parses is documented and matches what the daemon actually emits
  </acceptance_criteria>
  <done>The counter-exposure path is confirmed reachable in the bench sandbox with an exact, parseable signal format documented.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 1 (RED→GREEN→REFACTOR): counter==0 cell assertion + fail-cell on violation; remove deferral marker</name>
  <files>bench/runtime/cell.go, bench/runtime/no_semantic_zero_reads_test.go</files>
  <read_first>
    - bench/runtime/cell.go:52-63 (ablationStatusFor — the guarantee_pending_phase_81 branch to REMOVE) and :577 (the AblationStatus: ablationStatusFor(cfg.Mode) call site) and :557-597 (the grade/BuildResult/writeDurable cell-completion path where the assertion hooks in)
    - the Task-0 chosen exposure mechanism (from this plan's SUMMARY-in-progress); 81-01-SUMMARY for the counter name helix_semantic_store_reads_total; 81-04-SUMMARY for confirmation the gate is live
    - bench/runtime/cell.go (the Phase 80 fairness gate's fatal/failure pattern — mirror its fail-closed shape, not a warn)
    - 81-RESEARCH.md §"Pitfall 2" (build the assert path fresh — no liftable no_lsp helper) and §"Validation Architecture" (the TestNoSemanticZeroReads row)
    - 81-PATTERNS.md §"bench/runtime/cell.go + bench/runtime/no_semantic_zero_reads_test.go (E2E assertion)"
  </read_first>
  <behavior>
    - Test 1 (TestNoSemanticZeroReads, pass case): a no_semantic cell whose counter reads 0 completes successfully (no fail, no error)
    - Test 2 (fail case): a no_semantic cell whose counter reads N>0 LOGS the violation and FAILS the cell (the CellResult/CellOutcome reflects failure — hard fail, not a warn); drive the violation deterministically via a fake/injected counter value or a fake trace carrying a non-zero count
    - Test 3 (scope guard): the assertion fires ONLY on the no_semantic arm — other modes are unaffected
  </behavior>
  <action>
    RED: write bench/runtime/no_semantic_zero_reads_test.go (TestNoSemanticZeroReads) covering the pass/fail/scope behaviors above, driving the counter value via the Task-0 mechanism (inject a fake MergedTrace carrying the parsed count, or a seam returning the scraped value). Run `go test ./bench/runtime/ -run TestNoSemanticZeroReads -count=1` — MUST FAIL (no assertion exists). Commit `test(81-05): add failing no_semantic zero-reads cell assertion`.
    GREEN: in bench/runtime/cell.go, after the no_semantic run obtains its trace/metrics, read the helix_semantic_store_reads_total value via the Task-0 mechanism, assert == 0 for the no_semantic mode, and on violation LOG a clear diagnostic + FAIL the cell (mirror the existing fatal/failure path used by the Phase 80 fairness gate — fail-closed, not warn). If Task 0 chose path (A), add the single daemon-log emission of the counter at shutdown here. REMOVE the guarantee_pending_phase_81 branch from ablationStatusFor (cell.go:53-63) — the guarantee now lands, so the no_semantic arm no longer carries the deferral marker (honest modes already omit it via omitempty). Run the test — MUST pass. Commit `feat(81-05): assert helix_semantic_store_reads_total == 0 on no_semantic cell + remove deferral marker`.
    REFACTOR (if needed): extract the counter-read + assert into a small helper for clarity; tests stay green. Commit `refactor(81-05): ...` only if changed.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run TestNoSemanticZeroReads -count=1 && go test ./bench/runtime/... -count=1 && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./bench/runtime/ -run TestNoSemanticZeroReads -count=1` passes (pass + fail + scope cases)
    - `grep -c "helix_semantic_store_reads_total" bench/runtime/cell.go` ≥ 1
    - `grep -c "guarantee_pending_phase_81" bench/runtime/cell.go` == 0 (deferral marker removed)
    - the fail case produces a hard cell failure (CellResult/CellOutcome reflects failure), not a warning
    - git log shows `test(81-05):` preceding `feat(81-05):` (RED→GREEN gate)
    - `go vet ./...` clean
  </acceptance_criteria>
  <done>The no_semantic cell asserts the read counter == 0, fails hard on violation, the deferral marker is gone, and bench/runtime tests + go vet are green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite your_agent_no_semantic/MODE.md (criterion #4)</name>
  <files>bench/runners/your_agent_no_semantic/MODE.md</files>
  <read_first>
    - bench/runners/your_agent_no_semantic/MODE.md (the existing file — the "Deferred guarantee" / ablation_status: guarantee_pending_phase_81 section at ~lines 16-36 must be DELETED)
    - bench/runners/no_lsp/MODE.md (the clean post-deferral target shape — frontmatter mode/profile, one-paragraph arm description, one paragraph documenting the kernel gate)
    - 81-PATTERNS.md §"bench/runners/your_agent_no_semantic/MODE.md (REWRITE — criterion #4)" (the required enumeration)
  </read_first>
  <action>
    Rewrite bench/runners/your_agent_no_semantic/MODE.md mirroring the no_lsp/MODE.md shape: frontmatter (`mode: no_semantic`, `profile: bench-no-semantic`), a one-paragraph arm description, then a paragraph documenting the kernel gate. Per criterion #4 the doc MUST name (a) the gate key `semantic_index.bench_disabled` and its precedence (CLI > profile YAML > default-off), (b) the build-but-block behavior (store still built, reads forced to NoopLookup + disabled ConfigGate, source == "tree_sitter"), and (c) the FULL strangler-fig consumer enumeration so a future contributor cannot accidentally rip out the bypass: get_repo_map, get_context, find_related_symbols, explain_symbol_deep, validate_graph_edge, analyze_blast_radius, RankFiles, ExpandFrom. DELETE the `ablation_status: guarantee_pending_phase_81` / "deferred guarantee" section entirely (the guarantee now lands this phase).
  </action>
  <verify>
    <automated>for c in get_repo_map get_context find_related_symbols explain_symbol_deep validate_graph_edge analyze_blast_radius RankFiles ExpandFrom semantic_index.bench_disabled; do grep -q "$c" bench/runners/your_agent_no_semantic/MODE.md || { echo "MISSING $c"; exit 1; }; done; test "$(grep -c 'guarantee_pending_phase_81' bench/runners/your_agent_no_semantic/MODE.md)" = "0" && echo "MODE.md criterion #4 satisfied"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 consumers (get_repo_map, get_context, find_related_symbols, explain_symbol_deep, validate_graph_edge, analyze_blast_radius, RankFiles, ExpandFrom) are named in MODE.md
    - The gate key `semantic_index.bench_disabled` and its precedence are documented
    - `grep -c "guarantee_pending_phase_81" bench/runners/your_agent_no_semantic/MODE.md` == 0 (deferral section deleted)
    - Frontmatter is `mode: no_semantic` / `profile: bench-no-semantic`
  </acceptance_criteria>
  <done>MODE.md documents the gate key, precedence, build-but-block behavior, and the full 8-consumer enumeration; the deferral section is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no_semantic run → bench result | The runtime verification boundary: a semantic read that survived the Plan 04 gate must be caught here before a corrupted result row is published (integrity threat to benchmark fairness) |
| contributor edit → MODE.md | An undocumented gate is a gate a future contributor rips out — MODE.md is the human-readable contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-05-01 | Information Disclosure (vs. ablation contract) | the assertion failing OPEN (warns instead of fails, or asserts a phantom counter) | mitigate | Task 0 confirms the real exposure path; the fail case test (Test 2) proves a hard cell FAILURE, not a warn; the counter is the real Plan-01 metric |
| T-81-05-02 | Tampering | leaving the guarantee_pending_phase_81 marker → results falsely labeled "pending" | mitigate | Deferral marker removed from cell.go; acceptance criterion greps for its absence |
| T-81-05-03 | Repudiation | future contributor removes the gate because MODE.md doesn't document it | mitigate | Criterion #4 MODE.md enumerates the gate key + all 8 consumers; verify gate asserts all 8 names present |
| T-81-05-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages added (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `go test ./bench/runtime/ -run TestNoSemanticZeroReads -count=1` green (pass + fail + scope cases)
- `go test ./bench/runtime/... -count=1` green
- `guarantee_pending_phase_81` absent from cell.go and MODE.md
- MODE.md names all 8 consumers + the gate key
- `go vet ./...` clean
</verification>

<success_criteria>
- Counter-exposure path confirmed in the HTTP-disabled Unix-socket sandbox (Task 0)
- no_semantic cell asserts `helix_semantic_store_reads_total == 0`; LOGS + FAILS hard on violation (D-05, criterion #2)
- `guarantee_pending_phase_81` deferral marker removed (cell.go + MODE.md)
- MODE.md documents the gate key + precedence + build-but-block + all 8 consumers (criterion #4)
- RED→GREEN commits; `go vet ./...` and `go test ./...` green
</success_criteria>

<output>
Create `.planning/phases/81-no-semantic-kernel-flag-e2e-config-gate-test/81-05-SUMMARY.md` when done. Record the counter-exposure mechanism chosen (Task 0) and confirm the full-phase gate `go test ./... && make vet` is green.
</output>
