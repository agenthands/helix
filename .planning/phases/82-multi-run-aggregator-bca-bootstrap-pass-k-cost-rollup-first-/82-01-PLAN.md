---
phase: 82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-leaderboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/cost/cost_table.go
  - bench/cost/cost_table_test.go
  - cmd/helix-bench/validate_cost_table.go
  - bench/runtime/matrix.go
  - bench/runtime/matrix_test.go
  - cmd/helix-bench/main.go
autonomous: true
requirements: [STATS-01]
must_haves:
  truths:
    - "CostRow/CostTable + freshness gate live in an importable package both validate-cost-table and the aggregator can use (no duplicate types)"
    - "The existing validate-cost-table CLI behaves identically (freshness gate still fail-closed) after the move"
    - "ExpandMatrix emits N cells per (task,mode) with RunIndex 0..N-1 when given runs=N"
    - "helix-bench run --runs N threads N into ExpandMatrix so a multi-run matrix lands distinct run_index dirs"
  artifacts:
    - path: "bench/cost/cost_table.go"
      provides: "Exported CostRow, CostTable, ValidateCostTable(today,path), PriceFor(ct,modelID,today), dateLayout/stalenessWindowDays constants"
      contains: "func ValidateCostTable"
    - path: "bench/runtime/matrix.go"
      provides: "ExpandMatrix with a runs axis emitting N RunIndex cells"
      contains: "RunIndex: r"
  key_links:
    - from: "cmd/helix-bench/validate_cost_table.go"
      to: "bench/cost"
      via: "import + call cost.ValidateCostTable"
      pattern: "cost\\.ValidateCostTable"
    - from: "cmd/helix-bench/main.go"
      to: "runtime.ExpandMatrix"
      via: "--runs flag threaded into the ExpandMatrix call"
      pattern: "ExpandMatrix\\("
---

<objective>
Lay the Wave-1 foundation the parallel algorithm plans build on: (1) MOVE the cost-table
types + freshness gate out of `cmd/helix-bench` `package main` into a new importable
`bench/cost` package so BOTH the existing `validate-cost-table` subcommand and the new
`bench/aggregator/cost.go` (Plan 04) share ONE parser and ONE freshness gate (resolved Open
Q1 — MOVE, do not duplicate); and (2) extend `ExpandMatrix` with a `runs` axis so the matrix
runner emits N cells per (task, mode) with `RunIndex 0..N-1` (D-04, the producer half of
STATS-01), wiring a `--runs N` (default 3) flag on `helix-bench run`.

Purpose: STATS-01 has two halves — producing N runs (this plan, matrix side) and refusing on
deficiency (Plan 05, aggregator side). This plan also unblocks Plan 04's cost helper by giving
it an importable cost-table package.
Output: `bench/cost/` package (moved types + exported `ValidateCostTable` + new `PriceFor`),
an N-aware `ExpandMatrix`, and the `--runs` flag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-CONTEXT.md
@.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-RESEARCH.md
@.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-PATTERNS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Move cost-table types + freshness gate into an importable bench/cost package (Open Q1)</name>
  <read_first>
    - cmd/helix-bench/validate_cost_table.go (the file being gutted; the CostRow/CostTable structs lines 25-42, validateCostTable lines 49-107, dateLayout/stalenessWindowDays constants lines 14-23, newValidateCostTableCmd lines 113-133)
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-PATTERNS.md (§ bench/aggregator/cost.go + § Shared Pattern "Injected-clock freshness gate" — the exact priceFor shape and the move-vs-duplicate decision)
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-RESEARCH.md (§ Cost-table loader + freshness gate D-13; § Open Questions Q1 → MOVE)
  </read_first>
  <action>
    Create `bench/cost/cost_table.go` (package `cost`) and MOVE into it, EXPORTED, the
    cost-table contract currently in `cmd/helix-bench/validate_cost_table.go` (Open Q1 resolved
    as MOVE, not duplicate): the `CostRow` and `CostTable` structs (keep the yaml tags verbatim),
    the `dateLayout = "2006-01-02"` and `stalenessWindowDays = 90` constants, and the validator
    logic as an exported `ValidateCostTable(today time.Time, path string) error` (rename of the
    private `validateCostTable`, body unchanged — strict `yaml.NewDecoder` + `KnownFields(true)`,
    same per-row checks, same fail-closed messages, same injected-clock discipline per D-13).
    Add a NEW exported `func PriceFor(ct CostTable, modelID string, today time.Time) (CostRow, error)`
    that the aggregator (Plan 04) will reuse: iterate rows, match `r.ModelID == modelID`, parse
    `valid_until` and fail closed if before `today.UTC().Truncate(24h)`, parse `last_verified` and
    fail closed if before `today.AddDate(0,0,-stalenessWindowDays)`, and return a hard error
    `"cost: no row for model_id %q"` when no row matches (an unknown model is a hard error — cannot
    price). Also add an exported `func LoadCostTable(path string) (CostTable, error)` that does the
    strict decode (so Plan 04 can load once and price many). Keep all the fail-closed semantics
    from D-13.

    Then GUT `cmd/helix-bench/validate_cost_table.go`: delete the moved structs/constants/validator,
    `import "github.com/agenthands/helix/bench/cost"`, and have `newValidateCostTableCmd`'s RunE call
    `cost.ValidateCostTable(time.Now().UTC(), path)`. The CLI behavior and error wording MUST be
    byte-equivalent (the existing validate-cost-table tests must stay green). Run `gofmt -w` on both
    files. NOTE the `dateLayout`/`stalenessWindowDays` consts were shared with the TOS validator's
    "D-16 shared constants" comment — if `verify_tos.go` (or any sibling in `cmd/helix-bench`) still
    references the now-moved consts, re-declare a local copy there or import them from `bench/cost`;
    do NOT leave a dangling reference. Grep `cmd/helix-bench` for `dateLayout` / `stalenessWindowDays`
    before deleting and fix every consumer.
  </action>
  <verify>
    <automated>go build ./cmd/helix-bench && go test ./cmd/helix-bench/... ./bench/cost/... && go vet ./cmd/helix-bench/... ./bench/cost/...</automated>
  </verify>
  <done>
    `bench/cost` exports `CostRow`, `CostTable`, `LoadCostTable`, `ValidateCostTable`, `PriceFor`,
    `dateLayout`, `stalenessWindowDays`; `cmd/helix-bench` no longer declares the cost-table
    structs/validator; `go build ./cmd/helix-bench` and all existing cmd/helix-bench tests pass;
    no duplicate CostRow/CostTable type exists in the tree (grep `type CostTable` returns exactly
    one definition, in bench/cost).
  </done>
</task>

<task type="execute">
  <name>Task 2: Add a runs axis to ExpandMatrix + a --runs flag (D-04, STATS-01 producer)</name>
  <read_first>
    - bench/runtime/matrix.go (ExpandMatrix lines 110-156 — the inner append at line 150; Cell.RunIndex doc lines 35-40; runOneCell already threads c.RunIndex at line 266)
    - bench/runtime/cell.go (cellDurablePaths lines 310-318 + validateRunIndexSegment lines 327-332 — already RunIndex-aware, reuse as-is, no path code change)
    - cmd/helix-bench/main.go (newRunCmd flags lines 103-158; runBenchOpts lines 161-172; the ExpandMatrix call at line 218)
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-PATTERNS.md (§ bench/runtime/matrix.go — MODIFY ExpandMatrix: the exact before/after of the inner loop)
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-RESEARCH.md (§ N≥3 Enforcement → Producing N cells)
  </read_first>
  <action>
    Change `ExpandMatrix(benchmarks, languages, modes, tasks []string)` to
    `ExpandMatrix(benchmarks, languages, modes, tasks []string, runs int)` per D-04. Treat
    `runs < 1` as 1 (defensive). In the innermost loop replace the single
    `cells = append(cells, Cell{...})` with a `for r := 0; r < runs; r++` that emits
    `Cell{Benchmark: b, Language: l, Mode: m, Task: t, RunIndex: r}` — so each (b,l,m,t) yields
    `runs` cells with distinct RunIndex 0..runs-1. Update the `cells` capacity hint to multiply by
    `runs`. Do NOT touch `cellDurablePaths`, `validateRunIndexSegment`, `runOneCell`, or
    `writeDurable` — they are already RunIndex-aware (RESEARCH §Reusable Assets); distinct RunIndex
    already lands distinct durable dirs.

    Update the one in-tree caller: in `cmd/helix-bench/main.go` add `runs int` to `runBenchOpts`, add
    a `cmd.Flags().IntVar(&runs, "runs", 3, "runs per (task,mode); each lands a distinct run_index dir (STATS-01)")`
    in `newRunCmd`, pass it through `runBenchOpts`, and thread `o.Runs` into the `runtime.ExpandMatrix(...)`
    call at line 218. Run `gofmt -w` on both files. Fix any OTHER ExpandMatrix callers the build
    surfaces (grep `ExpandMatrix(` across the tree — including tests in the same package, which Task 3
    covers, but a non-test caller MUST be updated here for the build to pass).
  </action>
  <verify>
    <automated>go build ./... && go vet ./bench/runtime/... ./cmd/helix-bench/...</automated>
  </verify>
  <done>
    `ExpandMatrix` takes a `runs int` parameter and emits N cells per (task,mode) with RunIndex
    0..N-1; `helix-bench run --runs N` is wired; `go build ./...` is green (all ExpandMatrix
    callers updated).
  </done>
</task>

<task type="execute">
  <name>Task 3: Wave-0 test scaffold — ExpandMatrix N-cell expansion test (D-04)</name>
  <read_first>
    - bench/runtime/matrix_test.go (if present — existing ExpandMatrix tests that MUST be updated for the new signature; otherwise create)
    - bench/runtime/matrix.go (the modified ExpandMatrix from Task 2)
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-VALIDATION.md (D-04 row: TestExpandMatrixRuns)
  </read_first>
  <action>
    Update every existing `ExpandMatrix(...)` call in `bench/runtime/matrix_test.go` (and any other
    `_test.go` in package runtime) to pass the new `runs` argument (use `1` to preserve existing
    single-rep assertions unless a test specifically asserts the count). Add a new test
    `TestExpandMatrixRuns` (D-04) that calls `ExpandMatrix([]string{"internal-toolbench"},
    []string{"go"}, []string{"your_agent_full"}, []string{"t1","t2"}, 3)` and asserts: total cell
    count == 1*1*1*2*3 == 6; for each (task) there are exactly 3 cells with RunIndex {0,1,2} (a set,
    not order-dependent); and that `ExpandMatrix(..., 0)` clamps to runs=1 (one cell per task). This
    is the Nyquist Wave-0 scaffold for D-04 — it must compile and pass against the Task-2 change.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run TestExpandMatrix</automated>
  </verify>
  <done>
    `go test ./bench/runtime/ -run TestExpandMatrix` is green; TestExpandMatrixRuns asserts the
    N-cell expansion and the runs<1 clamp; all pre-existing matrix tests still pass with the updated
    signature.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local YAML file → cost loader | `cost-table.yaml` is a committed, in-repo file; not remote/untrusted, but a malformed/stale table must fail closed |
| task/mode/run_index → durable path segment | RunIndex becomes a path segment via cellDurablePaths; must stay non-negative-int-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01-01 | Tampering | RunIndex path segment in cellDurablePaths | mitigate | Reuse existing `validateRunIndexSegment` (cell.go:327) UNCHANGED — it rejects negative/`..`/separator segments before the join; this plan only feeds it 0..N-1 non-negative ints |
| T-82-01-02 | Information Disclosure / Integrity | stale or past-`valid_until` cost-table row | mitigate | The moved `ValidateCostTable`/`PriceFor` keep the D-13 fail-closed freshness gate (past valid_until or >90d last_verified → hard error); no behavior weakened by the move |
| T-82-01-03 | Denial of Service | --runs set very large (matrix fan-out) | accept | --runs is operator-supplied on a local CLI; the existing --parallel semaphore bounds host concurrency; no remote attacker surface |
| T-82-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (stdlib + existing yaml/cobra only); RESEARCH Package Legitimacy Audit shows zero new packages |
</threat_model>

<verification>
- `go build ./...` green (signature change propagated to all callers).
- `go test ./bench/cost/... ./bench/runtime/... ./cmd/helix-bench/...` green.
- `go vet ./...` clean for the touched packages.
- Exactly one `type CostTable` definition in the tree (the move, not a duplicate).
</verification>

<success_criteria>
- `bench/cost` is the single importable home for the cost-table contract + freshness gate + `PriceFor`/`LoadCostTable`.
- `validate-cost-table` CLI is behavior-identical (existing tests green).
- `ExpandMatrix` emits N RunIndex cells; `--runs N` (default 3) is wired on `helix-bench run`.
- TestExpandMatrixRuns proves the N-cell expansion (D-04).
</success_criteria>

<output>
Create `.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-01-SUMMARY.md` when done
</output>
