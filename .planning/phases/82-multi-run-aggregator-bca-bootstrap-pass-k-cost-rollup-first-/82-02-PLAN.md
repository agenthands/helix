---
phase: 82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-leaderboard
plan: 02
type: tdd
wave: 2
depends_on: []
files_modified:
  - bench/aggregator/bootstrap.go
  - bench/aggregator/bootstrap_test.go
autonomous: true
requirements: [STATS-02]
must_haves:
  truths:
    - "A proper BCa interval (bias-correction z0 + jackknife acceleration a) is computed over a per-task value vector with >=10,000 resamples (D-06)"
    - "On a skewed sample the BCa endpoints DIFFER from a plain-percentile bootstrap (proves z0/a fired, not a fake-BCa)"
    - "Resampling is deterministic: same seed + same input yields byte-identical [lo,hi] (D-08)"
    - "Degenerate inputs do not crash: empty vector -> null CI; all-identical -> point CI [v,v]; m==1 -> a=0 (D-09)"
  artifacts:
    - path: "bench/aggregator/bootstrap.go"
      provides: "BCaInterval(vals []float64, stat func([]float64) float64, B int, alpha float64, rng *rand.Rand) (lo,hi float64, ok bool) + phi/phiInv helpers"
      contains: "func BCaInterval"
      min_lines: 60
    - path: "bench/aggregator/bootstrap_test.go"
      provides: "STATS-02 closed-form + width + BCa!=percentile-on-skew + determinism tests"
      contains: "func TestBCa"
  key_links:
    - from: "bench/aggregator/bootstrap.go"
      to: "math.Erfinv / math.Erfc"
      via: "phiInv = Sqrt2*Erfinv(2p-1); phi = 0.5*Erfc(-z/Sqrt2)"
      pattern: "math\\.Erfinv"
    - from: "bench/aggregator/bootstrap.go"
      to: "math/rand/v2"
      via: "seeded rand.NewPCG resampling (D-08)"
      pattern: "rand\\.NewPCG"
---

<objective>
Implement the BCa (bias-corrected accelerated) bootstrap confidence interval in
`bench/aggregator/bootstrap.go` as a pure, deterministic, dependency-free function — the
statistical heart of STATS-02. It is a PROPER BCa (bias-correction z0 from the proportion of
bootstrap replicates below the observed statistic + acceleration a via jackknife over the
resampling units), NOT a percentile-only or normal-approx bootstrap (D-06), with >=10,000
resamples (D-07), a seeded RNG for reproducibility (D-08), and the D-09 degenerate-case
discipline (null CI / point CI / a=0, never a crash or NaN).

Purpose: every leaderboard and cost_quality.md cell needs a BCa CI; this is the single reusable
primitive Plan 06 calls for every metric. TDD because the algorithm has closed-form acceptance
anchors that a naive percentile bootstrap would fail.
Output: `bench/aggregator/bootstrap.go` (BCaInterval + phi/phiInv/jackknife/quantile helpers)
and a comprehensive `bootstrap_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-CONTEXT.md
@.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-RESEARCH.md
@.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — BCa test suite (closed-form, width, BCa!=percentile on skew, determinism, degenerate)</name>
  <read_first>
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-RESEARCH.md (§ BCa Bootstrap — Exact Algorithm: Steps 1-5, the degenerate/edge-case matrix, AND § Closed-form acceptance test design — the THREE tests: closed-form/location, CI-width sanity, determinism)
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-PATTERNS.md (§ bench/aggregator/bootstrap.go — phi/phiInv, seeded RNG, bcaPercentiles, degenerate matrix)
    - bench/runtime/deltas_test.go (table-driven Go test style + testify assert/require usage in this repo)
  </read_first>
  <behavior>
    - Test (BCa != percentile on skew): for a deliberately right-skewed per-task vector (e.g. an
      exponential-shaped sample like {1,1,1,2,2,3,5,8,13}), with a FIXED seed and B=10000, the BCa
      [lo,hi] endpoints DIFFER measurably from a plain-percentile interval over the SAME sorted
      bootstrap distribution (assert |bca_lo - pctl_lo| > 1e-6 OR |bca_hi - pctl_hi| > 1e-6). This
      is the discriminating signal a fake-BCa fails. The test helper computes the percentile interval
      itself (read the same sorted bootstrap, take alpha/2 and 1-alpha/2 quantiles).
    - Test (symmetric ~ percentile): for a symmetric sample (z0~0, a~0) the BCa interval is CLOSE to
      the percentile interval (assert within a small tolerance) — confirms z0/a vanish correctly.
    - Test (contains the parameter): the BCa interval for a known-mean synthetic sample CONTAINS the
      true mean (lo <= mean <= hi).
    - Test (width sanity): hi > lo for a non-degenerate sample; width shrinks as m grows on a fixed
      distribution.
    - Test (determinism, D-08): two BCaInterval calls with the SAME seed and SAME input return
      bit-identical [lo,hi].
    - Test (degenerate, D-09): empty vector -> ok==false (null CI); all-identical vector {5,5,5,5}
      -> lo==hi==5 (point CI), no NaN/Inf; m==1 {7} -> point CI [7,7] (a=0 path), no crash.
  </behavior>
  <action>
    Write `bench/aggregator/bootstrap_test.go` (package `aggregator`) FIRST, encoding every behavior
    above as table-driven / named subtests against the not-yet-existing `BCaInterval`. Decide and FIX
    the public signature here so Plan 06 can call it — recommended:
    `func BCaInterval(vals []float64, stat func([]float64) float64, B int, alpha float64, rng *rand.Rand) (lo, hi float64, ok bool)`
    where `ok==false` signals a null CI (empty input). Provide a package-level helper `StatMean` (or
    inline `func(x []float64) float64 { ... mean ... }`) for the tests. Construct the seeded RNG with
    `rand.New(rand.NewPCG(seed, seed^0x9E3779B97F4A7C15))`. RUN the test and confirm it FAILS to
    COMPILE / fails (RED) — `BCaInterval` does not exist yet. Commit: `test(82-02): add failing BCa
    bootstrap tests (STATS-02)`.
    NOTE: NO `HELIX_BIN` needed — this is a pure unit test (D-01).
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run TestBCa 2>&1 | grep -Eiq 'undefined: BCaInterval|cannot find|FAIL|build failed' && echo RED-OK</automated>
  </verify>
  <done>
    `bootstrap_test.go` exists with the six behavior groups above; `go test` fails RED because
    BCaInterval is undefined; commit recorded.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN+REFACTOR — implement BCaInterval (z0 + jackknife a + seeded resample)</name>
  <read_first>
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-RESEARCH.md (§ BCa — Steps 1-5 with the exact z0/a/percentile formulas and CITED equations; § Code Examples: phiInv/phi/bcaPercentiles)
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-PATTERNS.md (§ bench/aggregator/bootstrap.go)
    - bench/aggregator/bootstrap_test.go (the contract from Task 1 — implement to satisfy it)
  </read_first>
  <action>
    Implement `bench/aggregator/bootstrap.go` (package `aggregator`) to GREEN the Task-1 tests, per
    the RESEARCH §BCa exact algorithm:
    - Helpers: `phiInv(p) = math.Sqrt2 * math.Erfinv(2*p-1)`, `phi(z) = 0.5 * math.Erfc(-z/math.Sqrt2)`,
      `clamp01`.
    - Step 1: B bootstrap replicates by resampling m indices with replacement via the injected
      `*rand.Rand` (`rng.IntN(m)`); compute stat per resample; collect and SORT theta-star.
    - Step 2: z0 = phiInv(p0) where p0 = #{theta*_b < theta_hat}/B. Degenerate guard: if every
      theta*_b == theta_hat (or p0 in {0,1} from an all-identical distribution) return the point CI
      [theta_hat, theta_hat].
    - Step 3: jackknife leave-one-out theta_(i); a = sum((mean - theta_(i))^3) / (6 * (sum((mean -
      theta_(i))^2))^1.5). Degenerate guard: den==0 (m==1 or zero spread) -> a=0.
    - Step 4: alpha1/alpha2 via the bcaPercentiles adjustment from RESEARCH §Code Examples, with the
      `den ~ 0` guard (|1 - a*(z0+z)| < 1e-12 -> percentile fallback for that endpoint) and clamp01.
    - Step 5: read lo=quantile(sorted, alpha1), hi=quantile(sorted, alpha2). PICK ONE quantile rule
      (nearest-rank `idx = clamp(round(alpha*(B-1)), 0, B-1)` recommended) and document it in a comment
      for byte-stable reproduction (A5).
    - Empty `vals` -> return (0,0,false). This MUST NOT be a percentile bootstrap (Pitfall 2).
    RUN the tests to GREEN. Then REFACTOR for clarity (extract `jackknifeAccel`, `biasCorrect`,
    `quantile` helpers) keeping tests green. Run `gofmt -w` and `go vet ./bench/aggregator/...`.
    Commits: `feat(82-02): implement BCa bootstrap interval (STATS-02)` then optionally
    `refactor(82-02): factor BCa helpers`.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run TestBCa && go vet ./bench/aggregator/...</automated>
  </verify>
  <done>
    `go test ./bench/aggregator/ -run TestBCa` is GREEN (all six behavior groups pass), including the
    BCa!=percentile-on-skew assertion and the determinism assertion; `go vet` clean; bootstrap.go
    contains z0, jackknife a, and seeded resampling (not a percentile bootstrap).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| numeric input → stats math | per-task value vectors derived from local result rows; degenerate/extreme values must not produce NaN/Inf garbage CIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02-01 | Tampering (data integrity) | NaN/Inf from div-by-zero on degenerate samples (Pitfall 6) | mitigate | Explicit guards: empty->null CI, all-identical->point CI, m==1->a=0, den~0->percentile fallback + clamp01; tests assert no NaN/Inf |
| T-82-02-02 | Repudiation / reproducibility | unseeded RNG -> non-reproducible CI (Pitfall 5) | mitigate | Injected `*rand.Rand` from `rand.NewPCG(seed,...)`; determinism test asserts byte-identical output for same seed (D-08) |
| T-82-02-SC | Tampering | npm/pip/cargo installs | accept | No installs; stdlib `math`/`math/rand/v2` only (RESEARCH audit: zero new packages) |
</threat_model>

<verification>
- `go test ./bench/aggregator/ -run TestBCa` green.
- `go vet ./bench/aggregator/...` clean.
- No `HELIX_BIN` required (pure unit, D-01).
- Manual sanity: bootstrap.go uses `math.Erfinv` + `math/rand/v2`, computes z0 AND jackknife a.
</verification>

<success_criteria>
- BCaInterval is a proper BCa (z0 + jackknife a), >=10,000 resamples supported, seeded/deterministic.
- BCa endpoints differ from percentile endpoints on a skewed sample (no fake-BCa).
- Degenerate inputs handled per D-09 (null CI / point CI / a=0), no NaN/Inf.
</success_criteria>

<output>
Create `.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-02-SUMMARY.md` when done
</output>
