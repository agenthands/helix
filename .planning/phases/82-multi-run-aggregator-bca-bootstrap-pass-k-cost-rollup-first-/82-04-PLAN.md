---
phase: 82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-leaderboard
plan: 04
type: tdd
wave: 2
depends_on: ["82-01"]
files_modified:
  - bench/aggregator/cost.go
  - bench/aggregator/cost_test.go
  - bench/aggregator/testdata/cost-table.golden.yaml
autonomous: true
requirements: [COST-02]
must_haves:
  truths:
    - "USD per result = (tokens_input*input + tokens_input_cached_read*cached + tokens_input_cache_write*input + tokens_output*output)/1e6, joining model_id to the cost-table row (D-12/D-14)"
    - "cost_per_solved_task = sum(USD over task_success==true)/count(solved); no solved -> null/omitted (D-12)"
    - "The cost-table freshness gate is fail-closed: past valid_until, >90d last_verified, or unknown model_id is a hard error (D-13)"
    - "Nil token fields are never fabricated as 0 cost: a result with all-nil token metrics has no computable cost and is excluded (Pitfall 4)"
  artifacts:
    - path: "bench/aggregator/cost.go"
      provides: "perResultUSD(metrics, CostRow) (float64, bool) + costPerSolvedTask helpers, reusing bench/cost.PriceFor for the freshness gate"
      contains: "func perResultUSD"
    - path: "bench/aggregator/cost_test.go"
      provides: "COST-02 hand-computed golden (3.555 USD) + freshness-gate fail-closed + nil-token exclusion"
      contains: "func TestCostPerSolved"
  key_links:
    - from: "bench/aggregator/cost.go"
      to: "bench/cost.PriceFor"
      via: "join result.model_id to cost-table row with injected today + freshness gate"
      pattern: "cost\\.PriceFor"
---

<objective>
Implement the cost rollup in `bench/aggregator/cost.go` (COST-02): per-result USD from the
canonical token metrics joined to `cost-table.yaml` via the `bench/cost` package's `PriceFor`
freshness gate (D-12/D-13/D-14), and `cost_per_solved_task = sum(USD over solved)/count(solved)`.
The cache-write term is priced at the standard `input_per_mtok` rate as a documented conservative
v1 approximation with a clearly-commented TODO (D-14 — do NOT expand the cost-table schema). Null
discipline: a nil token field is never fabricated as 0 cost; a result with no computable cost is
excluded from the rollup (Pitfall 4).

Purpose: cost_per_solved_task is the COST-02 headline; the per-task USD vector is the BCa-CI input
for cost_quality.md (Plan 06). TDD because COST-02 has a hand-computed golden anchor (3.555 USD).
Output: `bench/aggregator/cost.go`, `cost_test.go`, and a golden cost-table fixture.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-CONTEXT.md
@.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-RESEARCH.md
@.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-PATTERNS.md
@bench/datasets/cost-table.yaml
@bench/evaluators/metrics.go
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — COST-02 golden + freshness-gate + nil-token-exclusion test</name>
  <read_first>
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-RESEARCH.md (§ Cost Rollup: USD-per-result formula D-12/D-14, cost_per_solved_task D-12, the COST-02 hand-computed golden example table → 3.555 USD, null discipline, freshness gate D-13)
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-PATTERNS.md (§ bench/aggregator/cost.go — reuse bench/cost shape, priceFor, USD formula, golden 3.555)
    - bench/evaluators/metrics.go (the exact nullable token pointer fields: TokensInput, TokensOutput, TokensInputCachedRead, TokensInputCacheWrite — all *int)
    - bench/cost/cost_table.go (from Plan 01 — CostRow fields + PriceFor signature + LoadCostTable)
    - bench/datasets/cost-table.yaml (committed rates: claude-sonnet-4-5-20250929 input=3.0 output=15.0 cached=0.30)
  </read_first>
  <behavior>
    - Golden cost_per_solved_task (COST-02): model claude-sonnet-4-5-20250929 (input=3.0, output=15.0,
      cached=0.30). Two SOLVED tasks: T1 {ti=1_000_000, cr=0, cw=0, to=100_000} -> USD 4.50;
      T2 {ti=500_000, cr=200_000, cw=100_000, to=50_000} -> USD 2.61 (cache-write at input rate per
      D-14: 100_000*3.0/1e6 = 0.30). cost_per_solved_task = (4.50+2.61)/2 = 3.555 (assert within 1e-9).
    - Solved-only: a task with task_success==false (or no solved run) contributes to NEITHER numerator
      nor denominator. With zero solved tasks, cost_per_solved_task is null/omitted (ok==false), not
      NaN.
    - Nil-token exclusion (Pitfall 4): a result whose token metrics are all nil has no computable cost
      (perResultUSD ok==false) and is excluded; a result with SOME nil fields skips only those terms
      (a present field still prices). Assert a fully-nil result yields ok==false, not 0.0-with-ok.
    - Freshness gate fail-closed (D-13): pricing against a cost-table whose matched row has a past
      valid_until OR a >90d-stale last_verified returns a hard error; an unknown model_id returns a
      hard error. Use an INJECTED `today` (fixed date in the test) — feed a fixture with a past
      valid_until to trip the gate, and a fresh fixture to pass it.
  </behavior>
  <action>
    Write `bench/aggregator/cost_test.go` (package `aggregator`) FIRST and a golden YAML fixture
    `bench/aggregator/testdata/cost-table.golden.yaml` mirroring the committed cost-table row (so the
    golden math is anchored to a stable in-test rate, independent of future edits to the committed
    table). Encode `TestCostPerSolved` (the 3.555 golden + solved-only + nil exclusion) and
    `TestCostFreshnessGate` (past valid_until / stale last_verified / unknown model_id -> hard error,
    via injected today). FIX the public surface — recommended:
    `func perResultUSD(m metricView, row cost.CostRow) (float64, bool)` and a rollup helper
    `func costPerSolvedTask(perTaskUSD map[string]float64) (float64, bool)` (Plan 06 supplies the
    solved-task USD map). Define a small `metricView` struct OR reuse the nullable token pointers
    directly (Tokens* *int) — decide and lock the shape here. RUN -> RED. Commit:
    `test(82-04): add failing cost rollup golden + freshness tests (COST-02)`. NO HELIX_BIN.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run 'TestCostPerSolved|TestCostFreshnessGate' 2>&1 | grep -Eiq 'undefined|cannot find|FAIL|build failed' && echo RED-OK</automated>
  </verify>
  <done>
    `cost_test.go` + `testdata/cost-table.golden.yaml` exist; tests encode the 3.555 golden, solved-only,
    nil-token exclusion, and the fail-closed freshness gate (injected clock); `go test` is RED; commit
    recorded.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN+REFACTOR — implement cost.go (USD formula + rollup, reusing bench/cost.PriceFor)</name>
  <read_first>
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-RESEARCH.md (§ Cost Rollup — USD formula with the D-14 cache-write-at-input-rate term + the // TODO(D-14) comment text; § Code Examples priceFor)
    - .planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-PATTERNS.md (§ bench/aggregator/cost.go)
    - bench/cost/cost_table.go (PriceFor + LoadCostTable from Plan 01 — DO NOT re-implement the loader/gate; import and call it)
    - bench/aggregator/cost_test.go (the contract from Task 1)
  </read_first>
  <action>
    Implement `bench/aggregator/cost.go` (package `aggregator`) to GREEN the Task-1 tests:
    - `perResultUSD`: term-by-term `(ti*input + tcr*cached + tcw*input + to*output)/1_000_000` where
      each term is SKIPPED if its token pointer is nil; if ALL token fields are nil return
      (0, false) — no computable cost (Pitfall 4). Add the EXACT D-14 comment on the cache-write term:
      `// TODO(D-14): add cache_write_per_mtok column; v1 prices cache-write at the input rate (conservative)`.
    - The cost-table load + join + freshness gate is DELEGATED to `bench/cost` (Plan 01): call
      `cost.LoadCostTable(path)` once and `cost.PriceFor(ct, modelID, today)` per result. Do NOT
      duplicate the parser or the gate (Open Q1 — one source of truth).
    - `costPerSolvedTask(perTaskUSD map[string]float64) (float64, bool)`: sum / count; empty -> (0,false).
      (Per-task USD = mean USD over the task's runs — A3 — but that reduction is the aggregator's job
      in Plan 06; this helper takes the already-reduced per-solved-task USD map.)
    RUN -> GREEN. REFACTOR for clarity keeping tests green. `gofmt -w` + `go vet ./bench/aggregator/...`.
    Commit: `feat(82-04): implement cost-per-solved rollup reusing bench/cost (COST-02)`.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run 'TestCostPerSolved|TestCostFreshnessGate' && go vet ./bench/aggregator/...</automated>
  </verify>
  <done>
    Cost tests GREEN (3.555 golden, solved-only, nil exclusion, fail-closed freshness gate); cost.go
    imports `bench/cost` and calls `cost.PriceFor`/`cost.LoadCostTable` (grep confirms no duplicate
    YAML decoder or freshness logic in bench/aggregator); the cache-write D-14 TODO comment is present;
    `go vet` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local cost-table.yaml → pricing | committed file; a stale/past-dated row must fail closed so wrong USD never ships |
| nil token metric → cost arithmetic | a nil must not become $0 silently (hides missing data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04-01 | Information Disclosure / Integrity | stale/incorrect cost-table silently producing wrong USD | mitigate | Delegate to `bench/cost.PriceFor` fail-closed gate (past valid_until / >90d last_verified / unknown model_id -> hard error, D-13); injected clock for determinism |
| T-82-04-02 | Tampering (data integrity) | fabricating 0 cost for nil token fields (Pitfall 4) | mitigate | perResultUSD returns ok==false when all token fields nil; per-term skip on nil; tests assert fully-nil -> ok==false not 0.0 |
| T-82-04-03 | Tampering (correctness) | cache-write priced wrong/silently (Pitfall 7) | mitigate | D-14: cache-write at input rate with explicit TODO comment; golden test asserts T2's 0.30 cache-write term |
| T-82-04-SC | Tampering | npm/pip/cargo installs | accept | No installs; stdlib + existing gopkg.in/yaml.v3 (via bench/cost) only |
</threat_model>

<verification>
- `go test ./bench/aggregator/ -run 'TestCostPerSolved|TestCostFreshnessGate'` green.
- `go vet ./bench/aggregator/...` clean.
- cost.go reuses `bench/cost` (no duplicate parser/gate).
- No HELIX_BIN required.
</verification>

<success_criteria>
- cost_per_solved_task matches the hand-computed golden (3.555 USD); cache-write at input rate (D-14).
- Freshness gate fail-closed (past valid_until / stale / unknown model -> hard error).
- Nil token fields never fabricated as 0 cost; no-computable-cost results excluded.
</success_criteria>

<output>
Create `.planning/phases/82-multi-run-aggregator-bca-bootstrap-pass-k-cost-rollup-first-/82-04-SUMMARY.md` when done
</output>
