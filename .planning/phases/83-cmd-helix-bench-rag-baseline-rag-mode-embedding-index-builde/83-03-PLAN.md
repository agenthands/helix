---
phase: 83
plan: 03
type: tdd
wave: 3
depends_on: ["83-01", "83-02"]
files_modified:
  - bench/runtime/result.go
  - bench/runtime/result_test.go
  - bench/runtime/cell.go
  - bench/runtime/cell_test.go
  - bench/runtime/baseline_rag_test.go
  - bench/runtime/deltas.go
  - bench/runners/baseline_rag/MODE.md
  - bench/BENCH.md
autonomous: true
requirements: [ABLATE-04]

must_haves:
  truths:
    - "A baseline_rag cell drives the agent against the spawned cmd/helix-bench-rag MCP server (not the Helix daemon) and emits a schema-valid result.v2.json row"
    - "Every baseline_rag result row records a non-empty embedder_id"
    - "baseline_rag reuses runners.DefaultContract verbatim — same ModelID snapshot + budget as your_agent_full"
    - "The embedding index is built/loaded OUT-OF-BAND (before the timed agent span); embedding-API tokens are NOT charged to tokens_input/tokens_output"
    - "baseline_rag is included as a delta operand once it emits rows; BENCH.md + MODE.md describe the real arm and the budget exclusion"
  artifacts:
    - path: "bench/runtime/result.go"
      provides: "embedder_id open-provenance key threaded ResultInput -> resultDoc -> BuildResult"
      contains: "EmbedderID"
    - path: "bench/runtime/cell.go"
      provides: "baseline_rag real drive leg replacing the Phase-80 fail-close at lines 429-433"
      contains: "baseline_rag"
    - path: "bench/runtime/baseline_rag_test.go"
      provides: "schema-valid row + embedder_id + same-contract/budget assertions"
      contains: "embedder_id"
    - path: "bench/runners/baseline_rag/MODE.md"
      provides: "real-arm rewrite (no longer a fail-closed stub)"
      contains: "rag_search"
    - path: "bench/BENCH.md"
      provides: "real baseline_rag arm + budget-exclusion documentation (criterion #4)"
      contains: "baseline_rag"
  key_links:
    - from: "bench/runtime/cell.go"
      to: "cmd/helix-bench-rag"
      via: "spawn the RAG MCP server for the baseline_rag arm"
      pattern: "helix-bench-rag"
    - from: "bench/runtime/cell.go"
      to: "bench/ragindex"
      via: "out-of-band index build/load before the agent drive span"
      pattern: "ragindex\\."
    - from: "bench/runtime/result.go"
      to: "result.v2 doc"
      via: "embedder_id omitempty open key"
      pattern: "embedder_id"
---

<objective>
Wire `baseline_rag` into the bench harness: add the `embedder_id` open-provenance key to `result.v2`, replace the Phase-80 fail-close in `RunCell` with a real RAG drive leg that spawns `cmd/helix-bench-rag`, build the embedding index OUT-OF-BAND (excluded from the per-task budget), and reuse `DefaultContract` verbatim. This is ABLATE-04 success criteria #3 and #4.

Purpose: Turn `baseline_rag` from a registered fail-closed stub into a real schema-valid control arm whose rows carry the embedder_id, under the SAME fairness-contract model + budget as `your_agent_full`, with embedding-API calls excluded from the agent budget. Flip the Phase-80 deferral tests and document the real arm.
Output: `result.go` (+`embedder_id`), `cell.go` (real drive leg), flipped `cell_test.go` + new `baseline_rag_test.go`, `deltas.go` (operand), `MODE.md` + `BENCH.md` rewrites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-cmd-helix-bench-rag-baseline-rag-mode-embedding-index-builde/83-RESEARCH.md
@.planning/phases/83-cmd-helix-bench-rag-baseline-rag-mode-embedding-index-builde/83-PATTERNS.md
@.planning/phases/83-cmd-helix-bench-rag-baseline-rag-mode-embedding-index-builde/83-01-PLAN.md
@.planning/phases/83-cmd-helix-bench-rag-baseline-rag-mode-embedding-index-builde/83-02-PLAN.md

<read_first>
- bench/runtime/result.go:48-78 (ResultInput open keys), :99-127 (resultDoc open keys + omitempty discipline), :154-169 (BuildResult literal) — embedder_id mirrors the ModelID/Outcome/TraceRef open keys exactly
- bench/runtime/cell.go:417-433 (the unconditional fairness gate + the baseline_rag fail-close to REPLACE), :435-518 (sandbox/clone/socket/daemon scaffolding to reuse), :491 (the start := time.Now() span anchor — index build must be BEFORE this), :528 (prePatchSnapshot runs out-of-band — mirror that ordering for the index build), :753-779 (the BuildResult + Validate call site to thread embedder_id into)
- bench/runtime/cell_test.go:234-285 (TestDeferred* — these MUST flip)
- bench/runtime/deltas.go:18-29,128-136,257 (baseline_rag currently excluded as non-operand)
- bench/runners/fairness_contract.go:84-100 (DefaultContract — reuse verbatim; criterion #4 identity)
- bench/runtime/subprocess/daemon.go:40 (StartDaemon signature — the RAG leg spawns cmd/helix-bench-rag instead, reusing the same sandbox/socket scaffolding)
- bench/runners/baseline_rag/MODE.md + bench/BENCH.md:49,77-83 (the stub prose to rewrite)
- RESEARCH "Adding embedder_id to result.v2" example + Pitfall 3 (budget exclusion) + Runtime State Inventory "Pre-existing behavior this phase changes"
- MEMORY note: bench/runtime cell tests are HELIX_BIN-gated; go test ./... is FALSE-GREEN without HELIX_BIN
</read_first>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add embedder_id open-provenance key to result.v2 (additive minor)</name>
  <files>bench/runtime/result.go, bench/runtime/result_test.go</files>
  <behavior>
    - ResultInput gains `EmbedderID string` (alongside Outcome/TraceRef).
    - resultDoc gains `EmbedderID string `json:"embedder_id,omitempty"`` (alongside ModelID) — omitempty so honest non-RAG modes (EmbedderID=="") drop the key.
    - BuildResult threads `EmbedderID: in.EmbedderID` into the doc literal.
    - Test 1 (TestEmbedderIDOmittedWhenEmpty): a build with EmbedderID="" produces JSON with NO embedder_id key AND still passes Validate (schema additionalProperties open — additive minor, no v3).
    - Test 2 (TestEmbedderIDEmittedWhenSet): a build with EmbedderID="text-embedding-3-small" produces JSON containing `"embedder_id":"text-embedding-3-small"` and passes Validate.
  </behavior>
  <action>
  Make the three additive edits per RESEARCH "Adding embedder_id to result.v2" and the PATTERNS result.go section: ResultInput field, resultDoc omitempty field, BuildResult literal assignment. This is additive-only=minor — schema_version stays "v2", NO v3, do not touch additionalProperties. Confirm the existing committed result.v2 golden still validates (honest modes omit the key). RED first: write result_test.go assertions, watch fail, implement to GREEN.
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'TestEmbedderID'</automated>
  </verify>
  <acceptance_criteria>
  `go test ./bench/runtime/ -run 'TestEmbedderID'` passes; an empty EmbedderID build emits no key and validates; a set EmbedderID build emits the key and validates; pre-existing result.v2 golden tests still green.
  </acceptance_criteria>
  <done>embedder_id is a working additive open-provenance key; empty omitted, set emitted, both schema-valid.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Real baseline_rag drive leg in RunCell + flip deferral tests + deltas operand</name>
  <files>bench/runtime/cell.go, bench/runtime/cell_test.go, bench/runtime/baseline_rag_test.go, bench/runtime/deltas.go</files>
  <behavior>
    - RunCell no longer short-circuits baseline_rag to Deferred. For mode=="baseline_rag" it: (a) keeps the unconditional DefaultContract.Validate() gate (cell.go:417-419) UNCHANGED; (b) clones the seed repo (existing scaffolding); (c) OUT-OF-BAND, BEFORE the `start := time.Now()` agent-span anchor, builds/loads the bench/ragindex index over the cloned repo working copy (corpus_sha keyed) — mirroring how prePatchSnapshot runs before driveScript; (d) spawns cmd/helix-bench-rag (NOT subprocess.StartDaemon) as the MCP server over the per-cell socket, reusing the sandbox/clone/socket scaffolding; (e) drives the agent, runs verify/RunTests, builds a result.v2 row with EmbedderID set from selectEmbedder and Fairness=runners.DefaultContract.
    - The embedding build is excluded from the timed/budgeted span: embedder tokens MUST NOT enter tokens_input/tokens_output (Pitfall 3 / criterion #4).
    - cell_test.go TestDeferred* (lines 234-285) FLIP: they currently assert res.Deferred==true, DeferredReason contains "Phase 83", and NO result.v2.json on disk. Re-scope or replace so baseline_rag now asserts a real row. New baseline_rag_test.go: TestBaselineRagEmitsRow (schema-valid result.v2.json on disk with non-empty embedder_id) and TestBaselineRagSameContractBudget (the row's model_id == DefaultContract.ModelID; the build is contract-identical to your_agent_full; embedding tokens excluded). These are HELIX_BIN-gated.
    - deltas.go: include baseline_rag as a delta operand now that it emits rows (Open Q2 — it is the headline control arm). Update isRequiredMode / the operand set + the exclusion comments (deltas.go:18-29,128-136,257).
  </behavior>
  <action>
  Replace cell.go:429-433 with the real RAG drive leg. Keep detection BY MODE NAME (do NOT add a MODE.md frontmatter key — the resolver is strict two-key with KnownFields(true)). Reuse benchsandbox.New / Prepare / CloneRepo / SocketFor scaffolding (cell.go:435-518); where the daemon path calls subprocess.StartDaemon, the baseline_rag leg instead spawns cmd/helix-bench-rag bound to the per-cell socket (add a sibling spawn helper in subprocess/ if needed, mirroring StartDaemon's socket/handle shape). Build the index via ragindex.Open(repoDir) BEFORE the `start := time.Now()` anchor (cell.go:491) so it is outside the timed span (Pitfall 3). Thread EmbedderID into the BuildResult call (cell.go:756) for the baseline_rag arm; honest modes leave it "". Reuse runners.DefaultContract verbatim in Fairness (criterion #4 — never a separate config). Flip cell_test.go TestDeferred* and add baseline_rag_test.go. Update deltas.go to add baseline_rag as an operand and revise the exclusion comments. RED first on the new tests (run with HELIX_BIN), then implement to GREEN.
  </action>
  <verify>
    <automated>go build ./... && HELIX_BIN="$(pwd)/helix" go test ./bench/runtime/ -run 'TestBaselineRag|TestDeferred'</automated>
  </verify>
  <acceptance_criteria>
  After `make build` (so `./helix` exists), `HELIX_BIN="$(pwd)/helix" go test ./bench/runtime/ -run 'TestBaselineRag|TestDeferred'` passes: a baseline_rag cell writes a schema-valid result.v2.json with a non-empty embedder_id and model_id == DefaultContract.ModelID; the flipped TestDeferred* no longer asserts deferral; `HELIX_BIN="$(pwd)/helix" go test ./bench/runtime/...` is green (NOT run without HELIX_BIN — false-green per MEMORY). `go test ./bench/runtime/ -run TestDelta` reflects baseline_rag as an operand.
  </acceptance_criteria>
  <done>baseline_rag drives cmd/helix-bench-rag, emits a schema-valid row with embedder_id under DefaultContract, excludes embedding cost from the budget, and is a delta operand; deferral tests flipped.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite baseline_rag MODE.md + BENCH.md from stub to real arm</name>
  <files>bench/runners/baseline_rag/MODE.md, bench/BENCH.md</files>
  <action>
  Rewrite bench/runners/baseline_rag/MODE.md body from "registered fail-closed stub / deferred to Phase 83" to the real arm: keep the two-key frontmatter (`mode: baseline_rag`, `profile: baseline`) STRUCTURALLY UNCHANGED (resolver is KnownFields-strict); rewrite the body to describe the standalone cmd/helix-bench-rag MCP server (4 tools), the chromem-go embedding index, and the same-contract/budget invariant. Update bench/BENCH.md:49 (the mode table row, drop "fail-closed stub / no row until Phase 83") and :77-83 (rewrite the stub paragraph) to document: baseline_rag is the real RAG control arm; it reuses DefaultContract's model snapshot + budget identically to your_agent_full; embedding-API index-build calls are built OUT-OF-BAND and are NOT charged to the agent's per-task budget (criterion #4); the embedder_id is recorded in every row. Reference EMBED-CHOICE.md for the model pin. Docs only — name symbols, no code fences with implementations.
  </action>
  <verify>
    <automated>grep -q "rag_search" bench/runners/baseline_rag/MODE.md && grep -qi "out-of-band\|not charged\|excluded from" bench/BENCH.md && ! grep -q "fail-closed stub" bench/runners/baseline_rag/MODE.md</automated>
  </verify>
  <acceptance_criteria>
  MODE.md frontmatter is still the two-key form and parses (`go test ./bench/runners/... -run TestMode` green); MODE.md body describes the real 4-tool arm (no "fail-closed stub"); BENCH.md documents the budget exclusion (criterion #4) and the embedder_id provenance.
  </acceptance_criteria>
  <done>MODE.md + BENCH.md describe the real baseline_rag arm and the same-model-same-budget invariant with embedding cost excluded; resolver still parses the frontmatter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench cell → RAG server subprocess | the cell spawns cmd/helix-bench-rag over a per-cell socket |
| index build → per-task budget | the boundary that must EXCLUDE embedding-API cost from tokens_input/output |
| result row → published leaderboard | embedder_id provenance must be present on every baseline_rag row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03-01 | Repudiation | result.v2 row | mitigate | Every baseline_rag row records embedder_id so "weak embedder" pushback is answerable with the recorded model string (criterion #3) |
| T-83-03-02 | Tampering (fairness) | cell.go fairness gate | mitigate | Reuse DefaultContract verbatim (no separate config) + the unconditional Validate() gate stays; TestBaselineRagSameContractBudget asserts model_id identity (criterion #4) |
| T-83-03-03 | Information disclosure (budget skew) | out-of-band index build | mitigate | Build the index before the `start := time.Now()` span anchor; embedder tokens excluded from tokens_input/output (Pitfall 3) |
| T-83-03-04 | Tampering | cmd/helix-bench-rag spawn path | accept | The RAG server is the same trusted local binary built from this tree; no untrusted input selects the binary path (low risk, local bench) |
</threat_model>

<verification>
- `go build ./...` clean; `make build` produces `./helix`.
- `go test ./bench/runtime/ -run 'TestEmbedderID'` green (pure).
- `HELIX_BIN="$(pwd)/helix" go test ./bench/runtime/...` green (the baseline_rag cell row tests — NOT run without HELIX_BIN, false-green per MEMORY).
- `go test ./bench/runners/...` green (MODE.md still parses).
- `go vet ./...` + `make vet` clean (includes the Plan 02 vet-bench-rag-leakage gate).
</verification>

<success_criteria>
- A baseline_rag ToolBench-Go run produces schema-valid result.v2.json rows with embedder_id in every row (criterion #3).
- baseline_rag reuses DefaultContract's model+budget; embedding-API calls are built out-of-band and NOT charged to the per-task budget; documented in BENCH.md (criterion #4).
- The Phase-80 deferral tests are flipped; baseline_rag is a delta operand; MODE.md + BENCH.md describe the real arm.
</success_criteria>

<output>
Create `.planning/phases/83-cmd-helix-bench-rag-baseline-rag-mode-embedding-index-builde/83-03-SUMMARY.md` when done.
</output>
