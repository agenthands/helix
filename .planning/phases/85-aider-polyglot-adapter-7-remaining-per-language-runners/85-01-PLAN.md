---
phase: 85
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bench/runtime/result.go
  - bench/schema/result.v2.schema.json
  - bench/runtime/cell.go
  - bench/aggregator/aggregate.go
  - bench/aggregator/report.go
  - bench/aggregator/load.go
  - bench/runtime/result_test.go
  - bench/aggregator/aggregate_test.go
autonomous: true
requirements: [ADAPTER-AIDER-01]
must_haves:
  truths:
    - "result.v2.json carries an optional `language` provenance field populated from Cell.Language"
    - "Old result.v2 artifacts without `language` still validate (additive-minor, omitempty)"
    - "The aggregator can slice pass-rate per language, answering 'Python pass-rate' (SC#1)"
  artifacts:
    - path: "bench/runtime/result.go"
      provides: "Language field threaded through ResultInput, resultDoc (omitempty), BuildResult"
      contains: "Language"
    - path: "bench/schema/result.v2.schema.json"
      provides: "optional `language` property doc entry (no v3 bump; additionalProperties stays open)"
      contains: "language"
    - path: "bench/aggregator/aggregate.go"
      provides: "per-language reduction analogous to reduceLeaderRow, reading language from Row.Doc"
      contains: "language"
  key_links:
    - from: "bench/runtime/cell.go"
      to: "bench/runtime/result.go ResultInput"
      via: "Language: cfg.Language at the BuildResult call site"
      pattern: "Language:\\s*cfg\\.Language"
    - from: "bench/aggregator/aggregate.go"
      to: "Row.Doc[\"language\"]"
      via: "open-key read mirroring rowModelID"
      pattern: "Doc\\[\"language\"\\]"
---

<objective>
Add the additive `language` axis to the bench result pipeline so SC#1's per-language pass-rate is sliceable. This is the shared dependency the Aider adapter (Plan 07) consumes; the 7 runner plans (03-05) produce `TestOutcome` and rely on the cell threading `Cell.Language` into the persisted result.

Closes RESEARCH Gap (a) / Pitfall 3 ("no language axis downstream") and Open Q1 (RESOLVED: additive optional `language` field + aggregator per-language grouping). additive-minor only — schema_version stays "v2", `additionalProperties` is already OPEN at the top level (mirror the `embedder_id` / `ablation_status` precedent verbatim).

Purpose: without a persisted `language` field the aggregator groups only (task, mode) and cannot emit per-language rows; SC#1 ("per-language pass-rate matches sanity benchmarks") is unanswerable.
Output: `language` threaded through result.go + schema + cell; an optional per-language reduction in the aggregator; golden tests proving both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@bench/runtime/result.go
@bench/runtime/cell.go
@bench/aggregator/aggregate.go
@bench/aggregator/report.go
@bench/aggregator/load.go
@bench/schema/result.v2.schema.json
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Thread the additive `language` field through result.v2 (RED then GREEN)</name>
  <files>bench/runtime/result.go, bench/schema/result.v2.schema.json, bench/runtime/cell.go</files>
  <read_first>
    - bench/runtime/result.go (the `EmbedderID` open-provenance thread: ResultInput field block ~lines 57-63, resultDoc omitempty field ~lines 125-129, BuildResult assembly ~line 180 `EmbedderID: in.EmbedderID`)
    - bench/schema/result.v2.schema.json (the `ablation_status` / `embedder_id` optional-property doc entries; top-level `$comment` additive-minor policy; `additionalProperties` is OPEN)
    - bench/runtime/cell.go (the `BuildResult(ResultInput{...})` call site ~lines 764-780; `Cell.Language` field ~line 189)
  </read_first>
  <behavior>
    - RED: a result_test.go case `expect(BuildResult(ResultInput{Language:"python", ...})).doc["language"] == "python"`.
    - RED: a case where `Language:""` produces a doc with NO `language` key (omitempty drops it) so old artifacts still validate via Validate().
    - RED: a Validate() case on a fixture WITHOUT `language` still passes (backward compatible — additionalProperties open, field optional).
  </behavior>
  <action>
    Mirror the `EmbedderID` thread EXACTLY (it is the proven additive-open-key template). In bench/runtime/result.go: (1) add `Language string` to `ResultInput` right beside `EmbedderID` with a doc comment noting it is populated from `Cell.Language` and is additive-minor; (2) add `Language string `json:"language,omitempty"`` to `resultDoc` beside the `EmbedderID` json-tagged field; (3) add `Language: in.Language` to the `resultDoc{...}` assembly in `BuildResult` beside `EmbedderID: in.EmbedderID`. In bench/runtime/cell.go: at the `BuildResult(ResultInput{...})` call site (~line 764) add `Language: cfg.Language,` so the persisted result records the cell's language axis (the field already exists at cell.go:189). In bench/schema/result.v2.schema.json: add a `language` property doc entry mirroring the `ablation_status` entry verbatim — `"type": "string"`, an OPTIONAL additive-minor description noting honest provenance, schema_version stays "v2", do NOT add it to `required`, do NOT close `additionalProperties`. Write the failing result_test.go cases FIRST (RED), then make them pass (GREEN).
  </action>
  <verify>
    <automated>go test ./bench/runtime/ -run 'Result|Language' -count=1</automated>
  </verify>
  <done>BuildResult emits `language` when set and omits it when empty; Validate() passes both with and without the field; cell threads cfg.Language; go vet clean.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add the optional per-language reduction to the aggregator (RED then GREEN)</name>
  <files>bench/aggregator/aggregate.go, bench/aggregator/load.go, bench/aggregator/report.go</files>
  <read_first>
    - bench/aggregator/aggregate.go (`Aggregate` orchestrator ~line 65; `reduceLeaderRow` ~line 156 with its hardcoded `Benchmark: "internal-toolbench"`; `rowModelID` open-key read ~line 290 `r.Doc["model_id"]`)
    - bench/aggregator/load.go (`Row.Doc map[string]json.RawMessage` ~line 48 preserves the full doc; `Loaded.Modes` ~line 85)
    - bench/aggregator/report.go (`LeaderRow` ~line 46; `Report` ~line 94; `renderLeaderboard` ~line 176)
  </read_first>
  <behavior>
    - RED: a `rowLanguage(r Row) string` reads `r.Doc["language"]` (mirroring rowModelID), returning "" when the key is absent.
    - RED: an aggregate fixture with rows tagged language=python (1 pass) and language=rust (1 fail) yields a per-language pass-rate slice reporting python=1.0, rust=0.0.
    - RED: rows with NO `language` key fall into an "" (unsliced) bucket and do not crash the existing (mode×benchmark) leaderboard.
  </behavior>
  <action>
    Add a `rowLanguage(r Row) string` helper beside `rowModelID` in aggregate.go reading `r.Doc["language"]` the same open-key way (json.Unmarshal of the RawMessage into a string; "" when absent — never derive from task_id here, that is the documented fallback only). Add an OPTIONAL per-language reduction: a small `reduceLanguageRows(loaded, cfg, ...)` that groups loaded rows by `rowLanguage`, computes per-language pass-rate (reuse the existing per-task success scalar path from reduceLeaderRow — do NOT reimplement BCa/PassAtK), and appends them to a new `Report.ByLanguage []LanguageRow` field (add a minimal `LanguageRow{Language string; PassRate float64; N int}` to report.go). Wire it into `Aggregate` alongside the existing `rep.Leaderboard` append. Do NOT change the existing (mode×benchmark) leaderboard output shape — this is purely additive (SC#1 needs the per-language SLICE to exist and be correct; rendering it into a file is downstream Phase 89). Write the failing aggregate_test.go fixture+assertions FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>go test ./bench/aggregator/ -run 'Language|Aggregate' -count=1</automated>
  </verify>
  <done>Aggregate populates Report.ByLanguage with correct per-language pass-rates from the `language` doc key; absent-key rows bucket cleanly; existing leaderboard tests unchanged and green; go vet clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| result.v2.json artifact → aggregator | persisted doc strings (`language`) are read back and grouped on |
| cell config → persisted result | `Cell.Language` is the source; already V5-validated via validateCellKey (cell.go:380) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01-01 | Tampering | `language` value read from an untrusted result doc into a path/group key | mitigate | `language` is a grouping label only (never joined into a filesystem path here); Cell.Language is already validated by validateCellKey before persistence (cell.go:380) |
| T-85-01-02 | Information Disclosure | additive schema field leaks nothing — it is the language axis already public in the matrix | accept | language axis is public benchmark metadata; no PII or secret |
| T-85-01-03 | Tampering | schema change accidentally tightens validation and rejects old artifacts | mitigate | field is OPTIONAL + omitempty; `required` unchanged; `additionalProperties` left OPEN; a backward-compat Validate() test on a pre-language artifact is part of Task 1 |
</threat_model>

<verification>
- `go test ./bench/runtime/ ./bench/aggregator/ -count=1` green.
- `go vet ./bench/...` clean.
- A pre-existing result.v2 artifact (no `language` key) still validates (backward-compat assertion in Task 1).
- `git log --oneline --grep='^test(85-01)'` and `--grep='^feat(85-01)'` both non-empty (RED→GREEN gate).
</verification>

<success_criteria>
- result.v2 has an optional `language` field, populated from Cell.Language, omitted when empty.
- The schema documents `language` as optional additive-minor (schema_version still "v2").
- The aggregator exposes a correct per-language pass-rate slice (SC#1 substrate).
- Zero change to existing (mode×benchmark) leaderboard output; all prior aggregator tests still green.
</success_criteria>

<output>
Create `.planning/phases/85-aider-polyglot-adapter-7-remaining-per-language-runners/85-01-SUMMARY.md` when done.
</output>
