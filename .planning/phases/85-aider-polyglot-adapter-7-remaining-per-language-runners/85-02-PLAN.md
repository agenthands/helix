---
phase: 85
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - bench/container/run.go
  - bench/container/run_test.go
  - bench/container/engine.go
autonomous: true
requirements: [ADAPTER-AIDER-01]
must_haves:
  truths:
    - "A hermetic test asserts the container run argv contains --network=none, --rm, and the -v <repoDir>:/work mount WITHOUT a live engine"
    - "Run uses fixed argv + strict env allowlist + the -- terminator (no shell injection); never the docker Go SDK"
    - "Live container Run skips cleanly when no engine is on PATH (Detect → errEngineUnavailable)"
  artifacts:
    - path: "bench/container/run.go"
      provides: "runArgs pure-argv builder + Run(ctx, image, args, mounts, netNone) seam"
      contains: "--network=none"
    - path: "bench/container/run_test.go"
      provides: "argv-assertion hermetic test + engine-absent live-skip test"
      contains: "network=none"
  key_links:
    - from: "bench/container/run.go Run"
      to: "os/exec fixed argv"
      via: "exec.CommandContext(e.bin, args...) with allowlistEnv + procGroupAttr (mirror PullByDigest)"
      pattern: "allowlistEnv\\(\\)"
    - from: "bench/container/run.go runArgs"
      to: "image/mount validation"
      via: "fail-close before crossing os/exec (mirror pullArgs isHexSHA256/isValidRepo)"
      pattern: "--network=none"
---

<objective>
Add the missing `--network=none` container-run seam to `bench/container/`. Today the package only does pull+verify (`PullByDigest`, `VerifyThenPull`); there is no run/exec. SC#3 ("tests run with `--network=none`") names container execution of hermetic toolchain tasks, so the seam must exist and be argv-provable hermetically.

Closes RESEARCH Pitfall 4 ("SC#3 container-run seam does not exist") and Open Q2 (RESOLVED: build a fixed-argv `Run` seam, prove `--network=none` via an argv-assertion test WITHOUT a live engine; the live run skips on Detect failure). The toolchain-IMAGE build (offline dep pre-resolution) is the larger, toolchain-host sub-effort and is recorded as toolchain-gated (per Validation Strategy "Toolchain-Gated Verifications") — this plan delivers the run seam + the `--network=none` argv proof + the env/argv security discipline, which is the hermetically-verifiable contract.

Purpose: the SC#3 enforcement point (`--network=none`) must be a real, fixed-argv seam in-tree and unit-proven, not a planning assumption.
Output: `runArgs` + `Run` in bench/container/run.go; a hermetic argv-assertion test; a live-skip test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@bench/container/engine.go
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: runArgs pure-argv builder for --network=none run (RED then GREEN)</name>
  <files>bench/container/engine.go, bench/container/engine_test.go, bench/container/run.go</files>
  <read_first>
    - bench/container/engine.go (`pullArgs` ~lines 43-55 returning `([]string, error)` and fail-closing via isHexSHA256/isValidRepo BEFORE producing argv; the `--` option-parsing terminator at line 54; `errBadRepo`/`errBadDigest` sentinels; `isValidRepo`)
    - bench/container/engine_test.go (`TestPullByDigestUsesAtSha256NeverTag` ~line 80 — the argv-assertion shape to mirror: call the builder, assert the returned slice equals the expected fixed argv)
  </read_first>
  <behavior>
    - RED: `runArgs(image, []string{"pytest","-q"}, map[string]string{repoDir:"/work"}, true)` returns argv beginning `["run","--rm","--network=none","-v","<repoDir>:/work","--",image,"pytest","-q"]`.
    - RED: a mount source containing `..` or an absolute-escape is rejected (fail-close, mirror isValidRepo style) before argv is produced.
    - RED: an image with a leading `-` (flag-smuggling) is rejected with a sentinel error.
    - RED: `netNone=false` omits `--network=none` (so the flag presence is exactly the network policy, not always-on).
  </behavior>
  <action>
    Create bench/container/run.go. Add `runArgs(image string, cmdArgs []string, mounts map[string]string, netNone bool) ([]string, error)` mirroring `pullArgs` discipline: validate `image` with the existing `isValidRepo` (reject leading `-`/flag-smuggling), validate each mount source path-segment-style (reject `..`, post-clean escape — mirror the bench/ragindex validatePath rejection and engine.go fail-close ordering), then assemble the FIXED argv: `run`, `--rm`, then `--network=none` IFF `netNone`, then a `-v <src>:<dst>` pair per mount (deterministic mount order — sort the keys so the argv is byte-stable for the golden assertion), then `--` to terminate option parsing, then `image`, then the cmdArgs. Return an error (never partial argv) on any bad input. Use the package's existing `errBadRepo` for the image case and a new `errBadMount` sentinel for mounts. Write the failing run_test.go argv assertions FIRST (RED), then implement (GREEN). NO docker Go SDK (the `make verify-no-docker-sdk` gate enforces it).
  </action>
  <verify>
    <automated>go test ./bench/container/ -run 'RunArgs|Network|Mount' -count=1</automated>
  </verify>
  <done>runArgs returns the exact fixed argv with `--network=none` present iff netNone; bad image/mount inputs fail-close before argv; deterministic mount ordering; go vet clean.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Run executor + engine-absent live-skip (RED then GREEN)</name>
  <files>bench/container/engine.go, bench/container/run.go</files>
  <read_first>
    - bench/container/engine.go (`PullByDigest` ~lines 62-78 — the exact executor shape to mirror: build args, `exec.CommandContext(e.bin, args...)`, `cmd.SysProcAttr = procGroupAttr()`, `cmd.Env = allowlistEnv()`, wrap the error; `Detect` ~lines 29-36 returning `errEngineUnavailable`; `allowlistEnv` ~lines 83-91)
  </read_first>
  <behavior>
    - RED (hermetic): given an `*Engine` with a stub bin, `Run` invokes the binary with exactly the `runArgs` output (assert via a recorded-argv shim or by asserting the constructed argv equals runArgs — keep it hermetic, no real engine).
    - RED (live-gated): a `TestRunLiveSkipsWithoutEngine` calls `Detect()`; if it returns `errEngineUnavailable` the test `t.Skip`s cleanly (no engine in this env) — it is NOT the sole proof; the argv assertion in Task 1 + this task's hermetic argv check are the authoritative proofs.
  </behavior>
  <action>
    In bench/container/run.go add `func (e *Engine) Run(ctx context.Context, image string, cmdArgs []string, mounts map[string]string, netNone bool) error` mirroring `PullByDigest` EXACTLY: call `runArgs` (return its error verbatim — fail-close before os/exec), then `cmd := exec.CommandContext(ctx, e.bin, args...)`, `cmd.SysProcAttr = procGroupAttr()` (ctx group-kill), `cmd.Env = allowlistEnv()` (PATH/HOME/HELIX_CACHE_DIR only — never the full env, T-84-01-01), `cmd.Run()`, wrap the error with context. Add the hermetic argv-equivalence test and the `Detect`-gated live-skip test in run_test.go. The live test MUST `t.Skip` (not fail, not pass-vacuously) when `Detect()` yields `errEngineUnavailable`. Write the tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>go test ./bench/container/ -count=1 && go vet ./bench/container/</automated>
  </verify>
  <done>Run executes the fixed argv with the strict env allowlist + process-group ownership; the live test skips cleanly with no engine present; `make verify-no-docker-sdk` still passes (no SDK import added); go vet clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helix-bench process → container engine | fixed argv crosses os/exec; untrusted task/image/mount values must never become flags or escape the mount root |
| container task code → host network | `--network=none` is the elevation barrier for untrusted Exercism test code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02-01 | Tampering | argv flag-smuggling via image/mount (a `-`-leading value reaching docker/podman as a flag) | mitigate | runArgs validates image via isValidRepo (rejects leading `-`) and mounts via path-segment rejection; `--` terminator before image/cmdArgs (mirror engine.go:54) — fail-close before crossing os/exec |
| T-85-02-02 | Elevation of Privilege | untrusted Exercism test code reaching the host network | mitigate | `--network=none` asserted present in the run argv (Task 1 golden); offline dep resolution is the image-build sibling (toolchain-gated) |
| T-85-02-03 | Information Disclosure | full parent env leaking into the engine/container subprocess | mitigate | `allowlistEnv()` forwards only PATH/HOME/HELIX_CACHE_DIR (engine.go:83-91) — reused verbatim, never `os.Environ()` |
| T-85-02-04 | Tampering | shelling out (sh -c) enabling injection | mitigate | fixed argv via exec.CommandContext, never a shell; same discipline as PullByDigest |
| T-85-02-SC | Tampering | adding the docker Go SDK as a dependency | mitigate | os/exec only; `make verify-no-docker-sdk` gate is run as part of `go vet` and asserted in <done> |
</threat_model>

<verification>
- `go test ./bench/container/ -count=1` green (argv assertion + live-skip).
- `make verify-no-docker-sdk` green (no SDK import).
- `go vet ./bench/container/` clean.
- `git log --oneline --grep='^test(85-02)'` and `--grep='^feat(85-02)'` both non-empty.
</verification>

<success_criteria>
- `bench/container/Run` exists and runs a fixed argv containing `--network=none`, `--rm`, and the `-v <repoDir>:/work` mount, with the strict env allowlist.
- The argv is hermetically asserted (no live engine needed) — this is the authoritative SC#3 proof.
- The live run skips cleanly when no engine is on PATH; no docker Go SDK is introduced.
</success_criteria>

<output>
Create `.planning/phases/85-aider-polyglot-adapter-7-remaining-per-language-runners/85-02-SUMMARY.md` when done.
</output>
