---
phase: 85
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - bench/languages/python/runner.go
  - bench/languages/python/runner_test.go
  - bench/languages/python/testdata/pytest_report.json
  - bench/languages/java/runner.go
  - bench/languages/java/runner_test.go
  - bench/languages/java/testdata/surefire-TEST.xml
  - bench/languages/csharp/runner.go
  - bench/languages/csharp/runner_test.go
  - bench/languages/csharp/testdata/results.trx
  - bench/datasets/internal-toolbench/python/
  - bench/datasets/internal-toolbench/java/
  - bench/datasets/internal-toolbench/csharp/
  - bench/languages/coverage_python_test.go
autonomous: false
requirements: [TOOLBENCH-03, TOOLBENCH-06, TOOLBENCH-07]
user_setup:
  - service: pypi-pytest-json-report
    why: "Verify the pytest-json-report package name before pinning it in the Python toolchain image (RESEARCH [SUS] download-gap, not a slopsquat)"
    dashboard_config:
      - task: "Confirm pytest-json-report (numirias/pytest-json-report) on pypi.org/project/pytest-json-report before pinning"
        location: "https://pypi.org/project/pytest-json-report/"
must_haves:
  truths:
    - "The Python runner parses a committed pytest-json report into pass/fail/skip rows WITHOUT invoking pytest (hermetic golden test)"
    - "The Java runner parses committed surefire XML into rows WITHOUT mvn/javac (which are absent in this env); the live mvn layer skips cleanly"
    - "The C# runner parses committed TRX XML into rows; Passed is gated on exit code, never inferred from zero failing rows"
    - "Python ≥8/10, Java ≥8/10, C# ≥6/10 capability coverage is met by authored capability-tagged task.json fixtures"
  artifacts:
    - path: "bench/languages/python/runner.go"
      provides: "PyRunner implementing LanguageRunner (pytest --json-report)"
      contains: "languages.Register(\"internal-toolbench\", \"python\""
    - path: "bench/languages/java/runner.go"
      provides: "JavaRunner (mvn test surefire XML); live-skip no JDK"
      contains: "languages.LanguageRunner"
    - path: "bench/languages/csharp/runner.go"
      provides: "CSharpRunner (dotnet test --logger trx)"
      contains: "languages.LanguageRunner"
  key_links:
    - from: "bench/languages/python/runner.go"
      to: "languages registry"
      via: "init() Register(internal-toolbench, python)"
      pattern: "Register\\(\"internal-toolbench\", \"python\""
    - from: "bench/languages/<L>/runner_test.go"
      to: "testdata committed golden report"
      via: "os.ReadFile of the captured reporter output, parsed with NO subprocess"
      pattern: "ReadFile\\(\"testdata/"
---

<objective>
Land three file-reporter LanguageRunners by cloning the Phase 78 Go runner template verbatim, swapping only `Detect`, `Setup`, the `exec.CommandContext` argv, and the parser: **Python** (`pytest --json-report --json-report-file=`, TOOLBENCH-03, ≥8/10), **Java** (`mvn test -Dsurefire.useFile=false` → surefire XML, TOOLBENCH-06, ≥8/10 — mvn+javac ABSENT here so the live layer skips), **C#** (`dotnet test --logger "trx;LogFileName="`, TOOLBENCH-07, ≥6/10).

The exit-code-authoritative gate, the `ctx.Err()`→infra vs `*exec.ExitError`→exit-code split, and the `var _ languages.LanguageRunner = (*XRunner)(nil)` conformance assertion are IDENTICAL across all three (and the four runners in plans 04-05). The seam (registry, grader, cell dispatch, coverage gate) is already built — these runners drop in with zero seam changes.

**The dominant risk this phase (RESEARCH Pitfall 1 / MEMORY false-green):** a runner whose only test invokes the live toolchain becomes a vacuous SKIP when the toolchain is absent. Every runner here gets a HERMETIC golden-fixture parser test (committed real reporter output, NO subprocess) as the SOLE authoritative proof; the live test is additive and `t.Skip`s cleanly.

Purpose: TOOLBENCH-03/06/07 coverage with hermetic proof that survives the absent javac/mvn and the npx-only env.
Output: 3 runners + 3 hermetic golden tests + per-language capability fixtures meeting the ≥N/10 gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@bench/languages/go/runner.go
@bench/languages/runner.go
@bench/languages/coverage.go
@bench/datasets/internal-toolbench/go/IT-go-rename-safety-1/task.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify pytest-json-report package legitimacy before pinning</name>
  <files>(none — verification gate; no files written until approved)</files>
  <action>Pause for a blocking human legitimacy verification of the pytest-json-report package name BEFORE Task 2 commits it into the Python runner argv or the toolchain image dep list. This gate is never auto-approvable (package-legitimacy checkpoints ignore workflow.auto_advance).</action>
  <verify>Human confirms the package on pypi.org per the steps below.</verify>
  <done>Operator typed "approved" (or named an alternative reporter); the chosen name is recorded for Task 2.</done>
  <what-built>RESEARCH flagged `pytest-json-report` [SUS] (the verdict reason is `unknown-downloads` — a registry download-count gap, NOT a slopsquat; it is the mature numirias plugin). The Package Legitimacy Gate requires a blocking human-verify before this name is committed/pinned in the Python runner argv + the toolchain image dep list.</what-built>
  <how-to-verify>
    1. Visit https://pypi.org/project/pytest-json-report/ and confirm the package exists, is published by `numirias`, and links to github.com/numirias/pytest-json-report.
    2. Confirm the `--json-report --json-report-file=<path>` CLI flags match what the Python runner will invoke.
    3. Confirm it is the package you intend to bake into the offline Python toolchain image (vs an alternative like pytest's built-in `--report-log`).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with pytest-json-report, or name the alternative reporter to use instead.</resume-signal>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Python runner + hermetic pytest-json golden + capability fixtures (RED then GREEN)</name>
  <files>bench/languages/go/runner.go, bench/languages/runner.go, bench/languages/coverage.go, bench/datasets/internal-toolbench/go/IT-go-rename-safety-1/task.json, bench/languages/python/runner.go, bench/datasets/internal-toolbench/python/IT-python-<cap>-1/task.json</files>
  <read_first>
    - bench/languages/go/runner.go (the FULL template: init() Register + conformance assertion lines 19-27; Detect 30-33; Setup 41-43 ctx-first; Capabilities 46-59; RunTests exit-code gate + ctx/ExitError split 70-104; the parser + malformed-line resync 127-159)
    - bench/languages/runner.go (the Capability enum constants lines 25-36 — string values are the source of truth for task.json `capability`; TestOutcome/TestResult shapes)
    - bench/languages/coverage.go (the ≥N/10 gate: Coverage(corpusRoot, benchmark, lang, declared) decodes ONLY the `capability` field from each task.json; benchmark is an explicit param)
    - bench/datasets/internal-toolbench/go/IT-go-rename-safety-1/task.json (the fixture shape to clone: id/benchmark/language/capability/prompt/verify)
  </read_first>
  <behavior>
    - RED: `parsePytestJSON(raw)` over a COMMITTED testdata/pytest_report.json (1 passed + 1 failed + 1 skipped) returns 3 rows with correct Passed/Skipped tri-state, NO subprocess.
    - RED: a `TestPyCapabilitiesAtLeast8` asserts `coverage.Coverage(corpusRoot, "internal-toolbench", "python", PyRunner{}.Capabilities())` reports Covered ≥ 8 from the authored fixtures.
    - RED (live, additive): `TestRunTestsLive` skips when `exec.LookPath("pytest")` (or pytest-json-report absent) fails — never the sole proof.
  </behavior>
  <action>
    Create bench/languages/python/runner.go as a clone of go/runner.go: package python; `init(){ languages.Register("internal-toolbench","python",&PyRunner{}) }`; `var _ languages.LanguageRunner=(*PyRunner)(nil)`; `Detect` checks for `pyproject.toml`/`setup.py`/`requirements.txt`; `Setup` returns `ctx.Err()` (ctx-first, WR-05); `Capabilities()` declares ≥8 of the 10 enum constants; `RunTests` runs `exec.CommandContext(ctx,"pytest","--json-report","--json-report-file="+filepath.Join(repoDir,".report.json"),"-q")` with `cmd.Dir=repoDir`, keeps the IDENTICAL ctx.Err()→infra and *exec.ExitError→exitCode split, reads the report file, and returns `TestOutcome{Passed:exitCode==0, Tests:parsePytestJSON(raw), Raw:raw, ExitCode:exitCode}`. Write `parsePytestJSON` decoding `tests[].nodeid`+`tests[].outcome` (passed/failed/skipped) and `summary` via encoding/json struct (only needed fields). Commit testdata/pytest_report.json (a real pytest-json-report sample — capture from a live pytest run if pytest+plugin are installable, else hand-build from the documented schema and label it in the test). Author ≥8 capability-tagged fixtures under bench/datasets/internal-toolbench/python/IT-python-<cap>-1/task.json (distinct `capability` values from the enum, language="python", benchmark="internal-toolbench"). Add the coverage assertion test (coverage_python_test.go in the languages package, mirroring how the Go coverage gate is tested). Write hermetic tests FIRST (RED), then the runner (GREEN). Do NOT alter `Passed: exitCode==0` (Pitfall 4).
  </action>
  <verify>
    <automated>go test ./bench/languages/python/ ./bench/languages/ -run 'Pytest|PyCapabilities|Coverage' -count=1</automated>
  </verify>
  <done>parsePytestJSON returns correct tri-state rows from the committed fixture with no subprocess; ≥8/10 coverage asserted; live test skips cleanly; go vet clean.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Java + C# runners + hermetic surefire/TRX goldens + capability fixtures (RED then GREEN)</name>
  <files>bench/languages/go/runner.go, bench/languages/python/runner.go, bench/languages/java/runner.go, bench/languages/csharp/runner.go, bench/datasets/internal-toolbench/</files>
  <read_first>
    - bench/languages/go/runner.go (same template as Task 2)
    - bench/languages/python/runner.go (Task 2's clone — the just-established per-language pattern)
    - RESEARCH §Reporter Formats: surefire `<testcase name= classname=>` + child `<failure>/<error>/<skipped>` (absent child ⇒ pass), `<testsuite tests= failures= errors=>`; TRX `<UnitTestResult testName= outcome="Passed|Failed|...">` + `<Counters total= passed= failed=>`
  </read_first>
  <behavior>
    - RED: `parseSurefireXML(raw)` over a COMMITTED testdata/surefire-TEST.xml (≥1 pass + ≥1 failure) returns correct rows via encoding/xml struct unmarshal (NOT regex), NO subprocess.
    - RED: `parseTRX(raw)` over a COMMITTED testdata/results.trx (the live-captured `outcome="Passed"`/`outcome="Failed"` sample from RESEARCH) returns correct rows.
    - RED: Java `TestJavaCapabilitiesAtLeast8` and C# `TestCSharpCapabilitiesAtLeast6` pass from authored fixtures.
    - RED (live, additive): Java `TestRunTestsLive` skips when `exec.LookPath("mvn")` fails (mvn ABSENT here); C# live skips when `dotnet` absent.
  </behavior>
  <action>
    Create bench/languages/java/runner.go (package java; Register internal-toolbench/java; Detect checks `pom.xml`; RunTests argv `exec.CommandContext(ctx,"mvn","test","-Dsurefire.useFile=false")`, then read+parse `target/surefire-reports/TEST-*.xml` via `filepath.Glob`; `parseSurefireXML` uses encoding/xml decoding only `<testcase name classname>` + child `<failure>/<error>/<skipped>` presence; ≥8 declared capabilities). Create bench/languages/csharp/runner.go (package csharp; Register internal-toolbench/csharp; Detect checks `*.csproj`/`*.sln`; RunTests argv `exec.CommandContext(ctx,"dotnet","test","--logger","trx;LogFileName=R.trx")`, parse the TRX file; `parseTRX` decodes `<UnitTestResult testName outcome>`; ≥6 declared capabilities). Both keep the IDENTICAL exit-code gate + ctx/ExitError split + conformance assertion. Commit testdata/surefire-TEST.xml — sourced from a REAL surefire run (A5: NOT hand-fabricated; if no JDK is reachable, label the fixture provenance in the test comment and flag it for replacement with a real capture in CI). Commit testdata/results.trx from the RESEARCH live-captured dotnet 8 sample. Author ≥8 java + ≥6 csharp capability-tagged fixtures under bench/datasets/internal-toolbench/{java,csharp}/. Add coverage assertion tests. Write hermetic tests FIRST (RED), then runners (GREEN). XML parse = encoding/xml struct unmarshal, NEVER regex (RESEARCH §Don't Hand-Roll).
  </action>
  <verify>
    <automated>go test ./bench/languages/java/ ./bench/languages/csharp/ ./bench/languages/ -run 'Surefire|TRX|JavaCapabilities|CSharpCapabilities' -count=1</automated>
  </verify>
  <done>Surefire and TRX parsers return correct rows from committed fixtures with no subprocess; Java ≥8/10, C# ≥6/10 coverage asserted; live mvn/dotnet layers skip cleanly when absent; go vet clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helix-bench → language toolchain (pytest/mvn/dotnet) | fixed argv crosses os/exec; repoDir holds untrusted task code |
| committed reporter fixtures → parser | trusted, in-repo; the parser must not crash on malformed XML/JSON (resync/ignore) |
| package registry → pinned reporter dep | pytest-json-report name is [SUS] (download-gap) until the human-verify gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03-01 | Tampering | os/exec argv injection via exercise name → test argv | mitigate | fixed argv (never a shell); repoDir is the only variable and is set as cmd.Dir, not interpolated into argv (mirror go/runner.go) |
| T-85-03-02 | Tampering | malformed/corrupt reporter file aborting the parse or flipping pass/fail | mitigate | Passed gated on exit code ONLY (Pitfall 4); per-row parse is advisory; malformed lines/elements skipped, never inferred-pass |
| T-85-03-SC | Tampering | pinning a slopsquatted pytest reporter package | mitigate | blocking human-verify checkpoint (Task 1) against pypi.org BEFORE the name is committed; never auto-approvable |
| T-85-03-03 | Denial of Service | unbounded reporter output exhausting memory | accept | reporter files are bounded per task; the Go template's scanner buffer cap (4MiB) applies to line-oriented streams in plans 04-05 |
</threat_model>

<verification>
- `go test ./bench/languages/python/ ./bench/languages/java/ ./bench/languages/csharp/ ./bench/languages/ -count=1` green.
- Each runner's hermetic golden parser test passes with NO toolchain (the authoritative proof).
- Coverage gate: python ≥8, java ≥8, csharp ≥6.
- `go vet ./bench/...` clean.
- `git log --oneline --grep='^test(85-03)'` and `--grep='^feat(85-03)'` both non-empty.
</verification>

<success_criteria>
- bench/languages/{python,java,csharp}/runner.go each implement LanguageRunner and register for (internal-toolbench, <lang>).
- Each has a hermetic golden-fixture parser test that is the SOLE authoritative proof (no subprocess).
- Live toolchain tests skip cleanly (mvn/javac absent; dotnet present).
- ≥N/10 capability coverage met per language via authored fixtures.
</success_criteria>

<output>
Create `.planning/phases/85-aider-polyglot-adapter-7-remaining-per-language-runners/85-03-SUMMARY.md` when done.
</output>
