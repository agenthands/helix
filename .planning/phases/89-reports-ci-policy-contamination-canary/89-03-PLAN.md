---
phase: 89-reports-ci-policy-contamination-canary
plan: 03
type: tdd
wave: 3
depends_on: ["89-01", "89-02"]
files_modified:
  - bench/aggregator/aggregate.go
  - bench/aggregator/byte_reproducible_test.go
  - bench/aggregator/aggregate_swebench_test.go
  - bench/aggregator/aggregate_canary_test.go
  - cmd/helix-bench/report.go
  - cmd/helix-bench/main.go
  - cmd/helix-bench/report_test.go
autonomous: true
requirements: [REPORT-05]
tags: [bench, aggregator, cli, helix-bench, report, determinism, byte-reproducible]

must_haves:
  truths:
    - "helix-bench report --run-id <id> regenerates ALL 4 reports byte-identically (a double-render diff-empty hermetic golden test)"
    - "report --run-id and aggregate produce byte-identical reports over the same run tree (shared renderAll path)"
    - "report validates --run-id as a [A-Za-z0-9_-]+ single path segment before filepath.Join (no traversal)"
    - "the frozen Phase-82/85/86/87 lockstep golden guards are UPDATED (not bypassed/deleted) — intent shifts from 'column invisible' to 'column/footnote renders correctly'"
  artifacts:
    - path: "bench/aggregator/aggregate.go"
      provides: "shared renderAll(rep, runDir) emitting all 4 reports + footnote, called by both Aggregate and report"
      contains: "renderAll"
    - path: "cmd/helix-bench/report.go"
      provides: "report --run-id RunE: validate run-id, resolve <out>/<run-id>/, call shared render path"
      contains: "run-id"
    - path: "bench/aggregator/byte_reproducible_test.go"
      provides: "double-render diff-empty + committed-golden assertion for all 4 reports (REPORT-05 load-bearing proof)"
      contains: "ByteReproducible"
  key_links:
    - from: "cmd/helix-bench/report.go"
      to: "bench/aggregator renderAll / Aggregate"
      via: "thin RunE resolving filepath.Join(out, runID) and calling the shared render path"
      pattern: "filepath\\.Join"
    - from: "cmd/helix-bench/report.go run-id validation"
      to: "bench/evaluators/swebench/harness.go isValidRunID pattern"
      via: "cloned [A-Za-z0-9_-]+ segment guard (rejects .. / leading-dot / absolute)"
      pattern: "A-Za-z0-9_-"
    - from: "bench/aggregator/aggregate.go Aggregate"
      to: "bench/aggregator/aggregate.go renderAll"
      via: "the inline 2-report render block is factored into renderAll (4 reports + footnote)"
      pattern: "renderAll"
---

<objective>
Wire `helix-bench report --run-id <id>` and prove the load-bearing REPORT-05 contract: all 4 reports regenerate BYTE-IDENTICALLY from a run-id (double-render diff-empty). Factor the inline render block into a shared `renderAll(rep, runDir)` so `report` and `aggregate` emit byte-identical output, and UPDATE the frozen lockstep golden guards (Pitfall 1) to their new "renders correctly" intent.

Purpose: REPORT-05 is the capstone proof — `diff` on regenerated vs original is empty across leaderboard/per_language/ablations/cost_quality. The `report` stub (`notYetImplemented`) becomes a thin RunE over the same render path `aggregate` uses (REPORT-05 byte-identity requires one render path, two entry points). Rendering the additive columns (89-01) + the canary footnote (89-02) regenerated the frozen goldens — this plan finalizes the byte-reproducibility test AND updates the prior "additive-perturbs-nothing" guards so their assertions reflect the now-rendered columns instead of being bypassed.

Output: shared `renderAll` factoring; the `report --run-id` RunE (validated, fail-closed, mirroring `aggregate.go`); the hermetic double-render byte-reproducibility test; the `report==aggregate` byte-equality test; updated `TestSwebenchColumnsGoldenStable` + `TestAggregateCanaryAbsentIsEmDash` lockstep guards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-RESEARCH.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-PATTERNS.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-VALIDATION.md

# The render path + CLI this plan wires:
@bench/aggregator/aggregate.go
@cmd/helix-bench/main.go
@cmd/helix-bench/aggregate.go
@bench/aggregator/aggregate_test.go
@bench/aggregator/aggregate_swebench_test.go
@bench/aggregator/aggregate_canary_test.go
@bench/evaluators/swebench/harness.go

# Depends on 89-01 (renderers) + 89-02 (canary footnote) — all 4 reports + footnote now exist.
@.planning/phases/89-reports-ci-policy-contamination-canary/89-01-SUMMARY.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (89-03) produces, of the phase-wide artifact set:
- the shared `renderAll(rep, runDir)` factoring (Aggregate + report emit all 4 byte-identically) (REPORT-05)
- the `helix-bench report --run-id <id>` CLI surface, validated + fail-closed (REPORT-05)
- the hermetic double-render byte-reproducibility golden test for all 4 reports — the LOAD-BEARING proof (REPORT-05 / SC#1)
- the `report == aggregate` byte-equality test
- the UPDATED lockstep golden guards (TestSwebenchColumnsGoldenStable @ aggregate_swebench_test.go:148, TestAggregateCanaryAbsentIsEmDash @ aggregate_canary_test.go:122) — intent shifted, NOT bypassed

Depends on 89-01 + 89-02 (the 4 renderers + the canary footnote must exist before the byte-reproducibility proof is meaningful).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Factor renderAll + wire report --run-id (validated, fail-closed) + report==aggregate byte-equality (RED→GREEN)</name>
  <files>bench/aggregator/aggregate.go, cmd/helix-bench/report.go, cmd/helix-bench/main.go, cmd/helix-bench/report_test.go</files>
  <read_first>
    - bench/aggregator/aggregate.go:147-156 (the inline 2-report render block to factor into renderAll), :67 (Aggregate signature), :37-66 (Config)
    - cmd/helix-bench/aggregate.go:1-97 (newAggregateCmd — the sibling pattern to mirror EXACTLY: Config build, clock injection at the CLI boundary, verbatim error propagation, flag defaults --runs 3/--seed 42/--iterations 10000/--ci-level 0.95/--cost-table)
    - cmd/helix-bench/main.go:101 (root.AddCommand(newReportCmd()) — registration stays), :178-181 (--out default bench/reports, --run-id default UTC timestamp), :202-259 (runBench: <out>/<run-id>/ durable tree — report MUST resolve the SAME tree), :419-426 (newReportCmd stub: RunE notYetImplemented — replace the body, KEEP registration; main_test.go:35 asserts 6 subcommands, do NOT change the count)
    - bench/evaluators/swebench/harness.go:73-97 (isValidRunID — [A-Za-z0-9_-]+, rejects leading '-'; it is package-private to swebench so CLONE the guard into cmd/helix-bench, do not import)
  </read_first>
  <behavior>
    - renderAll(rep, runDir) emits ALL 4 reports (leaderboard incl. verified_correctness/cost_per_solved/footnote, per_language, ablations, cost_quality incl. scatter) atomically via writeReport; Aggregate calls renderAll (no behavior change to aggregate's output beyond now-4 reports); report calls the SAME renderAll.
    - report --run-id <id>: validates <id> as a single [A-Za-z0-9_-]+ path segment (rejects .., leading '-', absolute, empty) BEFORE filepath.Join(out, id); resolves runDir = filepath.Join(out, id) with --out default bench/reports; builds aggregator.Config with the clock injected at the CLI boundary; propagates the Aggregate error verbatim for fail-closed non-zero exit.
    - report and aggregate produce BYTE-IDENTICAL reports over the same run tree (Pitfall 5: same directory resolution).
    - An invalid --run-id (e.g. "../etc", "-flag", "") is REFUSED with a non-zero exit before any filepath.Join.
    - Subcommand count stays 6 (registration untouched).
  </behavior>
  <action>
    In aggregate.go factor the inline render+write block (currently 2 reports at :147-156) into a shared renderAll(rep, runDir) error that emits all 4 reports + footnote through writeReport; call renderAll from Aggregate. In cmd/helix-bench add report.go: replace the newReportCmd RunE body (keep the main.go:101 registration) with a RunE that (1) requires --run-id non-empty, (2) validates it via a CLONED isValidRunID-style [A-Za-z0-9_-]+ guard that rejects leading-'-'/.. /absolute (clone, do not import the swebench-private func), (3) resolves runDir := filepath.Join(out, runID) with --out default "bench/reports", (4) builds aggregator.Config mirroring newAggregateCmd EXACTLY (same flag defaults, Today: time.Now().UTC() injected at the boundary), (5) calls Aggregate (which now renders all 4 via renderAll) and propagates the error verbatim. Add report_test.go: assert report --run-id over a committed fixture tree produces the same 4 report bytes as aggregate over the same tree (report==aggregate byte-equality), and that an invalid run-id ("../x", "-x", "") returns a non-zero/refused error before any path join. Do NOT change main_test.go's 6-subcommand assertion. No fenced code in this action; identifiers above. Per REPORT-05, V5 input validation.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/... ./cmd/helix-bench/... -run 'TestReportSubcommand|TestReportRunID|TestRenderAll|TestReportEqualsAggregate' -count=1</automated>
  </verify>
  <done>renderAll emits all 4 reports and is called by both Aggregate and report; report --run-id resolves <out>/<run-id>/, validates the segment, and is fail-closed; report==aggregate byte-equality holds; invalid run-id refused; subcommand count unchanged at 6; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hermetic double-render byte-reproducibility test (all 4) + update lockstep golden guards (RED→GREEN)</name>
  <files>bench/aggregator/byte_reproducible_test.go, bench/aggregator/aggregate_swebench_test.go, bench/aggregator/aggregate_canary_test.go</files>
  <read_first>
    - bench/aggregator/aggregate_test.go:228-274 (goldenFixture — the committed multi-run fixture tree harness; :20-22 the -update flag)
    - .planning/phases/89-reports-ci-policy-contamination-canary/89-VALIDATION.md (the load-bearing REPORT-05 hermetic proof shape: render twice, assert identical, AND diff committed goldens)
    - bench/aggregator/aggregate_swebench_test.go:143-160 (TestSwebenchColumnsGoldenStable — the frozen "additive column invisible" guard; UPDATE its intent to "verified_correctness/cost_per_solved render correctly", do NOT delete/bypass)
    - bench/aggregator/aggregate_canary_test.go:119-130 (TestAggregateCanaryAbsentIsEmDash — the golden guard whose golden shifted in 89-02; UPDATE its assertion to reflect the now-rendered footnote/column, do NOT delete/bypass)
  </read_first>
  <behavior>
    - TestReportByteReproducible: over the committed multi-run fixture tree, Aggregate renders all 4 reports; a SECOND Aggregate over the same tree produces byte-identical leaderboard.md / per_language.md / ablations.md / cost_quality.md (double-render diff-empty); AND each matches its committed testdata/*.golden.md.
    - TestSwebenchColumnsGoldenStable is updated: its assertion now verifies the verified_correctness/cost_per_solved columns RENDER CORRECTLY (the column is visible and correct), NOT that the additive column is invisible. The guard still runs (not deleted).
    - TestAggregateCanaryAbsentIsEmDash is updated: a row with NO completion key still yields em-dash for CanaryPassRate; the assertion is reconciled with the now-rendered footnote/column bytes (the guard still runs).
    - No live run is the proof: the byte-reproducibility is the hermetic golden test alone.
  </behavior>
  <action>
    Add byte_reproducible_test.go with TestReportByteReproducible using goldenFixture: render via Aggregate, snapshot all 4 report bytes, re-render via Aggregate over the SAME tree, assert each of the 4 is byte-identical to the snapshot AND to its committed testdata/*.golden.md (the VALIDATION.md double-render+diff shape). Update TestSwebenchColumnsGoldenStable: change its intent from asserting the additive columns are absent/invisible to asserting verified_correctness + cost_per_solved render with correct values — keep the test running, do NOT delete or t.Skip it (Pitfall 1: guards are updated, never bypassed). Update TestAggregateCanaryAbsentIsEmDash: reconcile its assertion with the regenerated golden (em-dash still holds for a no-completion row; the footnote section is empty when no contamination) — keep it running. Re-run the full aggregator suite to confirm every golden is byte-stable and every prior guard passes with its new intent. No fenced code in this action. Per REPORT-05, Pitfall 1.
  </action>
  <verify>
    <automated>go test ./bench/aggregator/... -count=1</automated>
  </verify>
  <done>TestReportByteReproducible proves all 4 reports are byte-identical on re-render AND match committed goldens; TestSwebenchColumnsGoldenStable + TestAggregateCanaryAbsentIsEmDash updated to the new render intent (running, not bypassed); full aggregator suite green; no live run is anyone's proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI --run-id (user input) → filepath.Join | Untrusted path segment; a `..`/absolute value must not escape the report tree (path traversal) |
| report vs aggregate render paths | Two entry points must share ONE render path or byte-reproducibility silently drifts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03-01 | Tampering | report --run-id path traversal | mitigate | validate run-id as a single [A-Za-z0-9_-]+ segment (clone isValidRunID; reject .., leading '-', absolute, empty) BEFORE filepath.Join (V5; RESEARCH Security Domain) |
| T-89-03-02 | Tampering | report vs aggregate render drift | mitigate | both call the shared renderAll; report==aggregate byte-equality test + the double-render byte-reproducibility test catch any drift (Pitfall 5) |
| T-89-03-03 | Denial of service | report on a malformed/under-populated run tree | mitigate | Aggregate's existing fail-closed N-gate is propagated verbatim by the RunE → non-zero exit, no partial report (mirrors aggregate.go fail-closed) |
| T-89-03-SC | Tampering | npm/pip/cargo installs | accept | zero external packages installed; no go.mod change (RESEARCH audit N/A) |
</threat_model>

<verification>
- `go test ./bench/aggregator/... ./cmd/helix-bench/... -count=1` green.
- `go vet ./bench/... ./cmd/helix-bench/...` clean.
- TestReportByteReproducible passes (double-render diff-empty + golden match for all 4).
- `go test ./cmd/helix-bench/ -run TestMain -count=1` (or the existing main_test) still asserts 6 subcommands.
- The two lockstep guards run (not skipped/deleted) and pass with their updated intent.
</verification>

<success_criteria>
- REPORT-05: helix-bench report --run-id <id> regenerates all 4 reports byte-identically (double-render diff-empty), proven by a hermetic golden test (never a live run).
- report and aggregate produce byte-identical reports over the same tree (shared renderAll).
- run-id is validated as a single path segment before filepath.Join (no traversal).
- TestSwebenchColumnsGoldenStable + TestAggregateCanaryAbsentIsEmDash updated to the new render intent, still running (Pitfall 1: not bypassed).
</success_criteria>

<output>
Create `.planning/phases/89-reports-ci-policy-contamination-canary/89-03-SUMMARY.md` when done. Record: the renderAll factoring, the report --run-id RunE + the cloned run-id validator, the report==aggregate byte-equality result, the TestReportByteReproducible double-render+golden proof, and the before/after intent of the two updated lockstep guards (explicitly confirming they were UPDATED, not bypassed/deleted). Confirm the 6-subcommand assertion is unchanged.
</output>
