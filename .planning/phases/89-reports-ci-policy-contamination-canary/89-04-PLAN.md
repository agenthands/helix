---
phase: 89-reports-ci-policy-contamination-canary
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/bench.yml
  - bench/ci_workflow_test.go
  - bench/BENCH.md
autonomous: true
requirements: [INFRA-04]
tags: [ci, github-actions, workflow, bench, cost-budget, docs]

must_haves:
  truths:
    - "A .github/workflows/bench.yml exists: a PR job runs `make bench-quick` with a hard 5-minute cap and no LLM cost (scripted, Go-only); a full job runs `make bench` nightly (schedule) or on-demand (maintainer-gated)"
    - "The workflow is valid YAML and structurally correct, proven by a hermetic Go YAML-parse/lint test (NOT a live CI run)"
    - "The workflow uses least-privilege permissions (contents: read) and pinned action references, and does NOT reference tool_behavior_judge (EVAL-07)"
    - "bench/BENCH.md documents the CI cost budget and the canary policy"
  artifacts:
    - path: ".github/workflows/bench.yml"
      provides: "PR bench-quick (5-min cap, no LLM cost) + nightly/maintainer-gated bench-full"
      contains: "make bench-quick"
    - path: "bench/ci_workflow_test.go"
      provides: "hermetic YAML-parse/lint test asserting the workflow structure (INFRA-04 acceptance)"
      contains: "bench.yml"
    - path: "bench/BENCH.md"
      provides: "documented CI cost budget + canary policy"
      contains: "cost budget"
  key_links:
    - from: ".github/workflows/bench.yml PR job"
      to: "Makefile bench-quick target"
      via: "run: make bench-quick with timeout-minutes: 5"
      pattern: "make bench-quick"
    - from: "bench/ci_workflow_test.go"
      to: ".github/workflows/bench.yml"
      via: "parse the YAML and assert the PR cap + no-secret + permissions structure"
      pattern: "timeout-minutes"
---

<objective>
Author the INFRA-04 CI cost-policy workflow and prove it hermetically: a PR job that runs the existing hermetic `make bench-quick` with a hard 5-minute cap and no LLM cost, plus a full `make bench` job gated nightly/on-demand behind a maintainer. Verify the workflow by a Go YAML-parse/lint test (NOT a live CI run) and document the cost budget + canary policy in `bench/BENCH.md`.

Purpose: INFRA-04 requires a workflow file that protects the milestone budget — the PR gate must be cheap (scripted agent, Go-only, ≤5 min) and the expensive full bench must be maintainer/schedule-gated. This plan is fully file-disjoint from the aggregator plans (89-01/02/03), so it runs in Wave 1 in parallel with 89-01.

Output: `.github/workflows/bench.yml`; a hermetic YAML-parse test; the documented cost budget + canary policy in `bench/BENCH.md`. The only inspection-gated item in the phase is the LIVE CI execution (recorded honestly in 89-VALIDATION.md); the YAML STRUCTURE is hermetically tested here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-RESEARCH.md
@.planning/phases/89-reports-ci-policy-contamination-canary/89-PATTERNS.md

# CI conventions to mirror + targets to invoke:
@.github/workflows/go-test.yml
@Makefile
@bench/BENCH.md
</context>

<artifacts_this_phase_produces>
This plan (89-04) produces, of the phase-wide artifact set:
- `.github/workflows/bench.yml` (PR bench-quick 5-min cap + nightly/maintainer-gated bench-full) (INFRA-04)
- a hermetic Go YAML-parse/lint test for the workflow structure (INFRA-04 acceptance — NOT a live run)
- the documented CI cost budget + canary policy in `bench/BENCH.md`

Fully file-disjoint from 89-01/02/03 → runs in Wave 1 parallel with 89-01. The live CI execution remains inspection-gated (honest, per 89-VALIDATION.md Manual-Only table).
</artifacts_this_phase_produces>

<gate_note>
RESEARCH A2 leaves the full-bench gate as `schedule` + `workflow_dispatch` vs a maintainer label — BOTH satisfy INFRA-04's "nightly or on-demand, gated on a maintainer." DERIVE the choice: prefer `schedule:` (nightly cron) + `workflow_dispatch:` (on-demand) as the primary gate (matches go-test.yml's `workflow_dispatch` convention and needs no label infrastructure). No human checkpoint is needed — this is Claude's discretion within INFRA-04.
</gate_note>

<tasks>

<task type="auto">
  <name>Task 1: Author .github/workflows/bench.yml (PR 5-min cap + nightly/maintainer-gated full)</name>
  <files>.github/workflows/bench.yml</files>
  <read_first>
    - .github/workflows/go-test.yml:1-30 (triggers, `permissions: {contents: read}`, `actions/checkout@v4`, `actions/setup-go@v5` with `go-version: '1.25.x'` + `cache: true`, `timeout-minutes`), :133-143 (the EVAL-07 forbid-judge grep gate convention — keep tool_behavior_judge OUT of the new file)
    - .github/workflows/release.yml (SHA-pinned-action convention reference)
    - Makefile:128-129 (`bench:` full target), :137-146 (`bench-quick:` — hermetic, scripted agent, NO API key, pins --tasks/--agent=scripted, builds helix first)
  </read_first>
  <action>
    Create .github/workflows/bench.yml with `permissions: {contents: read}` (least privilege). Triggers: `pull_request: {branches: [main]}`, `schedule: [{cron: "0 6 * * *"}]` (nightly), `workflow_dispatch:` (on-demand maintainer gate). Job `bench-quick`: `if: github.event_name == 'pull_request'`, `runs-on: ubuntu-latest`, `timeout-minutes: 5` (HARD cap — no LLM cost, scripted agent), steps = `actions/checkout@v4` + `actions/setup-go@v5` (go-version '1.25.x', cache true) + `run: make bench-quick`. Job `bench-full`: `if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'`, `runs-on: ubuntu-latest`, `timeout-minutes: 30`, same checkout/setup-go steps + `run: make bench`. Do NOT reference tool_behavior_judge anywhere (EVAL-07). Mirror go-test.yml's action versions and permissions exactly. Do NOT add any provider API-key secret to the bench-quick PR job (it is hermetic). No fenced code in this action; the structure is enumerated above. Per INFRA-04.
  </action>
  <verify>
    <automated>test -f .github/workflows/bench.yml && grep -q 'make bench-quick' .github/workflows/bench.yml && grep -q 'timeout-minutes: 5' .github/workflows/bench.yml && ! grep -q 'tool_behavior_judge' .github/workflows/bench.yml && echo OK</automated>
  </verify>
  <done>.github/workflows/bench.yml exists with a PR bench-quick job (5-min hard cap, no LLM secret) and a nightly/dispatch bench-full job (maintainer-gated); least-privilege permissions; no tool_behavior_judge reference.</done>
</task>

<task type="auto">
  <name>Task 2: Hermetic YAML-parse/lint test for bench.yml + document cost budget & canary policy in BENCH.md</name>
  <files>bench/ci_workflow_test.go, bench/BENCH.md</files>
  <read_first>
    - .github/workflows/go-test.yml (the structural shape the parse test asserts against — permissions, jobs, timeout-minutes)
    - bench/BENCH.md (existing 294-line doc — APPEND a CI cost-budget + canary-policy section; do not rewrite existing content)
    - .planning/phases/89-reports-ci-policy-contamination-canary/89-RESEARCH.md (Standard Stack: `gopkg.in/yaml.v3` — confirm it is already in go.sum before importing; if not present, parse via the stdlib or an already-vendored YAML lib)
  </read_first>
  <action>
    Add bench/ci_workflow_test.go: a hermetic Go test that reads ../.github/workflows/bench.yml (relative to the bench package dir — resolve the repo-root path deterministically), parses it as YAML (use yaml.v3 ONLY if already in go.sum; otherwise confirm an in-tree YAML decoder and use that — do NOT add a new dependency), and asserts: (1) it is valid YAML (parse succeeds), (2) a bench-quick job exists with `timeout-minutes: 5`, (3) its single run step invokes `make bench-quick`, (4) `permissions.contents == read`, (5) a bench-full job invokes `make bench` and is gated on schedule/workflow_dispatch, (6) the file contains NO `tool_behavior_judge` reference (EVAL-07 mirror). This is the INFRA-04 acceptance proof — NOT a live CI run. Then APPEND to bench/BENCH.md a "CI Cost Policy" section documenting: the PR bench-quick budget (hermetic, scripted agent, no LLM cost, ≤5 min hard cap), the nightly/maintainer-gated full bench, and the contamination-canary policy (select tasks carry the canary Sentinel; a model echoing it verbatim flags the (task,mode) cell, which is EXCLUDED from headline numbers and listed in the leaderboard.md footnote, while CanaryPassRate still measures it). Do NOT rewrite existing BENCH.md content; append. Confirm `go list -deps ./bench/...` adds no new external dep. No fenced code in this action. Per INFRA-04.
  </action>
  <verify>
    <automated>go test ./bench/ -run 'TestBenchWorkflow|TestCIWorkflow' -count=1 && grep -qi 'cost' bench/BENCH.md</automated>
  </verify>
  <done>bench/ci_workflow_test.go hermetically parses bench.yml and asserts the PR 5-min cap, no-LLM/no-secret PR job, least-privilege permissions, the maintainer-gated full job, and no tool_behavior_judge; bench/BENCH.md documents the CI cost budget + canary policy; no new dependency added; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo secrets | The PR bench-quick job must run with NO provider secret (untrusted-PR cost/secret-leak surface) |
| workflow YAML → CI execution | A malformed or over-privileged workflow is a supply-chain/budget risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04-01 | Information disclosure | secret leakage in the PR job | mitigate | bench-quick uses NO provider API key (scripted, hermetic, Makefile:137); no secret referenced in the PR job; full job is schedule/dispatch-gated |
| T-89-04-02 | Denial of service / budget | unbounded PR bench cost | mitigate | hard `timeout-minutes: 5` cap on the PR job; the parse test asserts the cap is present (INFRA-04 acceptance) |
| T-89-04-03 | Elevation of privilege | over-broad workflow token | mitigate | `permissions: {contents: read}` (least privilege); asserted by the YAML-parse test |
| T-89-04-04 | Tampering | LLM judge gating merges | mitigate | the parse test asserts the file contains no tool_behavior_judge reference (EVAL-07) |
| T-89-04-SC | Tampering | npm/pip/cargo installs | accept | zero external packages installed; the YAML-parse test uses an in-tree decoder, no go.mod change (RESEARCH audit N/A) |
</threat_model>

<verification>
- `go test ./bench/ -run TestBenchWorkflow -count=1` green (hermetic YAML-parse proof).
- `go vet ./bench/...` clean.
- `go list -deps ./bench/... | sort -u` shows no new external dependency.
- INSPECTION-GATED (honest, per 89-VALIDATION.md): the LIVE CI run (bench-quick ≤5 min on a real PR; full bench maintainer-gated) is verified after merge by opening a PR — NOT proven by this plan. The YAML STRUCTURE is the hermetic local proof here.
</verification>

<success_criteria>
- INFRA-04: .github/workflows/bench.yml exists with a PR `make bench-quick` job (hard 5-min cap, no LLM cost) and a nightly/maintainer-gated full `make bench` job; the cost budget is documented in bench/BENCH.md.
- The workflow structure is hermetically proven by a Go YAML-parse/lint test (least-privilege permissions, the 5-min cap, no tool_behavior_judge) — not by a live run.
- The canary policy is documented in bench/BENCH.md.
- No new external dependency introduced.
</success_criteria>

<output>
Create `.planning/phases/89-reports-ci-policy-contamination-canary/89-04-SUMMARY.md` when done. Record: the bench.yml job structure (PR cap + full-job gate), the YAML-parse test assertions, the BENCH.md cost-budget + canary-policy additions, and an explicit note that the LIVE CI execution (INFRA-04) is inspection-gated (verified post-merge by opening a PR) while the YAML structure is hermetically proven here.
</output>
