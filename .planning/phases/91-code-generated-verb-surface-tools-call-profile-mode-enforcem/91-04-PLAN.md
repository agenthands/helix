---
phase: 91-code-generated-verb-surface-tools-call-profile-mode-enforcement
plan: 04
type: execute
wave: 2
depends_on: ["91-01", "91-02"]
files_modified:
  - internal/cli/cli_e2e_test.go
  - internal/cli/cli_sec_e2e_test.go
autonomous: true
requirements: [SEC-01, VERB-03]
must_haves:
  truths:
    - "A real `helix <verb>` subprocess (flat, root-attached) round-trips against a live daemon — the E2E oracle no longer uses a `call` parent"
    - "Under a read-mode/ci-bot daemon, `helix replace-symbol-body` (a real destructive verb) is refused with a non-zero exit and a typed PermissionDenied surfaced on stderr"
    - "Under an edit-mode/full daemon, the same destructive verb is NOT refused by the enforcement gate (succeeds or fails only on its own tool semantics, never PermissionDenied)"
    - "`helix --help` from a real binary groups verbs by capability on the root command"
  artifacts:
    - path: "internal/cli/cli_sec_e2e_test.go"
      provides: "HELIX_BIN-gated !windows E2E: read-mode refusal + edit-mode allow for a destructive verb (SEC-01 live)"
      contains: "HELIX_BIN"
    - path: "internal/cli/cli_e2e_test.go"
      provides: "existing dial-spine oracle re-pointed from `helix call <verb>` to flat `helix <verb>`"
  key_links:
    - from: "internal/cli/cli_sec_e2e_test.go"
      to: "a read-profile daemon"
      via: "sandbox.StartDaemon with a read/ci-bot profile + a destructive verb subprocess"
      pattern: "StartDaemon|replace-symbol-body"
    - from: "internal/cli/cli_e2e_test.go"
      to: "flat helix <verb>"
      via: "runCLIVerb drops the `call` prefix"
      pattern: "runCLIVerb"
---

<objective>
Stand up the live, real-subprocess proof of the SEC-01 boundary and re-point the
existing Phase 90 E2E dial oracle to the flat `helix <verb>` surface that 91-01
shipped. The new HELIX_BIN-gated test starts a daemon under a read-mode (or
ci-bot) profile and asserts that a REAL `helix replace-symbol-body` subprocess is
refused with a non-zero exit and a typed PermissionDenied on stderr, while the
same verb under an edit-mode/full daemon is NOT blocked by the enforcement gate.
The Phase 90 oracle, which drives `helix call <verb>` via a `call` prefix, is
updated to the flattened `helix <verb>` invocation so it keeps passing against the
91-01 root-attached verb surface.

Purpose: SEC-01 (the live end-to-end refusal proof the VALIDATION manual-only row
calls out) + VERB-03 (grouped help / flat verb surface verified against a real
binary). The unit refusal test lives in 91-02; this is the daemon-subprocess
oracle that proves the typed error survives the full CLI->gRPC->daemon->back round
trip.
Output: a new `internal/cli/cli_sec_e2e_test.go` and a minimal re-point of the
existing `internal/cli/cli_e2e_test.go` runCLIVerb helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-code-generated-verb-surface-tools-call-profile-mode-enforcem/91-RESEARCH.md
@.planning/phases/91-code-generated-verb-surface-tools-call-profile-mode-enforcem/91-PATTERNS.md

# The Phase 90 E2E oracle to extend + re-point (HELIX_BIN gate, sandbox harness, runCLIVerb)
@internal/cli/cli_e2e_test.go

# The destructive-tool enum + how the read/ci-bot profile excludes them
@internal/mcp/guardrail_middleware.go
@internal/cli/verb.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the Phase 90 E2E oracle from `helix call <verb>` to flat `helix <verb>` (VERB-03)</name>
  <files>internal/cli/cli_e2e_test.go</files>
  <read_first>
    - internal/cli/cli_e2e_test.go:175-193 (runCLIVerb — currently prepends `"call"`: `full := append([]string{"call"}, args...)`) — the single line to re-point
    - internal/cli/cli_e2e_test.go:50-51, 206-238 (representativeVerbName="search" + TestCLI_E2E_OneShot which asserts CLI stdout == MCP-path text)
    - internal/cli/cli_e2e_test.go:324-391 (TestCLI_ParallelColdSingleDaemon also builds `"call", representativeVerbName, ...` inline at ~:356 — re-point that argv too)
    - 91-RESEARCH.md State of the Art table ("helix call <verb> parent -> helix <verb> on root") + Open Q1 recommendation (flatten)
    - 91-01-PLAN.md Task 2 (the flatten: generated verbs attach to root, the `call` parent is removed)
  </read_first>
  <action>
    In `runCLIVerb`, drop the `call` prefix: change `full := append([]string{"call"}, args...)` to invoke the verb directly on root (`full := args`), so the oracle drives `helix search --query=...` (flat) matching the 91-01 surface. Also re-point the inline argv in TestCLI_ParallelColdSingleDaemon (~:356) that hardcodes `"call", representativeVerbName, "--query=..."` to drop the `"call"` element. Leave the rest of the oracle (sandbox bringup, HELIX_BIN gate, MCP-path reference comparison, warm-reuse SLO, parallel-cold single-daemon, clean-shutdown lifecycle) unchanged — they exercise the SAME one-shot round-trip, now via the flattened verb. If 91-01 intentionally kept a `call` alias for back-compat (check its SUMMARY/decision), prefer the flat form here regardless since the ROADMAP UX is `helix <verb>`.
  </action>
  <verify>
    <automated>go build -o /tmp/helix-91 ./cmd/helix && HELIX_BIN=/tmp/helix-91 go test ./internal/cli/ -run 'TestCLI_E2E_OneShot|TestCLI_WarmReuseSLO' -count=1</automated>
  </verify>
  <done>The Phase 90 E2E oracle drives flat `helix <verb>` (no `call` prefix) and TestCLI_E2E_OneShot + TestCLI_WarmReuseSLO pass against a freshly built binary (or skip cleanly when HELIX_BIN is unset). `grep -n '"call"' internal/cli/cli_e2e_test.go` returns nothing.</done>
</task>

<task type="auto">
  <name>Task 2: HELIX_BIN-gated E2E — read-mode refuses a destructive verb, edit-mode allows it (SEC-01 live)</name>
  <files>internal/cli/cli_sec_e2e_test.go</files>
  <read_first>
    - internal/cli/cli_e2e_test.go:1-204 (the FULL harness to mirror: `//go:build !windows`, resolveHelixBin, newE2EFixture, sandbox.NewSandbox/Prepare/StartDaemon, runCLIVerb, mcpActivate, toolText — copy the bringup conventions, do NOT hand-roll a daemon)
    - internal/cli/cli_e2e_test.go:90-142 (newE2EFixture — note `sb.StartDaemon(ctx, taskID, mode, "", "", daemonOpts...)`; the 4th arg is the profile string and the 5th is cfg — pass a read-profile in the 4th here)
    - internal/mcp/guardrail_middleware.go:172-188 (the 6 destructive tools incl. replace_symbol_body — pick one as the refusal target)
    - internal/profile/modes/read.yaml + internal/profile/profiles/ci-bot.yaml (read/ci-bot exclude the edit tools -> AllowedTools omits replace_symbol_body, so 91-02's middleware refuses it)
    - internal/errors/kinds.go (the PermissionDenied kind whose stderr prefix the CLI surfaces — assert non-zero exit + stderr names permission_denied / "not available in profile")
    - 91-RESEARCH.md Pitfall 6 ("how profile/mode reaches the daemon" — the test daemon MUST be started with a read profile; the CLI does not pass mode per call) + Validation Architecture "Wave 0 Gaps" CLI_SecRefusal row
  </read_first>
  <action>
    Create `internal/cli/cli_sec_e2e_test.go` with `//go:build !windows`, mirroring cli_e2e_test.go's harness (reuse resolveHelixBin, the sandbox bringup pattern, runCLIVerb, mcpActivate — extract a shared fixture-with-profile helper if needed, or add a `newE2EFixtureWithProfile(t, runID, profile string)` variant that passes `profile` as StartDaemon's 4th arg). Write `TestCLI_SecRefusal_ReadMode`: bring up a daemon under a read-restricted profile (e.g. `ci-bot` whose default mode `review`, or a read-mode profile that excludes the edit tools), activate the workspace, then run a REAL `helix replace-symbol-body --path=... --symbol-name=... --new-body=...` subprocess via the flat runCLIVerb; assert the subprocess exits NON-ZERO and its combined output contains the typed permission-denied signal (the serr PermissionDenied stderr prefix or the "not available in profile" message from 91-02's refusal) — proving the typed error survived the CLI->gRPC->daemon->back round trip. Write `TestCLI_SecAllow_EditMode`: bring up an edit/full-profile daemon (the default "full" the existing fixture uses) and run the SAME destructive verb; assert it is NOT refused by the enforcement gate — the output does NOT contain the PermissionDenied signal (the call may still fail on the tool's own semantics, e.g. missing receipts or symbol-not-found, but NEVER permission_denied). Both tests SKIP cleanly when HELIX_BIN is unresolvable, exactly like the existing oracle. The args only need to satisfy CLI-side pre-dial required-flag validation; the enforcement refusal happens before the handler, so the verb need not produce a real edit to prove the refusal.
  </action>
  <verify>
    <automated>go build -o /tmp/helix-91 ./cmd/helix && HELIX_BIN=/tmp/helix-91 go test ./internal/cli/ -run 'TestCLI_SecRefusal|TestCLI_SecAllow' -count=1 -v</automated>
  </verify>
  <done>cli_sec_e2e_test.go GREEN (or skip without HELIX_BIN): under the read profile the destructive verb exits non-zero with a typed PermissionDenied stderr signal; under the full profile it is NOT refused by the gate. `go vet ./internal/cli/` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real `helix` subprocess -> live daemon over gRPC | The full production wire; the only place the typed-refusal round trip is proven end to end (the unit test in 91-02 stops at the middleware) |
| read-profile daemon session | The authoritative AllowedTools whitelist the destructive verb is tested against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-13 | Elevation of Privilege | the typed PermissionDenied is swallowed/lost on the CLI->gRPC->daemon round trip, so a refusal silently succeeds CLI-side | mitigate | TestCLI_SecRefusal_ReadMode asserts the REAL subprocess exits non-zero with the permission-denied signal (Task 2) — the live proof the 91-02 unit test cannot give |
| T-91-14 | Elevation of Privilege | edit-mode incorrectly refuses (false positive) hiding a real bypass under read mode | mitigate | TestCLI_SecAllow_EditMode asserts the same verb is NOT gate-refused under full profile (Task 2), so the refusal is profile-conditioned, not unconditional |
| T-91-15 | Repudiation | a green E2E that actually SKIPPED (HELIX_BIN unset) masks an unproven boundary | mitigate | phase gate requires the HELIX_BIN-gated test to RUN (not skip) before verify-work, per 91-RESEARCH Sampling Rate "Phase gate"; the verify command builds the binary and points HELIX_BIN at it |
| T-91-SC | Tampering | npm/pip/cargo installs | mitigate | zero new external packages; reuses the existing sandbox harness — no install task |
</threat_model>

<verification>
- `go build -o /tmp/helix-91 ./cmd/helix && HELIX_BIN=/tmp/helix-91 go test ./internal/cli/ -run 'TestCLI_SecRefusal|TestCLI_SecAllow|TestCLI_E2E_OneShot' -count=1 -v` — the security tests RAN (not skipped) and passed.
- `go vet ./...` clean.
- `grep -n '"call"' internal/cli/cli_e2e_test.go` returns nothing (flatten re-point complete).
- `git diff --exit-code api/proto/` empty (zero-proto invariant).
</verification>

<success_criteria>
- SEC-01 (live): a real `helix <destructive-verb>` subprocess under a read profile is refused with a non-zero exit + typed PermissionDenied on stderr; under the full profile the same verb is not gate-refused.
- VERB-03 (live): the flattened `helix <verb>` surface round-trips against a real daemon (the re-pointed Phase 90 oracle passes).
</success_criteria>

<output>
Create `.planning/phases/91-code-generated-verb-surface-tools-call-profile-mode-enforcem/91-04-SUMMARY.md` when done.
</output>
