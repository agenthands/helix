---
phase: 93-skill-md-nudge-repurpose-helix-setup-flip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cli/skill.go
  - internal/cli/skills/helix/SKILL.md
  - internal/cli/skill_test.go
autonomous: true
requirements: [SKILL-01, SKILL-04]
must_haves:
  truths:
    - "The helix binary embeds a non-empty SKILL.md via go:embed (compiled in, not read from disk at runtime)"
    - "SKILL.md has valid YAML frontmatter with name, description, allowed-tools; description+when_to_use is under 1,536 chars"
    - "Every helix verb named in the SKILL.md decision table is a real member of cli.VerbToolNames() (no invented/drifted verbs)"
    - "installSkill writes SKILL.md verbatim under a contained target dir, atomically (temp+rename), idempotently (re-run is byte-stable)"
    - "SKILL.md records a real measured idle-skill-cost token number and a preloaded-full-tool-schema number (SKILL-04 placeholder filled by plan 93-04 or by a recorded char-cap bound)"
  artifacts:
    - path: "internal/cli/skills/helix/SKILL.md"
      provides: "Embedded Claude Code skill: frontmatter + | Question | Use this | Not this | decision table citing frozen Phase 92 verbs + terse output"
      contains: "| Question | Use this | Not this |"
    - path: "internal/cli/skill.go"
      provides: "//go:embed of SKILL.md as a string + installSkill(targetDir)/skillTargetDir contained-write helper"
      exports: ["installSkill"]
    - path: "internal/cli/skill_test.go"
      provides: "embed-non-empty, frontmatter-valid, verb-in-catalog membership, token-note presence, idempotent-write tests"
  key_links:
    - from: "internal/cli/skill_test.go"
      to: "internal/cli/verb.go"
      via: "asserts SKILL.md-cited verbs ⊆ cli.VerbToolNames() (drift gate)"
      pattern: "VerbToolNames"
    - from: "internal/cli/skill.go"
      to: "internal/cli/skills/helix/SKILL.md"
      via: "//go:embed skills/helix/SKILL.md → embeddedSkillMD string"
      pattern: "go:embed skills/helix/SKILL.md"
---

<objective>
Author the embedded `SKILL.md` Claude Code skill and the `installSkill` writer that ships it. The skill teaches the agent the 50 FROZEN Phase 92 `helix` kebab verbs and the terse `relpath:line:col<TAB>payload` output via a `| Question | Use this | Not this |` decision table, with a description scoped to symbol-level code navigation/refactor so it fires on code tasks and stays dormant on prose/log/config tasks (SKILL-01). It records the idle-skill-cost token rationale (SKILL-04).

Purpose: this is the on-demand replacement for the preloaded MCP `tools/list` schema blob — the agent learns the verbs from a ≤1,536-char description instead of a full-schema preload tax. The asset and its writer are the dependency every other plan in this phase builds on (setup install in 93-03, behavioral oracle in 93-04).

Output: `internal/cli/skills/helix/SKILL.md` (the asset), `internal/cli/skill.go` (`//go:embed` + `installSkill`/`skillTargetDir`), `internal/cli/skill_test.go` (embed/frontmatter/verb-membership/token-note/idempotency tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-skill-md-nudge-repurpose-helix-setup-flip/93-RESEARCH.md
@.planning/phases/93-skill-md-nudge-repurpose-helix-setup-flip/93-PATTERNS.md

# Analogs (read these first)
@internal/eval/judge/client.go
@internal/cli/verb.go
@internal/cli/verbs_gen.go
@internal/cli/verbs_gen_test.go
@internal/cli/nudge.go
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan produces these new symbols/files/assets (downstream plans consume them):
- `internal/cli/skills/helix/SKILL.md` — the embedded skill asset (frontmatter + decision table).
- `embeddedSkillMD string` (package `cli`) — the `//go:embed`'d skill body. Consumed by 93-03 (`installSkill` write) and 93-04 (behavioral oracle system prompt).
- `func installSkill(targetDir string) error` — atomic contained write of `embeddedSkillMD` to `<targetDir>/SKILL.md`. Consumed by 93-03.
- `func skillTargetDir(claudeDir string) string` — resolves `<claudeDir>/skills/helix` (the per-client skills dir). Consumed by 93-03.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author SKILL.md asset + go:embed + verb-membership drift test (SKILL-01)</name>
  <files>internal/cli/skills/helix/SKILL.md, internal/cli/skill.go, internal/cli/skill_test.go</files>
  <read_first>
    - internal/eval/judge/client.go:10,21-22 — the `import _ "embed"` + `//go:embed prompts/rubric.md` → `var embeddedRubric string` single-file string-embed analog (Pattern 1 / Analog A).
    - internal/cli/verb.go:81 — `func VerbToolNames() []string` (the read-only catalog seam; the drift test asserts membership against this).
    - internal/cli/verbs_gen_test.go:80-104 — `TestVerbToolNames_SortedSetEqual` (the membership-assertion analog).
    - internal/cli/verbs_gen.go:10-20 — `verbSpecs` shape + `groupID` field (cite verbs by capability group, NOT by count).
    - CLAUDE.md "## Code intelligence: SMTC-first tool routing" → "### Decision matrix" — the EXACT `| Question | Use this | Not this |` 3-column table format to replicate (cite frozen helix verbs, not mcp__smtc__*).
    - 93-PATTERNS.md lines 57-66 — the authoritative frozen verb list by capability group.
    - 93-RESEARCH.md "SKILL.md frontmatter skeleton" + "Terse output shape to cite in SKILL.md" (the content skeleton).
  </read_first>
  <behavior>
    - Test 1 (embed non-empty): `embeddedSkillMD` is a non-empty string (the asset compiled into the binary).
    - Test 2 (frontmatter valid): the leading `---`...`---` block parses as YAML with keys `name`, `description`, `allowed-tools`; `name` == "helix".
    - Test 3 (description cap, SKILL-04 idle-cost bound): the combined `description` (+ `when_to_use` if present) byte length is ≤ 1536 (the Claude Code listing cap = idle-cost upper bound).
    - Test 4 (verb-membership drift gate, Pitfall 5): every `helix <verb>` token referenced inside the decision table is a member of the set returned by `cli.VerbToolNames()` AFTER converting the kebab verb to its tool-name form (kebab→snake: replace "-" with "_"). Parse verbs out of the table by scanning for backtick-fenced `helix <kebab>` occurrences; for each, assert the snake form is in `VerbToolNames()`. No SKILL.md-cited verb may be absent from the catalog.
    - Test 5 (no verb-count literal in prose, VERB-01 lesson): the SKILL.md body does NOT contain a hardcoded total-verb-count literal (e.g. "50 verbs" / "53 tools") — assert no `\b5[03]\s+(verbs|tools)\b` style count claim. (Cite by capability group, never a count.)
  </behavior>
  <action>
    Author `internal/cli/skills/helix/SKILL.md` per 93-RESEARCH.md "SKILL.md frontmatter skeleton" and 93-PATTERNS.md format analog. Frontmatter: `name: helix`; a `description:` scoped to SYMBOL-LEVEL code navigation and refactoring (find where a symbol is defined, who calls a function, all references, an interface's implementations, a type/call hierarchy, a file's symbol outline, rename a symbol, replace a function body — instead of grep/sed/cat) with an explicit dormant clause for prose/log/config tasks (Pitfall 1 — scope it so it does NOT fire on README/log/YAML); `allowed-tools` granting scoped Bash for the helix CLI. NOTE on allowed-tools syntax (resolves RESEARCH Assumption A2): the Claude Code skills doc shows the scoped-Bash form as `Bash(git add *)` (space then `*`), so use `allowed-tools: Bash(helix *)`. REQUIREMENTS.md SKILL-01 wrote `Bash(helix:*)` as the colon form — record that in a SKILL.md comment for traceability but emit the docs-confirmed `Bash(helix *)` form (per SKILL-01, validated by the frontmatter test). Body: a single `| Question | Use this | Not this |` table whose rows cite the FROZEN verbs from 93-PATTERNS.md lines 57-66 by capability group (navigation/edit/fileops/diagnostics/repomap/memory), each row mapping a code question → a `helix <verb>` → the grep/sed/cat it replaces; a short note that output is terse `relpath:line:col<TAB>payload`, copy-paste-able into the next verb. Add an HTML-comment token-note line reserving the SKILL-04 numbers (placeholder filled in 93-04, e.g. `<!-- Token note (SKILL-04): idle skill cost ≈ <N> tokens; preloaded full MCP tool schema ≈ <M> tokens. -->`). Do NOT hardcode a verb count anywhere (VERB-01 lesson).

    Create `internal/cli/skill.go` with a blank `import _ "embed"` and `//go:embed skills/helix/SKILL.md` over `var embeddedSkillMD string` (string form per Analog A — SKILL.md is one file; do NOT use embed.FS).

    Create `internal/cli/skill_test.go` implementing the five behaviors above. For frontmatter YAML parsing, use the already-vendored test deps (there is a YAML lib in go.mod via transitive deps; if no direct yaml import is available in the cli package, parse the frontmatter block minimally by splitting on the `---` fences and asserting the three keys are present as `key:` line prefixes — do NOT add a new dependency; `git diff go.mod` MUST stay empty). For the kebab→snake conversion use `strings.ReplaceAll(v, "-", "_")`.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run 'Skill' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `embeddedSkillMD` is non-empty and `internal/cli/skills/helix/SKILL.md` exists.
    - Frontmatter has `name: helix`, a `description`, and `allowed-tools`; description(+when_to_use) ≤ 1536 bytes.
    - Every `helix <kebab>` cited in the decision table maps (kebab→snake) to a member of `cli.VerbToolNames()`.
    - No hardcoded total-verb-count literal appears in the body.
    - `go test ./internal/cli/ -run 'Skill'` is green; `go build ./...` succeeds (embed resolves); `git diff go.mod` empty.
  </acceptance_criteria>
  <done>SKILL.md is embedded, frontmatter-valid, drift-gated against the live verb catalog, and dormant-scoped in its description.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: installSkill atomic contained writer + idempotency test (SKILL-01 install half)</name>
  <files>internal/cli/skill.go, internal/cli/skill_test.go</files>
  <read_first>
    - internal/cli/nudge.go:130-155 — `saveSessionStats` MkdirAll(0755) + temp-write(0644) + rename atomic-write analog (Analog B / Shared Pattern "Atomic disk write").
    - internal/cli/render.go `readSnippetLine` (referenced in 92-02-SUMMARY.md lines 76, 121 / 93-PATTERNS.md line 47) — the `filepath.Rel` + `..`-prefix/abs containment guard to mirror BEFORE writing (Security V12 path-traversal mitigation, T-93 analog of T-92-04).
  </read_first>
  <behavior>
    - Test 1 (write + content faithful): `installSkill(dir)` writes `<dir>/SKILL.md` whose bytes equal `embeddedSkillMD` (verbatim, modulo a single trailing newline policy matching saveSessionStats).
    - Test 2 (dir creation): `installSkill` on a non-existent nested `dir` creates it (MkdirAll 0755) and succeeds.
    - Test 3 (idempotent / byte-stable): calling `installSkill(dir)` twice leaves byte-identical file content (re-run is a no-op in effect).
    - Test 4 (atomic — no partial file): after a successful write there is no leftover `.tmp` sibling.
    - Test 5 (containment / traversal refusal): `skillTargetDir` (or installSkill's resolution) refuses or no-ops a target that resolves outside its claudeDir root (mirror readSnippetLine's `..`-prefix guard) — assert no write escapes the root.
  </behavior>
  <action>
    Add to `internal/cli/skill.go`:
    - `func skillTargetDir(claudeDir string) string` returning `filepath.Join(claudeDir, "skills", "helix")` (the `.claude/skills/helix` layout confirmed against the Claude Code skills doc: project `.claude/skills/<name>/SKILL.md`, personal `~/.claude/skills/<name>/SKILL.md`).
    - `func installSkill(targetDir string) error`: resolve+contain the target (mirror `readSnippetLine`'s containment check from internal/cli/render.go — compute `filepath.Rel(root, abs)` and refuse a path whose Rel begins with `..` or is absolute, so a crafted client/project dir cannot redirect the write outside the skills root; Security V12). Then `os.MkdirAll(targetDir, 0755)`, write `embeddedSkillMD` to `<targetDir>/SKILL.md.tmp` at 0644 (append a single trailing `\n` only if absent, matching saveSessionStats), then `os.Rename` to `<targetDir>/SKILL.md` (atomic; no corruption on concurrent setup). Return wrapped errors per the repo's `%w` convention.
    Extend `internal/cli/skill_test.go` with the five behaviors above using `t.TempDir()`.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run 'Skill' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `installSkill(t.TempDir()/sub/dir)` creates the dir and writes `SKILL.md` whose content equals `embeddedSkillMD`.
    - Re-running `installSkill` yields byte-identical content; no `.tmp` file remains.
    - A target resolving outside the root is refused (no escaping write).
    - `go test ./internal/cli/ -run 'Skill' -count=1` green; `go vet ./internal/cli/...` clean.
  </acceptance_criteria>
  <done>installSkill ships the embedded skill atomically, idempotently, and within a contained target dir; ready for 93-03 to wire into each registrar's Register.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup-caller → filesystem | `installSkill` writes a file path derived from a client/project dir; a crafted dir could attempt path traversal |
| embedded asset → disk | SKILL.md content is trusted (compiled in); the write target is the only untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering | `installSkill` target path | mitigate | Resolve+contain target with `filepath.Rel`+`..`-prefix/abs guard BEFORE write (mirror render.go `readSnippetLine`, Security V12); refuse escaping paths |
| T-93-02 | Tampering | concurrent SKILL.md write | mitigate | Atomic temp-file + `os.Rename` (mirror nudge.go `saveSessionStats`); no partial/corrupt file |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new packages this plan; `git diff go.mod` asserted empty in verification (no install task exists, so no legitimacy checkpoint required) |
</threat_model>

<verification>
- `go test ./internal/cli/ -run 'Skill' -count=1` green.
- `go build ./...` succeeds (the `//go:embed` resolves the asset).
- `go vet ./internal/cli/...` clean.
- `git diff go.mod` empty (zero new deps); `git diff api/proto/` empty (zero proto).
- Manual spot-check: the SKILL.md decision table cites verbs only from `cli.VerbToolNames()` and contains no verb-count literal.
</verification>

<success_criteria>
- An embedded, frontmatter-valid SKILL.md ships in the binary, scoped to symbol-level code work, drift-gated against the live verb catalog, with a reserved SKILL-04 token-note line.
- `installSkill`/`skillTargetDir` write the skill atomically, idempotently, and within a contained dir — the seam 93-03 consumes.
</success_criteria>

<output>
Create `.planning/phases/93-skill-md-nudge-repurpose-helix-setup-flip/93-01-SUMMARY.md` when done.
</output>
