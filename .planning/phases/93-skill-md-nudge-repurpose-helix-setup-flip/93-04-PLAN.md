---
phase: 93-skill-md-nudge-repurpose-helix-setup-flip
plan: 04
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - test/oracle/llm/skill_trigger_test.go
  - test/oracle/llm/prompt.go
  - internal/cli/skill.go
  - internal/cli/skill_test.go
  - internal/cli/skills/helix/SKILL.md
autonomous: true
requirements: [TEST-03, SKILL-04]
must_haves:
  truths:
    - "The behavioral oracle compares tool-selection with the SKILL.md body in the system prompt vs without it, and records whether selection shifts toward a helix verb vs the grep/sed/cat baseline (TEST-03)"
    - "The oracle is //go:build llm + SkipWithoutAPIKey gated — it RUNS with an API key and hermetically SKIPs in CI without one (never a vacuous pass)"
    - "SKILL.md records a real measured idle-skill-cost number AND a preloaded-full-tool-schema number (SKILL-04); a dependency-free char-cap bound is recorded unconditionally so SKILL-04 has a non-gated proof"
    - "The skill's idle cost is exposed/asserted by a non-gated unit test (the ≤1,536-char description bound) independent of any API key"
  artifacts:
    - path: "test/oracle/llm/skill_trigger_test.go"
      provides: "skill-vs-baseline tool-selection comparison + (optional) count_tokens of SKILL.md description vs full tools/list blob"
      contains: "//go:build llm"
    - path: "test/oracle/llm/prompt.go"
      provides: "SkillSystemPrompt(skillBody) + helix-verb task descriptions for the comparison"
    - path: "internal/cli/skill.go"
      provides: "skillDescription() accessor exposing the frontmatter description for the non-gated idle-cost bound test"
    - path: "internal/cli/skills/helix/SKILL.md"
      provides: "SKILL-04 token-note line filled with real measured numbers (or the recorded char-cap bound)"
  key_links:
    - from: "test/oracle/llm/skill_trigger_test.go"
      to: "internal/cli/skills/helix/SKILL.md (embeddedSkillMD)"
      via: "loads the embedded skill body into the skill-run system prompt"
      pattern: "SkillSystemPrompt"
    - from: "test/oracle/llm/skill_trigger_test.go"
      to: "test/harness Session.ListTools"
      via: "FormatToolList(tools) is the 'before' full-MCP-schema blob (MCP head alive until Phase 94)"
      pattern: "ListTools"
---

<objective>
Verify the skill empirically and record the SKILL-04 token rationale. Extend the existing `test/oracle/llm/` behavioral harness with a skill-vs-baseline comparison: run the same code task twice — once WITHOUT the SKILL.md body in the system prompt (baseline → grep/sed/cat) and once WITH it (skill → a `helix` verb) — and record whether selection shifts toward helix (TEST-03). Fill the SKILL.md token-note with a real measured idle-skill-cost (the ≤1,536-char description bound unconditionally, plus an exact `count_tokens` of the description vs the full `tools/list` schema blob when an API key is present) (SKILL-04).

Purpose: TEST-03 and SKILL-04 are the empirical close on the skill — proving the on-demand skill shifts tool selection toward helix and quantifying the idle-cost win vs the preloaded full-tool-schema blob. The "before" full-schema blob is still obtainable via `tools/list` because the MCP head is alive until Phase 94.

Output: `test/oracle/llm/skill_trigger_test.go` (NEW, `//go:build llm`), `test/oracle/llm/prompt.go` (skill system prompt + helix-verb tasks), a `skillDescription()` accessor + non-gated idle-cost bound test in `internal/cli/`, and the filled token-note in SKILL.md. No new deps, no proto change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-skill-md-nudge-repurpose-helix-setup-flip/93-RESEARCH.md
@.planning/phases/93-skill-md-nudge-repurpose-helix-setup-flip/93-PATTERNS.md

# Dependency (read the produced seam)
@.planning/phases/93-skill-md-nudge-repurpose-helix-setup-flip/93-01-PLAN.md

# Analogs (read these first)
@test/oracle/llm/selection_test.go
@test/oracle/llm/client.go
@test/oracle/llm/prompt.go
@test/oracle/llm/transcript.go
@internal/cli/skill.go
</context>

<artifacts_this_phase_produces>
- `func SkillSystemPrompt(skillBody string) string` + a baseline counterpart in `test/oracle/llm/prompt.go`, plus helix-verb-oriented code task descriptions.
- `TestSkillVsBaseline` (build tag `llm`) — the skill-vs-grep-baseline tool-selection comparison; records the result via WriteTranscript.
- (Optional, when keyed) a `count_tokens` measurement of the SKILL.md description vs the full `tools/list` blob, written into SKILL.md / a referenced transcript.
- `func skillDescription() (string, error)` in `internal/cli/skill.go` + a NON-gated unit test asserting the description(+when_to_use) ≤ 1,536 chars and recording that bound as the dependency-free idle-cost proof.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SKILL-04 dependency-free idle-cost bound (skillDescription accessor + non-gated test) + fill token-note</name>
  <files>internal/cli/skill.go, internal/cli/skill_test.go, internal/cli/skills/helix/SKILL.md</files>
  <read_first>
    - internal/cli/skill.go (from 93-01) — `embeddedSkillMD`. Add an accessor that returns the frontmatter `description` (+ `when_to_use` if present) so the cap can be asserted without a parser dep.
    - 93-RESEARCH.md SKILL-04 row + Open Q3 — record BOTH the hard char-cap bound (dependency-free, 1,536-char listing cap = idle-cost upper bound) AND, when keyed, an exact count_tokens. The acceptance text allows the number in SKILL.md OR a referenced doc.
    - Claude Code skills doc (confirmed this phase): the combined `description` + `when_to_use` is truncated at 1,536 chars in the skill listing — this IS the idle-cost ceiling (only the listing text is in context until the skill triggers; the body loads on use).
  </read_first>
  <behavior>
    - Test 1 (accessor): `skillDescription()` returns the non-empty frontmatter description string parsed from `embeddedSkillMD` (minimal `---`-fence split, no new dep).
    - Test 2 (idle-cost bound, NON-gated SKILL-04 proof): `len([]byte(description(+when_to_use))) <= 1536` — this runs in the default suite with NO API key, giving SKILL-04 a hermetic non-gated proof.
    - Test 3 (token-note filled): SKILL.md contains the token-note line with a real number filled in (not the `<N>`/`<M>` placeholder) — assert the placeholder tokens `<N>`/`<M>` are absent and a digit-bearing idle-cost figure is present.
  </behavior>
  <action>
    Add `func skillDescription() (string, error)` to `internal/cli/skill.go`: split `embeddedSkillMD` on the leading `---` fences, scan the frontmatter for the `description:` (and optional `when_to_use:`) value(s), return the combined text. Use only stdlib string handling (no YAML dep; `git diff go.mod` stays empty).
    In `internal/cli/skill_test.go` add the non-gated idle-cost bound test (Test 2) and the token-note-filled assertion (Test 3).
    Fill the SKILL.md token-note (the placeholder reserved in 93-01): record the dependency-free idle-cost bound — the description's actual byte length and the 1,536-char listing cap — as the idle-skill-cost figure, and the preloaded-full-tool-schema figure (the byte/token size of the full MCP `tools/list` blob; obtain the char size from the FormatToolList output produced by Task 2's oracle run, or record the char-count bound if no keyed run was performed). The note must contain a real digit-bearing number for the idle cost so Test 3 passes even in CI without a key. If a keyed count_tokens run (Task 2) produced exact token numbers, prefer those; otherwise record the char-cap bound and an explicit note that the exact token count is recorded in the keyed transcript when run.
  </action>
  <verify>
    <automated>go test ./internal/cli/ -run 'Skill' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `skillDescription()` returns the non-empty frontmatter description.
    - The description(+when_to_use) ≤ 1,536 bytes is asserted by a NON-gated test (no API key needed) — SKILL-04 has a hermetic proof.
    - SKILL.md's token-note has a real digit-bearing idle-cost number (no `<N>`/`<M>` placeholders remain).
    - `go test ./internal/cli/ -run 'Skill' -count=1` green; `git diff go.mod` empty.
  </acceptance_criteria>
  <done>SKILL-04 is backed by a dependency-free idle-cost bound recorded in SKILL.md and proven by a non-gated unit test; the exact-token path is available when keyed.</done>
</task>

<task type="auto">
  <name>Task 2: skill-vs-baseline behavioral oracle (TEST-03, build tag llm, key-gated)</name>
  <files>test/oracle/llm/skill_trigger_test.go, test/oracle/llm/prompt.go</files>
  <read_first>
    - test/oracle/llm/selection_test.go:1-64 — the EXACT analog: `//go:build llm`, `SkipWithoutAPIKey(t)` first, `harness.StartRunner`, `runner.Session.ListTools` → `FormatToolList` (the full-MCP-schema blob = SKILL-04 "before"), `AskSingleTurn`, `WriteTranscript`, `InterCallDelay()` between calls.
    - test/oracle/llm/client.go:48-63 (SkipWithoutAPIKey), :94-99 (AskSingleTurn), :159-162 (InterCallDelay).
    - test/oracle/llm/prompt.go:97-133 — `SelectionSystemPrompt` + `FormatToolList` style to mirror for `SkillSystemPrompt`.
    - test/oracle/llm/transcript.go:31 — `WriteTranscript` record shape.
    - internal/cli (from 93-01) — `embeddedSkillMD`; the oracle reads the skill body. Import the `cli` package OR read the embedded asset via a small exported accessor; do NOT duplicate the SKILL.md content in the test (single source of truth).
  </read_first>
  <behavior>
    - The test runs a small set of helix-appropriate CODE tasks (e.g. "find where the function X is defined", "find all callers of Y", "rename symbol Z across the codebase") in two conditions:
      - baseline: a system prompt that offers grep/sed/cat/read as the toolset WITHOUT the SKILL.md body → record the chosen approach.
      - skill: the same task with the SKILL.md body (decision table) in the system prompt → record the chosen approach.
    - Assert/record that the skill condition shifts selection toward a `helix` verb vs the baseline's grep/sed/cat (TEST-03 records a tool-selection improvement vs the grep/sed/cat baseline). Use a tolerant assertion (the comparison is the deliverable; record the per-task outcome via WriteTranscript so the improvement number is captured even if a single task is noisy — assert the aggregate shift, not every individual task, to avoid flakiness).
    - Gating: `//go:build llm` + `SkipWithoutAPIKey(t)` as the first line — the test SKIPs hermetically in CI without a key and RUNS locally with one (never a vacuous pass; the SKIP is honest, the assertion only fires when keyed).
    - (Optional, same test or a sibling) capture `FormatToolList(tools)` char/token size as the SKILL-04 "before" blob and `skillDescription()` size as the "after"; write both into the transcript (feeds Task 1's token-note when keyed).
  </behavior>
  <action>
    Create `test/oracle/llm/skill_trigger_test.go` (`//go:build llm`, package `llm`) mirroring `selection_test.go`: `SkipWithoutAPIKey(t)` first; start the runner (`SkipLS: true` is fine — the oracle measures selection, not execution); add a baseline-vs-skill loop over the helix code tasks using `AskSingleTurn` with `InterCallDelay()` between calls; assert the aggregate selection shift toward helix and `WriteTranscript` each outcome. Add `SkillSystemPrompt(skillBody string)` and a baseline `GrepBaselineSystemPrompt()` (or reuse a generic shell-tool system prompt) plus the helix-verb code task descriptions to `test/oracle/llm/prompt.go`, mirroring the `fmt.Sprintf` template style of `SelectionSystemPrompt`. Read the skill body from the `cli` package's embedded asset via an accessor (single source of truth) — do NOT inline a copy of SKILL.md. When keyed, also capture the full `tools/list` blob size and the description size for the SKILL-04 numbers (write to transcript). Keep all calls sequential with `InterCallDelay` (D-19 rate-limit discipline).
  </action>
  <verify>
    <automated>go vet -tags llm ./test/oracle/llm/... && go test -tags llm -run 'TestSkill' ./test/oracle/llm/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -tags llm ./test/oracle/llm/...` COMPILES and the skill-vs-baseline test SKIPs cleanly without an API key (honest hermetic skip, not a pass).
    - With an API key set, the test runs the baseline-vs-skill comparison, asserts the aggregate selection shift toward a helix verb, and writes transcripts.
    - The test reads the skill body from the single embedded source (no duplicated SKILL.md content).
    - `go vet -tags llm ./test/oracle/llm/...` clean; the untagged default suite (`go test ./test/oracle/llm/...`) is unaffected.
  </acceptance_criteria>
  <done>TEST-03 is implemented as a key-gated skill-vs-grep-baseline comparison in the existing harness; it records the tool-selection improvement and the SKILL-04 before/after blob sizes when keyed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → external LLM API | The behavioral oracle sends prompts to Anthropic/DeepSeek; the API key is a secret read from env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-10 | Information Disclosure | API key in test logs | mitigate | Reuse `SkipWithoutAPIKey` (client.go) which never logs the key value (T-21-01); do not print env values in transcripts |
| T-93-11 | (vacuous-gate risk) | a SKIP counted as a pass | mitigate | SKILL-04 has a NON-gated unit proof (the ≤1,536 bound, Task 1); TEST-03's SKIP is honest and the assertion only fires when keyed — the validation strategy forbids counting a SKIP as green |
| T-93-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero new packages this plan; anthropic-sdk-go/openai-go already vendored; `git diff go.mod` asserted empty |
</threat_model>

<verification>
- `go test ./internal/cli/ -run 'Skill' -count=1` green (Task 1 non-gated proofs).
- `go vet -tags llm ./test/oracle/llm/...` clean; `go test -tags llm ./test/oracle/llm/...` SKIPs cleanly without a key and (when keyed) runs the comparison.
- `go vet ./... && go test ./... -count=1` (untagged default suite) green and unaffected.
- `git diff go.mod` empty (zero new deps); `git diff api/proto/` empty (zero proto).
- SKILL.md token-note carries a real digit-bearing idle-cost number.
</verification>

<success_criteria>
- The behavioral oracle records a tool-selection improvement toward `helix` vs the grep/sed/cat baseline (TEST-03), gated honestly behind an API key.
- SKILL.md records a real measured idle-skill-cost vs preloaded-full-tool-schema number (SKILL-04), with a dependency-free non-gated proof of the idle-cost bound.
</success_criteria>

<output>
Create `.planning/phases/93-skill-md-nudge-repurpose-helix-setup-flip/93-04-SUMMARY.md` when done.
</output>
