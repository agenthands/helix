---
phase: 94-retire-the-agent-facing-mcp-surface-delete
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - internal/forwarder/forwarder.go
  - internal/forwarder/oneshot.go
  - internal/cli/root.go
  - internal/mcp/server.go
  - internal/daemon/daemon.go
  - internal/daemon/http_session_middleware.go
  - internal/config/config.go
  - internal/config/defaults.go
  - internal/forwarder/dial.go
  - test/harness/runner.go
  - test/integration/harness.go
  - test/oracle/protocol/handshake_test.go
  - test/oracle/protocol/reconnect_test.go
  - test/integration/smoke_http_test.go
  - test/integration/trace_propagation_test.go
  - test/integration/trace_continuity_test.go
  - test/integration/trace_shutdown_test.go
  - test/bench/bench_helpers_test.go
  - internal/forwarder/dial_windows_test.go
autonomous: true
requirements: [RETIRE-01, RETIRE-02]
must_haves:
  truths:
    - "After deletion, go build ./cmd/helix succeeds and the CLI still dials the daemon over the retained gRPC unix socket"
    - "The retained TestCLI_E2E_OneShot (gRPC unix path) and TestCLI_DualRunParity (gRPC reference) remain green post-deletion"
    - "go build -tags integration ./... and go vet -tags integration ./... succeed (HTTP test fallout re-targeted, not false-green)"
    - "git diff api/proto/ and git diff go.mod go.sum are empty after deletion"
  prohibitions:
    - "No reachable stdio MCP server code path remains: RunForwarder, the --mode=stdio route, and the dead RunStdio have zero references"
    - "No HTTP /mcp route remains: listenHTTP, HTTPHandler, httpSessionMiddleware, the listenHTTP launch, and the /mcp mount are gone; --mode http no longer serves MCP"
    - "generateSessionID is NOT deleted — it MOVES to a retained file because CallTool depends on it"
    - "The CLI dial path is NOT deleted: oneshot.go (CallTool), dial files, grpc_client_transport.go, listenSocket, StreamMCP, GRPCTransport, forwarderServiceHandler all retained"
    - "No api/proto/ change and no new/removed go.mod dependency"
  artifacts:
    - path: "internal/forwarder/oneshot.go"
      provides: "retained CLI one-shot CallTool + relocated generateSessionID"
      contains: "func CallTool"
    - path: "internal/cli/root.go"
      provides: "mode switch with stdio+http arms removed, --http-addr flag removed, --serve retained"
      exports: []
  key_links:
    - from: "internal/cli (verb path)"
      to: "internal/forwarder/oneshot.go"
      via: "CLI verb dispatch → CallTool → ConnectOrStartDaemon → StreamMCP (the retained surface)"
      pattern: "CallTool"
    - from: "internal/forwarder/dial.go"
      to: "internal/daemon/daemon.go"
      via: "startDaemon exec args (no http-addr) → listenSocket gRPC unix listener"
      pattern: "exec.Command"
---

<objective>
Wave 2 — the DELETE. With RETIRE-03 parity proven GREEN in 94-01's commit (heads alive) and the gated gRPC-TCP topology in place, remove exactly the two agent-facing MCP heads and the dead RunStdio, re-target the HTTP-transport test fallout onto the retained gRPC path, and assert the zero-proto / zero-dep invariants plus a Windows local-dial smoke.

DELETE: `forwarder.RunForwarder` (+ forwarder-only helpers `isToolsCall`/`sendWithSpan`), the `--mode=stdio` route + `runForwarder`/`runForwarderFn`; `daemon.listenHTTP` + its gated launch + the `/mcp` mount, `mcp.HTTPHandler`, `http_session_middleware.go`; the dead `mcp.RunStdio`; the entire `--http-addr` thread (9 sites incl. `startDaemon`'s exec arg).

KEEP (load-bearing — deleting any breaks the CLI): `oneshot.go`/`CallTool`, the dial files, `grpc_client_transport.go`, `generateSessionID` (MOVED), `listenSocket`, `StreamMCP`, `GRPCTransport`, `forwarderServiceHandler`, the 6 middlewares, and 94-01's `listenGRPCTCP`.

Purpose: the CLI becomes the sole agent surface; the daemon/wire/middleware engine stays behind it.
Output: a smaller tree where no stdio MCP path and no `/mcp` route are reachable, the CLI still dials, and integration-tagged transport tests are exercised (not false-green).

Sequencing: this plan `depends_on: ["94-01"]`. Its deletion commit comes strictly AFTER 94-01's parity-proof commit (RETIRE-03 acceptance: parity green in the commit immediately before deletion).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-retire-the-agent-facing-mcp-surface-delete/94-RESEARCH.md
@.planning/phases/94-retire-the-agent-facing-mcp-surface-delete/94-PATTERNS.md
@.planning/phases/94-retire-the-agent-facing-mcp-surface-delete/94-VALIDATION.md
@.planning/phases/94-retire-the-agent-facing-mcp-surface-delete/94-01-SUMMARY.md

# Delete/keep seam files (read before editing — keep-list is as important as delete-list):
@internal/forwarder/forwarder.go
@internal/forwarder/oneshot.go
@internal/forwarder/dial.go
@internal/cli/root.go
@internal/mcp/server.go
@internal/daemon/daemon.go
@internal/daemon/http_session_middleware.go
@internal/config/config.go
@internal/config/defaults.go
@test/harness/runner.go
@test/integration/harness.go
@test/oracle/protocol/handshake_test.go
@test/integration/smoke_http_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the stdio head (RunForwarder + stdio route + dead RunStdio); MOVE generateSessionID</name>
  <files>internal/forwarder/forwarder.go, internal/forwarder/oneshot.go, internal/cli/root.go, internal/mcp/server.go</files>
  <read_first>
    - internal/forwarder/forwarder.go:22-176 (RunForwarder:22; generateSessionID:141-147 MOVE-not-delete; isToolsCall:159; sendWithSpan:171 — forwarder-only, delete)
    - internal/forwarder/oneshot.go:32-87 (CallTool — RETAINED; depends on generateSessionID at oneshot.go:53 and NewGRPCClientTransport at :54)
    - internal/cli/root.go:67-73 (runForwarderFn seam var), :188-201 (the mode switch: the stdio arm calls runForwarderFn and is DELETED; the auto arm calls cmd.Help and is KEPT; default KEPT), :218-231 (runForwarder func — becomes unused, delete)
    - internal/mcp/server.go:190-193 (RunStdio — ZERO callers, dead, delete freely)
    - 94-PATTERNS.md Seam 1 + Seam 3 (the exact delete/move/keep lines) and Pitfall 1 (deleting shared dial code with the head)
  </read_first>
  <action>
DELETE the stdio head while keeping the shared CLI dial path intact.

1. In `internal/forwarder/forwarder.go`: delete `RunForwarder` (forwarder.go:22-131) and the forwarder-only helpers `isToolsCall` (forwarder.go:159-162) with its `toolsCallMethod`/`toolsCallMethodSpaced` vars (forwarder.go:151,154) and `sendWithSpan` (forwarder.go:171-176). MOVE `generateSessionID` (forwarder.go:141-147) into `internal/forwarder/oneshot.go` (its only remaining caller is CallTool at oneshot.go:53) along with its `crypto/rand`/`io`/`fmt` imports. If forwarder.go would be left holding only `generateSessionID`, you may instead leave a slim forwarder.go containing only that helper — Claude's discretion; the binding test is that `go build ./cmd/helix` succeeds with no undefined-symbol error for the session-id helper. Do NOT delete `oneshot.go`, the dial files, or `grpc_client_transport.go` (the retained wire — Pitfall 1).

2. In `internal/cli/root.go`: delete the stdio arm of the mode switch (root.go:189-192, the arm returning runForwarderFn). KEEP the auto arm (root.go:193, cmd.Help, CLI-04) and the default error arm. Delete `runForwarder` (root.go:218-231) and the now-unused `runForwarderFn` seam var (root.go:73) plus its assignment. Drop the `forwarder` import only if it becomes unused — it stays if the verb/activate path still imports `forwarder.CallTool` (decide by `go build`, not by eye). `--mode` flag handling: after the stdio arm is gone (and the http arm in Task 2), keep the switch coherent; removing `--mode` entirely versus keeping it with only the auto arm is Claude's discretion per RESEARCH Q2 — whichever, no test may assert a removed arm.

3. In `internal/mcp/server.go`: delete the dead `RunStdio` (server.go:190-193) — verified zero callers; the compiler confirms no orphan.

After edits, `go build ./cmd/helix && go vet ./...` MUST be clean. An undefined-symbol error for ConnectOrStartDaemon or the session-id helper means the move/keep was wrong — fix before committing.

Reachability assertion (RETIRE-01 acceptance, no stdio MCP server code path remains reachable): after build is green, prove zero non-comment references to the deleted symbols. The negative grep filters comments so header prose cannot self-invalidate.
  </action>
  <verify>
    <automated>go build ./cmd/helix && go vet ./... && go test ./internal/forwarder/... ./internal/cli/... -count=1</automated>
    <automated>test "$(grep -rn 'func RunForwarder\|func (s \*SerenaMCPServer) RunStdio\|runForwarderFn' internal/ --include='*.go' | grep -v '_test.go' | grep -v '^[^:]*:[0-9]*:[[:space:]]*//' | wc -l)" -eq 0</automated>
  </verify>
  <done>RunForwarder, the stdio mode arm, runForwarder/runForwarderFn, and the dead RunStdio are gone with zero non-comment references; generateSessionID relocated; `go build ./cmd/helix` and `go vet ./...` clean; forwarder + cli tests green; CLI still dials via the retained CallTool path.</done>
</task>
<task type="auto">
  <name>Task 2: Delete the HTTP /mcp head (listenHTTP + HTTPHandler + httpSessionMiddleware) and thread out --http-addr (9 sites)</name>
  <files>internal/daemon/daemon.go, internal/mcp/server.go, internal/daemon/http_session_middleware.go, internal/cli/root.go, internal/config/config.go, internal/config/defaults.go, internal/forwarder/dial.go</files>
  <read_first>
    - internal/daemon/daemon.go:1316-1321 (the gated listenHTTP launch block — delete), :1400-1431 (listenHTTP + the /mcp mount via httpSessionMiddleware(d.mcpServer.HTTPHandler(), ...) at :1405 — delete), :1336,:1412 (http_addr log fields — delete), :1355-1397 (listenSocket — KEEP), :1481+ (StreamMCP handler — KEEP)
    - internal/mcp/server.go:196-200 (HTTPHandler — delete AFTER Task 3 re-targets its 2 test callers)
    - internal/daemon/http_session_middleware.go (httpSessionMiddleware — whole file delete; delete its _test.go sibling if present)
    - internal/cli/root.go:127 (--http-addr flag decl), :184 (the `serve || mode == "http"` guard → reduce to `serve`), :237 (GetString http-addr), :252-254 (the http-addr override block)
    - internal/config/config.go:104-105 (DaemonConfig.HTTPAddr — delete), internal/config/defaults.go:30 (daemon.http_addr default — delete)
    - internal/forwarder/dial.go:145-151 (startDaemon exec args — LOAD-BEARING: the `--http-addr=` arg at dial.go:151 must be dropped, else the auto-spawned daemon dies on an unknown flag → 10s waitForDaemon timeout; update the comment at dial.go:145-150 too)
    - 94-PATTERNS.md Seam 2 (the 9 enumerated --http-addr sites) + Pitfall 4 (the cold-start hang)
  </read_first>
  <action>
DELETE the HTTP head and every `--http-addr` thread site. Order matters: Task 3 must re-target the 2 test callers of HTTPHandler BEFORE this task deletes HTTPHandler — if executing in one pass, do the Task 3 re-targets first, then delete HTTPHandler here. (They are split into two tasks for clarity; the executor may interleave so the build never breaks.)

1. `internal/daemon/daemon.go`: delete the gated listenHTTP launch (daemon.go:1316-1321), the `listenHTTP` function and its `/mcp` mount (daemon.go:1400-1431), and the `http_addr` log fields (daemon.go:1336 and the one inside the deleted listenHTTP at :1412). KEEP `listenSocket` (daemon.go:1355-1397), `StreamMCP`/`forwarderServiceHandler` (daemon.go:1481+), and 94-01's gated `listenGRPCTCP` launch.

2. `internal/mcp/server.go`: delete `HTTPHandler` (server.go:196-200) — only AFTER its test callers are re-targeted (Task 3). The `RunStdio` above it is already deleted in Task 1.

3. Delete `internal/daemon/http_session_middleware.go` entirely (httpSessionMiddleware is HTTP-only, used only by the deleted /mcp mount). Delete its `_test.go` sibling if one exists.

4. Remove the `--http-addr` thread, all sites: the flag declaration (root.go:127); reduce the daemon-start guard `if serve || mode == "http"` to `if serve` (root.go:184) so `--serve` still starts the daemon and `--mode http` no longer serves MCP; remove the `httpAddr` GetString (root.go:237) and the `daemon.http_addr` override block (root.go:252-254); delete `DaemonConfig.HTTPAddr` (config.go:104-105); delete the `daemon.http_addr` default (defaults.go:30).

5. LOAD-BEARING — `internal/forwarder/dial.go:151`: drop the `--http-addr=` arg from `startDaemon`'s `exec.Command(exe, "--serve", "--socket="+socketPath, "--http-addr=")` so the auto-spawned daemon does not die on an unknown flag (Pitfall 4: a missed site = 10s cold-start hang). Update the stale comment block (dial.go:145-150) that explains the `--http-addr=:8080` workaround.

After edits, `go build ./... && go vet ./...` clean, AND `go build -tags integration ./...` clean (the integration-tagged HTTP tests must compile — see Task 3; this is the false-green guard). RETIRE-02 acceptance: no `/mcp` route remains and `--mode http` no longer serves MCP — prove via the negative grep below and the retained E2E.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/daemon/... ./internal/cli/... -count=1</automated>
    <automated>test "$(grep -rn '"/mcp"\|func (d \*Daemon) listenHTTP\|func (s \*SerenaMCPServer) HTTPHandler\|httpSessionMiddleware\|HTTPAddr\|http-addr\|http_addr' internal/ --include='*.go' | grep -v '_test.go' | grep -v '^[^:]*:[0-9]*:[[:space:]]*//' | wc -l)" -eq 0</automated>
    <automated>test ! -f internal/daemon/http_session_middleware.go</automated>
  </verify>
  <done>listenHTTP, the /mcp mount, HTTPHandler, httpSessionMiddleware (file gone), all 9 --http-addr sites (incl. startDaemon's exec arg) removed with zero non-comment references; `--serve` still starts the daemon; `--mode http` no longer serves MCP; `go build ./...`, `go vet ./...`, and `go build -tags integration ./...` clean.</done>
</task>
<task type="auto">
  <name>Task 3: Re-target HTTP-transport test fallout onto gRPC/in-memory (before HTTPHandler deletion); add Windows local-dial smoke</name>
  <files>test/harness/runner.go, test/integration/harness.go, test/oracle/protocol/handshake_test.go, test/oracle/protocol/reconnect_test.go, test/integration/smoke_http_test.go, test/integration/trace_propagation_test.go, test/integration/trace_continuity_test.go, test/integration/trace_shutdown_test.go, test/bench/bench_helpers_test.go, internal/forwarder/dial_windows_test.go</files>
  <read_first>
    - test/harness/runner.go:64-85 (NewHTTPSession → MCPServer().HTTPHandler() at runner.go:70 — re-target), :88-138 (StartRunner wires InMemoryTransports at runner.go:138; runner.Session is the proven in-memory session consumed by TestHandshake_InMemory), :250 (cfg.Daemon.HTTPAddr = "" — delete once HTTPAddr is gone)
    - test/integration/harness.go:79-90 (NewHTTPSession → MCPServer().HTTPHandler() at harness.go:89 — re-target), :333 (cfg.Daemon.HTTPAddr = "" — delete)
    - test/oracle/protocol/handshake_test.go:18 (TestHandshake_InMemory — KEEP, unaffected), :36 (TestHandshake_HTTP — re-target or delete), :63 (TestHandshake_ProtocolVersionMatch — uses NewHTTPSession; re-target the http leg or fold into in-memory)
    - test/oracle/protocol/reconnect_test.go:18,50,73 (3 tests using NewHTTPSession — re-target to gRPC/in-memory or delete the HTTP-only legs)
    - test/integration/smoke_http_test.go:15 (TestHTTPSmoke_ToolCallRoundTrip), :44 (TestHTTPSmoke_WithLS) — entirely HTTP-endpoint; replace with a gRPC unix round-trip smoke or rely on the retained TestCLI_E2E_OneShot, then delete this file
    - test/integration/trace_propagation_test.go:58, trace_continuity_test.go:53, trace_shutdown_test.go:46, test/bench/bench_helpers_test.go:161 (cfg.Daemon.HTTPAddr = "" lines — delete once the field is gone, else build breaks)
    - 94-PATTERNS.md Seam 6 (the exact re-target list) + Pitfall 3 (these files are //go:build integration|llm|llmjudge — a plain `go test ./...` is FALSE-GREEN; you MUST run `go build -tags integration ./...`)
    - internal/forwarder/grpc_client_transport.go:31-53 (NewGRPCClientTransport / Connect — the retained transport a gRPC session can ride)
  </read_first>
  <action>
Re-target the HTTP-only test surface onto the retained gRPC/in-memory path BEFORE Task 2 deletes HTTPHandler (interleave so the build never breaks).

1. `test/harness/runner.go` and `test/integration/harness.go`: replace `NewHTTPSession` (which calls `MCPServer().HTTPHandler()`) with a session over the retained path. Simplest correct option: provide `NewGRPCSession`/reuse the existing in-memory `runner.Session` (StartRunner already wires `mcp.NewInMemoryTransports()` at runner.go:138 and TestHandshake_InMemory already proves it). Either expose a `NewGRPCSession(t)` over `GRPCClientTransport`/`listenSocket`, or have the former HTTP callers consume the in-memory `Session`. Remove `httptest`/`HTTPHandler` references from both harness files.

2. `test/oracle/protocol/handshake_test.go`: KEEP `TestHandshake_InMemory` (line 18, in-memory, unaffected). Re-target `TestHandshake_HTTP` (line 36) to the gRPC/in-memory session (rename appropriately, e.g. TestHandshake_GRPC) OR delete it as redundant with the in-memory test. Re-target the HTTP leg of `TestHandshake_ProtocolVersionMatch` (line 63) to compare in-memory vs gRPC (or drop the second leg). Same intent for `reconnect_test.go` (3 NewHTTPSession sites): re-target to gRPC/in-memory or delete the HTTP-only reconnect legs.

3. `test/integration/smoke_http_test.go`: this whole file is HTTP-endpoint. Replace `TestHTTPSmoke_ToolCallRoundTrip`/`TestHTTPSmoke_WithLS` with a gRPC-unix round-trip smoke (drive one tools/call through CallTool over the unix socket) OR rely on the retained `TestCLI_E2E_OneShot` and delete this file. If you delete it, confirm an equivalent gRPC round-trip remains exercised under the integration tag.

4. Drop the now-dangling `cfg.Daemon.HTTPAddr = ""` lines (trace_propagation_test.go:58, trace_continuity_test.go:53, trace_shutdown_test.go:46, harness.go:333, runner.go:250, test/bench/bench_helpers_test.go:161) — these reference the field deleted in Task 2; leaving them breaks the integration build.

5. Windows local-dial smoke (success criterion 2): add `internal/forwarder/dial_windows_test.go` guarded `//go:build windows` that brings up a daemon and dials it over the local named-pipe/unix-equivalent path, driving one tools/call (mirror the !windows cli_e2e shape). Keep `dial_windows.go` (detachFromProcessGroup) untouched. This is CI-Windows-gated — it will not run on this Linux host; the executor MUST report it honestly as CI-gated (VALIDATION.md Manual-Only) rather than claim a local pass. The TCP opt-in from 94-01 also helps Windows where unix sockets are flaky.

Run BOTH gates: `go test ./... -count=1` (untagged) AND `go build -tags integration ./... && go vet -tags integration ./... && go test -tags integration ./test/oracle/... -count=1` (the false-green guard — these tagged files must compile and pass). HELIX_BIN-gated suites skip cleanly when HELIX_BIN is unset.
  </action>
  <verify>
    <automated>go build -tags integration ./... && go vet -tags integration ./...</automated>
    <automated>go test ./... -count=1</automated>
    <automated>test "$(grep -rn 'NewHTTPSession\|HTTPHandler\|HTTPAddr' test/ --include='*.go' | grep -v '^[^:]*:[0-9]*:[[:space:]]*//' | wc -l)" -eq 0</automated>
    <automated>GOOS=windows go build ./internal/forwarder/...</automated>
  </verify>
  <done>NewHTTPSession/HTTPHandler/HTTPAddr have zero references across test/; the integration-tagged transport tests compile and pass (`go build -tags integration ./...` + `go test -tags integration ./test/oracle/...` green); TestHandshake_InMemory retained; a gRPC round-trip smoke replaces the HTTP smoke; a //go:build windows local-dial smoke exists (CI-gated, reported honestly); untagged `go test ./...` green.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → daemon (DELETED stdio /mcp heads) | Two agent-facing network/process surfaces removed — net attack-surface reduction |
| CLI process → daemon (unix socket, retained) | Local same-host IPC, filesystem-permission scoped |
| Auto-spawned daemon exec args | startDaemon's argv must stay valid post-flag-removal or auto-start breaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-05 | (positive — surface reduction) | removed unauthenticated HTTP /mcp listener (default :8080) | mitigate | Deleting listenHTTP/HTTPHandler removes an unauthenticated network endpoint; assert no listener binds the old http addr post-deletion (negative grep + no HTTPAddr field) |
| T-94-06 | Denial of Service | auto-spawn daemon argv left referencing the removed --http-addr flag | mitigate | startDaemon exec arg cleaned (dial.go:151); auto-start E2E (TestCLI_E2E_OneShot cold path) proves no unknown-flag death / 10s hang |
| T-94-07 | Tampering / regression | accidentally deleting shared dial code (CallTool/dial path) with the head | mitigate | Keep-list enforced; `go build ./cmd/helix` + retained TestCLI_E2E_OneShot + TestCLI_DualRunParity gate the CLI surface post-deletion |
| T-94-08 | (residual) | gated gRPC-TCP listener from 94-01 | accept | Loopback-only by default (T-94-01..04 in 94-01); unchanged by this deletion |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero-new-deps is a gate (`git diff go.mod go.sum` empty); no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- Per-task: `go build ./... && go vet ./...` after every commit (the primary delete-phase gate — orphaned refs surface immediately).
- Reachability (RETIRE-01/02): negative greps prove zero non-comment references to RunForwarder, RunStdio, listenHTTP, HTTPHandler, httpSessionMiddleware, /mcp, HTTPAddr, --http-addr across internal/ and test/.
- False-green guard (mandatory): `go build -tags integration ./... && go vet -tags integration ./... && go test -tags integration ./test/oracle/... -count=1` — the HTTP transport tests are integration-tagged; a plain `go test ./...` does NOT exercise them.
- Retained surface: `go build ./cmd/helix` + `HELIX_BIN=$(pwd)/helix go test -tags integration -run 'TestCLI_E2E_OneShot|TestCLI_DualRunParity' -count=1 ./internal/cli/...` green post-deletion (CLI still dials).
- Invariants: `git diff --exit-code api/proto/ go.mod go.sum` empty.
- Windows: `GOOS=windows go build ./internal/forwarder/...` clean; the //go:build windows local-dial smoke is CI-gated (reported honestly, not claimed locally).
</verification>

<success_criteria>
- RETIRE-01: no reachable stdio MCP server path — RunForwarder, the stdio mode arm, and dead RunStdio gone with zero non-comment references; the CLI still dials (E2E green).
- RETIRE-02: HTTP /mcp endpoint gone, httpSessionMiddleware file removed, all 9 --http-addr sites removed; `--mode http` no longer serves MCP; `--serve` still starts the daemon; auto-spawn argv valid (no cold-start hang).
- generateSessionID relocated (not deleted); the CLI dial path (CallTool, dial files, grpc_client_transport, listenSocket, StreamMCP, GRPCTransport) fully intact.
- Integration-tagged transport tests re-targeted to gRPC/in-memory and GREEN under `-tags integration` (not false-green); TestHandshake_InMemory retained.
- Windows local-dial smoke exists (CI-gated, honest report); `GOOS=windows go build ./internal/forwarder/...` clean.
- `git diff api/proto/ go.mod go.sum` empty.
</success_criteria>

<output>
Create `.planning/phases/94-retire-the-agent-facing-mcp-surface-delete/94-02-SUMMARY.md` when done.

## Artifacts this phase produces (94-02)
**Removed symbols:** `forwarder.RunForwarder`, `isToolsCall`, `sendWithSpan`; `cli.runForwarder`/`runForwarderFn` + the stdio mode arm; `mcp.RunStdio` (dead); `daemon.listenHTTP` + its gated launch + the /mcp mount; `mcp.HTTPHandler`; the `httpSessionMiddleware` file; `DaemonConfig.HTTPAddr` + the `daemon.http_addr` default; the `--http-addr` flag + all 9 thread sites (incl. startDaemon's exec arg).
**Relocated symbol:** `generateSessionID` (forwarder.go → oneshot.go), retained.
**New symbols:** a gRPC/in-memory test session helper replacing NewHTTPSession; a //go:build windows local-dial smoke (internal/forwarder/dial_windows_test.go).
**Retained (load-bearing):** CallTool, the dial files, grpc_client_transport.go, listenSocket, StreamMCP, GRPCTransport, forwarderServiceHandler, the 6 middlewares, and 94-01's listenGRPCTCP.
</output>