---
phase: 95-identity-docs-rewrite-docgen-regen
plan: 02
type: execute
wave: 2
depends_on: ["95-01"]
files_modified:
  - README.md
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [DOCS-01, DOCS-03]
must_haves:
  truths:
    - "No doc claims MCP as the primary agent interface (the two `Protocol: MCP — primary interface` lines in CLAUDE.md and PROJECT.md are reframed CLI-first)."
    - "README/CLAUDE.md/PROJECT.md present a consistent CLI-first identity: agents drive the `helix <verb>` CLI; MCP-SDK/gRPC are described as internal daemon plumbing (NOT removed)."
    - "Stale README config blocks (`--mode=stdio`, `--mode=http`, `/mcp`) are rewritten to `helix setup` + CLI usage (those flags were deleted in Phase 94; only `--mode=auto` remains)."
    - "CLAUDE.md has a Helix-CLI tool-routing section citing real `helix <verb>` names end-to-end."
    - "The external `## Code intelligence: SMTC-first tool routing` matrix (mcp__smtc__*) is LEFT ENTIRELY INTACT, with a one-line clarifier distinguishing it from Helix's own surface."
    - "Stale tool counts (53 / 41+) are removed or reconciled honestly (50 frozen verbs)."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Helix-CLI tool-routing decision matrix using real verbs + CLI-first identity/Constraints"
      contains: "helix go-to-definition"
    - path: "README.md"
      provides: "CLI-first hero/intro + helix setup config (stale MCP-mode blocks removed)"
    - path: ".planning/PROJECT.md"
      provides: "CLI-first What This Is / Core Value / Constraints"
  key_links:
    - from: "CLAUDE.md"
      to: "internal/cli/verbs_gen.go"
      via: "the new Helix-CLI routing section cites real frozen verb names (kebab = toolName.replace _→-)"
      pattern: "helix (go-to-definition|find-references|rename-symbol)"
    - from: "README.md / CLAUDE.md / PROJECT.md"
      to: ".planning/STATE.md"
      via: "Core Value prose reuses the canonical v2.0 wording (STATE.md:26)"
      pattern: "only surface an agent touches"
---

<objective>
Rewrite Helix's identity from "MCP-primary" to "CLI-first" across README.md, CLAUDE.md, and PROJECT.md (the Core Value, the two `Protocol: MCP — primary interface` Constraints lines, the hero/intro framing, and the stale Phase-94-deleted `--mode=stdio`/`--mode=http`/`/mcp` config blocks), and ADD a Helix-CLI tool-routing decision matrix to CLAUDE.md citing the real frozen `helix <verb>` names. Satisfies DOCS-01 and DOCS-03.

Purpose: this is the FINAL v2.0 phase — the docs must describe the frozen, MCP-head-free shape after Phase 94. The agent surface is the `helix` CLI; the MCP-SDK + gRPC IPC are RETAINED as internal daemon plumbing. The docs must reframe to CLI-first WITHOUT over-claiming that MCP was removed.

Output: CLI-first identity prose in the three docs; a new "Helix CLI tool routing" section in CLAUDE.md; the external SMTC matrix left intact with a clarifier; honest count reconciliation.

CONTEXT/REQUIREMENTS label note: REQUIREMENTS.md is canonical — DOCS-02 = table regen (plan 95-01), DOCS-03 = the CLAUDE.md routing-matrix rewrite (this plan). CONTEXT.md swaps the DOCS-02/03 labels for the same two behaviors; this plan uses the REQUIREMENTS.md IDs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-identity-docs-rewrite-docgen-regen/95-RESEARCH.md
@.planning/phases/95-identity-docs-rewrite-docgen-regen/95-PATTERNS.md

# The docs to edit (read the exact lines before editing):
@README.md       # hero (3-5,13,19), How-it-Works (43-49), config blocks (104-145)
@CLAUDE.md       # identity (18,20,24), Constraints (94,179), SMTC section (194-260 — LEAVE INTACT)
@.planning/PROJECT.md  # What This Is (5), Core Value (9), Constraints (222)
@internal/cli/verbs_gen.go  # the 50 frozen verbs — the routing section must cite REAL names
</context>

<artifacts_this_phase_produces>
NEW in this plan:
- A "Helix CLI tool routing" decision-matrix section in CLAUDE.md (real `helix <verb>` names) + a one-line clarifier marking the SMTC section as an external dev server
MODIFIED:
- README.md hero / How-Helix-Works / config blocks (CLI-first; stale MCP-mode JSON removed)
- CLAUDE.md identity / Core Value / both Constraints `Protocol` lines
- .planning/PROJECT.md What-This-Is / Core Value / Constraints `Protocol` line
LEFT INTACT (must NOT change):
- The `## Code intelligence: SMTC-first tool routing` matrix in CLAUDE.md (external mcp__smtc__* dev server)
- Accurate internal-MCP statements (internal/mcp SDK, gRPC IPC, middleware) and dated historical changelog lines
</artifacts_this_phase_produces>

<tasks>

<!-- planner-region-allow: primary interface -->
<!-- Intentional: Task 1 and Task 3 both negative-grep "primary interface" on the
     same docs. This is NOT a sibling conflict — both tasks REMOVE the phrase (no
     task needs it present). The shared file-wide ban is the intended, consistent
     end state (DOCS-01: zero MCP-as-primary-agent-interface claims). -->

<task type="auto">
  <name>Task 1: CLI-first identity rewrite across README, CLAUDE.md, PROJECT.md</name>
  <files>README.md, CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>
    - 95-PATTERNS.md:180-217 ("Doc Prose Rewrites (DOCS-01)") — the line-by-line current-text → action tables for all three docs, plus the canonical replacement Core Value and the MUST-stay-accurate framing
    - 95-RESEARCH.md:132-171 (MCP-Primary Claims Inventory + "What MUST stay accurate") — exact file:line claims and the precise retained-internal-MCP wording
    - .planning/STATE.md:26 (the canonical v2.0 Core Value to reuse verbatim) and STATE.md:82,133 (Phase-94: heads deleted, SDK/gRPC retained)
    - README.md:104-145 (the stale manual-config JSON blocks using `--mode=stdio` / `--mode=http` / `/mcp`)
    - CLAUDE.md:33,55,57,69 (accurate internal-MCP statements — LEAVE INTACT) and CLAUDE.md:194-260 (SMTC section — LEAVE INTACT)
    - .planning/PROJECT.md:15,29,35,71,83,145,203,207,290 (dated historical changelog lines — do NOT rewrite history)
  </read_first>
  <action>
    Reframe every present-tense MCP-as-primary-agent-interface claim to CLI-first, using `Edit` (scoped) on each doc — never a whole-file Write. Targets per 95-PATTERNS.md:186-217:
    - CLAUDE.md: line 18 drop "for MCP" from the identity; line 20 retire the stale "53 MCP-tools" count phrase → "50 `helix` CLI verbs" (or count-free per the STATE:84 "enumerated by name, not a hardcoded number" rule); line 24 replace the Core Value with the canonical STATE.md:26 wording; lines 94 and 179 reframe the `Protocol: MCP — primary-interface` Constraints lines to "CLI is the agent interface; the MCP Go SDK + gRPC IPC are retained as internal daemon plumbing."
    - README.md: line 13 the "platform for MCP — 41+ tools" hero → CLI-first + 50 verbs (or count-free); line 19 the "integrates via MCP" bullet → "agents drive the `helix` CLI (via Bash)"; line 43 the stale "41+ MCP-tools" count phrase → CLI-first; lines 47-49 the "Agents connect via MCP through stdio / Streamable HTTP" block is STALE/WRONG (both heads deleted Phase 94) — rewrite to "agents invoke `helix <verb>` via Bash; the CLI dials a warm daemon over internal gRPC"; lines 104-145 rewrite the manual-config JSON blocks (which document the removed stdio/http daemon-mode flags and the `/mcp` endpoint) to `helix setup` + CLI usage (only the auto daemon mode remains; legacy modes error per STATE 94-02).
    <!-- planner-discipline-allow: 41+ MCP tools -->
    <!-- planner-discipline-allow: --mode=stdio -->
    <!-- planner-discipline-allow: --mode=http -->
    - PROJECT.md: line 5 "platform for MCP … 41+ callable MCP tools" → CLI-first; line 9 Core Value → canonical STATE.md:26 wording; line 222 the `Protocol` Constraints line → same CLI-first reframe.
    Use the MUST-stay-accurate framing verbatim from 95-PATTERNS.md:184 — do NOT write "MCP removed/gone"; only the agent-facing heads were deleted, the SDK/gRPC are retained internally. LEAVE INTACT: the accurate internal-MCP statements (CLAUDE.md:33/55/57/69), the SMTC routing section (handled in Task 2), and dated historical changelog/milestone lines in PROJECT.md (these are records of shipped work, not present-tense identity claims). For any "MCP" string inside the GENERATED README tool table, do NOT hand-edit it here — that table is owned by plan 95-01 / `cmd/docgen`.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/helix && ! grep -rn 'primary interface' README.md CLAUDE.md .planning/PROJECT.md && ! grep -rn -- '--mode=stdio\|--mode=http' README.md && echo NO_PRIMARY_CLAIMS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'primary interface' README.md CLAUDE.md .planning/PROJECT.md` returns ZERO matches (the MCP-as-primary lines are reframed).
    - `grep -rn -- '--mode=stdio' README.md` and `grep -rn -- '--mode=http' README.md` return ZERO matches (stale removed-flag config gone).
    - `grep -rn '/mcp' README.md` returns ZERO matches in the config blocks (the deleted HTTP endpoint is gone). [Scope: the manual-config / How-it-Works region; verify by inspection.]
    - `grep -c '53 MCP tools' CLAUDE.md` == 0 and `grep -c '41+ MCP tools\|41+ tools\|41+ callable MCP tools' README.md .planning/PROJECT.md` == 0 (stale counts reconciled).
    - At least one of the three docs contains the canonical Core Value phrase `only surface an agent touches` (STATE.md:26 wording reused).
    - The retained-internal-MCP framing is present (grep for "internal daemon plumbing" or "retained" near an MCP mention in at least README + CLAUDE.md) — docs do NOT claim MCP was removed.
    - `git diff --quiet go.mod` (no code/dep change).
  </acceptance_criteria>
  <done>README, CLAUDE.md, and PROJECT.md present a consistent CLI-first identity with no present-tense MCP-as-primary-agent-interface claim and no stale removed-flag config; the retained-internal-MCP-plumbing framing is accurate; historical lines and accurate internal-MCP statements are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Helix-CLI tool-routing matrix to CLAUDE.md (SMTC section left intact)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - 95-PATTERNS.md:221-249 ("CLAUDE.md NEW Helix-CLI routing section") — the format analog (the SMTC decision-matrix shape to MIRROR), the new-section shape, and the explicit clarifier note
    - 95-RESEARCH.md:173-195 (CLAUDE.md Tool-Routing) — the decisive SMTC-vs-Helix distinction and why rewriting the SMTC section would be factually WRONG
    - 95-RESEARCH.md:186-195 ("Verb names for the routing section") — the 50 frozen kebab verbs grouped by groupID; cite REAL names only
    - CLAUDE.md:194-260 (the existing `## Code intelligence: SMTC-first tool routing` section — the FORMAT to mirror and the section to LEAVE INTACT)
    - internal/cli/verbs_gen.go (the authoritative frozen verb set — verb = toolName.replace("_","-"))
  </read_first>
  <action>
    ADD a new concise "## Helix CLI tool routing" section to CLAUDE.md (Open Q1 recommendation (a) — there is NO Helix-own routing matrix today to rewrite, so DOCS-03 is satisfied by creating the matrix it describes). Mirror the FORMAT of the existing SMTC "Decision matrix" (a `| Question | Use this | Not this |` table) per 95-PATTERNS.md:236-248, but cite REAL frozen `helix <verb>` names from internal/cli/verbs_gen.go — e.g. `helix go-to-definition`, `helix find-references`, `helix find-implementations`, `helix get-symbol-overview`, `helix rename-symbol`, `helix analyze-blast-radius`, `helix replace-symbol-body`, `helix search-in-files`, `helix get-diagnostics`. Open the section with a one-liner: agents drive Helix via the `helix <verb>` CLI (Bash), not MCP — prefer these over grep/sed/cat. Add the clarifier note (95-PATTERNS.md:246-247): the `## Code intelligence: SMTC-first tool routing` section below describes an EXTERNAL dev MCP server (show-me-the-code), NOT Helix's own tools — do not conflate the two. Do NOT invent verbs (Helix ships no taint/CFG/IR/slice tools — those are SMTC-only). CRITICAL: do NOT modify, rewrite, or re-anchor any `mcp__smtc__*` row in the existing SMTC section — leave that entire section byte-for-byte intact. Use `Edit` to insert the new section (do NOT Write the whole file). Place the new Helix section ABOVE the SMTC section so the clarifier reads naturally ("the section below…").
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/helix && grep -q 'helix go-to-definition' CLAUDE.md && grep -q 'helix find-references' CLAUDE.md && grep -q 'mcp__smtc__goto_definition' CLAUDE.md && grep -q 'Helix CLI tool routing' CLAUDE.md && echo ROUTING_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Helix CLI tool routing' CLAUDE.md` ≥ 1 (the new section header exists).
    - `grep -c 'helix go-to-definition' CLAUDE.md` ≥ 1 AND `grep -c '| \`helix ' CLAUDE.md` ≥ 5 (the routing matrix cites real CLI verbs end-to-end).
    - `grep -c 'mcp__smtc__' CLAUDE.md` is UNCHANGED from the pre-edit count (the external SMTC matrix is fully preserved — verify the count matches the baseline via `git show HEAD:CLAUDE.md | grep -c 'mcp__smtc__'`).
    - The clarifier distinguishing the SMTC section as an external dev server is present (grep for "external" near "show-me-the-code" or "SMTC").
    - Every verb cited in the new section exists in `internal/cli/verbs_gen.go` (no invented verbs — spot-check each kebab name maps to a real `verbSpecs` entry; the verb == toolName.replace("_","-") mapping is exact).
  </acceptance_criteria>
  <done>CLAUDE.md has a Helix-CLI tool-routing decision matrix citing only real frozen verbs end-to-end, with a clarifier separating it from the untouched external SMTC matrix; DOCS-03's "the routing matrix cites CLI verbs end-to-end" is literally true.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide doc consistency + drift-gate re-verification</name>
  <files>README.md, CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>
    - 95-VALIDATION.md:38-48 (Per-Task Verification Map anchors — the DOCS-01/02/03 grep gates)
    - 95-RESEARCH.md:248-260 (Runtime State Inventory — the "after docs are edited, what still claims MCP-primary?" canonical question)
    - 95-RESEARCH.md:280-291 (Pitfall 4: stale counts 53/41+/51 — reconcile honestly to 50 verbs)
  </read_first>
  <action>
    Final consistency sweep across the three docs (no new content — reconciliation only, via scoped `Edit`s where a residual claim survives):
    - Re-grep all three docs for any surviving present-tense MCP-as-primary framing, stale tool counts (the 53 / 41-plus phrases), or references to the removed stdio/http daemon-mode flags and the `/mcp` endpoint (outside accurate historical/internal context) flagged in 95-RESEARCH.md:136-165 (including the lower-priority README internal-detail lines ~199/411/428 — reword "isolated from MCP traffic" → "daemon traffic", "MCP Runtime (stdio/HTTP transports…)" → internal-runtime-without-agent-facing-heads framing). Fix any residual.
    - Confirm the docgen drift gate from plan 95-01 is still green AFTER the README prose edits (the prose edits are OUTSIDE the `<!-- BEGIN/END TOOLS -->` markers, so `--check` must remain green — if it trips, a prose edit accidentally touched the generated table region; fix by reverting that region and re-editing outside the markers).
    - Confirm the full Go suite + vet are unaffected (this plan is docs-only; `git diff go.mod` empty, no `.go` files changed by this plan).
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/agenthands/helix && go run ./cmd/docgen --check && ! grep -rn 'primary interface' README.md CLAUDE.md .planning/PROJECT.md && ! grep -rIn '53 MCP tools\|41+ MCP tools' README.md CLAUDE.md .planning/PROJECT.md && git diff --quiet go.mod && echo PHASE_CONSISTENT</automated>
  </verify>
  <acceptance_criteria>
    - `go run ./cmd/docgen --check` exits 0 (prose edits did NOT disturb the generated table; the 95-01 gate still holds).
    - `grep -rn 'primary interface' README.md CLAUDE.md .planning/PROJECT.md` → ZERO matches.
    - `grep -rIn '53 MCP tools\|41+ MCP tools\|41+ tools\|41+ callable' README.md CLAUDE.md .planning/PROJECT.md` → ZERO matches (counts reconciled).
    - `make verify-docs` exits 0 (the wired gate is green end-to-end).
    - `git diff --quiet go.mod` (zero dep change) AND no `.go` file appears in `git diff --name-only` for this plan's commit (docs-only).
    - `mcp__smtc__` count in CLAUDE.md still matches the pre-phase baseline (SMTC matrix untouched).
  </acceptance_criteria>
  <done>All three docs are CLI-first-consistent with zero MCP-as-primary claims and zero stale counts; the docgen drift gate remains green after the prose edits; the SMTC matrix is intact; no code or dependency changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Pure documentation edits. No runtime code path, input handling, network, or auth/crypto/data surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02 | Information Disclosure | Docs misstating network exposure of the retained gRPC/MCP plumbing | mitigate | Use the precise framing (default unix socket; non-loopback TCP is opt-in/gated per RETIRE-04); do NOT claim MCP removed and do NOT misstate the daemon's listener exposure (Task 1, 95-RESEARCH §Security Domain). |
| T-95-SC | Tampering | npm/pip/cargo/go installs | accept | Zero new dependencies — docs-only phase. Verified by `git diff go.mod` empty (every task's acceptance). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `grep -rn 'primary interface' README.md CLAUDE.md .planning/PROJECT.md` → zero (DOCS-01).
- `grep -q 'helix go-to-definition' CLAUDE.md && grep -q 'mcp__smtc__goto_definition' CLAUDE.md` → both present (DOCS-03 routing added, SMTC intact).
- `go run ./cmd/docgen --check` → exit 0 (prose edits did not disturb the generated table).
- `make verify-docs` → exit 0.
- `git diff go.mod` → empty; no `.go` files changed by this plan.
- Manual backstop: human skim that the three intros read as genuinely CLI-first, not keyword-swapped (95-VALIDATION Manual-Only Verifications).
</verification>

<success_criteria>
DOCS-01 satisfied: no doc claims MCP as the primary agent interface; CLI-first framing is consistent across README, CLAUDE.md, and PROJECT.md; MCP-SDK/gRPC are accurately described as retained internal plumbing (not removed). DOCS-03 satisfied: CLAUDE.md carries a Helix-CLI tool-routing matrix citing real frozen `helix <verb>` names end-to-end, with the external SMTC matrix left intact and a clarifier preventing conflation. Zero new dependencies; no code change.
</success_criteria>

<output>
Create `.planning/phases/95-identity-docs-rewrite-docgen-regen/95-02-SUMMARY.md` when done.
</output>
