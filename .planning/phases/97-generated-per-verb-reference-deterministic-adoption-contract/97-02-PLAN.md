---
phase: 97-generated-per-verb-reference-deterministic-adoption-contract
plan: 02
type: tdd
wave: 2
depends_on: ["97-01"]
files_modified:
  - internal/cli/reference_contract_test.go
  - internal/cli/nudge_test.go
autonomous: true
requirements: [ADOPT-01]
must_haves:
  truths:
    - "The completeness gate iterates cli.VerbToolNames() (the 50 frozen verbs = authority, NOT refgen's own output) and asserts the embedded reference.md mentions every kebab verb"
    - "Deleting one verb's section from an in-memory copy of reference.md turns the completeness gate RED (revert-and-fail proven)"
    - "A per-shape nudge golden maps each standard-tool shape (grep / grep-r / sed-i / cat / find) to the SPECIFIC emitted `helix <verb>` token, keyed on the emitted command — not on a `Contains(..., \"helix\")` substring"
    - "Breaking one nudge-shape mapping (asserting the wrong verb) turns the golden RED (revert-and-fail proven)"
    - "The golden rejects empty-bucket-as-pass: it asserts len(cases) >= 5 covering all five shapes, and asserts per-shape (never an empty-iteration pass)"
    - "The negative control (grep TODO README.md) produces no advisory, and every shape preserves the advisory exit-0 / fail-open contract"
    - "The contract runs in the default untagged `go test` suite — it BLOCKS merge"
  artifacts:
    - path: "internal/cli/reference_contract_test.go"
      provides: "ADOPT-01a completeness gate (authority = VerbToolNames) + revert-and-fail sibling"
      contains: "VerbToolNames"
    - path: "internal/cli/nudge_test.go"
      provides: "ADOPT-01b per-shape nudge golden (keyed on emitted command) + revert-and-fail + empty-bucket floor"
      contains: "GreaterOrEqual"
  key_links:
    - from: "internal/cli/reference_contract_test.go"
      to: "internal/cli/verb.go (VerbToolNames) + internal/cli/skill.go (embedded reference.md)"
      via: "iterate the 50-verb authority, assert each kebab verb appears in the embedded reference bytes"
      pattern: "VerbToolNames"
    - from: "internal/cli/nudge_test.go (golden)"
      to: "internal/cli/nudge.go (bashSteerMessage/steerMessage)"
      via: "runNudgeCapture/parseAdvisory assert the specific emitted helix verb per shape"
      pattern: "runNudgeCapture"
---

<objective>
Add the deterministic, merge-gating adoption contract (ADOPT-01): (a) a completeness gate proving the generated `reference.md` covers every frozen verb, sourced from `cli.VerbToolNames()` as the authority — NOT refgen's own output; and (b) a per-shape nudge-fires golden table mapping each standard-tool shape to the SPECIFIC suggested `helix <verb>`, keyed on the emitted command. Both ship a mandatory deliberate break-the-invariant → assert-RED test, and the golden rejects empty-bucket-as-pass.

Purpose: This is the phase's anti-vacuity heart. The v1.12 milestone shipped four vacuous-pass CRITICALs that only adversarial revert-and-fail tests caught. ADOPT-01 makes verb-reference completeness and nudge correctness a merge gate that can actually go RED.

Output: a new `internal/cli/reference_contract_test.go` (ADOPT-01a + revert-and-fail) and extensions to `internal/cli/nudge_test.go` (ADOPT-01b golden + revert-and-fail + empty-bucket floor), strengthening the existing weak `Contains(..., "helix")` assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-generated-per-verb-reference-deterministic-adoption-contract/97-RESEARCH.md
@.planning/phases/97-generated-per-verb-reference-deterministic-adoption-contract/97-PATTERNS.md
@.planning/phases/97-generated-per-verb-reference-deterministic-adoption-contract/97-01-SUMMARY.md

# Load-bearing analogs (read before editing):
@internal/cli/nudge.go
@internal/cli/verb.go
</context>

<feature>
  <name>ADOPT-01a — Reference completeness gate (authority = VerbToolNames)</name>
  <files>internal/cli/reference_contract_test.go</files>
  <read_first>
    - internal/cli/nudge_test.go:94-104 — TestHelixSymbolicTools_NoDrift, the existing precedent for cross-checking against VerbToolNames() as authority
    - internal/cli/verb.go:81-88 — VerbToolNames() (the 50 frozen verbs; the single completeness authority — Pitfall 2: NEVER source completeness from refgen's own output)
    - internal/cli/skill.go (Plan 01 result) — the embeddedSkillFS embed.FS + a reference.md reader; reference_contract_test.go is in-package (package cli) so it can read the embedded FS directly
    - cmd/docgen/main.go:129 — the mechanical kebab mapping strings.ReplaceAll(toolName, "_", "-")
  </read_first>
  <behavior>
    Cases (input -> expected):
    - TestReferenceCoversEveryVerb: for each toolName in VerbToolNames() (50/50), the embedded reference.md bytes contain the kebab verb (ReplaceAll(toolName,"_","-")) -> all present, gate GREEN
    - TestReferenceCompletenessRevertFails (MANDATORY revert-and-fail): take the embedded reference.md bytes, strip ONE verb's section in-memory, run the same completeness check against the mutated copy -> it reports the now-missing verb / the check fails. Proves the gate is registry-sourced, not a set-compared-to-itself tautology (deleting a verb CAN turn it RED)
    - The authority is VerbToolNames() — assert len() == 50 so a future verb addition without a reference regen also trips REF-03's --check and this gate
  </behavior>
  <implementation>
    RED first: write TestReferenceCoversEveryVerb iterating VerbToolNames() and asserting the embedded reference.md contains each kebab verb (assert.Containsf with a "reference.md missing section for verb %q" message), plus a small in-package reader for the embedded reference (read skills/helix/reference.md from embeddedSkillFS). Before Plan 01's reference.md exists this is RED; after Plan 01 it is GREEN. Then write the revert-and-fail sibling TestReferenceCompletenessRevertFails: factor the completeness check into a helper taking the reference bytes + the authority list and returning the missing verbs; the green test passes the real bytes, the revert test passes a copy with one verb's section deleted (delete by removing the line/section containing the kebab verb token) and asserts the helper returns that verb as missing. Do NOT source the verb list from refgen — only VerbToolNames(). Keep the test in `package cli` (untagged) so it BLOCKS merge.
  </implementation>
</feature>

<feature>
  <name>ADOPT-01b — Per-shape nudge golden, keyed on the emitted command</name>
  <files>internal/cli/nudge_test.go</files>
  <read_first>
    - internal/cli/nudge_test.go:276-332 — runNudgeCapture / parseAdvisory (reuse verbatim; redirects stdin/stdout, isolates session-stats per t.TempDir())
    - internal/cli/nudge_test.go:334-356 — TestNudgeAdvisory_BashCodeGrep_Suggests (the weak `Contains(..., "helix")` at :344 to STRENGTHEN) + TestNudgeAdvisory_BashNonCodeGrep_Silent (the negative control to fold in)
    - internal/cli/nudge.go:158-206 — steerMessage / bashSteerMessage: the EXACT emitted verbs per shape (find->helix find-files :189; cat->helix read-file :191-192; sed -i->helix replace-in-file :194-195; grep -r/-R->helix find-references :199-201; plain grep->helix search-symbols :203-204)
  </read_first>
  <behavior>
    Cases (Bash command -> expected emitted helix verb token in advisory.AdditionalContext):
    - `grep "func Foo" main.go`        -> "helix search-symbols"
    - `grep -r "Bar(" internal/x.go`   -> "helix find-references"
    - `sed -i 's/a/b/' pkg/s.go`       -> "helix replace-in-file"
    - `cat internal/edit.go`           -> "helix read-file"
    - `find . -name '*.go'`            -> "helix find-files"
    For every case: runNudge returns nil (exit-0 / fail-open contract preserved); the advisory parses; AdditionalContext Contains the SPECIFIC expected verb token (not merely "helix").
    Empty-bucket guard: require.GreaterOrEqual(len(cases), 5) with a comment naming the five shapes (grep / grep-r / sed-i / cat / find); assert per-shape, never an empty iteration.
    Negative control: `grep TODO README.md` -> no advisory (fold in the existing TestNudgeAdvisory_BashNonCodeGrep_Silent expectation).
    Revert-and-fail (MANDATORY): a deliberately-wrong expectation (e.g. `cat x.go` -> expect "helix rename-symbol") makes the golden assertion FAIL — proving the golden keys on the SPECIFIC verb, not on the "helix" substring.
  </behavior>
  <implementation>
    RED first: add TestNudgeShapeGolden with the case table above. For each case call runNudgeCapture(t, hookInput{ToolName:"Bash", ToolInput:map[string]any{"command":tc.cmd}}); require.NoError (exit-0); parseAdvisory; assert.Containsf(adv.HookSpecificOutput.AdditionalContext, tc.wantVerb, "shape %q must steer to %q, got %q", ...). Map wantVerb VERBATIM from bashSteerMessage (nudge.go:180-206) per the table in 97-PATTERNS.md. Add the empty-bucket floor: require.GreaterOrEqual(t, len(cases), 5) with the naming comment. Fold the negative control as a sub-case asserting parseAdvisory returns ok==false for `grep TODO README.md`. Replace (do not merely add alongside) the weak Contains(..., "helix") assertion at nudge_test.go:344 with the specific-verb assertion for that grep shape so the Pitfall 3 weak assertion is gone. Then write the revert-and-fail proof TestNudgeGoldenRevertFails: run the same harness for `cat x.go`, assert the advisory does NOT contain "helix rename-symbol" (i.e. a wrong-verb expectation would fail) — proving the golden distinguishes the specific verb. Keep all of it in the untagged suite (package cli) so it BLOCKS merge. Do NOT broaden the classifier (that is STEER-01, Phase 98) — assert only the current mapping.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → nudge stdin/stdout | runNudgeCapture feeds a crafted Bash command string as DATA into `helix nudge`; the classifier never execs it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-05 | Tampering/Elevation | nudge classifier reads a crafted Bash command (test feeds adversarial shapes) | accept | The classifier tokenizes the command as DATA and never calls os/exec (nudge.go classifyBashTarget, T-93-05); unchanged in P97. The golden only reads stdout. No new attack surface. |
| T-97-06 | Repudiation | a vacuous contract that cannot go RED (the v1.12 four-CRITICAL lesson) | mitigate | Two mandatory revert-and-fail tests (completeness drop-a-verb, nudge break-a-mapping) + an empty-bucket floor (len(cases) >= 5, per-shape assert) prove the gate is non-vacuous |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages; test-only plan using in-tree testify already present (RESEARCH "Package Legitimacy Audit"). |
</threat_model>

<verification>
- `go vet ./...` clean
- `go test ./internal/cli/... -count=1` green (completeness + golden + both revert-and-fail + negative control)
- The revert-and-fail tests demonstrably fail when the invariant is broken (proven during the RED step, asserted as the failing-condition path)
- Full suite `go test ./...` green; the contract runs untagged (BLOCKS merge)
- `git diff go.mod` empty
</verification>

<success_criteria>
- ADOPT-01a: completeness gate sourced from VerbToolNames() (50 frozen verbs) asserts reference.md covers every verb; deleting a verb turns it RED
- ADOPT-01b: per-shape golden maps each of the 5 standard-tool shapes to the SPECIFIC emitted helix verb, keyed on the command; breaking a mapping turns it RED; empty-bucket-as-pass rejected (len>=5, per-shape); negative control silent; exit-0 preserved on every shape
- The weak `Contains(..., "helix")` assertion (nudge_test.go:344) is replaced by a specific-verb assertion
- The contract runs in the default untagged suite and BLOCKS merge
</success_criteria>

<output>
Create `.planning/phases/97-generated-per-verb-reference-deterministic-adoption-contract/97-02-SUMMARY.md` when done
</output>
