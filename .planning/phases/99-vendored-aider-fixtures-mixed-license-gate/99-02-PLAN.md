---
phase: 99-vendored-aider-fixtures-mixed-license-gate
plan: 02
type: tdd
wave: 2
depends_on: [99-01]
files_modified:
  - cmd/helix-bench/verify_licenses.go
  - cmd/helix-bench/verify_licenses_test.go
  - Makefile
autonomous: true
requirements: [VENDOR-03]
must_haves:
  truths:
    - "make verify-licenses returns 0 on the committed good tree (the 6 MIT track blocks + the Apache-2.0 fixture block both validate, and every vendored file's on-disk sha256 matches its VENDOR-MANIFEST.md row)"
    - "The gate strict-decodes the Apache-2.0 fixture block via a FixtureLicense struct + hasTopLevelFixtureKey pre-filter (KnownFields(true) — unknown key fails closed), parallel to the existing TrackLicense path"
    - "The gate walks the full vendored tree bidirectionally: every on-disk file has a manifest entry with a matching sha256, AND every manifest entry exists on disk (both directions hard-fail)"
    - "The gate asserts dual disposition: ZERO Apache-2.0 fixture blocks is itself an error (regression to single-disposition fails closed)"
    - "An anti-vacuity tamper test makes the gate go RED on every sabotage class: flipped license value, dropped manifest entry, mutated fixture byte (sha mismatch), removed NOTICE/LICENSE, zero Apache-2.0 blocks"
  artifacts:
    - path: "cmd/helix-bench/verify_licenses.go"
      provides: "Extended dual-disposition gate: FixtureLicense + hasTopLevelFixtureKey + manifest-vs-disk sha256 walk + fail-closed dual-disposition assertion"
      contains: "FixtureLicense"
    - path: "cmd/helix-bench/verify_licenses_test.go"
      provides: "Anti-vacuity tamper tests + a good-tree control"
      contains: "Tamper"
    - path: "Makefile"
      provides: "verify-licenses target wired with --manifest/--tree flags over the full vendored tree"
      contains: "verify-licenses"
  key_links:
    - from: "cmd/helix-bench/verify_licenses.go"
      to: "bench/datasets/aider-polyglot/VENDOR-MANIFEST.md"
      via: "the gate reads the manifest, parses per-file sha256 rows, and cross-checks them against the on-disk tree"
      pattern: "VENDOR-MANIFEST|manifest"
    - from: "cmd/helix-bench/verify_licenses.go"
      to: "bench/datasets/aider-polyglot/fixtures/"
      via: "filepath.WalkDir over the --tree root recomputing crypto/sha256 per file"
      pattern: "WalkDir|sha256"
    - from: "Makefile"
      to: "cmd/helix-bench/verify_licenses.go"
      via: "make verify-licenses invokes go run ./cmd/helix-bench verify-licenses <audit> --manifest <m> --tree <t>"
      pattern: "go run ./cmd/helix-bench verify-licenses"
---

<objective>
Extend the existing `cmd/helix-bench` `verify-licenses` HARD-FAIL gate (VENDOR-03) to scan the full mixed-license vendored tree under BOTH dispositions: keep the existing MIT `track:` strict-decode loop, ADD an Apache-2.0 `fixture:` block kind, and ADD a bidirectional manifest-vs-disk sha256 walk that fails closed on any tamper/missing/extra file. Ship the anti-vacuity tamper test that proves the gate goes RED on every sabotage class (the v1.12 lesson).

Purpose: Without the manifest-vs-disk walk + dual-disposition assertion + tamper test, the gate is vacuous — it would pass whether or not the vendored tree is correctly dispositioned. This plan makes the license gate actually load-bearing over the Plan 01 tree.

Output: an extended `verify_licenses.go`, new tamper tests in `verify_licenses_test.go`, and a `Makefile` `verify-licenses` recipe wired over the full tree. Reuses the shipped strict-decode engine + pin.go digest discipline — extends, does NOT rebuild.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-vendored-aider-fixtures-mixed-license-gate/99-RESEARCH.md
@.planning/phases/99-vendored-aider-fixtures-mixed-license-gate/99-PATTERNS.md

# The gate to extend + its tests + the shared parse primitives + the SHA discipline:
@cmd/helix-bench/verify_licenses.go
@cmd/helix-bench/verify_licenses_test.go
@bench/datasets/aider-polyglot/pin.go
</context>

<artifacts_this_phase_produces>
This plan (Plan 02) produces the GATE that consumes Plan 01's tree + manifest + audit.

| Artifact | Role | Notes |
|----------|------|-------|
| `FixtureLicense` struct + `hasTopLevelFixtureKey` | gate (NEW) | second disposition; mirrors `TrackLicense` + `hasTopLevelTrackKey` |
| manifest-vs-disk sha256 walk | gate (NEW) | bidirectional fail-closed; reuses validatePathSegment containment idea + crypto/sha256 |
| dual-disposition assertion | gate (NEW) | ZERO Apache-2.0 blocks → error |
| `--manifest` / `--tree` flags on `newVerifyLicensesCmd` | CLI wiring (NEW) | NOT mounted on root (preserves `--help` count) |
| tamper tests + good-tree control | test (NEW) | the anti-vacuity SC4 proof |
| `Makefile` `verify-licenses` recipe | build (MODIFY) | wires the new flags over the full tree |
</artifacts_this_phase_produces>

<feature>
  <name>Dual-disposition license gate with manifest-vs-disk sha256 walk + anti-vacuity tamper proof</name>
  <files>cmd/helix-bench/verify_licenses.go, cmd/helix-bench/verify_licenses_test.go, Makefile</files>
  <behavior>
    Write these failing tests FIRST (RED), then extend the gate to GREEN. Each tamper case mirrors the existing `*FailsClosed` shape (verify_licenses_test.go:52-109) and asserts a non-nil error:

    - Good-tree control: a materialized good MIT+Apache tree + VENDOR-MANIFEST.md (real sha256) + dual-disposition LICENSE-AUDIT.md → the gate returns nil (so it is not RED-on-everything).
    - Apache-2.0 block decodes: the `fixture:` block strict-decodes into FixtureLicense; an unknown key in it → strict-decode error (mirrors TestVerifyLicenses_UnknownKeyFailsStrictDecode).
    - Dual-disposition required: zero Apache-2.0 `fixture:` blocks (only MIT track blocks present) → error (regression to single disposition fails closed).
    - Flipped license value: change an audit block's `license:` (e.g. MIT→GPL or Apache-2.0→MIT) in a way the recorded disposition forbids → error.
    - Dropped manifest entry: an on-disk vendored file with NO manifest row → error ("no manifest entry").
    - Mutated fixture byte: flip one byte of a vendored fixture so its on-disk sha256 ≠ its manifest sha256 → error ("sha256 mismatch").
    - Extra manifest entry: a manifest row whose file is absent on disk → error (reverse direction).
    - Removed sidecar: delete the `notice_path` or `license_path` target named by the Apache-2.0 fixture block → error ("missing notice/license path").

    Implementation (GREEN), extending verify_licenses.go in place:
    - Add `FixtureLicense` struct with EXACTLY the keys `fixture`/`source_repo`/`license`/`notice_path`/`license_path` (matches the Plan 01 audit block; KnownFields(true) rejects extras).
    - Add `hasTopLevelFixtureKey(seg)` mirroring `hasTopLevelTrackKey` (swap `"track:"`→`"fixture:"`).
    - In the audit loop, decode `fixture:` segments into FixtureLicense alongside the existing `track:` loop; count fixture blocks; assert ≥1 (dual-disposition fail-closed); for each, assert `License != ""` AND the `notice_path`/`license_path` files exist on disk (fail closed if missing).
    - Add a manifest-vs-disk walk: parse VENDOR-MANIFEST.md's per-file `path|sha256|...` rows into a map; `filepath.WalkDir` the `--tree` root, recompute `crypto/sha256` per file, assert every on-disk file has a manifest entry with a matching digest; then assert every manifest entry exists on disk. Both directions return a non-nil error on miss/mismatch. Apply path-segment containment (validatePathSegment idea, loader.go:81-89) so a crafted path cannot escape the tree root.
    - Wire optional `--manifest` and `--tree` flags on `newVerifyLicensesCmd` (do NOT mount the command on root — preserves the `--help` subcommand count, same rule as newVerifyTOSCmd). Keep the positional audit-path arg + its default. When `--manifest`/`--tree` are supplied, run the walk; the committed-tree test + the Makefile supply them.
  </behavior>
  <implementation>
    RED→GREEN→REFACTOR, atomic commits per the TDD workflow:

    1. RED — `test(99-02): add failing dual-disposition + manifest-vs-disk + tamper tests`
       - Add a `vendorTempTree(t)` helper that materializes a good MIT+Apache tree + manifest + audit in `t.TempDir()` (mirror `writeTempAudit`, verify_licenses_test.go:11-19).
       - Add the good-tree control + every tamper test above. Run `go test ./cmd/helix-bench/... -run 'Tamper|Vendored|Fixture|Dual'` and confirm they FAIL (the new functions/flags don't exist yet).

    2. GREEN — `feat(99-02): extend verify-licenses to dual disposition + manifest-vs-disk walk`
       - Add FixtureLicense + hasTopLevelFixtureKey + the fixture-block loop + the dual-disposition assertion + the manifest-vs-disk bidirectional walk + the `--manifest`/`--tree` flags, reusing splitOnHorizontalRules (verify_tos.go:111-128) + KnownFields(true). Do NOT touch the existing TrackLicense loop's behavior (the 6 MIT blocks must still pass).
       - Run `go test ./cmd/helix-bench/...` — all new tests GREEN, all existing `*FailsClosed` + `_CommittedAuditPasses` tests still GREEN.

    3. Wire the Makefile (`chore(99-02): wire verify-licenses over the full vendored tree`):
       - Extend the `verify-licenses` recipe (Makefile:330-331) to pass `--manifest bench/datasets/aider-polyglot/VENDOR-MANIFEST.md --tree bench/datasets/aider-polyglot/fixtures` (the covered root Plan 01's manifest header declares). Update the comment block (Makefile:324-329) to describe the dual MIT+Apache-2.0 disposition + the manifest-vs-disk walk. The target is already in `.PHONY` (Makefile:1) and CI-invoked — keep it a hard-fail build gate (no continue-on-error).
       - Add a committed-tree test sibling (mirror `TestVerifyLicenses_CommittedAuditPasses`, verify_licenses_test.go:111-124) that runs the FULL gate (audit + manifest + tree) against the REAL committed Plan 01 tree and asserts nil. (Skip cleanly if the tree isn't present yet so this plan's own RED phase isn't blocked.)

    4. REFACTOR (if needed) — `refactor(99-02): tidy dual-disposition gate`. Keep all tests GREEN.

    GATE DISCIPLINE: do NOT add `internal/kernel` / `internal/semantic` imports to the gate (it stays in `cmd/helix-bench`, stdlib + cobra + yaml.v3 + crypto/sha256 only). Zero new Go deps.
  </implementation>
</feature>

<verify>
  <automated>go vet ./... && go test ./cmd/helix-bench/... && make verify-licenses</automated>
</verify>

<acceptance_criteria>
  - `go test ./cmd/helix-bench/...` passes: the good-tree control returns nil; EVERY tamper case (flipped license, dropped manifest entry, mutated byte, extra manifest entry, removed sidecar, zero Apache-2.0 blocks) returns a non-nil error.
  - `make verify-licenses` returns 0 on the committed Plan 01 tree (6 MIT track blocks + ≥1 Apache-2.0 fixture block validate; every vendored file's on-disk sha256 matches its manifest row, both directions).
  - The Apache-2.0 fixture block is strict-decoded (KnownFields(true)) into FixtureLicense; an unknown key in it fails closed.
  - Zero Apache-2.0 fixture blocks → the gate errors (dual-disposition is required, not optional).
  - All pre-existing `verify_licenses_test.go` tests (`*FailsClosed`, `_UnknownKeyFailsStrictDecode`, `_ZeroTracksIsError`, `_CommittedAuditPasses`) still pass unchanged.
  - The `verify-licenses` command is NOT mounted on root (the `--help` subcommand-count acceptance is unchanged); no new Go dependency; no `internal/kernel`/`internal/semantic` import in the gate.
</acceptance_criteria>

<done>
`make verify-licenses` hard-fails over the full mixed-license vendored tree under both MIT and Apache-2.0 dispositions with a bidirectional manifest-vs-disk sha256 walk, AND the anti-vacuity tamper test proves the gate goes RED on every sabotage class — green-only-gate is ruled out (VENDOR-03 + the milestone anti-vacuity invariant).
</done>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed vendored tree → gate | the gate treats the on-disk tree + manifest + audit as the (committed) source of truth and verifies internal consistency |
| audit/manifest markdown → strict decoder | untrusted markdown frontmatter crosses into the YAML strict decoder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02-01 | Tampering | silently swapped fixture bytes | mitigate | Bidirectional manifest-vs-disk `crypto/sha256` walk fails closed on any digest mismatch (the load-bearing tamper-detection check; proven by the mutated-byte tamper test) |
| T-99-02-02 | Tampering | manifest drift (file added/removed without a manifest update) | mitigate | The walk is bidirectional — an on-disk file with no manifest row AND a manifest row with no on-disk file both hard-fail (proven by the dropped-entry + extra-entry tamper tests) |
| T-99-02-03 | Tampering | malformed/unknown-key audit block (disposition forgery) | mitigate | `KnownFields(true)` strict decode of both TrackLicense and FixtureLicense → unknown key errors non-zero (existing engine, extended) |
| T-99-02-04 | Tampering | path traversal on the tree walk | mitigate | validatePathSegment-style containment (loader.go:81-89) on the WalkDir so a crafted path cannot escape the `--tree` root |
| T-99-02-05 | Repudiation | vacuous gate (fails open / green-only) | mitigate | The anti-vacuity tamper test asserts RED on every sabotage class + a good-tree control asserts the gate is not RED-on-everything (the v1.12 four-CRITICAL lesson) |
| T-99-02-SC | Tampering | npm/pip/cargo installs | accept | ZERO packages installed — pure additive Go using stdlib + already-present cobra/yaml.v3 (99-RESEARCH.md §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go vet ./...` clean.
- `go test ./cmd/helix-bench/...` — all new tamper tests RED on their sabotaged input, the good-tree control + all pre-existing tests GREEN.
- `make verify-licenses` returns 0 on the committed tree.
- A manual sabotage smoke (optional, the test already proves it): mutate one byte of a committed vendored fixture → `make verify-licenses` exits non-zero; revert → exits 0.
- No new Go dependency (`git diff go.mod` empty); no `internal/kernel`/`internal/semantic` import in `cmd/helix-bench/verify_licenses.go`.
</verification>

<success_criteria>
- VENDOR-03: `make verify-licenses` hard-fails over the full vendored tree under both the MIT and Apache-2.0 dispositions (dual-disposition audit + bidirectional manifest-vs-disk sha256 walk).
- Anti-vacuity (SC4): a deliberate break-the-invariant tamper test ships in this phase and goes RED on every sabotage class; a good-tree control stays GREEN.
- Reuse, not rebuild: the shipped strict-decode engine + pin.go digest discipline are extended; zero new Go deps; the command stays off the root tree.
</success_criteria>

<output>
Create `.planning/phases/99-vendored-aider-fixtures-mixed-license-gate/99-02-SUMMARY.md` when done.
</output>
