---
phase: quick-260617-j29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/runners/fairness_contract.go
  - bench/datasets/cost-table.yaml
  - bench/datasets/testdata/cost-table-stale.yaml
  - cmd/helix-bench/validate_test.go
autonomous: true
requirements: [FAIR-02]

must_haves:
  truths:
    - "DefaultContract.ModelID is the authoritative dated snapshot claude-sonnet-4-5-20250929"
    - "cost-table.yaml model_id matches DefaultContract.ModelID (per its own line-15 invariant comment)"
    - "Zero occurrences of the placeholder claude-sonnet-4-5-20260128 remain under bench/ and cmd/helix-bench/ (excluding gitignored bench/reports/)"
    - "FAIR-02 dated-snapshot invariant still holds: TestModelIDIsDatedSnapshot stays green"
    - "bench and helix-bench packages build, vet, and test clean"
  artifacts:
    - path: "bench/runners/fairness_contract.go"
      provides: "DefaultContract.ModelID pinned to authoritative dated Sonnet 4.5 snapshot"
      contains: "claude-sonnet-4-5-20250929"
    - path: "bench/datasets/cost-table.yaml"
      provides: "cost-table model_id matching the contract"
      contains: "claude-sonnet-4-5-20250929"
    - path: "bench/datasets/testdata/cost-table-stale.yaml"
      provides: "stale-gate fixture model_id matching the contract"
      contains: "claude-sonnet-4-5-20250929"
    - path: "cmd/helix-bench/validate_test.go"
      provides: "9 inline matching fixtures aligned to the contract"
      contains: "claude-sonnet-4-5-20250929"
  key_links:
    - from: "bench/datasets/cost-table.yaml"
      to: "bench/runners/fairness_contract.go DefaultContract.ModelID"
      via: "value equality enforced by validate-cost-table tests"
      pattern: "claude-sonnet-4-5-20250929"
---

<objective>
Re-pin the fairness contract's pinned benchmark model snapshot from the Phase-75
placeholder `claude-sonnet-4-5-20260128` to the authoritative dated Sonnet 4.5
snapshot `claude-sonnet-4-5-20250929` everywhere it appears in CODE / CONFIG /
TESTS.

This is a mechanical, exact string replacement at a fixed, grep-verified set of
sites. It does NOT change the model tier (stays Sonnet 4.5) and it preserves the
FAIR-02 dated-snapshot invariant (`-YYYYMMDD` suffix, never a bare alias).

Purpose: FAIR-02 requires a real DATED snapshot. The current value is a placeholder
that does not match the authoritative Sonnet 4.5 snapshot in the Claude API model
catalog. Aligning the pin removes a known-wrong identifier from the audit-fact
compile-time contract and from the cost table that must mirror it.

Output: Four edited files; zero remaining placeholder occurrences under bench/ and
cmd/helix-bench/ (excluding gitignored bench/reports/); all bench + helix-bench
tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@bench/runners/fairness_contract.go
@bench/runners/fairness_contract_test.go
@bench/datasets/cost-table.yaml
@bench/datasets/testdata/cost-table-stale.yaml
@cmd/helix-bench/validate_test.go

# Invariants to preserve (no edits, must stay green):
#  - fairness_contract_test.go TestModelIDIsDatedSnapshot: checks `-\d{8}$` suffix
#    AND that ModelID != bare alias "claude-sonnet-4-6". `-20250929` satisfies both.
#  - bench/runtime/result_test.go: asserts result model_id == DefaultContract.ModelID
#    dynamically (reads the constant, so it tracks the new value automatically).
#  - cost-table.yaml line 15 comment: `model_id` MUST match DefaultContract.ModelID.

# DO NOT TOUCH:
#  - .planning/** (historical records keep the Phase-75 value as history)
#  - cost-table.yaml `deprecation_at: 2027-01-28` (far-future; keeps the 30-day
#    deprecation gate green — leave it)
#  - fairness_contract.go line 80's reference to the bare alias "claude-sonnet-4-6"
#    (it names the forbidden anti-pattern — must remain)
#  - bench/reports/** (gitignored runtime artifacts)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder model ID with authoritative dated snapshot at all four sites (FAIR-02)</name>
  <files>bench/runners/fairness_contract.go, bench/datasets/cost-table.yaml, bench/datasets/testdata/cost-table-stale.yaml, cmd/helix-bench/validate_test.go</files>
  <action>
    Perform the exact string replacement `claude-sonnet-4-5-20260128` →
    `claude-sonnet-4-5-20250929` at these and ONLY these sites (per FAIR-02):

    1. bench/runners/fairness_contract.go
       - line 85: `ModelID: "claude-sonnet-4-5-20260128",` → `...20250929"` (the
         authoritative DefaultContract.ModelID value).
       - line 67: the doc-comment example `// DATED snapshot (FAIR-02), e.g.
         "claude-sonnet-4-5-20260128"` → `...20250929"` (keep the example in sync
         with reality).
       - DO NOT edit line 80, which references the bare alias
         "claude-sonnet-4-6" as the forbidden anti-pattern — that text must remain.

    2. bench/datasets/cost-table.yaml
       - line 19: `model_id: claude-sonnet-4-5-20260128` → `...20250929`.
       - DO NOT change `deprecation_at: 2027-01-28` (or valid_until) — the far-future
         date keeps the 30-day deprecation gate green and is intentional.

    3. bench/datasets/testdata/cost-table-stale.yaml
       - line 8: `model_id: claude-sonnet-4-5-20260128` → `...20250929`.

    4. cmd/helix-bench/validate_test.go
       - ALL 9 inline fixture occurrences `model_id: claude-sonnet-4-5-20260128`
         (lines 47, 68, 101, 113, 141, 152, 164, 176, 205) → `...20250929`. These
         are "matching" fixtures that must equal DefaultContract.ModelID; replace
         every one.

    This is a pure value/identifier substitution. Do not refactor surrounding code,
    do not touch any .planning/** file, and do not touch bench/reports/** (gitignored
    runtime artifacts). The tier stays Sonnet 4.5; only the dated suffix changes.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'claude-sonnet-4-5-20260128' bench/ cmd/helix-bench/ 2>/dev/null | grep -v '^bench/reports/')" && grep -q 'claude-sonnet-4-5-20250929' bench/runners/fairness_contract.go && grep -q 'claude-sonnet-4-5-20250929' bench/datasets/cost-table.yaml && grep -q 'claude-sonnet-4-5-20250929' bench/datasets/testdata/cost-table-stale.yaml && test "$(grep -c 'claude-sonnet-4-5-20250929' cmd/helix-bench/validate_test.go)" = "9"</automated>
  </verify>
  <done>
    No occurrences of `claude-sonnet-4-5-20260128` remain under bench/ or
    cmd/helix-bench/ (excluding gitignored bench/reports/). The authoritative
    `claude-sonnet-4-5-20250929` is present in fairness_contract.go (DefaultContract
    value + doc example), cost-table.yaml, cost-table-stale.yaml, and all 9
    validate_test.go fixtures. Line 80's bare-alias anti-pattern reference and the
    `deprecation_at` date are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build, vet, test, and formatting are all clean</name>
  <files>(no edits — verification only)</files>
  <action>
    Run the full Go quality gate for the affected packages and confirm the FAIR-02
    invariants still hold. If any check fails, fix the offending edit (the change is
    a string substitution, so a failure indicates a typo in the new ID or a missed/
    extra site) and re-run until all are clean. Do not modify test logic or any
    excluded file to make a check pass.

    Specifically confirm:
      - TestModelIDIsDatedSnapshot passes (dated `-YYYYMMDD` suffix, not bare alias).
      - The validate-cost-table tests in cmd/helix-bench pass (fixtures now match
        DefaultContract.ModelID).
      - bench/runtime/result_test.go passes (it reads DefaultContract.ModelID
        dynamically and should track the new value with no edit).
  </action>
  <verify>
    <automated>go build ./bench/... ./cmd/helix-bench/... && go vet ./bench/... ./cmd/helix-bench/... && go test ./bench/... ./cmd/helix-bench/... && test -z "$(gofmt -l bench/ cmd/helix-bench/)"</automated>
  </verify>
  <done>
    `go build`, `go vet`, and `go test` for `./bench/...` and `./cmd/helix-bench/...`
    all pass with no failures; `gofmt -l bench/ cmd/helix-bench/` prints nothing.
    TestModelIDIsDatedSnapshot, the validate-cost-table tests, and
    bench/runtime/result_test.go are green.
  </done>
</task>

</tasks>

<verification>
- grep: zero `claude-sonnet-4-5-20260128` under bench/ and cmd/helix-bench/
  (excluding gitignored bench/reports/).
- `go build ./bench/... ./cmd/helix-bench/...` — clean.
- `go vet ./bench/... ./cmd/helix-bench/...` — clean.
- `go test ./bench/... ./cmd/helix-bench/...` — all green, including
  TestModelIDIsDatedSnapshot and the validate-cost-table tests.
- `gofmt -l bench/ cmd/helix-bench/` — empty.
</verification>

<success_criteria>
- DefaultContract.ModelID == "claude-sonnet-4-5-20250929" (FAIR-02 authoritative
  dated snapshot, tier unchanged at Sonnet 4.5).
- cost-table.yaml and cost-table-stale.yaml model_id == the same value (mirror
  invariant from cost-table.yaml line-15 comment satisfied).
- All 9 validate_test.go matching fixtures == the same value.
- No placeholder occurrences remain in tracked bench/ or cmd/helix-bench/ files.
- Full Go quality gate (build + vet + test + gofmt) clean for both package trees.
- Excluded sites untouched: .planning/**, the bare-alias anti-pattern reference,
  `deprecation_at`, and bench/reports/**.
</success_criteria>

<output>
Create `.planning/quick/260617-j29-re-pin-fairness-contract-modelid-to-auth/260617-j29-SUMMARY.md` when done
</output>
