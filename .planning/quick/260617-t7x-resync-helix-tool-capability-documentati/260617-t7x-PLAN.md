---
phase: quick
plan: 260617-t7x
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/docgen/main.go
  - README.md
  - test/bench/tools_manifest_test.go
  - test/bench/main_test.go
  - test/bench/testdata/tool_descriptions.golden
  - CLAUDE.md
autonomous: true
requirements: [DOC-RESYNC]
must_haves:
  truths:
    - "go test ./test/bench/... passes (TestBenchToolsManifestMatchesRegistry and TestToolDescriptionsGoldenFile both green)"
    - "README tool table is regenerated by docgen (51 ToolProvider rows, including the 10 semantic tools) — NOT hand-edited"
    - "test/bench expected count and golden match the live registry (53 tools); CLAUDE.md prose matches"
    - "No tool registrations or runtime code changed — docs/golden/expectations moved to match the registry, never the reverse"
  artifacts:
    - path: "cmd/docgen/main.go"
      provides: "docgen blank-imports internal/skill/semantic so the README table includes the 10 semantic tools"
      contains: "skill/semantic"
    - path: "test/bench/main_test.go"
      provides: "expectedCount bumped 47 -> 53"
      contains: "expectedCount = 53"
    - path: "test/bench/tools_manifest_test.go"
      provides: "6 new benchCase entries for the new semantic tools"
      contains: "explain_symbol_deep"
    - path: "test/bench/testdata/tool_descriptions.golden"
      provides: "golden refreshed to the live 53-tool registry"
    - path: "README.md"
      provides: "regenerated tool table between BEGIN TOOLS / END TOOLS markers"
    - path: "CLAUDE.md"
      provides: "prose updated from '41+ MCP tools' to the live count"
  key_links:
    - from: "cmd/docgen/main.go"
      to: "internal/skill/semantic"
      via: "blank import triggering init() registration (parity with internal/daemon/imports.go)"
      pattern: "skill/semantic"
---

<objective>
Resync Helix tool/capability documentation and test expectations to the live MCP tool registry. The daemon registers 53 MCP tools, but the docs and bench expectations lag: README's auto-generated tool table has only 40 rows, CLAUDE.md says "41+ MCP tools", and test/bench expects exactly 47 — so `TestBenchToolsManifestMatchesRegistry` and `TestToolDescriptionsGoldenFile` fail.

Purpose: Make docs and test expectations match the live registry so `go test ./test/bench/...` is green and the README table truthfully reflects every registered tool. This is a docs/golden/expectation resync ONLY — no tool registrations or runtime code change.

Output: docgen imports the semantic skill (root-cause fix for the README lag), README table regenerated, bench expected count bumped to 53, 6 new benchCase entries added, golden refreshed, CLAUDE.md prose updated.
</objective>

<investigation_findings>
All numbers below were DERIVED from the live registry at plan time (not assumed from the failure message):

- **Authoritative live registry count: 53 tools** (from `benchDaemon.RegistryNames()`,
  the source of truth the failing tests and the golden enumerate). This 53 INCLUDES the
  3 built-ins `ping`, `echo`, `activate_project` (registered directly in
  `internal/mcp/server.go`, not via any ToolProvider). Note: the README *table* is a
  separate view of `skill.ToolProviders()` and reads 51 rows after the fix — it excludes
  the 3 built-ins and includes `analyze_blast_radius` (a pre-existing ToolProvider/registry
  view difference, unrelated to this resync). The 53 count is what the tests assert.
- **Root cause of the README lag:** `cmd/docgen/main.go` is missing the
  `_ "github.com/agenthands/helix/internal/skill/semantic"` blank import that the daemon
  has in `internal/daemon/imports.go:13`. Without it, `skill.ToolProviders()` never sees
  the semantic provider, so docgen emits only the non-semantic tools (currently 40 rows).
- **The semantic provider registers 10 tools.** Four of them
  (`get_semantic_context`, `get_semantic_graph_status`, `index_semantic_graph`,
  `refresh_semantic_graph`) are ALREADY in `benchTools` (the "Semantic graph (4)" block).
  The other **6 are new and missing from the manifest and golden**:
  `explain_cluster`, `explain_symbol_deep`, `find_related_symbols`,
  `get_change_impact_graph`, `get_cluster_map`, `validate_graph_edge`.
- **Where the expected constant lives:** `test/bench/main_test.go:83` —
  `const expectedCount = 47`. The test asserts BOTH `len(benchTools) == expectedCount`
  AND `len(liveRegistry) == expectedCount`, plus bidirectional NAME parity.
- **The manifest parity test compares NAMES only — it never invokes the tools.**
  The `args` maps in `benchTools` are consumed by the `Benchmark*` runners, not by the
  parity test. So the 6 new `benchCase` entries only need correct `name` fields to make the
  test green; minimal sensible `args` are fine.
- **Docgen mechanism confirmed:** `make docs` → `go run ./cmd/docgen` rewrites the README
  tool table in place between `<!-- BEGIN TOOLS -->` / `<!-- END TOOLS -->`
  (README.md lines 279/323) and language table between `<!-- BEGIN LANGUAGES -->` /
  `<!-- END LANGUAGES -->`. It reads `skill.ToolProviders()` live.
- **Golden refresh mechanism confirmed:** the `-update` flag exists
  (`var updateGolden = flag.Bool("update", ...)` in `tools_descriptions_test.go`); run
  `go test ./test/bench/... -run TestToolDescriptionsGoldenFile -update`.
- **CLAUDE.md stale prose:** line 33 ("Helix provides 41+ MCP tools ...") and line 146
  ("Full tool inventory (41+ callable tools ...)").

Expected post-fix arithmetic:
- README ToolProvider table: 41 -> **51 rows** (41 current + 10 semantic). Built-ins are
  NOT ToolProviders and are correctly excluded from the README table — do not try to force
  the README table to 53.
- bench `expectedCount`: 47 -> **53**. `benchTools`: 47 -> **53 entries** (6 added).
  Golden: 47 -> **53 lines**.
</investigation_findings>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@cmd/docgen/main.go
@test/bench/main_test.go
@test/bench/tools_manifest_test.go
@test/bench/tools_descriptions_test.go
@internal/daemon/imports.go
</context>

<scope_guard>
This is a docs/golden/test-expectation resync ONLY. Do NOT change any tool registrations
or runtime code (no edits under internal/skill/semantic/, internal/mcp/, internal/kernel/,
internal/daemon/ EXCEPT the one blank import line in cmd/docgen/main.go, which lives in
cmd/, not a runtime package). If the live count differs from any doc, the docs and
expectations move to match the registry — NEVER the reverse. The only "code" edit permitted
is adding a blank import to cmd/docgen/main.go (a doc-generation tool, not the daemon).
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Fix docgen import + regenerate README table + bump bench manifest and expected count</name>
  <files>cmd/docgen/main.go, README.md, test/bench/main_test.go, test/bench/tools_manifest_test.go</files>
  <action>
Root-cause fix and the two source edits the golden depends on:

1. In cmd/docgen/main.go, add a blank import for the semantic skill alongside the other
   skill blank imports (the block at lines 22-31), matching the daemon's
   internal/daemon/imports.go:13 entry exactly:
   `_ "github.com/agenthands/helix/internal/skill/semantic"`
   Keep the import grouping/ordering consistent with the existing block. (Optionally also
   add `_ "github.com/agenthands/helix/internal/skill/guardrails"` for full parity with
   imports.go — harmless since guardrails.Tools() returns nil and adds no table rows — but
   it is NOT required for any test to pass; semantic is the load-bearing one.)

2. Regenerate the README tables IN PLACE via the generator (do NOT hand-edit the table):
   run `go run ./cmd/docgen`. This rewrites the rows between the `<!-- BEGIN TOOLS -->` /
   `<!-- END TOOLS -->` markers. After regeneration the tool table grows from 41 to 51
   ToolProvider rows (the 10 semantic tools now appear). Built-ins ping/echo/activate_project
   remain excluded from the table by design (they are not ToolProviders) — this is correct.

3. In test/bench/main_test.go, bump the expected count constant from 47 to 53:
   `const expectedCount = 53` (line ~83). This is a hand-edit of a test expectation and is
   permitted (per the plan constraints — bumping the bench expected count is fine).

4. In test/bench/tools_manifest_test.go, add 6 new `benchCase` entries to the `benchTools`
   slice for the new semantic tools so the manifest matches the live registry name-for-name
   (the parity test requires bidirectional NAME parity, not just count). Add them in a new
   block after the existing "Semantic graph (4, Phase 64)" block, e.g. a
   "Semantic deep-context (6, Phase 65)" comment block. The parity test compares NAMES only
   and never invokes these via the manifest, so minimal sensible args matching each tool's
   schema are sufficient. Add entries with these exact names (args shown are minimal/safe):
     - get_cluster_map         args: {} (all fields optional; defaults projection=weak_components, top_n=20)
     - explain_cluster         args: {"cluster_id": "c0"} (opaque token; bench-only, never asserted)
     - explain_symbol_deep     args: {"seed": {"file_path": "main.go", "symbol_name": "Helper"}}
     - find_related_symbols    args: {"seed": {"file_path": "main.go", "symbol_name": "Helper"}}
     - get_change_impact_graph args: {"seed": {"file_path": "main.go", "symbol_name": "Helper"}}
     - validate_graph_edge     args: {} (bench-only; never invoked by the parity test)
   Match the existing benchCase struct literal style in the file. Do NOT set needsCopy (these
   are read-only graph queries). Also update the count/breakdown prose in the file's header
   comment (the "43-tool" / "MUST equal 43" / breakdown lines near the top) to reflect the new
   total — the header comment is prose, not asserted by any test, but keep it honest: the
   manifest now has 53 entries.
  </action>
  <verify>
    <automated>go run ./cmd/docgen --check && go test ./test/bench/... -run TestBenchToolsManifestMatchesRegistry -count=1</automated>
  </verify>
  <done>
`go run ./cmd/docgen --check` prints "README.md is up to date." (table regenerated, no
pending drift). `TestBenchToolsManifestMatchesRegistry` passes: len(benchTools)==53,
live registry==53, and bidirectional name parity holds (no manifestOnly, no registryOnly).
README tool table between the markers has 51 `| \`tool\` |` rows including the 6 new
semantic tools.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh the descriptions golden + update CLAUDE.md prose to the live count</name>
  <files>test/bench/testdata/tool_descriptions.golden, CLAUDE.md</files>
  <action>
With Task 1's docgen/import fix in place (so the registry the test enumerates is the full
53-tool set), refresh the golden and update prose:

1. Regenerate the descriptions golden from the live registry using the confirmed `-update`
   flag: run `go test ./test/bench/... -run TestToolDescriptionsGoldenFile -update -count=1`.
   This rewrites test/bench/testdata/tool_descriptions.golden to the sorted 53-line
   "name: brief description" snapshot, adding the 6 new semantic tools' brief descriptions.
   Inspect `git diff test/bench/testdata/tool_descriptions.golden` and confirm the ONLY
   additions are the 6 new tool lines (explain_cluster, explain_symbol_deep,
   find_related_symbols, get_change_impact_graph, get_cluster_map, validate_graph_edge) —
   no existing description should change (if one did, that would indicate an unrelated drift
   to investigate, not silently accept).

2. Update CLAUDE.md prose to the live count (these ARE permitted hand-edits — CLAUDE.md prose
   is not the auto-generated README table):
     - Line ~33: "Helix provides 41+ MCP tools for semantic code retrieval, ..." ->
       "Helix provides 53 MCP tools for semantic code retrieval, ..."
     - Line ~146: "Full tool inventory (41+ callable tools with profile/mode matrix) is
       auto-generated in `README.md`; do not hand-edit the tool table" ->
       "Full tool inventory (53 callable tools with profile/mode matrix) is auto-generated
       in `README.md`; do not hand-edit the tool table"
   Use 53 (the full live registry count, including the 3 built-ins) for the prose claim —
   the prose describes the whole MCP surface, not just the README ToolProvider table.
   Do NOT touch the auto-generated README tool table in this step.
  </action>
  <verify>
    <automated>go test ./test/bench/... -count=1 && grep -q "53 MCP tools" CLAUDE.md && grep -c "^[a-z_]*:" test/bench/testdata/tool_descriptions.golden</automated>
  </verify>
  <done>
`go test ./test/bench/...` passes in full (both previously-failing tests green:
TestBenchToolsManifestMatchesRegistry and TestToolDescriptionsGoldenFile). The golden has
53 entries and `git diff` on it shows ONLY the 6 new semantic-tool lines added. CLAUDE.md
no longer says "41+ MCP tools" — both line 33 and line 146 say 53. No runtime tool
registration was changed.
  </done>
</task>

</tasks>

<verification>
Overall acceptance for the task set:

1. `go test ./test/bench/...` passes — both previously-failing tests
   (`TestBenchToolsManifestMatchesRegistry`, `TestToolDescriptionsGoldenFile`) are green.
2. `go run ./cmd/docgen --check` prints "README.md is up to date." (the table was
   regenerated by docgen, not hand-edited).
3. `git diff` shows:
   - cmd/docgen/main.go: one added blank import (`internal/skill/semantic`).
   - README.md: tool table grew from 41 to 51 rows (the 6 new semantic tools + the 4
     pre-existing semantic-graph tools now present), all between the BEGIN/END TOOLS markers.
   - test/bench/main_test.go: `expectedCount` 47 -> 53.
   - test/bench/tools_manifest_test.go: 6 new benchCase entries + updated header comment.
   - test/bench/testdata/tool_descriptions.golden: 6 new lines (now 53 total).
   - CLAUDE.md: "41+ MCP tools" -> "53 MCP tools" at lines 33 and 146.
4. NO changes to any tool registration or runtime package (internal/skill/semantic/**,
   internal/mcp/**, internal/kernel/**, internal/daemon/**). The only cmd/-tree edit is the
   docgen blank import.
5. Run `go vet ./...` to confirm the new import and manifest entries compile cleanly
   (per CLAUDE.md: always vet before completing a Go task).
</verification>

<success_criteria>
- `go test ./test/bench/...` is fully green.
- README tool table is regenerated by docgen (51 ToolProvider rows) and `--check` is clean.
- bench expected count, manifest entries, and golden all match the live 53-tool registry.
- CLAUDE.md prose says 53 MCP tools (no remaining "41+" tool-count claims).
- Zero runtime/registration changes — docs and expectations moved to the registry, not vice versa.
</success_criteria>

<output>
Create `.planning/quick/260617-t7x-SUMMARY.md` when done.
</output>
