# v1.10 Cross-Phase Integration Check (Phases 57–62)

Date: 2026-05-07
Auditor: cross-phase integration agent (read-only, /gsd-audit-milestone subagent)
Scope: Phases 57 (semantic store + DAG library), 58 (release + verifier + forwarder OTel), 59 (tree-sitter extraction), 59.1 (CGO=0 → CGO=1 split-runner), 60 (live update), 61 (LSP enrichment worker), 62 (graph engine + ranking + type resolution).

Supersedes the 2026-05-04 version of this file (Phases 57–59 only). Per-phase VERIFICATIONs are NOT re-run here; this audit asks the question those verifications cannot answer by definition: do the seams between phases actually carry data in production?

---

## Status: GAPS_FOUND (1 BLOCKER, 1 HIGH, 3 MEDIUM, 2 LOW)

| Severity | Count | Finding |
|---|---|---|
| BLOCKER | 1 | None against shipping daemon today; release-pipeline's BLOCKER from 2026-05-04 was retired by Phase 59.1's split-runner CGO=1 cutover (see "Closed since 2026-05-04"). |
| HIGH | 1 | F-01 — Phase 62 ApplyRepair runtime-disconnected: production has zero `FileFactDiffRecorder` populators, so the rank engine's post-commit hook short-circuits on every live edit. |
| MEDIUM | 3 | F-02 — `rankStoreAdapter` 4 read methods are stubs (Phase 64 follow-up). F-03 — three vet analyzers are NOT in CI. F-04 — `typeResolver` dispatcher has no in-process consumer (Phase 64 attaches one). |
| LOW | 2 | F-05 — lazy-init activation still skips `ScheduleInitialExtraction`. F-06 — `helix upgrade` daemon-detect short-circuit not exercised in CI smoke. |

`go vet ./internal/... ./cmd/... ./api/... ./protocol/...` exits 0.
`go build ./internal/... ./cmd/...` exits 0.
All three custom analyzers (`vet-noduckdb`, `vet-nokernel2semantic`, `vet-nosemantic2kernel`) pass when scoped to source dirs.

---

## Closed since 2026-05-04 baseline

| 2026-05-04 finding | Closure | Evidence |
|---|---|---|
| BLOCKER 1 — CGO=0 build broken by Phase 59 (`provider.go:31` `tree_sitter.Language` undefined) | Closed by Phase 59.1 (INT-BLOCKER-01). Source tree is single-mode CGO=1; the CGO=0 stub apparatus was removed entirely. Release pipeline split-runner architecture absorbs the cross-compile via zig + Apple clang. | `CLAUDE.md` "Build pipeline" block; `internal/semantic/store/duckdb.go` is the only remaining `//go:build` constraint and it is platform-conditional (`!(windows && arm64)`), not CGO-conditional. |
| WARNING 1 — Lazy-init does not kick `ScheduleInitialExtraction` | Still open. Re-flagged below as F-05 with reduced severity (LOW) because RequireReady cold-fallback covers it for live consumers. | `internal/daemon/daemon.go:565-582` lazyActivateFn body has no `semanticScheduler.ScheduleInitialExtraction` call; the explicit `activate_project` callback at lines 608-616 does. |

---

## Wiring Summary (verified end-to-end where annotated)

| Connection | Status | Evidence |
|---|---|---|
| **P57 → daemon bootstrap.** `cfg.SemanticIndex.Enabled` gates store + extract registry + scheduler with one config knob. | WIRED | `internal/daemon/daemon.go:268, 298` — same predicate guards `semanticstore.Open` AND the extract-registry/scheduler block. Step numbering (6, 6b, 6c, 6d, 6e, 6f, 6g) annotated in source. |
| **P57 GrammarRegistry singleton invariant (BUG-04 / EXTRACT-05).** | WIRED | `daemon.go:287` constructs once; `daemon.go:288, 299-303, 496` injects the same pointer into BodyExtractor + extract.Registry + 3 providers + repomap skill. Static enforcement: `internal/semantic/extract/registry_grep_test.go`. |
| **P58 forwarder ↔ daemon OTel propagation.** Same `obs.{Server,Client}StatsHandler` on both ends. | WIRED | `daemon.go:836` server side; `forwarder/dial.go` (per CLAUDE.md) client side; centralised in `internal/obs/grpc.go`. |
| **P58 `helix upgrade` cobra wiring.** | WIRED | `internal/cli/root.go:82` `rootCmd.AddCommand(newUpgradeCommand())`; `internal/cli/upgrade.go:24` Use="upgrade"; verifier callable: `internal/upgrade/upgrade.go:246-265` invokes `VerifyArchive` with sigstore-go bundle. |
| **P59 → P60 EditNotifier seam.** Kernel exposes `SetEditNotifier(liveService)`; live wiring installs it. | WIRED | `daemon.go:324-339` calls `buildLiveBundle`; `internal/daemon/live_wiring.go:265` calls `k.SetEditNotifier(liveService)`. Regression test: `internal/daemon/live_e2e_test.go:114-115` asserts `k.EditNotifier()` is non-nil after `buildLiveBundle`. |
| **P60 postEditHook coverage.** All 9 callsites named in Phase 60 SC4. | WIRED | `internal/kernel/edit/tools.go:383, 452, 521, 588, 670` (5 edit tools — replace_symbol_body, insert_after, insert_before, rename_symbol, replace_lines) + `internal/kernel/fileops/tools.go:258, 440, 456, 510` (4 fileops — create_file, replace_in_file new path, replace_in_file old path, fuzzy_edit). All call `k.EditNotifier()` non-nil-guarded → `OnEdit(ctx, wsKey, paths)`. |
| **P60 → P61 LaneEnqueuer chain.** Live handler enqueues into the lspenrich queue. | WIRED | `internal/semantic/live/handler/handler.go:422-427` calls `h.LSPQueue.EnqueueLane(...)` with a per-kind lane selection. |
| **P61 LSP enrichment worker — production CascadeLSP wiring (61-05 gap closure).** | WIRED | `internal/semantic/lspenrich/cascade_lsp_shim.go:85` `NewCascadeLSPShim(lease *lspool.WorkerLease)` is the production constructor; cited from `live_wiring.go:354` as the production factory. The shim adapts the kernel's `*lspool.WorkerLease` into the `lspenrich.CascadeLSP` interface, satisfying the kernel⊥semantic boundary by going through the carve-out package. |
| **P62 rank engine post-commit hook installation.** | WIRED (compile) | `daemon.go:364` calls `live.handler.SetRankApplier(rank.engine)` when both live and rank bundles exist. The handler's production code path at `handler.go:393-421` checks `h.rankApplier != nil`, branches on `recorder.IsEmpty()`, and only fires `ApplyRepair` when the recorder is non-empty. |
| **P62 rank engine errgroup ownership.** | WIRED | `daemon.go:773-777` `g.Go(func() error { return d.rank.Run(gctx) })` — rank fan-out goroutine attached to the daemon's top-level errgroup; per-workspace `RankScheduler.Run` goroutines started lazily under the captured `runCtx` in `rank_wiring.go:154-185`. Kernel-first shutdown invariant is preserved because all goroutines exit on `ctx.Done`. |
| **Kernel⊥semantic boundary (Phases 57+60+61).** Three vet analyzers and `go vet` is clean. | WIRED in source, PARTIAL in CI | All three analyzers pass when scoped to `./internal/... ./cmd/... ./api/... ./protocol/...`. CI gap is F-03 below. |
| **Phase 62 `typeResolver` dispatcher → graph consumer.** | WIRED (compile) / DISCONNECTED (runtime) | `daemon.go:394-403` constructs the 7-language dispatcher; `daemon.go:657` retains it on the daemon. `Daemon.TypeResolver()` accessor at `type_resolver_wiring.go:131` has no in-process caller. By design — Phase 64 will attach the MCP-tool consumer. |
| **Phase 60 → Phase 62 `FileFactDiff` post-commit pipeline.** | WIRED (compile) / DISCONNECTED (runtime) | See F-01 below. The `Set*RankApplier` is wired and the recorder type exists, but the production populator is empty. |

---

## Findings

### F-01 (HIGH) — Phase 62 ApplyRepair short-circuits on every production live edit

**From:** `internal/semantic/live/handler/handler.go:376-421` (`updateChangedFileWithKind`)
**To:** `internal/daemon/rank_wiring.go:101-110` (`rankBundle.engine`) → `internal/semantic/graph.Engine.ApplyRepair`
**Affected REQ-IDs:** GRAPH-01, GRAPH-02, LIVE-04 (live → graph delta application), and indirectly every Phase 64+ MCP tool that reads `semantic_graph_scores`

**Evidence:**
- `handler.go:376` constructs `recorder := &FileFactDiffRecorder{}` per tx span.
- `handler.go:377` checks `h.populateRecorderForTest != nil` — but the only writer of that field is `SetPopulateRecorderForTest` in `internal/semantic/live/handler/export_test.go` (test-only `_test.go` file).
- `handler.go:393-406` then takes the empty-diff branch on every production call: `recorder.IsEmpty() == true` → emits the once-INFO log per `(repoID, Handler instance)` via `emptyDiffOnce` and SKIPS `ApplyRepair`.
- Production grep for `RecordSymbol{Added,Removed,Changed}` and `RecordEdge{Added,Removed}` returns ZERO hits outside `_test.go` files. The recorder methods exist but nothing calls them in the live tx path.

**Severity rationale (HIGH not BLOCKER):**
The 62-09 closure that landed 2026-05-07 added the seam, the recorder, and the once-INFO log specifically to make this gap *visible*. The system therefore behaves as documented: ApplyRepair short-circuits, scheduler.Notify is never called from the post-commit path, `semantic_graph_scores` rows for live-edited files stay at the version they were on extraction. This is not a regression of any past behaviour. But it does mean Phase 62's "live edits advance graph_version" headline is FALSE for production today — it is true only under tests that install `SetPopulateRecorderForTest`. Operators monitoring the once-INFO log will see one entry per workspace; nothing else surfaces this gap until Phase 60 P04 lands.

**Recommended action:**
1. ROADMAP.md already tracks this as a Phase 60 P04 dependency. Confirm it stays on the v1.11 critical path.
2. Add a smoke assertion to the v1.10 release-smoke (or a daemon E2E test) that drives a `replace_symbol_body` against a fixture, then asserts the once-INFO log fired and `current_graph_version` did NOT advance — i.e. assert the *known-degraded* state explicitly so a future fix doesn't silently regress observability.
3. Surface the once-INFO log into a counter (e.g. `helix_semantic_live_apply_repair_skipped_total{reason="empty_diff"}`) so operators see a non-zero rate proportional to live edits, parallel to the `stub_no_data` harness in F-02. Today the only signal is a single INFO log per workspace per process, which can be missed in `LOG_LEVEL=warn`.

### F-02 (MEDIUM) — `rankStoreAdapter` 4 read methods are Phase-64-deferred stubs

**From:** `internal/daemon/rank_wiring.go:284-356`
**To:** `internal/semantic/store` typed effective-graph queries (Phase 64)
**Affected REQ-IDs:** GRAPH-03, GRAPH-05 (effective-graph reads), all PageRank scheduling against real data

**Evidence:**
- `QueryEffectiveGraph`, `QueryEffectiveAdjacency`, `CountStaleScoreRows`, `MarkAllScoreRowsStale` all return empty/zero values (`rank_wiring.go:331-357`).
- 62-08 closure added `stubObserve.emit(repoID, method)` to each → fires `SemanticGraphRepairInc("stub_no_data")` AND a once-WARN log. The `"stub_no_data"` label is a real registered enum value (`internal/obs/metrics.go:720`) and the family is scanned by the metrics-labels test (`metrics_labels_test.go:191`).
- These observability hooks fire ONLY when something actually calls the stub methods — i.e. when a per-workspace `RankScheduler` fires its incremental path after a `GraphVersionAdvance` event. Per F-01 above, `GraphVersionAdvance` is never emitted in production today, so the stub-no-data counter will also stay at zero in production.

**Severity rationale (MEDIUM not HIGH):**
This is the documented Phase-64 follow-up surface. The four stubs return zero-value, error-free responses so the scheduler does not crash. The only damage is that PageRank scores against the real effective graph are never computed; rank-aware MCP tools (Phase 64 surface) will see empty score rows. This was always a Phase 64 deliverable.

**Recommended action:**
1. Document explicitly in `v1.10-MILESTONE-AUDIT.md`'s "Known limitations shipped to v1.10" section.
2. The combination of F-01 + F-02 means BOTH halves of the rank pipeline are runtime-disconnected; neither half exercises the other. Treat F-01 closure (any populator) as the precondition for F-02 closure being verifiable.

### F-03 (MEDIUM) — Three vet analyzers are NOT in CI

**From:** `cmd/vet-noduckdb`, `cmd/vet-nokernel2semantic`, `cmd/vet-nosemantic2kernel`
**To:** the kernel⊥semantic and noduckdb-leak boundaries (foundational across Phases 57, 60, 61)
**Affected REQ-IDs:** STORE-06 (CGO=0 stub principle, now reframed as DuckDB import boundary), ENRICH-01 (kernel⊥semantic invariant), the entire Phase 57→60→61 layering invariant

**Evidence:**
- `Makefile:28-32` `vet:` target runs all three analyzers locally.
- `.github/workflows/go-test.yml:93-94` runs ONLY `go vet ./...` — none of the three custom singlecheckers are invoked. Searched all `.github/workflows/*.yml` for `vet-noduckdb`, `vet-nokernel2semantic`, `vet-nosemantic2kernel`: zero matches.
- Today the analyzers are local-only enforcement. A drive-by import that violates any of the three boundaries will pass CI green and only break the next developer who runs `make vet`.

**Recommended action:**
Add a CI step to `go-test.yml` that does the equivalent of `make vet` (install the three vettool binaries via `go install`, then run each `go vet -vettool=<bin> ./...`). The work is mechanical and the analyzers already pass — this is purely closing the CI loop.

### F-04 (MEDIUM) — `typeResolver` dispatcher has no in-process consumer

**From:** `internal/daemon/daemon.go:394-403, 657` (constructed and held)
**To:** *no consumer in the binary today*
**Affected REQ-IDs:** TYPES-01, TYPES-02, TYPES-03, TYPES-04 — the type-resolution surface itself

**Evidence:**
- `Daemon.TypeResolver()` accessor at `type_resolver_wiring.go:131` returns the dispatcher.
- `grep -rn 'TypeResolver(' internal/ --include='*.go'` (excluding `_test.go` and `internal/semantic/types/`) returns zero hits outside the constructor in `daemon.go` itself. No MCP tool, no skill, no kernel subsystem reads it.
- `SetSemanticGraph(ranker any, resolver types.Resolver)` at `type_resolver_wiring.go:118` is the documented Phase 64 attach point — it has zero callers today.

**Severity rationale (MEDIUM):**
This is documented as deferred ("Phase 64 will attach the MCP-tool consumer"), and the per-language constructors all light up at bootstrap (logged at `daemon.go:404-409`). The 7 resolver implementations exist and unit-test against fixtures. But "zero in-process consumers" means TYPES-01..04 are WIRED at compile time and DISCONNECTED at runtime — none of the type-resolution code paths are exercised by the running daemon today.

**Recommended action:**
Same as F-02: document explicitly under v1.10 "ships unwired pending Phase 64". Add a startup INFO log warning the operator that `type_resolver_attached=false` until SetSemanticGraph is called, parallel to F-01's once-INFO surfacing.

### F-05 (LOW) — Lazy-init activation still skips `ScheduleInitialExtraction`

**From:** `internal/daemon/daemon.go:565-582` (`lazyActivateFn`)
**To:** `internal/semantic/scheduler.Scheduler.ScheduleInitialExtraction`
**Affected REQ-IDs:** EXTRACT-01

**Evidence:**
Carry-over from the 2026-05-04 audit. The explicit `activate_project` MCP callback at `daemon.go:608-616` correctly calls `semanticScheduler.ScheduleInitialExtraction`; the lazy-init middleware path at `daemon.go:565-582` does not. Mitigated by `RequireReady`'s cold-start kick (`internal/semantic/scheduler/ready.go:109` per the 2026-05-04 cite).

**Severity rationale (LOW, downgraded from WARNING):**
Production traffic that uses lazy-init (i.e. clients that send a tool call before `activate_project`) trigger extraction via `RequireReady` on first read of semantic data. The cold-start delay is observable but not data-corrupting.

**Recommended action:**
Three-line fix mirroring the explicit-activation block. Defer to next phase that touches `daemon.go` activation wiring.

### F-06 (LOW) — `helix upgrade` daemon-detect short-circuit not exercised in CI smoke

**From:** `internal/upgrade/daemon_detect.go` (RunningInDaemon)
**To:** `helix upgrade` cobra entrypoint
**Affected REQ-IDs:** REL-05

**Evidence:**
The `HELIX_RUNNING_AS_DAEMON=1` env var is set at `daemon.go:737`; `internal/upgrade/daemon_detect.go` reads it. The release-smoke target in `Makefile:77-157` boots the daemon and hits MCP `tools/list`, but does NOT shell into the running daemon's process tree to invoke `helix upgrade` and verify the short-circuit message. CONTEXT.md D-08 (T-52-04-08) documents this as a high-impact safety mechanism but there is no integration test for the seam.

**Recommended action:**
Add a smoke-test variant that runs `helix upgrade` from inside the daemon's exec'd shell to assert the short-circuit. Low priority — the failure mode is highly visible (daemon binary swap under itself) and the env-var contract is straightforward.

---

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|---|---|---|---|
| STORE-01 (DuckDB three-tier open) | `daemon.go:269-281` → `semanticstore.Open` → migrations 0→1→2 | WIRED | — |
| STORE-02 (snapshot/effective views) | P57 internal | WIRED | self-contained |
| STORE-03 (forward-incompat refusal) | `migrations_registry_cgo.go` `ErrForwardIncompatible` | WIRED | — |
| STORE-04 (config keys / defaults) | `internal/config/defaults.go` → `SerenaConfig.SemanticIndex` → `daemon.go:268` | WIRED | — |
| STORE-05 (metrics) | `internal/obs/metrics.go` SemanticStore counters → `duckdb.go` callsites | WIRED | — |
| STORE-06 (CGO=0 stub + vet-noduckdb) | `cmd/vet-noduckdb/main.go` + `internal/lint/noduckdb/analyzer.go` | WIRED locally | F-03 (analyzer not in CI). 2026-05-04 transitive break is closed by Phase 59.1. |
| DAG-01..04 (`internal/phasegraph/`) | Library; `daemon.go:205` carries the v1.11 migration TODO | WIRED, no consumer | self-contained until v1.11 |
| REL-01 (cosign keyless signing) | `.goreleaser.yaml` + `.github/workflows/release.yml` (split-runner D-15) | WIRED | unblocked by 59.1 split-runner |
| REL-05 (verifier rewrite, sigstore-go) | `internal/upgrade/verify.go` → `internal/upgrade/upgrade.go:246-265` `VerifyArchive` | WIRED | — |
| REL-06 (forwarder span unification) | `internal/obs/grpc.go` + `daemon.go:836` + `forwarder/dial.go` | WIRED | — |
| EXTRACT-01 (extractor registry + scheduler) | `daemon.go:296-314` | WIRED | F-05 (lazy-init path). |
| EXTRACT-02 (per-language providers Go/TS/Python) | `daemon.go:301-303` + Languages() count check | WIRED | — |
| EXTRACT-03 (stable IDs / partial extraction) | schema v2 columns + `extract/stable_id.go` | WIRED | — |
| EXTRACT-04 (schema v1→v2 migration) | `migrations_registry_cgo.go:31` `applyMigration002` | WIRED | — |
| EXTRACT-05 (singleton GrammarRegistry invariant) | `daemon.go:287-303, 496` + `registry_grep_test.go` | WIRED | — |
| INT-BLOCKER-01 (CGO=1 split-runner cutover) | `CLAUDE.md` Build pipeline; release CI split-runner; source tree CGO=1 single-mode | WIRED | — |
| LIVE-01..03 (fsnotify watcher → coalescer → handler chain) | `internal/semantic/live/{watcher,coalescer,handler}` + `live_wiring.go:193-265` | WIRED | — |
| LIVE-04 (live → graph delta application) | `handler.go:393-421` recorder branch + rankApplier hook | WIRED (compile) / DISCONNECTED (runtime) | F-01 — empty recorder. |
| LIVE-05 (postEditHook 9 callsites) | `kernel/edit/tools.go` 5 sites + `kernel/fileops/tools.go` 4 sites | WIRED | — |
| LIVE-06 (overlay_epoch advance under live edit) | `live_e2e_test.go:158` regression | WIRED | — |
| LIVE-07 (overlay rows persisted) | `live_e2e_test.go:165` regression | WIRED | — |
| ENRICH-01 (kernel⊥semantic boundary) | three vet analyzers | WIRED locally | F-03 (CI gap). |
| ENRICH-02..04 (queue, lanes, worker) | `internal/semantic/lspenrich/{queue,worker,manager}` + `live_wiring.go:354` factory | WIRED | — |
| ENRICH-05 (production CascadeLSP) | `internal/semantic/lspenrich/cascade_lsp_shim.go:85` `NewCascadeLSPShim` (61-05 gap closure) | WIRED | — |
| GRAPH-01 (graph engine) | `internal/semantic/graph.Engine` + `daemon.go:359-373` | WIRED (compile) / DISCONNECTED (runtime) | F-01 — empty FileFactDiff. |
| GRAPH-02 (post-commit hook) | `live.handler.SetRankApplier(rank.engine)` `daemon.go:364` | WIRED (compile) / DISCONNECTED (runtime) | F-01. |
| GRAPH-03 (rank scheduler per workspace) | `rank_wiring.go:154-185` ensureScheduler under SetActivateCallback `daemon.go:629-631` | WIRED | scheduler runs against empty effective graph (F-02). |
| GRAPH-04 (errgroup ownership) | `daemon.go:773-777` `g.Go(func() error { return d.rank.Run(gctx) })` | WIRED | — |
| GRAPH-05 (effective graph reads) | `rankStoreAdapter` 4 read methods | WIRED (compile) / DISCONNECTED (runtime) | F-02 — Phase 64 stubs. |
| GRAPH-06 (stub_no_data observability) | 62-08 closure: `obs/metrics.go:720, 765` + `rank_wiring.go:240-269` once-WARN harness | WIRED | fires only when scheduler runs, blocked by F-01. |
| TYPES-01..04 (7-language dispatcher) | `daemon.go:390-410` | WIRED (compile) / DISCONNECTED (runtime) | F-04 — no in-process consumer until Phase 64. |

**Self-contained (no cross-phase wiring required):** STORE-02, DAG-01..04.

**Stale traceability:** `REQUIREMENTS.md` traceability table is stale for everything except LIVE-01..07. Process gap, not coverage gap. Recommend the milestone audit retrace and update marks for STORE-*, REL-*, EXTRACT-*, ENRICH-*, GRAPH-*, TYPES-* (all are at minimum WIRED-compile per this audit).

---

## E2E Flow Status

| Flow | Status | Notes |
|---|---|---|
| Daemon bootstrap (CGO=1, semantic enabled, live enabled, rank enabled) | OK | `go build ./internal/... ./cmd/...` clean; bootstrap step ordering 1 → 6b → 6 → 6c → 6d → 6e → 6f → 6g → 7+ → errgroup attach. Kernel-first shutdown invariant preserved. |
| Daemon bootstrap (semantic disabled) | OK | All four pointers (`semanticStore`, `semanticScheduler`, `live`, `rank`, `typeResolver`) stay nil; nil guards at `daemon.go:333, 350, 391, 608, 629, 761, 773` plus `live.startWorkspace` is nil-safe. v1.9 tools unaffected. |
| `activate_project` → initial extraction | OK | `daemon.go:608-616` |
| First-tool lazy activation → initial extraction | DEGRADED (LOW) | F-05; covered by RequireReady cold-fallback. |
| Forwarder → daemon trace span continuity | OK | REL-06 unaffected by P59-62. |
| `helix upgrade` cosign verify | OK | `internal/upgrade/upgrade.go:260` `VerifyArchive` reachable; v1.10.0-rc1 verify passed in 58-VERIFICATION (run 25310013651). |
| **Live edit → overlay tx → graph_version advance** (the headline P62 flow) | DISCONNECTED in production | Edit → kernel.OnEdit → live.handler.UpsertOverlayFile → COMMIT → recorder.IsEmpty()==true → emptyDiffOnce → SKIP ApplyRepair → enqueue LSP lane → done. graph_version is NOT advanced under any production live edit. Test-only path with `SetPopulateRecorderForTest` covers the populated case. (F-01) |
| **Live edit → graph_version advance → RankScheduler.Notify → effective-graph reads → score upserts** (the headline P62 flow, second leg) | DISCONNECTED | Reachable only if F-01 is fixed. The four `rankStoreAdapter` read methods would then return empty → scheduler short-circuits → no scores written. (F-02) |
| Workspace deactivation → enrichment lease release (Phase 61 P03 B2) | WIRED | `daemon.go:1064-1066` `live.OnWorkspaceDeactivate` from gRPC handler. |
| go vet (all three custom analyzers) on source tree | OK locally | F-03: not in CI. |

---

## What this audit did NOT verify (transparency)

- Per-phase contract assertions — already in each phase's `VERIFICATION.md`. This audit deliberately stays at the seams.
- Phase 62's own internal correctness (CR-01, CR-03, CR-04 closures from 62-06..09). Per the prompt the audit takes Phase 62 SUMMARYs at face value.
- Goreleaser CI matrix on a real CI run. Confirmed by reading `.goreleaser.yaml` + Makefile + 58-VERIFICATION cite of run 25310013651.
- Runtime behaviour under actual gRPC traffic (no live daemon was started). All findings are from static analysis of the post-62-09 tree.

---

## Sign-off

The v1.10 milestone wires together at compile time with one HIGH known-degraded path (F-01) that is the documented next-phase deliverable. The codebase compiles cleanly, all three custom vet analyzers pass on the source tree, and the daemon bootstrap composes the seven phases into a coherent errgroup-orchestrated lifecycle.

**Recommended posture for v1.10 ship:** ship with F-01 as a prominent line in CHANGELOG.md / KNOWN-LIMITATIONS.md. F-03 (CI gap on custom analyzers) should be closed before tagging — it is a one-PR mechanical fix and the cost of leaving it open compounds with every future phase that lands a kernel⊥semantic boundary import.
