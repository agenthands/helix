---
milestone: v1.12
milestone_name: Bench Stack & Tool Evaluation
audited: 2026-06-21
status: tech_debt
reason: all-phases-complete-and-verified; deferred live-execution items warrant a conscious accept
scope:
  phases_total: 15        # 75-89
  phases_complete: 15
  phases_remaining: 0
  requirements_total: 63
scores:
  requirements: 63/63
  phases: 15/15
  integration: sound      # 0 wiring breaks; 39/39 bench+cmd packages green uncached; 7/7 E2E seams wired
  flows: complete         # matrix->cell->runners->evaluators->aggregator->report wired end-to-end
nyquist:
  compliant_phases: 11
  partial_phases: 4       # 77, 79, 80, 81 (nyquist_compliant:false; all VERIFICATION passed)
  missing_phases: 0
  overall: partial
gaps: {}                  # No unsatisfied requirements; no critical blockers; no integration breaks
tech_debt:
  - phase: "external benchmarks (83-88) — live execution"
    items:
      - "Live external-benchmark EXECUTION is environment-gated and was never run in this milestone: SWE-bench Verified (P87), Multi-SWE-bench Mini (P88), Terminal-Bench 2.0 (P88), Aider Polyglot (P85), CrossCodeEval + RepoBench (P86), baseline_rag live embedding (P83). Each needs Docker / the upstream python harness / the tb-or-harbor CLI / network / full per-language toolchains — none present in this dev environment. Every adapter/scorer/gate's DECISION LOGIC is hermetically proven against committed fixtures; only the live numbers are deferred to an operator run on a provisioned host."
      - "The milestone HEADLINE CLAIM ('same model + same budget — with Helix the agent solves more tasks, fewer tokens, fewer files read, fewer destructive edits') has NO live numbers yet — it requires a real provisioned benchmark run. The stack to produce + publish those numbers is complete and verified; producing them is the operator's post-merge step."
  - phase: "84 / 87 / 88 — deferred-to-live upstream contracts"
    items:
      - "cosign-signed GHCR mirror namespace ghcr.io/agenthands/helix-bench-* (P84) is unpublished; UTBoost pin rev + report log-dir + run-all-tests mechanism (P87 A1-A5); Multi-SWE config field spellings + Mini-set HF repo id + tb-vs-harbor binary (P88 A1-A7). Pins are SHA-shaped placeholders documented in-code; confirm against upstream on a networked host."
      - "Multi-SWE-bench FULL set (1,632 instances) is the reach goal — deferred to v1.13 (CC0 license clear; Mini set ships now)."
  - phase: "89 — CI policy"
    items:
      - "INFRA-04 live CI execution (make bench-quick <=5min on a real PR; full make bench nightly/maintainer-gated) is inspection-gated; bench.yml structure is hermetically YAML-parse-tested; live runner execution verified post-merge by opening a PR."
  - phase: "nyquist (77, 79, 80, 81)"
    items:
      - "4 phases carry nyquist_compliant:false in VALIDATION.md (all VERIFICATION passed). Pre-existing early-milestone partials. Optional: /gsd-validate-phase 77|79|80|81 before archive."
  - phase: "80 — carryover"
    items:
      - "TestRunSubcommandWiresDeltaPass (Phase 80-05 RED whose GREEN never landed) — single-run delta-pass wiring follow-up tracked since Phase 82; not a v1.12 requirement gate."
---

# Milestone v1.12 — Bench Stack & Tool Evaluation — Audit (complete-milestone)

**Status:** tech_debt (no blockers; deferred live-execution items warrant a conscious accept before archive)
**Audited:** 2026-06-21 — supersedes the 2026-06-18 partial audit (which ran at 5/15 phases)

## Summary

All 15 phases (75–89) shipped and verified — every `*-VERIFICATION.md` is `status: passed`. All 63 requirements
are satisfied (checklist `[x]` + phase VERIFICATION passed + traceability reconciled to Complete; no orphans, no
unsatisfied). Cross-phase integration is **SOUND**: the integration checker found 0 wiring breaks, confirmed 39/39
bench/cmd packages green uncached, and verified all 7 E2E seams wired (matrix→cell→runners→evaluators→aggregator→report).

The milestone delivered the bench **STACK** in full and verified; what remains is the live **EVALUATION** — every
external-benchmark execution and the headline numbers are environment-gated and were never run here. Each adapter/
scorer/gate's decision logic is hermetically proven against committed fixtures; the live runs are the operator's
post-merge step on a provisioned host. That is the substance of the `tech_debt` classification: no requirement is
unmet, but a material, milestone-defining body of deferred live work should be consciously accepted before archive.

## Requirements — 63/63 satisfied
3-source cross-reference (REQUIREMENTS `[x]` + phase VERIFICATION `passed` + reconciled traceability table) shows
all 63 REQ-IDs satisfied. FAIL gate does not fire (0 unsatisfied, 0 orphaned).

## Phases — 15/15 passed (75–89 all VERIFICATION: passed)

## Integration — SOUND (0 findings)
7/7 E2E seams WIRED; additive open-provenance keys coexist with no v2→v3 bump (Validate gate green); task_success
vs verified_correctness independent; single shared `renderAll` for `aggregate` and `report --run-id`; all 6 ablation
modes + external adapters converge on one result.v2→aggregator→report pipeline. Build/vet/test/make-vet all green.

## Nyquist — partial (11 compliant / 4 partial / 0 missing); partials 77/79/80/81 (all VERIFICATION passed)

## Tech Debt / Deferred
See frontmatter for the structured list. Every item is a ROADMAP-sanctioned environment boundary or explicit v1.13
deferral, recorded honestly throughout the milestone — surfaced here so v1.12 is archived with eyes open.
