---
milestone: v2.0
audited: 2026-06-22
status: tech_debt
scores:
  requirements: 31/31
  phases: 6/6
  integration: clean
  flows: 4/4
gaps:
  requirements: []   # none — all 31 satisfied (3-source cross-referenced)
  integration: []    # none — integration checker: INTEGRATES CLEANLY
  flows: []          # none — all 4 cross-phase E2E flows WIRED
nyquist:
  compliant_phases: [90, 91, 92]
  partial_phases: [93, 94, 95]   # VALIDATION.md exists, nyquist_compliant:false (Wave-0 items unchecked) — discovery-only, non-blocking
  missing_phases: []
  overall: partial
tech_debt:
  - phase: 94-retire-the-agent-facing-mcp-surface-delete
    items:
      - "Follow-up: validateAdminAddr has the same empty-host/wildcard gap that CR-01 fixed in validateGRPCAddr (instrumentation-only admin listener; lower severity; documented in 94-REVIEW-FIX.md as a recommended follow-up)."
      - "IN-01: mergeJSONConfig is now dead production code after the Phase-93 setup flip (left to avoid entangling the head deletion; safe to remove)."
  - phase: 93-skill-md-nudge-repurpose-helix-setup-flip
    items:
      - "IN-02: the nudge classifier can count a grep *pattern* that looks like a code path as a file operand (advisory hook, exit-0 fail-open — cosmetic over-trigger only)."
  - phase: 95-identity-docs-rewrite-docgen-regen
    items:
      - "IN-01: the generated get_tool_help row says 'any MCP tool' — needs a source change + docgen regen (cannot hand-edit under the drift gate)."
  - phase: cross-milestone
    items:
      - "Nyquist: phases 93/94/95 VALIDATION.md carry nyquist_compliant:false (Wave-0 checkboxes unflipped). Phases passed verification; the flag is a coverage formality. Run /gsd-validate-phase 93|94|95 to close retroactively if desired."
      - "Pre-existing (carried, NOT v2.0): cmd/helix-bench TestRunSubcommandWires* require `helix` on PATH + a fully-functional bench agent/dataset env (the v1.12 'live execution honestly gated-skip' item); verified failing identically on the pre-v2.0 baseline."
---

# v2.0 Milestone Audit — CLI-First — MCP Surface Retirement

**Verdict:** Definition of Done MET. All 31 requirements satisfied, all 6 phases passed verification, cross-phase integration verified clean, all 4 E2E flows wired. Status is `tech_debt` (NOT `gaps_found`) — there are **zero blockers**; the classification reflects accumulated non-blocking deferred items + Nyquist-coverage formalities that warrant a review decision before archival.

## Requirements Coverage — 31/31 satisfied

3-source cross-referenced (REQUIREMENTS.md traceability `[x]` + phase VERIFICATION.md + SUMMARY frontmatter). All groups complete:

| Group | IDs | Phase(s) | Status |
|-------|-----|----------|--------|
| CLI | CLI-01..04 | 90 | ✅ satisfied |
| VERB | VERB-01..04 | 91 | ✅ satisfied |
| OUT | OUT-01..07 | 92 | ✅ satisfied |
| SEC | SEC-01..02 | 91 | ✅ satisfied |
| SKILL | SKILL-01..04 | 93 | ✅ satisfied |
| RETIRE | RETIRE-01..04 | 94 | ✅ satisfied |
| DOCS | DOCS-01..03 | 95 | ✅ satisfied |
| TEST | TEST-01..03 | 90/92/93 | ✅ satisfied |

Orphan check: none — every REQ-ID participates in ≥1 verified integration touchpoint.

## Phases — 6/6 passed

| Phase | Verification | Nyquist |
|-------|-------------|---------|
| 90 CLI one-shot dial spine + race-free warm reuse | passed | compliant |
| 91 Code-generated verb surface + tools/call enforcement | passed | compliant |
| 92 Terse output renderer + re-targeted contract oracle | passed | compliant |
| 93 SKILL.md + nudge repurpose + helix setup flip | passed | partial |
| 94 Retire the agent-facing MCP surface (DELETE) | passed | partial |
| 95 Identity & docs rewrite + docgen regen | passed | partial |

## Integration — INTEGRATES CLEANLY

`gsd-integration-checker` verified all four cross-phase E2E flows WIRED, zero orphaned/missing seams:
1. spine→verb→profile/mode-enforcement→terse-output over the retained gRPC `StreamMCP` wire (survived the head deletion).
2. Frozen verb surface consistent across 3 consumers — `verbs_gen.go` ↔ SKILL.md decision table ↔ docgen README table + CLAUDE.md routing matrix (drift-gated by `helix-cligen --check`, the SKILL membership test, and `docgen --check`).
3. CLI is the sole agent surface post-94; loopback-gated gRPC TCP dispatches into the identical `mcpServer.SDK()` with all middlewares.
4. install→steer→use: `helix setup` flip + nudge + SKILL.md cite the real frozen verbs.

One informational (non-defect) note: docgen/cligen/daemon blank-import lists are intentionally non-identical (health/help non-blank in daemon, guardrails tool-neutral, D-02 forbids double-importing extract/golang) — the enumerated tool SET is identical and gate-enforced.

## Tech Debt (non-blocking — see frontmatter for the structured list)

- **Security follow-up (Phase 94):** `validateAdminAddr` shares the empty-host/wildcard gap that CR-01 fixed in `validateGRPCAddr`. Lower severity (admin listener exposes instrumentation only, not the tool surface), explicitly documented for follow-up.
- **Dead code (Phase 94):** `mergeJSONConfig` unreferenced after the setup flip.
- **Cosmetic (Phase 93):** nudge classifier pattern-vs-path edge case (advisory, fail-open).
- **Generated row (Phase 95):** `get_tool_help` description "any MCP tool" needs a source change + regen.
- **Nyquist coverage:** 93/94/95 VALIDATION.md `nyquist_compliant:false` (discovery-only; `/gsd-validate-phase` can close retroactively).

## Pre-existing / environmental (NOT v2.0 regressions)

- `cmd/helix-bench` `TestRunSubcommandWires*` need `helix` on PATH + a full bench env; verified failing identically on the pre-v2.0 baseline (`ceeec1a9`) — the v1.12 "live execution honestly gated-skip" deferred item.
