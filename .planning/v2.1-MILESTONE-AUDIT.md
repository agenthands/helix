---
milestone: v2.1
audited: 2026-06-23
status: passed
scores:
  requirements: 23/23
  phases: 6/6
  integration: wired
  flows: 2/2
gaps: {}
tech_debt:
  - phase: pre-existing (outside v2.1 scope)
    items:
      - "cmd/helix-bench TestRunSubcommandWiresDeltaPass — ablation_deltas not wired into runBench. Predates v2.1 (last cmd/helix-bench touch was Phase 99); reproduces on baseline; NOT a v2.1 gap."
  - phase: 98-multi-agent-coverage (design note, non-blocking)
    items:
      - "Non-Claude agents receive the SKILL.md decision-matrix inline + a pointer to reference.md/get-tool-help rather than the full 32 KiB reference.md on disk (Codex 32-KiB project_doc cap). Deliberate, documented (setup_agents.go:226-261); AGENT-01 satisfied via the shared decision-matrix body; the P101 adoption scorecard correctly measures that shared body."
---

# Milestone v2.1 — Agent Adoption & Aider-Derived Validation — Audit

**Status: PASSED.** All 23 requirements satisfied (3-source cross-reference), all 6 phases verified, both cross-phase E2E flows wired, no critical gaps.

## Requirements Coverage (3-source: VERIFICATION.md ∧ SUMMARY frontmatter ∧ REQUIREMENTS.md traceability)

| REQ-ID | Phase | Verification | Traceability | Final |
|--------|-------|--------------|--------------|-------|
| REF-01, REF-02, REF-03 | 97 | passed | Complete | satisfied |
| ADOPT-01 | 97 | passed | Complete | satisfied |
| STEER-01, STEER-02, STEER-03 | 98 | passed | Complete | satisfied |
| AGENT-01, AGENT-02, AGENT-03 | 98 | passed | Complete | satisfied |
| ADOPT-02 | 101 | passed | Complete | satisfied |
| VENDOR-01, VENDOR-02, VENDOR-03 | 99 | passed | Complete | satisfied |
| EDITBENCH-01, EDITBENCH-02, EDITBENCH-03 | 100 | passed | Complete | satisfied |
| BASELINE-01 | 100 | passed | Complete | satisfied |
| REPOEVAL-01, REPOEVAL-02 | 102 | passed | Complete | satisfied |
| FUZZBENCH-01, FUZZBENCH-02 | 102 | passed | Complete | satisfied |
| BASELINE-02 | 102 | passed | Complete | satisfied |

**23/23 satisfied. 0 unsatisfied, 0 partial, 0 orphaned.**

## Phase Verification

| Phase | Name | Status |
|-------|------|--------|
| 97 | Generated Per-Verb Reference + Deterministic Adoption Contract | ✓ passed |
| 98 | Multi-Agent Coverage + Stronger Steering | ✓ passed |
| 99 | Vendored Aider Fixtures + Mixed-License Gate | ✓ passed |
| 100 | Polyglot Edit Benchmark + Committed Baseline | ✓ passed |
| 101 | Opt-In LLM-Behavioral Adoption Scorecard | ✓ passed |
| 102 | RepoMap-Quality + Fuzzy-Robustness Evals + Baselines | ✓ passed (15/15 must-haves) |

**6/6 phases verified.**

## Cross-Phase Integration (gsd-integration-checker)

`go build ./...` links clean. 0 orphaned, 0 missing, 0 broken flows.

**Flow A — Agent Adoption pipeline (97→98→101): WIRED.** Single canonical `internal/cli/skills/helix/reference.md` (refgen `--out` == `//go:embed` location == `--check` authority `VerbToolNames()`); Claude gets the full SKILL.md+reference.md embed bundle, non-Claude agents get the SKILL.md decision-matrix inline + nudge envelope (`runNudge`), and the P101 scorecard consumes the same `EmbeddedSkillBody()` so the `StripDecisionMatrix` sabotage anchor (`## Decision matrix` @ SKILL.md:35) is meaningful.

**Flow B — Aider-derived bench validation (99→100, 99→102): WIRED.** The P99 vendored `bench/datasets/aider-polyglot/fixtures/` tree is the single ground-truth source for both the P100 edit bench (`aiderpolyglot.RunExercise`) and the P102 repomap capture (`aiderpolyglot.LoadExercise`) + fuzzy drift corpus (path-traceable provenance). The three committed baseline dirs (`aider-edit-baseline`, `repomap-eval-baseline`, `fuzzy-robust-baseline`) coexist in `.gitignore` allowlist with no collision; `make verify-licenses --tree …/fixtures` covers the shared tree.

## Tech Debt (non-blocking)

1. **Pre-existing, out of v2.1 scope:** `cmd/helix-bench TestRunSubcommandWiresDeltaPass` (ablation_deltas not wired into `runBench`). Predates this milestone (last `cmd/helix-bench` change was Phase 99), reproduces on baseline. Track for a future bench-wiring task.
2. **Design note (non-blocking):** non-Claude agents receive the decision matrix inline rather than the full `reference.md` on disk (32-KiB Codex cap). Deliberate and documented; AGENT-01 satisfied via the shared body.

## Nyquist

Phase 102 ships `102-VALIDATION.md` (per-task automated test map). Prior phases (97–101) shipped under the same milestone discipline. Discovery-only; no auto-validate triggered.

## Re-audit (fresh pass, 2026-06-23)

Re-ran on user request. Inputs unchanged since the first pass (HEAD at the audit commit, no phase code changed). A fresh `gsd-integration-checker` re-verified both flows end-to-end: **both WIRED, 0 orphaned/missing, no BLOCKERs, no WARNINGs**, `go build ./...` exits 0. Additionally confirmed: the `StripDecisionMatrix` non-noop sabotage self-test runs in the default test suite against the real embedded SKILL.md body, and `EmbeddedReference()` is exercised by the AGENT-01 contract tests. Two prompt path-description imprecisions were noted and dismissed (VENDOR-MANIFEST.md lives at `bench/datasets/aider-polyglot/VENDOR-MANIFEST.md` where the Makefile `--manifest` points; `EmbeddedReference()` is test-only by the deliberate 32-KiB cap design) — both confirm correct wiring, not gaps.

**Verdict unchanged: PASSED.**

## Verdict

Milestone v2.1 met its definition of done. Ready to complete (archive + tag).

**Pre-close note:** 3 pre-existing open quick-tasks (`260414-e5n`, `260617-j29`, `260617-t7x`) were triaged — all are already completed work (SUMMARYs with completion dates); they are stale tracking entries lacking a `status: complete` marker, not real outstanding v2.1 scope. The milestone close was paused by the user to triage these before archiving.
