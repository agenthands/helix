#!/usr/bin/env bash
set -euo pipefail

# verify.sh for go-delete-003 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

go vet ./...
if grep -qw "DeprecatedFormat" main.go; then
  echo "FAIL: DeprecatedFormat still present in main.go" >&2
  exit 1
fi

echo "PASS: go-delete-003"
