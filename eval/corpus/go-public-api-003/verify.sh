#!/usr/bin/env bash
set -euo pipefail

# verify.sh for go-public-api-003 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

go vet ./...
grep -q "ValidationError" main.go

echo "PASS: go-public-api-003"
