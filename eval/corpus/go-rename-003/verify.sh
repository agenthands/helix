#!/usr/bin/env bash
set -euo pipefail

# verify.sh for go-rename-003 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

go vet ./...
grep -qw "Send" main.go

if grep -qw "Dispatch" main.go; then
  echo "FAIL: Dispatch still present in main.go" >&2
  exit 1
fi

echo "PASS: go-rename-003"
