The function `RunQuery` constructs a SQL query using string concatenation,
creating a SQL injection vulnerability. Rewrite it to use parameterised queries.
After the edit, use verify_edit and get_diagnostics to confirm no regressions.
