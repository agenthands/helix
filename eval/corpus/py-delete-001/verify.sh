#!/usr/bin/env bash
set -euo pipefail

# verify.sh for py-delete-001 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

python3 -c "import py_compile; py_compile.compile('main.py')"
if grep -qw "_legacy_helper" main.py; then
  echo "FAIL: _legacy_helper still present in main.py" >&2
  exit 1
fi

echo "PASS: py-delete-001"
