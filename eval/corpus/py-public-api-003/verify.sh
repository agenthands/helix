#!/usr/bin/env bash
set -euo pipefail

# verify.sh for py-public-api-003 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

python3 -c "import py_compile; py_compile.compile('main.py')"
grep -q "bonus: int = 10" main.py

echo "PASS: py-public-api-003"
