#!/usr/bin/env bash
set -euo pipefail

# verify.sh for py-rename-002 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

python3 -c "import py_compile; py_compile.compile('main.py')"
grep -qw "DataReader" main.py

if grep -qw "DataLoader" main.py; then
  echo "FAIL: DataLoader still present in main.py" >&2
  exit 1
fi

echo "PASS: py-rename-002"
