#!/usr/bin/env bash
set -euo pipefail

# verify.sh for py-rename-003 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

python3 -c "import py_compile; py_compile.compile('main.py')"
grep -qw "MAX_BUFFER_BYTES" main.py

if grep -qw "MAX_BUFFER" main.py; then
  echo "FAIL: MAX_BUFFER still present in main.py" >&2
  exit 1
fi

echo "PASS: py-rename-003"
