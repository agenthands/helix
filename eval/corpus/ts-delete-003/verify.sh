#!/usr/bin/env bash
set -euo pipefail

# verify.sh for ts-delete-003 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

if grep -qw "deadBranchHandler" index.ts; then
  echo "FAIL: deadBranchHandler still present in index.ts" >&2
  exit 1
fi

echo "PASS: ts-delete-003"
