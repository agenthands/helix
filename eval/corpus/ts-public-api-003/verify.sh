#!/usr/bin/env bash
set -euo pipefail

# verify.sh for ts-public-api-003 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

grep -q "opts" index.ts

echo "PASS: ts-public-api-003"
