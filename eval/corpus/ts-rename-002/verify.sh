#!/usr/bin/env bash
set -euo pipefail

# verify.sh for ts-rename-002 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

grep -qw "serializeMessage" index.ts

if grep -qw "encodeMessage" index.ts; then
  echo "FAIL: encodeMessage still present in index.ts" >&2
  exit 1
fi

echo "PASS: ts-rename-002"
