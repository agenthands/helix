#!/usr/bin/env bash
set -euo pipefail

# verify.sh for ts-rename-003 — generated by eval/gen.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/repo"

grep -qw "DEFAULT_TIMEOUT_MS" index.ts

if grep -qw "DEFAULT_TIMEOUT" index.ts; then
  echo "FAIL: DEFAULT_TIMEOUT still present in index.ts" >&2
  exit 1
fi

echo "PASS: ts-rename-003"
