; OCaml body extraction query
; OCaml value_definition -> let_binding has a "body" field.
; However, extractNodeName cannot resolve let_binding name without custom logic.
; Body extraction support is limited; may fall back to LSP-based editing.
