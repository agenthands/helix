// Code generated by cmd/lspgen from LSP metaModel.json version 3.17.0. DO NOT EDIT.

package gen

// ServerInterface defines the methods a server must implement.
type ServerInterface interface {
	// A request to resolve the implementation locations of a symbol at a given text
	// document position. The request's parameter is of type {@link TextDocumentPositionParams}
	// the response is of type {@link Definition} or a Thenable that resolves to such.
	TextDocumentImplementation(params *ImplementationParams) (*Or_Definition_DefinitionLinkArray_Null, error)
	// A request to resolve the type definition locations of a symbol at a given text
	// document position. The request's parameter is of type {@link TextDocumentPositionParams}
	// the response is of type {@link Definition} or a Thenable that resolves to such.
	TextDocumentTypeDefinition(params *TypeDefinitionParams) (*Or_Definition_DefinitionLinkArray_Null, error)
	// A request to list all color symbols found in a given text document. The request's
	// parameter is of type {@link DocumentColorParams} the
	// response is of type {@link ColorInformation ColorInformation[]} or a Thenable
	// that resolves to such.
	TextDocumentDocumentColor(params *DocumentColorParams) (*[]ColorInformation, error)
	// A request to list all presentation for a color. The request's
	// parameter is of type {@link ColorPresentationParams} the
	// response is of type {@link ColorInformation ColorInformation[]} or a Thenable
	// that resolves to such.
	TextDocumentColorPresentation(params *ColorPresentationParams) (*[]ColorPresentation, error)
	// A request to provide folding ranges in a document. The request's
	// parameter is of type {@link FoldingRangeParams}, the
	// response is of type {@link FoldingRangeList} or a Thenable
	// that resolves to such.
	TextDocumentFoldingRange(params *FoldingRangeParams) (*[]FoldingRange, error)
	// A request to resolve the type definition locations of a symbol at a given text
	// document position. The request's parameter is of type {@link TextDocumentPositionParams}
	// the response is of type {@link Declaration} or a typed array of {@link DeclarationLink}
	// or a Thenable that resolves to such.
	TextDocumentDeclaration(params *DeclarationParams) (*Or_Declaration_DeclarationLinkArray_Null, error)
	// A request to provide selection ranges in a document. The request's
	// parameter is of type {@link SelectionRangeParams}, the
	// response is of type {@link SelectionRange SelectionRange[]} or a Thenable
	// that resolves to such.
	TextDocumentSelectionRange(params *SelectionRangeParams) (*[]SelectionRange, error)
	// A request to result a `CallHierarchyItem` in a document at a given position.
	// Can be used as an input to an incoming or outgoing call hierarchy.
	//
	// @since 3.16.0
	TextDocumentPrepareCallHierarchy(params *CallHierarchyPrepareParams) (*[]CallHierarchyItem, error)
	// A request to resolve the incoming calls for a given `CallHierarchyItem`.
	//
	// @since 3.16.0
	CallHierarchyIncomingCalls(params *CallHierarchyIncomingCallsParams) (*[]CallHierarchyIncomingCall, error)
	// A request to resolve the outgoing calls for a given `CallHierarchyItem`.
	//
	// @since 3.16.0
	CallHierarchyOutgoingCalls(params *CallHierarchyOutgoingCallsParams) (*[]CallHierarchyOutgoingCall, error)
	// @since 3.16.0
	TextDocumentSemanticTokensFull(params *SemanticTokensParams) (*SemanticTokens, error)
	// @since 3.16.0
	TextDocumentSemanticTokensFullDelta(params *SemanticTokensDeltaParams) (*Or_Null_SemanticTokens_SemanticTokensDelta, error)
	// @since 3.16.0
	TextDocumentSemanticTokensRange(params *SemanticTokensRangeParams) (*SemanticTokens, error)
	// A request to provide ranges that can be edited together.
	//
	// @since 3.16.0
	TextDocumentLinkedEditingRange(params *LinkedEditingRangeParams) (*LinkedEditingRanges, error)
	// The will create files request is sent from the client to the server before files are actually
	// created as long as the creation is triggered from within the client.
	//
	// The request can return a `WorkspaceEdit` which will be applied to workspace before the
	// files are created. Hence the `WorkspaceEdit` can not manipulate the content of the file
	// to be created.
	//
	// @since 3.16.0
	WorkspaceWillCreateFiles(params *CreateFilesParams) (*WorkspaceEdit, error)
	// The will rename files request is sent from the client to the server before files are actually
	// renamed as long as the rename is triggered from within the client.
	//
	// @since 3.16.0
	WorkspaceWillRenameFiles(params *RenameFilesParams) (*WorkspaceEdit, error)
	// The did delete files notification is sent from the client to the server when
	// files were deleted from within the client.
	//
	// @since 3.16.0
	WorkspaceWillDeleteFiles(params *DeleteFilesParams) (*WorkspaceEdit, error)
	// A request to get the moniker of a symbol at a given text document position.
	// The request parameter is of type {@link TextDocumentPositionParams}.
	// The response is of type {@link Moniker Moniker[]} or `null`.
	TextDocumentMoniker(params *MonikerParams) (*[]Moniker, error)
	// A request to result a `TypeHierarchyItem` in a document at a given position.
	// Can be used as an input to a subtypes or supertypes type hierarchy.
	//
	// @since 3.17.0
	TextDocumentPrepareTypeHierarchy(params *TypeHierarchyPrepareParams) (*[]TypeHierarchyItem, error)
	// A request to resolve the supertypes for a given `TypeHierarchyItem`.
	//
	// @since 3.17.0
	TypeHierarchySupertypes(params *TypeHierarchySupertypesParams) (*[]TypeHierarchyItem, error)
	// A request to resolve the subtypes for a given `TypeHierarchyItem`.
	//
	// @since 3.17.0
	TypeHierarchySubtypes(params *TypeHierarchySubtypesParams) (*[]TypeHierarchyItem, error)
	// A request to provide inline values in a document. The request's parameter is of
	// type {@link InlineValueParams}, the response is of type
	// {@link InlineValue InlineValue[]} or a Thenable that resolves to such.
	//
	// @since 3.17.0
	TextDocumentInlineValue(params *InlineValueParams) (*[]InlineValue, error)
	// A request to provide inlay hints in a document. The request's parameter is of
	// type {@link InlayHintsParams}, the response is of type
	// {@link InlayHint InlayHint[]} or a Thenable that resolves to such.
	//
	// @since 3.17.0
	TextDocumentInlayHint(params *InlayHintParams) (*[]InlayHint, error)
	// A request to resolve additional properties for an inlay hint.
	// The request's parameter is of type {@link InlayHint}, the response is
	// of type {@link InlayHint} or a Thenable that resolves to such.
	//
	// @since 3.17.0
	InlayHintResolve(params *InlayHint) (*InlayHint, error)
	// The document diagnostic request definition.
	//
	// @since 3.17.0
	TextDocumentDiagnostic(params *DocumentDiagnosticParams) (*DocumentDiagnosticReport, error)
	// The workspace diagnostic request definition.
	//
	// @since 3.17.0
	WorkspaceDiagnostic(params *WorkspaceDiagnosticParams) (*WorkspaceDiagnosticReport, error)
	// A request to provide inline completions in a document. The request's parameter is of
	// type {@link InlineCompletionParams}, the response is of type
	// {@link InlineCompletion InlineCompletion[]} or a Thenable that resolves to such.
	//
	// @since 3.18.0
	// @proposed
	TextDocumentInlineCompletion(params *InlineCompletionParams) (*Or_InlineCompletionItemArray_InlineCompletionList_Null, error)
	// The initialize request is sent from the client to the server.
	// It is sent once as the request after starting up the server.
	// The requests parameter is of type {@link InitializeParams}
	// the response if of type {@link InitializeResult} of a Thenable that
	// resolves to such.
	Initialize(params *InitializeParams) (*InitializeResult, error)
	// A shutdown request is sent from the client to the server.
	// It is sent once when the client decides to shutdown the
	// server. The only notification that is sent after a shutdown request
	// is the exit event.
	Shutdown() (*interface{}, error)
	// A document will save request is sent from the client to the server before
	// the document is actually saved. The request can return an array of TextEdits
	// which will be applied to the text document before it is saved. Please note that
	// clients might drop results if computing the text edits took too long or if a
	// server constantly fails on this request. This is done to keep the save fast and
	// reliable.
	TextDocumentWillSaveWaitUntil(params *WillSaveTextDocumentParams) (*[]TextEdit, error)
	// Request to request completion at a given text document position. The request's
	// parameter is of type {@link TextDocumentPosition} the response
	// is of type {@link CompletionItem CompletionItem[]} or {@link CompletionList}
	// or a Thenable that resolves to such.
	//
	// The request can delay the computation of the {@link CompletionItem.detail `detail`}
	// and {@link CompletionItem.documentation `documentation`} properties to the `completionItem/resolve`
	// request. However, properties that are needed for the initial sorting and filtering, like `sortText`,
	// `filterText`, `insertText`, and `textEdit`, must not be changed during resolve.
	TextDocumentCompletion(params *CompletionParams) (*Or_CompletionItemArray_CompletionList_Null, error)
	// Request to resolve additional information for a given completion item.The request's
	// parameter is of type {@link CompletionItem} the response
	// is of type {@link CompletionItem} or a Thenable that resolves to such.
	CompletionItemResolve(params *CompletionItem) (*CompletionItem, error)
	// Request to request hover information at a given text document position. The request's
	// parameter is of type {@link TextDocumentPosition} the response is of
	// type {@link Hover} or a Thenable that resolves to such.
	TextDocumentHover(params *HoverParams) (*Hover, error)
	TextDocumentSignatureHelp(params *SignatureHelpParams) (*SignatureHelp, error)
	// A request to resolve the definition location of a symbol at a given text
	// document position. The request's parameter is of type {@link TextDocumentPosition}
	// the response is of either type {@link Definition} or a typed array of
	// {@link DefinitionLink} or a Thenable that resolves to such.
	TextDocumentDefinition(params *DefinitionParams) (*Or_Definition_DefinitionLinkArray_Null, error)
	// A request to resolve project-wide references for the symbol denoted
	// by the given text document position. The request's parameter is of
	// type {@link ReferenceParams} the response is of type
	// {@link Location Location[]} or a Thenable that resolves to such.
	TextDocumentReferences(params *ReferenceParams) (*[]Location, error)
	// Request to resolve a {@link DocumentHighlight} for a given
	// text document position. The request's parameter is of type {@link TextDocumentPosition}
	// the request response is an array of type {@link DocumentHighlight}
	// or a Thenable that resolves to such.
	TextDocumentDocumentHighlight(params *DocumentHighlightParams) (*[]DocumentHighlight, error)
	// A request to list all symbols found in a given text document. The request's
	// parameter is of type {@link TextDocumentIdentifier} the
	// response is of type {@link SymbolInformation SymbolInformation[]} or a Thenable
	// that resolves to such.
	TextDocumentDocumentSymbol(params *DocumentSymbolParams) (*Or_DocumentSymbolArray_Null_SymbolInformationArray, error)
	// A request to provide commands for the given text document and range.
	TextDocumentCodeAction(params *CodeActionParams) (*[]Or_CodeAction_Command, error)
	// Request to resolve additional information for a given code action.The request's
	// parameter is of type {@link CodeAction} the response
	// is of type {@link CodeAction} or a Thenable that resolves to such.
	CodeActionResolve(params *CodeAction) (*CodeAction, error)
	// A request to list project-wide symbols matching the query string given
	// by the {@link WorkspaceSymbolParams}. The response is
	// of type {@link SymbolInformation SymbolInformation[]} or a Thenable that
	// resolves to such.
	//
	// @since 3.17.0 - support for WorkspaceSymbol in the returned data. Clients
	//  need to advertise support for WorkspaceSymbols via the client capability
	//  `workspace.symbol.resolveSupport`.
	//
	WorkspaceSymbol(params *WorkspaceSymbolParams) (*Or_Null_SymbolInformationArray_WorkspaceSymbolArray, error)
	// A request to resolve the range inside the workspace
	// symbol's location.
	//
	// @since 3.17.0
	WorkspaceSymbolResolve(params *WorkspaceSymbol) (*WorkspaceSymbol, error)
	// A request to provide code lens for the given text document.
	TextDocumentCodeLens(params *CodeLensParams) (*[]CodeLens, error)
	// A request to resolve a command for a given code lens.
	CodeLensResolve(params *CodeLens) (*CodeLens, error)
	// A request to provide document links
	TextDocumentDocumentLink(params *DocumentLinkParams) (*[]DocumentLink, error)
	// Request to resolve additional information for a given document link. The request's
	// parameter is of type {@link DocumentLink} the response
	// is of type {@link DocumentLink} or a Thenable that resolves to such.
	DocumentLinkResolve(params *DocumentLink) (*DocumentLink, error)
	// A request to format a whole document.
	TextDocumentFormatting(params *DocumentFormattingParams) (*[]TextEdit, error)
	// A request to format a range in a document.
	TextDocumentRangeFormatting(params *DocumentRangeFormattingParams) (*[]TextEdit, error)
	// A request to format ranges in a document.
	//
	// @since 3.18.0
	// @proposed
	TextDocumentRangesFormatting(params *DocumentRangesFormattingParams) (*[]TextEdit, error)
	// A request to format a document on type.
	TextDocumentOnTypeFormatting(params *DocumentOnTypeFormattingParams) (*[]TextEdit, error)
	// A request to rename a symbol.
	TextDocumentRename(params *RenameParams) (*WorkspaceEdit, error)
	// A request to test and perform the setup necessary for a rename.
	//
	// @since 3.16 - support for default behavior
	TextDocumentPrepareRename(params *PrepareRenameParams) (*PrepareRenameResult, error)
	// A request send from the client to the server to execute a command. The request might return
	// a workspace edit which the client will apply to the workspace.
	WorkspaceExecuteCommand(params *ExecuteCommandParams) (*interface{}, error)
}

// LSP notification method constants.
const (
	MethodNotificationWorkspaceDidChangeWorkspaceFolders = "workspace/didChangeWorkspaceFolders"
	MethodNotificationWindowWorkDoneProgressCancel       = "window/workDoneProgress/cancel"
	MethodNotificationWorkspaceDidCreateFiles            = "workspace/didCreateFiles"
	MethodNotificationWorkspaceDidRenameFiles            = "workspace/didRenameFiles"
	MethodNotificationWorkspaceDidDeleteFiles            = "workspace/didDeleteFiles"
	MethodNotificationNotebookDocumentDidOpen            = "notebookDocument/didOpen"
	MethodNotificationNotebookDocumentDidChange          = "notebookDocument/didChange"
	MethodNotificationNotebookDocumentDidSave            = "notebookDocument/didSave"
	MethodNotificationNotebookDocumentDidClose           = "notebookDocument/didClose"
	MethodNotificationInitialized                        = "initialized"
	MethodNotificationExit                               = "exit"
	MethodNotificationWorkspaceDidChangeConfiguration    = "workspace/didChangeConfiguration"
	MethodNotificationWindowShowMessage                  = "window/showMessage"
	MethodNotificationWindowLogMessage                   = "window/logMessage"
	MethodNotificationTelemetryEvent                     = "telemetry/event"
	MethodNotificationTextDocumentDidOpen                = "textDocument/didOpen"
	MethodNotificationTextDocumentDidChange              = "textDocument/didChange"
	MethodNotificationTextDocumentDidClose               = "textDocument/didClose"
	MethodNotificationTextDocumentDidSave                = "textDocument/didSave"
	MethodNotificationTextDocumentWillSave               = "textDocument/willSave"
	MethodNotificationWorkspaceDidChangeWatchedFiles     = "workspace/didChangeWatchedFiles"
	MethodNotificationTextDocumentPublishDiagnostics     = "textDocument/publishDiagnostics"
	MethodNotificationSetTrace                           = "$/setTrace"
	MethodNotificationLogTrace                           = "$/logTrace"
	MethodNotificationCancelRequest                      = "$/cancelRequest"
	MethodNotificationProgress                           = "$/progress"
)

// NotificationHandler handles LSP notifications.
type NotificationHandler interface {
	// The `workspace/didChangeWorkspaceFolders` notification is sent from the client to the server when the workspace
	// folder configuration changes.
	WorkspaceDidChangeWorkspaceFolders(params *DidChangeWorkspaceFoldersParams) error
	// The `window/workDoneProgress/cancel` notification is sent from  the client to the server to cancel a progress
	// initiated on the server side.
	WindowWorkDoneProgressCancel(params *WorkDoneProgressCancelParams) error
	// The did create files notification is sent from the client to the server when
	// files were created from within the client.
	//
	// @since 3.16.0
	WorkspaceDidCreateFiles(params *CreateFilesParams) error
	// The did rename files notification is sent from the client to the server when
	// files were renamed from within the client.
	//
	// @since 3.16.0
	WorkspaceDidRenameFiles(params *RenameFilesParams) error
	// The will delete files request is sent from the client to the server before files are actually
	// deleted as long as the deletion is triggered from within the client.
	//
	// @since 3.16.0
	WorkspaceDidDeleteFiles(params *DeleteFilesParams) error
	// A notification sent when a notebook opens.
	//
	// @since 3.17.0
	NotebookDocumentDidOpen(params *DidOpenNotebookDocumentParams) error
	NotebookDocumentDidChange(params *DidChangeNotebookDocumentParams) error
	// A notification sent when a notebook document is saved.
	//
	// @since 3.17.0
	NotebookDocumentDidSave(params *DidSaveNotebookDocumentParams) error
	// A notification sent when a notebook closes.
	//
	// @since 3.17.0
	NotebookDocumentDidClose(params *DidCloseNotebookDocumentParams) error
	// The initialized notification is sent from the client to the
	// server after the client is fully initialized and the server
	// is allowed to send requests from the server to the client.
	Initialized(params *InitializedParams) error
	// The exit event is sent from the client to the server to
	// ask the server to exit its process.
	Exit() error
	// The configuration change notification is sent from the client to the server
	// when the client's configuration has changed. The notification contains
	// the changed configuration as defined by the language client.
	WorkspaceDidChangeConfiguration(params *DidChangeConfigurationParams) error
	// The show message notification is sent from a server to a client to ask
	// the client to display a particular message in the user interface.
	WindowShowMessage(params *ShowMessageParams) error
	// The log message notification is sent from the server to the client to ask
	// the client to log a particular message.
	WindowLogMessage(params *LogMessageParams) error
	// The telemetry event notification is sent from the server to the client to ask
	// the client to log telemetry data.
	TelemetryEvent(params *interface{}) error
	// The document open notification is sent from the client to the server to signal
	// newly opened text documents. The document's truth is now managed by the client
	// and the server must not try to read the document's truth using the document's
	// uri. Open in this sense means it is managed by the client. It doesn't necessarily
	// mean that its content is presented in an editor. An open notification must not
	// be sent more than once without a corresponding close notification send before.
	// This means open and close notification must be balanced and the max open count
	// is one.
	TextDocumentDidOpen(params *DidOpenTextDocumentParams) error
	// The document change notification is sent from the client to the server to signal
	// changes to a text document.
	TextDocumentDidChange(params *DidChangeTextDocumentParams) error
	// The document close notification is sent from the client to the server when
	// the document got closed in the client. The document's truth now exists where
	// the document's uri points to (e.g. if the document's uri is a file uri the
	// truth now exists on disk). As with the open notification the close notification
	// is about managing the document's content. Receiving a close notification
	// doesn't mean that the document was open in an editor before. A close
	// notification requires a previous open notification to be sent.
	TextDocumentDidClose(params *DidCloseTextDocumentParams) error
	// The document save notification is sent from the client to the server when
	// the document got saved in the client.
	TextDocumentDidSave(params *DidSaveTextDocumentParams) error
	// A document will save notification is sent from the client to the server before
	// the document is actually saved.
	TextDocumentWillSave(params *WillSaveTextDocumentParams) error
	// The watched files notification is sent from the client to the server when
	// the client detects changes to file watched by the language client.
	WorkspaceDidChangeWatchedFiles(params *DidChangeWatchedFilesParams) error
	// Diagnostics notification are sent from the server to the client to signal
	// results of validation runs.
	TextDocumentPublishDiagnostics(params *PublishDiagnosticsParams) error
	SetTrace(params *SetTraceParams) error
	LogTrace(params *LogTraceParams) error
	CancelRequest(params *CancelParams) error
	Progress(params *ProgressParams) error
}

// NotificationDispatch maps method names to dispatch functions.
var NotificationDispatch = map[string]string{
	"workspace/didChangeWorkspaceFolders": "WorkspaceDidChangeWorkspaceFolders",
	"window/workDoneProgress/cancel":      "WindowWorkDoneProgressCancel",
	"workspace/didCreateFiles":            "WorkspaceDidCreateFiles",
	"workspace/didRenameFiles":            "WorkspaceDidRenameFiles",
	"workspace/didDeleteFiles":            "WorkspaceDidDeleteFiles",
	"notebookDocument/didOpen":            "NotebookDocumentDidOpen",
	"notebookDocument/didChange":          "NotebookDocumentDidChange",
	"notebookDocument/didSave":            "NotebookDocumentDidSave",
	"notebookDocument/didClose":           "NotebookDocumentDidClose",
	"initialized":                         "Initialized",
	"exit":                                "Exit",
	"workspace/didChangeConfiguration":    "WorkspaceDidChangeConfiguration",
	"window/showMessage":                  "WindowShowMessage",
	"window/logMessage":                   "WindowLogMessage",
	"telemetry/event":                     "TelemetryEvent",
	"textDocument/didOpen":                "TextDocumentDidOpen",
	"textDocument/didChange":              "TextDocumentDidChange",
	"textDocument/didClose":               "TextDocumentDidClose",
	"textDocument/didSave":                "TextDocumentDidSave",
	"textDocument/willSave":               "TextDocumentWillSave",
	"workspace/didChangeWatchedFiles":     "WorkspaceDidChangeWatchedFiles",
	"textDocument/publishDiagnostics":     "TextDocumentPublishDiagnostics",
	"$/setTrace":                          "SetTrace",
	"$/logTrace":                          "LogTrace",
	"$/cancelRequest":                     "CancelRequest",
	"$/progress":                          "Progress",
}
