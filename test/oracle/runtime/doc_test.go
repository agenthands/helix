//go:build integration || llm || llmjudge

// Package runtime_test contains runtime robustness oracle tests:
// worker pool stress, clean shutdown, and degraded subsystem startup.
package runtime_test
