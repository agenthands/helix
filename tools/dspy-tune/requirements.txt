# Dev-time-only pins for the DSPy offline tuning harness (tools/dspy-tune/).
# NEVER enters the helix binary, go.mod, `helix setup`, or `go test ./...`.
# Installed into a git-ignored dev venv only, ALWAYS via uv (never bare pip):
#   uv venv && uv pip install -r requirements.txt   # then run via: uv run pytest / uv run python optimize.py
#
# dspy: research pinned 3.1.3; bumped to 3.2.1 (the then-current release at plan
# time per `pip index versions dspy`). The GEPA top-level API used by optimize.py
# (from dspy import GEPA; metric (gold,pred,trace,pred_name,pred_trace) ->
# dspy.Prediction(score=,feedback=); optimizer.compile(student,trainset,valset))
# is unchanged across 3.1.x -> 3.2.x.
dspy==3.2.1
# openai: NEW (Phase 107) — the single chat-completions tool-use client for the
# dev-time ReAct agent (tools/dspy-tune/agent/). Wraps BOTH DeepSeek primary
# (base_url=https://api.deepseek.com) and the OpenAI fallback; DeepSeek is
# OpenAI wire-compatible so one SDK covers both providers. Dev venv ONLY —
# never enters go.mod / the helix binary / `helix setup`. The hermetic
# test_agent.py injects duck-typed fakes and constructs no client, so it passes
# even with openai uninstalled.
openai==2.43.0
# swebench: NEW (Phase 109) — the upstream SWE-bench harness invoked as
# `python -m swebench.harness.run_evaluation` by the heaviest task-success
# grader (grade_swebench.py) on Podman (DOCKER_HOST→podman socket). Dev venv
# ONLY — never enters go.mod / the helix binary / `helix setup` / `go test ./...`
# / the merge path. The hermetic test_swebench.py injects a fake harness runner +
# report reader and imports no swebench symbol, so it passes even with swebench
# uninstalled; only the gated live leg needs it (plus a running container engine).
swebench==4.1.0
pytest==8.3.5
